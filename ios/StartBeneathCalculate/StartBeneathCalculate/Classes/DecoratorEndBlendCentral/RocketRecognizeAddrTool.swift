
//: Declare String Begin

/*: "http://" :*/
fileprivate let user_stretchHideUrl:[Character] = ["h","t","t","p",":","/"]
fileprivate let appCloudKey:String = "must"

/*: "https://" :*/
fileprivate let kDragData:[Character] = ["h","t","t","p","s",":","/","/"]

/*: "http:// :*/
fileprivate let dataRelateMessage:[Character] = ["h","t","t","p",":","/","/"]

/*: "serverSelctIndex =  :*/
fileprivate let main_smartRefUrl:[Character] = ["s","e","r","v","e","r","S","e","l"]
fileprivate let app_tellValue:[Character] = ["c","t","I","n","d","e","x"," ","="," "]

/*: "serverUrlStr =  :*/
fileprivate let noti_alleyText:[Character] = ["s","e"]
fileprivate let user_purchaseTitle:String = "blocker"

/*: "app. :*/
fileprivate let constResolutionTireStr:String = "alive diskapp."

/*: .com" :*/
fileprivate let noti_forwardName:String = "visual available since manage.com"

/*: "mf/chatRoom/sendGift" :*/
fileprivate let mainIngredientValue:String = "mf/chanetwork oval shoo calendar"
fileprivate let userCarrierMsg:String = "distantnd"

/*: "recharge/applePayNotify" :*/
fileprivate let noti_todayFormat:[Character] = ["r","e","c","h","a","r","g","e","/","a","p","p","l","e","P","a","y"]
fileprivate let mainLockChangeData:String = "word restoreNotify"

/*: "user/appleLogin" :*/
fileprivate let app_requireValue:[Character] = ["u","s","e","r","/"]
fileprivate let data_languagePhaseMessage:String = "certain commend fit listenerappleL"

/*: "IM/getMyBlackList" :*/
fileprivate let show_totalStr:String = "interest destroyIM/g"
fileprivate let k_absolutelyName:[Character] = ["L","i","s","t"]

/*: "mf/business/sendMsg" :*/
fileprivate let const_opTitle:[Character] = ["m"]
fileprivate let userAspectTitle:String = "f/busipipe spell again proposal pending"
fileprivate let show_acrossId:String = "character formation force literalsendMsg"

/*: "mf/business/sendGift" :*/
fileprivate let user_calendarPath:String = "first three satisfy commentmf/bus"
fileprivate let k_outputMsg:[Character] = ["i"]
fileprivate let showObserverStr:String = "calendar greenness/s"

/*: "mf/chatRoom/sendMsg" :*/
fileprivate let k_whoFormSiblingFormat:[Character] = ["m","f","/","c","h","a","t","R","o"]
fileprivate let appWeightId:[Character] = ["o","m","/","s","e","n","d","M","s","g"]

/*: "GJ21FB" :*/
fileprivate let user_stopWithStr:String = "GJ21Ffound"

/*: "完成了检测,请求状态正常" :*/
fileprivate let constWhiteAgainId:String = "完成了检测,请求状"
fileprivate let user_distantTitle:[Character] = ["\u{6001}","正","常"]

/*: "接口请求失败" :*/
fileprivate let userTireMessage:String = "接口请求失败"

/*: "完成了检测,需要更换server url" :*/
fileprivate let main_wouldMessage:String = "\u{5b8c}\u{6210}了检"
fileprivate let show_easternPath:String = "\u{66f4}换serv"

/*: "HTTPProxy" :*/
fileprivate let userRestrictionMessage:[Character] = ["H"]
fileprivate let k_legalMessage:String = "us table communicateTTPProxy"

/*: "__SCOPED__" :*/
fileprivate let showArrangementRangePath:[Character] = ["_","_","S","C","O","P","E","D","_","_"]

/*: "tap" :*/
fileprivate let constIqTitle:[Character] = ["t","a","p"]

/*: "tun" :*/
fileprivate let mainMarginName:String = "tuscore"

/*: "ipsec" :*/
fileprivate let appSongMessage:[Character] = ["i","p","s","e","c"]

/*: "ppp" :*/
fileprivate let noti_discountStr:[Character] = ["p","p","p"]

/*: "func__updateTalkingCookies =  :*/
fileprivate let dataEvenTitle:[Character] = ["f","u","n","c","_"]
fileprivate let appFoundUrl:[Character] = ["_","u","p","d","a","t","e","T","a","l","k","i","n","g","C","o","o","k","i","e","s"," ","="," "]

/*: "https:// :*/
fileprivate let data_petitionTitle:String = "HTTP"

/*: "func__deleteSameCookiesWithUrl =  :*/
fileprivate let kBlindStr:[UInt8] = [0x8b,0x98,0x83,0x8e,0xb2,0xb2,0x89,0x88,0x81,0x88,0x99,0x88,0xbe,0x8c,0x80,0x88,0xae,0x82,0x82,0x86,0x84,0x88,0x9e,0xba,0x84,0x99,0x85,0xb8,0x9f,0x81,0xcd,0xd0,0xcd]

/*: "UID" :*/
fileprivate let k_triumphKey:[Character] = ["U","I","D"]

/*: "PHPSESSID" :*/
fileprivate let app_facilityId:[Character] = ["P","H","P","S","E","S"]
fileprivate let showEnableceFosterId:[Character] = ["S","I","D"]

/*: "func__saveAllCookies =  :*/
fileprivate let user_videoMsg:[Character] = ["f","u","n","c","_","_","s","a","v","e","A","l","l","C","o","o"]
fileprivate let appManagerText:String = "help identify bounce badkies = "

/*: "func__saveAllCookies  error" :*/
fileprivate let data_lagPageFormat:[Character] = ["f","u"]
fileprivate let kReportedAppFormat:String = "nc__saclass number calculation"
fileprivate let main_cellFormat:String = "lCookilegal angle"
fileprivate let const_betweenFormat:String = "possiblepossibleopossible"

//: Declare String End

// __DEBUG__
// __CLOSE_PRINT__
//
//  RocketRecognizeAddrTool.swift
//  AbroadTalking
//
//  Created by Hemming on 2022/8/23.
//

//: import UIKit
import UIKit
//: import WebKit
import WebKit

//: enum LangType: String {
enum JumpScalarLiteral: String {
    //: case en = "en"
    case en
    //: case ar = "ar"
    case ar
    //: case es = "es"
    case es
    //: case pt = "pt"
    case pt
}

//: public class TalkingRequestAddrTool: NSObject {
public class RocketRecognizeAddrTool: NSObject {
    //: @objc static public let share = TalkingRequestAddrTool()
    @objc public static let share = RocketRecognizeAddrTool()
    //: private override init() {
    override private init() {
        //: super.init()
        super.init()
        //: setUp()
        opposition()
    }

    //: private var timerDispose: Disposable?
    private var timerDispose: Disposable?

    //: var serverSelctIndex = 0
    var serverSelctIndex = 0
    //: var hasFinishCheck   = false
    var hasFinishCheck = false
    //: var hostCheckNum     = 0
    var hostCheckNum = 0

    //: var domainStr: String?
    var domainStr: String? // 用户登录的信息
    //: var _serverUrlStr: String = ""       //
    var _serverUrlStr: String = "" //

    //: @objc public var serverUrlStr: String {
    @objc public var serverUrlStr: String {
        //: get {
        get {
            //: return _serverUrlStr
            return _serverUrlStr
        }
        //: set {
        set {
            //: var newServerUrl: String = ""
            var newServerUrl: String = ""
            //: if newValue.hasPrefix("http://") || newValue.hasPrefix("https://") {
            if newValue.hasPrefix((String(user_stretchHideUrl) + appCloudKey.replacingOccurrences(of: "must", with: "/"))) || newValue.hasPrefix((String(kDragData))) {
                //: newServerUrl = newValue
                newServerUrl = newValue
                //: } else {
            } else {
                //: if Environment_Formal && !Encryption_Request {
                if dataPopText && !const_saltUrl {
                    //: newServerUrl = "http://\(newValue )"
                    newServerUrl = (String(dataRelateMessage)) + "\(newValue)"
                    //: } else {
                } else {
                    //: newServerUrl = "http://\(newValue )"
                    newServerUrl = (String(dataRelateMessage)) + "\(newValue)"
                }
            }
            //: if NSURL.init(string: newServerUrl) == nil {
            if NSURL(string: newServerUrl) == nil {
                //: return
                return
            }
            //: let oldServerUrl: String = _serverUrlStr
            let oldServerUrl: String = _serverUrlStr

            //: if oldServerUrl.isEmptyString || oldServerUrl != newServerUrl {
            if oldServerUrl.isEmptyString || oldServerUrl != newServerUrl {
                //: _serverUrlStr = newServerUrl
                _serverUrlStr = newServerUrl
                //: if oldServerUrl.count > 0 {
                if oldServerUrl.count > 0 {
                    //: let oldCookies: [HTTPCookie] = HTTPCookieStorage.shared.cookies(for: URL.init(string: oldServerUrl)!)!
                    let oldCookies: [HTTPCookie] = HTTPCookieStorage.shared.cookies(for: URL(string: oldServerUrl)!)!
                    //: func__updateTalkingCookies(cookies: oldCookies)
                    listenerBeauty(cookies: oldCookies)
                }
            }
        }
    }

    //: func setUp() {
    func opposition() {
        //: serverSelctIndex = Defaults.integer(forKey: TalkingAddrServerUrlSelectIndexKey)
        serverSelctIndex = mainNameNearbyTitle.integer(forKey: showScreenAcceptTitle)
        //: printLog(message: "serverSelctIndex = \(serverSelctIndex)")
        printLog(message: (String(main_smartRefUrl) + String(app_tellValue)) + "\(serverSelctIndex)")
        //: let serverDomain = func__getInitialServerUrl()
        let serverDomain = teemUrl()
        //: var serverPrefix = "http://"
        var serverPrefix = (String(user_stretchHideUrl) + appCloudKey.replacingOccurrences(of: "must", with: "/"))
        //: if Environment_Formal && !Encryption_Request {
        if dataPopText && !const_saltUrl {
            //: serverPrefix = "http://"
            serverPrefix = (String(user_stretchHideUrl) + appCloudKey.replacingOccurrences(of: "must", with: "/"))
        }
        //: serverUrlStr = serverPrefix.appending(serverDomain)
        serverUrlStr = serverPrefix.appending(serverDomain)
        //: printLog(message: "serverUrlStr = \(serverUrlStr)")
        printLog(message: (String(noti_alleyText) + user_purchaseTitle.replacingOccurrences(of: "block", with: "rv") + "UrlStr = ") + "\(serverUrlStr)")
    }

    //: func func__getInitialServerUrl() -> String {
    func teemUrl() -> String {
        //: var appServerUrl = ""
        var appServerUrl = ""
        //: appServerUrl = serverUrlArr[serverSelctIndex]
        appServerUrl = serverUrlArr[serverSelctIndex]
        //: return appServerUrl
        return appServerUrl
    }

    //: func func__saveNewTalkingServerUrl(serverStr: String) {
    func chronologicalAge(serverStr: String) {
        //: serverUrlStr = serverStr
        serverUrlStr = serverStr
    }

    //: private lazy var serverUrlArr: Array = {
    private lazy var serverUrlArr: Array = //: return ["app.\(ReplaceUrlDomain).com", "app.\(ReplaceUrlDomain).com"]
        [(String(constResolutionTireStr.suffix(4))) + "\(app_frameName)" + (String(noti_forwardName.suffix(4)))]
    //: }()

    //: lazy var slowPathArr: Array = {
    lazy var slowPathArr: Array = //: return ["mf/chatRoom/sendGift",
        [(String(mainIngredientValue.prefix(6)) + "tRoom/" + userCarrierMsg.replacingOccurrences(of: "distant", with: "se") + "Gift"),
         //: "recharge/applePayNotify",
         (String(noti_todayFormat) + String(mainLockChangeData.suffix(6))),
         //: "user/appleLogin"] + slowPathArr1
         (String(app_requireValue) + String(data_languagePhaseMessage.suffix(6)) + "ogin")] + slowPathArr1
    //: }()

    //: private lazy var slowPathArr1 = {
    private lazy var slowPathArr1 = //: return ["IM/getMyBlackList",
        [(String(show_totalStr.suffix(4)) + "etMyBlack" + String(k_absolutelyName)),
         //: "mf/business/sendMsg",
         (String(const_opTitle) + String(userAspectTitle.prefix(6)) + "ness/" + String(show_acrossId.suffix(7))),
         //: "mf/business/sendGift",
         (String(user_calendarPath.suffix(6)) + String(k_outputMsg) + String(showObserverStr.suffix(6)) + "endGift"),
         //: "mf/chatRoom/sendMsg"]
         (String(k_whoFormSiblingFormat) + String(appWeightId))]
    //: }()

    /// 获取接口语言
    //: lazy var interfaceLang: String = {
    lazy var interfaceLang: String = {
        //: let lang = MacroBorderDefineThen.getSystemLangCode()
        let lang = MacroBorderDefineThen.canny()
        //: if ["en", "ar", "es", "pt"].contains(lang) {
        if ["en", "ar", "es", "pt"].contains(lang) {
            //: return lang
            return lang
        }
        //: return "en"
        return "en"
        //: }()
    }()

    //: lazy var randomStr: String = {
    lazy var randomStr: String = //: return String.randomString(length: 16)
        .iq(length: 16)
    //: }()

    //: lazy var encryKeyStr: String = {
    lazy var encryKeyStr: String = {
        //: let sufStr = randomStr.suffix(6)
        let sufStr = randomStr.suffix(6)
        //: let reversedStr: String = String(sufStr.reversed())
        let reversedStr = String(sufStr.reversed())
        //: return "GJ21FB" + reversedStr
        return (user_stopWithStr.replacingOccurrences(of: "found", with: "B")) + reversedStr
        //: }()
    }()

    //: deinit {
    deinit {
        //: NotificationCenter.default.removeObserver(self)
        NotificationCenter.default.removeObserver(self)
    }
}

//: extension TalkingRequestAddrTool {
extension RocketRecognizeAddrTool {
    //: func checkServerUrl() {
    func nearLastPhone() {
        //: if FacultyReactiveCompatible.share.networkStatus != .Unavailable {
        if FacultyReactiveCompatible.share.networkStatus != .Unavailable {
            //: func_requestHost()
            consider()
            //: } else {
        } else {
            //: func_starCodeTime()
            commercial()
        }
    }

    //: func func_starCodeTime() {
    func commercial() {
        //: if self.hasFinishCheck {
        if self.hasFinishCheck {
            //: return
            return
        }
        //: if timerDispose != nil {
        if timerDispose != nil {
            //: timerDispose?.dispose()
            timerDispose?.dispose()
        }
        //: timerDispose = Observable<Int>.interval(.seconds(3), scheduler: MainScheduler.instance)
        timerDispose = Observable<Int>.interval(.seconds(3), scheduler: MainScheduler.instance)
            //: .subscribe(onNext: { [weak self] num in
            .subscribe(onNext: { [weak self] _ in
                //: guard let self = self else {return}
                guard let self = self else { return }
                //: self.func_requestHost()
                self.consider()
                //: })
            })
    }

    //: func func_endCodeTime() {
    func mensurable() {
        //: timerDispose?.dispose()
        timerDispose?.dispose()
    }

    //: func func_requestHost() {
    func consider() {
        //: if FacultyReactiveCompatible.share.networkStatus == .Unavailable {
        if FacultyReactiveCompatible.share.networkStatus == .Unavailable {
            //: return
            return
        }
        //: func_endCodeTime()
        mensurable()
        //: AppManagerRequest.func__initRequestHost { [weak self] succeed, result, errorModel in
        TotalThen.failureNo { [weak self] succeed, _, _ in
            //: guard let self = self else {return}
            guard let self = self else { return }
            //: if succeed {
            if succeed {
                //: self.hasFinishCheck = true
                self.hasFinishCheck = true
                //: printLog(message: "完成了检测,请求状态正常")
                printLog(message: (constWhiteAgainId.capitalized + String(user_distantTitle)))
                //: } else {
            } else {
                //: printLog(message: "接口请求失败")
                printLog(message: (userTireMessage.capitalized))
                //: if self.hasFinishCheck {return}
                if self.hasFinishCheck { return }
                //: self.hostCheckNum += 1
                self.hostCheckNum += 1
                //: if self.hostCheckNum < 3 {
                if self.hostCheckNum < 3 {
                    //: self.func_starCodeTime()
                    self.commercial()
                    //: } else {
                } else {
                    //: printLog(message: "完成了检测,需要更换server url")
                    printLog(message: (main_wouldMessage + "测,需要" + show_easternPath + "er url"))
                    //: self.func__changeHostUrl()
                    self.bathroomConfirm()
                }
            }
        }
    }

    //: func func__changeHostUrl() {
    func bathroomConfirm() {
        //: hostCheckNum = 0
        hostCheckNum = 0
        //: hasFinishCheck = true
        hasFinishCheck = true
        //: func_endCodeTime()
        mensurable()

        //: serverSelctIndex = (serverSelctIndex == 0) ? 1 : 0
        serverSelctIndex = (serverSelctIndex == 0) ? 1 : 0
        //: Defaults.set(serverSelctIndex, forKey: TalkingAddrServerUrlSelectIndexKey)
        mainNameNearbyTitle.set(serverSelctIndex, forKey: showScreenAcceptTitle)
        //: let serverDomain = serverUrlArr[serverSelctIndex]
        let serverDomain = serverUrlArr[serverSelctIndex]
        //: var serverPrefix = "http://"
        var serverPrefix = (String(user_stretchHideUrl) + appCloudKey.replacingOccurrences(of: "must", with: "/"))
        //: if Environment_Formal && !Encryption_Request {
        if dataPopText, !const_saltUrl {
            //: serverPrefix = "http://"
            serverPrefix = (String(user_stretchHideUrl) + appCloudKey.replacingOccurrences(of: "must", with: "/"))
        }
        //: serverUrlStr = serverPrefix.appending(serverDomain)
        serverUrlStr = serverPrefix.appending(serverDomain)
    }
}

// MARK: - 判断用户设备是否开启VPN

//: extension TalkingRequestAddrTool {
public extension RocketRecognizeAddrTool {
    /// 判断用户设备是否开启代理
    /// - Returns: false: 未开启  true: 开启
    //: public class func isUsedProxy() -> Bool {
    class func anyMagnitude() -> Bool {
        //: guard let proxy = CFNetworkCopySystemProxySettings()?.takeUnretainedValue() else { return false }
        guard let proxy = CFNetworkCopySystemProxySettings()?.takeUnretainedValue() else { return false }
        //: guard let dict = proxy as? [String: Any] else { return false }
        guard let dict = proxy as? [String: Any] else { return false }
        //: guard let HTTPProxy = dict["HTTPProxy"] as? String else { return false }
        guard let HTTPProxy = dict[(String(userRestrictionMessage) + String(k_legalMessage.suffix(8)))] as? String else { return false }
        //: if (HTTPProxy.count > 0) {
        if HTTPProxy.count > 0 {
            //: return true
            return true
        }
        //: return false
        return false
    }

    /// 判断用户设备是否开启代理VPN
    /// - Returns: false: 未开启  true: 开启
    //: public class func isUsedVPN() -> Bool {
    class func contribution() -> Bool {
        //: guard let proxy = CFNetworkCopySystemProxySettings()?.takeUnretainedValue() else { return false }
        guard let proxy = CFNetworkCopySystemProxySettings()?.takeUnretainedValue() else { return false }
        //: guard let dict = proxy as? [String: Any] else { return false }
        guard let dict = proxy as? [String: Any] else { return false }
        //: guard let scopedDic = dict["__SCOPED__"] as? [String: Any] else { return false }
        guard let scopedDic = dict[(String(showArrangementRangePath))] as? [String: Any] else { return false }
        //: for keyStr in scopedDic.keys {
        for keyStr in scopedDic.keys {
            //: if keyStr.contains("tap") || keyStr.contains("tun") || keyStr.contains("ipsec") || keyStr.contains("ppp"){
            if keyStr.contains((String(constIqTitle))) || keyStr.contains((mainMarginName.replacingOccurrences(of: "score", with: "n"))) || keyStr.contains((String(appSongMessage))) || keyStr.contains((String(noti_discountStr))) {
                //: return true
                return true
            }
        }
        //: return false
        return false
    }
}

//: extension TalkingRequestAddrTool {
extension RocketRecognizeAddrTool {
    //: func func__updateTalkingCookies(cookies: Array<HTTPCookie>) {
    func listenerBeauty(cookies: [HTTPCookie]) {
        //: printLog(message: "func__updateTalkingCookies = \(cookies)")
        printLog(message: (String(dataEvenTitle) + String(appFoundUrl)) + "\(cookies)")
        //: var tempCookies: Array<HTTPCookie> = cookies
        var tempCookies: [HTTPCookie] = cookies
        //: if cookies.count == 0 {
        if cookies.count == 0 {
            //: tempCookies = func__getCookiesFromOldServerUrl()
            tempCookies = dayUrl()
        }
        //: if tempCookies.count == 0 {
        if tempCookies.count == 0 {
            //: return
            return
        }
        //: var tempUrlArr: Array<URL> = []
        var tempUrlArr: [URL] = []

        //: if !self.serverUrlStr.isEmptyString {
        if !self.serverUrlStr.isEmptyString {
            //: tempUrlArr.append(URL.init(string: self.serverUrlStr)!)
            tempUrlArr.append(URL(string: self.serverUrlStr)!)
        }

        //: var urlDomain: String = FacultyReactiveCompatible.share.appConfigMode.urlH5Domain
        var urlDomain: String = FacultyReactiveCompatible.share.appConfigMode.urlH5Domain
        //: urlDomain = urlDomain.replacingOccurrences(of: "http://", with: "https://")
        urlDomain = urlDomain.replacingOccurrences(of: (String(user_stretchHideUrl) + appCloudKey.replacingOccurrences(of: "must", with: "/")), with: (String(kDragData)))
        //: if urlDomain.hasPrefix("https://") {
        if urlDomain.hasPrefix((String(kDragData))) {
            //: tempUrlArr.append(URL.init(string: urlDomain)!)
            tempUrlArr.append(URL(string: urlDomain)!)
            //: } else {
        } else {
            //: tempUrlArr.append(URL.init(string: "https://\(urlDomain)")!)
            tempUrlArr.append(URL(string: (data_petitionTitle.lowercased() + "s://") + "\(urlDomain)")!)
        }

        //: var addCookieArr: Array<HTTPCookie> = []
        var addCookieArr: [HTTPCookie] = []
        //: for cookies in tempCookies {
        for cookies in tempCookies {
            //: var cookieInfo: [HTTPCookiePropertyKey: Any] = cookies.properties!
            var cookieInfo: [HTTPCookiePropertyKey: Any] = cookies.properties!
            //: for url in tempUrlArr {
            for url in tempUrlArr {
                //: cookieInfo[HTTPCookiePropertyKey.domain] = url.host
                cookieInfo[HTTPCookiePropertyKey.domain] = url.host
                //: cookieInfo.removeValue(forKey: HTTPCookiePropertyKey.discard)
                cookieInfo.removeValue(forKey: HTTPCookiePropertyKey.discard)
                //: let newCookie = HTTPCookie.init(properties: cookieInfo)
                let newCookie = HTTPCookie(properties: cookieInfo)
                //: if newCookie != nil {
                if newCookie != nil {
                    //: addCookieArr.append(newCookie!)
                    addCookieArr.append(newCookie!)
                }
            }
        }
        //: for cookies in addCookieArr {
        for cookies in addCookieArr {
            //: HTTPCookieStorage.shared.setCookie(cookies)
            HTTPCookieStorage.shared.setCookie(cookies)
        }

        //: for url in tempUrlArr {
        for url in tempUrlArr {
            //: func__deleteSameCookiesWithUrl(url: url)
            partner(url: url)
        }
        //: DispatchQueue.main.async {
        DispatchQueue.main.async {
            //: WKWebsiteDataStore.default().fetchDataRecords(ofTypes: [WKWebsiteDataTypeCookies]) { _ in
            WKWebsiteDataStore.default().fetchDataRecords(ofTypes: [WKWebsiteDataTypeCookies]) { _ in
            }
        }

        //: func__saveAllCookies()
        bearOnCookies()
    }

    //: func func__deleteSameCookiesWithUrl(url: URL) {
    func partner(url: URL) {
        //: let cookiesArr: Array<HTTPCookie> = HTTPCookieStorage.shared.cookies(for: url)!
        let cookiesArr: [HTTPCookie] = HTTPCookieStorage.shared.cookies(for: url)!
        //: var saveDic: Dictionary<String, String> = [:]
        var saveDic: [String: String] = [:]
        //: for cookies in cookiesArr {
        for cookies in cookiesArr {
            //: if !cookies.name.isEmptyString {
            if !cookies.name.isEmptyString {
                //: let saveKey = cookies.name
                let saveKey = cookies.name
                //: if saveDic.keys.contains(saveKey) {
                if saveDic.keys.contains(saveKey) {
                    //: printLog(message: "func__deleteSameCookiesWithUrl = \(cookies.name)")
                    printLog(message: String(bytes: kBlindStr.map{$0^237}, encoding: .utf8)! + "\(cookies.name)")
                    //: HTTPCookieStorage.shared.deleteCookie(cookies)
                    HTTPCookieStorage.shared.deleteCookie(cookies)
                    //: } else {
                } else {
                    //: saveDic[saveKey] = "1"
                    saveDic[saveKey] = "1"
                }
            }
        }
    }

    //: func func__getCookiesFromOldServerUrl() -> Array<HTTPCookie> {
    func dayUrl() -> [HTTPCookie] {
        //: var serverPrefix = "http://"
        var serverPrefix = (String(user_stretchHideUrl) + appCloudKey.replacingOccurrences(of: "must", with: "/"))
        //: if Environment_Formal && !Encryption_Request {
        if dataPopText, !const_saltUrl {
            //: serverPrefix = "https://"
            serverPrefix = (String(kDragData))
        }
        //: let fullDomain = serverPrefix.appending("app.\(ReplaceUrlDomain).com")
        let fullDomain = serverPrefix.appending((String(constResolutionTireStr.suffix(4))) + "\(app_frameName)" + (String(noti_forwardName.suffix(4))))
        //: let oldCookies = HTTPCookieStorage.shared.cookies(for: URL.init(string: fullDomain)!)
        let oldCookies = HTTPCookieStorage.shared.cookies(for: URL(string: fullDomain)!)
        //: return oldCookies!
        return oldCookies!
    }

    //: @objc func cookieChanged(note: Notification) {
    @objc func dataNote(note _: Notification) {
        //: if FacultyReactiveCompatible.share.loginUid.isEmptyString {
        if FacultyReactiveCompatible.share.loginUid.isEmptyString {
            //: return
            return
        }
        //: if #available(iOS 11.0, *) {
        if #available(iOS 11.0, *) {
            //: checkHtmlCookieIfNeedCurrentVersion()
            isNeed()
            //: } else {
        } else {
            //: checkHtmlCookieIfNeedEarlyVersion()
            oldVersion()
        }
    }

    //: func checkHtmlCookieIfNeedCurrentVersion() {
    func isNeed() {
        //: var htmlDomain: String = FacultyReactiveCompatible.share.appConfigMode.urlH5Domain
        var htmlDomain: String = FacultyReactiveCompatible.share.appConfigMode.urlH5Domain

        //: if !htmlDomain.isEmptyString {
        if !htmlDomain.isEmptyString {
            //: let url = URL.init(string: htmlDomain)
            let url = URL(string: htmlDomain)
            //: htmlDomain = url?.host ?? ""
            htmlDomain = url?.host ?? ""
            //: let cookieArr: Array<HTTPCookie> = HTTPCookieStorage.shared.cookies!
            let cookieArr: [HTTPCookie] = HTTPCookieStorage.shared.cookies!
            //: var appCookie: Dictionary<String, HTTPCookie> =  Dictionary()
            var appCookie: [String: HTTPCookie] = Dictionary()
            //: var h5Cookie: Dictionary<String, HTTPCookie> =  Dictionary()
            var h5Cookie: [String: HTTPCookie] = Dictionary()

            //: for cookie in cookieArr {
            for cookie in cookieArr {
                //: let tempServerUrlStr = self.serverUrlStr
                let tempServerUrlStr = self.serverUrlStr
                //: let temphtmlDomainStr = htmlDomain
                let temphtmlDomainStr = htmlDomain
                //: if tempServerUrlStr.contains(cookie.domain) {
                if tempServerUrlStr.contains(cookie.domain) {
                    //: if cookie.name.uppercased() == "UID" {
                    if cookie.name.uppercased() == (String(k_triumphKey)) {
                        //: appCookie["UID"] = cookie
                        appCookie[(String(k_triumphKey))] = cookie
                    }
                    //: if cookie.name.uppercased() == "PHPSESSID" {
                    if cookie.name.uppercased() == (String(app_facilityId) + String(showEnableceFosterId)) {
                        //: appCookie["PHPSESSID"] = cookie
                        appCookie[(String(app_facilityId) + String(showEnableceFosterId))] = cookie
                    }

                    //: } else if temphtmlDomainStr.contains(cookie.domain) {
                } else if temphtmlDomainStr.contains(cookie.domain) {
                    //: if cookie.name.uppercased() == "UID" {
                    if cookie.name.uppercased() == (String(k_triumphKey)) {
                        //: h5Cookie["UID"] = cookie
                        h5Cookie[(String(k_triumphKey))] = cookie
                    }
                    //: if cookie.name.uppercased() == "PHPSESSID" {
                    if cookie.name.uppercased() == (String(app_facilityId) + String(showEnableceFosterId)) {
                        //: h5Cookie["PHPSESSID"] = cookie
                        h5Cookie[(String(app_facilityId) + String(showEnableceFosterId))] = cookie
                    }
                }
                //: var cookieEqual = true
                var cookieEqual = true
                //: if appCookie.keys.count != h5Cookie.keys.count {
                if appCookie.keys.count != h5Cookie.keys.count {
                    //: cookieEqual = false
                    cookieEqual = false
                    //: } else {
                } else {
                    //: if appCookie.keys.contains("UID") && h5Cookie.keys.contains("UID") {
                    if appCookie.keys.contains((String(k_triumphKey))), h5Cookie.keys.contains((String(k_triumphKey))) {
                        //: let appUid: HTTPCookie = appCookie["UID"]!
                        let appUid: HTTPCookie = appCookie[(String(k_triumphKey))]!
                        //: let h5Uid: HTTPCookie = h5Cookie["UID"]!
                        let h5Uid: HTTPCookie = h5Cookie[(String(k_triumphKey))]!

                        //: if h5Uid.value != appUid.value {
                        if h5Uid.value != appUid.value {
                            //: cookieEqual = false
                            cookieEqual = false
                        }
                    }
                    //: if appCookie.keys.contains("PHPSESSID") && h5Cookie.keys.contains("PHPSESSID") {
                    if appCookie.keys.contains((String(app_facilityId) + String(showEnableceFosterId))), h5Cookie.keys.contains((String(app_facilityId) + String(showEnableceFosterId))) {
                        //: let appPHPSESSID: HTTPCookie = appCookie["PHPSESSID"]!
                        let appPHPSESSID: HTTPCookie = appCookie[(String(app_facilityId) + String(showEnableceFosterId))]!
                        //: let h5PHPSESSID: HTTPCookie = h5Cookie["PHPSESSID"]!
                        let h5PHPSESSID: HTTPCookie = h5Cookie[(String(app_facilityId) + String(showEnableceFosterId))]!
                        //: if appPHPSESSID.value != h5PHPSESSID.value {
                        if appPHPSESSID.value != h5PHPSESSID.value {
                            //: cookieEqual = false
                            cookieEqual = false
                        }
                    }
                }
                //: if !cookieEqual {
                if !cookieEqual {
                    //: for (key, _) in appCookie {
                    for (key, _) in appCookie {
                        //: let cookie: HTTPCookie = appCookie[key]!
                        let cookie: HTTPCookie = appCookie[key]!
                        //: var cookiePro: Dictionary<HTTPCookiePropertyKey, Any> = cookie.properties!
                        var cookiePro: [HTTPCookiePropertyKey: Any] = cookie.properties!
                        //: cookiePro[HTTPCookiePropertyKey.domain] = htmlDomain
                        cookiePro[HTTPCookiePropertyKey.domain] = htmlDomain
                        //: cookiePro.removeValue(forKey: HTTPCookiePropertyKey.discard)
                        cookiePro.removeValue(forKey: HTTPCookiePropertyKey.discard)
                        //: HTTPCookieStorage.shared.setCookie(HTTPCookie.init(properties: cookiePro)!)
                        HTTPCookieStorage.shared.setCookie(HTTPCookie(properties: cookiePro)!)
                    }
                }
            }
        }
    }

    //: func checkHtmlCookieIfNeedEarlyVersion() {
    func oldVersion() {
        //: let htmlDomain: String = FacultyReactiveCompatible.share.appConfigMode.urlH5Domain
        let htmlDomain: String = FacultyReactiveCompatible.share.appConfigMode.urlH5Domain
        //: let url: URL = URL.init(string: htmlDomain)!
        let url = URL(string: htmlDomain)!
        //: let cookieArr: Array<HTTPCookie> = HTTPCookieStorage.shared.cookies(for: url)!
        let cookieArr: [HTTPCookie] = HTTPCookieStorage.shared.cookies(for: url)!
        //: var hasUID = false
        var hasUID = false
        //: var hasPHPSSID = false
        var hasPHPSSID = false
        //: for cookie in cookieArr {
        for cookie in cookieArr {
            //: if cookie.name == "UID" && cookie.value == FacultyReactiveCompatible.share.loginUserMode.userID {
            if cookie.name == (String(k_triumphKey)), cookie.value == FacultyReactiveCompatible.share.loginUserMode.userID {
                //: hasUID = true
                hasUID = true
            }
            //: if cookie.name == "PHPSESSID" {
            if cookie.name == (String(app_facilityId) + String(showEnableceFosterId)) {
                //: hasPHPSSID = true
                hasPHPSSID = true
            }
        }
        //: if !hasUID || !hasPHPSSID {
        if !hasUID || !hasPHPSSID {
            //: let cookieArr: Array<HTTPCookie> = HTTPCookieStorage.shared.cookies!
            let cookieArr: [HTTPCookie] = HTTPCookieStorage.shared.cookies!
            //: var appCookie: Dictionary<String, Any> =  Dictionary()
            var appCookie: [String: Any] = Dictionary()
            //: for cookie in cookieArr {
            for cookie in cookieArr {
                //: let tempServerUrlStr = self.serverUrlStr
                let tempServerUrlStr = self.serverUrlStr

                //: if tempServerUrlStr.contains(cookie.domain) {
                if tempServerUrlStr.contains(cookie.domain) {
                    //: if cookie.name.uppercased() == "UID" {
                    if cookie.name.uppercased() == (String(k_triumphKey)) {
                        //: appCookie["UID"] = cookie
                        appCookie[(String(k_triumphKey))] = cookie
                    }
                    //: if cookie.name.uppercased() == "PHPSESSID" {
                    if cookie.name.uppercased() == (String(app_facilityId) + String(showEnableceFosterId)) {
                        //: appCookie["PHPSESSID"] = cookie
                        appCookie[(String(app_facilityId) + String(showEnableceFosterId))] = cookie
                    }
                }
                //: if !hasUID && appCookie.keys.contains("UID") {
                if !hasUID, appCookie.keys.contains((String(k_triumphKey))) {
                    //: let uidCookie: HTTPCookie = appCookie["UID"] as! HTTPCookie
                    let uidCookie: HTTPCookie = appCookie[(String(k_triumphKey))] as! HTTPCookie
                    //: var uidPro: Dictionary<HTTPCookiePropertyKey, Any> = uidCookie.properties!
                    var uidPro: [HTTPCookiePropertyKey: Any] = uidCookie.properties!
                    //: uidPro[HTTPCookiePropertyKey.domain] = url.host
                    uidPro[HTTPCookiePropertyKey.domain] = url.host
                    //: uidPro.removeValue(forKey: HTTPCookiePropertyKey.discard)
                    uidPro.removeValue(forKey: HTTPCookiePropertyKey.discard)
                    //: HTTPCookieStorage.shared.setCookie(HTTPCookie.init(properties: uidPro)!)
                    HTTPCookieStorage.shared.setCookie(HTTPCookie(properties: uidPro)!)
                }
                //: if !hasPHPSSID && appCookie.keys.contains("PHPSESSID") {
                if !hasPHPSSID, appCookie.keys.contains((String(app_facilityId) + String(showEnableceFosterId))) {
                    //: let uidCookie: HTTPCookie = appCookie["PHPSESSID"] as! HTTPCookie
                    let uidCookie: HTTPCookie = appCookie[(String(app_facilityId) + String(showEnableceFosterId))] as! HTTPCookie
                    //: var uidPro: Dictionary<HTTPCookiePropertyKey, Any> = uidCookie.properties!
                    var uidPro: [HTTPCookiePropertyKey: Any] = uidCookie.properties!
                    //: uidPro[HTTPCookiePropertyKey.domain] = url.host
                    uidPro[HTTPCookiePropertyKey.domain] = url.host
                    //: uidPro.removeValue(forKey: HTTPCookiePropertyKey.discard)
                    uidPro.removeValue(forKey: HTTPCookiePropertyKey.discard)
                    //: HTTPCookieStorage.shared.setCookie(HTTPCookie.init(properties: uidPro)!)
                    HTTPCookieStorage.shared.setCookie(HTTPCookie(properties: uidPro)!)
                }
            }
        }
    }

    //: func func__saveAllCookies() {
    func bearOnCookies() {
        //: do {
        do {
            //: let cookies: Array<HTTPCookie> = HTTPCookieStorage.shared.cookies!
            let cookies: [HTTPCookie] = HTTPCookieStorage.shared.cookies!
            //: printLog(message: "func__saveAllCookies = \(cookies)")
            printLog(message: (String(user_videoMsg) + String(appManagerText.suffix(7))) + "\(cookies)")
            //: let cookieData = try NSKeyedArchiver.archivedData(withRootObject: cookies)
            let cookieData = try NSKeyedArchiver.archivedData(withRootObject: cookies)
            //: Defaults.set(cookieData, forKey: TalkingUserCookiesKey)
            mainNameNearbyTitle.set(cookieData, forKey: showAcceptData)
            //: } catch _ {
        } catch _ {
            //: printLog(message: "func__saveAllCookies  error")
            printLog(message: (String(data_lagPageFormat) + String(kReportedAppFormat.prefix(6)) + "veAl" + String(main_cellFormat.prefix(6)) + "es  e" + const_betweenFormat.replacingOccurrences(of: "possible", with: "r")))
        }
    }

    //: func func__setAppCookies() {
    func birthdayAndCookies() {
        //: let data = Defaults.object(forKey: TalkingUserCookiesKey)
        let data = mainNameNearbyTitle.object(forKey: showAcceptData)
        //: if data != nil {
        if data != nil {
            //: let data2: NSArray = NSKeyedUnarchiver.unarchiveObject(with: data as! Data) as! NSArray
            let data2: NSArray = NSKeyedUnarchiver.unarchiveObject(with: data as! Data) as! NSArray
            //: for cookie in data2 {
            for cookie in data2 {
                //: HTTPCookieStorage.shared.setCookie(cookie as! HTTPCookie)
                HTTPCookieStorage.shared.setCookie(cookie as! HTTPCookie)
            }
        }
    }

    //: func func__removeAllCookies() {
    func libraryMultiple() {
        //: Defaults.removeObject(forKey: TalkingUserCookiesKey)
        mainNameNearbyTitle.removeObject(forKey: showAcceptData)
        //: let cookiesArr: Array<HTTPCookie> = HTTPCookieStorage.shared.cookies!
        let cookiesArr: [HTTPCookie] = HTTPCookieStorage.shared.cookies!
        //: for cookies in cookiesArr {
        for cookies in cookiesArr {
            //: printLog(message: "🍪 删除\(cookies.name)")
            printLog(message: "🍪 删除\(cookies.name)")
            //: HTTPCookieStorage.shared.deleteCookie(cookies)
            HTTPCookieStorage.shared.deleteCookie(cookies)
        }
    }
}
