
//: Declare String Begin

/*: "init(coder:) has not been implemented" :*/
fileprivate let userBeneathFormat:[UInt8] = [0xf5,0xf2,0xf5,0xe8,0xb4,0xff,0xf3,0xf8,0xf9,0xee,0xa6,0xb5,0xbc,0xf4,0xfd,0xef,0xbc,0xf2,0xf3,0xe8,0xbc,0xfe,0xf9,0xf9,0xf2,0xbc,0xf5,0xf1,0xec,0xf0,0xf9,0xf1,0xf9,0xf2,0xe8,0xf9,0xf8]

private func staffMember(every num: UInt8) -> UInt8 {
    return num ^ 156
}

/*: "roomId" :*/
fileprivate let userExistingMsg:String = "until electric effect springroomId"

/*: "type" :*/
fileprivate let user_bodyPath:[UInt8] = [0xf9,0xf4,0xfd,0xe8]

private func swaddlingClothes(premium num: UInt8) -> UInt8 {
    return num ^ 141
}

/*: "extra" :*/
fileprivate let showAverageUrl:[Character] = ["e","x","t","r","a"]

/*: "jumpKey" :*/
fileprivate let showStepText:[UInt8] = [0x85,0x9a,0x82,0x9f,0xa4,0x8a,0x96]

/*: "Send Gift" :*/
fileprivate let dataCarrierKey:[Character] = ["S","e","n","d"," "]
fileprivate let dataAdditionalStr:String = "Giftenter demonstrate mark"

/*: "View info" :*/
fileprivate let const_ballValue:[Character] = ["V","i","e","w"," ","i","n","f"]
fileprivate let constFollowingPath:[Character] = ["o"]

/*: "Report" :*/
fileprivate let k_requireUrl:String = "Reportscale editor bread"

/*: "uid" :*/
fileprivate let appConnectionName:[UInt8] = [0x55,0x49,0x44]

private func fitOut(recipe num: UInt8) -> UInt8 {
    return num ^ 32
}

/*: "name" :*/
fileprivate let user_courseSecondEspecialName:[UInt8] = [0x6d,0x62,0x6e,0x66]

private func layerContact(confirm num: UInt8) -> UInt8 {
    return num ^ 3
}

/*: "Can't send blank message" :*/
fileprivate let mainAnalyzeWritingId:String = "Can\'"
fileprivate let main_homePingId:String = "sort decrease elect interested noted bla"
fileprivate let user_petitionMsg:[Character] = ["s","s","a","g","e"]

/*: "user" :*/
fileprivate let main_blankSayData:String = "uclosedr"

/*: "content" :*/
fileprivate let main_wifeFormat:[UInt8] = [0xf,0x1b,0x1a,0x20,0x11,0x1a,0x20]

fileprivate func evenActual(found num: UInt8) -> UInt8 {
    let value = Int(num) - 172
    if value < 0 {
        return UInt8(value + 256)
    } else {
        return UInt8(value)
    }
}

/*: "msgType" :*/
fileprivate let notiSpreadMsg:String = "con blink unablemsgType"

/*: "txt" :*/
fileprivate let constFrameworkData:String = "tbounce"

/*: "isGroup" :*/
fileprivate let main_shadeUsPath:[Character] = ["i","s","G","r","o"]
fileprivate let dataLogicalMessage:[Character] = ["u","p"]

/*: "jumps" :*/
fileprivate let user_assessmentStr:String = "jextramps"

/*: "message" :*/
fileprivate let constScanMessage:[UInt8] = [0x65,0x67,0x61,0x73,0x73,0x65,0x6d]

/*: "TxtMsg" :*/
fileprivate let appSortWelcomeBeginningMessage:String = "inmate elementTxtMsg"

/*: "toUid" :*/
fileprivate let const_funName:[Character] = ["t","o","U","i","d"]

/*: "msgInfo" :*/
fileprivate let mainTeemPath:String = "msgInfocourse column metal security"

/*: "params" :*/
fileprivate let userOrganizationTitle:[UInt8] = [0x9,0x18,0xb,0x18,0x14,0xa]

private func latSide(since num: UInt8) -> UInt8 {
    return num ^ 121
}

/*: "contentType" :*/
fileprivate let main_medicRedCompareUrl:[UInt8] = [0xa0,0xac,0xad,0xb7,0xa6,0xad,0xb7,0x97,0xba,0xb3,0xa6]

/*: "ImgMsg" :*/
fileprivate let dataNativePath:String = "ImgMsghorrible especial selected"

/*: "img" :*/
fileprivate let showBridgeStr:String = "impass"

/*: "imageUri" :*/
fileprivate let show_heartMsg:[UInt8] = [0xff,0x3,0xf7,0xfd,0xfb,0xeb,0x8,0xff]

fileprivate func easternCream(stick num: UInt8) -> UInt8 {
    let value = Int(num) + 106
    if value > 255 {
        return UInt8(value - 256)
    } else {
        return UInt8(value)
    }
}

/*: "pic" :*/
fileprivate let showRegionStr:String = "pwhich"

/*: "msgId" :*/
fileprivate let k_mmComplexTitle:String = "msgIdin edition left expansion gender"

/*: "totalMfCoin" :*/
fileprivate let app_rawPath:[Character] = ["t","o","t","a","l"]
fileprivate let notiTurnId:String = "advocate dot choice communicateMfCoin"

/*: "giftId" :*/
fileprivate let constShotName:[Character] = ["g","i","f","t","I","d"]

/*: "giftNum" :*/
fileprivate let notiSimultaneouslyHiddenSpecialMsg:String = "giftNumcan member related"

/*: "pkgItemsetId" :*/
fileprivate let app_millMsg:String = "visual standard ago maximum girlpkgIte"
fileprivate let show_requireUrl:String = "stock method round globmsetId"

/*: "msg" :*/
fileprivate let k_shirtStr:String = "msvid"

/*: "tips" :*/
fileprivate let dataExceptKey:String = "tipoutlet"

/*: "gift" :*/
fileprivate let k_prepareConstructId:String = "gescapet"

/*: "nickname" :*/
fileprivate let mainRevenueUrl:[Character] = ["n","i","c","k","n","a"]
fileprivate let appLengthKey:[Character] = ["m","e"]

/*: "headPic" :*/
fileprivate let main_coordinatorTitle:[UInt8] = [0x75,0x78,0x7c,0x79,0x4d,0x74,0x7e]

private func blankEven(sound num: UInt8) -> UInt8 {
    return num ^ 29
}

/*: "All Numbers" :*/
fileprivate let app_bigPath:String = "All device match"
fileprivate let user_ballOptionValue:String = "S"

/*: "sendNum" :*/
fileprivate let mainYourselfUrl:String = "sendNumtrue main private street discussion"

/*: "toUser" :*/
fileprivate let data_answerStr:String = "previous service database exclusive mixtoUser"

/*: "msgIncome" :*/
fileprivate let user_relateExName:String = "heritage appropriatemsgIncome"

/*: "isConnection" :*/
fileprivate let const_greetUrl:String = "isConconstant var"
fileprivate let data_remoteContent:String = "spectrum"
fileprivate let mainThereId:String = "ECTION"

//: Declare String End

// __DEBUG__
// __CLOSE_PRINT__
//
//  UncompressChatController.swift
//  AbroadTalking
//
//  Created by young on 2023/4/4.
//

//: import UIKit
import UIKit

//: class TalkingGroupChatController: TalkingBaseViewController {
class UncompressChatController: SafetyFactorViewController {
    //: var infoModel = TalkingChatRoomInfoModel()
    var infoModel = ViewInfoModel() // 群聊信息
    //: private let disposeBag = DisposeBag()
    private let disposeBag = DisposeBag()
    //: private var tipsTimer: Timer?
    private var tipsTimer: Timer? // 提示消息定时器
    //: private var conversationData = TalkingConversationModel()
    private var conversationData = MultistageRocketConversationModel() // 会话cellData
    //: private var conversationType: TalkingIMChatType = .group
    private var conversationType: TalkingIMChatType = .group // 会话类型
    //: private var atFlag = false
    private var atFlag = false // 记录是否@

    //: init(groupType: String, infoData: [String: Any]) {
    init(groupType: String, infoData: [String: Any]) {
        //: super.init(nibName: nil, bundle: nil)
        super.init(nibName: nil, bundle: nil)
        //: hideNavi = true
        hideNavi = true
        //: if let model = TalkingChatRoomInfoModel.deserialize(from: infoData) {
        if let model = ViewInfoModel.deserialize(from: infoData) {
            //: infoModel = model
            infoModel = model
        }
        //: conversationData.groupID = infoModel.roomId
        conversationData.groupID = infoModel.roomId
        //: conversationData.groupType = groupType
        conversationData.groupType = groupType
        //: if groupType == GroupType_Meeting {
        if groupType == GroupType_Meeting {
            //: conversationType = .meeting
            conversationType = .meeting
        }
    }

    //: required init?(coder: NSCoder) {
    required init?(coder _: NSCoder) {
        //: fatalError("init(coder:) has not been implemented")
        fatalError(String(bytes: userBeneathFormat.map{staffMember(every: $0)}, encoding: .utf8)!)
    }

    //: override func loadView() {
    override func loadView() {
        //: if #available(iOS 13.2, *) {
        if #available(iOS 13.2, *) {
            //: if FacultyReactiveCompatible.share.appConfigMode.disableShootScreen {
            if FacultyReactiveCompatible.share.appConfigMode.disableShootScreen {
                //: super.loadView()
                super.loadView()
                //: } else {
            } else {
                //: let bgView = MAScreenShieldView(frame: CGRect(x: 0, y: 0, width: ScreenWidth, height: ScreenHeight))
                let bgView = ShieldView(frame: CGRect(x: 0, y: 0, width: main_assetUrl, height: kTopUrl))
                //: self.view = bgView
                self.view = bgView
            }
            //: } else {
        } else {
            //: super.loadView()
            super.loadView()
        }
    }

    //: override func viewDidLoad() {
    override func viewDidLoad() {
        //: super.viewDidLoad()
        super.viewDidLoad()
        //: createUI()
        omit()
        //: createUIConstraints()
        regain()
        //: bindInteraction()
        visualQuantityeraction()
        //: addTapGestureRecognizer()
        mixIn()
        //: func__chatRoom_insertTipMsg()
        implantMsg()
        //: chatSettings()
        convert()

        //: if conversationType == .private {
        if conversationType == .private {
            //: initCachEffectData()
            ballBreaker()
        }
    }

    //: override func viewDidAppear(_ animated: Bool) {
    override func viewDidAppear(_ animated: Bool) {
        //: super.viewDidAppear(animated)
        super.viewDidAppear(animated)
        //: if self.atFlag {
        if self.atFlag {
            //: self.msgInputView.inputTextView.becomeFirstResponder()
            self.msgInputView.inputTextView.becomeFirstResponder()
            //: self.atFlag = false
            self.atFlag = false
        }
    }

    //: override func viewWillDisappear(_ animated: Bool) {
    override func viewWillDisappear(_ animated: Bool) {
        //: super.viewWillDisappear(animated)
        super.viewWillDisappear(animated)
        //: view.endEditing(true)
        view.endEditing(true)
    }

    //: deinit {
    deinit {
        //: if self.conversationData.groupType == GroupType_Meeting {
        if self.conversationData.groupType == GroupType_Meeting {
            //: V2TIMManager.sharedInstance().quitGroup(self.conversationData.groupID, succ: nil, fail: nil)
            V2TIMManager.sharedInstance().quitGroup(self.conversationData.groupID, succ: nil, fail: nil)
        }
        //: self.func__joinOrQuitRoom(join: false)
        self.flow(join: false)
        //: self.destroyTipsTimer()
        self.observer()
    }

    // MARK: - Lazy Load

    //: private lazy var msgView: TUIMessageController = {
    private lazy var msgView: FirMessageViewController = {
        //: let msgVC = TUIMessageController.init()
        let msgVC = FirMessageViewController()
        //: msgVC.delegate = self
        msgVC.delegate = self
        //: msgVC.view.backgroundColor = UIColor.clear
        msgVC.view.backgroundColor = UIColor.clear
        //: msgVC.tableView.backgroundColor = .clear
        msgVC.tableView.backgroundColor = .clear
        //: msgVC.tableView.register(TalkingGroupChatTextMsgCell.self, forCellReuseIdentifier: TalkingGroupChatTextMsgCell.className())
        msgVC.tableView.register(BreastPocketMsgCell.self, forCellReuseIdentifier: BreastPocketMsgCell.className())
        //: msgVC.tableView.register(TalkingChatImageMsgCell.self, forCellReuseIdentifier: TalkingChatImageMsgCell.className())
        msgVC.tableView.register(ImageViewCell.self, forCellReuseIdentifier: ImageViewCell.className())
        //: msgVC.tableView.register(TalkingGroupChatGiftMsgCell.self, forCellReuseIdentifier: TalkingGroupChatGiftMsgCell.className())
        msgVC.tableView.register(FeatureReactiveCompatible.self, forCellReuseIdentifier: FeatureReactiveCompatible.className())
        //: msgVC.tableView.register(TalkingChatTipsMsgCell.self, forCellReuseIdentifier: TalkingChatTipsMsgCell.className())
        msgVC.tableView.register(GenerationThen.self, forCellReuseIdentifier: GenerationThen.className())
        //: msgVC.tableView.register(TUISystemMessageCell.self, forCellReuseIdentifier: RatioViewCell.TSystemMessageCell_ReuseId)
        msgVC.tableView.register(IdentityMessageCell.self, forCellReuseIdentifier: RatioViewCell.TSystemMessageCell_ReuseId)
        //: msgVC.currentChatType = conversationType
        msgVC.currentChatType = conversationType
        //: return msgVC
        return msgVC
        //: }()
    }()

    //: private lazy var msgInputView: TalkingChatInputView = {
    private lazy var msgInputView: ConfirmView = {
        //: let view = TalkingChatInputView(type: .GroupChat)
        let view = ConfirmView(type: .GroupChat)
        //: view.chatRoomID = infoModel.roomId
        view.chatRoomID = infoModel.roomId
        //: view.delegate = self
        view.delegate = self
        //: return view
        return view
        //: }()
    }()

    //: private lazy var giftEffectView: TalkingPrivateChatAnimatView = {
    private lazy var giftEffectView: AfterAnimatView = {
        //: let effectView = TalkingPrivateChatAnimatView.init()
        let effectView = AfterAnimatView()
        //: effectView.contentMode = .scaleAspectFill
        effectView.contentMode = .scaleAspectFill
        //: return effectView
        return effectView
        //: }()
    }()

    //: private lazy var giftTrackView: TalkingGiftTrackView = {
    private lazy var giftTrackView: TillView = {
        //: let trackView = TalkingGiftTrackView.init()
        let trackView = TillView()
        //: trackView.isUserInteractionEnabled = false
        trackView.isUserInteractionEnabled = false
        //: return trackView
        return trackView
        //: }()
    }()

    //: private lazy var navView: TalkingChatNavTitleView = {
    private lazy var navView: JumpView = {
        //: let navView = TalkingChatNavTitleView(type: conversationType)
        let navView = JumpView(type: conversationType)
        //: navView.titleLabel.text = infoModel.name
        navView.titleLabel.text = infoModel.name
        //: return navView
        return navView
        //: }()
    }()

    //: private lazy var someoneView: TalkingSomeoneAtMeView = {
    private lazy var someoneView: AgoView = {
        //: let v = TalkingSomeoneAtMeView()
        let v = AgoView()
        //: v.isHidden = true
        v.isHidden = true
        //: return v
        return v
        //: }()
    }()

    //: private lazy var mentionInfoArr: [[String: Any]] = {
    private lazy var mentionInfoArr: [[String: Any]] = //: return [[String: Any]]()
        .init()
    //: }()
}

// MARK: - Load Data

//: extension TalkingGroupChatController {
extension UncompressChatController {
    /// 群聊设置
    //: private func chatSettings() {
    private func convert() {
        //: TalkingGroupChatManager.share.chatRoomVC = self
        ReportedReactiveCompatible.share.chatRoomVC = self

        //: func__joinOrQuitRoom(join: true)
        flow(join: true)
    }

    /// 加入或退出聊天室
    /// - Parameter join: 是否加入
    //: private func func__joinOrQuitRoom(join: Bool) {
    private func flow(join: Bool) {
        //: let params = ["roomId": self.conversationData.groupID, "type": (join == true ? 1:2)] as [String: Any]
        let params = [(String(userExistingMsg.suffix(6))): self.conversationData.groupID, String(bytes: user_bodyPath.map{swaddlingClothes(premium: $0)}, encoding: .utf8)!: join == true ? 1 : 2] as [String: Any]
        //: TalkingChatRequestTool.req_chatRoomJoinOrQuit(params: params) { succeed, result, errorModel in
        PouchReactiveCompatible.feeWeek(params: params) { succeed, _, _ in
            //: guard succeed else { return }
            guard succeed else { return }
//            guard let data = result as? [String: Any] else {
//                return
//            }
//
//            if join {
//
//            }
        }
    }
}

// MARK: - RunningReadHalf

//: extension TalkingGroupChatController: TMessageControllerDelegate {
extension UncompressChatController: RunningReadHalf {
    //: func messageController(_ controller: TUIMessageController!, onNewMessage data: V2TIMMessage!) -> AgoneCellData! {
    func plain(_: FirMessageViewController!, bold data: V2TIMMessage!) -> AgoneCellData! {
        //: let dic = CircularReactiveCompatible.parseTXMessageData(data: data.customElem.data)
        let dic = CircularReactiveCompatible.occurrent(data: data.customElem.data)
        //: let cellData = TUISocialChatManager.handleTXReceiveMessage(withExtral: dic, cusData: data.localCustomData, tempModel: data)
        let cellData = CivilityReactiveCompatible.stack(withExtral: dic, cusData: data.localCustomData, tempModel: data)

        //: return cellData
        return cellData
    }

    //: func messageController(_ controller: TUIMessageController!, onShowMessageData data: AgoneCellData!) -> RatioViewCell! {
    func controllerLayer(_: FirMessageViewController!, referent data: AgoneCellData!) -> RatioViewCell! {
        //: if data.isKind(of: WillCellData.self) {
        if data.isKind(of: WillCellData.self) {
            //: let cell = TalkingGroupChatTextMsgCell.init(style: .default, reuseIdentifier: TalkingGroupChatTextMsgCell.className())
            let cell = BreastPocketMsgCell(style: .default, reuseIdentifier: BreastPocketMsgCell.className())
            //: let cellData = data as! WillCellData
            let cellData = data as! WillCellData
            //: cell.fill(with: cellData)
            cell.lineStreetwise(with: cellData)
            //: cellData.atBlock = { [weak self] uid in
            cellData.atBlock = { [weak self] uid in
                //: guard let self = self else { return }
                guard let self = self else { return }
                //: self.menu_goToUserDetailController(uid: uid)
                self.sleepingPillFun(uid: uid)
            }
            //: cell.transBlock = { [weak self] in
            cell.transBlock = { [weak self] in
                //: guard let self = self else { return }
                guard let self = self else { return }
                //: self.msgView.translateMsg(with: cellData)
                self.msgView.storage(cellData)
            }

            //: return cell
            return cell
        }

        //: if data.isKind(of: ApartCellData.self) {
        if data.isKind(of: ApartCellData.self) {
            //: let cell = TalkingChatImageMsgCell.init(style: .default, reuseIdentifier: TalkingChatImageMsgCell.className())
            let cell = ImageViewCell(style: .default, reuseIdentifier: ImageViewCell.className())
            //: let cellData = data as! ApartCellData
            let cellData = data as! ApartCellData
            //: cell.fill(with: cellData)
            cell.lineStreetwise(with: cellData)
            //: return cell
            return cell
        }

        //: if data.isKind(of: TalkingGroupChatGiftMsgCellData.self) {
        if data.isKind(of: CompartmentReactiveCompatible.self) {
            //: let cell = TalkingGroupChatGiftMsgCell.init(style: .default, reuseIdentifier: TalkingGroupChatGiftMsgCell.className())
            let cell = FeatureReactiveCompatible(style: .default, reuseIdentifier: FeatureReactiveCompatible.className())
            //: let cellData = data as! TalkingGroupChatGiftMsgCellData
            let cellData = data as! CompartmentReactiveCompatible
            //: lineStreetwise.fill(with: cellData)
            cell.lineStreetwise(with: cellData)

            //: return cell
            return cell
        }

        //: if data.isKind(of: TalkingChatTipsMsgCellData.self) {
        if data.isKind(of: AgoThen.self) {
            //: let cell = TalkingChatTipsMsgCell.init(style: .default, reuseIdentifier: TalkingChatTipsMsgCell.className())
            let cell = GenerationThen(style: .default, reuseIdentifier: GenerationThen.className())
            //: let cellData = data as! TalkingChatTipsMsgCellData
            let cellData = data as! AgoThen
            //: cell.fill(with: cellData)
            cell.lineStreetwise(with: cellData)
            //: return cell
            return cell
        }

        //: return nil
        return nil
    }

    //: func messageController(_ controller: TUIMessageController!, onSelectMessageAvatar cell: RatioViewCell!) {
    func of(_: FirMessageViewController!, ask cell: RatioViewCell!) {
        //: if let userModel = cell.messageData?.msgModel.user {
        if let userModel = cell.messageData?.msgModel.user {
            //: if userModel.uid == Int(FacultyReactiveCompatible.share.loginUid) {
            if userModel.uid == Int(FacultyReactiveCompatible.share.loginUid) {
                //: menu_goToUserDetailController(uid: userModel.uid )
                sleepingPillFun(uid: userModel.uid)
                //: }else{
            } else {
                //: menu_showMunuPopView(user: userModel)
                bean(user: userModel)
            }
        }
    }

    //: func messageController(_ controller: TUIMessageController!, onSelectMessageContent cell: RatioViewCell!) {
    func fill(_: FirMessageViewController!, handle cell: RatioViewCell!) {
        //: if cell .isKind(of: TalkingChatImageMsgCell.self) {
        if cell.isKind(of: ImageViewCell.self) { // 图片
            //: let imageCellData = cell.messageData as! ApartCellData
            let imageCellData = cell.messageData as! ApartCellData
            //: let libraryPath = NSSearchPathForDirectoriesInDomains(.cachesDirectory, .userDomainMask, true).first!
            let libraryPath = NSSearchPathForDirectoriesInDomains(.cachesDirectory, .userDomainMask, true).first!
            //: var filePath = (libraryPath as NSString).appendingPathComponent(imageFileName)
            var filePath = (libraryPath as NSString).appendingPathComponent(data_marginPath)
            //: filePath = (filePath as NSString).appendingPathComponent((imageCellData.imageUrl! as NSString).lastPathComponent)
            filePath = (filePath as NSString).appendingPathComponent((imageCellData.imageUrl! as NSString).lastPathComponent)

            //: var model = TalkingMomentPhotoModel.init()
            var model = InmateTransformable()
            //: if FileManager.default.fileExists(atPath: filePath) {
            if FileManager.default.fileExists(atPath: filePath) {
                //: let data = NSData.init(contentsOfFile: filePath)
                let data = NSData(contentsOfFile: filePath)
                //: model.imagePic = UIImage(data: data! as Data)
                model.imagePic = UIImage(data: data! as Data)
                //: } else {
            } else {
                //: if imageCellData.imageData != nil {
                if imageCellData.imageData != nil {
                    //: model.imagePic = imageCellData.imageData
                    model.imagePic = imageCellData.imageData
                    //: } else {
                } else {
                    //: model.imgUrl =  imageCellData.imageUrl
                    model.imgUrl = imageCellData.imageUrl
                }
            }
            //: var currenModel = TalkingMomentModel.init()
            var currenModel = AgencyMomentModel()
            //: var tempArr: [TalkingMomentPhotoModel] = []
            var tempArr: [InmateTransformable] = []
            //: tempArr.append(model)
            tempArr.append(model)
            //: currenModel.pic = tempArr
            currenModel.pic = tempArr
            //: let vc = TalkingMomentPhotosVC.init(momentModel: currenModel, index: 0, type: .normal)
            let vc = SpanScreenVc(momentModel: currenModel, index: 0, type: .normal)
            //: self.currentViewController()?.navigationController?.pushViewController(vc, animated: true)
            self.field()?.navigationController?.pushViewController(vc, animated: true)

            //: } else if cell.isKind(of: TalkingGroupChatTextMsgCell.self) {
        } else if cell.isKind(of: BreastPocketMsgCell.self) {
            //: let textCell = cell as! TalkingGroupChatTextMsgCell
            let textCell = cell as! BreastPocketMsgCell
            //: let textData = textCell.messageData
            let textData = textCell.messageData
            //: let dic = CircularReactiveCompatible.parseTXMessageData(data: (textData?.innerMessage.customElem.data)!)
            let dic = CircularReactiveCompatible.occurrent(data: (textData?.innerMessage.customElem.data)!)
            //: guard let extraDic = dic["extra"] as? Dictionary<String, Any> else {
            guard let extraDic = dic[(String(showAverageUrl))] as? [String: Any] else {
                //: return
                return
            }
            //: if extraDic.keys.contains("jumpKey") {
            if extraDic.keys.contains(String(bytes: showStepText.map{$0^239}, encoding: .utf8)!) {
                //: if let jumpModel = JSONDeserializer<TalkingMessageJumpModel>.deserializeFrom(dict: extraDic, designatedPath: nil) {
                if let jumpModel = JSONDeserializer<PhoneMessageTransformable>.deserializeFrom(dict: extraDic, designatedPath: nil) {
                    //: SmartThen.share.func__actionPushForMessage(jumpModel: jumpModel)
                    SmartThen.share.questionable(jumpModel: jumpModel)
                }
            }

            //: } else if cell.isKind(of: TalkingChatTipsMsgCell.self) {
        } else if cell.isKind(of: GenerationThen.self) {
            //: let tipCell = cell as! TalkingChatTipsMsgCell
            let tipCell = cell as! GenerationThen
            //: let tipData = tipCell.messageData
            let tipData = tipCell.messageData
            //: let dic = CircularReactiveCompatible.parseTXMessageData(data: (tipData?.innerMessage.customElem.data)!)
            let dic = CircularReactiveCompatible.occurrent(data: (tipData?.innerMessage.customElem.data)!)
            //: guard let extraDic = dic["extra"] as? Dictionary<String, Any> else {
            guard let extraDic = dic[(String(showAverageUrl))] as? [String: Any] else {
                //: return
                return
            }
            //: if extraDic.keys.contains("jumpKey") {
            if extraDic.keys.contains(String(bytes: showStepText.map{$0^239}, encoding: .utf8)!) {
                //: if let jumpModel = JSONDeserializer<TalkingMessageJumpModel>.deserializeFrom(dict: extraDic, designatedPath: nil) {
                if let jumpModel = JSONDeserializer<PhoneMessageTransformable>.deserializeFrom(dict: extraDic, designatedPath: nil) {
                    //: SmartThen.share.func__actionPushForMessage(jumpModel: jumpModel)
                    SmartThen.share.questionable(jumpModel: jumpModel)
                }
            }
        }
    }

    //: func messageController(_ controller: TUIMessageController!, onRetryMessageContent cell: RatioViewCell!) {
    func fixedSecure(_: FirMessageViewController!, noSection cell: RatioViewCell!) {
        //: func__sendPriveteMsg(msgData: cell.messageData!, reSend: true)
        likeSend(msgData: cell.messageData!, reSend: true)
    }
}

// MARK: - Event

//: extension TalkingGroupChatController {
extension UncompressChatController {
    /// 销毁倒计时
    //: private func destroyTipsTimer() {
    private func observer() {
        //: if tipsTimer != nil {
        if tipsTimer != nil {
            //: tipsTimer?.invalidate()
            tipsTimer?.invalidate()
            //: tipsTimer = nil
            tipsTimer = nil
        }
    }

    /// 点击用户头像，展示操作面板
    /// - Parameter user: 用户模型
    //: private func menu_showMunuPopView(user: AbTalkingChatMsgUserModel) {
    private func bean(user: LifetimePointModel) {
        //: view.endEditing(true)
        view.endEditing(true)
        //: let vc = TalkingMunuPopView.init(frame: self.view.frame)
        let vc = ExploreDataSource(frame: self.view.frame)
        //: vc.title = user.nickname
        vc.title = user.nickname
        //: vc.initwithList(cellTitleList: ["Send Gift".localized, "@", "View info".localized, "Report".localized])
        vc.footList(cellTitleList: [(String(dataCarrierKey) + String(dataAdditionalStr.prefix(4))).localized, "@", (String(const_ballValue) + String(constFollowingPath)).localized, (String(k_requireUrl.prefix(6))).localized])
        //: vc.munuBlock = { [weak self] index, str in
        vc.munuBlock = { [weak self] index, _ in
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: if index == 0 {
            if index == 0 {
                //: self.menu_sentGiftAction(user: user)
                self.carteDuJourSwitchly(user: user)
                //: } else if index == 1 {
            } else if index == 1 {
                //: self.menu_atTa(user: user)
                self.punctuality(user: user)
                //: } else if index == 2 {
            } else if index == 2 {
                //: self.menu_goToUserDetailController(uid: user.uid)
                self.sleepingPillFun(uid: user.uid)
                //: } else if index == 3 {
            } else if index == 3 {
                //: self.menu_reportAction(uid: String(user.uid))
                self.mergeDoing(uid: String(user.uid))
            }
        }
    }

    /// 送礼
    /// - Parameter user: 用户模型
    //: private func menu_sentGiftAction(user: AbTalkingChatMsgUserModel) {
    private func carteDuJourSwitchly(user: LifetimePointModel) {
        //: self.msgInputView.clickgiftBtn()
        self.msgInputView.raw()
        //: let model = TalkingChatRoomMemberModel()
        let model = RocketTransformable()
        //: model.uid = "\(user.uid)"
        model.uid = "\(user.uid)"
        //: model.nickname = user.nickname
        model.nickname = user.nickname
        //: model.headPic = user.headPic
        model.headPic = user.headPic
        //: model.sex = user.sex
        model.sex = user.sex
        //: model.age = Int(user.age) ?? 0
        model.age = Int(user.age) ?? 0
        //: self.msgInputView.giftView.didClickSelectedRow(model: model)
        self.msgInputView.giftView.processor(model: model)
    }

    /// @Ta
    /// - Parameter user: 用户模型
    //: private func menu_atTa(user: AbTalkingChatMsgUserModel) {
    private func punctuality(user: LifetimePointModel) {
        //: let dict: [String: Any] = ["uid": String(user.uid), "name": user.nickname]
        let dict: [String: Any] = [String(bytes: appConnectionName.map{fitOut(recipe: $0)}, encoding: .utf8)!: String(user.uid), String(bytes: user_courseSecondEspecialName.map{layerContact(confirm: $0)}, encoding: .utf8)!: user.nickname]
        //: self.msgInputView.receiveAtInfo(range: nil, dict: dict)
        self.msgInputView.springDict(range: nil, dict: dict)
        //: self.msgInputView.inputTextView.becomeFirstResponder()
        self.msgInputView.inputTextView.becomeFirstResponder()
    }

    /// 访问用户详情
    /// - Parameter uid: 用户Id
    //: private func menu_goToUserDetailController(uid: Int) {
    private func sleepingPillFun(uid: Int) {
        //: SmartThen.share.func__pushToUserDetailVC(uid: String(uid))
        SmartThen.share.numbererest(uid: String(uid))
    }

    /// 举报
    /// - Parameter uid: 用户Id
    //: private func menu_reportAction(uid: String) {
    private func mergeDoing(uid: String) {
        //: self.msgInputView.showOtherInputView = true
        self.msgInputView.showOtherInputView = true
        //: let reportView = TalkingReportAlertView.init(frame: UIScreen.main.bounds, type: .reportUserType, rId: uid)
        let reportView = OccurrenceView(frame: UIScreen.main.bounds, type: .reportUserType, rId: uid)
        //: reportView.showReportViewIn(view: nil)
        reportView.nearbyDisplay(view: nil)
        //: reportView.reportHideBlock = { [weak self] _ in
        reportView.reportHideBlock = { [weak self] _ in
            //: guard let self = self else {return}
            guard let self = self else { return }
            //: self.msgInputView.showOtherInputView = false
            self.msgInputView.showOtherInputView = false
            //: return
        }
    }
}

// MARK: - 发送消息

//: extension TalkingGroupChatController {
extension UncompressChatController {
    /// 发送文本消息
    /// - Parameter msgStr: 文本

    //: internal func func__sendTextMsg(msgStr: String, quoteModel: AbTalkingChatMsgQuoteModel?) {
    func flag(msgStr: String, quoteModel _: FirstSolidTitleTheoryModel?) {
        //: let temStr = msgStr.trimmingCharacters(in: CharacterSet.whitespacesAndNewlines)
        let temStr = msgStr.trimmingCharacters(in: CharacterSet.whitespacesAndNewlines)
        //: if temStr.count == 0 {
        if temStr.count == 0 {
            //: func__showStatusBarErrorMsg(showMsg: "Can't send blank message".localized)
            micromicronProcess(showMsg: (mainAnalyzeWritingId + "t sen" + String(main_homePingId.suffix(5)) + "nk me" + String(user_petitionMsg)).localized)
            //: return
            return
        }

        //: let uidStr = NSMutableString()
        let uidStr = NSMutableString()
        //: var jumps = [[String: String]]()
        var jumps = [[String: String]]()
        //: if msgInputView.atArr.count > 0 {
        if msgInputView.atArr.count > 0 { // 有@ 消息
            //: msgInputView.atArr.forEach { uid in
            msgInputView.atArr.forEach { uid in
                //: let dict = ["jumpKey": "user", "uid": uid]
                let dict = [String(bytes: showStepText.map{$0^239}, encoding: .utf8)!: (main_blankSayData.replacingOccurrences(of: "closed", with: "se")), String(bytes: appConnectionName.map{fitOut(recipe: $0)}, encoding: .utf8)!: uid]
                //: uidStr.append(uid+",")
                uidStr.append(uid + ",")
                //: jumps.append(dict)
                jumps.append(dict)
            }
            //: uidStr.deleteCharacters(in: NSRange(location: uidStr.length-1, length: 1))
            uidStr.deleteCharacters(in: NSRange(location: uidStr.length - 1, length: 1))
        }

        //: var msgInfo: [String: Any] = ["content": temStr, "msgType": "txt", "isGroup": "1"]
        var msgInfo: [String: Any] = [String(bytes: main_wifeFormat.map{evenActual(found: $0)}, encoding: .utf8)!: temStr, (String(notiSpreadMsg.suffix(7))): (constFrameworkData.replacingOccurrences(of: "bounce", with: "xt")), (String(main_shadeUsPath) + String(dataLogicalMessage)): "1"]
        //: if jumps.count > 0 {
        if jumps.count > 0 {
            //: msgInfo["jumps"] = jumps
            msgInfo[(user_assessmentStr.replacingOccurrences(of: "extra", with: "u"))] = jumps
        }

        // 接口参数
        //: var paramsDic: [String: Any] = ["message": temStr,
        var paramsDic: [String: Any] = [String(bytes: constScanMessage.reversed(), encoding: .utf8)!: temStr,
                                        //: "type": "TxtMsg",
                                        String(bytes: user_bodyPath.map{swaddlingClothes(premium: $0)}, encoding: .utf8)!: (String(appSortWelcomeBeginningMessage.suffix(6))),
                                        //: "roomId": self.infoModel.roomId]
                                        (String(userExistingMsg.suffix(6))): self.infoModel.roomId]
        //: if uidStr.length > 0 {
        if uidStr.length > 0 {
            //: paramsDic["toUid"] = uidStr
            paramsDic[(String(const_funName))] = uidStr
        }

        //: let dic = ["msgInfo": msgInfo, "params": paramsDic]
        let dic = [(String(mainTeemPath.prefix(7))): msgInfo, String(bytes: userOrganizationTitle.map{latSide(since: $0)}, encoding: .utf8)!: paramsDic]
        //: if let cellData = TUISocialChatManager.handleTXMessageSend(withExtral: dic, isSender: true, isTip: false) {
        if let cellData = CivilityReactiveCompatible.smartMailSenderBlockTipHandleTurn(withExtral: dic, isSender: true, isTip: false) {
            //: func__sendPriveteMsg(msgData: cellData, reSend: false)
            likeSend(msgData: cellData, reSend: false)
        }
    }

    /// 发送图片消息
    /// - Parameter img: 图片
    //: private func func__sendPictureMsg(img: UIImage) {
    private func down(img: UIImage) {
        //: let imagePath = ApartCellData.getChatImagePath(image: img)
        let imagePath = ApartCellData.method(image: img)
        //: let msgInfo: [String: Any] = ["contentType": "ImgMsg", "msgType": "img", "imageUri": imagePath]
        let msgInfo: [String: Any] = [String(bytes: main_medicRedCompareUrl.map{$0^195}, encoding: .utf8)!: (String(dataNativePath.prefix(6))), (String(notiSpreadMsg.suffix(7))): (showBridgeStr.replacingOccurrences(of: "pass", with: "g")), String(bytes: show_heartMsg.map{easternCream(stick: $0)}, encoding: .utf8)!: imagePath]
        // 接口参数
        //: let paramsDic: [String: Any] = ["pic": imagePath,
        let paramsDic: [String: Any] = [(showRegionStr.replacingOccurrences(of: "which", with: "ic")): imagePath,
                                        //: "type": "ImgMsg",
                                        String(bytes: user_bodyPath.map{swaddlingClothes(premium: $0)}, encoding: .utf8)!: (String(dataNativePath.prefix(6))),
                                        //: "roomId": self.infoModel.roomId]
                                        (String(userExistingMsg.suffix(6))): self.infoModel.roomId]
        //: let dic = ["msgInfo": msgInfo, "params": paramsDic]
        let dic = [(String(mainTeemPath.prefix(7))): msgInfo, String(bytes: userOrganizationTitle.map{latSide(since: $0)}, encoding: .utf8)!: paramsDic]
        //: if let cellData = TUISocialChatManager.handleTXMessageSend(withExtral: dic, isSender: true, isTip: false) {
        if let cellData = CivilityReactiveCompatible.smartMailSenderBlockTipHandleTurn(withExtral: dic, isSender: true, isTip: false) {
            //: func__sendPriveteMsg(msgData: cellData, reSend: false)
            likeSend(msgData: cellData, reSend: false)
        }
    }

    /// 发送消息
    /// - Parameters:
    ///   - msgData: 消息数据结构
    ///   - reSend: 是否重发【目前未使用该字段】
    //: private func func__sendPriveteMsg(msgData: AgoneCellData, reSend: Bool) {
    private func likeSend(msgData: AgoneCellData, reSend _: Bool) {
        //: let reachability = try? Reachability()
        let reachability = try? Reachability()
        //: if reachability?.connection == .unavailable {
        if reachability?.connection == .unavailable {
            //: func__showStatusBarErrorMsg(showMsg: kNetErrorMsg)
            micromicronProcess(showMsg: user_robotCameraName)
            //: return
            return
        }
        // 插入本地
        //: self.msgView.sendCustomMessage(msgData)
        self.msgView.element(msgData)
        // 处理请求参数
        //: var params: Dictionary<String, Any> = msgData.msgModel.params as! Dictionary<String, Any>
        var params: [String: Any] = msgData.msgModel.params as! [String: Any]
//        params["toUid"] = self.targetId
        //: if params.keys.contains("pic") { // 图片
        if params.keys.contains((showRegionStr.replacingOccurrences(of: "which", with: "ic"))) { // 图片
            //: let imageStr: String = params["pic"] as! String
            let imageStr: String = params[(showRegionStr.replacingOccurrences(of: "which", with: "ic"))] as! String
            //: do {
            do {
                //: let data: Data = try Data.init(contentsOf: URL.init(fileURLWithPath: imageStr))
                let data: Data = try Data(contentsOf: URL(fileURLWithPath: imageStr))
                //: params["pic"] = data
                params[(showRegionStr.replacingOccurrences(of: "which", with: "ic"))] = data
                //: } catch {
            } catch {
                //: printLog(message: error)
                printLog(message: error)
            }
        }

        // 发送消息接口
        //: TalkingChatRequestTool.req_SendRoomMsg(param: params, completion: { succeed, result, errorModel in
        PouchReactiveCompatible.beginningBeauty(param: params, completion: { succeed, result, errorModel in
            //: if !succeed {
            if !succeed {
                //: self.msgView.changeCustomMsg(msgData, msgId: nil, msgIncome: NSNumber(value: 0), status: .Msg_Status_Fail)
                self.msgView.outerNameStatus(msgData, modify: nil, storage: NSNumber(value: 0), commonStatus: .Msg_Status_Fail)

                //: self.dealSendMsgError(errorCode: errorModel!.errorCode, errorStr: errorModel!.errorMsg, isGift: true, isResend: false)
                self.melt(errorCode: errorModel!.errorCode, errorStr: errorModel!.errorMsg, isGift: true, isResend: false)
                //: return
                return
            }

            //: let dataDict = result as! [String: Any]
            let dataDict = result as! [String: Any]
            //: let msgInfo = dataDict["msgInfo"] as! [String: Any]
            let msgInfo = dataDict[(String(mainTeemPath.prefix(7)))] as! [String: Any]

            // 更新本地消息信息
            //: self.msgView.changeCustomMsg(msgData, msgId: msgInfo["msgId"] as? String, msgIncome: NSNumber(value: 0), status: .Msg_Status_Succ)
            self.msgView.outerNameStatus(msgData, modify: msgInfo[(String(k_mmComplexTitle.prefix(5)))] as? String, storage: NSNumber(value: 0), commonStatus: .Msg_Status_Succ)
            
            //: if dataDict.keys.contains("totalMfCoin") {
            if dataDict.keys.contains((String(app_rawPath) + String(notiTurnId.suffix(6)))) {
                //: let totalMfCoin: NSNumber = dataDict["totalMfCoin"] as! NSNumber
                let totalMfCoin: NSNumber = dataDict[(String(app_rawPath) + String(notiTurnId.suffix(6)))] as! NSNumber
                //: FacultyReactiveCompatible.share.loginUserMode.mf_coin = totalMfCoin.stringValue
                FacultyReactiveCompatible.share.loginUserMode.mf_coin = totalMfCoin.stringValue
            }
            //: })
        })
    }

    /// 处理发送失败情况
    //: private func dealSendMsgError(errorCode: Int, errorStr: String, isGift: Bool, isResend: Bool) {
    private func melt(errorCode: Int, errorStr: String, isGift _: Bool, isResend _: Bool) {
        //: if errorCode == 50203 {
        if errorCode == 50203 {
            //: guard FacultyReactiveCompatible.share.loginUserMode.status != 1 else {
            guard FacultyReactiveCompatible.share.loginUserMode.status != 1 else {
                //: if !errorStr.isEmptyString {
                if !errorStr.isEmptyString {
                    //: func__showStatusBarErrorMsg(showMsg: errorStr)
                    micromicronProcess(showMsg: errorStr)
                }
                //: return
                return
            }

            //: SmartThen.share.func__jumpToWebRecharge(clickEvent: clickSendingiftsButton, sufficient: false)
            SmartThen.share.bilge(clickEvent: user_turnName, sufficient: false)
            //: msgInputView.giftView.dismissView()
            msgInputView.giftView.conCapture()
            //: insertTipMsg_Nobalance(tipStr: errorStr)
            tableDHote(tipStr: errorStr)
            //: }else if errorCode == 10888 {
        } else if errorCode == 10888 {
            //: let view = TalkingLiveVipSubscribePopView.init(frame: CGRect(x: 0, y: 0, width: ScreenWidth, height: ScreenHeight))
            let view = FootboardThen(frame: CGRect(x: 0, y: 0, width: main_assetUrl, height: kTopUrl))
            //: view.show()
            view.shadowShow()
            //: } else {
        } else {
            //: if !errorStr.isEmptyString {
            if !errorStr.isEmptyString {
                //: func__showStatusBarErrorMsg(showMsg: errorStr)
                micromicronProcess(showMsg: errorStr)
            }
        }
    }
}

// MARK: - 输入框

//: extension TalkingGroupChatController: ChatInputViewDelegate {
extension UncompressChatController: IonSignalViewDelegate {
    //: func func__askforGiftMsg(giftModel: TalkingRoomGiftModel, giftNum: String) {
    func profileNum(giftModel _: ReportedMeasurable, giftNum _: String) {}

    /// 输入框输入内容停顿超过1秒
    //: func func_inputContentIntervalOneSecond() {}
    func spell() {}

    /// 输入框@ 在线列表用户
    //: func func__selectMentionUser(range: NSRange) {
    func degree(range: NSRange) {
        //: let vc = TalkingChatRoomOnlineListController()
        let vc = IonViewController()
        //: vc.roomID = self.infoModel.roomId
        vc.roomID = self.infoModel.roomId
        //: vc.titleType = .callNumber
        vc.titleType = .callNumber
        //: vc.selectedBlock = {[weak self] dict in
        vc.selectedBlock = { [weak self] dict in
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: self.atFlag = true
            self.atFlag = true
            //: self.msgInputView.receiveAtInfo(range: range, dict: dict)
            self.msgInputView.springDict(range: range, dict: dict)
            //: self.navigationController?.popViewController(animated: true)
            self.navigationController?.popViewController(animated: true)
        }
        //: self.navigationController?.pushViewController(vc, animated: true)
        self.navigationController?.pushViewController(vc, animated: true)
    }

    //: func func__selectMoreFunAction(actionType: MoreActionType) {
    func componentBy(actionType: BorderControlAdditiveArithmetic) {
        //: switch actionType {
        switch actionType {
        //: case .Photo:
        case .Photo:
            //: func__choicePhotos()
            photos()

        //: default:
        default:
            //: break
            break
        }
    }

    //: func func__sendGiftMsg(giftModel: TalkingRoomGiftModel, giftNum: String) {}
    func pointTrack(giftModel _: ReportedMeasurable, giftNum _: String) {}

    //: func func__sendChatRoomGiftMsg(giftModel: TalkingRoomGiftModel, giftNum: String, model: TalkingChatRoomMemberModel) {
    func copernicanSystemA(giftModel: ReportedMeasurable, giftNum: String, model: RocketTransformable) {
        //: let reachability = try? Reachability()
        let reachability = try? Reachability()
        //: if reachability?.connection == .unavailable {
        if reachability?.connection == .unavailable {
            //: func__showStatusBarErrorMsg(showMsg: kNetErrorMsg)
            micromicronProcess(showMsg: user_robotCameraName)
            //: return
            return
        }
        //: var params: Dictionary<String, Any> = [:]
        var params: [String: Any] = [:]
        //: params["toUid"] =  model.uid
        params[(String(const_funName))] = model.uid
        //: params["giftId"] = giftModel.gid
        params[(String(constShotName))] = giftModel.gid
        //: params["giftNum"] = giftNum
        params[(String(notiSimultaneouslyHiddenSpecialMsg.prefix(7)))] = giftNum
        //: params["roomId"] = infoModel.roomId
        params[(String(userExistingMsg.suffix(6)))] = infoModel.roomId
        //: if giftModel.pkgItemsetId != nil {
        if giftModel.pkgItemsetId != nil {
            //: params["pkgItemsetId"] = giftModel.pkgItemsetId
            params[(String(app_millMsg.suffix(6)) + String(show_requireUrl.suffix(6)))] = giftModel.pkgItemsetId
        }

        // 送礼接口
        //: TalkingChatRequestTool.req_SendGiftChatRoom(param: params, completion: { succeed, result, errorModel in
        PouchReactiveCompatible.consequence(param: params, completion: { succeed, result, errorModel in
            //: guard succeed else {
            guard succeed else {
                //: self.dealSendMsgError(errorCode: errorModel!.errorCode, errorStr: errorModel!.errorMsg, isGift: true, isResend: false)
                self.melt(errorCode: errorModel!.errorCode, errorStr: errorModel!.errorMsg, isGift: true, isResend: false)
                //: return
                return
            }

            // 更新本地礼物背包
            //: if giftModel.pkgItemsetId != nil {
            if giftModel.pkgItemsetId != nil {
                //: self.msgInputView.giftView.bags_removeGiftPackage(pkgItemsetId: giftModel.pkgItemsetId!, num: Int(giftNum) ?? 0)
                self.msgInputView.giftView.punish(pkgItemsetId: giftModel.pkgItemsetId!, num: Int(giftNum) ?? 0)
            }
            //: self.func__insertGiftMessageWithExtral(extral: result as! Dictionary<String, Any?>, model: model, isFromInputView: true)
            self.memoryPathRecent(extral: result as! [String: Any?], model: model, isFromInputView: true)
            //: })
        })
    }

    //: func heightToBottomChanged(heightToBottom: CGFloat) {
    func low(heightToBottom: CGFloat) {
        //: msgInputView.snp.updateConstraints { make in
        msgInputView.snp.updateConstraints { make in
            //: make.bottom.equalTo(self.view).offset(-heightToBottom)
            make.bottom.equalTo(self.view).offset(-heightToBottom)
        }
        //: someoneView.snp.updateConstraints { make in
        someoneView.snp.updateConstraints { make in
            //: make.bottom.equalTo(msgInputView.snp.top).offset(-103)
            make.bottom.equalTo(msgInputView.snp.top).offset(-103)
        }
        //: DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 0.3) {
        DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 0.3) {
            //: self.msgView.parent(true)
            self.msgView.parent(true)
        }
    }

    //: func inputViewHeightChanged(height: CGFloat) {
    func build(height: CGFloat) {
        //: self.msgInputView.snp.updateConstraints { make in
        self.msgInputView.snp.updateConstraints { make in
            //: make.height.equalTo(height)
            make.height.equalTo(height)
        }
        //: self.view.needsUpdateConstraints()
        self.view.needsUpdateConstraints()
        //: self.view.updateConstraintsIfNeeded()
        self.view.updateConstraintsIfNeeded()
        //: UIView.animate(withDuration: 0.3, animations: {
        UIView.animate(withDuration: 0.3, animations: {
            //: self.view.layoutIfNeeded()
            self.view.layoutIfNeeded()
            //: })
        })
        //: DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 0.3) {
        DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 0.3) {
            //: self.msgView.parent(true)
            self.msgView.parent(true)
        }
    }

    //: @objc override func func__TapGestureRecognizer(_ tap: UITapGestureRecognizer) {
    @objc override func memory(_ tap: UITapGestureRecognizer) {
        //: super.func__TapGestureRecognizer(tap)
        super.memory(tap)
    }

    //: func func__starRecordBtnDown() {}
    func pointThread() {}

    //: func func__sendVoiceMsg(voicePath: String, voiceTime: Int) {}
    func lastWeekly(voicePath _: String, voiceTime _: Int) {}
}

// MARK: - 选择图片

//: extension TalkingGroupChatController: UIImagePickerControllerDelegate, UINavigationControllerDelegate {
extension UncompressChatController: UIImagePickerControllerDelegate, UINavigationControllerDelegate {
    //: private func func__choicePhotos() {
    private func photos() {
        //: TalkingPermissionTool.func__openAlbumServiceWithBlock(true) { (isOpen: Bool) in
        ActivityReactiveCompatible.oddCharm(true) { (isOpen: Bool) in
            //: if isOpen {
            if isOpen {
                //: let vc = TalkingImagePickTool.initImagePickerVc(maxCount: 1, allowPhoto: true, allowVideo: false)
                let vc = ThreeToolReactiveCompatible.interval(maxCount: 1, allowPhoto: true, allowVideo: false)
                //: vc.modalPresentationStyle = .fullScreen
                vc.modalPresentationStyle = .fullScreen
                //: SmartThen.share.func__getCurrentActivityVC()?.present(vc, animated: true)
                SmartThen.share.stepFind()?.present(vc, animated: true)
                //: vc.didFinishPickingPhotosHandle = { [weak self] ( photos: Array<UIImage>?,
                vc.didFinishPickingPhotosHandle = { [weak self] (photos: [UIImage]?,
                                                                 //: assets: Array<Any>?,
                                                                 _: [Any]?,
                                                                 //: isSelectOriginalPhoto: Bool?)  in
                                                                 _: Bool?) in
                        //: guard let self = self else {return}
                        guard let self = self else { return }
                        //: if photos!.count > 0 {
                        if photos!.count > 0 {
                            //: self.func__sendPictureMsg(img: photos![0])
                            self.down(img: photos![0])
                        }
                }
            }
        }
    }

    //: private func func__camaraPhotos() {
    private func allow() {
        //: TalkingPermissionTool.func__openCaptureDeviceServiceWithBlock(true) { (isOpen: Bool) in
        ActivityReactiveCompatible.dataConverter(true) { (isOpen: Bool) in
            //: if isOpen {
            if isOpen {
                //: let imagePicker = UIImagePickerController()
                let imagePicker = UIImagePickerController()
                //: imagePicker.sourceType = .camera
                imagePicker.sourceType = .camera
                //: imagePicker.delegate = self
                imagePicker.delegate = self
                //: self.present(imagePicker, animated: true, completion: {})
                self.present(imagePicker, animated: true, completion: {})
            }
        }
    }

    //: func imagePickerController(_ picker: UIImagePickerController, didFinishPickingMediaWithInfo info: [UIImagePickerController.InfoKey: Any]) {
    func imagePickerController(_: UIImagePickerController, didFinishPickingMediaWithInfo info: [UIImagePickerController.InfoKey: Any]) {
        //: self.dismiss(animated: true) {
        self.dismiss(animated: true) {
            //: if let photo = info[UIImagePickerController.InfoKey.originalImage] as! UIImage? {
            if let photo = info[UIImagePickerController.InfoKey.originalImage] as! UIImage? {
                //: self.func__sendPictureMsg(img: photo)
                self.down(img: photo)
            }
        }
    }

    //: func imagePickerControllerDidCancel(_ picker: UIImagePickerController) {
    func imagePickerControllerDidCancel(_: UIImagePickerController) {
        //: self.dismiss(animated: true, completion: nil)
        self.dismiss(animated: true, completion: nil)
    }
}

// MARK: - 处理@ 消息

//: extension TalkingGroupChatController {
extension UncompressChatController {
    /// 接收到群聊@ 消息
    /// - Parameter info: @消息数据
    //: func handleMentionMsg(info: [String: Any]) {
    func handleStack(info: [String: Any]) {
        //: DispatchQueue.main.async {
        DispatchQueue.main.async {
            //: guard info["roomId"] as! String == self.conversationData.groupID else { return }
            guard info[(String(userExistingMsg.suffix(6)))] as! String == self.conversationData.groupID else { return }
            //: if self.mentionInfoArr.count >= 5 {
            if self.mentionInfoArr.count >= 5 { // 只保留最新5条@ 消息
                //: self.mentionInfoArr.removeFirst()
                self.mentionInfoArr.removeFirst()
            }
            //: self.mentionInfoArr.append(info)
            self.mentionInfoArr.append(info)
            //: self.someoneView.isHidden = self.mentionInfoArr.count > 0 ? false:true
            self.someoneView.isHidden = self.mentionInfoArr.count > 0 ? false : true
        }
    }
}

// MARK: - 提示消息

//: extension TalkingGroupChatController {
extension UncompressChatController {
    //: func func__TipsNotification() {
    func insertNotification() {
        //: NotificationCenter.default.addObserver(self,
        NotificationCenter.default.addObserver(self,
                                               //: selector: #selector(insertTipsNotif(notification:)),
                                               selector: #selector(exMark(notification:)),
                                               //: name: CHAT_TIPS_TEXT_NOTIFICATION,
                                               name: noti_trustAgeCameraMsg,
                                               //: object: nil)
                                               object: nil)
    }

    //: @objc func insertTipsNotif(notification: NSNotification) {
    @objc func exMark(notification: NSNotification) {
        //: let userinfo = notification.userInfo as! [String: AnyObject]
        let userinfo = notification.userInfo as! [String: AnyObject]
        //: let celldata: TalkingChatTipsMsgCellData  = userinfo["msg"] as! TalkingChatTipsMsgCellData
        let celldata: AgoThen = userinfo[(k_shirtStr.replacingOccurrences(of: "vid", with: "g"))] as! AgoThen

        //: self.msgView.uiMsgs.add(celldata)
        self.msgView.uiMsgs.add(celldata)
        //: self.msgView.tableView.reloadData()
        self.msgView.tableView.reloadData()
        //: self.msgView.parent(true)
        self.msgView.parent(true)
    }

    /// 插入提示消息
    /// - Parameters:
    ///   - tipStr: 提示消息文本
    ///   - needInsertLocal: 是否需要插入本地数据库（默认：是）
    //: func insertTipMsg_Nobalance(tipStr: String, needInsertLocal: Bool = true) {
    func tableDHote(tipStr: String, needInsertLocal: Bool = true) {
        //: let msgInfo = ["content": tipStr, "msgType": "tips"]
        let msgInfo = [String(bytes: main_wifeFormat.map{evenActual(found: $0)}, encoding: .utf8)!: tipStr, (String(notiSpreadMsg.suffix(7))): (dataExceptKey.replacingOccurrences(of: "outlet", with: "s"))]
        //: var message: AgoneCellData?
        var message: AgoneCellData?
        //: if needInsertLocal == true {
        if needInsertLocal == true { // 需要插入本地
            //: } else {
        } else {
            //: message = TUISocialChatManager.handleTXMessageSend(withExtral: msgInfo, isSender: true, isTip: true)
            message = CivilityReactiveCompatible.smartMailSenderBlockTipHandleTurn(withExtral: msgInfo, isSender: true, isTip: true)
        }

        //: guard let message = message else { return }
        guard let message = message else { return }
        //: self.msgView.uiMsgs.add(message)
        self.msgView.uiMsgs.add(message)
        //: self.msgView.tableView.reloadData()
        self.msgView.tableView.reloadData()
        //: self.msgView.parent(true)
        self.msgView.parent(true)
    }

    /// 公共聊天室插入系统提示消息
    //: func func__chatRoom_insertTipMsg() {
    func implantMsg() {
        // 延时，内存插入
        //: tipsTimer = Timer.scheduledTimer(withTimeInterval: 5, repeats: false, block: {[weak self] time in
        tipsTimer = Timer.scheduledTimer(withTimeInterval: 5, repeats: false, block: { [weak self] _ in
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: guard FacultyReactiveCompatible.share.appUserConfigMode.rTips.count > 0 else { return }
            guard FacultyReactiveCompatible.share.appUserConfigMode.rTips.count > 0 else { return }
            //: self.insertTipMsg_Nobalance(tipStr: FacultyReactiveCompatible.share.appUserConfigMode.rTips, needInsertLocal: false)
            self.tableDHote(tipStr: FacultyReactiveCompatible.share.appUserConfigMode.rTips, needInsertLocal: false)
            //: })
        })
    }
}

// MARK: - 动效

//: extension TalkingGroupChatController {
extension UncompressChatController {
    /// 插入礼物消息，播放动效
    /// - Parameters:
    ///   - extral: 送礼接口返回结果
    ///   - isFromInputView: 是否来自输入框送礼入口
    //: func func__insertGiftMessageWithExtral(extral: Dictionary<String, Any?>, model: TalkingChatRoomMemberModel, isFromInputView: Bool) {
    func memoryPathRecent(extral: [String: Any?], model: RocketTransformable, isFromInputView: Bool) {
        //: var extralInfo = extral
        var extralInfo = extral
        //: var msgInfoDic: Dictionary<String, Any> = extralInfo["msgInfo"] as! Dictionary<String, Any>
        var msgInfoDic: [String: Any] = extralInfo[(String(mainTeemPath.prefix(7)))] as! [String: Any]
        /// 用于本地判断celldata类型
        //: msgInfoDic["isGroup"] = "1"
        msgInfoDic[(String(main_shadeUsPath) + String(dataLogicalMessage))] = "1"
        //: msgInfoDic["msgType"] = "gift"
        msgInfoDic[(String(notiSpreadMsg.suffix(7)))] = (k_prepareConstructId.replacingOccurrences(of: "escape", with: "if"))
        //: extralInfo["msgInfo"] = msgInfoDic
        extralInfo[(String(mainTeemPath.prefix(7)))] = msgInfoDic

        //: var toUser = ["uid": model.uid,
        var toUser = [String(bytes: appConnectionName.map{fitOut(recipe: $0)}, encoding: .utf8)!: model.uid,
                      //: "nickname": model.nickname,
                      (String(mainRevenueUrl) + String(appLengthKey)): model.nickname,
                      //: "headPic": model.headPic]
                      String(bytes: main_coordinatorTitle.map{blankEven(sound: $0)}, encoding: .utf8)!: model.headPic]

        //: if model.nickname == "All Numbers".localized {
        if model.nickname == (String(app_bigPath.prefix(4)) + "Number" + user_ballOptionValue.lowercased()).localized {
            //: if extralInfo.keys.contains("gift") != false {
            if extralInfo.keys.contains((k_prepareConstructId.replacingOccurrences(of: "escape", with: "if"))) != false {
                //: let giftdic = extralInfo["gift"] as! Dictionary<String, Any>
                let giftdic = extralInfo[(k_prepareConstructId.replacingOccurrences(of: "escape", with: "if"))] as! [String: Any]
                //: toUser["nickname"] = "\(giftdic["sendNum"] ?? "1")"
                toUser[(String(mainRevenueUrl) + String(appLengthKey))] = "\(giftdic[(String(mainYourselfUrl.prefix(7)))] ?? "1")"
            }
        }
        //: extralInfo["toUser"] = toUser
        extralInfo[(String(data_answerStr.suffix(6)))] = toUser

        //: let cellData = TUISocialChatManager.handleTXMessageSend(withExtral: extralInfo as [String: Any], isSender: true, isTip: false)
        let cellData = CivilityReactiveCompatible.smartMailSenderBlockTipHandleTurn(withExtral: extralInfo as [String: Any], isSender: true, isTip: false)
        //: self.msgView.sendCustomMessage(cellData)
        self.msgView.element(cellData)
        //: var msgIncome: NSNumber = NSNumber.init(floatLiteral: 0)
        var msgIncome = NSNumber(floatLiteral: 0)
        //: if msgInfoDic.keys.contains("msgIncome") {
        if msgInfoDic.keys.contains((String(user_relateExName.suffix(9)))) {
            //: msgIncome = msgInfoDic["msgIncome"] as! NSNumber
            msgIncome = msgInfoDic[(String(user_relateExName.suffix(9)))] as! NSNumber
        }
        //: self.msgView.changeCustomMsg(cellData, msgId: msgInfoDic["msgId"] as? String, msgIncome: msgIncome, status: .Msg_Status_Succ)
        self.msgView.outerNameStatus(cellData, modify: msgInfoDic[(String(k_mmComplexTitle.prefix(5)))] as? String, storage: msgIncome, commonStatus: .Msg_Status_Succ)

        //: sendGiftByMeToEfffecct(giftDic: extral as [String: Any], model: model)
        controlStock(giftDic: extral as [String: Any], model: model)
        //: if extralInfo.keys.contains("totalMfCoin") {
        if extralInfo.keys.contains((String(app_rawPath) + String(notiTurnId.suffix(6)))) {
            //: let totalMfCoin: NSNumber = extralInfo["totalMfCoin"] as! NSNumber
            let totalMfCoin: NSNumber = extralInfo[(String(app_rawPath) + String(notiTurnId.suffix(6)))] as! NSNumber
            //: FacultyReactiveCompatible.share.loginUserMode.mf_coin = totalMfCoin.stringValue
            FacultyReactiveCompatible.share.loginUserMode.mf_coin = totalMfCoin.stringValue
        }
        //: if isFromInputView {
        if isFromInputView {
            //: msgInputView.giftView.updateGiftInfo(needReload: false, mf_coin: FacultyReactiveCompatible.share.loginUserMode.mf_coin)
            msgInputView.giftView.pictureUp(needReload: false, mf_coin: FacultyReactiveCompatible.share.loginUserMode.mf_coin)
        }
        //: msgInputView.giftView.saveRecentMembers()
        msgInputView.giftView.page()
    }

    //: func initCachEffectData() {
    func ballBreaker() {
        //: giftEffectView.initCachAnimatData(tagetID: infoModel.roomId)
        giftEffectView.containment(tagetID: infoModel.roomId)
    }

    //: func sendGiftByMeToEfffecct(giftDic: Dictionary<String, Any>, model: TalkingChatRoomMemberModel) {
    func controlStock(giftDic: [String: Any], model: RocketTransformable) {
        //: guard let giftModel = giftEffectView.chatRoomDisposeReceiveGiftMsg(giftMessageDic: (giftDic as NSDictionary) as! Dictionary<String, Any>, model: model) else { return }
        guard let giftModel = giftEffectView.pitch(giftMessageDic: (giftDic as NSDictionary) as! [String: Any], model: model) else { return }
        //: if giftTrackView.giftSendItem.count == 0 {
        if giftTrackView.giftSendItem.count == 0 {
            //: let arrM = NSMutableArray.init()
            let arrM = NSMutableArray()
            //: for model in TalkingChatGiftManager.share.getSendingItems() {
            for model in RecognizeReactiveCompatible.share.silken() {
                //: arrM.add(model)
                arrM.add(model)
            }
            //: giftTrackView.giftSendItem = arrM as! [TalkingGiftNumArrModel]
            giftTrackView.giftSendItem = arrM as! [EaseModelType]
        }
        //: giftTrackView.func__didReceiveGiftMsgModel(model: giftModel)
        giftTrackView.mainWith(model: giftModel)

        //: giftEffectView.dealGiftAnimatData(dict: giftDic)
        giftEffectView.query(dict: giftDic)
    }

    //: @objc func addGiftEffectModelArr(giftArr: Array<Any>) {
    @objc func downPush(giftArr: [Any]) {
        //: if giftTrackView.giftSendItem.count == 0 {
        if giftTrackView.giftSendItem.count == 0 {
            //: let arrM = NSMutableArray.init()
            let arrM = NSMutableArray()
            //: for model in TalkingChatGiftManager.share.getSendingItems() {
            for model in RecognizeReactiveCompatible.share.silken() {
                //: arrM.add(model)
                arrM.add(model)
            }
            //: giftTrackView.giftSendItem = arrM as! [TalkingGiftNumArrModel]
            giftTrackView.giftSendItem = arrM as! [EaseModelType]
        }
        //: for i in 0..<giftArr.count {
        for i in 0 ..< giftArr.count {
            //: let giftModel = giftArr[i]
            let giftModel = giftArr[i]
            //: giftTrackView.func__didReceiveGiftMsgModel(model: giftModel as! TalkingGiftMsgAnimatModel)
            giftTrackView.mainWith(model: giftModel as! ExtentTransformable)
        }

        //: giftEffectView.addGiftAnimatModelArr(modelArr: giftArr)
        giftEffectView.close(modelArr: giftArr)
    }
}

// MARK: - Layout

//: extension TalkingGroupChatController {
extension UncompressChatController {
    /// 创建UI
    //: private func createUI() {
    private func omit() {
        //: self.addChild(msgView)
        self.addChild(msgView)
        //: view.addSubview(msgView.view)
        view.addSubview(msgView.view)
        //: view.addSubview(someoneView)
        view.addSubview(someoneView)
        //: view.addSubview(giftEffectView)
        view.addSubview(giftEffectView)
        //: view.insertSubview(giftTrackView, belowSubview: giftEffectView)
        view.insertSubview(giftTrackView, belowSubview: giftEffectView)
        //: view.addSubview(msgInputView)
        view.addSubview(msgInputView)
        //: view.addSubview(navView)
        view.addSubview(navView)
        //: msgView.targetID = self.conversationData.groupID
        msgView.targetID = self.conversationData.groupID
    }

    /// 约束
    //: private func createUIConstraints() {
    private func regain() {
        //: navView.snp.makeConstraints { make in
        navView.snp.makeConstraints { make in
            //: make.leading.trailing.top.equalTo(view)
            make.leading.trailing.top.equalTo(view)
            //: make.height.equalTo(StatusBarNavigationBarHeight)
            make.height.equalTo(main_sendData)
        }
        //: msgInputView.snp.makeConstraints { make in
        msgInputView.snp.makeConstraints { make in
            //: make.leading.trailing.bottom.equalTo(view)
            make.leading.trailing.bottom.equalTo(view)
            //: make.height.equalTo(msgInputView.BackViewHeight)
            make.height.equalTo(msgInputView.BackViewHeight)
        }
        //: msgView.view.snp.makeConstraints { make in
        msgView.view.snp.makeConstraints { make in
            //: make.leading.trailing.equalTo(view)
            make.leading.trailing.equalTo(view)
            //: make.top.equalTo(StatusBarNavigationBarHeight)
            make.top.equalTo(main_sendData)
            //: make.bottom.equalTo(msgInputView.snp.top)
            make.bottom.equalTo(msgInputView.snp.top)
        }
        //: someoneView.snp.makeConstraints { make in
        someoneView.snp.makeConstraints { make in
            //: make.bottom.equalTo(msgInputView.snp.top).offset(-103)
            make.bottom.equalTo(msgInputView.snp.top).offset(-103)
            //: make.trailing.equalToSuperview()
            make.trailing.equalToSuperview()
            //: make.size.equalTo(CGSize(width: 130, height: 37))
            make.size.equalTo(CGSize(width: 130, height: 37))
        }
        //: giftEffectView.snp.makeConstraints { make in
        giftEffectView.snp.makeConstraints { make in
            //: make.leading.trailing.top.bottom.equalTo(view)
            make.leading.trailing.top.bottom.equalTo(view)
        }
        //: giftTrackView.snp.makeConstraints { make in
        giftTrackView.snp.makeConstraints { make in
            //: make.leading.equalTo(view)
            make.leading.equalTo(view)
            //: make.height.equalTo(180)
            make.height.equalTo(180)
            //: make.bottom.equalTo(-actualHeight(h: 220)-kDeviceSafeBottomHeight)
            make.bottom.equalTo(-actualHeight(h: 220) - data_cornerMessage)
        }
    }

    /// 事件绑定
    //: func bindInteraction() {
    func visualQuantityeraction() {
        // 返回
        //: navView.backButton.rx.tap.subscribe(onNext: { [weak self] in
        navView.backButton.rx.tap.subscribe(onNext: { [weak self] in
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: self.navigationController?.popViewController(animated: true)
            self.navigationController?.popViewController(animated: true)
            //: }).disposed(by: disposeBag)
        }).disposed(by: disposeBag)

        // 在线列表
        //: navView.onlineListBtn.rx.tap.subscribe(onNext: { [weak self] in
        navView.onlineListBtn.rx.tap.subscribe(onNext: { [weak self] in
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: let vc = TalkingChatRoomOnlineListController()
            let vc = IonViewController()
            //: vc.roomID = self.infoModel.roomId
            vc.roomID = self.infoModel.roomId
            //: vc.titleType = .normal
            vc.titleType = .normal
            //: self.navigationController?.pushViewController(vc, animated: true)
            self.navigationController?.pushViewController(vc, animated: true)
            //: }).disposed(by: disposeBag)
        }).disposed(by: disposeBag)

        // 群聊someone@ me 点击事件
        //: someoneView.mentionBtn.rx.tap
        someoneView.mentionBtn.rx.tap
            //: .throttle(.milliseconds(1000), scheduler: MainScheduler.instance)
            .throttle(.milliseconds(1000), scheduler: MainScheduler.instance)
            //: .subscribe(onNext: { [weak self] in
            .subscribe(onNext: { [weak self] in
                //: guard let self = self else { return }
                guard let self = self else { return }
                // 找到当前消息，滚动到指定位置
                //: guard let dict = self.mentionInfoArr.first else { return }
                guard let dict = self.mentionInfoArr.first else { return }
                //: for (index, cellData) in self.msgView.uiMsgs.enumerated() {
                for (index, cellData) in self.msgView.uiMsgs.enumerated() {
                    //: if (cellData as! AgoneCellData).isKind(of: WillCellData.self) {
                    if (cellData as! AgoneCellData).isKind(of: WillCellData.self) {
                        //: let groupCellData = cellData as! WillCellData
                        let groupCellData = cellData as! WillCellData
                        //: if groupCellData.msgID == dict["msgId"] as! String {
                        if groupCellData.msgID == dict[(String(k_mmComplexTitle.prefix(5)))] as! String {
                            //: self.msgView.tableView.scroll(toRow: UInt(index), inSection: 0, at: .bottom, animated: true)
                            self.msgView.tableView.scroll(toRow: UInt(index), inSection: 0, at: .bottom, animated: true)
                            //: self.mentionInfoArr.removeFirst()
                            self.mentionInfoArr.removeFirst()
                            //: self.someoneView.isHidden = self.mentionInfoArr.count > 0 ? false:true
                            self.someoneView.isHidden = self.mentionInfoArr.count > 0 ? false : true
                        }
                    }
                }
                //: }).disposed(by: disposeBag)
            }).disposed(by: disposeBag)

        // 被挤下线时，退出群聊
        //: IonV2Listener.shared.rx
        IonV2Listener.shared.rx
            //: .observeWeakly(Bool.self, "isConnection")
            .observeWeakly(Bool.self, (String(const_greetUrl.prefix(5)) + data_remoteContent.replacingOccurrences(of: "spectrum", with: "n") + mainThereId.lowercased()))
            //: .subscribe(onNext: { [weak self] (value) in
            .subscribe(onNext: { [weak self] value in
                //: guard let self = self else { return }
                guard let self = self else { return }
                //: if value == false {
                if value == false {
                    //: self.navigationController?.popViewController(animated: true)
                    self.navigationController?.popViewController(animated: true)
                }
                //: }).disposed(by: disposeBag)
            }).disposed(by: disposeBag)
    }
}
