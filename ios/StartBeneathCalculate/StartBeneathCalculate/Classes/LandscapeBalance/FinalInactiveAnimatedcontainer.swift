
//: Declare String Begin

/*: "init(coder:) has not been implemented" :*/
fileprivate let const_fatalUrl:[UInt8] = [0x16,0x1b,0x16,0x21,0xd5,0x10,0x1c,0x11,0x12,0x1f,0xe7,0xd6,0xcd,0x15,0xe,0x20,0xcd,0x1b,0x1c,0x21,0xcd,0xf,0x12,0x12,0x1b,0xcd,0x16,0x1a,0x1d,0x19,0x12,0x1a,0x12,0x1b,0x21,0x12,0x11]

fileprivate func returnExpected(compute num: UInt8) -> UInt8 {
    let value = Int(num) - 173
    if value < 0 {
        return UInt8(value + 256)
    } else {
        return UInt8(value)
    }
}

/*: "uid" :*/
fileprivate let app_rootWealthyPath:[UInt8] = [0x9d,0x81,0x8c]

private func justNet(narrow num: UInt8) -> UInt8 {
    return num ^ 232
}

/*: "headPic" :*/
fileprivate let mainConfirmId:String = "headPiccant injury"

/*: "nickname" :*/
fileprivate let kSpellId:String = "giving"
fileprivate let const_deviceId:String = "cklastame"

/*: "birthday" :*/
fileprivate let main_handMessage:[Character] = ["b","i","r","t","h","d","a"]
fileprivate let kSeaTitle:[Character] = ["y"]

/*: "videoUrl" :*/
fileprivate let constBeatText:String = "pass aspect readervideoU"
fileprivate let notiRoutineFormat:[Character] = ["r","l"]

/*: "location" :*/
fileprivate let const_mentionMessage:String = "locput"
fileprivate let app_rawText:[Character] = ["i","o","n"]

/*: "cmd" :*/
fileprivate let appItemMessage:[UInt8] = [0xf6,0xf8,0xf1]

/*: "refuseCall" :*/
fileprivate let show_seriesBoneMessage:[Character] = ["r","e","f","u","s","e","C","a","l"]
fileprivate let mainTransferPath:String = "into"

/*: "data" :*/
fileprivate let const_saveId:[UInt8] = [0x61,0x74,0x61,0x64]

/*: "Call interruption" :*/
fileprivate let data_paraName:String = "clot maximum multi soundCall"
fileprivate let userChannelWarnSituationText:String = "rruautomatic"

/*: "stopCall" :*/
fileprivate let userMilkUrl:String = "inner tun peer take phasestopCall"

/*: "logId" :*/
fileprivate let noti_boltFormat:[UInt8] = [0x6,0x9,0x1,0xe3,0xfe]

fileprivate func anyCenter(panel num: UInt8) -> UInt8 {
    let value = Int(num) - 154
    if value < 0 {
        return UInt8(value + 256)
    } else {
        return UInt8(value)
    }
}

/*: "cancelCall" :*/
fileprivate let const_givingDatabaseValue:[Character] = ["c","a","n","c","e"]
fileprivate let app_expressionPath:String = "lCallcan biological"

/*: "No answer" :*/
fileprivate let k_shirtFormat:[Character] = ["N","o"]
fileprivate let showSecretId:String = " answeryield top depth red"

/*: "duration" :*/
fileprivate let main_fileId:String = "DUR"
fileprivate let constClearlyReplacementFormat:[Character] = ["a","t","i","o","n"]

/*: "She has hung up" :*/
fileprivate let showCurMessage:String = "premium guard destroy your openShe ha"
fileprivate let data_markValue:String = "g upremote military ingredient slip"

/*: "cancelSex" :*/
fileprivate let data_indicatorUpperPath:String = "ccantc"

/*: "He has hung up" :*/
fileprivate let main_backgroundPath:[Character] = ["H","e"," ","h","a","s"," ","h","u","n","g"," ","u","p"]

/*: "endType" :*/
fileprivate let show_rugPath:String = "slown"
fileprivate let appCombineBoardData:String = "attention albumdType"

/*: "stopSex" :*/
fileprivate let const_reserveContent:String = "mode multi city sectionstopSex"

/*: "Insufficient gold coins, call has ended" :*/
fileprivate let show_recommendUrl:[UInt8] = [0xad,0xd2,0xd7,0xd9,0xca,0xca,0xcd,0xc7,0xcd,0xc9,0xd2,0xd8,0x84,0xcb,0xd3,0xd0,0xc8,0x84,0xc7,0xd3,0xcd,0xd2,0xd7,0x90,0x84,0xc7,0xc5,0xd0,0xd0,0x84,0xcc,0xc5,0xd7,0x84,0xc9,0xd2,0xc8,0xc9,0xc8]

fileprivate func lessSuspend(fore num: UInt8) -> UInt8 {
    let value = Int(num) - 100
    if value < 0 {
        return UInt8(value + 256)
    } else {
        return UInt8(value)
    }
}

/*: "The other hung up the phone" :*/
fileprivate let notiEffectWeMsg:[Character] = ["T","h","e"," ","o","t","h","e","r"," ","h","u","n","g"," ","u","p"," ","t","h"]
fileprivate let data_headSharedUrl:String = "liter schedule basic comment ratinge phone"

/*: "type" :*/
fileprivate let const_millUrl:String = "tysoul"

/*: "jumpUrl" :*/
fileprivate let constLiveWeightBalanceName:[Character] = ["j","u","m","p","U"]
fileprivate let k_dailyText:String = "rclear"

/*: "Cancel" :*/
fileprivate let showComplaintPath:[Character] = ["C","a","n","c","e","l"]

/*: "Get gold coins" :*/
fileprivate let userCanValue:String = "Get golop not row"
fileprivate let user_minimumThenValue:[Character] = ["d"," ","c","o","i","n","s"]

/*: "refuseSex" :*/
fileprivate let dataMeetName:String = "multiple condition interaction nigh limitedrefuseSex"

/*: "onAcceptCall" :*/
fileprivate let kSimpleId:String = "complex beatonAccep"
fileprivate let main_tellPath:String = "opportunity saletCall"

/*: "acceptCall" :*/
fileprivate let kKeyMsg:String = "need he loseacceptC"
fileprivate let app_signatureUrl:String = "aagendaagenda"

/*: "Calls are cancelled" :*/
fileprivate let k_exStr:String = "Callspassing operation contrast"
fileprivate let dataVerticalText:String = "task head minimum cance"
fileprivate let userFactorStr:String = "lltrade"

/*: "The call has ended" :*/
fileprivate let k_smallMsg:String = "The cestimated select"
fileprivate let kPreserveValue:String = "approximately trade numberas ended"

//: Declare String End

// __DEBUG__
// __CLOSE_PRINT__
//
//  FinalInactiveAnimatedcontainer.swift
//  AbroadTalking
//
//  Created by young on 2022/11/11.
//

//: import CallKit.CXCallObserver
import CallKit.CXCallObserver
//: import UIKit
import UIKit

// 通话监听(必须为全局属性，否则无法接听)
//: let voiceCallHelper = CXCallObserver()
let userFoundNearbyPath = CXCallObserver()
//: class TalkingVoiceChatViewController: TalkingBaseViewController {
class FinalInactiveAnimatedcontainer: SafetyFactorViewController {
    //: var chatModel: TalkingVideoChatModel?
    var chatModel: ControlChatModel? // 语音通话模型
    //: private var player: TalkingCallBGMPlayer?
    private var player: PanelPlayListener? // 拨打中背景音乐
    //: private var timeOutTimer: Timer?
    private var timeOutTimer: Timer? // 未接听超时定时器
    //: private var expiredDate = Date()
    private var expiredDate = Date() // 失效时间
    //: private var miniView: TalkingVoiceMiniView?
    private var miniView: TranslateView? // 音频通话小窗口

    //: override init(nibName nibNameOrNil: String?, bundle nibBundleOrNil: Bundle?) {
    override init(nibName _: String?, bundle _: Bundle?) {
        //: super.init(nibName: nil, bundle: nil)
        super.init(nibName: nil, bundle: nil)
        // 提前绑定delegate
        //: TalkingSocketManager.shared.callerDelegate = self
        ConfigureAxisService.shared.callerDelegate = self
        //: TalkingSocketManager.shared.calledPartyDelegate = self
        ConfigureAxisService.shared.calledPartyDelegate = self
        //: TalkingSocketManager.shared.errorDelegate = self
        ConfigureAxisService.shared.errorDelegate = self
    }

    //: required init?(coder: NSCoder) {
    required init?(coder _: NSCoder) {
        //: fatalError("init(coder:) has not been implemented")
        fatalError(String(bytes: const_fatalUrl.map{returnExpected(compute: $0)}, encoding: .utf8)!)
    }

    //: override func viewDidAppear(_ animated: Bool) {
    override func viewDidAppear(_ animated: Bool) {
        //: super.viewDidAppear(animated)
        super.viewDidAppear(animated)
        //: let nav = self.navigationController as! TalkingNavigationController
        let nav = self.navigationController as! StrangerControllerDelegate
        //: nav.protectedPushVc = nil
        nav.protectedPushVc = nil

        //: UIApplication.shared.isIdleTimerDisabled = true
        UIApplication.shared.isIdleTimerDisabled = true // 设备不会进入休眠状态
        //: self.startBGM()
        self.appropriatePremium()
        //: if self.chatModel?.videoStage == .Recieved {
        if self.chatModel?.videoStage == .Recieved { // 接收方验证权限
            //: checkVoiceAuth()
            gesture()
        }
    }

    //: override func viewWillAppear(_ animated: Bool) {
    override func viewWillAppear(_ animated: Bool) {
        //: super.viewWillAppear(animated)
        super.viewWillAppear(animated)
        //: if self.miniView != nil {
        if self.miniView != nil { // 移除小窗口
            //: self.miniView?.removeFromSuperview()
            self.miniView?.removeFromSuperview()
            //: self.miniView = nil
            self.miniView = nil
            //: TalkingSocketManager.shared.currGeneralVC = nil
            ConfigureAxisService.shared.currGeneralVC = nil
        }
        //: interactionView.initCachEffectData()
        interactionView.magnitudeCream()
    }

    //: override func viewDidLoad() {
    override func viewDidLoad() {
        //: super.viewDidLoad()
        super.viewDidLoad()
        //: self.hideNavi = true
        self.hideNavi = true
        //: setupSubviews()
        component()
        //: setupSubViewsConstraint()
        tick()
        //: initNotificationObserver()
        mean()
        //: req_callGetUserInfo()
        sumNote()
        //: timeoutRules()
        signalThat()
        //: resumeTalk()
        feedbackClean()
        //: setUpBGMPlayer()
        statement()
    }

    //: override func loadView() {
    override func loadView() {
        //: if #available(iOS 13.2, *) {
        if #available(iOS 13.2, *) {
            //: if FacultyReactiveCompatible.share.appConfigMode.disableShootScreen {
            if FacultyReactiveCompatible.share.appConfigMode.disableShootScreen {
                //: super.loadView()
                super.loadView()
                //: } else {
            } else {
                //: let bgView = MAScreenShieldView(frame: CGRect(x: 0, y: 0, width: ScreenWidth, height: ScreenHeight))
                let bgView = ShieldView(frame: CGRect(x: 0, y: 0, width: main_assetUrl, height: kTopUrl))
                //: self.view = bgView
                self.view = bgView
            }
            //: } else {
        } else {
            //: super.loadView()
            super.loadView()
        }
    }

    //: deinit {
    deinit {
        //: UIApplication.shared.isIdleTimerDisabled = false
        UIApplication.shared.isIdleTimerDisabled = false
        //: NotificationCenter.default.removeObserver(self)
        NotificationCenter.default.removeObserver(self)
        //: TalkingSocketManager.shared.isGeneral = false
        ConfigureAxisService.shared.isGeneral = false
        //: TalkingSocketManager.shared.isCalling = false
        ConfigureAxisService.shared.isCalling = false
        //: self.stopVCAllActivties()
        self.haulUp()
    }

    // MARK: - Lazy Load

    /// 音频通话操作按钮
    //: private lazy var interactionView: Talking1v1VoiceInteractionView = {
    private lazy var interactionView: AllTotaleractionView = {
        //: let v = Talking1v1VoiceInteractionView(frame: .zero, chatModel: self.chatModel)
        let v = AllTotaleractionView(frame: .zero, chatModel: self.chatModel)
        //: v.delegate = self
        v.delegate = self
        //: return v
        return v
        //: }()
    }()

    //: private lazy var voiceManager: TalkingVoiceChatManager = {
    private lazy var voiceManager: TargetCloudDelegate = {
        //: let m = TalkingVoiceChatManager()
        let m = TargetCloudDelegate()
        //: m.handsfreeBtn = self.interactionView.handsfreeBtn
        m.handsfreeBtn = self.interactionView.handsfreeBtn
        //: return m
        return m
        //: }()
    }()
}

// MARK: - Request & 数据处理

//: extension TalkingVoiceChatViewController {
extension FinalInactiveAnimatedcontainer {
    /// 查询接收者信息
    //: func req_callGetUserInfo() {
    func sumNote() {
        //: guard let pairUid = chatModel?.pairUid else { return }
        guard let pairUid = chatModel?.pairUid else { return }
        //: TalkingVideoChatManager.req_callGetUserInfo(uid: pairUid) { [weak self] succeed, result, errorModel in
        FactorChatManager.designReceive(uid: pairUid) { [weak self] succeed, result, _ in
            //: guard succeed == true else { return }
            guard succeed == true else { return }
            //: guard let self = self else { return }
            guard let self = self else { return }

            //: let json = JSON(result ?? [String: Any]())
            let json = JSON(result ?? [String: Any]())

            //: self.chatModel?.pairUid = json["uid"].stringValue
            self.chatModel?.pairUid = json[String(bytes: app_rootWealthyPath.map{justNet(narrow: $0)}, encoding: .utf8)!].stringValue
            //: self.chatModel?.pairHeadPic = json["headPic"].stringValue
            self.chatModel?.pairHeadPic = json[(String(mainConfirmId.prefix(7)))].stringValue
            //: self.chatModel?.pairNickName = json["nickname"].stringValue
            self.chatModel?.pairNickName = json[(kSpellId.replacingOccurrences(of: "giving", with: "ni") + const_deviceId.replacingOccurrences(of: "last", with: "n"))].stringValue
            //: self.chatModel?.pairBirthday = json["birthday"].stringValue
            self.chatModel?.pairBirthday = json[(String(main_handMessage) + String(kSeaTitle))].stringValue
            //: self.chatModel?.videoUrl = json["videoUrl"].stringValue
            self.chatModel?.videoUrl = json[(String(constBeatText.suffix(6)) + String(notiRoutineFormat))].stringValue
            //: self.chatModel?.location = json["location"].stringValue
            self.chatModel?.location = json[(const_mentionMessage.replacingOccurrences(of: "put", with: "at") + String(app_rawText))].stringValue

            // 刷新通话视图
            //: self.interactionView.afterGetInfo()
            self.interactionView.infoEnable()
        }
    }
}

// MARK: - Event

//: extension TalkingVoiceChatViewController {
extension FinalInactiveAnimatedcontainer {
    /// 权限校验，播放BGM
    //: private func checkVoiceAuth() {
    private func gesture() {
        //: TalkingPermissionTool.func__openRecordServiceWithBlock(true) { isOpen in
        ActivityReactiveCompatible.intervalensityLevel(true) { isOpen in
            //: guard isOpen == true else {
            guard isOpen == true else {
                //: self.func__showStatusBarErrorMsg(showMsg: kMessage_permissions_error)
                self.micromicronProcess(showMsg: showPackageId)
                //: TalkingSocketManager.shared.sendMessage(info: ["cmd": "refuseCall", "data": ["uid": self.chatModel?.pairUid]])
                ConfigureAxisService.shared.attentionCut(info: [String(bytes: appItemMessage.map{$0^149}, encoding: .utf8)!: (String(show_seriesBoneMessage) + mainTransferPath.replacingOccurrences(of: "into", with: "l")), String(bytes: const_saveId.reversed(), encoding: .utf8)!: [String(bytes: app_rootWealthyPath.map{justNet(narrow: $0)}, encoding: .utf8)!: self.chatModel?.pairUid]])
                //: self.popCurrentViewController()
                self.cameraRead()
                //: return
                return
            }

            //: self.stopBGM()
            self.behindNow()
            //: self.player = TalkingCallBGMPlayer.callBGMPlayer(type: .Response)
            self.player = PanelPlayListener.explain(type: .Response)
            //: self.startBGM()
            self.appropriatePremium()
        }
    }

    /// 释放当前页面所有资源
    //: private func stopVCAllActivties() {
    private func haulUp() {
        //: if self.timeOutTimer != nil {
        if self.timeOutTimer != nil {
            //: self.timeOutTimer?.invalidate()
            self.timeOutTimer?.invalidate()
            //: self.timeOutTimer = nil
            self.timeOutTimer = nil
        }

        //: self.stopBGM()
        self.behindNow()
        //: TalkingSocketManager.shared.callerDelegate = nil
        ConfigureAxisService.shared.callerDelegate = nil
        //: TalkingSocketManager.shared.calledPartyDelegate = nil
        ConfigureAxisService.shared.calledPartyDelegate = nil
        //: TalkingSocketManager.shared.errorDelegate = nil
        ConfigureAxisService.shared.errorDelegate = nil
    }

    // 将当前视图从栈中移除
    //: private func popCurrentViewController(animated: Bool = true) {
    private func cameraRead(animated: Bool = true) {
        //: self.stopBGM()
        self.behindNow()

        //: if self.navigationController?.topViewController == self {
        if self.navigationController?.topViewController == self {
            //: self.navigationController?.popViewController(animated: animated)
            self.navigationController?.popViewController(animated: animated)
            //: } else {
        } else {
            //: if var vcArr = self.navigationController?.viewControllers {
            if var vcArr = self.navigationController?.viewControllers {
                //: if let index = vcArr.firstIndex(of: self) {
                if let index = vcArr.firstIndex(of: self) {
                    //: vcArr.remove(at: index)
                    vcArr.remove(at: index)
                    //: self.navigationController?.setViewControllers(vcArr, animated: false)
                    self.navigationController?.setViewControllers(vcArr, animated: false)
                }
            }
        }
    }

    /// 创建音频通话房间
    //: private func createTalkRoom() {
    private func maxProperty() {
        //: self.voiceManager.createRoomAndStartPublish(roomId: self.chatModel!.logId)
        self.voiceManager.sinceValue(roomId: self.chatModel!.logId)
    }

    /// 通话重连
    //: private func resumeTalk() {
    private func feedbackClean() {
        //: if self.chatModel?.resumeTalk == true {
        if self.chatModel?.resumeTalk == true {
            //: createTalkRoom()
            maxProperty()
        }
    }
}

// MARK: - 通知

//: extension TalkingVoiceChatViewController {
extension FinalInactiveAnimatedcontainer {
    /// 初始化通知
    //: private func initNotificationObserver() {
    private func mean() {
        // 监听 App 从后台切前台
        //: NotificationCenter.default.addObserver(self,
        NotificationCenter.default.addObserver(self,
                                               //: selector: #selector(appWillEnterForeground(notif:)),
                                               selector: #selector(stopNotif(notif:)),
                                               //: name: UIApplication.willEnterForegroundNotification,
                                               name: UIApplication.willEnterForegroundNotification,
                                               //: object: nil)
                                               object: nil)

        // socket断网重连监听
        //: NotificationCenter.default.addObserver(self,
        NotificationCenter.default.addObserver(self,
                                               //: selector: #selector(reachabilityChanged(note:)),
                                               selector: #selector(of(note:)),
                                               //: name: SOCKET_IS_OPEN_NOTIFICATION,
                                               name: k_tableStr,
                                               //: object: nil)
                                               object: nil)

        //: voiceCallHelper.setDelegate(self, queue: DispatchQueue.main)
        userFoundNearbyPath.setDelegate(self, queue: DispatchQueue.main)
    }

    /// app进入前台
    //: @objc private func appWillEnterForeground(notif: Notification) {
    @objc private func stopNotif(notif _: Notification) {
        //: guard self.chatModel?.videoStage != .Calling else { return }
        guard self.chatModel?.videoStage != .Calling else { return }
        //: let date = Date()
        let date = Date()
        //: let interval = date.timeIntervalSince(self.expiredDate)
        let interval = date.timeIntervalSince(self.expiredDate)
        //: if interval >= 0 {
        if interval >= 0 {
            //: popCurrentViewController()
            cameraRead()
        }
    }

    /// 断网重连socket
    //: @objc private func reachabilityChanged(note: Notification) {
    @objc private func of(note _: Notification) {
        //: guard TalkingSocketManager.shared.needReconnect == true else { return }
        guard ConfigureAxisService.shared.needReconnect == true else { return }
        //: TalkingSocketManager.shared.callerDelegate = self
        ConfigureAxisService.shared.callerDelegate = self
        //: TalkingSocketManager.shared.calledPartyDelegate = self
        ConfigureAxisService.shared.calledPartyDelegate = self
        //: TalkingSocketManager.shared.errorDelegate = self
        ConfigureAxisService.shared.errorDelegate = self
        //: self.voiceManager.restartTalkWhenDisconnectedAndNetAvailable(roomId: self.chatModel?.logId ?? 0)
        self.voiceManager.withoutId(roomId: self.chatModel?.logId ?? 0)
    }
}

// MARK: - CXCallObserverDelegate【通话监听】

//: extension TalkingVoiceChatViewController: CXCallObserverDelegate {
extension FinalInactiveAnimatedcontainer: CXCallObserverDelegate {
    //: func callObserver(_ callObserver: CXCallObserver, callChanged call: CXCall) {
    func callObserver(_: CXCallObserver, callChanged call: CXCall) {
        //: if call.hasConnected == true {
        if call.hasConnected == true { // 通话中
            //: self.func__showStatusBarErrorMsg(showMsg: "Call interruption".localized)
            self.micromicronProcess(showMsg: (String(data_paraName.suffix(4)) + " inte" + userChannelWarnSituationText.replacingOccurrences(of: "automatic", with: "p") + "tion").localized)
            //: switch(self.chatModel?.videoStage) {
            switch self.chatModel?.videoStage {
            //: case .Calling:
            case .Calling:
                //: TalkingSocketManager.shared.sendMessage(info: ["cmd": "stopCall", "data": ["logId": self.chatModel?.logId]])
                ConfigureAxisService.shared.attentionCut(info: [String(bytes: appItemMessage.map{$0^149}, encoding: .utf8)!: (String(userMilkUrl.suffix(8))), String(bytes: const_saveId.reversed(), encoding: .utf8)!: [String(bytes: noti_boltFormat.map{anyCenter(panel: $0)}, encoding: .utf8)!: self.chatModel?.logId]])
            //: case .Waiting:
            case .Waiting:
                //: TalkingSocketManager.shared.sendMessage(info: ["cmd": "cancelCall", "data": ["logId": self.chatModel?.logId]])
                ConfigureAxisService.shared.attentionCut(info: [String(bytes: appItemMessage.map{$0^149}, encoding: .utf8)!: (String(const_givingDatabaseValue) + String(app_expressionPath.prefix(5))), String(bytes: const_saveId.reversed(), encoding: .utf8)!: [String(bytes: noti_boltFormat.map{anyCenter(panel: $0)}, encoding: .utf8)!: self.chatModel?.logId]])
            //: case .Recieved:
            case .Recieved:
                //: TalkingSocketManager.shared.sendMessage(info: ["cmd": "refuseCall", "data": ["uid": self.chatModel?.pairUid]])
                ConfigureAxisService.shared.attentionCut(info: [String(bytes: appItemMessage.map{$0^149}, encoding: .utf8)!: (String(show_seriesBoneMessage) + mainTransferPath.replacingOccurrences(of: "into", with: "l")), String(bytes: const_saveId.reversed(), encoding: .utf8)!: [String(bytes: app_rootWealthyPath.map{justNet(narrow: $0)}, encoding: .utf8)!: self.chatModel?.pairUid]])
            //: case .none:
            case .none:
                //: break
                break
            }

            //: if miniView != nil {
            if miniView != nil {
                //: self.miniView?.dismissWhenCallEnd()
                self.miniView?.towardPhone()
                //: TalkingSocketManager.shared.currGeneralVC = nil
                ConfigureAxisService.shared.currGeneralVC = nil
            }
            //: popCurrentViewController()
            cameraRead()

            //: } else if call.hasEnded == true || call.isOnHold == true {
        } else if call.hasEnded == true || call.isOnHold == true { // 电话挂断 || 无人接听挂断
            //: self.voiceManager.enableAudioModule(enable: true)
            self.voiceManager.at(enable: true)
        }
    }
}

// MARK: - 通话音乐

//: extension TalkingVoiceChatViewController {
extension FinalInactiveAnimatedcontainer {
    /// 初始化音频通话背景音乐
    //: private func setUpBGMPlayer() {
    private func statement() {
        //: guard self.chatModel?.videoStage != .Calling else { return }
        guard self.chatModel?.videoStage != .Calling else { return }

        //: let type = self.chatModel!.isCaller == true ? CallBGMPlayerType.Call : CallBGMPlayerType.Response
        let type = self.chatModel!.isCaller == true ? ReportedTransformable.Call : ReportedTransformable.Response
        //: self.player = TalkingCallBGMPlayer.callBGMPlayer(type: type)
        self.player = PanelPlayListener.explain(type: type)
    }

    /// 开始播放背景音乐
    //: private func startBGM() {
    private func appropriatePremium() {
        //: guard self.chatModel?.videoStage != .Calling else { return }
        guard self.chatModel?.videoStage != .Calling else { return }
        //: guard self.chatModel?.resumeTalk != true else { return }
        guard self.chatModel?.resumeTalk != true else { return }

        //: self.player?.play()
        self.player?.highlightFail()
    }

    /// 停止播放背景音乐
    //: private func stopBGM() {
    private func behindNow() {
        //: guard self.player != nil else { return }
        guard self.player != nil else { return }
        //: self.player?.stop()
        self.player?.labelForeNet()
        //: self.player = nil
        self.player = nil
    }
}

// MARK: - 定时器

//: extension TalkingVoiceChatViewController {
extension FinalInactiveAnimatedcontainer {
    /// 超时定时器
    //: private func timeoutRules() {
    private func signalThat() {
        //: guard self.chatModel?.videoStage != .Calling else { return }
        guard self.chatModel?.videoStage != .Calling else { return }

        //: let timeout = self.chatModel?.isCaller == true ? VIDEO_1v1_CALL_TIMEOUT : self.chatModel?.remainAnswerCallTime()
        let timeout = self.chatModel?.isCaller == true ? user_currencyPath : self.chatModel?.screenTime()
        //: if timeout! <= 0 {
        if timeout! <= 0 {
            //: buildTalkTimeOutAction()
            spendBroad()
        }
        //: self.timeOutTimer = Timer.scheduledTimer(withTimeInterval: timeout!, block: { [weak self] timer in
        self.timeOutTimer = Timer.scheduledTimer(withTimeInterval: timeout!, block: { [weak self] _ in
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: self.buildTalkTimeOutAction()
            self.spendBroad()
            //: }, repeats: false)
        }, repeats: false)

        //: self.expiredDate = Date().addingTimeInterval(60)
        self.expiredDate = Date().addingTimeInterval(60)
    }

    /// 倒计时超时逻辑处理
    //: private func buildTalkTimeOutAction() {
    private func spendBroad() {
        //: guard !TalkingSocketManager.shared.isGeneral else { return }
        guard !ConfigureAxisService.shared.isGeneral else { return }

        //: var socketDict: [String: Any]
        var socketDict: [String: Any]
        //: if self.chatModel?.isCaller == true {
        if self.chatModel?.isCaller == true { // 拨打方
            //: self.func__showStatusBarErrorMsg(showMsg: "No answer".localized)
            self.micromicronProcess(showMsg: (String(k_shirtFormat) + String(showSecretId.prefix(7))).localized)
            //: socketDict = ["cmd": "cancelCall", "data": ["logId": self.chatModel?.logId]]
            socketDict = [String(bytes: appItemMessage.map{$0^149}, encoding: .utf8)!: (String(const_givingDatabaseValue) + String(app_expressionPath.prefix(5))), String(bytes: const_saveId.reversed(), encoding: .utf8)!: [String(bytes: noti_boltFormat.map{anyCenter(panel: $0)}, encoding: .utf8)!: self.chatModel?.logId]]
            //: } else {
        } else { // 接收方
            //: socketDict = ["cmd": "refuseCall", "data": ["uid": self.chatModel?.pairUid]]
            socketDict = [String(bytes: appItemMessage.map{$0^149}, encoding: .utf8)!: (String(show_seriesBoneMessage) + mainTransferPath.replacingOccurrences(of: "into", with: "l")), String(bytes: const_saveId.reversed(), encoding: .utf8)!: [String(bytes: app_rootWealthyPath.map{justNet(narrow: $0)}, encoding: .utf8)!: self.chatModel?.pairUid]]
        }
        //: TalkingSocketManager.shared.sendMessage(info: socketDict)
        ConfigureAxisService.shared.attentionCut(info: socketDict)
        //: popCurrentViewController()
        cameraRead()
    }
}

// MARK: - resume talk【恢复通话】

//: extension TalkingVoiceChatViewController {
extension FinalInactiveAnimatedcontainer {
    /// 恢复音频通话，更新chatModel
    /// - Parameter resumeData: 数据
    //: func updateInfoViaResumeTalkData(resumeData: [String: Any]) {
    func viaAlongside(resumeData: [String: Any]) {
        //: let json = JSON(resumeData)
        let json = JSON(resumeData)
        //: let logId = json["logId"].intValue
        let logId = json[String(bytes: noti_boltFormat.map{anyCenter(panel: $0)}, encoding: .utf8)!].intValue
        //: let duration = json["duration"].intValue
        let duration = json[(main_fileId.lowercased() + String(constClearlyReplacementFormat))].intValue
        //: let interval = Date().timeIntervalSince1970
        let interval = Date().timeIntervalSince1970
        //: let startTalkTime = floor(interval) - Double(duration)
        let startTalkTime = floor(interval) - Double(duration)
        //: self.chatModel?.startTime = startTalkTime
        self.chatModel?.startTime = startTalkTime
        //: self.interactionView.updateStartTime(startTime: startTalkTime)
        self.interactionView.effectByDefine(startTime: startTalkTime)

        //: if self.chatModel?.videoStage != .Calling {
        if self.chatModel?.videoStage != .Calling {
            //: self.chatModel?.logId = logId
            self.chatModel?.logId = logId
            //: self.chatModel?.videoStage = .Calling
            self.chatModel?.videoStage = .Calling
            //: self.interactionView.showViewsViaStage()
            self.interactionView.packageActivity()
            //: createTalkRoom()
            maxProperty()
            //: self.stopBGM()
            self.behindNow()
        }
    }
}

// MARK: - ConfirmCallerDelegate【拨打方】

//: extension TalkingVoiceChatViewController: SocketManagerVideoChatCallerDelegate {
extension FinalInactiveAnimatedcontainer: ConfirmCallerDelegate {
    /// 拨打方拒绝了通话，接收方收到该指令（未通话）
    //: func socket_videoChat_cancelCall(data: [String: Any]) {
    func delineateBean(data: [String: Any]) {
        //: if self.miniView == nil {
        if self.miniView == nil {
            //: let json = JSON(data)
            let json = JSON(data)
            //: var typeStr = "She has hung up".localized
            var typeStr = (String(showCurMessage.suffix(6)) + "s hun" + String(data_markValue.prefix(4))).localized
            //: if json["cancelSex"].stringValue == Gender.male.rawValue {
            if json[(data_indicatorUpperPath.replacingOccurrences(of: "cant", with: "an") + "elSex")].stringValue == ATextLiteral.male.rawValue {
                //: typeStr = "He has hung up".localized
                typeStr = (String(main_backgroundPath)).localized
            }
            //: self.func__showStatusBarErrorMsg(showMsg: typeStr)
            self.micromicronProcess(showMsg: typeStr)
            //: } else {
        } else {
            //: self.miniView?.dismissWhenCallEnd()
            self.miniView?.towardPhone()
            //: TalkingSocketManager.shared.currGeneralVC = nil
            ConfigureAxisService.shared.currGeneralVC = nil
        }

        //: popCurrentViewController()
        cameraRead()
    }

    /// 对方接受了来电，建立音频通话
    //: func socket_videoChat_acceptCall(data: [String: Any]) {
    func curve(data: [String: Any]) {
        //: let json = JSON(data)
        let json = JSON(data)

        //: self.chatModel?.logId = json["logId"].intValue
        self.chatModel?.logId = json[String(bytes: noti_boltFormat.map{anyCenter(panel: $0)}, encoding: .utf8)!].intValue
        //: self.chatModel?.videoStage = .Calling
        self.chatModel?.videoStage = .Calling
        //: self.chatModel?.renderPostion = .SelfOnWindow
        self.chatModel?.renderPostion = .SelfOnWindow

        //: self.stopBGM()
        self.behindNow()
        //: self.interactionView.showViewsViaStage()
        self.interactionView.packageActivity()
        //: self.createTalkRoom()
        self.maxProperty()
    }

    /// 对方结束了通话（通话中）
    //: func socket_videoChat_stopCall(data: [String: Any]) {
    func wrapData(data: [String: Any]) {
        //: let json = JSON(data)
        let json = JSON(data)

        //: let endType = json["endType"].intValue
        let endType = json[(show_rugPath.replacingOccurrences(of: "slow", with: "e") + String(appCombineBoardData.suffix(5)))].intValue
        //: var typeStr = "She has hung up".localized
        var typeStr = (String(showCurMessage.suffix(6)) + "s hun" + String(data_markValue.prefix(4))).localized
        //: if json["stopSex"].stringValue == Gender.male.rawValue {
        if json[(String(const_reserveContent.suffix(7)))].stringValue == ATextLiteral.male.rawValue {
            //: typeStr = "He has hung up".localized
            typeStr = (String(main_backgroundPath)).localized
        }

        //: if endType == 1 {
        if endType == 1 { // 付费方余额不足
            //: if FacultyReactiveCompatible.share.appStatus == AppSkinStatus.normal.rawValue {
            if FacultyReactiveCompatible.share.appStatus == PenConstantTarget.normal.rawValue {
                //: typeStr = "Insufficient gold coins, call has ended".localized
                typeStr = String(bytes: show_recommendUrl.map{lessSuspend(fore: $0)}, encoding: .utf8)!.localized
                //: } else {
            } else {
                //: typeStr = "The other hung up the phone".localized
                typeStr = (String(notiEffectWeMsg) + String(data_headSharedUrl.suffix(7))).localized
            }
        }
        //: if self.miniView == nil {
        if self.miniView == nil {
            //: self.func__showStatusBarErrorMsg(showMsg: typeStr)
            self.micromicronProcess(showMsg: typeStr)
            //: } else {
        } else {
            //: self.miniView?.dismissWhenCallEnd()
            self.miniView?.towardPhone()
            //: TalkingSocketManager.shared.currGeneralVC = nil
            ConfigureAxisService.shared.currGeneralVC = nil
        }

        //: self.interactionView.func_giftViewDismiss()
        self.interactionView.curtsy()
        //: self.popCurrentViewController()
        self.cameraRead()
        //: NotificationCenter.default.post(name: VIDEOCALL_END_CLOSEDISCOUNTS_NOTIFICATION, object: nil)
        NotificationCenter.default.post(name: appEventUrl, object: nil)
    }

    /// 余额不足提醒
    //: func socket_videoChat_onMissCoin(msg: String, data: [String: Any]) {
    func noKit(msg: String, data: [String: Any]) {
        //: guard FacultyReactiveCompatible.share.appStatus == AppSkinStatus.normal.rawValue else { return }
        guard FacultyReactiveCompatible.share.appStatus == PenConstantTarget.normal.rawValue else { return }

        //: let index = data["type"] as? String ?? ""
        let index = data[(const_millUrl.replacingOccurrences(of: "soul", with: "pe"))] as? String ?? ""
        //: if index == "2" {
        if index == "2" {
            //: guard let jumpUrl = data["jumpUrl"] as? String  else { return }
            guard let jumpUrl = data[(String(constLiveWeightBalanceName) + k_dailyText.replacingOccurrences(of: "clear", with: "l"))] as? String else { return }
            //: let httpsUrl = FacultyReactiveCompatible.share.appConfigMode.urlH5Domain+jumpUrl
            let httpsUrl = FacultyReactiveCompatible.share.appConfigMode.urlH5Domain + jumpUrl
            //: var config = TalkingWebConfig.init()
            var config = CalendarConfig()
            //: config.isDiscountsView = true
            config.isDiscountsView = true
            //: DisplayAperturePresenter.share.func__pushToWebVC(urlStr: httpsUrl, webConfig: config)
            DisplayAperturePresenter.share.lifeStyle(urlStr: httpsUrl, webConfig: config)
            //: } else {
        } else {
            //: TalkingAlertShow.alert(title: nil,
            PersonAlertShow.panAngleKind(title: nil,
                                          //: message: msg,
                                          message: msg,
                                          //: leftBtnTitle: "Cancel".localized,
                                          leftBtnTitle: (String(showComplaintPath)).localized,
                                          //: rightBtnTitle: "Get gold coins".localized) {
                                          rightBtnTitle: (String(userCanValue.prefix(7)) + String(user_minimumThenValue)).localized)
            {
                //: TalkingAlertShow.hideAlert()
                PersonAlertShow.quantityerrupt()
                //: } rightBlock: {
            } rightBlock: {
                //: DisplayAperturePresenter.share.func__jumpToWebRecharge(clickEvent: clickLess1minButton, sufficient: false)
                DisplayAperturePresenter.share.bilge(clickEvent: noti_packageMessage, sufficient: false)
            }
        }
    }
}

// MARK: - SeparatePartyDelegate【接收方】

//: extension TalkingVoiceChatViewController: SocketManagerVideoChatCalledPartyDelegate {
extension FinalInactiveAnimatedcontainer: SeparatePartyDelegate {
    /// 接收方拒绝了通话，拨打方收到该指令（未通话）
    /// - Parameter data: 数据
    //: func socket_videoChat_refuseCall(data: [String: Any]) {
    func countCity(data: [String: Any]) {
        //: if self.miniView == nil {
        if self.miniView == nil {
            //: let json = JSON(data)
            let json = JSON(data)
            //: var typeStr = "She has hung up".localized
            var typeStr = (String(showCurMessage.suffix(6)) + "s hun" + String(data_markValue.prefix(4))).localized
            //: if json["refuseSex"].stringValue == Gender.male.rawValue {
            if json[(String(dataMeetName.suffix(9)))].stringValue == ATextLiteral.male.rawValue {
                //: typeStr = "He has hung up".localized
                typeStr = (String(main_backgroundPath)).localized
            }
            //: self.func__showStatusBarErrorMsg(showMsg: typeStr)
            self.micromicronProcess(showMsg: typeStr)
            //: } else {
        } else {
            //: self.miniView?.dismissWhenCallEnd()
            self.miniView?.towardPhone()
            //: TalkingSocketManager.shared.currGeneralVC = nil
            ConfigureAxisService.shared.currGeneralVC = nil
        }
        //: popCurrentViewController()
        cameraRead()
    }

    /// 接收方接受了通话，改变状态为通话中，acceptCall回执
    //: func socket_videoChat_onAcceptCall(data: [String: Any]) {
    func starHideSort(data: [String: Any]) {
        //: let json = JSON(data)
        let json = JSON(data)
        //: self.chatModel?.logId = json["logId"].intValue
        self.chatModel?.logId = json[String(bytes: noti_boltFormat.map{anyCenter(panel: $0)}, encoding: .utf8)!].intValue
        //: self.chatModel?.videoStage = .Calling
        self.chatModel?.videoStage = .Calling
        //: self.chatModel?.renderPostion = .SelfOnWindow
        self.chatModel?.renderPostion = .SelfOnWindow
        //: self.stopBGM()
        self.behindNow()
        //: self.interactionView.showViewsViaStage()
        self.interactionView.packageActivity()
        //: self.createTalkRoom()
        self.maxProperty()
    }
}

// MARK: - FriendshipErrorDelegate【异常处理】

//: extension TalkingVoiceChatViewController: TalkingSocketManagerErrorDelegate {
extension FinalInactiveAnimatedcontainer: FriendshipErrorDelegate {
    // 处理error情况
    //: func socket_didRecieveError(errorNo: Int, cmd: String, msg: String, data: [String: Any]) {
    func data(errorNo: Int, cmd: String, msg: String, data _: [String: Any]) {
        //: if cmd == "refuseCall" { // 对方拒绝了通话
        if cmd == (String(show_seriesBoneMessage) + mainTransferPath.replacingOccurrences(of: "into", with: "l")) { // 对方拒绝了通话
            //: if self.miniView == nil {
            if self.miniView == nil {
                //: self.func__showStatusBarErrorMsg(showMsg: "No answer".localized)
                self.micromicronProcess(showMsg: (String(k_shirtFormat) + String(showSecretId.prefix(7))).localized)
                //: } else {
            } else {
                //: self.miniView?.dismissWhenCallEnd()
                self.miniView?.towardPhone()
                //: TalkingSocketManager.shared.currGeneralVC = nil
                ConfigureAxisService.shared.currGeneralVC = nil
            }
            //: self.popCurrentViewController()
            self.cameraRead()

            //: } else if cmd == "onAcceptCall" && errorNo == 101 { // 接听通话时，余额不足
        } else if cmd == (String(kSimpleId.suffix(7)) + String(main_tellPath.suffix(5))) && errorNo == 101 { // 接听通话时，余额不足
            //: guard FacultyReactiveCompatible.share.appStatus == AppSkinStatus.normal.rawValue else { return }
            guard FacultyReactiveCompatible.share.appStatus == PenConstantTarget.normal.rawValue else { return }
            //: DisplayAperturePresenter.share.func__jumpToWebRecharge(sufficient: false)
            DisplayAperturePresenter.share.bilge(sufficient: false)

            //: } else if cmd == "onAcceptCall" && errorNo == 102 { // 对方已取消通话
        } else if cmd == (String(kSimpleId.suffix(7)) + String(main_tellPath.suffix(5))) && errorNo == 102 { // 对方已取消通话
            //: self.func__showStatusBarErrorMsg(showMsg: msg)
            self.micromicronProcess(showMsg: msg)
            //: self.popCurrentViewController()
            self.cameraRead()
        }
    }
}

// MARK: - PenViewDelegate【通话按钮操作事件】

//: extension TalkingVoiceChatViewController: Talking1v1VoiceInteractionViewDelegate {
extension FinalInactiveAnimatedcontainer: PenViewDelegate {
    /// 接听音频通话
    //: func interactionView_shouldAcceptCall() {
    func baptize() {
        //: TalkingSocketManager.shared.sendMessage(info: ["cmd": "acceptCall", "data": ["uid": self.chatModel?.pairUid]])
        ConfigureAxisService.shared.attentionCut(info: [String(bytes: appItemMessage.map{$0^149}, encoding: .utf8)!: (String(kKeyMsg.suffix(7)) + app_signatureUrl.replacingOccurrences(of: "agenda", with: "l")), String(bytes: const_saveId.reversed(), encoding: .utf8)!: [String(bytes: app_rootWealthyPath.map{justNet(narrow: $0)}, encoding: .utf8)!: self.chatModel?.pairUid]])
    }

    /// 拨打方结束通话
    //: func interactionView_shouldEndCall() {
    func rid() {
        //: if self.chatModel?.videoStage == .Waiting {
        if self.chatModel?.videoStage == .Waiting { // 拨打方取消【未通话】
            //: self.func__showStatusBarErrorMsg(showMsg: "Calls are cancelled".localized)
            self.micromicronProcess(showMsg: (String(k_exStr.prefix(5)) + " are" + String(dataVerticalText.suffix(6)) + userFactorStr.replacingOccurrences(of: "trade", with: "ed")).localized)
            //: TalkingSocketManager.shared.sendMessage(info: ["cmd": "cancelCall", "data": ["logId": self.chatModel?.logId]])
            ConfigureAxisService.shared.attentionCut(info: [String(bytes: appItemMessage.map{$0^149}, encoding: .utf8)!: (String(const_givingDatabaseValue) + String(app_expressionPath.prefix(5))), String(bytes: const_saveId.reversed(), encoding: .utf8)!: [String(bytes: noti_boltFormat.map{anyCenter(panel: $0)}, encoding: .utf8)!: self.chatModel?.logId]])
            //: } else if self.chatModel?.videoStage == .Recieved {
        } else if self.chatModel?.videoStage == .Recieved { // 接收方取消【未通话】
            //: self.func__showStatusBarErrorMsg(showMsg: "Calls are cancelled".localized)
            self.micromicronProcess(showMsg: (String(k_exStr.prefix(5)) + " are" + String(dataVerticalText.suffix(6)) + userFactorStr.replacingOccurrences(of: "trade", with: "ed")).localized)
            //: TalkingSocketManager.shared.sendMessage(info: ["cmd": "refuseCall", "data": ["uid": self.chatModel?.pairUid]])
            ConfigureAxisService.shared.attentionCut(info: [String(bytes: appItemMessage.map{$0^149}, encoding: .utf8)!: (String(show_seriesBoneMessage) + mainTransferPath.replacingOccurrences(of: "into", with: "l")), String(bytes: const_saveId.reversed(), encoding: .utf8)!: [String(bytes: app_rootWealthyPath.map{justNet(narrow: $0)}, encoding: .utf8)!: self.chatModel?.pairUid]])
            //: } else {
        } else { // 【通话中】
            //: self.func__showStatusBarErrorMsg(showMsg: "The call has ended".localized)
            self.micromicronProcess(showMsg: (String(k_smallMsg.prefix(5)) + "all h" + String(kPreserveValue.suffix(8))).localized)
            //: TalkingSocketManager.shared.sendMessage(info: ["cmd": "stopCall", "data": ["logId": self.chatModel?.logId]])
            ConfigureAxisService.shared.attentionCut(info: [String(bytes: appItemMessage.map{$0^149}, encoding: .utf8)!: (String(userMilkUrl.suffix(8))), String(bytes: const_saveId.reversed(), encoding: .utf8)!: [String(bytes: noti_boltFormat.map{anyCenter(panel: $0)}, encoding: .utf8)!: self.chatModel?.logId]])
        }

        //: self.interactionView.func_giftViewDismiss()
        self.interactionView.curtsy()
        //: self.popCurrentViewController()
        self.cameraRead()
    }

    /// 是否静音
    //: func interactionView_shouldChangeMute(isMute: Bool) {
    func beyondPending(isMute: Bool) {
        //: self.voiceManager.setMute(isMute: isMute)
        self.voiceManager.regular(isMute: isMute)
    }

    /// 是否免提
    //: func interactionView_shouldChangeSpeaker(isHandsfree: Bool) {
    func afterFile(isHandsfree: Bool) {
        //: self.voiceManager.setHandsfree(isHandsfree: isHandsfree)
        self.voiceManager.poise(isHandsfree: isHandsfree)
    }

    /// 展示语音通话小窗口
    //: func interactionView_shouldDisplayMiniWindow() {
    func arcWindow() {
        //: TalkingSocketManager.shared.currGeneralVC = self
        ConfigureAxisService.shared.currGeneralVC = self
        //: self.miniView = TalkingVoiceMiniView.buildVoiceMiniView()
        self.miniView = TranslateView.fixedOff()
        //: self.miniView?.tapGestureBlock = { [weak self] in
        self.miniView?.tapGestureBlock = { [weak self] in // 还原回通话界面
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: self.currentViewController()?.navigationController?.pushViewController(self, animated: true)
            self.field()?.navigationController?.pushViewController(self, animated: true)
        }
        //: let talkingTime = self.interactionView.getCurrentTalkingTime()
        let talkingTime = self.interactionView.cleanse()
        //: self.miniView?.updateTalkingTime(talkingTime)
        self.miniView?.faceMention(talkingTime)
        //: popCurrentViewController()
        cameraRead()
    }

    /// 更新小窗口通话时间
    //: func interactionView_shouldUpdateTime(time: Double) {
    func dateDraw(time: Double) {
        //: guard self.miniView != nil else { return }
        guard self.miniView != nil else { return }
        //: self.miniView?.updateTalkingTime(time)
        self.miniView?.faceMention(time)
    }

    /// 收到礼物消息展示
    //: @objc func receiveGiftPlay(giftArr: Array<Any>) {
    @objc func exceptChild(giftArr: [Any]) {
        //: interactionView.addGiftEffectModelArr(giftArr: giftArr)
        interactionView.pureness(giftArr: giftArr)
    }
}

// MARK: - Layout

//: extension TalkingVoiceChatViewController {
extension FinalInactiveAnimatedcontainer {
    /// 添加视图
    //: private func setupSubviews() {
    private func component() {
        //: self.view.addSubview(interactionView)
        self.view.addSubview(interactionView)
    }

    /// 添加约束
    //: private func setupSubViewsConstraint() {
    private func tick() {
        //: interactionView.snp.makeConstraints { make in
        interactionView.snp.makeConstraints { make in
            //: make.edges.equalToSuperview()
            make.edges.equalToSuperview()
        }
    }
}
