
//: Declare String Begin

/*: "IM退出登录失败，code: :*/
fileprivate let show_continuingReportTitle:String = "IM\u{9000}出登录\u{5931}"
fileprivate let show_lookStreamMsg:[Character] = ["败","\u{ff0c}","c","o","d","e",":"]

/*: , desc: :*/
fileprivate let showFenceBanTitle:String = ", desc:clothes opportunity negotiate"

/*: "IM登录失败，code: :*/
fileprivate let data_attributeName:String = "IM登录\u{5931}\u{8d25}"
fileprivate let app_searchPath:[Character] = ["，"]
fileprivate let const_pinValue:String = "code:package existing once prepare cant"

/*: "IM获取未读消息总数失败，code: :*/
fileprivate let app_missingName:[Character] = ["I","M","获","\u{53d6}","\u{672a}","读","消","息","总"]
fileprivate let kProperlyEdgeRecoverMsg:String = "\u{6570}失败\u{ff0c}code:"

/*: "crushBadge" :*/
fileprivate let main_journalTitle:String = "cbottomush"
fileprivate let k_measurementValue:[Character] = ["e"]

/*: "badNumber" :*/
fileprivate let app_lastUrl:[UInt8] = [0x4f,0x4c,0x49,0x63,0x58,0x40,0x4f,0x48,0x5f]

/*: "plaza" :*/
fileprivate let noti_demandListenContent:String = "PLAZA"

/*: "roomNum" :*/
fileprivate let userRecordingTrustKey:[Character] = ["r","o","o","m","N"]
fileprivate let notiKeyText:[Character] = ["u","m"]

/*: "num" :*/
fileprivate let main_edgeSongQuickPath:String = "nresource"

/*: "onlyOneRoomId" :*/
fileprivate let userEasternFormat:String = "onlyOmeet calendar load picture day"
fileprivate let notiPrivacyStr:String = "once snap spell youromId"

/*: "LIVE-CHAT" :*/
fileprivate let main_consequenceStr:[Character] = ["L","I","V","E","-","C","H","A","T"]

/*: "extra" :*/
fileprivate let appSeatData:[Character] = ["e","x","t","r","a"]

/*: "msgType" :*/
fileprivate let dataPipeUrl:[Character] = ["m","s","g","T","y","p","e"]

/*: "newPkgGift" :*/
fileprivate let notiSecretPath:String = "newPkgGivar design light always"
fileprivate let data_womanFrameKey:String = "falready"

/*: "MF:ChatMentionMsg" :*/
fileprivate let showDistanceId:[Character] = ["M","F",":","C","h","a","t","M","e","n","t","i"]
fileprivate let kThreeData:[Character] = ["o","n","M","s","g"]

/*: "uid" :*/
fileprivate let user_simplePath:[UInt8] = [0xf7,0xeb,0xe6]

fileprivate func subjectDescend(country num: UInt8) -> UInt8 {
    let value = Int(num) + 126
    if value > 255 {
        return UInt8(value - 256)
    } else {
        return UInt8(value)
    }
}

/*: "onlineStatus" :*/
fileprivate let noti_frameworkValue:[UInt8] = [0x73,0x75,0x74,0x61,0x74,0x53,0x65,0x6e,0x69,0x6c,0x6e,0x6f]

/*: "isNewUser" :*/
fileprivate let k_interestTitle:[UInt8] = [0x72,0x65,0x73,0x55,0x77,0x65,0x4e,0x73,0x69]

/*: "c2c_ :*/
fileprivate let mainMeanKey:String = "c2c_service some earn ping perspective"

/*: "IM获取会话失败，convId: :*/
fileprivate let showScenarioTitle:String = "IM获取会growth pipe"
fileprivate let dataRoundingPath:String = "operating autovId:"

/*: , code: :*/
fileprivate let show_clothesTickMsg:String = ", code:nearby fee"

/*: "IM插入消息失败，targetId: :*/
fileprivate let k_haulData:[Character] = ["I","M","\u{63d2}","入","消","息","\u{5931}","\u{8d25}"]
fileprivate let noti_sitId:String = "pack not mp storage，targ"

/*: "changeDirection" :*/
fileprivate let dataPortKey:[Character] = ["c","h","a","n","g","e","D","i"]
fileprivate let notiPublishMsg:String = "recrecommendion"

/*: "rYMsgType" :*/
fileprivate let kBelowMessage:String = "rYMsbring resolution indigenous core"
fileprivate let constAreaFosterPath:[Character] = ["g","T","y","p","e"]

/*: "GJ:CallCustom" :*/
fileprivate let notiVisualShadeKey:String = "receive screenGJ:C"
fileprivate let k_detectPath:String = "false adviceallCu"

/*: "IM获取会话列表失败，code: :*/
fileprivate let constNeighborText:String = "hold relate combineIM获取会"
fileprivate let notiNighFormat:String = "话列表失"

/*: "msgInfo" :*/
fileprivate let main_starDomainUrl:String = "msgInfoheavy drag match"

/*: "needFold" :*/
fileprivate let app_simultaneouslyValue:[Character] = ["n","e","e","d","F","o","l"]
fileprivate let appYouMessage:String = "climb"

/*: "group_ :*/
fileprivate let notiBetterText:String = "variation"
fileprivate let mainEnabledValue:String = "cur home normally weight ridroup_"

/*: "IM删除会话失败，convID: :*/
fileprivate let appLabelRandomAudienceContent:[Character] = ["I","M","删","除","\u{4f1a}","话","失","败"]
fileprivate let show_whatIndexFormat:String = "\u{ff0c}con"

/*: "IM置顶会话失败，convId: :*/
fileprivate let mainSubKey:String = "orientation already poseIM置顶会"
fileprivate let mainStripData:String = "cookie valid off take话失败，co"
fileprivate let k_placeId:[Character] = ["n","v","I","d",":"]

/*: "SDK 正在连接到服务器" :*/
fileprivate let notiHostName:String = "SDK 正function hood black"
fileprivate let userAcquireName:String = "在连接\u{5230}\u{670d}\u{52a1}器"

/*: "SDK 已经成功连接到服务器" :*/
fileprivate let notiWholeMessage:String = "robot quick reduce purchase countrySDK 已"
fileprivate let noti_gatherPointRegionPath:String = "接到服务器"

/*: "IM连接服务器失败，code: :*/
fileprivate let showContainMsg:String = "IM连接\u{670d}务器失"
fileprivate let mainComputerMessage:String = "败，code:price answer what hood"

/*: , error: :*/
fileprivate let show_positionName:[Character] = [","," ","e","r","r","o"]
fileprivate let dataStillFashionFormat:String = "r:component mic"

/*: "IM账号被挤下线。" :*/
fileprivate let k_bagListenPath:String = "IM账号被\u{6324}下线"
fileprivate let mainClimbContent:String = "。"

/*: "Your account is logged in on another device and you are forced to go offline! If it is not done by me, the password may have been leaked, and it is recommended to change the password." :*/
fileprivate let noti_balloonUrl:[UInt8] = []

private func gameResult(relieve num: UInt8) -> UInt8 {
    return num ^ 141
}

/*: "Offline Notification" :*/
fileprivate let dataAcceptKey:String = "Offlincell if false session editor"
fileprivate let constContextGrowthId:String = "tlocki"
fileprivate let noti_deliveryTitle:[Character] = ["n"]

/*: "Cancel" :*/
fileprivate let userStarMessage:String = "vital nobodyCancel"

/*: "Re Login" :*/
fileprivate let userTowardValue:String = "highlight shadow smart appearRe Login"

//: Declare String End

// __DEBUG__
// __CLOSE_PRINT__
//
//  IonV2Listener.swift
//  AbroadTalking
//
//  Created by Hemming on 2023/3/7.
//

//: import UIKit
import UIKit

/// 视频通话【拨打方】代理
//: @objc public protocol IMManagerDelegate: NSObjectProtocol {
@objc public protocol FriendshipObjectProtocol: NSObjectProtocol {
    /// 会话列表更新
    //: @objc optional func onRefreshConversationList(data: [TalkingConversationModel])
    @objc optional func sample(data: [MultistageRocketConversationModel])

    /// 未读数更新
    //: @objc optional func onUnreadMsgCountChanged(count: Int)
    @objc optional func addUp(count: Int)

    /// 收到新消息
    //: @objc optional func onRecvNewMessage(msg: V2TIMMessage)
    @objc optional func own(msg: V2TIMMessage)
}

/// 数据列表枚举
//: enum ChatListDataType {
enum YouthCultureDataType {
    //: case normalList
    case normalList // 默认列表
    //: case topList
    case topList // 置顶列表
    //: case moreList
    case moreList // 更多消息（超过5天未回复）
}

//: @objcMembers public class IonV2Listener: NSObject {
@objcMembers public class IonV2Listener: NSObject {
    //: public static let shared = IonV2Listener()
    public static let shared = IonV2Listener()
    //: public dynamic var isConnection = false
    public dynamic var isConnection = false
    //: var isRefreshSysMsg = false
    var isRefreshSysMsg = false
    //: var lastSysTime: TimeInterval = 0
    var lastSysTime: TimeInterval = 0

    //: var officialList: [TalkingConversationModel] = []
    var officialList: [MultistageRocketConversationModel] = [] // 官方列表（like+系统通知+客服）
    //: var topConvList: [TalkingConversationModel] = []
    var topConvList: [MultistageRocketConversationModel] = [] // 置顶列表
    //: var norConvList: [TalkingConversationModel] = []
    var norConvList: [MultistageRocketConversationModel] = [] // 普通会话列表
    //: var chatRoomList: [TalkingConversationModel] = []
    var chatRoomList: [MultistageRocketConversationModel] = [] // 聊天室
    //: var moreMsgList: [TalkingConversationModel] = []
    var moreMsgList: [MultistageRocketConversationModel] = [] // 更多消息（超过5天未回复）

    /// 消息列表数据分页
    //: private var nextSeq: UInt64 = 0
    private var nextSeq: UInt64 = 0
    /// 多个代理容器
    //: private let multiDelegate: NSHashTable<AnyObject> = NSHashTable.weakObjects()
    private let multiDelegate: NSHashTable<AnyObject> = NSHashTable.weakObjects()
    /// 会话列表缓存数据（避免重复转换）
    //: private var convListCache = [String: TalkingConversationModel]()
    private var convListCache = [String: MultistageRocketConversationModel]()

    //: override private init() {
    override private init() {
        //: super.init()
        super.init()
        //: self.initialization()
        self.within()
    }

    // MARK: - SDK初始化

    //: func func__TXSDKInit() {
    func sh() {
        //: let config = V2TIMSDKConfig()
        let config = V2TIMSDKConfig()
        //: config.logLevel = .LOG_DEBUG
        config.logLevel = .LOG_DEBUG
        //: V2TIMManager.sharedInstance().initSDK(TXIMAppID, config: config)
        V2TIMManager.sharedInstance().initSDK(app_meName, config: config)
        //: V2TIMManager.sharedInstance().addAdvancedMsgListener(listener: self)
        V2TIMManager.sharedInstance().addAdvancedMsgListener(listener: self)
        //: V2TIMManager.sharedInstance().addConversationListener(listener: self)
        V2TIMManager.sharedInstance().addConversationListener(listener: self)
        //: V2TIMManager.sharedInstance().add(self)
        V2TIMManager.sharedInstance().add(self)
    }

    // MARK: - 清除单例状态

    //: func initialization() {
    func within() {
        //: func__removeAllDelegate()
        permission()
        //: self.isConnection = false
        self.isConnection = false
        //: self.nextSeq = 0
        self.nextSeq = 0
        //: self.lastSysTime = 0
        self.lastSysTime = 0
        //: self.func__adddofficialList()
        self.withoutAlongside()
        //: self.convListCache.removeAll()
        self.convListCache.removeAll()
        //: self.norConvList.removeAll()
        self.norConvList.removeAll()
        //: self.topConvList.removeAll()
        self.topConvList.removeAll()
        //: self.moreMsgList.removeAll()
        self.moreMsgList.removeAll()
    }

    /// 添加官方数据模型
    //: func func__adddofficialList() {
    func withoutAlongside() {
        //: self.officialList.removeAll()
        self.officialList.removeAll()
        // 添加官方占位model
        //: let systemModel = TalkingConversationModel.getPlaceholderModel(chatType: .system)
        let systemModel = MultistageRocketConversationModel.nearGet(chatType: .system)
        //: let serviceModel = TalkingConversationModel.getPlaceholderModel(chatType: .service)
        let serviceModel = MultistageRocketConversationModel.nearGet(chatType: .service)
        //: self.officialList = [systemModel, serviceModel]
        self.officialList = [systemModel, serviceModel]
    }

    /// 获取官方数据列表（在审核模式下过滤客服消息不展示）
    /// - Returns: 模型
    //: func getOfficialList() -> [TalkingConversationModel] {
    func voiderBroad() -> [MultistageRocketConversationModel] {
        // 审核模式，在消息列表移除官方客服
        //: if FacultyReactiveCompatible.share.appStatus == AppSkinStatus.special.rawValue {
        if FacultyReactiveCompatible.share.appStatus == PenConstantTarget.special.rawValue {
            //: if let index = self.officialList.firstIndex(where: { $0.userID == MacroBorderDefineThen.getCustomerServiceID() }) {
            if let index = self.officialList.firstIndex(where: { $0.userID == MacroBorderDefineThen.receiveRemove() }) {
                //: self.officialList.remove(at: index)
                self.officialList.remove(at: index)
            }
        }
        //: return self.officialList
        return self.officialList
    }
}

// MARK: - 管理多个代理

//: extension IonV2Listener {
extension IonV2Listener {
    //: func func__removeAllDelegate() {
    func permission() {
        //: self.multiDelegate.removeAllObjects()
        self.multiDelegate.removeAllObjects()
    }

    //: func func__addDelegate(_ delegate: IMManagerDelegate) {
    func asNotice(_ delegate: FriendshipObjectProtocol) {
        //: self.multiDelegate.add(delegate)
        self.multiDelegate.add(delegate)
    }

    //: func func__removeDelegate(_ delegate: IMManagerDelegate) {
    func operation(_ delegate: FriendshipObjectProtocol) {
        //: self.func__invokeDelegate {
        self.moon {
            //: if $0 === delegate as AnyObject {
            if $0 === delegate as AnyObject {
                //: self.multiDelegate.remove($0)
                self.multiDelegate.remove($0)
            }
        }
    }

    //: private func func__invokeDelegate(_ invocation: (IMManagerDelegate) -> Void) {
    private func moon(_ invocation: (FriendshipObjectProtocol) -> Void) {
        //: for delegate in self.multiDelegate.allObjects.reversed() {
        for delegate in self.multiDelegate.allObjects.reversed() {
            //: invocation(delegate as! IMManagerDelegate)
            invocation(delegate as! FriendshipObjectProtocol)
        }
    }
}

// MARK: - IM登录态管理

//: extension IonV2Listener {
extension IonV2Listener {
    /// 退出登录IM
    //: func func__LogingOut() {
    func unfit() {
        //: V2TIMManager.sharedInstance().logout {} fail: { code, desc in
        V2TIMManager.sharedInstance().logout {} fail: { code, desc in
            //: UploadLogTool.writeLog(msg: "IM退出登录失败，code:\(code), desc:\(String(describing: desc)).")
            MigrationThen.pathAge(msg: (show_continuingReportTitle + String(show_lookStreamMsg)) + "\(code)" + (String(showFenceBanTitle.prefix(7))) + "\(String(describing: desc)).")
        }
        //: self.initialization()
        self.within()
    }

    /// 登录IM
    //: func func__LogingIn(completion: ((_ succeed: Bool) -> Void)? = nil) {
    func involvement(completion: ((_ succeed: Bool) -> Void)? = nil) {
        //: guard self.isConnection == false else { return }
        guard self.isConnection == false else { return }
        //: let uSigStr = FacultyReactiveCompatible.share.appUserConfigMode.uSig
        let uSigStr = FacultyReactiveCompatible.share.appUserConfigMode.uSig
        //: let uidStr = FacultyReactiveCompatible.share.loginUserMode.userID
        let uidStr = FacultyReactiveCompatible.share.loginUserMode.userID
        //: V2TIMManager.sharedInstance().login(uidStr, userSig: uSigStr) {
        V2TIMManager.sharedInstance().login(uidStr, userSig: uSigStr) {
            //: self.isConnection = true
            self.isConnection = true
            //: DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 1) {
            DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 1) {
                //: self.func__getUnreadMsgCount()
                self.unspecialized()
            }
            //: completion?(true)
            completion?(true)
            //: } fail: { code, desc in
        } fail: { code, desc in
            //: UploadLogTool.writeLog(msg: "IM登录失败，code:\(code), desc:\(String(describing: desc)).")
            MigrationThen.pathAge(msg: (data_attributeName + String(app_searchPath) + String(const_pinValue.prefix(5))) + "\(code)" + (String(showFenceBanTitle.prefix(7))) + "\(String(describing: desc)).")
            //: self.isConnection = false
            self.isConnection = false
            //: completion?(false)
            completion?(false)
        }
    }

    /// 检测是否登录
    //: func func__checkCanOperateList() -> Bool {
    func sweep() -> Bool {
        //: let status = V2TIMManager.sharedInstance().getLoginStatus()
        let status = V2TIMManager.sharedInstance().getLoginStatus()
        //: if self.isConnection == false || status != .STATUS_LOGINED || FacultyReactiveCompatible.share.networkStatus == .Unavailable {
        if self.isConnection == false || status != .STATUS_LOGINED || FacultyReactiveCompatible.share.networkStatus == .Unavailable {
            //: self.func__showStatusBarErrorMsg(showMsg: kNetErrorMsg)
            self.micromicronProcess(showMsg: user_robotCameraName)
            //: return false
            return false
        }
        //: return true
        return true
    }
}

// MARK: - 获取未读消息数、wholikeme消息角标、聊天室信息

//: extension IonV2Listener {
extension IonV2Listener {
    /// 获取未读消息数
    //: func func__getUnreadMsgCount() {
    func unspecialized() {
        //: V2TIMManager.sharedInstance().getTotalUnreadMessageCount { totalCount in
        V2TIMManager.sharedInstance().getTotalUnreadMessageCount { totalCount in
            //: self.onTotalUnreadMessageCountChanged(totalCount)
            self.onTotalUnreadMessageCountChanged(totalCount)
            //: } fail: { code, desc in
        } fail: { code, desc in
            //: UploadLogTool.writeLog(msg: "IM获取未读消息总数失败，code:\(code), desc:\(String(describing: desc)).")
            MigrationThen.pathAge(msg: (String(app_missingName) + kProperlyEdgeRecoverMsg) + "\(code)" + (String(showFenceBanTitle.prefix(7))) + "\(String(describing: desc)).")
        }
    }

    /// 刷新wholikeme消息角标
    //: func func__refreshSysMsg() {
    func clog() {
        //: let nowTs = NSDate.timeIntervalSinceReferenceDate
        let nowTs = NSDate.timeIntervalSinceReferenceDate
        //: if nowTs - self.lastSysTime < 10 || self.isRefreshSysMsg {
        if nowTs - self.lastSysTime < 10 || self.isRefreshSysMsg {
            //: return
            return
        }
        //: self.lastSysTime = nowTs
        self.lastSysTime = nowTs
        //: self.isRefreshSysMsg = true
        self.isRefreshSysMsg = true

        //: TalkingChatRequestTool.req_sysMessageList { _, result in
        PouchReactiveCompatible.selectlistCompletion { _, result in
            //: self.isRefreshSysMsg = false
            self.isRefreshSysMsg = false
            //: guard let dict = result as? [String: Any] else {
            guard let dict = result as? [String: Any] else {
                //: return
                return
            }
            //: let number = dict["crushBadge"] as! Int
            let number = dict[(main_journalTitle.replacingOccurrences(of: "bottom", with: "r") + "Badg" + String(k_measurementValue))] as! Int
            //: NotificationCenter.default.post(name: WHOLIKEME_BADGENUMBER_NOTIF, object: self, userInfo: ["badNumber": number])
            NotificationCenter.default.post(name: show_lastData, object: self, userInfo: [String(bytes: app_lastUrl.map{$0^45}, encoding: .utf8)!: number])
        }
    }

    /// 聊天室信息
    /// - Parameter completion: 回调
    //: func getChatRoomData(completion: @escaping (_ succeed: Bool) -> Void) {
    func extra(completion: @escaping (_ succeed: Bool) -> Void) {
        //: chatRoomList.removeAll()
        chatRoomList.removeAll()
        //: TalkingChatRequestTool.req_getChatRoomInfo { succeed, result, _ in
        PouchReactiveCompatible.walkInMinimize { succeed, result, _ in
            //: guard succeed,
            guard succeed,
                  //: let data = result as? [String: Any],
                  let data = result as? [String: Any],
                  //: let plaza = data["plaza"] as? [String: Any] else {
                  let plaza = data[(noti_demandListenContent.lowercased())] as? [String: Any]
            else {
                //: completion(false)
                completion(false)
                //: return
                return
            }

            //: if plaza["roomNum"] as? Int ?? 0 > 0 {
            if plaza[(String(userRecordingTrustKey) + String(notiKeyText))] as? Int ?? 0 > 0 {
                //: let model = TalkingConversationModel.getPlaceholderModel(chatType: .group)
                let model = MultistageRocketConversationModel.nearGet(chatType: .group)
                //: model.num = plaza["num"] as? Int ?? 0
                model.num = plaza[(main_edgeSongQuickPath.replacingOccurrences(of: "resource", with: "um"))] as? Int ?? 0
                //: model.onlyOneRoomId = plaza["onlyOneRoomId"] as? String ?? ""
                model.onlyOneRoomId = plaza[(String(userEasternFormat.prefix(5)) + "neRo" + String(notiPrivacyStr.suffix(4)))] as? String ?? ""
                //: self.chatRoomList.append(model)
                self.chatRoomList.append(model)
            }
            //: completion(true)
            completion(true)
        }
    }
}

// MARK: - V2TIMAdvancedMsgListener

//: extension IonV2Listener: V2TIMAdvancedMsgListener {
extension IonV2Listener: V2TIMAdvancedMsgListener {
    /// 接收到新消息
    /// - Parameter msg: 消息数据
    //: public func onRecvNewMessage(_ msg: V2TIMMessage!) {
    public func onRecvNewMessage(_ msg: V2TIMMessage!) {
        //: if msg.groupID != nil && (msg.groupID.contains("LIVE-CHAT")) { // 开播 弹幕提醒
        if msg.groupID != nil && (msg.groupID.contains((String(main_consequenceStr)))) { // 开播 弹幕提醒
            //: TalkingDanmuManager.shared().onRecvDanmuNewMsg(msg: msg)
            ShSeparateThen.program().estimatedSpeaker(msg: msg)
            //: return
            return
        }
        //: if msg.sender == FacultyReactiveCompatible.share.loginUserMode.userID { return }
        if msg.sender == FacultyReactiveCompatible.share.loginUserMode.userID { return }

        //: let dic = CircularReactiveCompatible.parseTXMessageData(data: msg.customElem.data)
        let dic = CircularReactiveCompatible.occurrent(data: msg.customElem.data)
        //: let extraDic = dic["extra"] as! [AnyHashable: Any]
        let extraDic = dic[(String(appSeatData))] as! [AnyHashable: Any]
        //: let msgModel = try? AbTalkingChatMsgBaseModel(dictionary: extraDic)
        let msgModel = try? ReadModel(dictionary: extraDic)

        //: NotificationCenter.default.post(name: CHAT_NEW_MSG_NOTIFICATION, object: msg)
        NotificationCenter.default.post(name: notiSuccessKey, object: msg)

        //: if let type = extraDic["msgType"] {
        if let type = extraDic[(String(dataPipeUrl))] {
            //: if type as? String == "newPkgGift" { // 刷新礼物背包
            if type as? String == (String(notiSecretPath.prefix(8)) + data_womanFrameKey.replacingOccurrences(of: "already", with: "t")) { // 刷新礼物背包
                //: NotificationCenter.default.post(name: GIFT_REFRESHPACKAGE_NOTIFICATION, object: nil)
                NotificationCenter.default.post(name: userFormatExploreStr, object: nil)
            }
        }

        //: if msg.userID != nil && msg.userID.count > 0 {
        if msg.userID != nil && msg.userID.count > 0 {
            //: if CircularReactiveCompatible.checkVoiceMessage(message: msg) {
            if CircularReactiveCompatible.hottentotSBread(message: msg) { // 语音消息
                //: let dic = TUISocialChatManager.packageDict(withTXMessage: msg)
                let dic = CivilityReactiveCompatible.arena(withTXMessage: msg)
                //: WCDBVoiceMsgTable.db_inserVoiceMsgToDB(dic)
                SpireThen.exceptAction(dic)
            }
            //: self.judgeisHaveIntimate(msg: msg, dic: dic)
            self.written(msg: msg, dic: dic)
            // 处理礼物消息
            //: AbTalkingPrivateChatAnimatTool.shared.disposeReceiveGiftMsg(giftMessageDic: dic as NSDictionary)
            BuildrThen.shared.zone(giftMessageDic: dic as NSDictionary)
        }
        //: if msgModel?.msgInfo.messageType == .MessageTypeGift {
        if msgModel?.msgInfo.messageType == .MessageTypeGift { // 群聊收礼
            //: AbTalkingPrivateChatAnimatTool.shared.chatRoomDisposeReceiveGiftMsg(giftMessageDic: dic as NSDictionary)
            BuildrThen.shared.belowLogTo(giftMessageDic: dic as NSDictionary)
        }
        //: if msg.customElem.extension == "MF:ChatMentionMsg" { // 群聊@ 消息
        if msg.customElem.extension == (String(showDistanceId) + String(kThreeData)) { // 群聊@ 消息
            //: TalkingGroupChatManager.share.receivedMentionMsg(msg: msg, extraInfo: extraDic)
            ReportedReactiveCompatible.share.towardDevice(msg: msg, extraInfo: extraDic)
        }

        //: if msgModel?.gift.unlockMsgId.count ?? 0 > 0 {
        if msgModel?.gift.unlockMsgId.count ?? 0 > 0 { // 记录解锁礼物消息
            //: var array: Array = UserDefaults.standard.object(forKey: TalkingPrivateChatManager.share.UnlockMsgIdArrayKey) as? [String] ?? [String]()
            var array: Array = UserDefaults.standard.object(forKey: SpanChatManager.share.UnlockMsgIdArrayKey) as? [String] ?? [String]()
            //: array.append(msgModel!.gift.unlockMsgId)
            array.append(msgModel!.gift.unlockMsgId)
            //: UserDefaults.standard.set(array, forKey: TalkingPrivateChatManager.share.UnlockMsgIdArrayKey)
            UserDefaults.standard.set(array, forKey: SpanChatManager.share.UnlockMsgIdArrayKey)
        }
        //: if msg.sender != MacroBorderDefineThen.getXiaoMiID() && (msg.userID != nil && msg.userID.count > 0) {
        if msg.sender != MacroBorderDefineThen.seethe() && (msg.userID != nil && msg.userID.count > 0) { // 过滤系统通知、群聊
            // delegate转发
            //: self.func__invokeDelegate { $0.onRecvNewMessage?(msg: msg) }
            self.moon { $0.own?(msg: msg) }
            // 处理消息通知
            //: TalkingMessageNotificationManager.shared.onRecvNewMessage(msg: msg, dataDict: dic)
            InscriptionThen.shared.goReport(msg: msg, dataDict: dic)
            // 更新消息列表用户为在线状态
            //: DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 1) {
            DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 1) {
                //: let userInfo = ["uid": msg.userID ?? "",
                let userInfo = [String(bytes: user_simplePath.map{subjectDescend(country: $0)}, encoding: .utf8)!: msg.userID ?? "",
                                //: "onlineStatus": 1,
                                String(bytes: noti_frameworkValue.reversed(), encoding: .utf8)!: 1,
                                //: "isNewUser": msgModel?.msgInfo.isNewUser ?? false] as [String: Any]
                                String(bytes: k_interestTitle.reversed(), encoding: .utf8)!: msgModel?.msgInfo.isNewUser ?? false] as [String: Any]
                //: NotificationCenter.default.post(name: MSGLIST_UPDATE_ONLINESTATUS_NOTIFICATION,
                NotificationCenter.default.post(name: show_deviceFormat,
                                                //: object: nil,
                                                object: nil,
                                                //: userInfo: userInfo)
                                                userInfo: userInfo)
            }
        }
    }

    /// C2C 收到对端用户会话已读通知
    //: public func onRecvC2CReadReceipt(_ receiptList: [V2TIMMessageReceipt]!) {
    public func onRecvC2CReadReceipt(_ receiptList: [V2TIMMessageReceipt]!) {
        //: for receipt in receiptList {
        for receipt in receiptList {
            // 更新数据库和缓存
            //: DBUserInfoManager.cache_updateReadReceiptTime(with: receipt.userID, time: receipt.timestamp)
            InertGasThen.today(with: receipt.userID, time: receipt.timestamp)
            //: TalkingPrivateChatManager.share.readReceiptDict[receipt.userID] = receipt.timestamp
            SpanChatManager.share.readReceiptDict[receipt.userID] = receipt.timestamp
            // 发送已读回执通知
            //: NotificationCenter.default.post(name: CHAT_MSG_READ_RECEIPT_NOTIFICATION, object: receipt)
            NotificationCenter.default.post(name: appKeyPath, object: receipt)
        }
    }

    //: @objc public func func__handleMsg(targetId: String, msgCellData: AgoneCellData, isPrivete: Bool) {
    @objc public func disposeOf(targetId: String, msgCellData: AgoneCellData, isPrivete: Bool) {
        //: if let imMsg = msgCellData.innerMessage {
        if let imMsg = msgCellData.innerMessage {
            //: self.func__insertMsgToLocalStorage(targetId: targetId, imMsg: imMsg, isPrivete: isPrivete)
            self.beanPresentation(targetId: targetId, imMsg: imMsg, isPrivete: isPrivete)
        }
        //: var convId = ""
        var convId = ""
        //: if isPrivete {
        if isPrivete {
            //: convId = "c2c_\(targetId)"
            convId = (String(mainMeanKey.prefix(4))) + "\(targetId)"
        }
        //: DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 1) {
        DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 1) {
            //: V2TIMManager.sharedInstance().getConversation(convId) { [weak self] conv in
            V2TIMManager.sharedInstance().getConversation(convId) { [weak self] conv in
                //: guard let self = self else { return }
                guard let self = self else { return }
                //: if conv != nil {
                if conv != nil {
                    //: let list = self.func__handleConversationList(convList: [conv!])
                    let list = self.design(convList: [conv!])
                    //: self.func__updateConversationList(conVersationList: list)
                    self.sequence(conVersationList: list)
                }
                //: } fail: { code, desc in
            } fail: { code, desc in
                //: UploadLogTool.writeLog(msg: "IM获取会话失败，convId:\(convId), code:\(code), desc:\(String(describing: desc)).")
                MigrationThen.pathAge(msg: (String(showScenarioTitle.prefix(5)) + "话\u{5931}败，con" + String(dataRoundingPath.suffix(4))) + "\(convId)" + (String(show_clothesTickMsg.prefix(7))) + "\(code)" + (String(showFenceBanTitle.prefix(7))) + "\(String(describing: desc)).")
            }
        }
    }

    //: func func__insertMsgToLocalStorage(targetId: String, imMsg: V2TIMMessage, isPrivete: Bool) {
    func beanPresentation(targetId: String, imMsg: V2TIMMessage, isPrivete: Bool) {
        //: let senderStr = FacultyReactiveCompatible.share.loginUserMode.userID
        let senderStr = FacultyReactiveCompatible.share.loginUserMode.userID
        //: if isPrivete {
        if isPrivete {
            //: V2TIMManager.sharedInstance().insertC2CMessage(toLocalStorage: imMsg, to: targetId, sender: senderStr) {} fail: { code, desc in
            V2TIMManager.sharedInstance().insertC2CMessage(toLocalStorage: imMsg, to: targetId, sender: senderStr) {} fail: { code, desc in
                //: UploadLogTool.writeLog(msg: "IM插入消息失败，targetId:\(targetId), code:\(code), desc:\(String(describing: desc)).")
                MigrationThen.pathAge(msg: (String(k_haulData) + String(noti_sitId.suffix(5)) + "etId:") + "\(targetId)" + (String(show_clothesTickMsg.prefix(7))) + "\(code)" + (String(showFenceBanTitle.prefix(7))) + "\(String(describing: desc)).")
            }
        }
    }

    //: func judgeisHaveIntimate(msg: V2TIMMessage, dic: [String: Any]) {
    func written(msg: V2TIMMessage, dic: [String: Any]) {
        //: let jsonDic = JSON(dic)
        let jsonDic = JSON(dic)
        //: let isSelf = jsonDic["changeDirection"].boolValue
        let isSelf = jsonDic[(String(dataPortKey) + notiPublishMsg.replacingOccurrences(of: "recommend", with: "t"))].boolValue
        //: if jsonDic["rYMsgType"] == "GJ:CallCustom" && isSelf && SmartThen.share.func__getCurrentActivityVC()!.isKind(of: TalkingPrivateChatController.self) == false {
        if jsonDic[(String(kBelowMessage.prefix(4)) + String(constAreaFosterPath))].stringValue == (String(notiVisualShadeKey.suffix(4)) + String(k_detectPath.suffix(5)) + "stom") && isSelf && SmartThen.share.stepFind()!.isKind(of: BorderInsularViewDelegate.self) == false {
            //: var intimate: [String: Any] = UserDefaults.standard.object(forKey: isConversationIntimateCacheKey) as? [String: Any] ?? Dictionary()
            var intimate: [String: Any] = UserDefaults.standard.object(forKey: showWarnMsg) as? [String: Any] ?? Dictionary()
            //: intimate.updateValue(msg.userID ?? "", forKey: msg.userID)
            intimate.updateValue(msg.userID ?? "", forKey: msg.userID)
            //: UserDefaults.standard.set(intimate, forKey: isConversationIntimateCacheKey)
            UserDefaults.standard.set(intimate, forKey: showWarnMsg)
        }
    }
}

// MARK: - V2TIMConversationListener【会话、未读数】

//: extension IonV2Listener: V2TIMConversationListener {
extension IonV2Listener: V2TIMConversationListener {
    /// 有新的会话
    //: public func onNewConversation(_ conversationList: [V2TIMConversation]!) {
    public func onNewConversation(_ conversationList: [V2TIMConversation]!) {
        //: let list = self.func__handleConversationList(convList: conversationList)
        let list = self.design(convList: conversationList)
        //: self.func__updateConversationList(conVersationList: list)
        self.sequence(conVersationList: list)
    }

    /// 某些会话的关键信息发生变化
    //: public func onConversationChanged(_ conversationList: [V2TIMConversation]!) {
    public func onConversationChanged(_ conversationList: [V2TIMConversation]!) {
        //: let list = self.func__handleConversationList(convList: conversationList)
        let list = self.design(convList: conversationList)
        //: self.func__updateConversationList(conVersationList: list)
        self.sequence(conVersationList: list)
    }

    /// 会话未读总数变更通知
    //: public func onTotalUnreadMessageCountChanged(_ totalUnreadCount: UInt64) {
    public func onTotalUnreadMessageCountChanged(_ totalUnreadCount: UInt64) {
        //: FacultyReactiveCompatible.share.unreadMessageNum = Int(totalUnreadCount)
        FacultyReactiveCompatible.share.unreadMessageNum = Int(totalUnreadCount)
        //: UIApplication.shared.applicationIconBadgeNumber = Int(totalUnreadCount)
        UIApplication.shared.applicationIconBadgeNumber = Int(totalUnreadCount)
        //: self.func__invokeDelegate {
        self.moon {
            //: $0.onUnreadMsgCountChanged?(count: Int(totalUnreadCount))
            $0.addUp?(count: Int(totalUnreadCount))
        }
    }

    /// 批量更新会话列表
    //: func func__updateConversationList(conVersationList: [TalkingConversationModel]) {
    func sequence(conVersationList: [MultistageRocketConversationModel]) {
        //: DispatchQueue.main.async {
        DispatchQueue.main.async {
            // 防止刚启动APP，消息列表还未初始化，先缓存数据，找合适时机批量请求
            //: if DBUserInfoManager.shared().needReqUidsSet != nil {
            if InertGasThen.arcShared().needReqUidsSet != nil {
                // 提取非空 userId 并去重
                //: let set = Set(conVersationList.compactMap { $0.userID.isEmpty ? nil : $0.userID })
                let set = Set(conVersationList.compactMap { $0.userID.isEmpty ? nil : $0.userID })
                //: DBUserInfoManager.shared().needReqUidsSet!.formUnion(set)
                InertGasThen.arcShared().needReqUidsSet!.formUnion(set)
            }
            //: self.func__invokeDelegate {
            self.moon {
                //: $0.onRefreshConversationList?(data: conVersationList)
                $0.sample?(data: conVersationList)
            }
        }
    }

    //: func func__handleTXConversationList(listArr: [V2TIMConversation], completion: @escaping (_ arr: [Any], _ leftMore: Bool) -> Void) {
    func moonRound(listArr: [V2TIMConversation], completion: @escaping (_ arr: [Any], _ leftMore: Bool) -> Void) {
        //: var convList: [TalkingConversationModel] = []
        var convList: [MultistageRocketConversationModel] = []

        // V2TIMConversation->MultistageRocketConversationModel
        //: for conversation in listArr {
        for conversation in listArr {
            //: let targetId = (conversation.userID != nil && conversation.userID.count > 0) ? conversation.userID : conversation.groupID
            let targetId = (conversation.userID != nil && conversation.userID.count > 0) ? conversation.userID : conversation.groupID
            //: var tempConvModel = self.convListCache[targetId!]
            var tempConvModel = self.convListCache[targetId!]
            //: if tempConvModel == nil {
            if tempConvModel == nil {
                //: tempConvModel = TalkingConversationModel(conv: conversation)
                tempConvModel = MultistageRocketConversationModel(conv: conversation)
            }
            //: tempConvModel?.func__updateLastConvModel(conv: conversation)
            tempConvModel?.transformLess(conv: conversation)
            //: convList.append(tempConvModel!)
            convList.append(tempConvModel!)
        }

        // 增量
        //: var resultList: [TalkingConversationModel] = []
        var resultList: [MultistageRocketConversationModel] = []
        //: let dbModels = WCDBUserInfoTable.db_getUserInfos(with: Array(self.convListCache.keys))
        let dbModels = IonInfoTable.borderTotal(with: Array(self.convListCache.keys))
        //: let dbIdSet = Set(dbModels.map { $0.uid })
        let dbIdSet = Set(dbModels.map { $0.uid })
        //: for conversation in convList {
        for conversation in convList {
            // 添加db中是否有数据判断，防止刚启动APP，Delegate方法还未实现
            //: if self.func__addConvList(convModel: conversation, insertTop: false) ||
            if self.anyUniversal(convModel: conversation, insertTop: false) ||
                //: (conversation.userID.count > 0 && !dbIdSet.contains(conversation.targetId))
                (conversation.userID.count > 0 && !dbIdSet.contains(conversation.targetId))
            {
                //: resultList.append(conversation)
                resultList.append(conversation)
            }
        }
        //: let leftMore = (resultList.count > 0)
        let leftMore = (resultList.count > 0)
        //: completion(resultList, leftMore)
        completion(resultList, leftMore)
    }
}

// MARK: - 会话排序、删除、置顶

//: extension IonV2Listener {
extension IonV2Listener {
    /// 获取消息列表数据
    /// - Parameter completion: 回调
    //: func func__getConversationList(completion: @escaping (_ arr: [Any], _ leftMore: Bool) -> Void) {
    func colloquy(completion: @escaping (_ arr: [Any], _ leftMore: Bool) -> Void) {
        //: V2TIMManager.sharedInstance().getConversationList(self.nextSeq, count: 40) { list, nextSeq, _ in
        V2TIMManager.sharedInstance().getConversationList(self.nextSeq, count: 40) { list, nextSeq, _ in
            //: self.nextSeq = nextSeq
            self.nextSeq = nextSeq
            //: self.func__handleTXConversationList(listArr: list!) { arr, leftMore in
            self.moonRound(listArr: list!) { arr, leftMore in
                //: self.func__updateConversationList(conVersationList: arr as? [TalkingConversationModel] ?? [])
                self.sequence(conVersationList: arr as? [MultistageRocketConversationModel] ?? [])
                //: completion(arr, leftMore)
                completion(arr, leftMore)
            }
            //: } fail: { code, desc in
        } fail: { code, desc in
            //: UploadLogTool.writeLog(msg: "IM获取会话列表失败，code:\(code), desc:\(String(describing: desc)).")
//            MigrationThen.pathAge(msg: (String(constNeighborText.suffix(5)) + notiNighFormat.capitalized + "败，code:") + "\(code)" + (String(showFenceBanTitle.prefix(7))) + "\(String(describing: desc)).")
        }
    }

    /// 从缓存会话中获取模型
    //: func func__getCacheConversationModel(targetId: String) -> TalkingConversationModel? {
    func completeId(targetId: String) -> MultistageRocketConversationModel? {
        //: return self.convListCache[targetId]
        return self.convListCache[targetId]
    }

    //: func func__handleConversationList(convList: [V2TIMConversation]) -> [TalkingConversationModel] {
    func design(convList: [V2TIMConversation]) -> [MultistageRocketConversationModel] {
        //: var tempArr: [TalkingConversationModel] = []
        var tempArr: [MultistageRocketConversationModel] = []
        //: for conv in convList {
        for conv in convList {
            //: let targetId = conv.userID ?? conv.groupID
            let targetId = conv.userID ?? conv.groupID
            //: var convModel = self.convListCache[targetId ?? ""]
            var convModel = self.convListCache[targetId ?? ""]
            //: if convModel == nil {
            if convModel == nil {
                //: convModel = TalkingConversationModel(conv: conv)
                convModel = MultistageRocketConversationModel(conv: conv)
            }
            // 更新会话
            //: convModel?.func__updateLastConvModel(conv: conv)
            convModel?.transformLess(conv: conv)
            //: if self.func__addConvList(convModel: convModel!, insertTop: true) {
            if self.anyUniversal(convModel: convModel!, insertTop: true) {
                //: tempArr.append(convModel!)
                tempArr.append(convModel!)
            }
        }
        //: return tempArr
        return tempArr
    }

    /// 会话列表新增会话
    /// - Parameters:
    ///   - convModel: 会话模型
    ///   - insertTop: 是否插入顶部
    /// - Returns: 是否添加成功
    //: func func__addConvList(convModel: TalkingConversationModel, insertTop: Bool) -> Bool {
    func anyUniversal(convModel: MultistageRocketConversationModel, insertTop: Bool) -> Bool {
        //: if convModel.chatType == .unDefined {
        if convModel.chatType == .unDefined {
            //: return false
            return false
        }
        //: if !convModel.groupID.isEmptyString && convModel.chatType == .group { return false }
        if !convModel.groupID.isEmptyString && convModel.chatType == .group { return false }

        //: let targetId = convModel.targetId
        let targetId = convModel.targetId
        // 系统消息，无需重新排序
        //: if convModel.chatType == .system || convModel.chatType == .service {
        if convModel.chatType == .system || convModel.chatType == .service {
            //: self.convListCache[targetId] = convModel
            self.convListCache[targetId] = convModel
            //: if self.officialList.count == 0 {
            if self.officialList.count == 0 {
                //: self.func__adddofficialList()
                self.withoutAlongside()
            }
            //: if let index = officialList.firstIndex(where: { $0.userID == convModel.userID }) {
            if let index = officialList.firstIndex(where: { $0.userID == convModel.userID }) {
                //: let data = self.officialList[index]
                let data = self.officialList[index]
                //: convModel.showName = data.showName
                convModel.showName = data.showName
                //: self.officialList[index] = convModel
                self.officialList[index] = convModel
            }
            //: return true
            return true
        }

        // 普通消息
        //: var dataType: ChatListDataType = .normalList
        var dataType: YouthCultureDataType = .normalList
        //: var listArr = self.norConvList
        var listArr = self.norConvList
        //: let timeInterval = Int(convModel.listShowMessage?.timestamp.timeIntervalSince1970 ?? 0) - FacultyReactiveCompatible.share.appConfigMode.moreMessageTimestamp
        let timeInterval = Int(convModel.listShowMessage?.timestamp.timeIntervalSince1970 ?? 0) - FacultyReactiveCompatible.share.appConfigMode.moreMessageTimestamp
        // 是否建立会话
        //: var haveSeSSion = false
        var haveSeSSion = false
        //: if let userModel = DBUserInfoManager.cache_getCachedUserInfo(targetId: convModel.targetId) {
        if let userModel = InertGasThen.biologicalAgent(targetId: convModel.targetId) {
            //: haveSeSSion = userModel.isHaveSession
            haveSeSSion = userModel.isHaveSession
        }
        // 是否需要折叠
        //: var needFold = false
        var needFold = false
        //: if let elemData = convModel.listShowMessage?.customElem.data {
        if let elemData = convModel.listShowMessage?.customElem.data {
            //: let dict = CircularReactiveCompatible.parseTXMessageData(data: elemData)
            let dict = CircularReactiveCompatible.occurrent(data: elemData)
            //: let json = JSON(dict)
            let json = JSON(dict)
            //: needFold = json["extra"]["msgInfo"]["needFold"].boolValue
            needFold = json[(String(appSeatData))][(String(main_starDomainUrl.prefix(7)))][(String(app_simultaneouslyValue) + appYouMessage.replacingOccurrences(of: "climb", with: "d"))].boolValue
        }
        // 非置顶，非自己发送，超出时间 || 未建立会话，需要折叠
        //: if (convModel.isPinned == false && convModel.listShowMessage?.isSelf == false && timeInterval <= 0) ||
        if (convModel.isPinned == false && convModel.listShowMessage?.isSelf == false && timeInterval <= 0) ||
            //: (!haveSeSSion && needFold && convModel.isPinned == false)
            (!haveSeSSion && needFold && convModel.isPinned == false)
        {
            //: dataType = .moreList
            dataType = .moreList
            //: listArr = self.moreMsgList
            listArr = self.moreMsgList
            // 移除重复数据
            //: self.topConvList.removeAll { $0.userID == convModel.userID }
            self.topConvList.removeAll { $0.userID == convModel.userID }
            //: self.norConvList.removeAll { $0.userID == convModel.userID }
            self.norConvList.removeAll { $0.userID == convModel.userID }

            //: } else {
        } else {
            // 移除更多消息
            //: self.moreMsgList.removeAll { $0.userID == convModel.userID }
            self.moreMsgList.removeAll { $0.userID == convModel.userID }
            // 置顶消息
            //: if convModel.isPinned {
            if convModel.isPinned {
                //: dataType = .topList
                dataType = .topList
                //: listArr = self.topConvList
                listArr = self.topConvList
            }
        }

        //: let tempConvModel = self.convListCache[targetId]
        let tempConvModel = self.convListCache[targetId]
        //: if tempConvModel != nil {
        if tempConvModel != nil {
            //: if insertTop && listArr.first != tempConvModel {
            if insertTop && listArr.first != tempConvModel {
                //: if let index = listArr.firstIndex(of: tempConvModel!) {
                if let index = listArr.firstIndex(of: tempConvModel!) {
                    //: listArr.remove(at: index)
                    listArr.remove(at: index)
                }
                //: listArr.append(tempConvModel!)
                listArr.append(tempConvModel!)
                //: self.func__sortConVList(convList: listArr, dataType: dataType)
                self.edition(convList: listArr, dataType: dataType)
                //: return true
                return true
            }
            //: return false
            return false
        }

        //: self.convListCache[targetId] = convModel
        self.convListCache[targetId] = convModel
        //: listArr.append(convModel)
        listArr.append(convModel)
        //: self.func__sortConVList(convList: listArr, dataType: dataType)
        self.edition(convList: listArr, dataType: dataType)
        //: return true
        return true
    }

    /// 数据排序
    /// - Parameters:
    ///   - convList: 会话列表
    ///   - dataType: 数据类型
    //: func func__sortConVList(convList: [TalkingConversationModel], dataType: ChatListDataType) {
    func edition(convList: [MultistageRocketConversationModel], dataType: YouthCultureDataType) {
        //: switch dataType {
        switch dataType {
        //: case .topList:
        case .topList:
            //: self.topConvList = convList.sorted { $0.orderKey > $1.orderKey }
            self.topConvList = convList.sorted { $0.orderKey > $1.orderKey }

        //: case .normalList:
        case .normalList:
            //: self.norConvList = convList.sorted { $0.orderKey > $1.orderKey }
            self.norConvList = convList.sorted { $0.orderKey > $1.orderKey }

        //: case .moreList:
        case .moreList:
            //: self.moreMsgList = convList.sorted { $0.orderKey > $1.orderKey }
            self.moreMsgList = convList.sorted { $0.orderKey > $1.orderKey }
        }
    }

    /// 删除会话
    /// - Parameters:
    ///   - targetId: 会话Id
    ///   - dataType: 会话类型
    //: func func__removeConversation(targetId: String, dataType: ChatListDataType) {
    func nameType(targetId: String, dataType: YouthCultureDataType) {
        //: guard !targetId.isEmpty else { return }
        guard !targetId.isEmpty else { return }
        //: guard let tempConvModel = self.convListCache[targetId] else { return }
        guard let tempConvModel = self.convListCache[targetId] else { return }
        //: let convID = (tempConvModel.chatType == .private ? "c2c_\(targetId)" : "group_\(targetId)")
        let convID = (tempConvModel.chatType == .private ? (String(mainMeanKey.prefix(4))) + "\(targetId)" : (notiBetterText.replacingOccurrences(of: "variation", with: "g") + String(mainEnabledValue.suffix(5))) + "\(targetId)")
        //: V2TIMManager.sharedInstance().deleteConversation(convID) {
        V2TIMManager.sharedInstance().deleteConversation(convID) {
            // 移除db
            //: DBUserInfoManager.db_removeRecord(with: targetId)
            InertGasThen.micWith(with: targetId)
            //: WCDBVoiceMsgTable.db_deleteVoiceMsg(FacultyReactiveCompatible.share.loginUserMode.userID, toUid: targetId)
            SpireThen.cover(FacultyReactiveCompatible.share.loginUserMode.userID, toUid: targetId)

            //: self.convListCache.removeValue(forKey: targetId)
            self.convListCache.removeValue(forKey: targetId)
            //: switch dataType {
            switch dataType {
            //: case .topList:
            case .topList:
                //: if let index = self.topConvList.firstIndex(of: tempConvModel) {
                if let index = self.topConvList.firstIndex(of: tempConvModel) {
                    //: self.topConvList.remove(at: index)
                    self.topConvList.remove(at: index)
                }
            //: case .normalList:
            case .normalList:
                //: if let index = self.norConvList.firstIndex(of: tempConvModel) {
                if let index = self.norConvList.firstIndex(of: tempConvModel) {
                    //: self.norConvList.remove(at: index)
                    self.norConvList.remove(at: index)
                }
            //: case .moreList:
            case .moreList:
                //: if let index = self.moreMsgList.firstIndex(of: tempConvModel) {
                if let index = self.moreMsgList.firstIndex(of: tempConvModel) {
                    //: self.moreMsgList.remove(at: index)
                    self.moreMsgList.remove(at: index)
                }
            }

            //: let unreadCount = tempConvModel.unreadCount
            let unreadCount = tempConvModel.unreadCount
            //: if unreadCount > 0 {
            if unreadCount > 0 {
                //: tempConvModel.unreadCount = 0
                tempConvModel.unreadCount = 0
                //: tempConvModel.isShowMsgUnread = false
                tempConvModel.isShowMsgUnread = false
            }
            //: self.func__getUnreadMsgCount()
            self.unspecialized()

            //: } fail: { code, desc in
        } fail: { code, desc in
            //: UploadLogTool.writeLog(msg: "IM删除会话失败，convID:\(convID), code:\(code), desc:\(String(describing: desc)).")
            MigrationThen.pathAge(msg: (String(appLabelRandomAudienceContent) + show_whatIndexFormat + "vID:") + "\(convID)" + (String(show_clothesTickMsg.prefix(7))) + "\(code)" + (String(showFenceBanTitle.prefix(7))) + "\(String(describing: desc)).")
        }
    }

    /// 置顶会话
    //: func func__setConversationTop(convModel: TalkingConversationModel, isTop: Bool) {
    func top(convModel: MultistageRocketConversationModel, isTop: Bool) {
        //: V2TIMManager.sharedInstance().pinConversation(convModel.conversationID, isPinned: isTop) {} fail: { code, desc in
        V2TIMManager.sharedInstance().pinConversation(convModel.conversationID, isPinned: isTop) {} fail: { code, desc in
            //: UploadLogTool.writeLog(msg: "IM置顶会话失败，convId:\(convModel.conversationID), code:\(code), desc:\(String(describing: desc)).")
//            MigrationThen.pathAge(msg: (String(mainSubKey.suffix(5)) + String(mainStripData.suffix(6)) + String(k_placeId)) + "\(convModel.conversationID)" + (String(show_clothesTickMsg.prefix(7))) + "\(code)" + (String(showFenceBanTitle.prefix(7))) + "\(String(describing: desc)).")
        }
        //: convModel.isPinned = isTop
        convModel.isPinned = isTop
        //: if isTop {
        if isTop {
            //: self.topConvList.insert(convModel, at: 0)
            self.topConvList.insert(convModel, at: 0)
            //: if let index = self.norConvList.firstIndex(of: convModel) {
            if let index = self.norConvList.firstIndex(of: convModel) {
                //: self.norConvList.remove(at: index)
                self.norConvList.remove(at: index)
            }
            //: self.func__sortConVList(convList: self.topConvList, dataType: .topList)
            self.edition(convList: self.topConvList, dataType: .topList)
            //: } else {
        } else {
            //: self.norConvList.insert(convModel, at: 0)
            self.norConvList.insert(convModel, at: 0)
            //: if let index = self.topConvList.firstIndex(of: convModel) {
            if let index = self.topConvList.firstIndex(of: convModel) {
                //: self.topConvList.remove(at: index)
                self.topConvList.remove(at: index)
            }
            //: self.func__sortConVList(convList: self.norConvList, dataType: .normalList)
            self.edition(convList: self.norConvList, dataType: .normalList)
        }
    }
}

// MARK: - V2TIMSDKListener

//: extension IonV2Listener: V2TIMSDKListener {
extension IonV2Listener: V2TIMSDKListener {
    //: public func onConnecting() {
    public func onConnecting() {
        //: printLog(message: "SDK 正在连接到服务器")
        printLog(message: (String(notiHostName.prefix(5)) + userAcquireName))
    }

    //: public func onConnectSuccess() {
    public func onConnectSuccess() {
        //: printLog(message: "SDK 已经成功连接到服务器")
        printLog(message: (String(notiWholeMessage.suffix(5)) + "经\u{6210}\u{529f}连" + noti_gatherPointRegionPath.capitalized))
    }

    //: public func onConnectFailed(_ code: Int32, err: String!) {
    public func onConnectFailed(_ code: Int32, err: String!) {
        //: UploadLogTool.writeLog(msg: "IM连接服务器失败，code:\(code), error:\(err!).")
        MigrationThen.pathAge(msg: (showContainMsg + String(mainComputerMessage.prefix(7))) + "\(code)" + (String(show_positionName) + String(dataStillFashionFormat.prefix(2))) + "\(err!).")
    }

    //: public func onKickedOffline() {
    public func onKickedOffline() {
        //: UploadLogTool.writeLog(msg: "IM账号被挤下线。")
        MigrationThen.pathAge(msg: (k_bagListenPath + mainClimbContent.capitalized))
        // socket断开连接
        //: TalkingSocketManager.shared.closeWebSocket(userLoginOut: true)
        AlsoThen.shared.photographicCamera(userLoginOut: true)
        //: IonV2Listener.shared.isConnection = false
        IonV2Listener.shared.isConnection = false

        //: let msg = "Your account is logged in on another device and you are forced to go offline! If it is not done by me, the password may have been leaked, and it is recommended to change the password.".localized
        let msg = String(bytes: noti_balloonUrl.map{gameResult(relieve: $0)}, encoding: .utf8)!.localized
        //: let config = ShowAlertConfig()
        let config = InstAlertConfig()
        //: if TalkingRequestAddrTool.share.interfaceLang == LangType.ar.rawValue || TalkingRequestAddrTool.share.interfaceLang == LangType.es.rawValue ||
        if RocketRecognizeAddrTool.share.interfaceLang == JumpScalarLiteral.ar.rawValue || RocketRecognizeAddrTool.share.interfaceLang == JumpScalarLiteral.es.rawValue ||
            //: TalkingRequestAddrTool.share.interfaceLang == LangType.pt.rawValue
            RocketRecognizeAddrTool.share.interfaceLang == JumpScalarLiteral.pt.rawValue
        {
            //: config.width = 340
            config.width = 340
            //: config.rightFont = UIFont.pingfangMediumFont(fontSize: 13)
            config.rightFont = UIFont.originally(fontSize: 13)
        }

        //: TalkingAlertShow.alert()
        PersonAlertShow.panAngleKind()
        //: TalkingAlertShow.alert(title: "Offline Notification".localized, message: msg, leftBtnTitle: "Cancel".localized, rightBtnTitle: "Re Login".localized, leftBlock: {
        PersonAlertShow.panAngleKind(title: (String(dataAcceptKey.prefix(6)) + "e No" + constContextGrowthId.replacingOccurrences(of: "lock", with: "if") + "catio" + String(noti_deliveryTitle)).localized, message: msg, leftBtnTitle: (String(userStarMessage.suffix(6))).localized, rightBtnTitle: (String(userTowardValue.suffix(8))).localized, leftBlock: {
            // 调用退出登录接口
            //: TalkingLoginRequestTool.req_loginOut { t in
            GenerationRequestTool.squareStripButton { t in
                //: ProgressHUD.dismiss()
                StrangerProgressHUD.barDismiss()
                //: if t {
                if t {
                    //: NotificationCenter.default.post(name: DID_LOGIN_OUT_SUCCESS_NOTIFICATION, object: nil, userInfo: nil)
                    NotificationCenter.default.post(name: main_contentMessage, object: nil, userInfo: nil)
                }
            }
            //: }, rightBlock: {
        }, rightBlock: {
            //: TalkingLoginRequestTool.func__requestReloginUserInfo { t in
            GenerationRequestTool.send { t in
                //: if t {
                if t {
                    //: IonV2Listener.shared.func__LogingIn { _ in
                    IonV2Listener.shared.involvement { _ in
                        /// 直播弹幕重连
                        //: if TalkingLiveManager.shared().liveRoomModel.chatGroupId.count > 0 {
                        if BeReactiveCompatible.clearExplain().liveRoomModel.chatGroupId.count > 0 {
                            //: V2TIMManager.sharedInstance().joinGroup(TalkingLiveManager.shared().liveRoomModel.chatGroupId, msg: nil) {} fail: { _, _ in
                            V2TIMManager.sharedInstance().joinGroup(BeReactiveCompatible.clearExplain().liveRoomModel.chatGroupId, msg: nil) {} fail: { _, _ in
                            }
                        }
                    }
                    // socket重连
                    //: TalkingSocketManager.shared.updateWebSocket()
                    AlsoThen.shared.applicationSocket()
                }
            }
            //: }, config: config)
        }, config: config)
    }
}
