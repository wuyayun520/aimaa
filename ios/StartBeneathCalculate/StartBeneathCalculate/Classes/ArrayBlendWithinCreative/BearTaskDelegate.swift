
//: Declare String Begin

/*: ; build: :*/
fileprivate let kSegmentTheoryUrl:String = "; build:gallery climb partner detail document"

/*: ; iOS  :*/
fileprivate let notiReasonId:[Character] = [";"," ","i","O","S"," "]

/*: "audio" :*/
fileprivate let dataContinuingWriteTitle:String = "inviteudio"

/*: .wav" :*/
fileprivate let kBalloonValue:String = "mutual wed slow.wav"

/*: "audio/wav" :*/
fileprivate let data_duringFormat:[Character] = ["a","u","d","i","o","/","w","a","v"]

/*: .jpg" :*/
fileprivate let notiProcessingMsg:String = ".jpgink box"

/*: "image/jpeg" :*/
fileprivate let show_collectStr:String = "image/var complex op"

/*: "://app." :*/
fileprivate let kDefinePromptTitle:[Character] = [":","/","/","a","p","p","."]

/*: "data" :*/
fileprivate let mainAppearFormat:String = "dspiretspire"

/*: ":null" :*/
fileprivate let app_remNurseFormat:String = "front dynamics live:null"

/*: "Request failed, Try again later" :*/
fileprivate let showProvidePath:[UInt8] = [0xab,0x9c,0x88,0x8c,0x9c,0x8a,0x8d,0xd9,0x9f,0x98,0x90,0x95,0x9c,0x9d,0xd5,0xd9,0xad,0x8b,0x80,0xd9,0x98,0x9e,0x98,0x90,0x97,0xd9,0x95,0x98,0x8d,0x9c,0x8b]

private func plusEnablely(help num: UInt8) -> UInt8 {
    return num ^ 249
}

/*: "json error" :*/
fileprivate let noti_centerMessage:String = "full feedback breast peer plainjson "
fileprivate let showPrimaName:[Character] = ["e","r","r","o","r"]

/*: "platform=iphone&version= :*/
fileprivate let data_handUrl:[Character] = ["p","l","a","t","f","o","r","m","=","i","p","h","o","n","e","&","v","e","r","s","i","o","n"]
fileprivate let dataHospitalStr:String = "succeed"

/*: &packageId= :*/
fileprivate let const_twoMessage:String = "&packcuriosity relate continuing"

/*: &bundleId= :*/
fileprivate let k_banStr:String = "&bundleIpeer whole"
fileprivate let dataOperationText:String = "measurement wait teemd="

/*: &lang= :*/
fileprivate let app_contextData:String = "channel sit&lang"
fileprivate let kBlendFormat:String = "="

/*: "/route/slowindex" :*/
fileprivate let app_tooPath:String = "/route/slsolution available"
fileprivate let notiModifyMsg:String = "ownodex"

/*: "/route/index" :*/
fileprivate let mainReportName:String = "member spot session/rou"
fileprivate let user_heartMsg:String = "te/indexcarpet arena satisfy mill wife"

/*: "?path= :*/
fileprivate let kWeightText:[Character] = ["?"]
fileprivate let mainPerspectiveData:String = "path=blind or running accommodate red"

/*: "&GJNonceStr= :*/
fileprivate let constWeValidFormat:String = "&GJNoncarena crush carpet"
fileprivate let appIncentiveTitle:String = "eStr=level give"

/*: "&params= :*/
fileprivate let data_pleaseId:[Character] = ["&","p","a","r","a","m","s","="]

/*: "token" :*/
fileprivate let dataInformationId:String = "todecreasen"

/*: "请求成功 :*/
fileprivate let appTingSafetyUrl:[Character] = ["\u{8bf7}","\u{6c42}","成","\u{529f}"]

/*: "AAAAAAAA" :*/
fileprivate let kTempFormat:String = "swallowswallowswallowswallowswallowswallowswallow"
fileprivate let appProductValue:[Character] = ["A"]

/*: "下载完成 -  :*/
fileprivate let notiOndStrongName:String = "下载完成 -"
fileprivate let kListenerText:String = "balloon"

/*: "/Documents/" :*/
fileprivate let const_stretchMessage:[Character] = ["/","D","o","c"]
fileprivate let dataPersonalMessage:String = "out increase pass counteruments/"

/*: ".mp4" :*/
fileprivate let data_lockStr:[Character] = [".","m","p","4"]

/*: "移动地址: :*/
fileprivate let userEarnKey:[Character] = ["移","动","地","址",":"]

/*: "后台任务下载回来" :*/
fileprivate let showDatePath:String = "后\u{53f0}任\u{52a1}下载回来"

/*: "下载成功 : :*/
fileprivate let kDataPromptUrl:String = "下载成功 :"

/*: "Net Error" :*/
fileprivate let main_secureTillAccelerateName:String = "Net magnitude require wound"
fileprivate let main_directionBalanceId:String = "Erroraccess retain simple communication"

/*: "下载失败 : :*/
fileprivate let showOnPushData:String = "下\u{8f7d}\u{5931}败 :"

//: Declare String End

// __DEBUG__
// __CLOSE_PRINT__
//
//  BearTaskDelegate.swift
//  AbroadTalking
//
//  Created by Hemming on 2022/1/28.
//

//: import Alamofire
import Alamofire
//: import CoreMedia
import CoreMedia
//: import UIKit
import UIKit

// 默认请求超时时间
//: let REQUEST_TIMEOUT_INTERVAL = 10.0
let mainBeginUrl = 10.0

//: let userAgent = "\(AppName)/\(AppVersion) (\(AppBundle); build:\(AppBuildNumber); iOS \(UIDevice.current.systemVersion); \(UIDevice.modelName))"
let const_pingReasonPath = "\(appCollectionKey)/\(kTitleEnterId) (\(const_onMsg)" + (String(kSegmentTheoryUrl.prefix(8))) + "\(const_matchMsg)" + (String(notiReasonId)) + "\(UIDevice.current.systemVersion); \(UIDevice.modelName))"

//: typealias FinishBlock = (_ succeed: Bool, _ result: Any?, _ errorModel: TalkingErrorResponse?) -> Void
typealias FinishBlock = (_ succeed: Bool, _ result: Any?, _ errorModel: MovementErrorResponse?) -> Void

//: let GJ = TalkingRequestTool.init()
let appAdjustData = BearTaskDelegate()

//: @objc class TalkingRequestTool: NSObject, URLSessionDownloadDelegate {
@objc class BearTaskDelegate: NSObject, URLSessionDownloadDelegate {
    //: public func startRequest(model: TalkingRequestModel, completion: @escaping FinishBlock) {
    public func kitEspecial(model: ReportedThen, completion: @escaping FinishBlock) {
        //: if model.paramsContainBinaryObject() {
        if model.ownAcrossJournal() {
            //: self.startUploadDataRequest(model: model, completion: completion)
            self.forwardBy(model: model, completion: completion)
            //: } else {
        } else {
            //: self.startNormalRequest(model: model, completion: completion)
            self.signToCompletion(model: model, completion: completion)
        }
    }

    //: public func startUploadDataRequest(model: TalkingRequestModel, completion: @escaping FinishBlock) {
    public func forwardBy(model: ReportedThen, completion: @escaping FinishBlock) {
        //: let serverUrl = self.buildServerUrl(model: model)
        let serverUrl = self.quantityermanIq(model: model)
        //: let headers = self.getRequestHeader(model: model)
        let headers = self.intervalerrelate(model: model)

        //: if model.requestType == .GET {
        if model.requestType == .GET {
            //: AF.request(serverUrl, method: .get, parameters: model.params, headers: headers).responseData { [self] responseData in
            AF.request(serverUrl, method: .get, parameters: model.params, headers: headers).responseData { [self] responseData in
                //: switch responseData.result {
                switch responseData.result {
                //: case .success:
                case .success:
                    //: func__requestSucess(model: model, response: responseData.response!, responseData: responseData.data!, completion: completion)
                    wharfageObjectCompletionFactor(model: model, response: responseData.response!, responseData: responseData.data!, completion: completion)
                //: break
                //: case .failure:
                case .failure:
                    //: completion(false, nil, TalkingErrorResponse.init(errorCode: RequestResultCode.NetError.rawValue, errorMsg: kNetErrorMsg))
                    completion(false, nil, MovementErrorResponse(errorCode: AuditorInsetTarget.NetError.rawValue, errorMsg: user_robotCameraName))
                    //: if model.showErrorStatusBar {
                    if model.showErrorStatusBar {
                        //: func__showStatusBarErrorMsg(showMsg: kNetErrorMsg)
                        micromicronProcess(showMsg: user_robotCameraName)
                    }

                    //: break
                }
            }
            //: } else {
        } else {
            //: AF.upload(multipartFormData: { (multipartFormData) in
            AF.upload(multipartFormData: { multipartFormData in
                //: for (key, value) in model.params {
                for (key, value) in model.params {
                    //: if value is Data {
                    if value is Data {
                        //: if key == "audio" {
                        if key == (dataContinuingWriteTitle.replacingOccurrences(of: "invite", with: "a")) {
                            //: multipartFormData.append(value as! Data, withName: key, fileName: "\(NSDate.getCurrentTimeStamp())+\(PackageID).wav", mimeType: "audio/wav")
                            multipartFormData.append(value as! Data, withName: key, fileName: "\(NSDate.voiceBy())+\(showViewValue)" + (String(kBalloonValue.suffix(4))), mimeType: (String(data_duringFormat)))
                            //: } else {
                        } else {
                            //: multipartFormData.append(value as! Data, withName: key, fileName: "\(key).jpg", mimeType: "image/jpeg")
                            multipartFormData.append(value as! Data, withName: key, fileName: "\(key)" + (String(notiProcessingMsg.prefix(4))), mimeType: (String(show_collectStr.prefix(6)) + "jpeg"))
                        }
                        //: } else {
                    } else {
                        //: let data_ = "\(value)".data(using: String.Encoding.utf8)!
                        let data_ = "\(value)".data(using: String.Encoding.utf8)!
                        //: multipartFormData.append(data_, withName: key)
                        multipartFormData.append(data_, withName: key)
                    }
                }
                //: }, to: serverUrl, method: .post, headers: headers).responseData { [self] responseData in
            }, to: serverUrl, method: .post, headers: headers).responseData { [self] responseData in
                //: switch responseData.result {
                switch responseData.result {
                //: case .success:
                case .success:
                    //: func__requestSucess(model: model, response: responseData.response!, responseData: responseData.data!, completion: completion)
                    wharfageObjectCompletionFactor(model: model, response: responseData.response!, responseData: responseData.data!, completion: completion)
                //: break
                //: case .failure:
                case .failure:
                    //: completion(false, nil, TalkingErrorResponse.init(errorCode: RequestResultCode.NetError.rawValue, errorMsg: kNetErrorMsg))
                    completion(false, nil, MovementErrorResponse(errorCode: AuditorInsetTarget.NetError.rawValue, errorMsg: user_robotCameraName))
                    //: if model.showErrorStatusBar {
                    if model.showErrorStatusBar {
                        //: func__showStatusBarErrorMsg(showMsg: kNetErrorMsg)
                        micromicronProcess(showMsg: user_robotCameraName)
                    }

                    //: break
                }
            }
        }
    }

    //: public func startNormalRequest(model: TalkingRequestModel, completion: @escaping FinishBlock) {
    public func signToCompletion(model: ReportedThen, completion: @escaping FinishBlock) {
        //: let serverUrl = self.buildServerUrl(model: model)
        let serverUrl = self.quantityermanIq(model: model)
        //: let headers = self.getRequestHeader(model: model)
        let headers = self.intervalerrelate(model: model)

        //: if model.requestType == .GET {
        if model.requestType == .GET {
            //: AF.request(serverUrl, method: .get, parameters: (Encryption_Request ? nil : model.params), headers: headers, requestModifier: { $0.timeoutInterval = REQUEST_TIMEOUT_INTERVAL }).responseData { [self] responseData in
            AF.request(serverUrl, method: .get, parameters: const_saltUrl ? nil : model.params, headers: headers, requestModifier: { $0.timeoutInterval = mainBeginUrl }).responseData { [self] responseData in
                //: switch responseData.result {
                switch responseData.result {
                //: case .success:
                case .success:
                    //: func__requestSucess(model: model, response: responseData.response!, responseData: responseData.data!, completion: completion)
                    wharfageObjectCompletionFactor(model: model, response: responseData.response!, responseData: responseData.data!, completion: completion)
                //: break
                //: case .failure:
                case .failure:
                    //: completion(false, nil, TalkingErrorResponse.init(errorCode: RequestResultCode.NetError.rawValue, errorMsg: kNetErrorMsg))
                    completion(false, nil, MovementErrorResponse(errorCode: AuditorInsetTarget.NetError.rawValue, errorMsg: user_robotCameraName))
                    //: if model.showErrorStatusBar {
                    if model.showErrorStatusBar {
                        //: func__showStatusBarErrorMsg(showMsg: kNetErrorMsg)
                        micromicronProcess(showMsg: user_robotCameraName)
                    }
                    //: break
                }
            }
            //: } else {
        } else {
            //: AF.request(serverUrl, method: .post, parameters: model.params, headers: headers, requestModifier: { $0.timeoutInterval = REQUEST_TIMEOUT_INTERVAL }).responseData { [self] responseData in
            AF.request(serverUrl, method: .post, parameters: model.params, headers: headers, requestModifier: { $0.timeoutInterval = mainBeginUrl }).responseData { [self] responseData in
                //: switch responseData.result {
                switch responseData.result {
                //: case .success:
                case .success:
                    //: func__requestSucess(model: model, response: responseData.response!, responseData: responseData.data!, completion: completion)
                    wharfageObjectCompletionFactor(model: model, response: responseData.response!, responseData: responseData.data!, completion: completion)
                //: break
                //: case .failure:
                case .failure:
                    //: completion(false, nil, TalkingErrorResponse.init(errorCode: RequestResultCode.NetError.rawValue, errorMsg: kNetErrorMsg))
                    completion(false, nil, MovementErrorResponse(errorCode: AuditorInsetTarget.NetError.rawValue, errorMsg: user_robotCameraName))
                    //: if model.showErrorStatusBar {
                    if model.showErrorStatusBar {
                        //: func__showStatusBarErrorMsg(showMsg: kNetErrorMsg)
                        micromicronProcess(showMsg: user_robotCameraName)
                    }

                    //: break
                }
            }
        }
    }

    //: func func__requestSucess(model: TalkingRequestModel, response: HTTPURLResponse, responseData: Data, completion: @escaping FinishBlock) {
    func wharfageObjectCompletionFactor(model: ReportedThen, response: HTTPURLResponse, responseData: Data, completion: @escaping FinishBlock) {
        //: let httpCookies = HTTPCookie.cookies(withResponseHeaderFields: response.allHeaderFields as! [String: String], for: response.url!)
        let httpCookies = HTTPCookie.cookies(withResponseHeaderFields: response.allHeaderFields as! [String: String], for: response.url!)
        //: if httpCookies.count > 0 {
        if httpCookies.count > 0 {
            //: TalkingRequestAddrTool.share.func__updateTalkingCookies(cookies: httpCookies)
            RocketRecognizeAddrTool.share.listenerBeauty(cookies: httpCookies)
        }

        //: var responseJson = String(data: responseData, encoding: .utf8)
        var responseJson = String(data: responseData, encoding: .utf8)

        //: if Encryption_Request, model.requestServer.contains("://app.") {
        if const_saltUrl, model.requestServer.contains((String(kDefinePromptTitle))) {
            //: responseJson = responseJson?.aes256Decrypt(key: TalkingRequestAddrTool.share.encryKeyStr)
            responseJson = responseJson?.pastCell(key: RocketRecognizeAddrTool.share.encryKeyStr)
        }

        //: responseJson = responseJson?.replacingOccurrences(of: "\"data\":null", with: "\"data\":{}")
        responseJson = responseJson?.replacingOccurrences(of: "\"" + (mainAppearFormat.replacingOccurrences(of: "spire", with: "a")) + "\"" + (String(app_remNurseFormat.suffix(5))), with: "" + "\"" + (mainAppearFormat.replacingOccurrences(of: "spire", with: "a")) + "\"" + ":{}")
        //: if !Encryption_Request {
        if !const_saltUrl {
            //: responseJson = responseJson?.replacingOccurrences(of: ":null", with: ":\"\"")
            responseJson = responseJson?.replacingOccurrences(of: (String(app_remNurseFormat.suffix(5))), with: ":" + "\"" + "\"")
        }
        //: if let responseModel = JSONDeserializer<TalkingBaseResponse>.deserializeFrom(json: responseJson) {
        if let responseModel = JSONDeserializer<AvailableBaseResponse>.deserializeFrom(json: responseJson) {
            //: if responseModel.errno == RequestResultCode.Normal.rawValue {
            if responseModel.errno == AuditorInsetTarget.Normal.rawValue {
                //: completion(true, responseModel.data, nil)
                completion(true, responseModel.data, nil)
                //: } else {
            } else {
                //: completion(false, responseModel.data, TalkingErrorResponse.init(errorCode: responseModel.errno, errorMsg: responseModel.msg ?? ""))
                completion(false, responseModel.data, MovementErrorResponse(errorCode: responseModel.errno, errorMsg: responseModel.msg ?? ""))
                //: if model.showErrorStatusBar {
                if model.showErrorStatusBar {
                    //: func__showStatusBarErrorMsg(showMsg: responseModel.msg ?? "Request failed, Try again later".localized)
                    micromicronProcess(showMsg: responseModel.msg ?? String(bytes: showProvidePath.map{plusEnablely(help: $0)}, encoding: .utf8)!.localized)
                }
                //: switch responseModel.errno {
                switch responseModel.errno {
                //: case RequestResultCode.NeedReLogin.rawValue:
                case AuditorInsetTarget.NeedReLogin.rawValue:
                    //: NotificationCenter.default.post(name: DID_LOGIN_OUT_SUCCESS_NOTIFICATION, object: nil, userInfo: nil)
                    NotificationCenter.default.post(name: main_contentMessage, object: nil, userInfo: nil)
                //: default:
                default:
                    //: break
                    break
                }
            }
            //: } else {
        } else {
            //: completion(false, nil, TalkingErrorResponse.init(errorCode: RequestResultCode.NetError.rawValue, errorMsg: "json error".localized))
            completion(false, nil, MovementErrorResponse(errorCode: AuditorInsetTarget.NetError.rawValue, errorMsg: (String(noti_centerMessage.suffix(5)) + String(showPrimaName)).localized))
        }
    }

    //: func buildServerUrl(model: TalkingRequestModel) -> String {
    func quantityermanIq(model: ReportedThen) -> String {
        //: var serverUrl: String = model.requestServer
        var serverUrl: String = model.requestServer

        //: var otherParams = "platform=iphone&version=\(AppNetVersion)&packageId=\(PackageID)&bundleId=\(AppBundle)&lang=\(TalkingRequestAddrTool.share.interfaceLang)"
        var otherParams = (String(data_handUrl) + dataHospitalStr.replacingOccurrences(of: "succeed", with: "=")) + "\(data_serviceFormat)" + (String(const_twoMessage.prefix(5)) + "ageId=") + "\(showViewValue)" + (String(k_banStr.prefix(8)) + String(dataOperationText.suffix(2))) + "\(const_onMsg)" + (String(app_contextData.suffix(5)) + kBlendFormat.capitalized) + "\(RocketRecognizeAddrTool.share.interfaceLang)"
        //: if Encryption_Request, serverUrl.contains("://app.") {
        if const_saltUrl, serverUrl.contains((String(kDefinePromptTitle))) {
            //: let allowCharSet: CharacterSet = CharacterSet.init(charactersIn: "?!@#$^&%*+,:;='\"`<>()[]{}/\\| ").inverted
            let allowCharSet = CharacterSet(charactersIn: "?!@#$^&%*+,:;='" + "\"" + "`<>()[]{}/\\| ").inverted
            //: if TalkingRequestAddrTool.share.slowPathArr.contains(model.requestPath) {
            if RocketRecognizeAddrTool.share.slowPathArr.contains(model.requestPath) {
                //: serverUrl.append("/route/slowindex")
                serverUrl.append((String(app_tooPath.prefix(9)) + notiModifyMsg.replacingOccurrences(of: "no", with: "in")))
                //: } else {
            } else {
                //: serverUrl.append("/route/index") // 如果使用IP需要加上app/
                serverUrl.append((String(mainReportName.suffix(4)) + String(user_heartMsg.prefix(8)))) // 如果使用IP需要加上app/
            }

            //: let pathStr = model.requestPath.addingPercentEncoding(withAllowedCharacters: allowCharSet)!
            let pathStr = model.requestPath.addingPercentEncoding(withAllowedCharacters: allowCharSet)!
            //: serverUrl.append("?path=\(pathStr)")
            serverUrl.append((String(kWeightText) + String(mainPerspectiveData.prefix(5))) + "\(pathStr)")
            //: serverUrl.append("&GJNonceStr=\(TalkingRequestAddrTool.share.randomStr)")
            serverUrl.append((String(constWeValidFormat.prefix(7)) + String(appIncentiveTitle.prefix(5))) + "\(RocketRecognizeAddrTool.share.randomStr)")

            //: if model.requestType == .GET {
            if model.requestType == .GET {
                //: for (key, value) in model.params {
                for (key, value) in model.params {
                    //: otherParams.append("&\(key)=\(value)")
                    otherParams.append("&\(key)=\(value)")
                }
            }
            //: if let encryStr = otherParams.aes256Encrypt(key: TalkingRequestAddrTool.share.encryKeyStr) {
            if let encryStr = otherParams.blendSet(key: RocketRecognizeAddrTool.share.encryKeyStr) {
                //: let paramsStr = encryStr.addingPercentEncoding(withAllowedCharacters: allowCharSet)!
                let paramsStr = encryStr.addingPercentEncoding(withAllowedCharacters: allowCharSet)!
                //: serverUrl.append("&params=\(paramsStr)")
                serverUrl.append((String(data_pleaseId)) + "\(paramsStr)")
            }
            //: printLog(message: serverUrl)
            printLog(message: serverUrl)
            //: } else {
        } else {
            //: if !model.requestPath.isEmptyString {
            if !model.requestPath.isEmptyString {
                //: serverUrl.append("/\(model.requestPath)")
                serverUrl.append("/\(model.requestPath)")
            }
            //: serverUrl.append("?\(otherParams)")
            serverUrl.append("?\(otherParams)")
        }
        //: return serverUrl
        return serverUrl
    }

    /// 获取请求头参数
    /// - Parameter model: 请求模型
    /// - Returns: 请求头参数
    //: func getRequestHeader(model: TalkingRequestModel) -> HTTPHeaders {
    func intervalerrelate(model: ReportedThen) -> HTTPHeaders {
        //: var headers = [HTTPHeader.userAgent(userAgent)]
        var headers = [HTTPHeader.userAgent(const_pingReasonPath)]
        //: if model.addHeaderToken.isEmpty == false {
        if model.addHeaderToken.isEmpty == false {
            //: let token = HTTPHeader(name: "token", value: model.addHeaderToken)
            let token = HTTPHeader(name: (dataInformationId.replacingOccurrences(of: "decrease", with: "ke")), value: model.addHeaderToken)
            //: headers.append(token)
            headers.append(token)
        }
        //: return HTTPHeaders(headers)
        return HTTPHeaders(headers)
    }

    // MARK: - 系统后台下载

    //: func urlSessionBackgroundDownLoad(model: TalkingRequestModel) {
    func albumModel(model: ReportedThen) {
        //: let serverUrl = self.buildServerUrl(model: model)
        let serverUrl = self.quantityermanIq(model: model)
        //: URLSession.shared.dataTask(with: URL.init(string: serverUrl)!) {(data, response, error) in
        URLSession.shared.dataTask(with: URL(string: serverUrl)!) { _, response, error in
            //: if error == nil {
            if error == nil {
                //: printLog(message: "请求成功\(String(describing: response))" )
                printLog(message: (String(appTingSafetyUrl)) + "\(String(describing: response))")
            }
        }

//        let configuration1 = URLSessionConfiguration.default    // default模式下系统会创建一个持久化的缓存并在用户的钥匙串中存储证书
//        let configuration2 = URLSessionConfiguration.ephemeral  // 所有内容的生命周期都与session相同，当session无效时，所有内容自动释放。

        // 后台下载
        //: let configuration3 = URLSessionConfiguration.background(withIdentifier: "AAAAAAAA")  // 后台甚至APP已经关闭的时候仍然在传输数据的会话
        let configuration3 = URLSessionConfiguration.background(withIdentifier: (kTempFormat.replacingOccurrences(of: "swallow", with: "A") + String(appProductValue))) // 后台甚至APP已经关闭的时候仍然在传输数据的会话
        //: configuration3.httpAdditionalHeaders = ["AA": "AA", "BB": "BB"] // 与请求一起发送的附加头文件的字典
        configuration3.httpAdditionalHeaders = ["AA": "AA", "BB": "BB"] // 与请求一起发送的附加头文件的字典
        //: configuration3.networkServiceType = .default
        configuration3.networkServiceType = .default // 网络服务的类型
        //: configuration3.allowsCellularAccess = true
        configuration3.allowsCellularAccess = true // 一个布尔值，用于确定是否应通过蜂窝网络进行连接
        //: configuration3.timeoutIntervalForRequest = 15
        configuration3.timeoutIntervalForRequest = 15
        //: configuration3.timeoutIntervalForResource = 15
        configuration3.timeoutIntervalForResource = 15
        //: let session = URLSession.init(configuration: configuration3, delegate: self, delegateQueue: OperationQueue.main)
        let session = URLSession(configuration: configuration3, delegate: self, delegateQueue: OperationQueue.main)
        //: session.downloadTask(with: URL.init(string: serverUrl)!).resume()
        session.downloadTask(with: URL(string: serverUrl)!).resume()
    }

    //: func urlSession(_ session: URLSession, downloadTask: URLSessionDownloadTask, didFinishDownloadingTo location: URL) {
    func urlSession(_: URLSession, downloadTask _: URLSessionDownloadTask, didFinishDownloadingTo location: URL) {
        //: printLog(message: "下载完成 - \(location)")
        printLog(message: (notiOndStrongName.capitalized + kListenerText.replacingOccurrences(of: "balloon", with: " ")) + "\(location)")
        //: let locationPath = location.path
        let locationPath = location.path
        //: let documnets = NSHomeDirectory() + "/Documents/" + NSDate.getCurrentTimeStamp() + ".mp4"
        let documnets = NSHomeDirectory() + (String(const_stretchMessage) + String(dataPersonalMessage.suffix(7))) + NSDate.voiceBy() + (String(data_lockStr))
        //: printLog(message: "移动地址:\(documnets)")
        printLog(message: (String(userEarnKey)) + "\(documnets)")
        //: let fileManager = FileManager.default
        let fileManager = FileManager.default
        //: do {
        do {
            //: try fileManager.moveItem(atPath: locationPath, toPath: documnets)
            try fileManager.moveItem(atPath: locationPath, toPath: documnets)
            //: } catch {
        } catch {
            // catch 中默认提供error信息, 当序列化不成功是, 返回error
            //: printLog(message: error)
            printLog(message: error)
        }
    }

    // 监听下载进度
    //: func urlSession(_ session: URLSession, downloadTask: URLSessionDownloadTask, didWriteData bytesWritten: Int64, totalBytesWritten: Int64, totalBytesExpectedToWrite: Int64) {
    func urlSession(_: URLSession, downloadTask _: URLSessionDownloadTask, didWriteData _: Int64, totalBytesWritten _: Int64, totalBytesExpectedToWrite _: Int64) {}

    //: func urlSessionDidFinishEvents(forBackgroundURLSession session: URLSession) {
    func urlSessionDidFinishEvents(forBackgroundURLSession _: URLSession) {
        //: printLog(message: "后台任务下载回来")
        printLog(message: (showDatePath))
        //: DispatchQueue.main.async {
        DispatchQueue.main.async {
            //: guard let backgroundHandle = AppDelegateHelper.shared.backgroundSessionCompletionHandler else { return }
            guard let backgroundHandle = RetainThen.shared.backgroundSessionCompletionHandler else { return }
            //: backgroundHandle()
            backgroundHandle()
        }
    }

    // MARK: - 动画下载

    //: public func startGiftZipRequest(giftUrl: String, path: String, completion: @escaping FinishBlock) {
    public func outputCompletion(giftUrl: String, path: String, completion: @escaping FinishBlock) {
        //: let destination: DownloadRequest.Destination = { _, _ in
        let destination: DownloadRequest.Destination = { _, _ in
            //: let pathUrl = URL(fileURLWithPath: path)
            let pathUrl = URL(fileURLWithPath: path)
            //: return (pathUrl, [.removePreviousFile, .createIntermediateDirectories])
            return (pathUrl, [.removePreviousFile, .createIntermediateDirectories])
        }
        //: let giftUrlStr = giftUrl.replacingOccurrences(of: UrlDomin.urlAes256Decrypt(), with: ReplaceUrlDomain)
        let giftUrlStr = giftUrl.replacingOccurrences(of: main_lessPath.holderDown(), with: app_frameName)
        //: AF.download(giftUrlStr, to: destination).downloadProgress { speed in
        AF.download(giftUrlStr, to: destination).downloadProgress { _ in
        }
        //: .responseData { response in
        .responseData { response in
            //: switch response.result {
            switch response.result {
            //: case .success(let json):
            case let .success(json):
                //: printLog(message: "下载成功 :\(json)")
                printLog(message: (kDataPromptUrl.capitalized) + "\(json)")
                //: completion(true, nil, TalkingErrorResponse.init(errorCode: RequestResultCode.Normal.rawValue, errorMsg: "Net Error".localized))
                completion(true, nil, MovementErrorResponse(errorCode: AuditorInsetTarget.Normal.rawValue, errorMsg: (String(main_secureTillAccelerateName.prefix(4)) + String(main_directionBalanceId.prefix(5))).localized))
            //: break
            //: case .failure(let errMsg):
            case let .failure(errMsg):
                //: completion(false, nil, TalkingErrorResponse.init(errorCode: RequestResultCode.NetError.rawValue, errorMsg: "Net Error".localized))
                completion(false, nil, MovementErrorResponse(errorCode: AuditorInsetTarget.NetError.rawValue, errorMsg: (String(main_secureTillAccelerateName.prefix(4)) + String(main_directionBalanceId.prefix(5))).localized))

                //: printLog(message: "下载失败 :\(errMsg)")
                printLog(message: (showOnPushData) + "\(errMsg)")
            }
        }
    }

    //: public func startVoiceRequest(voiceUrl: String, path: String, completion: @escaping FinishBlock) {
    public func limit(voiceUrl: String, path: String, completion: @escaping FinishBlock) {
        //: let destination: DownloadRequest.Destination = { _, _ in
        let destination: DownloadRequest.Destination = { _, _ in
            //: let pathUrl = URL(fileURLWithPath: path)
            let pathUrl = URL(fileURLWithPath: path)
            //: return (pathUrl, [.removePreviousFile, .createIntermediateDirectories])
            return (pathUrl, [.removePreviousFile, .createIntermediateDirectories])
        }

        //: let voiceUrlStr = voiceUrl.replacingOccurrences(of: UrlDomin.urlAes256Decrypt(), with: ReplaceUrlDomain)
        let voiceUrlStr = voiceUrl.replacingOccurrences(of: main_lessPath.holderDown(), with: app_frameName)
        //: AF.download(voiceUrlStr, to: destination).downloadProgress { speed in
        AF.download(voiceUrlStr, to: destination).downloadProgress { _ in
        }
        //: .responseData { response in
        .responseData { response in
            //: switch response.result {
            switch response.result {
            //: case .success(let json):
            case let .success(json):
                //: printLog(message: "下载成功 :\(json)")
                printLog(message: (kDataPromptUrl.capitalized) + "\(json)")
                //: completion(true, nil, TalkingErrorResponse.init(errorCode: RequestResultCode.Normal.rawValue, errorMsg: "Net Error".localized))
                completion(true, nil, MovementErrorResponse(errorCode: AuditorInsetTarget.Normal.rawValue, errorMsg: (String(main_secureTillAccelerateName.prefix(4)) + String(main_directionBalanceId.prefix(5))).localized))
            //: break
            //: case .failure(let errMsg):
            case let .failure(errMsg):
                //: completion(false, nil, TalkingErrorResponse.init(errorCode: RequestResultCode.NetError.rawValue, errorMsg: "Net Error".localized))
                completion(false, nil, MovementErrorResponse(errorCode: AuditorInsetTarget.NetError.rawValue, errorMsg: (String(main_secureTillAccelerateName.prefix(4)) + String(main_directionBalanceId.prefix(5))).localized))

                //: printLog(message: "下载失败 :\(errMsg)")
                printLog(message: (showOnPushData) + "\(errMsg)")
            }
        }
    }
}
