
//: Declare String Begin

/*: "CacheIntimatePhotoUnlockMsgIdArrayKey" :*/
fileprivate let main_strikeFormat:[UInt8] = [0x77,0x55,0x57,0x5c,0x51,0x7d,0x5a,0x40,0x5d,0x59,0x55,0x40,0x51,0x64,0x5c,0x5b,0x40,0x5b,0x61,0x5a,0x58,0x5b,0x57,0x5f,0x79,0x47,0x53,0x7d,0x50,0x75,0x46,0x46,0x55,0x4d,0x7f,0x51,0x4d]

/*: "CacheQuestionReplayMsgIdIdArrayKey" :*/
fileprivate let showOccurMsg:[UInt8] = [0xb5,0x97,0x95,0x9e,0x93,0xa7,0x83,0x93,0x85,0x82,0x9f,0x99,0x98,0xa4,0x93,0x86,0x9a,0x97,0x8f,0xbb,0x85,0x91,0xbf,0x92,0xbf,0x92,0xb7,0x84,0x84,0x97,0x8f,0xbd,0x93,0x8f]

private func brokenAlways(coal num: UInt8) -> UInt8 {
    return num ^ 246
}

/*: "CachePrivateChatVideoTipsIsShow_ :*/
fileprivate let userSituationControlPath:[UInt8] = [0x2c,0xe,0xc,0x7,0xa,0x3f,0x1d,0x6,0x19,0xe,0x1b,0xa,0x2c,0x7,0xe,0x1b,0x39,0x6,0xb,0xa,0x0,0x3b,0x6,0x1f,0x1c,0x26,0x1c,0x3c,0x7,0x0,0x18,0x30]

private func receptionAccount(kit num: UInt8) -> UInt8 {
    return num ^ 111
}

/*: "Quick greeting can only be sent to him once" :*/
fileprivate let const_searchBouncePath:[UInt8] = [0xa5,0x81,0x9d,0x97,0x9f,0xd4,0x93,0x86,0x91,0x91,0x80,0x9d,0x9a,0x93,0xd4,0x97,0x95,0x9a,0xd4,0x9b,0x9a,0x98,0x8d,0xd4,0x96,0x91,0xd4,0x87,0x91,0x9a,0x80,0xd4,0x80,0x9b,0xd4,0x9c,0x9d,0x99,0xd4,0x9b,0x9a,0x97,0x91]

private func triggerMid(match num: UInt8) -> UInt8 {
    return num ^ 244
}

/*: "txt" :*/
fileprivate let app_staffBellyStr:[Character] = ["t","x","t"]

/*: "audio" :*/
fileprivate let noti_shoreSureValue:String = "acontributionio"

/*: "Please add greeting text" :*/
fileprivate let dataObserverReachPath:String = "Pleareading shade eh field convert"
fileprivate let show_whichStyleValue:String = "personal maled greeti"
fileprivate let kResourceValue:String = "ng textabove signal"

/*: "You have not set up a photo greeting, please set it up before sending!" :*/
fileprivate let show_oldId:[UInt8] = [0xc7,0xdd,0xe3,0x8e,0xd6,0xcf,0xe4,0xd3,0x8e,0xdc,0xdd,0xe2,0x8e,0xe1,0xd3,0xe2,0x8e,0xe3,0xde,0x8e,0xcf,0x8e,0xde,0xd6,0xdd,0xe2,0xdd,0x8e,0xd5,0xe0,0xd3,0xd3,0xe2,0xd7,0xdc,0xd5,0x9a,0x8e,0xde,0xda,0xd3,0xcf,0xe1,0xd3,0x8e,0xe1,0xd3,0xe2,0x8e,0xd7,0xe2,0x8e,0xe3,0xde,0x8e,0xd0,0xd3,0xd4,0xdd,0xe0,0xd3,0x8e,0xe1,0xd3,0xdc,0xd2,0xd7,0xdc,0xd5,0x8f]

fileprivate func districtManager(expression num: UInt8) -> UInt8 {
    let value = Int(num) + 146
    if value > 255 {
        return UInt8(value - 256)
    } else {
        return UInt8(value)
    }
}

/*: "Cancel" :*/
fileprivate let data_solutionMsg:[Character] = ["C","a","n","c","e","l"]

/*: "Go to set" :*/
fileprivate let mainBathroomContextStr:String = "Go to smedium user universal ping"
fileprivate let main_regularTitle:String = "eserver"

/*: "#startTime#" :*/
fileprivate let k_sparkName:String = "segment person#start"
fileprivate let noti_versionTitle:String = "fashion status loopTime#"

/*: "#endTime#" :*/
fileprivate let data_primaryUrl:String = "#endTvisual define"

/*: "replayId" :*/
fileprivate let app_walkUrl:String = "rcraml"

/*: "text" :*/
fileprivate let constNeverPath:[Character] = ["t","e","x","t"]

/*: "img" :*/
fileprivate let k_channelDestroyGoingKey:String = "imunder"

/*: "video" :*/
fileprivate let mainComplexMessage:[Character] = ["v","i","d","e","o"]

/*: "gift" :*/
fileprivate let main_originallyMsg:String = "gifgin"

/*: "PrivateChat 点击引用消息 :*/
fileprivate let userActivityMessage:String = "Privaaddress that secret fit"
fileprivate let mainRefuseTitle:[Character] = ["t","e","C","h"]
fileprivate let user_securityFormat:[Character] = ["a","t"," ","点","击","引","用","消","\u{606f}"]

/*: "Sent " :*/
fileprivate let kProcedurePath:[Character] = ["S","e","n","t"," "]

//: Declare String End

// __DEBUG__
// __CLOSE_PRINT__
//
//  SpanChatManager.swift
//  AbroadTalking
//
//  Created by young on 2023/1/5.
//

//: import UIKit
import UIKit

/// 一键打招呼Block
//: typealias QuickGreetingBlock = (_ errorCode: Int?, _ errorMsg: String?, _ textDict: [String: Any]?, _ audioDict: [String: Any]?) -> Void
typealias QuickGreetingBlock = (_ errorCode: Int?, _ errorMsg: String?, _ textDict: [String: Any]?, _ audioDict: [String: Any]?) -> Void

/// 图片打招呼Block
//: typealias BeautyPhotosBlock = (_ isPushSet: Bool, _ PhotoDict: [String: Any]?) -> Void
typealias BeautyPhotosBlock = (_ isPushSet: Bool, _ PhotoDict: [String: Any]?) -> Void

//: public class TalkingPrivateChatManager: NSObject {
public class SpanChatManager: NSObject {
    /// 记录私信对端已读回执时间 [key：userID，value：对端最后已读回执时间]
    //: var readReceiptDict = [String: Int]()
    var readReceiptDict = [String: Int]()

    //: @objc static let share = TalkingPrivateChatManager()
    @objc static let share = SpanChatManager()
    //: private override init() {}
    override private init() {}
    // 私密照片已解锁礼物消息数组的key
    //: @objc let UnlockMsgIdArrayKey = "CacheIntimatePhotoUnlockMsgIdArrayKey"
    @objc let UnlockMsgIdArrayKey = String(bytes: main_strikeFormat.map{$0^52}, encoding: .utf8)!
    // 问题回答消息已回复消息数组的key
    //: let QuestionReplayMsgIdIdArrayKey = "CacheQuestionReplayMsgIdIdArrayKey"
    let QuestionReplayMsgIdIdArrayKey = String(bytes: showOccurMsg.map{brokenAlways(coal: $0)}, encoding: .utf8)!
}

// MARK: - 插入音视频通话提示消息

//: extension TalkingPrivateChatManager {
extension SpanChatManager {
    /// 判断是否需要插入音视频通话提示消息
    /// - Parameters:
    ///   - toUid: 对方Id
    ///   - msgView: TUI视图
    ///   - completionHandler: 回调
    //: static func chat_privateChatVideo_insertTipMsg(toUid: String, msgView: TUIMessageController, needInsertHandler: (() -> Void)?) {
    static func descriptionHandler(toUid: String, msgView: ActivatedAlignmentLoader, needInsertHandler: (() -> Void)?) {
        // 非审核模式
        //: guard FacultyReactiveCompatible.share.appStatus == AppSkinStatus.normal.rawValue else { return }
        guard FacultyReactiveCompatible.share.appStatus == PenConstantTarget.normal.rawValue else { return }
        // 男性
        //: guard FacultyReactiveCompatible.share.loginUserMode.sex == Gender.male.rawValue else { return }
        guard FacultyReactiveCompatible.share.loginUserMode.sex == ATextLiteral.male.rawValue else { return }

        // 是否展示过提示文案
        //: let TalkingPrivateChatVideoTipsIsShow = "CachePrivateChatVideoTipsIsShow_\(String(FacultyReactiveCompatible.share.loginUserMode.userID))_\(toUid)"
        let TalkingPrivateChatVideoTipsIsShow = String(bytes: userSituationControlPath.map{receptionAccount(kit: $0)}, encoding: .utf8)! + "\(String(FacultyReactiveCompatible.share.loginUserMode.userID))_\(toUid)"
        //: let isShow = Defaults.bool(forKey: TalkingPrivateChatVideoTipsIsShow)
        let isShow = mainNameNearbyTitle.bool(forKey: TalkingPrivateChatVideoTipsIsShow)
        //: guard isShow == false else { return }
        guard isShow == false else { return }

        // 消息达到5条
        //: guard msgView.uiMsgs.count >= 5 else { return }
        guard msgView.uiMsgs.count >= 5 else { return }

        //: var hasVoiceVideo = false
        var hasVoiceVideo = false // 是否有音视频通话消息
        //: var outgoingMsgCount = 0
        var outgoingMsgCount = 0 // 消息发送方已发送消息数量
        //: for indexModel in msgView.uiMsgs {
        for indexModel in msgView.uiMsgs {
            //: let indexModel = indexModel as? AgoneCellData
            let indexModel = indexModel as? AgoneCellData
            //: if indexModel != nil {
            if indexModel != nil {
                //: if indexModel!.isKind(of: AgoArrayCellData.self) {
                if indexModel!.isKind(of: AgoArrayCellData.self) { // 是否包含音视频文本
                    //: let textMsgModel = indexModel as! AgoArrayCellData
                    let textMsgModel = indexModel as! AgoArrayCellData
                    //: if textMsgModel.isVoiceVideo == true {
                    if textMsgModel.isVoiceVideo == true {
                        //: hasVoiceVideo = true
                        hasVoiceVideo = true
                        //: break
                        break
                    }
                }

                // 语音消息 || 文本消息 || 图片消息 || 视频消息
                //: if indexModel!.isKind(of: TagCellData.self) ||
                if indexModel!.isKind(of: TagCellData.self) ||
                    //: indexModel!.isKind(of: AgoArrayCellData.self) ||
                    indexModel!.isKind(of: AgoArrayCellData.self) ||
                    //: indexModel!.isKind(of: ApartCellData.self) ||
                    indexModel!.isKind(of: ApartCellData.self) ||
                    //: indexModel!.isKind(of: SaverCellData.self) {
                    indexModel!.isKind(of: SaverCellData.self)
                {
                    //: if indexModel?.direction == .MsgDirectionOutgoing {
                    if indexModel?.direction == .MsgDirectionOutgoing { // 消息发送方
                        //: outgoingMsgCount += 1
                        outgoingMsgCount += 1
                    }
                }
            }
        }

        //: guard hasVoiceVideo == false else {
        guard hasVoiceVideo == false else {
            //: Defaults.set(true, forKey: TalkingPrivateChatVideoTipsIsShow)
            mainNameNearbyTitle.set(true, forKey: TalkingPrivateChatVideoTipsIsShow)
            //: return
            return
        }
        //: guard outgoingMsgCount >= 5 else { return }
        guard outgoingMsgCount >= 5 else { return }

        // 回调外部插入音视频提示消息
        //: if needInsertHandler != nil {
        if needInsertHandler != nil {
            //: needInsertHandler!()
            needInsertHandler!()
            //: Defaults.set(true, forKey: TalkingPrivateChatVideoTipsIsShow)
            mainNameNearbyTitle.set(true, forKey: TalkingPrivateChatVideoTipsIsShow)
        }
    }
}

// MARK: - 一键打招呼

//: extension TalkingPrivateChatManager {
extension SpanChatManager {
    /// 判断是否可以一键打招呼，回调结果
    /// - Parameters:
    ///   - toUserId: 接收方UserId
    ///   - handler: errorCode：1 = 已发送过；2=未添加打招呼数据;
    //: static func chat_sendQuickGreeting(toUserId: String, completionHandler: @escaping QuickGreetingBlock) {
    static func counterHandler(toUserId: String, completionHandler: @escaping QuickGreetingBlock) {
        //: let key = "\(FacultyReactiveCompatible.share.loginUserMode.userID)_\(TalkingSendAutoGreetIsToKey)_\(toUserId)"
        let key = "\(FacultyReactiveCompatible.share.loginUserMode.userID)_\(notiPageStr)_\(toUserId)"
        //: let ishave = UserDefaults.standard.bool(forKey: key)
        let ishave = UserDefaults.standard.bool(forKey: key)
        //: guard !ishave else {
        guard !ishave else {
            //: completionHandler(1, "Quick greeting can only be sent to him once".localized, nil, nil)
            completionHandler(1, String(bytes: const_searchBouncePath.map{triggerMid(match: $0)}, encoding: .utf8)!.localized, nil, nil)
            //: return
            return
        }

        //: ProgressHUD.show()
        StrangerProgressHUD.greenDirect()
        //: TalkingChatRequestTool.req_getAutoGreetContent { succeed, result, errorModel in
        PouchReactiveCompatible.mugshot { succeed, result, errorModel in
            //: ProgressHUD.dismiss()
            StrangerProgressHUD.barDismiss()
            //: guard succeed == true else {
            guard succeed == true else {
                //: completionHandler(2, errorModel?.errorMsg ?? "", nil, nil)
                completionHandler(2, errorModel?.errorMsg ?? "", nil, nil)
                //: return
                return
            }
            //: guard let dict = result as? [String: Any] else {
            guard let dict = result as? [String: Any] else {
                //: completionHandler(2, "", nil, nil)
                completionHandler(2, "", nil, nil)
                //: return
                return
            }
            //: let textDict = dict["txt"] as? [String: Any]
            let textDict = dict[(String(app_staffBellyStr))] as? [String: Any]
            //: let audioDict = dict["audio"] as? [String: Any]
            let audioDict = dict[(noti_shoreSureValue.replacingOccurrences(of: "contribution", with: "ud"))] as? [String: Any]
            //: if textDict?.count == 0 && audioDict?.count == 0 {
            if textDict?.count == 0, audioDict?.count == 0 {
                //: completionHandler(2, "Please add greeting text".localized, nil, nil)
                completionHandler(2, (String(dataObserverReachPath.prefix(4)) + "se ad" + String(show_whichStyleValue.suffix(8)) + String(kResourceValue.prefix(7))).localized, nil, nil)
                //: return
                return
            }

            // 一键打招呼本地存储
            //: chat_changeSaveGreetData(toUserId: toUserId, isHave: true)
            ping(toUserId: toUserId, isHave: true)
            //: completionHandler(nil, nil, textDict, audioDict)
            completionHandler(nil, nil, textDict, audioDict)
        }
    }

    /// 一键打招呼状态本地存储
    /// - Parameters:
    ///   - toUserId: 接收方UserId
    ///   - isHave: 是否已发送
    //: static func chat_changeSaveGreetData(toUserId: String, isHave: Bool) {
    static func ping(toUserId: String, isHave: Bool) {
        // 一键打招呼本地cunc
        //: let key = "\(FacultyReactiveCompatible.share.loginUserMode.userID)_\(TalkingSendAutoGreetIsToKey)_\(toUserId)"
        let key = "\(FacultyReactiveCompatible.share.loginUserMode.userID)_\(notiPageStr)_\(toUserId)"
        //: UserDefaults.standard.set(isHave, forKey: key)
        UserDefaults.standard.set(isHave, forKey: key)
    }

    /// 图片打招呼
    /// - Parameters:
    ///   - toUserId: 接收方UserId
    ///   - handler: errorCode：1 = 已发送过；2=未添加打招呼数据；
    //: static func chat_sendBeautyPhotos(toUserId: String, completionHandler: @escaping BeautyPhotosBlock) {
    static func outFail(toUserId: String, completionHandler: @escaping BeautyPhotosBlock) {
        //: ProgressHUD.show()
        StrangerProgressHUD.greenDirect()
        //: TalkingChatRequestTool.req_getIntimatePhoto(toUid: toUserId) { succeed, result, errorModel in
        PouchReactiveCompatible.squelchDownFromCompletionJust(toUid: toUserId) { succeed, result, errorModel in
            //: ProgressHUD.dismiss()
            StrangerProgressHUD.barDismiss()
            /// 未添加打招呼图片
            //: if errorModel?.errorCode == 111 {
            if errorModel?.errorCode == 111 {
                //: let config = ShowAlertConfig()
                let config = InstAlertConfig()
                //: config.alignment = .center
                config.alignment = .center
                //: if TalkingRequestAddrTool.share.interfaceLang == LangType.pt.rawValue {
                if RocketRecognizeAddrTool.share.interfaceLang == JumpScalarLiteral.pt.rawValue {
                    //: config.width = 330
                    config.width = 330
                }
                //: TalkingAlertShow.customAlert(message: "You have not set up a photo greeting, please set it up before sending!".localized, leftBtnTitle: "Cancel".localized, rightBtnTitle: "Go to set".localized, leftBlock: {
                PersonAlertShow.customOfConfig(message: String(bytes: show_oldId.map{districtManager(expression: $0)}, encoding: .utf8)!.localized, leftBtnTitle: (String(data_solutionMsg)).localized, rightBtnTitle: (String(mainBathroomContextStr.prefix(7)) + main_regularTitle.replacingOccurrences(of: "server", with: "t")).localized, leftBlock: {
                    //: TalkingAlertShow.hideAlert()
                    PersonAlertShow.quantityerrupt()

                    //: }, rightBlock: {
                }, rightBlock: {
                    //: TalkingAlertShow.hideAlert()
                    PersonAlertShow.quantityerrupt()
                    //: completionHandler(true, nil)
                    completionHandler(true, nil)
                    //: }, config: config)
                }, config: config)
                //: return
                return
            }
            //: if succeed {
            if succeed {
                //: guard let dict = result as? [String: Any] else {
                guard let dict = result as? [String: Any] else {
                    //: return
                    return
                }
                //: completionHandler(false, dict)
                completionHandler(false, dict)
            }
        }
    }
}

// MARK: - 插入与官方客服聊天提示消息

//: extension TalkingPrivateChatManager {
extension SpanChatManager {
    ///  判断是否需要插入与官方客服聊天提示消息
    /// - Parameter needInsertHandler: 回调
    //: static func chat_customerServiceCenter_insertTipMsg(needInsertHandler: ((_ tipsStr: String?) -> Void)?) {
    static func seatStrokeMessage(needInsertHandler: ((_ tipsStr: String?) -> Void)?) {
        //: guard FacultyReactiveCompatible.share.appConfigMode.CSConfig.systemTips.count > 0 else {
        guard FacultyReactiveCompatible.share.appConfigMode.CSConfig.systemTips.count > 0 else { // 接口没下发，无需提示
            //: if needInsertHandler != nil {
            if needInsertHandler != nil {
                //: needInsertHandler!(nil)
                needInsertHandler!(nil)
            }
            //: return
            return
        }

        // 根据设备所在时区转换时间
        //: let startTime = NSDate.getCurrentZoneDateString(shDateStr: FacultyReactiveCompatible.share.appConfigMode.CSConfig.startTime)
        let startTime = NSDate.invest(shDateStr: FacultyReactiveCompatible.share.appConfigMode.CSConfig.startTime)
        //: let endTime = NSDate.getCurrentZoneDateString(shDateStr: FacultyReactiveCompatible.share.appConfigMode.CSConfig.endTime)
        let endTime = NSDate.invest(shDateStr: FacultyReactiveCompatible.share.appConfigMode.CSConfig.endTime)
        //: let isIncluded = NSDate.compareCurrentTimeIsIncluded(startTime: startTime, endTime: endTime)
        let isIncluded = NSDate.redInk(startTime: startTime, endTime: endTime)

        //: guard isIncluded == false else {
        guard isIncluded == false else {
            //: if needInsertHandler != nil {
            if needInsertHandler != nil {
                //: needInsertHandler!(nil)
                needInsertHandler!(nil)
            }
            //: return
            return
        }

        // 非工作时间段内，添加提示
        //: var tips = FacultyReactiveCompatible.share.appConfigMode.CSConfig.systemTips
        var tips = FacultyReactiveCompatible.share.appConfigMode.CSConfig.systemTips
        //: tips = tips.replacingOccurrences(of: "#startTime#", with: "\(startTime)")
        tips = tips.replacingOccurrences(of: (String(k_sparkName.suffix(6)) + String(noti_versionTitle.suffix(5))), with: "\(startTime)")
        //: tips = tips.replacingOccurrences(of: "#endTime#", with: "\(endTime)")
        tips = tips.replacingOccurrences(of: (String(data_primaryUrl.prefix(5)) + "ime#"), with: "\(endTime)")
        //: if needInsertHandler != nil {
        if needInsertHandler != nil {
            //: needInsertHandler!(tips)
            needInsertHandler!(tips)
        }
    }
}

// MARK: - 刷新私密消息（照片、视频）展示状态

//: extension TalkingPrivateChatManager {
extension SpanChatManager {
    /// 接收到解锁私密消息（照片、视频）的礼物，刷新本地消息状态
    /// - Parameters:
    ///   - cellData: 数据
    ///   - msgView: TUI视图
    //: static func chat_onNewMessage_refreshLocalIntimateMsgStatus(cellData: AgoneCellData, msgView: TUIMessageController) {
    static func publish(cellData: AgoneCellData, msgView: ActivatedAlignmentLoader) {
        //: guard msgView.uiMsgs.count > 0 else {
        guard msgView.uiMsgs.count > 0 else {
            // --------- 【外部页面】收到了解锁礼物消息 ---------
            // 首次进入到当前页面，刷新消息状态
            //: var array = Defaults.object(forKey: TalkingPrivateChatManager.share.UnlockMsgIdArrayKey) as? [String]
            var array = mainNameNearbyTitle.object(forKey: SpanChatManager.share.UnlockMsgIdArrayKey) as? [String]

            // 本地有私密消息解锁礼物缓存，且是图片消息 或 视频消息
            //: guard array?.count ?? 0 > 0 &&
            guard array?.count ?? 0 > 0,
                  //: (cellData.isKind(of: ApartCellData.self) ||
                  cellData.isKind(of: ApartCellData.self) ||
                  //: cellData.isKind(of: SaverCellData.self)) else { return }
                  cellData.isKind(of: SaverCellData.self) else { return }

            // 找到本人发送的私密消息，更新状态为已解锁
            //: if array!.contains(cellData.msgModel.msgInfo.msgId) {
            if array!.contains(cellData.msgModel.msgInfo.msgId) {
                //: inner_changeIntimateMsg(cellData, msgView)
                impending(cellData, msgView)

                // 更新本地缓存
                //: array!.removeAll(where: { $0 == cellData.msgModel.msgInfo.msgId })
                array!.removeAll(where: { $0 == cellData.msgModel.msgInfo.msgId })
                //: Defaults.set(array, forKey: TalkingPrivateChatManager.share.UnlockMsgIdArrayKey)
                mainNameNearbyTitle.set(array, forKey: SpanChatManager.share.UnlockMsgIdArrayKey)
            }

            //: return
            return
        }

        //  --------- 在【当前页面】收到解锁礼物消息 ---------
        // 只有收到礼物解锁消息才刷新
        //: guard cellData.isKind(of: RangeCellData.self) && cellData.msgModel.gift.unlockMsgId.count > 0 else { return }
        guard cellData.isKind(of: RangeCellData.self), cellData.msgModel.gift.unlockMsgId.count > 0 else { return }

        // 找到本人发送的私密消息，更新状态为已解锁
        //: let unlockMsgId = cellData.msgModel.gift.unlockMsgId
        let unlockMsgId = cellData.msgModel.gift.unlockMsgId
        //: for msgCellData in msgView.uiMsgs {
        for msgCellData in msgView.uiMsgs {
            //: let data = msgCellData as! AgoneCellData
            let data = msgCellData as! AgoneCellData
            //: if data.msgModel.msgInfo.msgId == unlockMsgId {
            if data.msgModel.msgInfo.msgId == unlockMsgId {
                //: inner_changeIntimateMsg(data, msgView)
                impending(data, msgView)
                //: break
                break
            }
        }
    }

    /// 刷新本地消息状态
    /// - Parameters:
    ///   - cellData: 数据
    ///   - msgView: TUI视图
    //: static private func inner_changeIntimateMsg(_ cellData: AgoneCellData, _ msgView: TUIMessageController) {
    private static func impending(_ cellData: AgoneCellData, _ msgView: ActivatedAlignmentLoader) {
        //: if cellData.isKind(of: ApartCellData.self) {
        if cellData.isKind(of: ApartCellData.self) { // 私密照片
            //: let model = cellData.msgModel.msgInfo.intimatePhoto
            let model = cellData.msgModel.msgInfo.intimatePhoto
            //: if model.lockStatus != 0 {
            if model.lockStatus != 0 {
                //: model.lockStatus = 0
                model.lockStatus = 0
                //: msgView.changeIntimatePhotoMsg(cellData, photoModel: model)
                msgView.meanSun(cellData, cut: model)
            }

            //: } else if cellData.isKind(of: SaverCellData.self) {
        } else if cellData.isKind(of: SaverCellData.self) { // 私密视频
            //: let model = cellData.msgModel.msgInfo.video
            let model = cellData.msgModel.msgInfo.video
            //: if model.lockStatus != 0 {
            if model.lockStatus != 0 {
                //: model.lockStatus = 0
                model.lockStatus = 0
                //: msgView.changeIntimateVideoMsg(cellData, videoModel: model)
                msgView.takePosition(cellData, tableModel: model)
            }
        }
    }
}

// MARK: - 问题消息快捷回复

//: extension TalkingPrivateChatManager {
extension SpanChatManager {
    /// 接收到问题回答消息，判断是否需要展示消息快捷回复弹窗
    /// - Parameters:
    ///   - isRecharge: 是否充值
    ///   - msgView: TUI视图
    ///   - handler: 回调
    //: static func chat_onNewMessage_checkQuestionQuickReplayMsg(isRecharge: Bool,
    static func blue(isRecharge: Bool,
                     //: msgView: TUIMessageController,
                     msgView: ActivatedAlignmentLoader,
                     //: handler: ((AbTalkingChatMsgInfoModel?) -> Void)?) {
                     handler: ((DisplaceModel?) -> Void)?)
    {
        //: guard FacultyReactiveCompatible.share.loginUserMode.sex == Gender.male.rawValue else { return }
        guard FacultyReactiveCompatible.share.loginUserMode.sex == ATextLiteral.male.rawValue else { return }
        //: guard isRecharge == false else { return }
        guard isRecharge == false else { return }
        // 获取已处理消息ID数组
        //: let cacheMsgIds = Defaults.object(forKey: TalkingPrivateChatManager.share.QuestionReplayMsgIdIdArrayKey) as? [String] ?? []
        let cacheMsgIds = mainNameNearbyTitle.object(forKey: SpanChatManager.share.QuestionReplayMsgIdIdArrayKey) as? [String] ?? []
        //: for case let msgModel as AgoneCellData in msgView.uiMsgs.reversed() {
        for case let msgModel as AgoneCellData in msgView.uiMsgs.reversed() {
            //: let msgInfo = msgModel.msgModel.msgInfo
            let msgInfo = msgModel.msgModel.msgInfo
            //: if msgInfo.isEnd && msgInfo.replay.isEmpty {
            if msgInfo.isEnd, msgInfo.replay.isEmpty { // 问答已结束
                //: handler?(nil)
                handler?(nil)
                //: return
                return
            }
            //: if msgInfo.isEnd && !msgInfo.replay.isEmpty && !cacheMsgIds.contains(msgInfo.msgId) {
            if msgInfo.isEnd, !msgInfo.replay.isEmpty, !cacheMsgIds.contains(msgInfo.msgId) { // 找到符合条件问答消息
                //: handler?(msgInfo)
                handler?(msgInfo)
                //: return
                return
            }
        }
        //: handler?(nil)
        handler?(nil)
    }
}

// MARK: - 未回复Reply to get points~逻辑

//: extension TalkingPrivateChatManager {
extension SpanChatManager {
    /// 是否是回复获取积分消息
    /// - Parameters:
    ///   - direction: 消息方向
    ///   - msgType: 消息类型
    ///   - msgTime: 消息时间
    /// - Returns: 结果
    //: static func isReplyToGetPointsMsg(_ direction: TMsgDirection, _ msgType: Int, msgTime: Date?) -> Bool {
    static func attributeTime(_ direction: BeAdditiveArithmetic, _ msgType: Int, msgTime: Date?) -> Bool {
        // 非审核模式, messageType == 5, 时间<24小时
        //: guard FacultyReactiveCompatible.share.appStatus == AppSkinStatus.normal.rawValue else { return false }
        guard FacultyReactiveCompatible.share.appStatus == PenConstantTarget.normal.rawValue else { return false }
        //: guard direction == .MsgDirectionIncoming else { return false }
        guard direction == .MsgDirectionIncoming else { return false }
        //: guard msgType == 5 else { return false }
        guard msgType == 5 else { return false }
        //: guard let msgTime = msgTime else { return false }
        guard let msgTime = msgTime else { return false }
        //: guard Date().timeIntervalSince(msgTime) < 24*60*60 else { return false }
        guard Date().timeIntervalSince(msgTime) < 24 * 60 * 60 else { return false }
        //: return true
        return true
    }
}

// MARK: - VIP展示消息已读逻辑

//: extension TalkingPrivateChatManager {
extension SpanChatManager {
    /// 是否需要插入已读回执引导充值VIP提醒消息
    /// - Parameter cellData: 消息数据
    /// - Returns: 结果
    //: static func needInsertVIPReadReceiptMsg(cellData: AgoneCellData) -> Bool {
    static func totalerest(cellData: AgoneCellData) -> Bool {
        //: if FacultyReactiveCompatible.share.loginUserMode.loungePlus == false,
        if FacultyReactiveCompatible.share.loginUserMode.loungePlus == false,
           //: cellData.innerMessage.groupID == nil,
           cellData.innerMessage.groupID == nil,
           //: cellData.status == .Msg_Status_Succ,
           cellData.status == .Msg_Status_Succ,
           //: cellData.direction == .MsgDirectionOutgoing,
           cellData.direction == .MsgDirectionOutgoing,
           //: cellData.msgModel.msgInfo.callCmd.count <= 0 {
           cellData.msgModel.msgInfo.callCmd.count <= 0
        {
            //: return true
            return true
        }
        //: return false
        return false
    }

    /// 是否展示消息 已读/未读 标识
    /// - Parameter cellData: 消息数据
    /// - Returns: 结果
    //: static func isShowReadMsg(cellData: AgoneCellData) -> Bool {
    static func bassProcessor(cellData: AgoneCellData) -> Bool {
        // vip && 非群聊 && 发送成功 && 本人发送 && 非音视频通话文本消息，展示消息是否已读标识
        //: if FacultyReactiveCompatible.share.loginUserMode.loungePlus == true,
        if FacultyReactiveCompatible.share.loginUserMode.loungePlus == true,
           //: cellData.innerMessage.groupID == nil,
           cellData.innerMessage.groupID == nil,
           //: cellData.status == .Msg_Status_Succ,
           cellData.status == .Msg_Status_Succ,
           //: cellData.direction == .MsgDirectionOutgoing,
           cellData.direction == .MsgDirectionOutgoing,
           //: cellData.msgModel.msgInfo.callCmd.count <= 0 {
           cellData.msgModel.msgInfo.callCmd.count <= 0
        {
            //: return true
            return true
        }
        //: return false
        return false
    }

    /// 消息对方是否已读
    /// - Parameter cellData: 消息数据
    /// - Returns: 结果
    //: static func msgIsRead(cellData: AgoneCellData) -> Bool {
    static func mirrorSatisfySend(cellData: AgoneCellData) -> Bool {
        //: if cellData.msgModel.params["replayId"] != nil { // 问题回答消息默认已读
        if cellData.msgModel.params[(app_walkUrl.replacingOccurrences(of: "cram", with: "ep") + "ayId")] != nil { // 问题回答消息默认已读
            //: return true
            return true
        }
        //: guard let selfMsgTime = cellData.innerMessage.timestamp else { return false }
        guard let selfMsgTime = cellData.innerMessage.timestamp else { return false }
        //: if cellData.innerMessage.isPeerRead == true {
        if cellData.innerMessage.isPeerRead == true {
            //: return true
            return true
        }
        //: let readTimeInterval = TalkingPrivateChatManager.share.readReceiptDict[cellData.innerMessage.userID] ?? 0
        let readTimeInterval = SpanChatManager.share.readReceiptDict[cellData.innerMessage.userID] ?? 0
        //: let readTime = Date(timeIntervalSince1970: TimeInterval(readTimeInterval))
        let readTime = Date(timeIntervalSince1970: TimeInterval(readTimeInterval))
        //: if selfMsgTime <= readTime {
        if selfMsgTime <= readTime {
            //: return true
            return true
        }
        //: return false
        return false
    }
}

// MARK: - Reply消息逻辑

//: extension TalkingPrivateChatManager {
extension SpanChatManager {
    /// 回复消息
    /// - Parameters:
    ///   - cellData: 数据
    ///   - targetId: 对方uid
    /// - Returns: 引用消息模型
    //: static func handleReplyMsg(cellData: AgoneCellData, targetId: String) -> AbTalkingChatMsgQuoteModel {
    static func zoneHandle(cellData: AgoneCellData, targetId: String) -> FirstSolidTitleTheoryModel {
        //: let quoteModel = AbTalkingChatMsgQuoteModel()
        let quoteModel = FirstSolidTitleTheoryModel()
        //: quoteModel.msgId = cellData.msgModel.msgInfo.msgId
        quoteModel.msgId = cellData.msgModel.msgInfo.msgId
        //: if cellData.direction == .MsgDirectionIncoming {
        if cellData.direction == .MsgDirectionIncoming {
            //: quoteModel.uid = targetId
            quoteModel.uid = targetId
            //: } else {
        } else {
            //: quoteModel.uid = FacultyReactiveCompatible.share.loginUserMode.userID
            quoteModel.uid = FacultyReactiveCompatible.share.loginUserMode.userID
        }
        //: let renderData = AbTalkingChatMsgQuoteDataModel()
        let renderData = ComputerBusinessModel()
        //: if cellData.isKind(of: AgoArrayCellData.self) {
        if cellData.isKind(of: AgoArrayCellData.self) {
            //: quoteModel.renderType = "text"
            quoteModel.renderType = (String(constNeverPath))
            //: renderData.content = cellData.msgModel.msgInfo.content
            renderData.content = cellData.msgModel.msgInfo.content

            //: } else if cellData.isKind(of: ApartCellData.self) {
        } else if cellData.isKind(of: ApartCellData.self) {
            //: quoteModel.renderType = "img"
            quoteModel.renderType = (k_channelDestroyGoingKey.replacingOccurrences(of: "under", with: "g"))
            //: renderData.previewUrl = cellData.msgModel.msgInfo.imageUri
            renderData.previewUrl = cellData.msgModel.msgInfo.imageUri

            //: } else if cellData.isKind(of: SaverCellData.self) {
        } else if cellData.isKind(of: SaverCellData.self) {
            //: quoteModel.renderType = "video"
            quoteModel.renderType = (String(mainComplexMessage))
            //: renderData.videoUrl = cellData.msgModel.msgInfo.video.videoUrl
            renderData.videoUrl = cellData.msgModel.msgInfo.video.videoUrl
            //: renderData.coverUrl = cellData.msgModel.msgInfo.video.coverImg
            renderData.coverUrl = cellData.msgModel.msgInfo.video.coverImg

            //: } else if cellData.isKind(of: TagCellData.self) {
        } else if cellData.isKind(of: TagCellData.self) {
            //: let audioCelldata = cellData as! TagCellData
            let audioCelldata = cellData as! TagCellData
            //: quoteModel.renderType = "audio"
            quoteModel.renderType = (noti_shoreSureValue.replacingOccurrences(of: "contribution", with: "ud"))
            //: let voiceCache = DBUserInfoManager.cache_getAudioDB(message: audioCelldata.innerMessage)
            let voiceCache = DeployMatrixDelegate.molarityMessage(message: audioCelldata.innerMessage)
            //: renderData.duration = Int(voiceCache.db_audioLength) ?? 0
            renderData.duration = Int(voiceCache.db_audioLength) ?? 0

            //: } else if cellData.isKind(of: RangeCellData.self) {
        } else if cellData.isKind(of: RangeCellData.self) {
            //: quoteModel.renderType = "gift"
            quoteModel.renderType = (main_originallyMsg.replacingOccurrences(of: "gin", with: "t"))
            //: let giftCellData = cellData as! RangeCellData
            let giftCellData = cellData as! RangeCellData
            //: renderData.giftName = giftCellData.msgModel.gift.name
            renderData.giftName = giftCellData.msgModel.gift.name
            //: renderData.giftNameI18n = giftCellData.msgModel.gift.nameI18n
            renderData.giftNameI18n = giftCellData.msgModel.gift.nameI18n
            //: renderData.giftIcon = giftCellData.msgModel.gift.imgPreview
            renderData.giftIcon = giftCellData.msgModel.gift.imgPreview
            //: renderData.giftPrice = giftCellData.msgModel.gift.price
            renderData.giftPrice = giftCellData.msgModel.gift.price
            //: renderData.giftNum = Int(giftCellData.msgModel.gift.num) ?? 0
            renderData.giftNum = Int(giftCellData.msgModel.gift.num) ?? 0
        }

        //: quoteModel.renderData = renderData
        quoteModel.renderData = renderData
        //: return quoteModel
        return quoteModel
    }

    /// 点击“引用”消息
    /// - Parameters:
    ///   - cellData: 数据
    ///   - targetId: 对方uid
    //: static func handleClickReplyMsg(cellData: AgoneCellData, targetId: String) {
    static func takeCareSharedPost(cellData: AgoneCellData, targetId: String) {
        //: let renderData = cellData.msgModel.quoteMsgInfo
        let renderData = cellData.msgModel.quoteMsgInfo
        //: UploadLogTool.writeLog(msg: "PrivateChat 点击引用消息\(renderData.renderType).")
        ElasticActiveEffect.pathAge(msg: (String(userActivityMessage.prefix(5)) + String(mainRefuseTitle) + String(user_securityFormat)) + "\(renderData.renderType).")
        //: if renderData.renderType == "text" {
        if renderData.renderType == (String(constNeverPath)) {
            //: let popView = QuoteDetailPopView(frame: CGRect(x: 0, y: 0, width: ScreenWidth, height: ScreenHeight))
            let popView = AdvancedReactiveCompatible(frame: CGRect(x: 0, y: 0, width: main_assetUrl, height: kTopUrl))
            //: popView.msgDetailText = renderData.renderData.content
            popView.msgDetailText = renderData.renderData.content
            //: popView.show()
            popView.forrad()

            //: } else if renderData.renderType == "img" {
        } else if renderData.renderType == (k_channelDestroyGoingKey.replacingOccurrences(of: "under", with: "g")) {
            //: let libraryPath = NSSearchPathForDirectoriesInDomains(.cachesDirectory, .userDomainMask, true).first!
            let libraryPath = NSSearchPathForDirectoriesInDomains(.cachesDirectory, .userDomainMask, true).first!
            //: var filePath = (libraryPath as NSString).appendingPathComponent(imageFileName)
            var filePath = (libraryPath as NSString).appendingPathComponent(data_marginPath)
            //: filePath = (filePath as NSString).appendingPathComponent((renderData.renderData.previewUrl as NSString).lastPathComponent)
            filePath = (filePath as NSString).appendingPathComponent((renderData.renderData.previewUrl as NSString).lastPathComponent)

            //: var model = TalkingMomentPhotoModel.init()
            var model = InmateTransformable()
            //: if FileManager.default.fileExists(atPath: filePath) {
            if FileManager.default.fileExists(atPath: filePath) {
                //: let data = NSData.init(contentsOfFile: filePath)
                let data = NSData(contentsOfFile: filePath)
                //: model.imagePic = UIImage(data: data! as Data)
                model.imagePic = UIImage(data: data! as Data)
                //: } else {
            } else {
                //: model.imgUrl =  renderData.renderData.previewUrl
                model.imgUrl = renderData.renderData.previewUrl
            }
            //: var currenModel = TalkingMomentModel.init()
            var currenModel = AgencyMomentModel()
            //: var tempArr: [TalkingMomentPhotoModel] = []
            var tempArr: [InmateTransformable] = []
            //: tempArr.append(model)
            tempArr.append(model)
            //: currenModel.pic = tempArr
            currenModel.pic = tempArr
            //: let vc = TalkingMomentPhotosVC.init(momentModel: currenModel, index: 0, type: .normal)
            let vc = SpanScreenVc(momentModel: currenModel, index: 0, type: .normal)
            //: TalkingPrivateChatManager.share.getNavigationController()?.pushViewController(vc, animated: true)
            SpanChatManager.share.shorePast()?.pushViewController(vc, animated: true)

            //: } else if renderData.renderType == "video" {
        } else if renderData.renderType == (String(mainComplexMessage)) {
            //: let videoPath = renderData.renderData.videoUrl
            let videoPath = renderData.renderData.videoUrl
            //: let vc = TalkingMomentVideoVC.init(videoPath: videoPath)
            let vc = AssociatedDirectProject(videoPath: videoPath)
            //: TalkingPrivateChatManager.share.getNavigationController()?.pushViewController(vc, animated: true)
            SpanChatManager.share.shorePast()?.pushViewController(vc, animated: true)

            //: } else if renderData.renderType == "audio" {
        } else if renderData.renderType == (noti_shoreSureValue.replacingOccurrences(of: "contribution", with: "ud")) {
            //: let cacheWrap = WCDBVoiceMsgTable.db_getVoiceMsg(with: renderData.msgId)
            let cacheWrap = DrawButtonMechanism.familyLayer(with: renderData.msgId)
            //: let playModel = TalkingVoiceMsgPlayModel()
            let playModel = CommunicationHandyJSON()
            //: if cacheWrap != nil {
            if cacheWrap != nil {
                //: playModel.db_voiceCacheWrap = cacheWrap!
                playModel.db_voiceCacheWrap = cacheWrap!

                //: } else {
            } else {
                //: let model = WCDBVoiceMsgTable()
                let model = DrawButtonMechanism()
                //: model.msgId = renderData.msgId
                model.msgId = renderData.msgId
                //: model.db_voiceUri = renderData.renderData.url
                model.db_voiceUri = renderData.renderData.url
                //: model.db_senduid = renderData.uid
                model.db_senduid = renderData.uid
                //: if renderData.uid == FacultyReactiveCompatible.share.loginUserMode.userID {
                if renderData.uid == FacultyReactiveCompatible.share.loginUserMode.userID {
                    //: model.db_touid = targetId
                    model.db_touid = targetId
                    //: } else {
                } else {
                    //: model.db_touid = FacultyReactiveCompatible.share.loginUserMode.userID
                    model.db_touid = FacultyReactiveCompatible.share.loginUserMode.userID
                }
                //: model.db_audioLength = "\(renderData.renderData.duration)"
                model.db_audioLength = "\(renderData.renderData.duration)"
                //: WCDBVoiceMsgTable.db_insertVoiceMsg(model)
                DrawButtonMechanism.tapTransfer(model)
                //: playModel.db_voiceCacheWrap = model
                playModel.db_voiceCacheWrap = model
            }

            //: TalkingVoiceMsgPlayManager.shared.stopAudioPlayer()
            LargeObjectProtocol.shared.narrowPlayer()
            //: TalkingVoiceMsgPlayManager.shared.starPlayReplyMsg(playModel: playModel)
            LargeObjectProtocol.shared.spellModel(playModel: playModel)

            //: } else if renderData.renderType == "gift" {
        } else if renderData.renderType == (main_originallyMsg.replacingOccurrences(of: "gin", with: "t")) {
            //: let popView = QuoteDetailPopView(frame: CGRect(x: 0, y: 0, width: ScreenWidth, height: ScreenHeight))
            let popView = AdvancedReactiveCompatible(frame: CGRect(x: 0, y: 0, width: main_assetUrl, height: kTopUrl))
            //: popView.msgDetailText = "Sent ".localized + renderData.renderData.giftNameLocal() + " x\(renderData.renderData.giftNum)"
            popView.msgDetailText = (String(kProcedurePath)).localized + renderData.renderData.arraySave() + " x\(renderData.renderData.giftNum)"
            //: popView.show()
            popView.forrad()
        }
    }
}

// MARK: - Event

//: extension TalkingPrivateChatManager {
extension SpanChatManager {
    /// 检测用户短视频版本是否可用【> = 1.1.6 可用】
    /// - Returns: 是否可用
    //: static func checkShortVideo(userVersion: String) -> Bool {
    static func thumb(userVersion: String) -> Bool {
        //: guard let version = Int(userVersion.replacingOccurrences(of: ".", with: "")) else {
        guard let version = Int(userVersion.replacingOccurrences(of: ".", with: "")) else {
            //: return false
            return false
        }
        //: if version*100 < 116*100 {
        if version * 100 < 116 * 100 {
            //: return false
            return false
        }

        //: return true
        return true
    }
}
