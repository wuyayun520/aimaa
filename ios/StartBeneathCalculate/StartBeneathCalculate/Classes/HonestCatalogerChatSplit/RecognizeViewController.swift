
//: Declare String Begin

/*: "init(coder:) has not been implemented" :*/
fileprivate let noti_giftId:[UInt8] = [0x4b,0x50,0x4b,0x56,0xa,0x45,0x51,0x46,0x47,0x54,0x1c,0xb,0x2,0x4a,0x43,0x55,0x2,0x50,0x51,0x56,0x2,0x44,0x47,0x47,0x50,0x2,0x4b,0x4f,0x52,0x4e,0x47,0x4f,0x47,0x50,0x56,0x47,0x46]

fileprivate func allLeast(origin num: UInt8) -> UInt8 {
    let value = Int(num) - 226
    if value < 0 {
        return UInt8(value + 256)
    } else {
        return UInt8(value)
    }
}

/*: "Load..." :*/
fileprivate let noti_hiddenMsg:String = "l"
fileprivate let kCityIndexUrl:[Character] = ["o","a","d",".",".","."]

/*: "Skip" :*/
fileprivate let constDeviceStr:String = "personal ownerSkip"

/*: "HttpTool.NativeToJs('recharge')" :*/
fileprivate let main_boxMsg:[UInt8] = [0x29,0x27,0x65,0x67,0x72,0x61,0x68,0x63,0x65,0x72,0x27,0x28,0x73,0x4a,0x6f,0x54,0x65,0x76,0x69,0x74,0x61,0x4e,0x2e,0x6c,0x6f,0x6f,0x54,0x70,0x74,0x74,0x48]

/*: "window.onPageShow&&onPageShow();" :*/
fileprivate let noti_globalStr:[UInt8] = [0xdc,0xc2,0xc5,0xcf,0xc4,0xdc,0x85,0xc4,0xc5,0xfb,0xca,0xcc,0xce,0xf8,0xc3,0xc4,0xdc,0x8d,0x8d,0xc4,0xc5,0xfb,0xca,0xcc,0xce,0xf8,0xc3,0xc4,0xdc,0x83,0x82,0x90]

private func freshScenario(yield num: UInt8) -> UInt8 {
    return num ^ 171
}

/*: "jsEvent(onPageShow):  :*/
fileprivate let const_maxStr:String = "jsEvenpossible evaluate lag"
fileprivate let showPriceMessage:String = "automatic unable perspectivegeShow"
fileprivate let k_usedPath:[Character] = [")",":"," "]

/*: "window.onPageHide&&onPageHide();" :*/
fileprivate let showRankMsg:[UInt8] = [0xfe,0xf0,0xf5,0xeb,0xf6,0xfe,0xb5,0xf6,0xf5,0xd7,0xe8,0xee,0xec,0xcf,0xf0,0xeb,0xec,0xad,0xad,0xf6,0xf5,0xd7,0xe8,0xee,0xec,0xcf,0xf0,0xeb,0xec,0xaf,0xb0,0xc2]

fileprivate func dataCoordinate(application num: UInt8) -> UInt8 {
    let value = Int(num) - 135
    if value < 0 {
        return UInt8(value + 256)
    } else {
        return UInt8(value)
    }
}

/*: "jsEvent(onPageHide):  :*/
fileprivate let const_oatYourMsg:String = "jsEvemark some context possible"
fileprivate let constAnonymousFormat:[Character] = ["a","g","e","H"]
fileprivate let data_partDownValue:[Character] = ["i","d","e",")",":"," "]

/*: "title" :*/
fileprivate let appOndShoreId:String = "perspectivetle"

/*: "canGoBack" :*/
fileprivate let notiEverySwallowCorrectName:String = "canGlicense boundary join"

/*: "PHPSESSID= :*/
fileprivate let appVidMessage:[Character] = ["P","H","P","S","E","S","S","I","D","="]

/*: ";uid= :*/
fileprivate let mainHimName:String = ";uid=daily balloon unusual heart receive"

/*: "Cookie" :*/
fileprivate let appAlleyStr:String = "Cookiecontinue height edit written"

/*: "nav_back_black_nor" :*/
fileprivate let constHelpTitle:String = "bronze under i compositionnav_b"
fileprivate let dataLanguageTitle:String = "k_nornor identity hormone beat"

/*: "ico_intimate_list_sm" :*/
fileprivate let k_writingData:String = "ico_sun journal combined shrink sign"
fileprivate let show_globDistantFormat:String = "valid shrinkmate_l"
fileprivate let kSuccessfullyKey:[Character] = ["i","s","t","_","s","m"]

/*: "document.getElementsByTagName('body')[0].style.background='rgba(0,0,0,0)'" :*/
fileprivate let app_hideText:[UInt8] = [0xea,0xf5,0xe9,0xfb,0xf3,0xeb,0xf4,0xfa,0xb4,0xed,0xeb,0xfa,0xcb,0xf2,0xeb,0xf3,0xeb,0xf4,0xfa,0xf9,0xc8,0xff,0xda,0xe7,0xed,0xd4,0xe7,0xf3,0xeb,0xae,0xad,0xe8,0xf5,0xea,0xff,0xad,0xaf,0xe1,0xb6,0xe3,0xb4,0xf9,0xfa,0xff,0xf2,0xeb,0xb4,0xe8,0xe7,0xe9,0xf1,0xed,0xf8,0xf5,0xfb,0xf4,0xea,0xc3,0xad,0xf8,0xed,0xe8,0xe7,0xae,0xb6,0xb2,0xb6,0xb2,0xb6,0xb2,0xb6,0xaf,0xad]

fileprivate func cramRange(user num: UInt8) -> UInt8 {
    let value = Int(num) - 134
    if value < 0 {
        return UInt8(value + 256)
    } else {
        return UInt8(value)
    }
}

/*: "Alert" :*/
fileprivate let data_safetyStr:String = "Alertcompare allow bread"

/*: "Cancle" :*/
fileprivate let constBounceExploreName:[Character] = ["C","a","n","c","l","e"]

/*: "Done" :*/
fileprivate let noti_thinExplainFormat:String = "Doneyou group blue video computer"

/*: "js call method name =  :*/
fileprivate let appExactlyValue:String = "js call owner coordinate error plain cut"
fileprivate let userScheduleId:[Character] = ["m","e","t"]
fileprivate let user_birthdayWhatFormat:String = "hod why wife stock map and"

/*: , message =  :*/
fileprivate let mainGlobUrl:String = "stick examine blend ready, messa"
fileprivate let notiCarrierName:[Character] = ["g","e"," ","="," "]

/*: "http://" :*/
fileprivate let app_questionTitle:[Character] = ["h"]
fileprivate let noti_bitKey:String = "ttp://eastern scope yield pop"

/*: "https://" :*/
fileprivate let main_tireStr:[Character] = ["h","t","t","p","s",":","/","/"]

/*: "var cookieNames = document.cookie.split('; ').map(function(cookie) { return cookie.split('=')[0] } );\n" :*/
fileprivate let showInsertAttributeName:[UInt8] = [0x73,0x5e,0x6f,0x1d,0x60,0x6c,0x6c,0x68,0x66,0x62,0x4b,0x5e,0x6a,0x62,0x70,0x1d,0x3a,0x1d,0x61,0x6c,0x60,0x72,0x6a,0x62,0x6b,0x71,0x2b,0x60,0x6c,0x6c,0x68,0x66,0x62,0x2b,0x70,0x6d,0x69,0x66,0x71,0x25,0x24,0x38,0x1d,0x24,0x26,0x2b,0x6a,0x5e,0x6d,0x25,0x63,0x72,0x6b,0x60,0x71,0x66,0x6c,0x6b,0x25,0x60,0x6c,0x6c,0x68,0x66,0x62,0x26,0x1d,0x78,0x1d,0x6f,0x62,0x71,0x72,0x6f,0x6b,0x1d,0x60,0x6c,0x6c,0x68,0x66,0x62,0x2b,0x70,0x6d,0x69,0x66,0x71,0x25,0x24,0x3a,0x24,0x26,0x58,0x2d,0x5a,0x1d,0x7a,0x1d,0x26,0x38,0x7]

fileprivate func clipMessage(good num: UInt8) -> UInt8 {
    let value = Int(num) + 3
    if value > 255 {
        return UInt8(value - 256)
    } else {
        return UInt8(value)
    }
}

/*: "uid= :*/
fileprivate let k_pureCapableUrl:String = "guide simultaneouslyuid="

/*: ;domain= :*/
fileprivate let app_hoodStr:String = "upon place alongside sew wait;domain"
fileprivate let userNearbyName:String = "snap"

/*: ;path=/" :*/
fileprivate let noti_multiMessage:[Character] = [";","p","a","t","h","=","/"]

/*: "document.cookie=' :*/
fileprivate let notiFarStr:String = "DOCUM"
fileprivate let app_backyardContent:String = "okie=\'"

/*: ;domin= :*/
fileprivate let appHiddenMsg:String = ";domin=listener manual off fast"

/*: "if (cookieNames.indexOf('PHPSESSID') == -1) { document.cookie=' :*/
fileprivate let data_scopeFormat:[UInt8] = [0x27,0x3d,0x65,0x69,0x6b,0x6f,0x6f,0x63,0x2e,0x74,0x6e,0x65,0x6d,0x75,0x63,0x6f,0x64,0x20,0x7b,0x20,0x29,0x31,0x2d,0x20,0x3d,0x3d,0x20,0x29,0x27,0x44,0x49,0x53,0x53,0x45,0x53,0x50,0x48,0x50,0x27,0x28,0x66,0x4f,0x78,0x65,0x64,0x6e,0x69,0x2e,0x73,0x65,0x6d,0x61,0x4e,0x65,0x69,0x6b,0x6f,0x6f,0x63,0x28,0x20,0x66,0x69]

/*: "h5Domain =  :*/
fileprivate let mainTranslationEditorFormat:[Character] = ["h","5","D","o"]
fileprivate let showClickMessage:String = "main = year define"

/*: "getAllCookies =  :*/
fileprivate let dataRangeKey:String = "absolutely chance nevergetAll"
fileprivate let show_feedStr:String = "ies =database broad spire tip"
fileprivate let notiRocketFormat:[Character] = [" "]

/*: "PHPSESSID" :*/
fileprivate let user_taskPerformStr:String = "PHPcloud"

/*: "uid" :*/
fileprivate let user_propertyMessage:String = "urefuse"

/*: "appCookies =  :*/
fileprivate let k_listenerName:[Character] = ["a","p","p","C","o","o"]
fileprivate let main_worldRemoteName:String = "quality abs detailed tingkies = "

/*: "UID" :*/
fileprivate let kReportedFormat:[Character] = ["U","I","D"]

/*: "navigator.userAgent" :*/
fileprivate let data_fragmentUrl:String = "naburn"
fileprivate let show_paraData:[Character] = [".","u","s","e","r","A","g","e","n","t"]

/*: "xunwu_iphone" :*/
fileprivate let mainComplexFormat:String = "memorynwu"
fileprivate let constBodyStr:[Character] = ["_","i","p","h","o","n","e"]

/*: " xunwu_device_id/ :*/
fileprivate let user_tickId:String = " xunwlist unable write honey"
fileprivate let main_glassStr:[Character] = ["u","_","d","e","v","i","c","e","_","i","d","/"]

/*: "xunwu_iphone xunwu_version/ :*/
fileprivate let noti_ignoreValue:String = "xuflash"
fileprivate let user_earlyUrl:String = "crow insertwu_iph"
fileprivate let app_ratingName:String = "XUNW"
fileprivate let show_innerUrl:String = "sion/fun found handle slide"

/*:  xunwu_package/ :*/
fileprivate let userSpringKey:[Character] = [" ","x","u","n","w","u","_","p"]
fileprivate let appFullyBlueName:String = "rating flash contrastackage/"

/*:  xunwu_bundleId/ :*/
fileprivate let data_burnRegainKey:[Character] = [" ","x","u","n","w","u","_"]
fileprivate let const_someoneKey:String = "bujust"
fileprivate let k_stripPath:[Character] = ["d","l","e","I","d","/"]

/*:   webkit/1" :*/
fileprivate let user_needFormat:[Character] = [" "," ","w","e","b"]
fileprivate let constSideMeltKey:String = "necessary mic bucket upper ourkit/1"

//: Declare String End

// __DEBUG__
// __CLOSE_PRINT__
//
//  RecognizeViewController.swift
//  AbroadTalking
//
//  Created by Hemming on 2022/9/2.
//

//: import UIKit
import UIKit
//: import WebKit
import WebKit

//: class TalkingWebViewController: TalkingBasePresentViewController {
class RecognizeViewController: ReserveViewController {
    //: typealias ExitWebAction = ( ) -> Void
    typealias ExitWebAction = () -> Void
    //: typealias OpenWebSuccess = (_ openSucc: Bool) -> Void
    typealias OpenWebSuccess = (_ openSucc: Bool) -> Void
    //: typealias HandleJSAction = (_ actionName: String) -> Void
    typealias HandleJSAction = (_ actionName: String) -> Void

    //: private let disposeBag = DisposeBag()
    private let disposeBag = DisposeBag()
    //: private var requestReady = false
    private var requestReady = false

    //: private var config: TalkingWebConfig!
    private var config: CalendarConfig!
    //: var isModal = false
    var isModal = false // 是否是模态弹出
    //: var urlStr: String!
    var urlStr: String!
    //: private var openBlock: OpenWebSuccess!
    private var openBlock: OpenWebSuccess!
    //: private var handleJSBlock: HandleJSAction!
    private var handleJSBlock: HandleJSAction!
    //: private var exitBlock: ExitWebAction!
    private var exitBlock: ExitWebAction!
    //: private var webUrlType: WebUrlType?
    private var webUrlType: BorderBasicType?
    //: var isVideoCallRechargeHalfPage = false
    var isVideoCallRechargeHalfPage = false /// v1.5.8是否视频弹窗唤醒的半屏充值或订阅

    //: init(urlString: String, webViewType: WebUrlType?, webConfig: TalkingWebConfig) {
    init(urlString: String, webViewType: BorderBasicType?, webConfig: CalendarConfig) {
        //: super.init()
        super.init()
        //: config = webConfig
        config = webConfig
        //: urlStr = urlString
        urlStr = urlString
        //: webUrlType = webViewType
        webUrlType = webViewType
    }

    //: required init?(coder: NSCoder) {
    required init?(coder _: NSCoder) {
        //: fatalError("init(coder:) has not been implemented")
        fatalError(String(bytes: noti_giftId.map{allLeast(origin: $0)}, encoding: .utf8)!)
    }

    //: deinit {
    deinit {
        //: NSObject.cancelPreviousPerformRequests(withTarget: self)
        NSObject.cancelPreviousPerformRequests(withTarget: self)
        //: webView.stopLoading()
        webView.stopLoading()
        //: webView.uiDelegate = nil
        webView.uiDelegate = nil
        //: webView.navigationDelegate = nil
        webView.navigationDelegate = nil
        //: webView.removeFromSuperview()
        webView.removeFromSuperview()

        // 清缓存
        //: let websiteDataTypes: Set<String> = [WKWebsiteDataTypeDiskCache]
        let websiteDataTypes: Set<String> = [WKWebsiteDataTypeDiskCache]
        //: let dateForm = Date.init(timeIntervalSince1970: 0)
        let dateForm = Date(timeIntervalSince1970: 0)
        //: WKWebsiteDataStore.default().removeData(ofTypes: websiteDataTypes, modifiedSince: dateForm) {
        WKWebsiteDataStore.default().removeData(ofTypes: websiteDataTypes, modifiedSince: dateForm) {}
    }

    //: override func viewWillAppear(_ animated: Bool) {
    override func viewWillAppear(_ animated: Bool) {
        //: super.viewWillAppear(animated)
        super.viewWillAppear(animated)
        //: jsEvent_onPageShow()
        outmost()
    }

    //: override func viewWillDisappear(_ animated: Bool) {
    override func viewWillDisappear(_ animated: Bool) {
        //: super.viewWillDisappear(animated)
        super.viewWillDisappear(animated)
        //: jsEvent_onPageHide()
        targetForward()
        /// 移除通知
        //: NotificationCenter.default.removeObserver(self)
        NotificationCenter.default.removeObserver(self)
    }

    //: override func viewDidLoad() {
    override func viewDidLoad() {
        //: super.viewDidLoad()
        super.viewDidLoad()
        //: view.clipsToBounds = true
        view.clipsToBounds = true
        //: self.title = "Load..."
        self.title = (noti_hiddenMsg.uppercased() + String(kCityIndexUrl))
        //: if config.isHideBackBtn {
        if config.isHideBackBtn {
            //: hideNavi = true
            hideNavi = true
        }
        //: addNotification()
        shrinkNotification()
        //: func__checkConfig()
        bronzeStarMedalPlain()
        //: func__setUpWebView()
        vital()
        //: func__registerObserver()
        boneOfContention()
        //: func__beginStartRequest()
        policyIn()
        //: func_checkAppleIAP()
        guideAcrossBlend()

        //: self.tranConfig.exitPrentView = { [weak self] in
        self.tranConfig.exitPrentView = { [weak self] in
            //: guard let self = self else {
            guard let self = self else {
                //: return
                return
            }
            //: if self.config.isHalfView && self.webUrlType == .RechargeHalfPage {
            if self.config.isHalfView && self.webUrlType == .RechargeHalfPage {
                //: SmartThen.share.func__pushToWebVC(webViewType: .DiscountPopup)
                SmartThen.share.joinType(webViewType: .DiscountPopup)
                //: NotificationCenter.default.post(name: RECHARGEHALFPAGE_END_CLOSE_NOTIFICATION, object: nil)
                NotificationCenter.default.post(name: userAppMsg, object: nil)
            }
        }
    }

    // MARK: - Lazy load

    //: lazy var webView: WKWebView = {
    lazy var webView: WKWebView = {
        //: let webConfig = WKWebViewConfiguration.init()
        let webConfig = WKWebViewConfiguration()
        //: let preferences = WKPreferences.init()
        let preferences = WKPreferences()
        //: preferences.javaScriptEnabled = true
        preferences.javaScriptEnabled = true
        //: webConfig.preferences = preferences
        webConfig.preferences = preferences
        //: webConfig.allowsInlineMediaPlayback = true
        webConfig.allowsInlineMediaPlayback = true
        //: webConfig.mediaTypesRequiringUserActionForPlayback = []
        webConfig.mediaTypesRequiringUserActionForPlayback = []

        //: let userControl = WKUserContentController.init()
        let userControl = WKUserContentController()
        //: webConfig.userContentController = userControl
        webConfig.userContentController = userControl
        //: if #available(iOS 11.0, *) {
        if #available(iOS 11.0, *) {
            //: func__checkWebCookiesBeforeRequest(webConfig: webConfig)
            plain(webConfig: webConfig)
        }
        //: let w = WKWebView.init(frame: .zero, configuration: webConfig)
        let w = WKWebView(frame: .zero, configuration: webConfig)
        //: w.uiDelegate = self
        w.uiDelegate = self
        //: w.navigationDelegate = self
        w.navigationDelegate = self
        //: w.allowsLinkPreview = false
        w.allowsLinkPreview = false
        //: w.allowsBackForwardNavigationGestures = true
        w.allowsBackForwardNavigationGestures = true
        //: if #available(iOS 11.0, *) {
        if #available(iOS 11.0, *) {
            //: w.scrollView.contentInsetAdjustmentBehavior = .never
            w.scrollView.contentInsetAdjustmentBehavior = .never
        }
        //: return w
        return w
        //: }()
    }()

    //: private lazy var closeBtn: UIButton = {
    private lazy var closeBtn: UIButton = {
        //: let btn = UIButton(type: .custom)
        let btn = UIButton(type: .custom)
        //: let attributes = [.foregroundColor: UIColor.appThemeColor(), .font: UIFont.pingfangFont(type: .Medium, fontSize: 22), .underlineStyle: 1] as [NSAttributedString.Key: Any]
        let attributes = [.foregroundColor: UIColor.crosswise(), .font: UIFont.recipeDecision(type: .Medium, fontSize: 22), .underlineStyle: 1] as [NSAttributedString.Key: Any]
        //: let attributeString = NSAttributedString(string: "Skip".localized, attributes: attributes)
        let attributeString = NSAttributedString(string: (String(constDeviceStr.suffix(4))).localized, attributes: attributes)
        //: btn.setAttributedTitle(attributeString, for: .normal)
        btn.setAttributedTitle(attributeString, for: .normal)
        //: return btn
        return btn
        //: }()
    }()
}

// MARK: - 【通知、js事件】

//: extension TalkingWebViewController {
extension RecognizeViewController {
    /// 添加通知
    //: private func addNotification() {
    private func shrinkNotification() {
        //: NotificationCenter.default.addObserver(self,
        NotificationCenter.default.addObserver(self,
                                               //: selector: #selector(third_jsEvent_refreshCoin),
                                               selector: #selector(eventPut),
                                               //: name: WEBVIEW_THIRD_REFRESHCOIN_NOTIFICATION,
                                               name: mainFrameServiceTitle,
                                               //: object: nil)
                                               object: nil)
        // 应用从后台切换到前台
        //: NotificationCenter.default.addObserver(self,
        NotificationCenter.default.addObserver(self,
                                               //: selector: #selector(jsEvent_onPageShow),
                                               selector: #selector(outmost),
                                               //: name: UIApplication.willEnterForegroundNotification,
                                               name: UIApplication.willEnterForegroundNotification,
                                               //: object: nil)
                                               object: nil)
    }

    /// 通知三方H5刷新金币
    //: @objc private func third_jsEvent_refreshCoin() {
    @objc private func eventPut() {
        //: self.webView.evaluateJavaScript("HttpTool.NativeToJs('recharge')") { data, error in
        self.webView.evaluateJavaScript(String(bytes: main_boxMsg.reversed(), encoding: .utf8)!) { _, _ in
        }
    }

    /// js事件：网页展示
    //: @objc private func jsEvent_onPageShow() {
    @objc private func outmost() {
        //: self.webView.evaluateJavaScript("window.onPageShow&&onPageShow();") { data, error in
        self.webView.evaluateJavaScript(String(bytes: noti_globalStr.map{freshScenario(yield: $0)}, encoding: .utf8)!) { data, error in
            //: printLog(message: "jsEvent(onPageShow): \(String(describing: data))---\(String(describing: error))")
            printLog(message: (String(const_maxStr.prefix(6)) + "t(onPa" + String(showPriceMessage.suffix(6)) + String(k_usedPath)) + "\(String(describing: data))---\(String(describing: error))")
        }
    }

    /// js事件：网页消失
    //: private func jsEvent_onPageHide() {
    private func targetForward() {
        //: self.webView.evaluateJavaScript("window.onPageHide&&onPageHide();") { data, error in
        self.webView.evaluateJavaScript(String(bytes: showRankMsg.map{dataCoordinate(application: $0)}, encoding: .utf8)!) { data, error in
            //: printLog(message: "jsEvent(onPageHide): \(String(describing: data))---\(String(describing: error))")
            printLog(message: (String(const_oatYourMsg.prefix(5)) + "nt(onP" + String(constAnonymousFormat) + String(data_partDownValue)) + "\(String(describing: data))---\(String(describing: error))")
        }
    }
}

//: extension TalkingWebViewController {
extension RecognizeViewController {
    //: func func__registerObserver() {
    func boneOfContention() {
        //: webView.rx.observeWeakly(String.self, "title")
        webView.rx.observeWeakly(String.self, (appOndShoreId.replacingOccurrences(of: "perspective", with: "ti")))
            //: .subscribe(onNext: { [weak self] (value) in
            .subscribe(onNext: { [weak self] value in
                //: guard let self = self else { return }
                guard let self = self else { return }
                //: self.title = value
                self.title = value
                //: })
            })
            //: .disposed(by: disposeBag)
            .disposed(by: disposeBag)

        //: webView.rx.observeWeakly(String.self, "canGoBack")
        webView.rx.observeWeakly(String.self, (String(notiEverySwallowCorrectName.prefix(4)) + "oBack"))
            //: .subscribe(onNext: { _ in
            .subscribe(onNext: { _ in
                //: })
            })
            //: .disposed(by: disposeBag)
            .disposed(by: disposeBag)

        //: NotificationCenter.default.addObserver(self, selector: #selector(closeDiscountsView), name: VIDEOCALL_END_CLOSEDISCOUNTS_NOTIFICATION, object: nil)
        NotificationCenter.default.addObserver(self, selector: #selector(picStar), name: appEventUrl, object: nil)
    }

    //: func func__setUpWebView() {
    func vital() {
        //: view.addSubview(webView)
        view.addSubview(webView)
        //: webView.snp.makeConstraints { make in
        webView.snp.makeConstraints { make in
            //: make.edges.equalToSuperview()
            make.edges.equalToSuperview()
        }
        //: func__addcustomUserAgent()
        incentiveOption()
        //: func__addBridgeMethod()
        outsideBurn()

        //: if config.clearBgColor {
        if config.clearBgColor {
            //: view.backgroundColor = .clear
            view.backgroundColor = .clear
            //: } else if config.isAlertType || config.showLoadingView {
        } else if config.isAlertType || config.showLoadingView {
            //: view.backgroundColor = .black.withAlphaComponent(0.4)
            view.backgroundColor = .black.withAlphaComponent(0.4)
            //: webView.backgroundColor = UIColor.clear
            webView.backgroundColor = UIColor.clear
            //: } else {
        } else {
            //: view.backgroundColor = UIColor.appBgColor()
            view.backgroundColor = UIColor.changeSibling()
        }
        //: if config.isHalfView {
        if config.isHalfView {
            //: webView.scrollView.bounces = false
            webView.scrollView.bounces = false
        }

        //: if config.showLoadingView {
        if config.showLoadingView {
            //: ProgressHUD.show(superView: webView)
            StrangerProgressHUD.superCollection(superView: webView)
        }

        //: if config.showCloseBtn {
        if config.showCloseBtn {
            //: view.addSubview(closeBtn)
            view.addSubview(closeBtn)
            //: view.bringSubviewToFront(closeBtn)
            view.bringSubviewToFront(closeBtn)
            //: closeBtn.isHidden = false
            closeBtn.isHidden = false
            //: closeBtn.snp.makeConstraints { make in
            closeBtn.snp.makeConstraints { make in
                //: make.height.width.equalTo(50)
                make.height.width.equalTo(50)
                //: make.trailing.equalTo(-10)
                make.trailing.equalTo(-10)
                //: make.height.equalToSuperview().offset(StatusBarHeight)
                make.height.equalToSuperview().offset(show_textNextFormat)
            }
        }
    }

    //: func func__beginStartRequest() {
    func policyIn() {
        //: if !requestReady {
        if !requestReady {
            //: return
            return
        }
        //: if config.isAlertType {
        if config.isAlertType {
            //: DispatchQueue.main.asyncAfter(deadline: .now()+1) { [self] in
            DispatchQueue.main.asyncAfter(deadline: .now() + 1) { [self] in
                //: self.func__startRequest()
                self.dayOfRemembrance()
            }
            //: } else {
        } else {
            //: func__startRequest()
            dayOfRemembrance()
        }
    }

    //: func func__startRequest() {
    func dayOfRemembrance() {
        //: var urlRequest = URLRequest.init(url: URL.init(string: urlStr)!)
        var urlRequest = URLRequest(url: URL(string: urlStr)!)
        //: let uid = FacultyReactiveCompatible.share.loginUid
        let uid = FacultyReactiveCompatible.share.loginUid
        //: let ssid = FacultyReactiveCompatible.share.loginSessionId
        let ssid = FacultyReactiveCompatible.share.loginSessionId
        //: var cookieStr = "PHPSESSID=\(ssid)"
        var cookieStr = (String(appVidMessage)) + "\(ssid)"
        //: if uid.count > 0 {
        if uid.count > 0 {
            //: cookieStr.append(";uid=\(uid)")
            cookieStr.append((String(mainHimName.prefix(5))) + "\(uid)")
        }
        //: urlRequest.addValue(cookieStr, forHTTPHeaderField: "Cookie")
        urlRequest.addValue(cookieStr, forHTTPHeaderField: (String(appAlleyStr.prefix(6))))
        //: urlRequest.httpShouldHandleCookies = false
        urlRequest.httpShouldHandleCookies = false
        //: webView.load(urlRequest)
        webView.load(urlRequest)
        //: func__clearJSBgColor()
        isSkin()
        //: func__initRightButton()
        sinceTalk()
    }

    /// apple支付重试末完成的订单
    //: func func_checkAppleIAP() {
    func guideAcrossBlend() {
        //: if webUrlType == .RechargeFullPage || webUrlType == .RechargeHalfPage {
        if webUrlType == .RechargeFullPage || webUrlType == .RechargeHalfPage {
            //: AppleIAPManager.shared.completionHandle = { [weak self] status, reportMoney, payType in
            CreaterReactiveCompatible.shared.completionHandle = { [weak self] status, reportMoney, payType in
                //: guard let self = self else {
                guard let self = self else {
                    //: return
                    return
                }
                //: if status == .veritySucceed {
                if status == .veritySucceed {
                    //: self.appleIAPVeritySucceed(price: reportMoney, payType: payType)
                    self.manifest(price: reportMoney, payType: payType)
                }
            }
            //: AppleIAPManager.shared.iap_checkUnfinishedTransactions()
            CreaterReactiveCompatible.shared.labelTransactions()
        }
    }

    //: func func__initLeftButton() {
    func naturalEventButton() {
        //: if self.presentingViewController != nil {
        if self.presentingViewController != nil {
            //: return
            return
        }
        //: let item = UIBarButtonItem.init(image: UIImage.BundleImageNamed(name: "nav_back_black_nor"), style: .plain, target: self, action: #selector(func__closeAction(sender:)))
        let item = UIBarButtonItem(image: UIImage.adName(name: (String(constHelpTitle.suffix(5)) + "ack_blac" + String(dataLanguageTitle.prefix(5)))), style: .plain, target: self, action: #selector(hourSender(sender:)))
        //: item.imageInsets = UIEdgeInsets(top: 0, left: -6, bottom: 0, right: 0)
        item.imageInsets = UIEdgeInsets(top: 0, left: -6, bottom: 0, right: 0)
        //: item.width = 34
        item.width = 34
        //: self.navigationItem.leftBarButtonItem = item
        self.navigationItem.leftBarButtonItem = item
    }

    //: func func__initRightButton() {
    func sinceTalk() {
        //: self.navigationItem.rightBarButtonItem = nil
        self.navigationItem.rightBarButtonItem = nil
        //: if urlStr.contains(WebUrlType.Intimate.rawValue) {
        if urlStr.contains(BorderBasicType.Intimate.rawValue) {
            //: let item = UIBarButtonItem.init(image: UIImage.BundleImageNamed(name: "ico_intimate_list_sm"), style: .plain, target: self, action: #selector(func__intimateAction))
            let item = UIBarButtonItem(image: UIImage.adName(name: (String(k_writingData.prefix(4)) + "inti" + String(show_globDistantFormat.suffix(6)) + String(kSuccessfullyKey))), style: .plain, target: self, action: #selector(blindAction))
            //: self.navigationItem.rightBarButtonItem = item
            self.navigationItem.rightBarButtonItem = item
        }
    }

    //: @objc func func__closeAction(sender: UIBarButtonItem?) {
    @objc func hourSender(sender: UIBarButtonItem?) {
        //: if webView.canGoBack && sender != nil {
        if webView.canGoBack, sender != nil {
            //: webView.goBack()
            webView.goBack()
            //: return
            return
        }
        //: if  exitBlock != nil {
        if exitBlock != nil {
            //: exitBlock()
            exitBlock()
        }
        //: self.tranConfig = nil
        self.tranConfig = nil
        //: func__removeBridgeMethod()
        commonTotal()
        //: if self.presentingViewController != nil {
        if self.presentingViewController != nil {
            // 当前页面dismiss后，下面还是网页时，手动调用viewDidAppear
            //: dismiss(animated: true) {
            dismiss(animated: true) {
                //: if let currentVC = self.currentViewController() {
                if let currentVC = self.field() {
                    //: if currentVC.isKind(of: TalkingWebViewController.self) &&
                    if currentVC.isKind(of: RecognizeViewController.self),
                       //: (currentVC as! TalkingWebViewController).isModal == true {
                       (currentVC as! RecognizeViewController).isModal == true
                    {
                        //: (currentVC as! TalkingWebViewController).jsEvent_onPageShow()
                        (currentVC as! RecognizeViewController).outmost()
                    }
                }
            }
            //: } else {
        } else {
            //: navigationController?.popViewController(animated: true)
            navigationController?.popViewController(animated: true)
        }
    }

    //: func func__checkConfig() {
    func bronzeStarMedalPlain() {
        //: if config.isAlertType {
        if config.isAlertType {
            //: config.showLoadingView = true
            config.showLoadingView = true
            //: config.showCloseBtn = true
            config.showCloseBtn = true
        }
        //: if #available(iOS 11.0, *) {
        if #available(iOS 11.0, *) {
            //: } else {
        } else {
            //: requestReady = true
            requestReady = true
        }
    }

    //: func func__hideLoadingBgViewAndCloseBtn(sucess: Bool) {
    func immediateIn(sucess: Bool) {
        //: ProgressHUD.dismiss()
        StrangerProgressHUD.barDismiss()
        //: UIView.animate(withDuration: 0.1) {
        UIView.animate(withDuration: 0.1) {
            //: self.view.backgroundColor = .clear
            self.view.backgroundColor = .clear
            //: if sucess {
            if sucess {
                //: self.closeBtn.isHidden = true
                self.closeBtn.isHidden = true
            }
        }
    }

    //: func func__clearJSBgColor() {
    func isSkin() {
        //: if config.clearBgColor {
        if config.clearBgColor {
            //: self.webView.evaluateJavaScript("document.getElementsByTagName('body')[0].style.background='rgba(0,0,0,0)'") { _, _  in
            self.webView.evaluateJavaScript(String(bytes: app_hideText.map{cramRange(user: $0)}, encoding: .utf8)!) { _, _ in
            }

            //: webView.backgroundColor = .clear
            webView.backgroundColor = .clear
            //: webView.scrollView.backgroundColor = .clear
            webView.scrollView.backgroundColor = .clear
            //: webView.scrollView.bounces = false
            webView.scrollView.bounces = false
            //: webView.isOpaque = false
            webView.isOpaque = false
        }
    }

    //: func openTourl(url: String) {
    func minimum(url: String) {
        //: if url.count > 0 {
        if url.count > 0 {
            //: SmartThen.share.func__pushToWebVC(urlStr: url)
            SmartThen.share.lifeStyle(urlStr: url)
        }
    }

    //: func openToOutUrl(url: String) {
    func gap(url: String) {
        //: UIApplication.shared.open(URL(string: url)!, options: [:], completionHandler: nil)
        UIApplication.shared.open(URL(string: url)!, options: [:], completionHandler: nil)
    }

    //: @objc private func closeDiscountsView() {
    @objc private func picStar() {
        //: if config.isDiscountsView {
        if config.isDiscountsView {
            //: var root = self.presentingViewController
            var root = self.presentingViewController
            //: while root != nil  {
            while root != nil {
                //: root?.dismiss(animated: true)
                root?.dismiss(animated: true)
                //: root = root?.presentingViewController
                root = root?.presentingViewController
            }
            //: NotificationCenter.default.removeObserver(self, name: VIDEOCALL_END_CLOSEDISCOUNTS_NOTIFICATION, object: nil)
            NotificationCenter.default.removeObserver(self, name: appEventUrl, object: nil)
        }
    }

    /// 亲密度说明
    //: @objc private func func__intimateAction() {
    @objc private func blindAction() {
        //: let view = TalkingIntimateInstructionsPopUpView.init(frame: CGRect(x: 0, y: 0, width: ScreenWidth, height: ScreenHeight))
        let view = AfterThen(frame: CGRect(x: 0, y: 0, width: main_assetUrl, height: kTopUrl))
        //: view.show()
        view.stop()
    }
}

//: extension TalkingWebViewController: WKNavigationDelegate, WKUIDelegate {
extension RecognizeViewController: WKNavigationDelegate, WKUIDelegate {
    //: func webView(_ webView: WKWebView, decidePolicyFor navigationAction: WKNavigationAction, decisionHandler: @escaping (WKNavigationActionPolicy) -> Void) {
    func webView(_: WKWebView, decidePolicyFor _: WKNavigationAction, decisionHandler: @escaping (WKNavigationActionPolicy) -> Void) {
        //: decisionHandler(.allow)
        decisionHandler(.allow)
    }

    //: func webView(_ webView: WKWebView, didStartProvisionalNavigation navigation: WKNavigation!) {
    func webView(_: WKWebView, didStartProvisionalNavigation _: WKNavigation!) {
        //: UIApplication.shared.isNetworkActivityIndicatorVisible = true
        UIApplication.shared.isNetworkActivityIndicatorVisible = true
        //: if config.showLoadingView {
        if config.showLoadingView {
            //: ProgressHUD.show(superView: view)
            StrangerProgressHUD.superCollection(superView: view)
        }
        //: if config.showCloseBtn {
        if config.showCloseBtn {
            //: view.bringSubviewToFront(closeBtn)
            view.bringSubviewToFront(closeBtn)
        }
    }

    //: func webView(_ webView: WKWebView, didFinish navigation: WKNavigation!) {
    func webView(_: WKWebView, didFinish _: WKNavigation!) {
        //: UIApplication.shared.isNetworkActivityIndicatorVisible = false
        UIApplication.shared.isNetworkActivityIndicatorVisible = false
        //: if config.showCloseBtn && config.showLoadingView {
        if config.showCloseBtn && config.showLoadingView {
            //: func__hideLoadingBgViewAndCloseBtn(sucess: true)
            immediateIn(sucess: true)
        }
        //: if openBlock != nil {
        if openBlock != nil {
            //: openBlock(true)
            openBlock(true)
        }
        //: func__initLeftButton()
        naturalEventButton()
        //: func__clearJSBgColor()
        isSkin()
    }

    //: func webView(_ webView: WKWebView, didFailProvisionalNavigation navigation: WKNavigation!, withError error: Error) {
    func webView(_: WKWebView, didFailProvisionalNavigation _: WKNavigation!, withError _: Error) {
        //: UIApplication.shared.isNetworkActivityIndicatorVisible = false
        UIApplication.shared.isNetworkActivityIndicatorVisible = false
        //: if config.showCloseBtn && config.showLoadingView {
        if config.showCloseBtn && config.showLoadingView {
            //: func__hideLoadingBgViewAndCloseBtn(sucess: false)
            immediateIn(sucess: false)
        }

        //: if openBlock != nil {
        if openBlock != nil {
            //: openBlock(false)
            openBlock(false)
        }
    }

    //: func webView(_ webView: WKWebView, didFail navigation: WKNavigation!, withError error: Error) {
    func webView(_: WKWebView, didFail _: WKNavigation!, withError _: Error) {
        //: if config.showCloseBtn && config.showLoadingView {
        if config.showCloseBtn && config.showLoadingView {
            //: func__hideLoadingBgViewAndCloseBtn(sucess: false)
            immediateIn(sucess: false)
        }
        //: if openBlock != nil {
        if openBlock != nil {
            //: openBlock(false)
            openBlock(false)
        }
    }

    //: func webView(_ webView: WKWebView, didReceive challenge: URLAuthenticationChallenge, completionHandler: @escaping (URLSession.AuthChallengeDisposition, URLCredential?) -> Void) {
    func webView(_: WKWebView, didReceive challenge: URLAuthenticationChallenge, completionHandler: @escaping (URLSession.AuthChallengeDisposition, URLCredential?) -> Void) {
        //: DispatchQueue.global().async {
        DispatchQueue.global().async {
            //: if challenge.protectionSpace.authenticationMethod == NSURLAuthenticationMethodServerTrust {
            if challenge.protectionSpace.authenticationMethod == NSURLAuthenticationMethodServerTrust {
                //: if challenge.previousFailureCount == 0 {
                if challenge.previousFailureCount == 0 {
                    //: let credential = URLCredential.init(trust: challenge.protectionSpace.serverTrust!)
                    let credential = URLCredential(trust: challenge.protectionSpace.serverTrust!)
                    //: completionHandler(.useCredential, credential)
                    completionHandler(.useCredential, credential)
                    //: } else {
                } else {
                    //: completionHandler(.cancelAuthenticationChallenge, nil)
                    completionHandler(.cancelAuthenticationChallenge, nil)
                }
                //: } else {
            } else {
                //: completionHandler(.cancelAuthenticationChallenge, nil)
                completionHandler(.cancelAuthenticationChallenge, nil)
            }
        }
    }

//    func webView(_ webView: WKWebView, runJavaScriptAlertPanelWithMessage message: String, initiatedByFrame frame: WKFrameInfo, completionHandler: @escaping () -> Void) {
//        let alertController = UIAlertController.init(title: "Alert", message: message, preferredStyle: .alert)
//        let action =  UIAlertAction.init(title: "OK".localized, style: .default) { _ in
//            completionHandler()
//        }
//        alertController.addAction(action)
//        present(alertController, animated: true)
//    }

    //: func webView(_ webView: WKWebView, runJavaScriptConfirmPanelWithMessage message: String, initiatedByFrame frame: WKFrameInfo, completionHandler: @escaping (Bool) -> Void) {
    func webView(_: WKWebView, runJavaScriptConfirmPanelWithMessage message: String, initiatedByFrame _: WKFrameInfo, completionHandler: @escaping (Bool) -> Void) {
        //: let alertController = UIAlertController.init(title: "Alert", message: message, preferredStyle: .alert)
        let alertController = UIAlertController(title: (String(data_safetyStr.prefix(5))), message: message, preferredStyle: .alert)
        //: let action =  UIAlertAction.init(title: "OK".localized, style: .default) { _ in
        let action = UIAlertAction(title: "OK".localized, style: .default) { _ in
            //: completionHandler(true)
            completionHandler(true)
        }
        //: let cancleAction =  UIAlertAction.init(title: "Cancle", style: .cancel) { _ in
        let cancleAction = UIAlertAction(title: (String(constBounceExploreName)), style: .cancel) { _ in
            //: completionHandler(false)
            completionHandler(false)
        }
        //: alertController.addAction(cancleAction)
        alertController.addAction(cancleAction)
        //: alertController.addAction(action)
        alertController.addAction(action)

        //: present(alertController, animated: true)
        present(alertController, animated: true)
    }

    //: func webView(_ webView: WKWebView, runJavaScriptTextInputPanelWithPrompt prompt: String, defaultText: String?, initiatedByFrame frame: WKFrameInfo, completionHandler: @escaping (String?) -> Void) {
    func webView(_: WKWebView, runJavaScriptTextInputPanelWithPrompt prompt: String, defaultText: String?, initiatedByFrame _: WKFrameInfo, completionHandler: @escaping (String?) -> Void) {
        //: let alertController = UIAlertController.init(title: prompt, message: "", preferredStyle: .alert)
        let alertController = UIAlertController(title: prompt, message: "", preferredStyle: .alert)
        //: alertController.addTextField { textField in
        alertController.addTextField { textField in
            //: textField.text = defaultText
            textField.text = defaultText
        }
        //: let action =  UIAlertAction.init(title: "Done".localized, style: .default) { _ in
        let action = UIAlertAction(title: (String(noti_thinExplainFormat.prefix(4))).localized, style: .default) { _ in
            //: completionHandler(alertController.textFields![0].text)
            completionHandler(alertController.textFields![0].text)
        }
        //: alertController.addAction(action)
        alertController.addAction(action)

        //: present(alertController, animated: true)
        present(alertController, animated: true)
    }
}

//: extension TalkingWebViewController: WKScriptMessageHandler {
extension RecognizeViewController: WKScriptMessageHandler {
    //: func userContentController(_ userContentController: WKUserContentController, didReceive message: WKScriptMessage) {
    func userContentController(_: WKUserContentController, didReceive message: WKScriptMessage) {
        //: printLog(message: "js call method name = \(message.name), message = \(message.body)")
        printLog(message: (String(appExactlyValue.prefix(8)) + String(userScheduleId) + String(user_birthdayWhatFormat.prefix(4)) + "name = ") + "\(message.name)" + (String(mainGlobUrl.suffix(7)) + String(notiCarrierName)) + "\(message.body)")
        //: DispatchQueue.main.async {
        DispatchQueue.main.async {
            //: self.HandleScriptMessage(message)
            self.limitedMessage(message)
            //: if self.handleJSBlock != nil {
            if self.handleJSBlock != nil {
                //: self.handleJSBlock(message.name)
                self.handleJSBlock(message.name)
            }
        }
    }

    //: func func__addBridgeMethod() {
    func outsideBurn() {
        //: let ucController = webView.configuration.userContentController
        let ucController = webView.configuration.userContentController
        //: for method in WebMethodType.allCases {
        for method in EveryContentMagnitudeerpolation.allCases {
            //: ucController.add(self, name: method.rawValue)
            ucController.add(self, name: method.rawValue)
        }
    }

    //: func func__removeBridgeMethod() {
    func commonTotal() {
        //: let ucController = webView.configuration.userContentController
        let ucController = webView.configuration.userContentController
        //: for method in WebMethodType.allCases {
        for method in EveryContentMagnitudeerpolation.allCases {
            //: ucController.removeScriptMessageHandler(forName: method.rawValue)
            ucController.removeScriptMessageHandler(forName: method.rawValue)
        }
    }
}

//: extension TalkingWebViewController {
extension RecognizeViewController {
    //: func func__addUserScript() {
    func woundMp() {
        //: var htmlDomain: String = FacultyReactiveCompatible.share.appConfigMode.urlH5Domain
        var htmlDomain: String = FacultyReactiveCompatible.share.appConfigMode.urlH5Domain

        //: if htmlDomain.hasPrefix("http://") {
        if htmlDomain.hasPrefix((String(app_questionTitle) + String(noti_bitKey.prefix(6)))) {
            //: htmlDomain = htmlDomain.replacingOccurrences(of: "http://", with: "")
            htmlDomain = htmlDomain.replacingOccurrences(of: (String(app_questionTitle) + String(noti_bitKey.prefix(6))), with: "")
        }
        //: if htmlDomain.hasPrefix("https://") {
        if htmlDomain.hasPrefix((String(main_tireStr))) {
            //: htmlDomain = htmlDomain.replacingOccurrences(of: "https://", with: "")
            htmlDomain = htmlDomain.replacingOccurrences(of: (String(main_tireStr)), with: "")
        }
        //: var cookiesStr = ""
        var cookiesStr = ""
        //: let tempUid: String = FacultyReactiveCompatible.share.loginUserMode.userID
        let tempUid: String = FacultyReactiveCompatible.share.loginUserMode.userID
        //: let tempSessionID: String = FacultyReactiveCompatible.share.loginSessionId
        let tempSessionID: String = FacultyReactiveCompatible.share.loginSessionId
        //: if #available(iOS 11.0, *) {
        if #available(iOS 11.0, *) {
            //: cookiesStr.append("var cookieNames = document.cookie.split('; ').map(function(cookie) { return cookie.split('=')[0] } );\n")
            cookiesStr.append(String(bytes: showInsertAttributeName.map{clipMessage(good: $0)}, encoding: .utf8)!)
            //: var isfirst = true
            var isfirst = true
            //: if tempUid.count > 0 {
            if tempUid.count > 0 {
                //: isfirst = false
                isfirst = false
                //: let tempStr = "uid=\(tempUid);domain=\(htmlDomain);path=/"
                let tempStr = (String(k_pureCapableUrl.suffix(4))) + "\(tempUid)" + (String(app_hoodStr.suffix(7)) + userNearbyName.replacingOccurrences(of: "snap", with: "=")) + "\(htmlDomain)" + (String(noti_multiMessage))
                //: cookiesStr.append("document.cookie='\(tempStr)';\n")
                cookiesStr.append((notiFarStr.lowercased() + "ent.co" + app_backyardContent) + "\(tempStr)';\n")
            }
            //: if tempSessionID.count > 0 {
            if tempSessionID.count > 0 {
                //: let tempStr = "PHPSESSID=\(tempSessionID);domin=\(htmlDomain);path=/"
                let tempStr = (String(appVidMessage)) + "\(tempSessionID)" + (String(appHiddenMsg.prefix(7))) + "\(htmlDomain)" + (String(noti_multiMessage))
                //: if isfirst {
                if isfirst {
                    //: cookiesStr.append("document.cookie='\(tempStr)';\n")
                    cookiesStr.append((notiFarStr.lowercased() + "ent.co" + app_backyardContent) + "\(tempStr)';\n")
                    //: } else {
                } else {
                    //: cookiesStr.append("if (cookieNames.indexOf('PHPSESSID') == -1) { document.cookie='\(tempStr)'; };\n")
                    cookiesStr.append(String(bytes: data_scopeFormat.reversed(), encoding: .utf8)! + "\(tempStr)'; };\n")
                }
            }
            //: } else {
        } else {
            //: if tempUid.count > 0 {
            if tempUid.count > 0 {
                //: let tempStr = "uid=\(tempUid);domain=\(htmlDomain);path=/"
                let tempStr = (String(k_pureCapableUrl.suffix(4))) + "\(tempUid)" + (String(app_hoodStr.suffix(7)) + userNearbyName.replacingOccurrences(of: "snap", with: "=")) + "\(htmlDomain)" + (String(noti_multiMessage))
                //: cookiesStr.append("document.cookie='\(tempStr)'")
                cookiesStr.append((notiFarStr.lowercased() + "ent.co" + app_backyardContent) + "\(tempStr)'")
            }
            //: if tempSessionID.count > 0 {
            if tempSessionID.count > 0 {
                //: let tempStr = "uid=\(tempSessionID);domain=\(htmlDomain);path=/"
                let tempStr = (String(k_pureCapableUrl.suffix(4))) + "\(tempSessionID)" + (String(app_hoodStr.suffix(7)) + userNearbyName.replacingOccurrences(of: "snap", with: "=")) + "\(htmlDomain)" + (String(noti_multiMessage))
                //: cookiesStr.append("document.cookie='\(tempStr)'")
                cookiesStr.append((notiFarStr.lowercased() + "ent.co" + app_backyardContent) + "\(tempStr)'")
            }
        }

        //: if cookiesStr.count > 0 {
        if cookiesStr.count > 0 {
            //: let userScript = WKUserScript.init(source: cookiesStr, injectionTime: .atDocumentStart, forMainFrameOnly: false)
            let userScript = WKUserScript(source: cookiesStr, injectionTime: .atDocumentStart, forMainFrameOnly: false)
            //: webView.configuration.userContentController.addUserScript(userScript)
            webView.configuration.userContentController.addUserScript(userScript)
        }
    }

    //: func func__checkWebCookiesBeforeRequest(webConfig: WKWebViewConfiguration) {
    func plain(webConfig: WKWebViewConfiguration) {
        // iOS11+ 判断当前的WKWebViewCookie
        //: if #available(iOS 11.0, *) {
        if #available(iOS 11.0, *) {
            //: let cookieStore = webConfig.websiteDataStore.httpCookieStore
            let cookieStore = webConfig.websiteDataStore.httpCookieStore
            //: let htmlDomain: String = FacultyReactiveCompatible.share.appConfigMode.urlH5Domain
            let htmlDomain: String = FacultyReactiveCompatible.share.appConfigMode.urlH5Domain
            //: let domain: String = URL.init(string: htmlDomain)?.host ?? ""
            let domain: String = URL(string: htmlDomain)?.host ?? ""
            //: printLog(message: "h5Domain = \(htmlDomain)")
            printLog(message: (String(mainTranslationEditorFormat) + String(showClickMessage.prefix(7))) + "\(htmlDomain)")
            //: let userID: String = FacultyReactiveCompatible.share.loginUserMode.userID
            let userID: String = FacultyReactiveCompatible.share.loginUserMode.userID
            //: cookieStore.getAllCookies { [weak self] cookies in
            cookieStore.getAllCookies { [weak self] cookies in
                //: guard let self = self else { return }
                guard let self = self else { return }
                //: printLog(message: "getAllCookies = \(cookies)")
                printLog(message: (String(dataRangeKey.suffix(6)) + "Cook" + String(show_feedStr.prefix(5)) + String(notiRocketFormat)) + "\(cookies)")
                //: var hasUidCookie = false
                var hasUidCookie = false
                //: var hasSessionCookie = false
                var hasSessionCookie = false
                //: for cookie in cookies {
                for cookie in cookies {
                    //: if domain.contains(cookie.domain) {
                    if domain.contains(cookie.domain) {
                        //: if cookie.name == "PHPSESSID" {
                        if cookie.name == (user_taskPerformStr.replacingOccurrences(of: "cloud", with: "SE") + "SSID") {
                            //: hasSessionCookie = true
                            hasSessionCookie = true
                            //: continue
                            continue
                        }
                        //: if cookie.name == "uid" && cookie.value == userID {
                        if cookie.name == (user_propertyMessage.replacingOccurrences(of: "refuse", with: "id")), cookie.value == userID {
                            //: hasUidCookie = true
                            hasUidCookie = true
                            //: continue
                            continue
                        }
                        //: if hasSessionCookie && hasUidCookie {
                        if hasSessionCookie, hasUidCookie {
                            //: break
                            break
                        }
                    }
                }
                /// 服务器有返回
                //: if hasUidCookie && hasSessionCookie {
                if hasUidCookie, hasSessionCookie {
                    //: self.requestReady = true
                    self.requestReady = true
                    //: self.func__beginStartRequest()
                    self.policyIn()
                    //: } else {
                } else {
                    //: self.func__insertWebCookies(webConfig: webConfig, hasUid: hasUidCookie, hasSession: hasSessionCookie)
                    self.anyIcySession(webConfig: webConfig, hasUid: hasUidCookie, hasSession: hasSessionCookie)
                }
            }
        }
    }

    //: func func__insertWebCookies(webConfig: WKWebViewConfiguration, hasUid: Bool, hasSession: Bool ) {
    func anyIcySession(webConfig: WKWebViewConfiguration, hasUid: Bool, hasSession: Bool) {
        //: if #available(iOS 11.0, *) {
        if #available(iOS 11.0, *) {
            //: DispatchQueue.main.async {
            DispatchQueue.main.async {
                //: let cookieStore = webConfig.websiteDataStore.httpCookieStore
                let cookieStore = webConfig.websiteDataStore.httpCookieStore
                //: let htmlDomain: String = FacultyReactiveCompatible.share.appConfigMode.urlH5Domain
                let htmlDomain: String = FacultyReactiveCompatible.share.appConfigMode.urlH5Domain
                //: let h5Domain: String = URL.init(string: htmlDomain)?.host ?? ""
                let h5Domain: String = URL(string: htmlDomain)?.host ?? ""
                //: printLog(message: "h5Domain = \(htmlDomain)")
                printLog(message: (String(mainTranslationEditorFormat) + String(showClickMessage.prefix(7))) + "\(htmlDomain)")
                //: let appCookies: [HTTPCookie] = HTTPCookieStorage.shared.cookies(for: URL.init(string: TalkingRequestAddrTool.share.serverUrlStr)!)!
                let appCookies: [HTTPCookie] = HTTPCookieStorage.shared.cookies(for: URL(string: RocketRecognizeAddrTool.share.serverUrlStr)!)!
                //: var addCookies: [HTTPCookie] = []
                var addCookies: [HTTPCookie] = []

                //: printLog(message: "appCookies = \(appCookies)")
                printLog(message: (String(k_listenerName) + String(main_worldRemoteName.suffix(7))) + "\(appCookies)")

                //: for cookies in appCookies {
                for cookies in appCookies {
                    //: if cookies.name.uppercased() == "UID" && !hasUid {
                    if cookies.name.uppercased() == (String(kReportedFormat)), !hasUid {
                        //: var temPro: Dictionary<HTTPCookiePropertyKey, Any> = cookies.properties!
                        var temPro: [HTTPCookiePropertyKey: Any] = cookies.properties!
                        //: temPro[HTTPCookiePropertyKey.name] = "uid"
                        temPro[HTTPCookiePropertyKey.name] = (user_propertyMessage.replacingOccurrences(of: "refuse", with: "id"))
                        //: temPro[HTTPCookiePropertyKey.domain] = h5Domain
                        temPro[HTTPCookiePropertyKey.domain] = h5Domain
                        //: let temCookie: HTTPCookie = HTTPCookie.init(properties: temPro)!
                        let temCookie: HTTPCookie = .init(properties: temPro)!
                        //: addCookies.append(temCookie)
                        addCookies.append(temCookie)
                    }
                    //: if cookies.name.uppercased() == "PHPSESSID" && !hasSession {
                    if cookies.name.uppercased() == (user_taskPerformStr.replacingOccurrences(of: "cloud", with: "SE") + "SSID"), !hasSession {
                        //: var temPro: Dictionary<HTTPCookiePropertyKey, Any> = cookies.properties!
                        var temPro: [HTTPCookiePropertyKey: Any] = cookies.properties!
                        //: temPro[HTTPCookiePropertyKey.name] = "PHPSESSID"
                        temPro[HTTPCookiePropertyKey.name] = (user_taskPerformStr.replacingOccurrences(of: "cloud", with: "SE") + "SSID")
                        //: temPro[HTTPCookiePropertyKey.domain] = h5Domain
                        temPro[HTTPCookiePropertyKey.domain] = h5Domain
                        //: let temCookie: HTTPCookie = HTTPCookie.init(properties: temPro)!
                        let temCookie: HTTPCookie = .init(properties: temPro)!
                        //: addCookies.append(temCookie)
                        addCookies.append(temCookie)
                    }
                }
                //: for cookies in addCookies {
                for cookies in addCookies {
                    //: cookieStore.setCookie(cookies)
                    cookieStore.setCookie(cookies)
                }
                //: self.requestReady = true
                self.requestReady = true
                //: self.func__beginStartRequest()
                self.policyIn()
            }
        }
    }

    //: func func__addcustomUserAgent() {
    func incentiveOption() {
        //: self.webView.evaluateJavaScript("navigator.userAgent") { [weak self] (result, err) in
        self.webView.evaluateJavaScript((data_fragmentUrl.replacingOccurrences(of: "burn", with: "vi") + "gator" + String(show_paraData))) { [weak self] result, _ in
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: printLog(message: "navigator.userAgent")
            printLog(message: (data_fragmentUrl.replacingOccurrences(of: "burn", with: "vi") + "gator" + String(show_paraData)))
            //: var userAgent = result as? String ?? ""
            var userAgent = result as? String ?? ""
            //: if !userAgent.contains("xunwu_iphone") {
            if !userAgent.contains((mainComplexFormat.replacingOccurrences(of: "memory", with: "xu") + String(constBodyStr))) {
                //: let str = getUserAgentString()
                let str = someoneName()
                //: userAgent.append(" \(str)")
                userAgent.append(" \(str)")
                //: DispatchQueue.main.async {
                DispatchQueue.main.async {
                    //: self.webView.customUserAgent = userAgent
                    self.webView.customUserAgent = userAgent
                }
            }
            //: UIDevice.getDeviceDeviceIdentifier { deviceId in
            UIDevice.identifierWork { deviceId in
                //: guard deviceId.count > 0 else { return }
                guard deviceId.count > 0 else { return }
                //: let str = " xunwu_device_id/\(deviceId)"
                let str = (String(user_tickId.prefix(5)) + String(main_glassStr)) + "\(deviceId)"
                //: if !userAgent.contains(str) {
                if !userAgent.contains(str) {
                    //: userAgent.append(" \(str)")
                    userAgent.append(" \(str)")
                    //: DispatchQueue.main.async {
                    DispatchQueue.main.async {
                        //: self.webView.customUserAgent = userAgent
                        self.webView.customUserAgent = userAgent
                    }
                }
            }
        }
    }

    //: func getUserAgentString() -> String {
    func someoneName() -> String {
        //: return "xunwu_iphone xunwu_version/\(AppNetVersion) xunwu_package/\(PackageID) xunwu_bundleId/\(AppBundle)  webkit/1"
        return (noti_ignoreValue.replacingOccurrences(of: "flash", with: "n") + String(user_earlyUrl.suffix(6)) + "one " + app_ratingName.lowercased() + "u_ver" + String(show_innerUrl.prefix(5))) + "\(data_serviceFormat)" + (String(userSpringKey) + String(appFullyBlueName.suffix(7))) + "\(showViewValue)" + (String(data_burnRegainKey) + const_someoneKey.replacingOccurrences(of: "just", with: "n") + String(k_stripPath)) + "\(const_onMsg)" + (String(user_needFormat) + String(constSideMeltKey.suffix(5)))
    }
}
