
//: Declare String Begin

/*: "init(coder:) has not been implemented" :*/
fileprivate let main_effectiveName:[UInt8] = [0x64,0x65,0x74,0x6e,0x65,0x6d,0x65,0x6c,0x70,0x6d,0x69,0x20,0x6e,0x65,0x65,0x62,0x20,0x74,0x6f,0x6e,0x20,0x73,0x61,0x68,0x20,0x29,0x3a,0x72,0x65,0x64,0x6f,0x63,0x28,0x74,0x69,0x6e,0x69]

/*: "#128CFF" :*/
fileprivate let user_enterText:String = "#128CFForientation she bit phone"

/*: "Feedback" :*/
fileprivate let k_neverName:[Character] = ["F","e"]
fileprivate let show_enoughTitle:String = "edbdialogk"

/*: "#8370FF" :*/
fileprivate let show_primaryPath:String = "quit recover population item slide#8370FF"

/*: "Quick Greeting" :*/
fileprivate let userHouseMessage:[Character] = ["Q","u","i","c","k"," ","G"]
fileprivate let const_temporaryFormat:String = "reetinwork"

/*: "Beauty Photos" :*/
fileprivate let showCrushData:[Character] = ["B","e","a","u","t","y"," ","P"]
fileprivate let app_cornerId:[Character] = ["h","o","t","o","s"]

/*: "url" :*/
fileprivate let data_littleSpanId:String = "URL"

/*: "unlockGift" :*/
fileprivate let showSmallData:String = "unlockGmaker command thumb replacement"
fileprivate let appAppropriateGoingPath:[Character] = ["i","f","t"]

/*: "isBurn" :*/
fileprivate let data_commonText:[UInt8] = [0x6e,0x72,0x75,0x42,0x73,0x69]

/*: "giftId" :*/
fileprivate let userPetitionTitle:[Character] = ["g","i","f","t","I","d"]

/*: "rYMsgType" :*/
fileprivate let main_eventTagTitle:String = "rYMsgTypeshirt manage"

/*: "GJ:CallCustom" :*/
fileprivate let app_loadDimensionUrl:String = "GJ:Clight dynamics"

/*: "changeDirection" :*/
fileprivate let user_termsName:[Character] = ["c","h","a","n","g"]
fileprivate let main_collectValue:[Character] = ["e"]
fileprivate let k_scenarioKey:String = "Directiongray representation ask"

/*: "extra" :*/
fileprivate let noti_yearFormat:[Character] = ["e","x","t","r","a"]

/*: "msgInfo" :*/
fileprivate let dataReadMsg:String = "replacement bot effective template detailedmsgInfo"

/*: "toUid" :*/
fileprivate let show_occurTitle:String = "presentation reading shake till pulltoUid"

/*: "wantGift" :*/
fileprivate let data_bringName:[Character] = ["w","a","n","t","G","i","f","t"]

/*: "jumpKey" :*/
fileprivate let main_gestureData:String = "input heavy small childjumpKey"

/*: "logType" :*/
fileprivate let show_activityName:String = "logTypemilitary object cloud"

/*: "content" :*/
fileprivate let dataDefineValue:[UInt8] = [0xbb,0xc7,0xc6,0xcc,0xbd,0xc6,0xcc]

fileprivate func fromMystery(desk num: UInt8) -> UInt8 {
    let value = Int(num) + 168
    if value > 255 {
        return UInt8(value - 256)
    } else {
        return UInt8(value)
    }
}

/*: "msgType" :*/
fileprivate let noti_filterPath:[UInt8] = [0xc6,0xd8,0xcc,0xff,0xd2,0xdb,0xce]

private func shoreCounteraction(bright num: UInt8) -> UInt8 {
    return num ^ 171
}

/*: "txt" :*/
fileprivate let mainRetirementKey:String = "tincident"

/*: "message" :*/
fileprivate let app_packageValue:[UInt8] = [0x3,0xb,0x1d,0x1d,0xf,0x9,0xb]

private func absExcept(arrow num: UInt8) -> UInt8 {
    return num ^ 110
}

/*: "replayId" :*/
fileprivate let noti_normalMsg:[UInt8] = [0x4d,0x5a,0x4f,0x53,0x5e,0x46,0x76,0x5b]

private func hireService(portrait num: UInt8) -> UInt8 {
    return num ^ 63
}

/*: "type" :*/
fileprivate let kExitStr:String = "ttimingpe"

/*: "TxtMsg" :*/
fileprivate let mainDateFormat:[Character] = ["T","x","t","M","s","g"]

/*: "params" :*/
fileprivate let showAbsoluteStr:[UInt8] = [0x73,0x6d,0x61,0x72,0x61,0x70]

/*: "MultiQuickMsg" :*/
fileprivate let app_healthyPath:String = "under score seat enter sinceMultiQ"
fileprivate let noti_noticeFireCellMsg:String = "beam called calleduickMsg"

/*: "QuickMsg" :*/
fileprivate let k_spaceText:String = "QuickMbook bright street communicate"
fileprivate let const_pointData:[Character] = ["s","g"]

/*: "txtId" :*/
fileprivate let notiStillGivingFormat:String = "txtIdmemory block search thread"

/*: "Voice hello download failed. Procedure" :*/
fileprivate let noti_weightUrl:[UInt8] = [0xc4,0xfd,0xfb,0xf1,0xf7,0xb2,0xfa,0xf7,0xfe,0xfe,0xfd,0xb2,0xf6,0xfd,0xe5,0xfc,0xfe,0xfd,0xf3,0xf6,0xb2,0xf4,0xf3,0xfb,0xfe,0xf7,0xf6,0xbc,0xb2,0xc2,0xe0,0xfd,0xf1,0xf7,0xf6,0xe7,0xe0,0xf7]

private func bondRating(mean num: UInt8) -> UInt8 {
    return num ^ 146
}

/*: "length" :*/
fileprivate let userLackName:[Character] = ["l","e","n","g","t","h"]

/*: "audio" :*/
fileprivate let kVisualId:[Character] = ["a","u","d","i","o"]

/*: "audioId" :*/
fileprivate let main_belowUrl:String = "AU"
fileprivate let show_agoMessage:[Character] = ["d","i","o","I","d"]

/*: "audioLength" :*/
fileprivate let constFullSegmentFadeContent:[UInt8] = [0xad,0xb9,0xa8,0xa5,0xa3,0x80,0xa9,0xa2,0xab,0xb8,0xa4]

private func challengeEnd(adopt num: UInt8) -> UInt8 {
    return num ^ 204
}

/*: "audioSandbox" :*/
fileprivate let noti_figureBowlFormat:[Character] = ["a","u","d","i","o","S","a"]
fileprivate let noti_creationStr:[Character] = ["n","d","b","o","x"]

/*: "senderId" :*/
fileprivate let appDimensionFormat:[UInt8] = [0xc,0x1a,0x11,0x1b,0x1a,0xd,0x36,0x1b]

private func freshStart(mention num: UInt8) -> UInt8 {
    return num ^ 127
}

/*: "AudioMsg" :*/
fileprivate let notiBowlFormat:[Character] = ["A","u","d","i","o"]
fileprivate let k_createDepictPath:String = "Msguntil bowl all channel"

/*: "Can't send blank message" :*/
fileprivate let main_taTitle:[Character] = ["C","a","n","\'","t"," ","s","e","n","d"," ","b","l","a","n","k"," ","m","e","s","s"]
fileprivate let dataTrackHolderContent:String = "AGE"

/*: "quoteMsgId" :*/
fileprivate let kChamberMessage:String = "quoteMoverall half country transform"

/*: "quoteMsgInfo" :*/
fileprivate let mainPastId:String = "clearote"

/*: "contentType" :*/
fileprivate let mainFastAliveName:[UInt8] = [0x7d,0x89,0x88,0x8e,0x7f,0x88,0x8e,0x6e,0x93,0x8a,0x7f]

fileprivate func recognizeDecision(arc num: UInt8) -> UInt8 {
    let value = Int(num) + 230
    if value > 255 {
        return UInt8(value - 256)
    } else {
        return UInt8(value)
    }
}

/*: "ImgMsg" :*/
fileprivate let appReloadKey:[Character] = ["I","m","g","M","s","g"]

/*: "img" :*/
fileprivate let user_equallyTitle:String = "IMG"

/*: "imageUri" :*/
fileprivate let showQuickKey:[UInt8] = [0x2a,0x2e,0x22,0x24,0x26,0x16,0x31,0x2a]

private func factorCalculationSeason(disable num: UInt8) -> UInt8 {
    return num ^ 67
}

/*: "customSend" :*/
fileprivate let kPlatformPath:[UInt8] = [0x3a,0x4c,0x4a,0x4b,0x46,0x44,0x2a,0x3c,0x45,0x3b]

fileprivate func availableTransfer(radical num: UInt8) -> UInt8 {
    let value = Int(num) - 215
    if value < 0 {
        return UInt8(value + 256)
    } else {
        return UInt8(value)
    }
}

/*: "pic" :*/
fileprivate let user_airKey:String = "pipopulation"

/*: "VideoMsg" :*/
fileprivate let k_fragmentName:String = "indigenous provideVideoM"
fileprivate let show_formationPath:[Character] = ["s","g"]

/*: "video" :*/
fileprivate let k_contextMoreFormat:String = "teemideo"

/*: "coverImg" :*/
fileprivate let app_cellValue:[UInt8] = [0xc7,0xd3,0xda,0xc9,0xd6,0xad,0xd1,0xcb]

fileprivate func inkProperty(ret num: UInt8) -> UInt8 {
    let value = Int(num) - 100
    if value < 0 {
        return UInt8(value + 256)
    } else {
        return UInt8(value)
    }
}

/*: "videoUrl" :*/
fileprivate let mainAllowPath:[UInt8] = [0x1b,0x4,0x9,0x8,0x2,0x38,0x1f,0x1]

private func artisticStyle(another num: UInt8) -> UInt8 {
    return num ^ 109
}

/*: "duration" :*/
fileprivate let user_foundValue:[UInt8] = [0x60,0x71,0x76,0x65,0x70,0x6d,0x6b,0x6a]

/*: "len" :*/
fileprivate let app_totalPath:[UInt8] = [0x52,0x5b,0x50]

/*: "Documents/User/voice/" :*/
fileprivate let k_commentMsg:String = "bad heap production not plainDocume"
fileprivate let k_effectiveData:String = "User/voifully back encouraging"
fileprivate let userHeartId:[Character] = ["c","e","/"]

/*: "Documents/User/record/" :*/
fileprivate let user_limitedMessage:String = "Docugreet remark ex formation blend"
fileprivate let data_assetWritingMsg:String = "s/Useskip professional script"
fileprivate let app_oddData:String = "disabled highlight tired/"

/*: "msgIncome" :*/
fileprivate let showExploreValue:[Character] = ["m","s","g","I","n","c","o","m"]
fileprivate let k_bowlId:String = "say"

/*: "messageType" :*/
fileprivate let dataRoundAbsolutelyUrl:String = "MESSAGE"
fileprivate let app_loseId:String = "Typerelieve extent"

/*: "WantGift" :*/
fileprivate let k_conTrackFormat:[Character] = ["W","a","n","t","G","i"]
fileprivate let userHeartValue:String = "fattention"

/*: _PrivateChatTipsTitle_ :*/
fileprivate let const_serverData:[Character] = ["_","P","r","i","v","a","t","e","C","h","a","t","T","i","p","s","T","i"]
fileprivate let appTipUserValue:[Character] = ["t","l","e","_"]

/*: "msgId" :*/
fileprivate let k_earnValue:String = "msgIdspan military background fast response"

/*: "totalMfCoin" :*/
fileprivate let kWealthyPath:String = "effectiveta"

/*: "attachSysMsg" :*/
fileprivate let noti_clickStr:String = "simultaneouslytac"
fileprivate let user_capValue:String = "tag formerMsg"

/*: "intimatePhoto" :*/
fileprivate let data_eastPath:[Character] = ["i","n","t","i","m","a","t","e","P","h"]
fileprivate let constMarketingId:[Character] = ["o","t","o"]

/*: "totalIntimate" :*/
fileprivate let app_componentScoreRestrictionData:[Character] = ["t","o","t","a","l","I"]
fileprivate let mainMenuTitle:String = "ntimatproblem"

/*: "uid" :*/
fileprivate let appTarData:[UInt8] = [0xb,0xff,0xfa]

fileprivate func floorCombine(productivity num: UInt8) -> UInt8 {
    let value = Int(num) - 150
    if value < 0 {
        return UInt8(value + 256)
    } else {
        return UInt8(value)
    }
}

/*: "intimate" :*/
fileprivate let mainLetterKey:[UInt8] = [0xd5,0xd2,0xc8,0xd5,0xd1,0xdd,0xc8,0xd9]

private func oldWould(female num: UInt8) -> UInt8 {
    return num ^ 188
}

/*: "Can't use the function due to your lack of intimacy level." :*/
fileprivate let const_referMessage:[UInt8] = [0xdf,0xfd,0xf2,0xbb,0xe8,0xbc,0xe9,0xef,0xf9,0xbc,0xe8,0xf4,0xf9,0xbc,0xfa,0xe9,0xf2,0xff,0xe8,0xf5,0xf3,0xf2,0xbc,0xf8,0xe9,0xf9,0xbc,0xe8,0xf3,0xbc,0xe5,0xf3,0xe9,0xee,0xbc,0xf0,0xfd,0xff,0xf7,0xbc,0xf3,0xfa,0xbc,0xf5,0xf2,0xe8,0xf5,0xf1,0xfd,0xff,0xe5,0xbc,0xf0,0xf9,0xea,0xf9,0xf0,0xb2]

private func userVery(mirror num: UInt8) -> UInt8 {
    return num ^ 156
}

/*: "giftNum" :*/
fileprivate let appFileRecordData:String = "giftNummulti host read"

/*: "pkgItemsetId" :*/
fileprivate let kTrustUrl:String = "pkgItrestore red"

/*: "from" :*/
fileprivate let k_natureValue:[Character] = ["f","r","o","m"]

/*: "wantGiftId" :*/
fileprivate let dataCountervalTitle:String = "wauntilt"
fileprivate let constExtraResolutionId:[Character] = ["G","i","f","t","I","d"]

/*: "WantGiftMsg" :*/
fileprivate let user_homeName:String = "process beauty upon possibleWantG"

/*: "gift" :*/
fileprivate let data_youUrl:String = "gibottom"

/*: "View info" :*/
fileprivate let k_shouldMessage:[Character] = ["V"]
fileprivate let noti_bowlRecoverValue:String = "iew should processor"

/*: "Notes" :*/
fileprivate let dataHelpQueryedName:String = "disable temporaryNotes"

/*: "Report" :*/
fileprivate let main_beautyTheMsg:[Character] = ["R","e","p","o","r","t"]

/*: "View read receipts?" :*/
fileprivate let k_bulletText:String = "calendar dynamicsView"
fileprivate let data_ofName:String = "reception forth theoryeipts?"

/*: "She" :*/
fileprivate let showAssistantMessage:String = "existing limit connection adoptShe"

/*: "%@ is using too low a version" :*/
fileprivate let const_serverSubjectHimValue:String = "direction routine%@ is"
fileprivate let noti_documentText:String = "oo lworld basic side"
fileprivate let show_partnerUrl:String = " verpo outside gallery meeting"

/*: "Shoot video at least 5 seconds" :*/
fileprivate let appMemberUrl:String = "Shoot force due custom"
fileprivate let constReferWedStr:String = "attribute relieve pipe superiorat le"
fileprivate let appElementGrayTitle:String = "collection increase line operationast 5 s"
fileprivate let dataDeleteTitle:String = "ECONDS"

/*: "Upload failed" :*/
fileprivate let const_bathroomUrl:String = "Upload score out"
fileprivate let kSwallowMessage:[Character] = ["f","a","i","l","e","d"]

/*: "播放状态: :*/
fileprivate let showPenKey:String = "播放状态:"

/*: "c2c_" :*/
fileprivate let const_sectionTitle:String = "march language stillc2c_"

/*: "msg" :*/
fileprivate let kSpotKey:String = "mselect"

/*: "tips" :*/
fileprivate let kExtentValue:String = "trefuses"

/*: "You can use the video chat feature to give %@ a call" :*/
fileprivate let show_stageUrl:[UInt8] = [0x95,0xa3,0xb9,0xec,0xaf,0xad,0xa2,0xec,0xb9,0xbf,0xa9,0xec,0xb8,0xa4,0xa9,0xec,0xba,0xa5,0xa8,0xa9,0xa3,0xec,0xaf,0xa4,0xad,0xb8,0xec,0xaa,0xa9,0xad,0xb8,0xb9,0xbe,0xa9,0xec,0xb8,0xa3,0xec,0xab,0xa5,0xba,0xa9,0xec,0xe9,0x8c,0xec,0xad,0xec,0xaf,0xad,0xa0,0xa0]

private func selfPropelledVehicle(direction num: UInt8) -> UInt8 {
    return num ^ 204
}

/*: "cellData" :*/
fileprivate let data_banMsg:[Character] = ["c"]
fileprivate let show_changeMessage:[Character] = ["e","l","l","D","a","t","a"]

/*: "F5F5F5" :*/
fileprivate let main_anotherName:[Character] = ["F","5","F","5","F"]
fileprivate let constPetitionMsg:String = "5"

/*: "System notification" :*/
fileprivate let dataRowModeMsg:[Character] = ["S","y","s","t","e"]
fileprivate let mainAgainName:String = "tap pendingm not"
fileprivate let k_guessMsg:String = "atiago"

//: Declare String End

// __DEBUG__
// __CLOSE_PRINT__
//
//  BorderInsularViewDelegate.swift
//  AbroadTalking
//
//  Created by Hemming on 2022/9/9.
//

//: import AVFAudio
import AVFAudio
//: import Photos
import Photos
//: import UIKit
import UIKit

/// 私聊入口枚举
//: enum PrivateChatIsFromEnum {
enum ArrayAdvancedFromEnum {
    //: case Normal
    case Normal // 默认入口
    //: case LiveRoom
    case LiveRoom // 来自直播间（半屏展示）
}

//: class TalkingPrivateChatController: TalkingBaseViewController {
class BorderInsularViewDelegate: SafetyFactorViewController {
    //: static var replyLatestTimes: Date = Date(timeIntervalSince1970: 0)
    static var replyLatestTimes: Date = .init(timeIntervalSince1970: 0) // 最新回复时间
    //: var needShowRating = false
    var needShowRating = false // 是否展示app store评分
    //: var quickGreetingTextDict: [String: Any]?
    var quickGreetingTextDict: [String: Any]? // 一键打招呼文本消息
    //: var quickGreetingAudioDict: [String: Any]?
    var quickGreetingAudioDict: [String: Any]? // 一键打招呼语音消息
    //: private let disposeBag = DisposeBag()
    private let disposeBag = DisposeBag()
    //: private var conversationType: AboveCellPreviewType = .private
    private var conversationType: AboveCellPreviewType = .private
    //: private var userWrap = ConsumerPenHandyJSON()
    private var userWrap = ConsumerPenHandyJSON() // chatInfo模型
    //: private var isFrom = PrivateChatIsFromEnum.Normal
    private var isFrom = ArrayAdvancedFromEnum.Normal // 私聊入口

    //: private var isShowIntimateView = false
    private var isShowIntimateView = false // 顶部是否已经展示了亲密度头像
    //: var QuickEndBlock: ((_ isSuccess: Bool, _ errCode: Int) -> Void)?
    var QuickEndBlock: ((_ isSuccess: Bool, _ errCode: Int) -> Void)? /// 1.7.8快速打招呼完成后

    //: init(chatID: String, isFrom: PrivateChatIsFromEnum = .Normal, userWrap: ConsumerPenHandyJSON = ConsumerPenHandyJSON()) {
    init(chatID: String, isFrom: ArrayAdvancedFromEnum = .Normal, userWrap: ConsumerPenHandyJSON = ConsumerPenHandyJSON()) {
        //: super.init(nibName: nil, bundle: nil)
        super.init(nibName: nil, bundle: nil)
        //: self.msgView.targetID = chatID
        self.msgView.targetID = chatID
        //: self.userWrap = userWrap
        self.userWrap = userWrap
        //: self.msgView.isRecharge = userWrap.isRecharge
        self.msgView.isRecharge = userWrap.isRecharge

        //: TalkingPrivateChatController.replyLatestTimes = Date(timeIntervalSince1970: 0)
        BorderInsularViewDelegate.replyLatestTimes = Date(timeIntervalSince1970: 0)
        //: TalkingPrivateChatManager.share.readReceiptDict[msgView.targetID] = self.userWrap.readReceiptTime
        SpanChatManager.share.readReceiptDict[msgView.targetID] = self.userWrap.readReceiptTime
        //: self.isFrom = isFrom
        self.isFrom = isFrom
        //: if msgView.targetID == AnimateUnaryAllocator.getXiaoMiID() {
        if msgView.targetID == AnimateUnaryAllocator.seethe() { // 系统消息
            //: conversationType = .system
            conversationType = .system
            //: hideNavi = false
            hideNavi = false
            //: } else if msgView.targetID == AnimateUnaryAllocator.getCustomerServiceID() {
        } else if msgView.targetID == AnimateUnaryAllocator.receiveRemove() { // 官方客服
            //: conversationType = .service
            conversationType = .service
            //: hideNavi = true
            hideNavi = true

            //: } else {
        } else {
            //: hideNavi = true
            hideNavi = true
        }
    }

    //: required init?(coder: NSCoder) {
    required init?(coder _: NSCoder) {
        //: fatalError("init(coder:) has not been implemented")
        fatalError(String(bytes: main_effectiveName.reversed(), encoding: .utf8)!)
    }

    //: override func loadView() {
    override func loadView() {
        //: if #available(iOS 13.2, *) {
        if #available(iOS 13.2, *) {
            //: if FacultyReactiveCompatible.share.appConfigMode.disableShootScreen {
            if FacultyReactiveCompatible.share.appConfigMode.disableShootScreen {
                //: super.loadView()
                super.loadView()
                //: } else {
            } else {
                //: let bgView = MAScreenShieldView(frame: CGRect(x: 0, y: 0, width: ScreenWidth, height: ScreenHeight))
                let bgView = ShieldView(frame: CGRect(x: 0, y: 0, width: main_assetUrl, height: kTopUrl))
                //: self.view = bgView
                self.view = bgView
            }
            //: } else {
        } else {
            //: super.loadView()
            super.loadView()
        }
    }

    //: override func viewDidLoad() {
    override func viewDidLoad() {
        //: super.viewDidLoad()
        super.viewDidLoad()
        //: half_showViewAnimation()
        timingEnablee()
        //: createUI()
        self.share()
        //: createUIConstraints()
        nameConstraints()
        //: bindInteraction()
        contact()
        //: addTapGestureRecognizer()
        mixIn()
        //: func__requestChatInfo()
        doingdResume()
        //: func__getFillDraftText()
        daily()
        //: TalkingVoiceMsgPlayManager.shared.addDaskManagerDelegate()
        LargeObjectProtocol.shared.doingAliveUsed()
        //: func__addVoiceNotification()
        tarNotification()
        //: addReadPeceiptNotification()
        counteractionNotification()

        //: if conversationType == .private {
        if conversationType == .private {
            //: initCachEffectData()
            taskMin()
            //: func__TipsNotification()
            detectByPress()
            //: if FacultyReactiveCompatible.share.loginUserMode.sex == Gender.male.rawValue {
            if FacultyReactiveCompatible.share.loginUserMode.sex == ATextLiteral.male.rawValue {
                // 处理男性侧快捷回复逻辑
                //: func__setReplyTipsView()
                beginView()
                // 判断问题回答消息遮罩是否展示
                //: refreshRechargeRequiredView()
                unitedNationsAgency()
            }
        }
    }

    //: override func viewDidAppear(_ animated: Bool) {
    override func viewDidAppear(_ animated: Bool) {
        //: super.viewDidAppear(animated)
        super.viewDidAppear(animated)
        //: func__addOrRrmoveObserverForMsgPlayer(isAdd: true)
        usa(isAdd: true)
    }

    //: override func viewWillDisappear(_ animated: Bool) {
    override func viewWillDisappear(_ animated: Bool) {
        //: super.viewWillDisappear(animated)
        super.viewWillDisappear(animated)
        //: view.endEditing(true)
        view.endEditing(true)
        //: func__addOrRrmoveObserverForMsgPlayer(isAdd: false)
        usa(isAdd: false)
        //: if conversationType == .private {
        if conversationType == .private {
            //: func__saveFillDraftText()
            bird()
        }
    }

    //: override func viewDidDisappear(_ animated: Bool) {
    override func viewDidDisappear(_ animated: Bool) {
        //: super.viewDidDisappear(animated)
        super.viewDidDisappear(animated)
        // 展示App Store评分
        //: if needShowRating {
        if needShowRating {
            //: AppStoreRatingManager.shared.requestQuickRating()
            JumpRatingManager.shared.across()
        }
    }

    //: deinit {
    deinit {
        //: NotificationCenter.default.removeObserver(self)
        NotificationCenter.default.removeObserver(self)
    }

    // MARK: - Lazy Load

    //: private lazy var contentView: UIView = {
    private lazy var contentView: UIView = {
        //: let v = UIView()
        let v = UIView()
        //: v.backgroundColor = .white
        v.backgroundColor = .white
        //: return v
        return v
        //: }()
    }()

    //: lazy var msgView: TUIMessageController = {
    lazy var msgView: ActivatedAlignmentLoader = {
        //: let msgVC = TUIMessageController.init()
        let msgVC = ActivatedAlignmentLoader()
        //: msgVC.delegate = self
        msgVC.delegate = self
        //: msgVC.tableView.register(TalkingChatTextMsgCell.self, forCellReuseIdentifier: TalkingChatTextMsgCell.className())
        msgVC.tableView.register(RowMessageCell.self, forCellReuseIdentifier: RowMessageCell.className())
        //: msgVC.tableView.register(TalkingChatImageMsgCell.self, forCellReuseIdentifier: TalkingChatImageMsgCell.className())
        msgVC.tableView.register(ImageViewCell.self, forCellReuseIdentifier: ImageViewCell.className())
        //: msgVC.tableView.register(TalkingChatVideoMsgCell.self, forCellReuseIdentifier: TalkingChatVideoMsgCell.className())
        msgVC.tableView.register(AfterMessageCell.self, forCellReuseIdentifier: AfterMessageCell.className())
        //: msgVC.tableView.register(TalkingChatGiftMsgCell.self, forCellReuseIdentifier: TalkingChatGiftMsgCell.className())
        msgVC.tableView.register(CollectionReactiveCompatible.self, forCellReuseIdentifier: CollectionReactiveCompatible.className())
        //: msgVC.tableView.register(TalkingChatAskForGiftMsgCell.self, forCellReuseIdentifier: TalkingChatAskForGiftMsgCell.className())
        msgVC.tableView.register(ChatMessageCell.self, forCellReuseIdentifier: ChatMessageCell.className())
        //: msgVC.tableView.register(TalkingChatTipsMsgCell.self, forCellReuseIdentifier: TalkingChatTipsMsgCell.className())
        msgVC.tableView.register(EventTaskVisibility.self, forCellReuseIdentifier: EventTaskVisibility.className())
        //: msgVC.tableView.register(TalkingChatMatchMsgCell.self, forCellReuseIdentifier: TalkingChatMatchMsgCell.className())
        msgVC.tableView.register(PeerSaverMsgCell.self, forCellReuseIdentifier: PeerSaverMsgCell.className())
        //: msgVC.tableView.register(TalkingChatAudioMsgCell.self, forCellReuseIdentifier: TalkingChatAudioMsgCell.className())
        msgVC.tableView.register(RetainViewCell.self, forCellReuseIdentifier: RetainViewCell.className())
        //: msgVC.tableView.register(TalkingChatSystemNotifJumpMsgCell.self, forCellReuseIdentifier: TalkingChatSystemNotifJumpMsgCell.className())
        msgVC.tableView.register(AudiencePersonMsgCell.self, forCellReuseIdentifier: AudiencePersonMsgCell.className())
        //: msgVC.tableView.register(TUISystemMessageCell.self, forCellReuseIdentifier: RatioViewCell.TSystemMessageCell_ReuseId)
        msgVC.tableView.register(IdentityMessageCell.self, forCellReuseIdentifier: RatioViewCell.TSystemMessageCell_ReuseId)
        //: msgVC.currentChatType = conversationType
        msgVC.currentChatType = conversationType

        //: return msgVC
        return msgVC
        //: }()
    }()

    //: lazy var bgImgeView: UIImageView = {
    lazy var bgImgeView: UIImageView = {
        //: let imgView = UIImageView.init()
        let imgView = UIImageView()
        //: imgView.backgroundColor = .clear
        imgView.backgroundColor = .clear
        //: imgView.contentMode = .scaleAspectFill
        imgView.contentMode = .scaleAspectFill
        //: imgView.clipsToBounds = true
        imgView.clipsToBounds = true
        //: return imgView
        return imgView
        //: }()
    }()

    //: lazy var coverView: UIVisualEffectView = {
    lazy var coverView: UIVisualEffectView = {
        //: let effect = UIBlurEffect.init(style: .dark)
        let effect = UIBlurEffect(style: .dark)
        //: let effectView = UIVisualEffectView.init(effect: effect)
        let effectView = UIVisualEffectView(effect: effect)
        //: effectView.alpha = 0.45
        effectView.alpha = 0.45
        //: return effectView
        return effectView
        //: }()
    }()

    //: lazy var bgView: UIView = {
    lazy var bgView: UIView = {
        //: let view = UIImageView.init()
        let view = UIImageView()
        //: view.backgroundColor = UIColor.black.withAlphaComponent(0.75)
        view.backgroundColor = UIColor.black.withAlphaComponent(0.75)
        //: return view
        return view
        //: }()
    }()

    //: private lazy var videoCallManager: TalkingCallMenuManager = {
    private lazy var videoCallManager: StretchObjectProtocol = {
        //: let m = TalkingCallMenuManager()
        let m = StretchObjectProtocol()
        //: m.uid = msgView.targetID
        m.uid = msgView.targetID
        //: m.onReqestCallErrorBlock = { [weak self] errNo in
        m.onReqestCallErrorBlock = { [weak self] errNo in
            //: guard let self = self,errNo == 101 else { return }
            guard let self = self, errNo == 101 else { return }
            //: self.msgInputView.setHidenFreeMsgTimesView()
            self.msgInputView.times()
        }
        //: return m
        return m
        //: }()
    }()

    //: lazy var msgInputView: TalkingChatInputView = {
    lazy var msgInputView: ConfirmView = {
        //: var type: InputViewType = .PrivateChat
        var type: GoViewType = .PrivateChat
        //: if conversationType == .service {
        if conversationType == .service {
            //: type = .CustomerChat
            type = .CustomerChat
            //: } else if self.isFrom == .LiveRoom {
        } else if self.isFrom == .LiveRoom {
            //: type = .LiveRoom
            type = .LiveRoom
        }

        //: let view = TalkingChatInputView(type: type)
        let view = ConfirmView(type: type)
        //: view.delegate = self
        view.delegate = self
        //: return view
        return view
        //: }()
    }()

    //: lazy var feedView: UIView = {
    lazy var feedView: UIView = {
        //: let view = UIView.init()
        let view = UIView()
        //: view.backgroundColor = .white
        view.backgroundColor = .white
        //: return view
        return view
        //: }()
    }()

    //: lazy var feedbackBtn: UIButton = {
    lazy var feedbackBtn: UIButton = {
        //: let btn = UIButton.init(type: UIButton.ButtonType.custom)
        let btn = UIButton(type: UIButton.ButtonType.custom)
        //: btn.backgroundColor = .white
        btn.backgroundColor = .white
        //: let attributes = [.foregroundColor: UIColor(hex: "#128CFF")!, .font: UIFont.pingfangFont(type: .Medium, fontSize: 16), .underlineStyle: 1] as [NSAttributedString.Key: Any]
        let attributes = [.foregroundColor: UIColor(hex: (String(user_enterText.prefix(7))))!, .font: UIFont.recipeDecision(type: .Medium, fontSize: 16), .underlineStyle: 1] as [NSAttributedString.Key: Any]
        //: let attributeString = NSAttributedString(string: "Feedback".localized, attributes: attributes)
        let attributeString = NSAttributedString(string: (String(k_neverName) + show_enoughTitle.replacingOccurrences(of: "dialog", with: "ac")).localized, attributes: attributes)
        //: btn.setAttributedTitle(attributeString, for: .normal)
        btn.setAttributedTitle(attributeString, for: .normal)
        //: btn.addTarget(self, action: #selector(feedbackBtnClick), for: .touchUpInside)
        btn.addTarget(self, action: #selector(executeRange), for: .touchUpInside)
        //: return btn
        return btn
        //: }()
    }()

    //: lazy var giftEffectView: TalkingPrivateChatAnimatView = {
    lazy var giftEffectView: AfterAnimatView = {
        //: let effectView = TalkingPrivateChatAnimatView.init()
        let effectView = AfterAnimatView()
        //: effectView.contentMode = .scaleAspectFill
        effectView.contentMode = .scaleAspectFill
        //: return effectView
        return effectView
        //: }()
    }()

    //: lazy var navView: TalkingChatNavTitleView = {
    lazy var navView: JumpView = {
        //: let navView = TalkingChatNavTitleView(type: conversationType)
        let navView = JumpView(type: conversationType)
        //: return navView
        return navView
        //: }()
    }()

    //: lazy var sendGreetBtn: UIButton = {
    lazy var sendGreetBtn: UIButton = {
        //: let btn = UIButton.init(type: UIButton.ButtonType.custom)
        let btn = UIButton(type: UIButton.ButtonType.custom)
        //: btn.backgroundColor = .white
        btn.backgroundColor = .white
        //: btn.titleLabel?.font = UIFont.pingfangFont(type: .Medium, fontSize: 15)
        btn.titleLabel?.font = UIFont.recipeDecision(type: .Medium, fontSize: 15)
        //: btn.setTitleColor(UIColor(hex: "#8370FF"), for: .normal)
        btn.setTitleColor(UIColor(hex: (String(show_primaryPath.suffix(7)))), for: .normal)
        //: btn.setTitle("Quick Greeting".localized, for: .normal)
        btn.setTitle((String(userHouseMessage) + const_temporaryFormat.replacingOccurrences(of: "work", with: "g")).localized, for: .normal)
        //: btn.layer.cornerRadius = 15
        btn.layer.cornerRadius = 15
        //: btn.layer.borderColor = UIColor.appThemeColor().cgColor
        btn.layer.borderColor = UIColor.crosswise().cgColor
        //: btn.layer.borderWidth = 1
        btn.layer.borderWidth = 1
        //: btn.layer.masksToBounds = true
        btn.layer.masksToBounds = true
        //: btn.addTarget(self, action: #selector(sendGreetBtnClick), for: .touchUpInside)
        btn.addTarget(self, action: #selector(placeSend), for: .touchUpInside)
        //: btn.isHidden = !FacultyReactiveCompatible.share.appUserConfigMode.showDirectGreet || FacultyReactiveCompatible.share.loginUserMode.sex == Gender.male.rawValue || FacultyReactiveCompatible.share.appStatus == AppSkinStatus.special.rawValue
        btn.isHidden = !FacultyReactiveCompatible.share.appUserConfigMode.showDirectGreet || FacultyReactiveCompatible.share.loginUserMode.sex == ATextLiteral.male.rawValue || FacultyReactiveCompatible.share.appStatus == PenConstantTarget.special.rawValue
        //: return btn
        return btn
        //: }()
    }()

    //: private lazy var replyTipsView: TalkingPrivateChatReplyTipsView? = {
    private lazy var replyTipsView: RatioReactiveCompatible? = {
        //: let view = TalkingPrivateChatReplyTipsView.init()
        let view = RatioReactiveCompatible()
        //: view.delegate = self
        view.delegate = self
        //: return view
        return view
        //: }()
    }()

    //: private lazy var beautyPhotosBtn: UIButton = {
    private lazy var beautyPhotosBtn: UIButton = {
        //: let btn = UIButton.init(type: UIButton.ButtonType.custom)
        let btn = UIButton(type: UIButton.ButtonType.custom)
        //: btn.backgroundColor = .white
        btn.backgroundColor = .white
        //: btn.titleLabel?.font = UIFont.pingfangFont(type: .Medium, fontSize: 15)
        btn.titleLabel?.font = UIFont.recipeDecision(type: .Medium, fontSize: 15)
        //: btn.setTitleColor(UIColor(hex: "#8370FF"), for: .normal)
        btn.setTitleColor(UIColor(hex: (String(show_primaryPath.suffix(7)))), for: .normal)
        //: btn.setTitle("Beauty Photos".localized, for: .normal)
        btn.setTitle((String(showCrushData) + String(app_cornerId)).localized, for: .normal)
        //: btn.layer.cornerRadius = 15
        btn.layer.cornerRadius = 15
        //: btn.layer.borderColor = UIColor.appThemeColor().cgColor
        btn.layer.borderColor = UIColor.crosswise().cgColor
        //: btn.layer.borderWidth = 1
        btn.layer.borderWidth = 1
        //: btn.layer.masksToBounds = true
        btn.layer.masksToBounds = true
        //: btn.addTarget(self, action: #selector(beautyPhotosBtnClick), for: .touchUpInside)
        btn.addTarget(self, action: #selector(nearBlind), for: .touchUpInside)
        //: btn.isHidden = FacultyReactiveCompatible.share.loginUserMode.sex == Gender.male.rawValue || FacultyReactiveCompatible.share.appStatus == AppSkinStatus.special.rawValue
        btn.isHidden = FacultyReactiveCompatible.share.loginUserMode.sex == ATextLiteral.male.rawValue || FacultyReactiveCompatible.share.appStatus == PenConstantTarget.special.rawValue
        //: return btn
        return btn
        //: }()
    }()

    //: private lazy var gamesView: TalkingLiveRoomGamesView = {
    private lazy var gamesView: DwellDataSource = {
        //: let v = TalkingLiveRoomGamesView()
        let v = DwellDataSource()
        //: return v
        return v
        //: }()
    }()

    //: private lazy var bannerView: TalkingPrivateBannerView = {
    private lazy var bannerView: PrivateDataSource = {
        //: let view = TalkingPrivateBannerView.init()
        let view = PrivateDataSource()
        //: view.delegate = self
        view.delegate = self
        //: return view
        return view
        //: }()
    }()

    //: private lazy var floatScreenView: TalkingGameFloatingScreenView = {
    private lazy var floatScreenView: CompareSlashChannel = { // 飘屏
        //: let v = TalkingGameFloatingScreenView()
        let v = CompareSlashChannel()
        //: v.isHidden = (FacultyReactiveCompatible.share.appStatus == AppSkinStatus.special.rawValue)
        v.isHidden = (FacultyReactiveCompatible.share.appStatus == PenConstantTarget.special.rawValue)
        //: v.isUserInteractionEnabled = false
        v.isUserInteractionEnabled = false
        //: v.endPlayBlock = { [weak self] in
        v.endPlayBlock = { [weak self] in
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: v.isUserInteractionEnabled = false
            v.isUserInteractionEnabled = false
        }
        //: return v
        return v
        //: }()
    }()

    //: private lazy var notesTopView: PrivateChatNotesTopView = {
    private lazy var notesTopView: SuiteTopView = { // 备注置顶
        //: let v = PrivateChatNotesTopView()
        let v = SuiteTopView()
        //: v.tapBlock = { [weak self] in
        v.tapBlock = { [weak self] in
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: self.jumpToNotesView()
            self.underBackground()
        }
        //: v.isHidden = true
        v.isHidden = true
        //: return v
        return v
        //: }()
    }()

    //: private lazy var rechargeView: RechargeRequiredView = {
    private lazy var rechargeView: CompleteRequiredView = { // 充值限制视图
        //: let v = RechargeRequiredView()
        let v = CompleteRequiredView()
        //: v.tapBlock = { [weak self] in
        v.tapBlock = { [weak self] in
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: if self.userWrap.isRecharge == -1 {
            if self.userWrap.isRecharge == -1 { // 首充优惠
                //: DisplayAperturePresenter.share.func_pushToDiscountRechargeView()
                DisplayAperturePresenter.share.onConvert()
                //: } else if self.userWrap.isRecharge == 0 {
            } else if self.userWrap.isRecharge == 0 { // 半屏充值
                //: DisplayAperturePresenter.share.func__pushToHalfWebVC(webViewType: .RechargeHalfPage)
                DisplayAperturePresenter.share.gallery(webViewType: .RechargeHalfPage)
            }
        }
        //: v.isHidden = true
        v.isHidden = true
        //: return v
        return v
        //: }()
    }()

    //: private lazy var quickReplayView: QuestionQuickReplayView = {
    private lazy var quickReplayView: AttentionAutoCustomOperation = { // 问题快捷回复视图
        //: let v = QuestionQuickReplayView()
        let v = AttentionAutoCustomOperation()
        //: v.tapBlock = { [weak self] model in
        v.tapBlock = { [weak self] model in
            //: guard let self = self else { return }
            guard let self = self else { return }
            // 调用发送消息接口
            //: self.func_sendQuestionQuickReplayMsg(model)
            self.everyLevel(model)
        }
        //: v.isHidden = true
        v.isHidden = true
        //: return v
        return v
        //: }()
    }()
}

// MARK: - banner

//: extension TalkingPrivateChatController: PrivateBannerDelegate {
extension BorderInsularViewDelegate: MixBannerDelegate {
    //: func changeBannerViewFream(isMiniView: Bool) {
    func bannerEye(isMiniView: Bool) {
        //: guard self.isFrom == .Normal else { return }
        guard self.isFrom == .Normal else { return }
        //: UIView.animate(withDuration: 0.3) {
        UIView.animate(withDuration: 0.3) {
            //: if isMiniView {
            if isMiniView {
                //: self.bannerView.snp.remakeConstraints { make in
                self.bannerView.snp.remakeConstraints { make in
                    //: if self.replyTipsView != nil {
                    if self.replyTipsView != nil {
                        //: make.bottom.equalTo(self.replyTipsView!.snp.top).offset(-8)
                        make.bottom.equalTo(self.replyTipsView!.snp.top).offset(-8)
                        //: } else {
                    } else {
                        //: make.bottom.equalTo(self.msgInputView.snp.top).offset(-8)
                        make.bottom.equalTo(self.msgInputView.snp.top).offset(-8)
                    }
                    //: make.trailing.equalTo(-4)
                    make.trailing.equalTo(-4)
                    //: make.height.equalTo(36)
                    make.height.equalTo(36)
                    //: make.width.equalTo(89)
                    make.width.equalTo(89)
                }
                //: } else {
            } else {
                //: self.bannerView.snp.remakeConstraints { make in
                self.bannerView.snp.remakeConstraints { make in
                    //: make.trailing.equalToSuperview()
                    make.trailing.equalToSuperview()
                    //: if self.replyTipsView != nil {
                    if self.replyTipsView != nil {
                        //: make.bottom.equalTo(self.replyTipsView!.snp.top).offset(-8)
                        make.bottom.equalTo(self.replyTipsView!.snp.top).offset(-8)
                        //: } else {
                    } else {
                        //: make.bottom.equalTo(self.msgInputView.snp.top).offset(-8)
                        make.bottom.equalTo(self.msgInputView.snp.top).offset(-8)
                    }
                    //: make.height.equalTo(106)
                    make.height.equalTo(106)
                    //: make.width.equalTo(93)
                    make.width.equalTo(93)
                }
            }
            //: self.bannerView.layoutIfNeeded()
            self.bannerView.layoutIfNeeded()
            //: self.bannerView.resetView(isMiniView: isMiniView)
            self.bannerView.checkion(isMiniView: isMiniView)
        }
    }
}

// MARK: - 聊天相关

//: extension TalkingPrivateChatController: TMessageControllerDelegate {
extension BorderInsularViewDelegate: RunningReadHalf {
    /// 一键打招呼
    //: @objc func sendGreetBtnClick() {
    @objc func placeSend() {
        //: TalkingPrivateChatManager.chat_sendQuickGreeting(toUserId: self.msgView.targetID) { errorCode, errorMsg, textDict, audioDict in
        SpanChatManager.counterHandler(toUserId: self.msgView.targetID) { _, errorMsg, textDict, audioDict in
            //: guard (textDict?.count ?? 0 > 0) || (audioDict?.count ?? 0 > 0) else {
            guard (textDict?.count ?? 0 > 0) || (audioDict?.count ?? 0 > 0) else {
                //: if errorMsg?.count ?? 0 > 0 {
                if errorMsg?.count ?? 0 > 0 {
                    //: self.micromicronProcess(showMsg: errorMsg!)
                    self.micromicronProcess(showMsg: errorMsg!)
                }
                //: return
                return
            }

            // 发送一键打招呼消息
            //: self.func_sendQuickGreetingMsg(textDict: textDict, audioDict: audioDict)
            self.fieldDict(textDict: textDict, audioDict: audioDict)
        }
    }

    /// 图片打招呼
    //: @objc func beautyPhotosBtnClick() {
    @objc func nearBlind() {
        //: TalkingPrivateChatManager.chat_sendBeautyPhotos(toUserId: self.msgView.targetID) { isPushSet, dict in
        SpanChatManager.outFail(toUserId: self.msgView.targetID) { isPushSet, dict in
            //: if isPushSet {
            if isPushSet {
                //: return
                return
            }
            //: if dict != nil {
            if dict != nil {
                //: UIImageView().setUrlImage(urlStr: dict?["url"] as? String ?? "") {[weak self] _, image in
                UIImageView().sortAge(urlStr: dict?[(data_littleSpanId.lowercased())] as? String ?? "") { [weak self] _, image in
                    //: guard let self = self else { return }
                    guard let self = self else { return }
                    //: if image != nil {
                    if image != nil {
                        //: let unlockGift = dict?["unlockGift"] as? Dictionary<String, Any>
                        let unlockGift = dict?[(String(showSmallData.prefix(7)) + String(appAppropriateGoingPath))] as? [String: Any]
                        //: let sendModel = AbTalkingChatMsgCustomSendModel()
                        let sendModel = HintModel()
                        //: sendModel.isBurn = String.init(format: "%d", dict?["isBurn"] as? Int ?? 0)
                        sendModel.isBurn = String(format: "%d", dict?[String(bytes: data_commonText.reversed(), encoding: .utf8)!] as? Int ?? 0)
                        //: sendModel.giftId = unlockGift?["giftId"] as? String ?? "0"
                        sendModel.giftId = unlockGift?[(String(userPetitionTitle))] as? String ?? "0"
                        //: self.func__sendPictureMsg(img: image!, sendModel: sendModel)
                        self.echogram(img: image!, sendModel: sendModel)
                        //: return
                        return
                    }
                }
            }
        }
    }

    //: func messageController(_ controller: TUIMessageController!, onNewMessage data: V2TIMMessage!) -> AgoneCellData! {
    func plain(_: ActivatedAlignmentLoader!, bold data: V2TIMMessage!) -> AgoneCellData! {
        //: let dic = CircularReactiveCompatible.parseTXMessageData(data: data.customElem.data)
        let dic = CircularReactiveCompatible.occurrent(data: data.customElem.data)
        //: var cellData = TUISocialChatManager.handleTXReceiveMessage(withExtral: dic, cusData: data.localCustomData, tempModel: data)
        var cellData = CivilityReactiveCompatible.stack(withExtral: dic, cusData: data.localCustomData, tempModel: data)
        //: if data.isKind(of: TalkingChatMatchMsgCellData.self), self.userWrap.uid.count > 0 {
        if data.isKind(of: SubjectCellData.self), self.userWrap.uid.count > 0 {
            //: cellData?.msgModel.userInfoModel = self.userWrap
            cellData?.msgModel.userInfoModel = self.userWrap
        }

        //: let jsonDic = JSON(dic)
        let jsonDic = JSON(dic)
        //: if jsonDic["rYMsgType"].stringValue == "GJ:CallCustom" { // 音视频通话消息
        if jsonDic[(String(main_eventTagTitle.prefix(9)))].stringValue == (String(app_loadDimensionUrl.prefix(4)) + "allCustom") { // 音视频通话消息
            //: let isSelf = jsonDic["changeDirection"].boolValue
            let isSelf = jsonDic[(String(user_termsName) + String(main_collectValue) + String(k_scenarioKey.prefix(9)))].boolValue
            //: if isSelf == true {
            if isSelf == true { // 消息需要翻转方向
                // 更正当前用户uid
                //: cellData?.msgModel.user.uid = jsonDic["extra"]["msgInfo"]["toUid"].intValue
                cellData?.msgModel.user.uid = jsonDic[(String(noti_yearFormat))][(String(dataReadMsg.suffix(7)))][(String(show_occurTitle.suffix(5)))].intValue
                // 更正消息展示方向
                //: let msgModel = cellData!.msgModel
                let msgModel = cellData!.msgModel
                //: cellData = AgoArrayCellData(direction: isSelf ? .MsgDirectionOutgoing:.MsgDirectionIncoming)
                cellData = AgoArrayCellData(direction: isSelf ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
                //: cellData?.msgModel = msgModel
                cellData?.msgModel = msgModel
            }
        }
        //: if cellData?.isKind(of: TagCellData.self) == true ||
        if cellData?.isKind(of: TagCellData.self) == true ||
            //: cellData?.isKind(of: ApartCellData.self) == true ||
            cellData?.isKind(of: ApartCellData.self) == true ||
            //: cellData?.isKind(of: AgoArrayCellData.self) == true ||
            cellData?.isKind(of: AgoArrayCellData.self) == true ||
            //: cellData?.isKind(of: SaverCellData.self) == true {
            cellData?.isKind(of: SaverCellData.self) == true
        {
            //: self.userWrap.existSess = true
            self.userWrap.existSess = true
        }

        //: if let cellData = cellData {
        if let cellData = cellData {
            // 接收解锁 私密消息（照片、视频）礼物，刷新本地消息状态
            //: TalkingPrivateChatManager.chat_onNewMessage_refreshLocalIntimateMsgStatus(cellData: cellData, msgView: self.msgView)
            SpanChatManager.publish(cellData: cellData, msgView: self.msgView)
            // 男性是否展示快捷回复消息
            //: checkQuestionQuickReplayView()
            queryOffAcross()
        }

        //: return cellData
        return cellData
    }

    //: func messageController(_ controller: TUIMessageController!, onShowMessageData data: AgoneCellData!) -> RatioViewCell! {
    func controllerLayer(_: ActivatedAlignmentLoader!, referent data: AgoneCellData!) -> RatioViewCell! {
        // 更新聊天头像，不会更新腾讯数据库
        //: let data = changeChatHeadPic(data: data)
        let data = east(data: data)

        //: if data.isKind(of: AgoArrayCellData.self) {
        if data.isKind(of: AgoArrayCellData.self) {
            //: let cell = TalkingChatTextMsgCell.init(style: .default, reuseIdentifier: TalkingChatTextMsgCell.className())
            let cell = RowMessageCell(style: .default, reuseIdentifier: RowMessageCell.className())
            //: let cellData = data as! AgoArrayCellData
            let cellData = data as! AgoArrayCellData

            //: if cellData.msgModel.quoteMsgInfo.isQuoteMsg() {
            if cellData.msgModel.quoteMsgInfo.hide() {
                //: if cellData.msgModel.quoteMsgInfo.uid == FacultyReactiveCompatible.share.loginUserMode.userID {
                if cellData.msgModel.quoteMsgInfo.uid == FacultyReactiveCompatible.share.loginUserMode.userID {
                    //: cellData.msgModel.quoteMsgInfo.sendName = FacultyReactiveCompatible.share.loginUserMode.nickname ?? ""
                    cellData.msgModel.quoteMsgInfo.sendName = FacultyReactiveCompatible.share.loginUserMode.nickname ?? ""
                    //: }else{
                } else {
                    //: cellData.msgModel.quoteMsgInfo.sendName = self.userWrap.nickname
                    cellData.msgModel.quoteMsgInfo.sendName = self.userWrap.nickname
                }
            }
            //: cell.fill(with: cellData)
            cell.lineStreetwise(with: cellData)
//            cell.transBlock = { [weak self] in
//                guard let self = self else { return }
//                self.msgView.translateMsg(with: cellData)
//            }
            //: return cell
            return cell
        }

        //: if data.isKind(of: ApartCellData.self) {
        if data.isKind(of: ApartCellData.self) {
            //: let cell = TalkingChatImageMsgCell.init(style: .default, reuseIdentifier: TalkingChatImageMsgCell.className())
            let cell = ImageViewCell(style: .default, reuseIdentifier: ImageViewCell.className())
            //: let cellData = data as! ApartCellData
            let cellData = data as! ApartCellData
            //: cell.fill(with: cellData)
            cell.lineStreetwise(with: cellData)
            //: return cell
            return cell
        }

        //: if data.isKind(of: SaverCellData.self) {
        if data.isKind(of: SaverCellData.self) {
            //: let cell = TalkingChatVideoMsgCell.init(style: .default, reuseIdentifier: TalkingChatVideoMsgCell.className())
            let cell = AfterMessageCell(style: .default, reuseIdentifier: AfterMessageCell.className())
            //: let cellData = data as! SaverCellData
            let cellData = data as! SaverCellData
            //: cell.fill(with: cellData)
            cell.lineStreetwise(with: cellData)
            //: return cell
            return cell
        }

        //: if data.isKind(of: RangeCellData.self) {
        if data.isKind(of: RangeCellData.self) {
            //: let cell = TalkingChatGiftMsgCell.init(style: .default, reuseIdentifier: TalkingChatGiftMsgCell.className())
            let cell = CollectionReactiveCompatible(style: .default, reuseIdentifier: CollectionReactiveCompatible.className())
            //: let cellData = data as! RangeCellData
            let cellData = data as! RangeCellData
            //: .fill(with: cellData)
            cell.lineStreetwise(with: cellData)
            //: return cell
            return cell
        }
        //: if data.isKind(of: CommandTypeTag.self) {
        if data.isKind(of: CommandTypeTag.self) {
            //: let cell = TalkingChatAskForGiftMsgCell.init(style: .default, reuseIdentifier: TalkingChatAskForGiftMsgCell.className())
            let cell = ChatMessageCell(style: .default, reuseIdentifier: ChatMessageCell.className())
            //: let cellData = data as! CommandTypeTag
            let cellData = data as! CommandTypeTag
            //: cell.fill(with: cellData)
            cell.lineStreetwise(with: cellData)
            //: cell.sendBlock = { [weak self] in
            cell.sendBlock = { [weak self] in
                //: guard let self = self else {
                guard let self = self else {
                    //: return
                    return
                }
                //: var model = TalkingRoomGiftModel.init()
                var model = ReportedMeasurable()
                //: model.gid = cellData.msgModel.msgInfo.wantGift.giftId
                model.gid = cellData.msgModel.msgInfo.wantGift.giftId
                //: self.func__sendGiftMsg(giftModel: model, giftNum: "1", cellData: cellData, fromStr: "wantGift")
                self.nearness(giftModel: model, giftNum: "1", cellData: cellData, fromStr: (String(data_bringName)))
            }
            //: return cell
            return cell
        }
        //: if data.isKind(of: TagCellData.self) {
        if data.isKind(of: TagCellData.self) {
            //: let cell = TalkingChatAudioMsgCell.init(style: .default, reuseIdentifier: TalkingChatAudioMsgCell.className())
            let cell = RetainViewCell(style: .default, reuseIdentifier: RetainViewCell.className())
            //: let cellData = data as! TagCellData
            let cellData = data as! TagCellData
            //: cell.fill(with: cellData)
            cell.lineStreetwise(with: cellData)
            //: return cell
            return cell
        }

        //: if data.isKind(of: TalkingChatMatchMsgCellData.self) {
        if data.isKind(of: SubjectCellData.self) {
            //: let cell = TalkingChatMatchMsgCell.init(style: .default, reuseIdentifier: TalkingChatMatchMsgCell.className())
            let cell = PeerSaverMsgCell(style: .default, reuseIdentifier: PeerSaverMsgCell.className())
            //: let cellData = data as! TalkingChatMatchMsgCellData
            let cellData = data as! SubjectCellData
            //: if self.userWrap.uid.count > 0 {
            if self.userWrap.uid.count > 0 {
                //: cellData.msgModel.userInfoModel = self.userWrap
                cellData.msgModel.userInfoModel = self.userWrap
            }
            //: cell.fill(with: cellData)
            cell.lineStreetwise(with: cellData)
            //: return cell
            return cell
        }

        //: if data.isKind(of: TalkingChatTipsMsgCellData.self) {
        if data.isKind(of: ComposableAnimatedPromise.self) {
            //: let cell = TalkingChatTipsMsgCell.init(style: .default, reuseIdentifier: TalkingChatTipsMsgCell.className())
            let cell = EventTaskVisibility(style: .default, reuseIdentifier: EventTaskVisibility.className())
            //: let cellData = data as! TalkingChatTipsMsgCellData
            let cellData = data as! ComposableAnimatedPromise
            //: cell.fill(with: cellData)
            cell.lineStreetwise(with: cellData)
            //: return cell
            return cell
        }

        //: if data.isKind(of: TalkingChatSystemNotifJumpMsgCellData.self) {
        if data.isKind(of: PersonCellData.self) {
            //: let cell = TalkingChatSystemNotifJumpMsgCell.init(style: .default, reuseIdentifier: TalkingChatSystemNotifJumpMsgCell.className())
            let cell = AudiencePersonMsgCell(style: .default, reuseIdentifier: AudiencePersonMsgCell.className())
            //: let cellData = data as! TalkingChatSystemNotifJumpMsgCellData
            let cellData = data as! PersonCellData
            //: cell.fill(with: cellData)
            cell.lineStreetwise(with: cellData)
            //: return cell
            return cell
        }

        //: return nil
        return nil
    }

    //: func messageController(_ controller: TUIMessageController!, onSelectMessageAvatar cell: RatioViewCell!) {
    func of(_: ActivatedAlignmentLoader!, ask cell: RatioViewCell!) {
        //: guard self.isFrom != .LiveRoom,
        guard self.isFrom != .LiveRoom,
              //: conversationType != .system,
              conversationType != .system,
              //: String(cell.messageData?.msgModel.user.uid ?? 0) != AnimateUnaryAllocator.getCustomerServiceID() else { return }
              String(cell.messageData?.msgModel.user.uid ?? 0) != AnimateUnaryAllocator.receiveRemove() else { return }

        //: if let uid = cell.messageData?.msgModel.user.uid {
        if let uid = cell.messageData?.msgModel.user.uid {
            //: DisplayAperturePresenter.share.func__pushToUserDetailVC(uid: String(uid))
            DisplayAperturePresenter.share.numbererest(uid: String(uid))
        }
    }

    //: func messageController(_ controller: TUIMessageController!, onSelectMessageContent cell: RatioViewCell!) {
    func fill(_: ActivatedAlignmentLoader!, handle cell: RatioViewCell!) {
        //: if cell .isKind(of: TalkingChatImageMsgCell.self) {
        if cell.isKind(of: ImageViewCell.self) { // 图片
            //: let imageCellData = cell.messageData as! ApartCellData
            let imageCellData = cell.messageData as! ApartCellData
            //: if imageCellData.msgModel.msgInfo.intimatePhoto.previewUrl.count > 0 {
            if imageCellData.msgModel.msgInfo.intimatePhoto.previewUrl.count > 0 { // 是私密照片
                // 已销毁的图片不能点击
                //: guard imageCellData.msgModel.msgInfo.intimatePhoto.burnStatus != 1 else { return }
                guard imageCellData.msgModel.msgInfo.intimatePhoto.burnStatus != 1 else { return }
                // 展示私密照片视图
                //: let vc = TalkingIntimatePhotoPreviewVC(imageCellData: imageCellData, toUid: self.msgView.targetID)
                let vc = ConfirmRecognizerDelegate(imageCellData: imageCellData, toUid: self.msgView.targetID)
                // 更改本地私密照片状态
                //: vc.statusBlock = { (model) in
                vc.statusBlock = { model in
                    //: self.msgView.changeIntimatePhotoMsg(imageCellData, photoModel: model)
                    self.msgView.meanSun(imageCellData, cut: model)
                }
                // 送礼效果在当前页面处理
                //: vc.sendGiftResultBlock = { succeed, result, errorModel in
                vc.sendGiftResultBlock = { succeed, result, errorModel in
                    //: guard succeed else {
                    guard succeed else {
                        //: self.dealSendMsgError(errorCode: errorModel!.errorCode, errorStr: errorModel!.errorMsg, isGift: true, isResend: false, clickEvent: clickUnlockButton)
                        self.decideSlipUp(errorCode: errorModel!.errorCode, errorStr: errorModel!.errorMsg, isGift: true, isResend: false, clickEvent: k_assetKey)
                        //: return
                        return
                    }

                    //: self.func__insertGiftMessageWithExtral(extral: result as! Dictionary<String, Any?>, isFromInputView: false)
                    self.general(extral: result as! [String: Any?], isFromInputView: false)
                    //: let dataDict = result as! Dictionary<String, Any>
                    let dataDict = result as! [String: Any]
                    //: self.unlockPhotoOrVideoTool(dataDict: dataDict)
                    self.verbalDescription(dataDict: dataDict)
                }
                //: getNavigationController()?.pushViewController(vc, animated: true)
                shorePast()?.pushViewController(vc, animated: true)

                //: } else {
            } else { // 普通图片
                //: let libraryPath = NSSearchPathForDirectoriesInDomains(.cachesDirectory, .userDomainMask, true).first!
                let libraryPath = NSSearchPathForDirectoriesInDomains(.cachesDirectory, .userDomainMask, true).first!

                //: var filePath = (libraryPath as NSString).appendingPathComponent(imageFileName)
                var filePath = (libraryPath as NSString).appendingPathComponent(data_marginPath)
                //: filePath = (filePath as NSString).appendingPathComponent((imageCellData.imageUrl! as NSString).lastPathComponent)
                filePath = (filePath as NSString).appendingPathComponent((imageCellData.imageUrl! as NSString).lastPathComponent)

                //: var model = TalkingMomentPhotoModel.init()
                var model = InmateTransformable()
                //: if FileManager.default.fileExists(atPath: filePath) {
                if FileManager.default.fileExists(atPath: filePath) {
                    //: let data = NSData.init(contentsOfFile: filePath)
                    let data = NSData(contentsOfFile: filePath)
                    //: model.imagePic = UIImage(data: data! as Data)
                    model.imagePic = UIImage(data: data! as Data)
                    //: } else {
                } else {
                    //: if imageCellData.imageData != nil {
                    if imageCellData.imageData != nil {
                        //: model.imagePic = imageCellData.imageData
                        model.imagePic = imageCellData.imageData
                        //: } else {
                    } else {
                        //: model.imgUrl =  imageCellData.imageUrl
                        model.imgUrl = imageCellData.imageUrl
                    }
                }
                //: var currenModel = TalkingMomentModel.init()
                var currenModel = AgencyMomentModel()
                //: var tempArr: [TalkingMomentPhotoModel] = []
                var tempArr: [InmateTransformable] = []
                //: tempArr.append(model)
                tempArr.append(model)
                //: currenModel.pic = tempArr
                currenModel.pic = tempArr
                //: let vc = TalkingMomentPhotosVC.init(momentModel: currenModel, index: 0, type: .normal)
                let vc = SpanScreenVc(momentModel: currenModel, index: 0, type: .normal)
                //: getNavigationController()?.pushViewController(vc, animated: true)
                shorePast()?.pushViewController(vc, animated: true)
            }

            //: } else if cell.isKind(of: TalkingChatVideoMsgCell.self) {
        } else if cell.isKind(of: AfterMessageCell.self) { // 视频
            //: let videoCellData = cell.messageData as! SaverCellData
            let videoCellData = cell.messageData as! SaverCellData
            //: let videoModel = videoCellData.msgModel.msgInfo.video
            let videoModel = videoCellData.msgModel.msgInfo.video
            //: if (videoModel.burnStatus == -1 && videoModel.lockStatus == -1 && videoModel.giftId == "0") { // 普通视频
            if videoModel.burnStatus == -1 && videoModel.lockStatus == -1 && videoModel.giftId == "0" { // 普通视频
                // 优先使用本地视频，没有再使用网络
                //: var videoPath = videoCellData.msgModel.msgInfo.customSend.localVideoUrl
                var videoPath = videoCellData.msgModel.msgInfo.customSend.localVideoUrl
                //: if videoPath.count == 0 || !FileManager.default.fileExists(atPath: videoPath) {
                if videoPath.count == 0 || !FileManager.default.fileExists(atPath: videoPath) {
                    //: videoPath = videoModel.videoUrl
                    videoPath = videoModel.videoUrl
                }

                //: let vc = TalkingMomentVideoVC.init(videoPath: videoPath)
                let vc = AssociatedDirectProject(videoPath: videoPath)
                //: getNavigationController()?.pushViewController(vc, animated: true)
                shorePast()?.pushViewController(vc, animated: true)

                //: } else {
            } else { // 私密视频
                // 已销毁的视频不能点击
                //: guard videoModel.burnStatus != 1 else { return }
                guard videoModel.burnStatus != 1 else { return }
                // 预览私密视频
                //: let vc = TalkingIntimateVideoPreviewVC(videoCellData: videoCellData, toUid: self.msgView.targetID)
                let vc = AttachReactiveCompatible(videoCellData: videoCellData, toUid: self.msgView.targetID)
                // 更改本地私密视频状态
                //: vc.statusBlock = { (model) in
                vc.statusBlock = { model in
                    //: self.msgView.changeIntimateVideoMsg(videoCellData, videoModel: model)
                    self.msgView.takePosition(videoCellData, tableModel: model)
                }
                // 送礼效果在当前页面处理
                //: vc.sendGiftResultBlock = { succeed, result, errorModel in
                vc.sendGiftResultBlock = { succeed, result, errorModel in
                    //: guard succeed else {
                    guard succeed else {
                        //: self.dealSendMsgError(errorCode: errorModel!.errorCode, errorStr: errorModel!.errorMsg, isGift: true, isResend: false, clickEvent: clickUnlockButton)
                        self.decideSlipUp(errorCode: errorModel!.errorCode, errorStr: errorModel!.errorMsg, isGift: true, isResend: false, clickEvent: k_assetKey)
                        //: return
                        return
                    }

                    //: self.func__insertGiftMessageWithExtral(extral: result as! Dictionary<String, Any?>, isFromInputView: false)
                    self.general(extral: result as! [String: Any?], isFromInputView: false)
                    //: let dataDict = result as! Dictionary<String, Any>
                    let dataDict = result as! [String: Any]
                    //: self.unlockPhotoOrVideoTool(dataDict: dataDict)
                    self.verbalDescription(dataDict: dataDict)
                }
                //: getNavigationController()?.pushViewController(vc, animated: true)
                shorePast()?.pushViewController(vc, animated: true)
            }

            //: } else if cell.isKind(of: TalkingChatAudioMsgCell.self) {
        } else if cell.isKind(of: RetainViewCell.self) {
            //: let audioCell = cell as! TalkingChatAudioMsgCell
            let audioCell = cell as! RetainViewCell
            //: func__DidTouchVoiceCellNeedPlayWithCurrentModel(cellData: audioCell.cellData as! TagCellData, cellView: audioCell)
            party(cellData: audioCell.cellData as! TagCellData, cellView: audioCell)

            //: } else if cell.isKind(of: TalkingChatTextMsgCell.self) {
        } else if cell.isKind(of: RowMessageCell.self) {
            //: let textCell = cell as! TalkingChatTextMsgCell
            let textCell = cell as! RowMessageCell
            //: let textData = textCell.messageData
            let textData = textCell.messageData
            //: let dic = CircularReactiveCompatible.parseTXMessageData(data: (textData?.innerMessage.customElem.data)!)
            let dic = CircularReactiveCompatible.occurrent(data: (textData?.innerMessage.customElem.data)!)
            //: guard let extraDic = dic["extra"] as? Dictionary<String, Any> else {
            guard let extraDic = dic[(String(noti_yearFormat))] as? [String: Any] else {
                //: return
                return
            }
            //: if extraDic.keys.contains("jumpKey") {
            if extraDic.keys.contains((String(main_gestureData.suffix(7)))) {
                //: let truePersonAuth = extraDic["jumpKey"] as? String ?? ""
                let truePersonAuth = extraDic[(String(main_gestureData.suffix(7)))] as? String ?? ""

                //: if let jumpModel = JSONDeserializer<TalkingMessageJumpModel>.deserializeFrom(dict: extraDic, designatedPath: nil) {
                if let jumpModel = JSONDeserializer<PhoneMessageTransformable>.deserializeFrom(dict: extraDic, designatedPath: nil) {
                    //: DisplayAperturePresenter.share.func__actionPushForMessage(jumpModel: jumpModel)
                    DisplayAperturePresenter.share.questionable(jumpModel: jumpModel)
                }
            }

            //: let extraJson = JSON(extraDic)
            let extraJson = JSON(extraDic)
            //: if  dic["rYMsgType"] as? String == "GJ:CallCustom" {  // 音视频通话消息
            if dic[(String(main_eventTagTitle.prefix(9)))] as? String == (String(app_loadDimensionUrl.prefix(4)) + "allCustom") { // 音视频通话消息
                //: if extraJson["msgInfo"]["logType"].intValue == 1 {
                if extraJson[(String(dataReadMsg.suffix(7)))][(String(show_activityName.prefix(7)))].intValue == 1 {
                    //: checkAndCallVoice()
                    bottom()
                    //: } else {
                } else {
                    //: checkAndCallVideo()
                    rating()
                }
            }

            //: } else if cell.isKind(of: TalkingChatTipsMsgCell.self) {
        } else if cell.isKind(of: EventTaskVisibility.self) {
            //: let tipCell = cell as! TalkingChatTipsMsgCell
            let tipCell = cell as! EventTaskVisibility
            //: let tipData = tipCell.messageData
            let tipData = tipCell.messageData
            //: let dic = CircularReactiveCompatible.parseTXMessageData(data: (tipData?.innerMessage.customElem.data)!)
            let dic = CircularReactiveCompatible.occurrent(data: (tipData?.innerMessage.customElem.data)!)
            //: guard let extraDic = dic["extra"] as? Dictionary<String, Any> else {
            guard let extraDic = dic[(String(noti_yearFormat))] as? [String: Any] else {
                //: return
                return
            }
            //: if extraDic.keys.contains("jumpKey") {
            if extraDic.keys.contains((String(main_gestureData.suffix(7)))) {
                //: if let jumpModel = JSONDeserializer<TalkingMessageJumpModel>.deserializeFrom(dict: extraDic, designatedPath: nil) {
                if let jumpModel = JSONDeserializer<PhoneMessageTransformable>.deserializeFrom(dict: extraDic, designatedPath: nil) {
                    //: DisplayAperturePresenter.share.func__actionPushForMessage(jumpModel: jumpModel)
                    DisplayAperturePresenter.share.questionable(jumpModel: jumpModel)
                }
            }
            //: } else if cell.isKind(of: TalkingChatMatchMsgCell.self) {
        } else if cell.isKind(of: PeerSaverMsgCell.self) {
            //: DisplayAperturePresenter.share.func__pushToUserDetailVC(uid: msgView.targetID)
            DisplayAperturePresenter.share.numbererest(uid: msgView.targetID)
        }
    }

    //: func messageController(_ controller: TUIMessageController!, onRetryMessageContent cell: RatioViewCell!) {
    func fixedSecure(_: ActivatedAlignmentLoader!, noSection cell: RatioViewCell!) {
        //: var cellData: TalkingChatMsgBaseCellData?
        var cellData: CompleteCellData?
        //: if cell .isKind(of: TalkingChatImageMsgCell.self) {
        if cell.isKind(of: ImageViewCell.self) {
            //: let imageCell = cell as! TalkingChatImageMsgCell
            let imageCell = cell as! ImageViewCell
            //: cellData = imageCell.cellData as! ApartCellData
            cellData = imageCell.cellData as! ApartCellData
            //: } else if cell.isKind(of: TalkingChatVideoMsgCell.self) {
        } else if cell.isKind(of: AfterMessageCell.self) {
            //: let videoCell = cell as! TalkingChatVideoMsgCell
            let videoCell = cell as! AfterMessageCell
            //: cellData = videoCell.cellData as! SaverCellData
            cellData = videoCell.cellData as! SaverCellData
            //: } else if cell.isKind(of: TalkingChatAudioMsgCell.self) {
        } else if cell.isKind(of: RetainViewCell.self) {
            //: let audioCell = cell as! TalkingChatAudioMsgCell
            let audioCell = cell as! RetainViewCell
            //: cellData = audioCell.cellData as! TagCellData
            cellData = audioCell.cellData as! TagCellData
            //: } else if cell.isKind(of: TalkingChatTextMsgCell.self) {
        } else if cell.isKind(of: RowMessageCell.self) {
            //: let textCell = cell as! TalkingChatTextMsgCell
            let textCell = cell as! RowMessageCell
            //: cellData = textCell.cellData as! AgoArrayCellData
            cellData = textCell.cellData as! AgoArrayCellData
        }
        //: if cellData != nil {
        if cellData != nil {
            //: let reCellData: AgoneCellData? = TUISocialChatManager.handleTXMessageReSend(withData: cellData!)
            let reCellData: AgoneCellData? = CivilityReactiveCompatible.set(withData: cellData!)
            //: if reCellData != nil {
            if reCellData != nil {
                //: cell.messageData?.msgModel = reCellData!.msgModel
                cell.messageData?.msgModel = reCellData!.msgModel
                //: cell.messageData?.status = .Msg_Status_Fail
                cell.messageData?.status = .Msg_Status_Fail
                //: if reCellData!.isKind(of: SaverCellData.self) {
                if reCellData!.isKind(of: SaverCellData.self) { // 视频重发
                    //: func__sendVideoMsg(msgData: cell.messageData, sendModel: cell.messageData!.msgModel.msgInfo.customSend)
                    listener(msgData: cell.messageData, sendModel: cell.messageData!.msgModel.msgInfo.customSend)
                    //: } else {
                } else {
                    //: func__sendPriveteMsg(msgData: cell.messageData!, reSend: true)
                    clear(msgData: cell.messageData!, reSend: true)
                }
            }
        }
    }

    /// 回复消息
    //: func messageController(_ controller: TUIMessageController!, onReplyMessage cellData: AgoneCellData!) {
    func outline(_: ActivatedAlignmentLoader!, border cellData: AgoneCellData!) {
        //: msgInputView.quoteModel = TalkingPrivateChatManager.handleReplyMsg(cellData: cellData, targetId: userWrap.uid)
        msgInputView.quoteModel = SpanChatManager.zoneHandle(cellData: cellData, targetId: userWrap.uid)
    }

    /// 点击“引用”消息
    //: func messageController(_ controller: TUIMessageController!, onClickReplyMessage cellData: AgoneCellData!) {
    func effective(_: ActivatedAlignmentLoader!, before cellData: AgoneCellData!) {
        //: TalkingPrivateChatManager.handleClickReplyMsg(cellData: cellData, targetId: userWrap.uid)
        SpanChatManager.takeCareSharedPost(cellData: cellData, targetId: userWrap.uid)
    }

    /// 实时更新聊天双方头像
    /// - Parameter data: 腾讯数据
    /// - Returns: 腾讯数据
    //: private func changeChatHeadPic(data: AgoneCellData) -> AgoneCellData {
    private func east(data: AgoneCellData) -> AgoneCellData {
        //: let isSelf = data.msgModel.user.uid == Int(FacultyReactiveCompatible.share.loginUserMode.userID)
        let isSelf = data.msgModel.user.uid == Int(FacultyReactiveCompatible.share.loginUserMode.userID)
        //: if isSelf {
        if isSelf {
            //: if FacultyReactiveCompatible.share.loginUserMode.headPic?.count ?? 0 > 0 {
            if FacultyReactiveCompatible.share.loginUserMode.headPic?.count ?? 0 > 0 {
                //: data.msgModel.user.headPic = FacultyReactiveCompatible.share.loginUserMode.headPic!
                data.msgModel.user.headPic = FacultyReactiveCompatible.share.loginUserMode.headPic!
                //: data.msgModel.user.headPicFrame = FacultyReactiveCompatible.share.loginUserMode.headPicFrame
                data.msgModel.user.headPicFrame = FacultyReactiveCompatible.share.loginUserMode.headPicFrame
            }
            //: } else {
        } else {
            //: if self.userWrap.headPic.count > 0 {
            if self.userWrap.headPic.count > 0 {
                //: data.msgModel.user.headPic = self.userWrap.headPic
                data.msgModel.user.headPic = self.userWrap.headPic
                //: data.msgModel.user.headPicFrame = self.userWrap.headPicFrame
                data.msgModel.user.headPicFrame = self.userWrap.headPicFrame
            }
        }
        //: return data
        return data
    }
}

// MARK: - 发送消息

//: extension TalkingPrivateChatController {
extension BorderInsularViewDelegate {
    /// 发送问题回复消息 -- 回答
    /// - Parameter model: 回答问题模型
    //: func func_sendQuestionQuickReplayMsg(_ model: QuestionQuickReplayModel) {
    func everyLevel(_ model: VitaminPillModel) {
        //: let msgInfo = ["content": model.content, "msgType": "txt"]
        let msgInfo = [String(bytes: dataDefineValue.map{fromMystery(desk: $0)}, encoding: .utf8)!: model.content, String(bytes: noti_filterPath.map{shoreCounteraction(bright: $0)}, encoding: .utf8)!: (mainRetirementKey.replacingOccurrences(of: "incident", with: "xt"))]
        //: let paramsDic = ["message": model.content, "replayId": model.replayId, "type": "TxtMsg"] as [String : Any]
        let paramsDic = [String(bytes: app_packageValue.map{absExcept(arrow: $0)}, encoding: .utf8)!: model.content, String(bytes: noti_normalMsg.map{hireService(portrait: $0)}, encoding: .utf8)!: model.replayId, (kExitStr.replacingOccurrences(of: "timing", with: "y")): (String(mainDateFormat))] as [String: Any]
        //: let dic = ["msgInfo": msgInfo, "params": paramsDic]
        let dic = [(String(dataReadMsg.suffix(7))): msgInfo, String(bytes: showAbsoluteStr.reversed(), encoding: .utf8)!: paramsDic]
        //: if let cellData = TUISocialChatManager.handleTXMessageSend(withExtral: dic, isSender: true, isTip: false) {
        if let cellData = CivilityReactiveCompatible.smartMailSenderBlockTipHandleTurn(withExtral: dic, isSender: true, isTip: false) {
            //: self.func__sendPriveteMsg(msgData: cellData, reSend: false)
            self.clear(msgData: cellData, reSend: false)
        }
    }

    /// 发送一键打招呼消息
    //: func func_sendSayHiMsg() {
    func sufficient() {
        //: let msgInfo = ["msgType": "txt"]
        let msgInfo = [String(bytes: noti_filterPath.map{shoreCounteraction(bright: $0)}, encoding: .utf8)!: (mainRetirementKey.replacingOccurrences(of: "incident", with: "xt"))]
        //: let paramsDic = ["type": "MultiQuickMsg"]
        let paramsDic = [(kExitStr.replacingOccurrences(of: "timing", with: "y")): (String(app_healthyPath.suffix(6)) + String(noti_noticeFireCellMsg.suffix(7)))]
        //: let dic = ["msgInfo": msgInfo, "params": paramsDic]
        let dic = [(String(dataReadMsg.suffix(7))): msgInfo, String(bytes: showAbsoluteStr.reversed(), encoding: .utf8)!: paramsDic]
        //: if let cellData = TUISocialChatManager.handleTXMessageSend(withExtral: dic, isSender: true, isTip: false) {
        if let cellData = CivilityReactiveCompatible.smartMailSenderBlockTipHandleTurn(withExtral: dic, isSender: true, isTip: false) {
            //: self.func__sendPriveteMsg(msgData: cellData, reSend: false)
            self.clear(msgData: cellData, reSend: false)
        }
    }

    /// 发送快速打招呼消息
    //: func func_sendQuickGreetingMsg() {
    func limitOn() {
        //: let msgInfo = ["msgType": "txt"]
        let msgInfo = [String(bytes: noti_filterPath.map{shoreCounteraction(bright: $0)}, encoding: .utf8)!: (mainRetirementKey.replacingOccurrences(of: "incident", with: "xt"))]
        //: let paramsDic = ["type": "QuickMsg"]
        let paramsDic = [(kExitStr.replacingOccurrences(of: "timing", with: "y")): (String(k_spaceText.prefix(6)) + String(const_pointData))]
        //: let dic = ["msgInfo": msgInfo, "params": paramsDic]
        let dic = [(String(dataReadMsg.suffix(7))): msgInfo, String(bytes: showAbsoluteStr.reversed(), encoding: .utf8)!: paramsDic]
        //: if let cellData = TUISocialChatManager.handleTXMessageSend(withExtral: dic, isSender: true, isTip: false) {
        if let cellData = CivilityReactiveCompatible.smartMailSenderBlockTipHandleTurn(withExtral: dic, isSender: true, isTip: false) {
            //: self.func__sendPriveteMsg(msgData: cellData, reSend: false)
            self.clear(msgData: cellData, reSend: false)
        }
    }

    /// 发送一键打招呼消息
    /// - Parameters:
    ///   - textDict: 文本消息
    ///   - audioDict: 语音消息
    //: private func func_sendQuickGreetingMsg(textDict: [String: Any]?, audioDict: [String: Any]?) {
    private func fieldDict(textDict: [String: Any]?, audioDict: [String: Any]?) {
        //: if textDict?.count ?? 0 > 0 {
        if textDict?.count ?? 0 > 0 { // 发送文本消息
            //: let content = textDict?["content"]
            let content = textDict?[String(bytes: dataDefineValue.map{fromMystery(desk: $0)}, encoding: .utf8)!]
            //: let greetId = textDict?["id"]
            let greetId = textDict?["id"]
            //: let msgInfo = ["content": content, "txtId": greetId, "msgType": "txt"]
            let msgInfo = [String(bytes: dataDefineValue.map{fromMystery(desk: $0)}, encoding: .utf8)!: content, (String(notiStillGivingFormat.prefix(5))): greetId, String(bytes: noti_filterPath.map{shoreCounteraction(bright: $0)}, encoding: .utf8)!: (mainRetirementKey.replacingOccurrences(of: "incident", with: "xt"))]
            //: let paramsDic = ["message": content, "txtId": greetId, "type": "TxtMsg"]
            let paramsDic = [String(bytes: app_packageValue.map{absExcept(arrow: $0)}, encoding: .utf8)!: content, (String(notiStillGivingFormat.prefix(5))): greetId, (kExitStr.replacingOccurrences(of: "timing", with: "y")): (String(mainDateFormat))]
            //: let dic = ["msgInfo": msgInfo, "params": paramsDic]
            let dic = [(String(dataReadMsg.suffix(7))): msgInfo, String(bytes: showAbsoluteStr.reversed(), encoding: .utf8)!: paramsDic]
            //: if let cellData = TUISocialChatManager.handleTXMessageSend(withExtral: dic, isSender: true, isTip: false) {
            if let cellData = CivilityReactiveCompatible.smartMailSenderBlockTipHandleTurn(withExtral: dic, isSender: true, isTip: false) {
                //: self.func__sendPriveteMsg(msgData: cellData, reSend: false)
                self.clear(msgData: cellData, reSend: false)
            }
        }

        //: if audioDict?.count ?? 0 > 0 {
        if audioDict?.count ?? 0 > 0 { // 发送语音消息
            //: VoiceDownloadTaskManager.shared.addAudioGreetDownLoadTask(audioDict!) { model in
            DurationJobHue.shared.key(audioDict!) { model in
                //: if model.taskTypeId != .oneKeyGreet {
                if model.taskTypeId != .oneKeyGreet {
                    //: return
                    return
                }
                //: if model.taskStatus != .finish {
                if model.taskStatus != .finish {
                    //: DispatchQueue.main.async {
                    DispatchQueue.main.async {
                        //: self.micromicronProcess(showMsg: "Voice hello download failed. Procedure")
                        self.micromicronProcess(showMsg: String(bytes: noti_weightUrl.map{bondRating(mean: $0)}, encoding: .utf8)!)
                        //: TalkingPrivateChatManager.chat_changeSaveGreetData(toUserId: self.msgView.targetID, isHave: false)
                        SpanChatManager.ping(toUserId: self.msgView.targetID, isHave: false)
                    }
                    //: return
                    return
                }
                //: let greetId = audioDict?["id"]
                let greetId = audioDict?["id"]
                //: let length = audioDict?["length"]
                let length = audioDict?[(String(userLackName))]
                //: let msgInfo = ["msgType": "audio",
                let msgInfo = [String(bytes: noti_filterPath.map{shoreCounteraction(bright: $0)}, encoding: .utf8)!: (String(kVisualId)),
                               //: "audioId": greetId,
                               (main_belowUrl.lowercased() + String(show_agoMessage)): greetId,
                               //: "audioLength": length,
                               String(bytes: constFullSegmentFadeContent.map{challengeEnd(adopt: $0)}, encoding: .utf8)!: length,
                               //: "audioSandbox": model.unZipFilePath,
                               (String(noti_figureBowlFormat) + String(noti_creationStr)): model.unZipFilePath,
                               //: "senderId": FacultyReactiveCompatible.share.loginUserMode.userID]
                               String(bytes: appDimensionFormat.map{freshStart(mention: $0)}, encoding: .utf8)!: FacultyReactiveCompatible.share.loginUserMode.userID]

                //: let paramsDic = ["audioId": greetId,
                let paramsDic = [(main_belowUrl.lowercased() + String(show_agoMessage)): greetId,
                                 //: "audioLength": length,
                                 String(bytes: constFullSegmentFadeContent.map{challengeEnd(adopt: $0)}, encoding: .utf8)!: length,
                                 //: "audioSandbox": model.unZipFilePath,
                                 (String(noti_figureBowlFormat) + String(noti_creationStr)): model.unZipFilePath,
                                 //: "type": "AudioMsg"]
                                 (kExitStr.replacingOccurrences(of: "timing", with: "y")): (String(notiBowlFormat) + String(k_createDepictPath.prefix(3)))]
                //: let dic = ["msgInfo": msgInfo, "params": paramsDic]
                let dic = [(String(dataReadMsg.suffix(7))): msgInfo, String(bytes: showAbsoluteStr.reversed(), encoding: .utf8)!: paramsDic]
                //: if let cellData = TUISocialChatManager.handleTXMessageSend(withExtral: dic, isSender: true, isTip: false) {
                if let cellData = CivilityReactiveCompatible.smartMailSenderBlockTipHandleTurn(withExtral: dic, isSender: true, isTip: false) {
                    //: self.func__sendPriveteMsg(msgData: cellData, reSend: false)
                    self.clear(msgData: cellData, reSend: false)
                }
            }
        }
    }

    /// 发送文本消息
    /// - Parameter msgStr: 文本
    //: internal func func__sendTextMsg(msgStr: String, quoteModel: AbTalkingChatMsgQuoteModel?) {
    func flag(msgStr: String, quoteModel: FirstSolidTitleTheoryModel?) {
        //: let temStr = msgStr.trimmingCharacters(in: CharacterSet.whitespacesAndNewlines)
        let temStr = msgStr.trimmingCharacters(in: CharacterSet.whitespacesAndNewlines)
        //: if temStr.count == 0 {
        if temStr.count == 0 {
            //: micromicronProcess(showMsg: "Can't send blank message".localized)
            micromicronProcess(showMsg: (String(main_taTitle) + dataTrackHolderContent.lowercased()).localized)
            //: return
            return
        }

        //: let msgInfo = ["content": temStr, "msgType": "txt"]
        let msgInfo = [String(bytes: dataDefineValue.map{fromMystery(desk: $0)}, encoding: .utf8)!: temStr, String(bytes: noti_filterPath.map{shoreCounteraction(bright: $0)}, encoding: .utf8)!: (mainRetirementKey.replacingOccurrences(of: "incident", with: "xt"))]
        //: var paramsDic = ["message": temStr, "type": "TxtMsg"]
        var paramsDic = [String(bytes: app_packageValue.map{absExcept(arrow: $0)}, encoding: .utf8)!: temStr, (kExitStr.replacingOccurrences(of: "timing", with: "y")): (String(mainDateFormat))]
        //: var dic:[String: Any] = ["msgInfo": msgInfo]
        var dic: [String: Any] = [(String(dataReadMsg.suffix(7))): msgInfo]

        //: if let tempQuoteModel = quoteModel{
        if let tempQuoteModel = quoteModel {
            //: paramsDic["quoteMsgId"] = tempQuoteModel.msgId
            paramsDic[(String(kChamberMessage.prefix(6)) + "sgId")] = tempQuoteModel.msgId
            //: dic["quoteMsgInfo"] = tempQuoteModel.toDictionary()
            dic[(mainPastId.replacingOccurrences(of: "clear", with: "qu") + "MsgInfo")] = tempQuoteModel.toDictionary()
        }
        //: dic["params"] = paramsDic
        dic[String(bytes: showAbsoluteStr.reversed(), encoding: .utf8)!] = paramsDic

        //: if let cellData = TUISocialChatManager.handleTXMessageSend(withExtral: dic, isSender: true, isTip: false) {
        if let cellData = CivilityReactiveCompatible.smartMailSenderBlockTipHandleTurn(withExtral: dic, isSender: true, isTip: false) {
            //: cellData.msgModel.quoteMsgInfo = quoteModel ?? AbTalkingChatMsgQuoteModel()
            cellData.msgModel.quoteMsgInfo = quoteModel ?? FirstSolidTitleTheoryModel()
            //: func__sendPriveteMsg(msgData: cellData, reSend: false)
            clear(msgData: cellData, reSend: false)
        }
    }

    /// 发送图片消息
    /// - Parameters:
    ///   - img: 图片
    ///   - sendModel: 本地数据模型
    //: private func func__sendPictureMsg(img: UIImage, sendModel: AbTalkingChatMsgCustomSendModel) {
    private func echogram(img: UIImage, sendModel: HintModel) {
        //: let imagePath = ApartCellData.getChatImagePath(image: img)
        let imagePath = ApartCellData.method(image: img)
        // 缓存本地的消息
        //: let sendDict = sendModel.toDictionary() ?? [:]
        let sendDict = sendModel.toDictionary() ?? [:]
        //: let msgInfo: [String: Any] = ["contentType": "ImgMsg", "msgType": "img", "imageUri": imagePath, "customSend": sendDict]
        let msgInfo: [String: Any] = [String(bytes: mainFastAliveName.map{recognizeDecision(arc: $0)}, encoding: .utf8)!: (String(appReloadKey)), String(bytes: noti_filterPath.map{shoreCounteraction(bright: $0)}, encoding: .utf8)!: (user_equallyTitle.lowercased()), String(bytes: showQuickKey.map{factorCalculationSeason(disable: $0)}, encoding: .utf8)!: imagePath, String(bytes: kPlatformPath.map{availableTransfer(radical: $0)}, encoding: .utf8)!: sendDict]
        // 接口参数
        //: let paramsDic: [String: Any] = ["pic": imagePath,
        let paramsDic: [String: Any] = [(user_airKey.replacingOccurrences(of: "population", with: "c")): imagePath,
                                        //: "type": "ImgMsg",
                                        (kExitStr.replacingOccurrences(of: "timing", with: "y")): (String(appReloadKey)),
                                        //: "isBurn": sendModel.isBurn,
                                        String(bytes: data_commonText.reversed(), encoding: .utf8)!: sendModel.isBurn,
                                        //: "giftId": sendModel.giftId]
                                        (String(userPetitionTitle)): sendModel.giftId]
        //: let dic = ["msgInfo": msgInfo, "params": paramsDic]
        let dic = [(String(dataReadMsg.suffix(7))): msgInfo, String(bytes: showAbsoluteStr.reversed(), encoding: .utf8)!: paramsDic]
        //: if let cellData = TUISocialChatManager.handleTXMessageSend(withExtral: dic, isSender: true, isTip: false) {
        if let cellData = CivilityReactiveCompatible.smartMailSenderBlockTipHandleTurn(withExtral: dic, isSender: true, isTip: false) {
            //: func__sendPriveteMsg(msgData: cellData, reSend: false)
            clear(msgData: cellData, reSend: false)
        }
    }

    /// 发送视频消息
    /// - Parameters:
    ///   - msgData: 发送视频cellData；nil：上传流程，有值 ：发送流程
    ///   - sendModel: 视频消息自定义本地模型
    //: private func func__sendVideoMsg(msgData: AgoneCellData?, sendModel: AbTalkingChatMsgCustomSendModel) {
    private func listener(msgData: AgoneCellData?, sendModel: HintModel) {
        // 缓存本地的消息
        //: let videoDict = sendModel.toDictionary() ?? [:]
        let videoDict = sendModel.toDictionary() ?? [:]
        //: let msgInfo: [String: Any] = ["contentType": "VideoMsg", "msgType": "video", "customSend": videoDict]
        let msgInfo: [String: Any] = [String(bytes: mainFastAliveName.map{recognizeDecision(arc: $0)}, encoding: .utf8)!: (String(k_fragmentName.suffix(6)) + String(show_formationPath)), String(bytes: noti_filterPath.map{shoreCounteraction(bright: $0)}, encoding: .utf8)!: (k_contextMoreFormat.replacingOccurrences(of: "teem", with: "v")), String(bytes: kPlatformPath.map{availableTransfer(radical: $0)}, encoding: .utf8)!: videoDict]
        // 接口参数
        //: let paramsDic: [String: Any] = ["type": "VideoMsg",
        let paramsDic: [String: Any] = [(kExitStr.replacingOccurrences(of: "timing", with: "y")): (String(k_fragmentName.suffix(6)) + String(show_formationPath)),
                                        //: "coverImg": sendModel.coverImg,
                                        String(bytes: app_cellValue.map{inkProperty(ret: $0)}, encoding: .utf8)!: sendModel.coverImg,
                                        //: "videoUrl": sendModel.videoUrl,
                                        String(bytes: mainAllowPath.map{artisticStyle(another: $0)}, encoding: .utf8)!: sendModel.videoUrl,
                                        //: "duration": sendModel.duration,
                                        String(bytes: user_foundValue.map{$0^4}, encoding: .utf8)!: sendModel.duration,
                                        //: "isBurn": sendModel.isBurn,
                                        String(bytes: data_commonText.reversed(), encoding: .utf8)!: sendModel.isBurn,
                                        //: "giftId": sendModel.giftId]
                                        (String(userPetitionTitle)): sendModel.giftId]
        //: let dict = ["msgInfo": msgInfo, "params": paramsDic]
        let dict = [(String(dataReadMsg.suffix(7))): msgInfo, String(bytes: showAbsoluteStr.reversed(), encoding: .utf8)!: paramsDic]

        // 压缩成功 ---> 创建cellData ---> 上传视频
        //: guard let cellData = msgData else {
        guard let cellData = msgData else {
            //: if let uploadCellData = TUISocialChatManager.handleTXMessageSend(withExtral: dict, isSender: true, isTip: false) {
            if let uploadCellData = CivilityReactiveCompatible.smartMailSenderBlockTipHandleTurn(withExtral: dict, isSender: true, isTip: false) {
                //: self.msgView.sendCustomMessage(uploadCellData)
                self.msgView.element(uploadCellData)
                //: self.cos_UploadVideo(msgData: uploadCellData, sendModel: sendModel)
                self.secure(msgData: uploadCellData, sendModel: sendModel)
            }
            //: return
            return
        }

        // 失败重试，展示加载动画
        //: if cellData.status != .Msg_Status_Sending_2 {
        if cellData.status != .Msg_Status_Sending_2 {
            //: self.msgView.changeCustomMsg(cellData, status: .Msg_Status_Sending_2)
            self.msgView.confirm(cellData, barroom: .Msg_Status_Sending_2)
        }

        // 失败重试上传视频流程
        //: guard sendModel.videoUrl.count > 0 else {
        guard sendModel.videoUrl.count > 0 else {
            //: self.cos_UploadVideo(msgData: cellData, sendModel: sendModel)
            self.secure(msgData: cellData, sendModel: sendModel)
            //: return
            return
        }

        // 修改本地消息数据，调用发送视频接口
        //: self.msgView.modifyCustomMessageData(withExtral: dict, cellData: cellData)
        self.msgView.communicationSmartCustomModify(dict, ignore: cellData)
        //: func__sendPriveteMsg(msgData: cellData, reSend: false)
        clear(msgData: cellData, reSend: false)
    }

    /// 发送语音
    /// - Parameters:
    ///   - voicePath: 语音路径
    ///   - voiceTime: 时间
    //: func func__sendVoiceMsg(voicePath: String, voiceTime: Int) {
    func lastWeekly(voicePath: String, voiceTime: Int) {
        //: let msgInfo = ["msgType": "audio",
        let msgInfo = [String(bytes: noti_filterPath.map{shoreCounteraction(bright: $0)}, encoding: .utf8)!: (String(kVisualId)),
                       //: "audioLength": "\(voiceTime)",
                       String(bytes: constFullSegmentFadeContent.map{challengeEnd(adopt: $0)}, encoding: .utf8)!: "\(voiceTime)",
                       //: "audioSandbox": voicePath,
                       (String(noti_figureBowlFormat) + String(noti_creationStr)): voicePath,
                       //: "senderId": FacultyReactiveCompatible.share.loginUserMode.userID]
                       String(bytes: appDimensionFormat.map{freshStart(mention: $0)}, encoding: .utf8)!: FacultyReactiveCompatible.share.loginUserMode.userID]

        //: let paramsDic = ["audioSandbox": voicePath,
        let paramsDic = [(String(noti_figureBowlFormat) + String(noti_creationStr)): voicePath,
                         //: "type": "AudioMsg",
                         (kExitStr.replacingOccurrences(of: "timing", with: "y")): (String(notiBowlFormat) + String(k_createDepictPath.prefix(3))),
                         //: "len": "\(voiceTime)"]
                         String(bytes: app_totalPath.map{$0^62}, encoding: .utf8)!: "\(voiceTime)"]
        //: let dic = ["msgInfo": msgInfo, "params": paramsDic]
        let dic = [(String(dataReadMsg.suffix(7))): msgInfo, String(bytes: showAbsoluteStr.reversed(), encoding: .utf8)!: paramsDic]
        //: if let cellData = TUISocialChatManager.handleTXMessageSend(withExtral: dic, isSender: true, isTip: false) {
        if let cellData = CivilityReactiveCompatible.smartMailSenderBlockTipHandleTurn(withExtral: dic, isSender: true, isTip: false) {
            //: func__sendPriveteMsg(msgData: cellData, reSend: false)
            clear(msgData: cellData, reSend: false)
        }
    }

    /// 发送消息
    /// - Parameters:
    ///   - msgData: 消息数据结构
    ///   - reSend: 是否重发【目前未使用该字段】
    //: private func func__sendPriveteMsg(msgData: AgoneCellData, reSend: Bool) {
    private func clear(msgData: AgoneCellData, reSend _: Bool) {
        //: let reachability = try? Reachability()
        let reachability = try? Reachability()
        //: if reachability?.connection == .unavailable {
        if reachability?.connection == .unavailable {
            //: micromicronProcess(showMsg: kNetErrorMsg)
            micromicronProcess(showMsg: user_robotCameraName)
            //: return
            return
        }

        //: var params: Dictionary<String, Any> = msgData.msgModel.params as! Dictionary<String, Any>
        var params: [String: Any] = msgData.msgModel.params as! [String: Any]
        //: params["toUid"] = self.msgView.targetID
        params[(String(show_occurTitle.suffix(5)))] = self.msgView.targetID

        // 发送视频消息，索要礼物消息，快速打招呼消息，不在此处插入
        //: if msgData.isKind(of: SaverCellData.self) == false && msgData.isKind(of: CommandTypeTag.self) == false && params["type"] as? String != "QuickMsg" && params["type"] as? String != "MultiQuickMsg" {
        if msgData.isKind(of: SaverCellData.self) == false, msgData.isKind(of: CommandTypeTag.self) == false, params[(kExitStr.replacingOccurrences(of: "timing", with: "y"))] as? String != (String(k_spaceText.prefix(6)) + String(const_pointData)), params[(kExitStr.replacingOccurrences(of: "timing", with: "y"))] as? String != (String(app_healthyPath.suffix(6)) + String(noti_noticeFireCellMsg.suffix(7))) {
            //: self.msgView.sendCustomMessage(msgData)
            self.msgView.element(msgData)
        }
//        let recordTime = CircularReactiveCompatible.getMessageInsertTime()

        //: if params.keys .contains("pic") { // 图片
        if params.keys.contains((user_airKey.replacingOccurrences(of: "population", with: "c"))) { // 图片
            //: let imageStr: String = params["pic"] as! String
            let imageStr: String = params[(user_airKey.replacingOccurrences(of: "population", with: "c"))] as! String
            //: do {
            do {
                //: let data: Data = try Data.init(contentsOf: URL.init(fileURLWithPath: imageStr))
                let data: Data = try Data(contentsOf: URL(fileURLWithPath: imageStr))
                //: params["pic"] = data
                params[(user_airKey.replacingOccurrences(of: "population", with: "c"))] = data
                //: } catch {
            } catch {
                //: printLog(message: error)
                printLog(message: error)
            }
        }
        //: let type  = params["type"] as? String
        let type = params[(kExitStr.replacingOccurrences(of: "timing", with: "y"))] as? String
        //: if type == "AudioMsg" { // 语音
        if type == (String(notiBowlFormat) + String(k_createDepictPath.prefix(3))) { // 语音
            //: if params.keys.contains("audioSandbox") {
            if params.keys.contains((String(noti_figureBowlFormat) + String(noti_creationStr))) {
                //: var path =  params["audioSandbox"] as! String
                var path = params[(String(noti_figureBowlFormat) + String(noti_creationStr))] as! String
                //: params.removeValue(forKey: "audioSandbox")
                params.removeValue(forKey: (String(noti_figureBowlFormat) + String(noti_creationStr)))

                //: let theFileName = (path as NSString).lastPathComponent
                let theFileName = (path as NSString).lastPathComponent

                //: if path.contains("Documents/User/voice/") {
                if path.contains((String(k_commentMsg.suffix(6)) + "nts/" + String(k_effectiveData.prefix(8)) + String(userHeartId))) {
                    //: path = Talking_Chat_voice_SandboxPath + theFileName
                    path = dataMethodMessage + theFileName
                    //: } else if(path.contains("Documents/User/record/")) {
                } else if path.contains((String(user_limitedMessage.prefix(4)) + "ment" + String(data_assetWritingMsg.prefix(5)) + "r/recor" + String(app_oddData.suffix(2)))) {
                    //: path = Talking_Chat_record_SandboxPath + theFileName
                    path = appPlainFormat + theFileName
                }

                //: let audioId = params["audioId"] ?? "0"
                let audioId = params[(main_belowUrl.lowercased() + String(show_agoMessage))] ?? "0"
                //: if  (audioId as! NSString).integerValue == 0 {
                if (audioId as! NSString).integerValue == 0 {
                    //: do {
                    do {
                        //: let voiceData = try Data.init(contentsOf: URL.init(fileURLWithPath: path))
                        let voiceData = try Data(contentsOf: URL(fileURLWithPath: path))
                        //: params["audio"] = voiceData
                        params[(String(kVisualId))] = voiceData
                        //: } catch {
                    } catch {}
                }
            }
        }

        // 发送消息接口
        //: TalkingChatRequestTool.req_SendPrivete(param: params, completion: { succeed, result, errorModel in
        PouchReactiveCompatible.modeNetwork(param: params, completion: { succeed, result, errorModel in
            //: if !succeed {
            if !succeed {
                //: if type == "QuickMsg" || type == "MultiQuickMsg" {
                if type == (String(k_spaceText.prefix(6)) + String(const_pointData)) || type == (String(app_healthyPath.suffix(6)) + String(noti_noticeFireCellMsg.suffix(7))) {
                    //: self.QuickEndBlock?(false, errorModel?.errorCode ?? 0)
                    self.QuickEndBlock?(false, errorModel?.errorCode ?? 0)
                }
                //: self.msgView.changeCustomMsg(msgData, msgId: nil, msgIncome: NSNumber(value: 0), status: .Msg_Status_Fail)
                self.msgView.outerNameStatus(msgData, modify: nil, storage: NSNumber(value: 0), commonStatus: .Msg_Status_Fail)
                
                //: self.dealSendMsgError(errorCode: errorModel!.errorCode, errorStr: errorModel!.errorMsg, isGift: true, isResend: false, clickEvent: clickMessagesendButton)
                self.decideSlipUp(errorCode: errorModel!.errorCode, errorStr: errorModel!.errorMsg, isGift: true, isResend: false, clickEvent: appPartyKey)
                //: if params.keys.contains("txtId") {
                if params.keys.contains((String(notiStillGivingFormat.prefix(5)))) {
                    //: TalkingPrivateChatManager.chat_changeSaveGreetData(toUserId: self.msgView.targetID, isHave: false)
                    SpanChatManager.ping(toUserId: self.msgView.targetID, isHave: false)
                }
                /// 打招呼达到设定次数失败时
                //: if errorModel!.errorCode == 10015 {
                if errorModel!.errorCode == 10015 {
                    //: self.msgView.deleteCustomMessage(msgData)
                    self.msgView.result(msgData)
                }
                //: return
                return
            }
            //: var updateMsgData = msgData
            var updateMsgData = msgData
            //: let dataDict = result as! Dictionary<String, Any>
            let dataDict = result as! [String: Any]
            //: var msgInfo = dataDict["msgInfo"] as! Dictionary<String, Any>
            var msgInfo = dataDict[(String(dataReadMsg.suffix(7)))] as! [String: Any]

            //: var msgIncome: NSNumber = NSNumber.init(value: 0)
            var msgIncome: NSNumber = .init(value: 0)
            //: if msgInfo.keys.contains("msgIncome") {
            if msgInfo.keys.contains((String(showExploreValue) + k_bowlId.replacingOccurrences(of: "say", with: "e"))) {
                //: msgIncome = msgInfo["msgIncome"] as! NSNumber
                msgIncome = msgInfo[(String(showExploreValue) + k_bowlId.replacingOccurrences(of: "say", with: "e"))] as! NSNumber
            }

            /// 亲密度
            //: self.sendMsgIntimateTool(msgInfo: msgInfo)
            self.hi(msgInfo: msgInfo)

            /// 索要礼物，快速打招呼
            //: let messageType = msgInfo["messageType"] as? Int
            let messageType = msgInfo[(dataRoundAbsolutelyUrl.lowercased() + String(app_loseId.prefix(4)))] as? Int
            //: if messageType == 25 || type == "QuickMsg" || type == "MultiQuickMsg" {
            if messageType == 25 || type == (String(k_spaceText.prefix(6)) + String(const_pointData)) || type == (String(app_healthyPath.suffix(6)) + String(noti_noticeFireCellMsg.suffix(7))) {
                //: if messageType == 25 {
                if messageType == 25 {
                    //: msgInfo["msgType"] = "WantGift"
                    msgInfo[String(bytes: noti_filterPath.map{shoreCounteraction(bright: $0)}, encoding: .utf8)!] = (String(k_conTrackFormat) + userHeartValue.replacingOccurrences(of: "attention", with: "t"))
                    //: uploadRecord.uploadRecordEvent(eventID: clickMessageDetailGiftPopAskBtnSusType)
                    kSaveKey.putAvailable(eventID: app_eventValue)
                    //: } else if type == "QuickMsg" || type == "MultiQuickMsg" {
                } else if type == (String(k_spaceText.prefix(6)) + String(const_pointData)) || type == (String(app_healthyPath.suffix(6)) + String(noti_noticeFireCellMsg.suffix(7))) {
                    //: msgInfo["msgType"] = "txt"
                    msgInfo[String(bytes: noti_filterPath.map{shoreCounteraction(bright: $0)}, encoding: .utf8)!] = (mainRetirementKey.replacingOccurrences(of: "incident", with: "xt"))
                    //: self.QuickEndBlock?(true, errorModel?.errorCode ?? 0)
                    self.QuickEndBlock?(true, errorModel?.errorCode ?? 0)
                }
                //: let dic = ["msgInfo": msgInfo]
                let dic = [(String(dataReadMsg.suffix(7))): msgInfo]
                //: if let cellData = TUISocialChatManager.handleTXMessageSend(withExtral: dic, isSender: true, isTip: false) {
                if let cellData = CivilityReactiveCompatible.smartMailSenderBlockTipHandleTurn(withExtral: dic, isSender: true, isTip: false) {
                    //: updateMsgData = cellData
                    updateMsgData = cellData
                    //: self.msgView.sendCustomMessage(updateMsgData)
                    self.msgView.element(updateMsgData)
                }
            }

            // 更新提示文案
            //: self.userWrap.existSess = true
            self.userWrap.existSess = true
            //: if self.rechargeView.isHidden {
            if self.rechargeView.isHidden {
                //: if FacultyReactiveCompatible.share.loginUserMode.sex == Gender.male.rawValue &&
                if FacultyReactiveCompatible.share.loginUserMode.sex == ATextLiteral.male.rawValue,
                   //: type != "QuickMsg" &&
                   type != (String(k_spaceText.prefix(6)) + String(const_pointData)),
                   //: type != "MultiQuickMsg" {
                   type != (String(app_healthyPath.suffix(6)) + String(noti_noticeFireCellMsg.suffix(7)))
                {
                    //: let key =  "\(FacultyReactiveCompatible.share.loginUserMode.userID)_PrivateChatTipsTitle_\(String(self.msgView.targetID))"
                    let key = "\(FacultyReactiveCompatible.share.loginUserMode.userID)" + (String(const_serverData) + String(appTipUserValue)) + "\(String(self.msgView.targetID))"
                    //: UserDefaults.standard.set(true, forKey: key)
                    UserDefaults.standard.set(true, forKey: key)
                    //: self.removeReplyTipsView()
                    self.reward()
                }
            }
            // 更新本地消息信息
            //: self.msgView.changeCustomMsg(updateMsgData, msgId: msgInfo["msgId"] as? String, msgIncome: msgIncome, status: .Msg_Status_Succ)
            self.msgView.outerNameStatus(updateMsgData, modify: msgInfo[(String(k_earnValue.prefix(5)))] as? String, storage: msgIncome, commonStatus: .Msg_Status_Succ)
            
            //: if dataDict.keys.contains("totalMfCoin") {
            if dataDict.keys.contains((kWealthyPath.replacingOccurrences(of: "effective", with: "to") + "lMfCoin")) {
                //: let totalMfCoin: NSNumber = dataDict["totalMfCoin"] as! NSNumber
                let totalMfCoin: NSNumber = dataDict[(kWealthyPath.replacingOccurrences(of: "effective", with: "to") + "lMfCoin")] as! NSNumber
                //: FacultyReactiveCompatible.share.loginUserMode.mf_coin = totalMfCoin.stringValue
                FacultyReactiveCompatible.share.loginUserMode.mf_coin = totalMfCoin.stringValue
            }

            /// 免费消息提示
            //: if dataDict.keys.contains("attachSysMsg") {
            if dataDict.keys.contains((noti_clickStr.replacingOccurrences(of: "simultaneously", with: "at") + "hSys" + String(user_capValue.suffix(3)))) {
                //: let attachSysMsg: String = dataDict["attachSysMsg"] as? String ?? ""
                let attachSysMsg: String = dataDict[(noti_clickStr.replacingOccurrences(of: "simultaneously", with: "at") + "hSys" + String(user_capValue.suffix(3)))] as? String ?? ""
                //: if attachSysMsg.count>0 {
                if attachSysMsg.count > 0 {
                    //: self.func__privateAttachSys_insertTipMsg(str: attachSysMsg)
                    self.hidden(str: attachSysMsg)
                }
            }

            //: if msgInfo.keys.contains("intimatePhoto") { // 发送成功私密照片 => model，更新本地消息
            if msgInfo.keys.contains((String(data_eastPath) + String(constMarketingId))) { // 发送成功私密照片 => model，更新本地消息
                //: let intimatePhotoDict = msgInfo["intimatePhoto"] as! [String: Any]
                let intimatePhotoDict = msgInfo[(String(data_eastPath) + String(constMarketingId))] as! [String: Any]
                //: if let model = try? AbTalkingChatMsgIntimatePhotoModel(dictionary: intimatePhotoDict) {
                if let model = try? CarryModel(dictionary: intimatePhotoDict) {
                    //: updateMsgData.msgModel.msgInfo.intimatePhoto = model
                    updateMsgData.msgModel.msgInfo.intimatePhoto = model
                    //: self.msgView.changeIntimatePhotoMsg(updateMsgData, photoModel: model)
                    self.msgView.meanSun(updateMsgData, cut: model)
                }

                //: } else if msgInfo.keys.contains("video") { // 发送成功私密视频 => model，更新本地消息
            } else if msgInfo.keys.contains((k_contextMoreFormat.replacingOccurrences(of: "teem", with: "v"))) { // 发送成功私密视频 => model，更新本地消息
                //: let intimateVideoDict = msgInfo["video"] as! [String: Any]
                let intimateVideoDict = msgInfo[(k_contextMoreFormat.replacingOccurrences(of: "teem", with: "v"))] as! [String: Any]
                //: if let model = try? AbTalkingChatMsgVideoModel(dictionary: intimateVideoDict) {
                if let model = try? OthersModel(dictionary: intimateVideoDict) {
                    //: updateMsgData.msgModel.msgInfo.video = model
                    updateMsgData.msgModel.msgInfo.video = model
                    //: self.msgView.changeIntimateVideoMsg(updateMsgData, videoModel: model)
                    self.msgView.takePosition(updateMsgData, tableModel: model)
                }
            }
            //: })
        })

        // 判断是否需要插入音视频提示消息
        //: func__privateChatVideo_insertTipMsg()
        beak()

        //: updateSession()
        flaunt()
    }

    /// 建立会话标识，本地更新数据和会话列表
    //: func updateSession() {
    func flaunt() {
        //: DBUserInfoManager.cache_updateSessionAndPostNotif(with: msgView.targetID)
        DeployMatrixDelegate.reduceWith(with: msgView.targetID)
    }

    /// 处理发送失败情况
    //: private func dealSendMsgError(errorCode: Int, errorStr: String, isGift: Bool, isResend: Bool, clickEvent: String) {
    private func decideSlipUp(errorCode: Int, errorStr: String, isGift _: Bool, isResend _: Bool, clickEvent: String) {
        //: if errorCode == 50203 {
        if errorCode == 50203 { // 余额不足
            //: guard FacultyReactiveCompatible.share.loginUserMode.status != 1 else {
            guard FacultyReactiveCompatible.share.loginUserMode.status != 1 else {
                //: if !errorStr.isEmptyString {
                if !errorStr.isEmptyString {
                    //: micromicronProcess(showMsg: errorStr)
                    micromicronProcess(showMsg: errorStr)
                }
                //: return
                return
            }

            //: DisplayAperturePresenter.share.func__jumpToWebRecharge(clickEvent: clickEvent, sufficient: false)
            DisplayAperturePresenter.share.bilge(clickEvent: clickEvent, sufficient: false)
            //: msgInputView.giftView.dismissView()
            msgInputView.giftView.conCapture()
            //: FacultyReactiveCompatible.share.loginUserMode.freeMsgTimes = ""
            FacultyReactiveCompatible.share.loginUserMode.freeMsgTimes = ""
            //: msgInputView.setHidenFreeMsgTimesView()
            msgInputView.times()
//            insertTipMsg_Nobalance(tipStr: errorStr)
            //: } else if errorCode == 10888 {
        } else if errorCode == 10888 { // vip订阅弹窗
            //: let view = TalkingLiveVipSubscribePopView.init(frame: CGRect(x: 0, y: 0, width: ScreenWidth, height: ScreenHeight))
            let view = ImmediateKeyCollection(frame: CGRect(x: 0, y: 0, width: main_assetUrl, height: kTopUrl))
            //: view.show()
            view.shadowShow()
            //: } else {
        } else {
            //: if !errorStr.isEmptyString {
            if !errorStr.isEmptyString {
                //: micromicronProcess(showMsg: errorStr)
                micromicronProcess(showMsg: errorStr)
            }
        }
    }
}

// MARK: - 亲密度

//: extension TalkingPrivateChatController {
extension BorderInsularViewDelegate {
    /// 解锁图片视频送礼亲密度
    //: private func unlockPhotoOrVideoTool(dataDict: Dictionary<String, Any>) {
    private func verbalDescription(dataDict: [String: Any]) {
        //: let msgInfo = dataDict["msgInfo"] as! Dictionary<String, Any>
        let msgInfo = dataDict[(String(dataReadMsg.suffix(7)))] as! [String: Any]
        /// 亲密度
        //: self.sendMsgIntimateTool(msgInfo: msgInfo)
        self.hi(msgInfo: msgInfo)
    }

    /// 初次展示导航栏的亲密度头像
    //: private func showFirstIntimateView() {
    private func menuContact() {
        //: if self.userWrap.intimate >= FacultyReactiveCompatible.share.appUserConfigMode.intimateLimit.chatTitleEffect && conversationType == .private  {
        if self.userWrap.intimate >= FacultyReactiveCompatible.share.appUserConfigMode.intimateLimit.chatTitleEffect, conversationType == .private {
            //: self.isShowIntimateView = true
            self.isShowIntimateView = true
            //: navView.private_updateIntimateNavView(userModel: userWrap)
            navView.prepare(userModel: userWrap)
            /// 初次展示动效
            //: let key = "\(FacultyReactiveCompatible.share.loginUserMode.userID)_\(String(self.msgView.targetID))"
            let key = "\(FacultyReactiveCompatible.share.loginUserMode.userID)_\(String(self.msgView.targetID))"
            //: var intimateCacheData = (Defaults.array(forKey: TalkingFirstShowIntimateGuide)) as? [String] ?? []
            var intimateCacheData = (mainNameNearbyTitle.array(forKey: notiInfoPingUrl)) as? [String] ?? []
            //: if !intimateCacheData.contains(key) {
            if !intimateCacheData.contains(key) {
                //: navView.playAnimation()
                navView.suborn()
                //: intimateCacheData.append(key)
                intimateCacheData.append(key)
                //: Defaults.set(intimateCacheData, forKey: TalkingFirstShowIntimateGuide)
                mainNameNearbyTitle.set(intimateCacheData, forKey: notiInfoPingUrl)
            }
        }
    }

    /// 收到亲密度消息
    //: @objc func newMsgIntimate(notification: NSNotification) {
    @objc func confirm(notification: NSNotification) {
        //: let data = notification.object as! V2TIMMessage
        let data = notification.object as! V2TIMMessage
        //: let dic = CircularReactiveCompatible.parseTXMessageData(data: data.customElem.data)
        let dic = CircularReactiveCompatible.occurrent(data: data.customElem.data)
        //: let jsonDic = JSON(dic)
        let jsonDic = JSON(dic)
        //: let extra = jsonDic["extra"].dictionary
        let extra = jsonDic[(String(noti_yearFormat))].dictionary
        //: let msgInfo = extra?["msgInfo"]?.dictionary
        let msgInfo = extra?[(String(dataReadMsg.suffix(7)))]?.dictionary
        //: if data.sender == self.msgView.targetID {
        if data.sender == self.msgView.targetID {
            //: if let totalIntimate = msgInfo?["totalIntimate"]?.int {
            if let totalIntimate = msgInfo?[(String(app_componentScoreRestrictionData) + mainMenuTitle.replacingOccurrences(of: "problem", with: "e"))]?.int {
                //: if self.conversationType == .private {
                if self.conversationType == .private {
                    //: let time = data.timestamp.timeIntervalSince1970
                    let time = data.timestamp.timeIntervalSince1970
                    //: self.intimateTool(currenIntimate: totalIntimate, time: Int(time))
                    self.read(currenIntimate: totalIntimate, time: Int(time))
                }
            }
        }
    }

    /// 发送消息时
    //: private func sendMsgIntimateTool(msgInfo: Dictionary<String, Any>) {
    private func hi(msgInfo: [String: Any]) {
        //: if msgInfo.keys.contains("totalIntimate") && self.conversationType == .private {
        if msgInfo.keys.contains((String(app_componentScoreRestrictionData) + mainMenuTitle.replacingOccurrences(of: "problem", with: "e"))), self.conversationType == .private {
            //: let totalIntimacy = msgInfo["totalIntimate"] as? Int ?? 0
            let totalIntimacy = msgInfo[(String(app_componentScoreRestrictionData) + mainMenuTitle.replacingOccurrences(of: "problem", with: "e"))] as? Int ?? 0
            //: self.intimateTool(currenIntimate: totalIntimacy)
            self.read(currenIntimate: totalIntimacy)
        }
    }

    /// 亲密度操作
    //: func intimateTool(currenIntimate: Int, time: Int = 0) {
    func read(currenIntimate: Int, time: Int = 0) {
        //: guard currenIntimate > 0 else { return }
        guard currenIntimate > 0 else { return }
        //: let model = DBUserInfoManager.cache_getCachedUserInfo(targetId: self.msgView.targetID)
        let model = DeployMatrixDelegate.biologicalAgent(targetId: self.msgView.targetID)
        //: self.userWrap.intimate = currenIntimate
        self.userWrap.intimate = currenIntimate
        /// 满足最低条件，展示页面
        //: if currenIntimate >= FacultyReactiveCompatible.share.appUserConfigMode.intimateLimit.chatTitleEffect {
        if currenIntimate >= FacultyReactiveCompatible.share.appUserConfigMode.intimateLimit.chatTitleEffect {
            /// 亲密度增加
            //: if !self.isShowIntimateView {
            if !self.isShowIntimateView {
                //: self.isShowIntimateView = true
                self.isShowIntimateView = true
                //: navView.private_updateIntimateNavView(userModel: self.userWrap)
                navView.prepare(userModel: self.userWrap)
            }
            //: if currenIntimate - (model?.intimate ?? 0) > 0 {
            if currenIntimate - (model?.intimate ?? 0) > 0 {
                //: self.navView.playIntimateAnim(intimate: currenIntimate - (model?.intimate ?? 0))
                self.navView.moment(intimate: currenIntimate - (model?.intimate ?? 0))
                /// 播放完毕
                //: self.navView.animEndBlock = { [weak self] in
                self.navView.animEndBlock = { [weak self] in
                    //: guard let self = self else { return }
                    guard let self = self else { return }
                    //: self.navView.setAllData(intimate: currenIntimate)
                    self.navView.rewardRetain(intimate: currenIntimate)
                }
                //: } else {
            } else {
                //: self.navView.setAllData(intimate: currenIntimate)
                self.navView.rewardRetain(intimate: currenIntimate)
            }
        }

        //: let currTime = time > 0 ? time: Int(Date().timeIntervalSince1970)
        let currTime = time > 0 ? time : Int(Date().timeIntervalSince1970)
        //: DBUserInfoManager.cache_updateIntimate(with: msgView.targetID, intimate: currenIntimate, currTime: currTime)
        DeployMatrixDelegate.totalimate(with: msgView.targetID, intimate: currenIntimate, currTime: currTime)
        //: let userInfo = ["uid": self.msgView.targetID ?? "",
        let userInfo = [String(bytes: appTarData.map{floorCombine(productivity: $0)}, encoding: .utf8)!: self.msgView.targetID ?? "",
                        //: "intimate": currenIntimate] as [String : Any]
                        String(bytes: mainLetterKey.map{oldWould(female: $0)}, encoding: .utf8)!: currenIntimate] as [String: Any]
        //: NotificationCenter.default.post(name: MSGLIST_UPDATE_INTIMATE_NOTIFICATION,
        NotificationCenter.default.post(name: mainSampleTitle,
                                        //: object: nil,
                                        object: nil,
                                        //: userInfo: userInfo)
                                        userInfo: userInfo)
    }
}

// MARK: - 输入框

//: extension TalkingPrivateChatController: ChatInputViewDelegate {
extension BorderInsularViewDelegate: IonSignalViewDelegate {
    //: func func_inputContentIntervalOneSecond() {
    func spell() {}

    //: func func__selectMentionUser(range: NSRange) {}
    func degree(range _: NSRange) {}

    //: func func__selectMoreFunAction(actionType: MoreActionType) {
    func componentBy(actionType: BorderControlAdditiveArithmetic) {
        //: switch actionType {
        switch actionType {
        //: case .Photo:
        case .Photo:
            //: if self.conversationType == .private {
            if self.conversationType == .private {
                //: if self.userWrap.intimate >= FacultyReactiveCompatible.share.appUserConfigMode.intimateLimit.sendImg {
                if self.userWrap.intimate >= FacultyReactiveCompatible.share.appUserConfigMode.intimateLimit.sendImg {
                    //: func__choicePhotos()
                    feedback()
                    //: } else {
                } else {
                    //: showIntimateErrorStr()
                    askStr()
                }
                //: } else {
            } else {
                //: func__choicePhotos()
                feedback()
            }
        //: case .VideoCall:
        case .VideoCall: // 音/视频通话
            //: var videoStr: String?
            var videoStr: String?
            //: var voiceStr: String?
            var voiceStr: String?
            //: if FacultyReactiveCompatible.share.loginUserMode.loungePlus {
            if FacultyReactiveCompatible.share.loginUserMode.loungePlus { // 男性会员
                //: videoStr = self.userWrap.videoVIPPrice
                videoStr = self.userWrap.videoVIPPrice
                //: voiceStr = self.userWrap.voiceVIPPrice
                voiceStr = self.userWrap.voiceVIPPrice
                //: } else {
            } else {
                //: videoStr = self.userWrap.videoPrice
                videoStr = self.userWrap.videoPrice
                //: voiceStr = self.userWrap.voicePrice
                voiceStr = self.userWrap.voicePrice
            }
            //: if self.userWrap.intimate >= FacultyReactiveCompatible.share.appUserConfigMode.intimateLimit.requestCall {
            if self.userWrap.intimate >= FacultyReactiveCompatible.share.appUserConfigMode.intimateLimit.requestCall {
                //: videoCallManager.showVideoCallMenu(videoPrice: videoStr, voicePrice: voiceStr)
                videoCallManager.behindFront(videoPrice: videoStr, voicePrice: voiceStr)
                //: } else {
            } else {
                //: showIntimateErrorStr()
                askStr()
            }

        //: case .ShortVideo:
        case .ShortVideo: // 短视频
            //: if self.conversationType == .private {
            if self.conversationType == .private {
                //: if self.userWrap.intimate >= FacultyReactiveCompatible.share.appUserConfigMode.intimateLimit.sendVideo {
                if self.userWrap.intimate >= FacultyReactiveCompatible.share.appUserConfigMode.intimateLimit.sendVideo {
                    //: func__sendShortVideo()
                    readyPostCircle()
                    //: } else {
                } else {
                    //: showIntimateErrorStr()
                    askStr()
                }
                //: } else {
            } else {
                //: func__sendShortVideo()
                readyPostCircle()
            }

        //: case .Gift: break
        case .Gift: break

        //: case .Game:
        case .Game: // 游戏
            //: gamesView.showView(from: .PrivateChat)
            gamesView.priority(from: .PrivateChat)
        }
    }

    //: private func showIntimateErrorStr() {
    private func askStr() {
        //: self.micromicronProcess(showMsg: "Can't use the function due to your lack of intimacy level.".localized)
        self.micromicronProcess(showMsg: String(bytes: const_referMessage.map{userVery(mirror: $0)}, encoding: .utf8)!.localized)
    }

    //: func func__sendChatRoomGiftMsg(giftModel: TalkingRoomGiftModel, giftNum: String, model: TalkingChatRoomMemberModel) {}
    func copernicanSystemA(giftModel _: ReportedMeasurable, giftNum _: String, model _: RocketTransformable) {}

    // 聊天面板送礼
    /// - Parameters:
    ///   - giftModel: 礼物模型
    ///   - giftNum: 礼物数量
    //: func func__sendGiftMsg(giftModel: TalkingRoomGiftModel, giftNum: String) {
    func pointTrack(giftModel: ReportedMeasurable, giftNum: String) {
        //: func__sendGiftMsg(giftModel: giftModel, giftNum: giftNum, cellData: nil, fromStr: nil)
        nearness(giftModel: giftModel, giftNum: giftNum, cellData: nil, fromStr: nil)
    }

    //: func func__sendGiftMsg(giftModel: TalkingRoomGiftModel, giftNum: String, cellData: CommandTypeTag?, fromStr: String?) {
    func nearness(giftModel: ReportedMeasurable, giftNum: String, cellData: CommandTypeTag?, fromStr: String?) {
        //: let reachability = try? Reachability()
        let reachability = try? Reachability()
        //: if reachability?.connection == .unavailable {
        if reachability?.connection == .unavailable {
            //: micromicronProcess(showMsg: kNetErrorMsg)
            micromicronProcess(showMsg: user_robotCameraName)
            //: return
            return
        }
        //: var params: Dictionary<String, Any> = [:]
        var params: [String: Any] = [:]
        //: params["toUid"] = self.msgView.targetID
        params[(String(show_occurTitle.suffix(5)))] = self.msgView.targetID
        //: params["giftId"] = giftModel.gid
        params[(String(userPetitionTitle))] = giftModel.gid
        //: params["giftNum"] = giftNum
        params[(String(appFileRecordData.prefix(7)))] = giftNum
        //: if giftModel.pkgItemsetId != nil {
        if giftModel.pkgItemsetId != nil {
            //: params["pkgItemsetId"] = giftModel.pkgItemsetId
            params[(String(kTrustUrl.prefix(5)) + "emsetId")] = giftModel.pkgItemsetId
        }
        //: if fromStr != nil {
        if fromStr != nil {
            //: params["from"] = fromStr
            params[(String(k_natureValue))] = fromStr
        }
        // 送礼接口
        //: TalkingChatRequestTool.req_SendGiftPrivete(param: params, completion: { succeed, result, errorModel in
        PouchReactiveCompatible.streetSmartCompletion(param: params, completion: { succeed, result, errorModel in
            //: guard succeed else {
            guard succeed else {
                //: self.dealSendMsgError(errorCode: errorModel!.errorCode, errorStr: errorModel!.errorMsg, isGift: true, isResend: false, clickEvent: clickSendingiftsButton)
                self.decideSlipUp(errorCode: errorModel!.errorCode, errorStr: errorModel!.errorMsg, isGift: true, isResend: false, clickEvent: user_turnName)
                //: return
                return
            }

            // 更新本地礼物背包
            //: if giftModel.pkgItemsetId != nil {
            if giftModel.pkgItemsetId != nil {
                //: self.msgInputView.giftView.bags_removeGiftPackage(pkgItemsetId: giftModel.pkgItemsetId!, num: Int(giftNum) ?? 0)
                self.msgInputView.giftView.punish(pkgItemsetId: giftModel.pkgItemsetId!, num: Int(giftNum) ?? 0)
            }
            //: self.func__insertGiftMessageWithExtral(extral: result as! Dictionary<String, Any?>, isFromInputView: true)
            self.general(extral: result as! [String: Any?], isFromInputView: true)
            //: self.updateSession()
            self.flaunt()
            /// 亲密度
            //: let extralInfo = result as! Dictionary<String, Any?>
            let extralInfo = result as! [String: Any?]
            //: let msgInfoDic: Dictionary<String, Any> = extralInfo["msgInfo"] as! Dictionary<String, Any>
            let msgInfoDic: [String: Any] = extralInfo[(String(dataReadMsg.suffix(7)))] as! [String: Any]
            //: if msgInfoDic.keys.contains("totalIntimate") && self.conversationType == .private {
            if msgInfoDic.keys.contains((String(app_componentScoreRestrictionData) + mainMenuTitle.replacingOccurrences(of: "problem", with: "e"))), self.conversationType == .private {
                //: let totalIntimacy = msgInfoDic["totalIntimate"] as? Int ?? 0
                let totalIntimacy = msgInfoDic[(String(app_componentScoreRestrictionData) + mainMenuTitle.replacingOccurrences(of: "problem", with: "e"))] as? Int ?? 0
                //: self.intimateTool(currenIntimate: totalIntimacy)
                self.read(currenIntimate: totalIntimacy)
            }
            /// 索要礼物送礼
            //: if cellData != nil {
            if cellData != nil {
                // 修改状态
                //: self.msgView.changeIntimateGiftMsg(cellData)
                self.msgView.giftMsg(cellData)
                //: uploadRecord.uploadRecordEvent(eventID: ClickmessageDetailaskMsgsendBtn)
                kSaveKey.putAvailable(eventID: show_indexDetailUrl)
            }
            //: })
        })
    }

    /// 索要礼物
    //: func func__askforGiftMsg(giftModel: TalkingRoomGiftModel, giftNum: String) {
    func profileNum(giftModel: ReportedMeasurable, giftNum _: String) {
        //: uploadRecord.uploadRecordEvent(eventID: ClickMessageDetailGiftPopAskBtnAllType)
        kSaveKey.putAvailable(eventID: app_failureMsg)
        //: let reachability = try? Reachability()
        let reachability = try? Reachability()
        //: if reachability?.connection == .unavailable {
        if reachability?.connection == .unavailable {
            //: micromicronProcess(showMsg: kNetErrorMsg)
            micromicronProcess(showMsg: user_robotCameraName)
            //: return
            return
        }
        //: guard self.userWrap.intimate >= FacultyReactiveCompatible.share.appUserConfigMode.intimateLimit.wantGift else {
        guard self.userWrap.intimate >= FacultyReactiveCompatible.share.appUserConfigMode.intimateLimit.wantGift else {
            //: showIntimateErrorStr()
            askStr()
            //: return
            return
        }
        //: var params: Dictionary<String, Any> = [:]
        var params: [String: Any] = [:]
        //: params["wantGiftId"] = giftModel.gid
        params[(dataCountervalTitle.replacingOccurrences(of: "until", with: "n") + String(constExtraResolutionId))] = giftModel.gid
        //: params["type"] = "WantGiftMsg"
        params[(kExitStr.replacingOccurrences(of: "timing", with: "y"))] = (String(user_homeName.suffix(5)) + "iftMsg")

        //: let msgInfo = ["msgType": "WantGift"]
        let msgInfo = [String(bytes: noti_filterPath.map{shoreCounteraction(bright: $0)}, encoding: .utf8)!: (String(k_conTrackFormat) + userHeartValue.replacingOccurrences(of: "attention", with: "t"))]
        //: let dic = ["msgInfo": msgInfo, "params": params]
        let dic = [(String(dataReadMsg.suffix(7))): msgInfo, String(bytes: showAbsoluteStr.reversed(), encoding: .utf8)!: params]
        //: if let cellData = TUISocialChatManager.handleTXMessageSend(withExtral: dic, isSender: true, isTip: false) {
        if let cellData = CivilityReactiveCompatible.smartMailSenderBlockTipHandleTurn(withExtral: dic, isSender: true, isTip: false) {
            //: func__sendPriveteMsg(msgData: cellData, reSend: false)
            clear(msgData: cellData, reSend: false)
        }
    }

    //: func heightToBottomChanged(heightToBottom: CGFloat) {
    func low(heightToBottom: CGFloat) {
        //: if self.isFrom == .LiveRoom {
        if self.isFrom == .LiveRoom {
            //: contentView.snp.updateConstraints { make in
            contentView.snp.updateConstraints { make in
                //: make.top.equalTo(HalfViewTopMargin-heightToBottom)
                make.top.equalTo(data_windowMsg - heightToBottom)
            }
        }
        //: msgInputView.snp.updateConstraints { make in
        msgInputView.snp.updateConstraints { make in
            //: make.bottom.equalTo(self.view).offset(-heightToBottom)
            make.bottom.equalTo(self.view).offset(-heightToBottom)
        }
        //: DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 0.3) {
        DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 0.3) {
            //: self.msgView.parent(true)
            self.msgView.parent(true)
        }
    }

    //: func inputViewHeightChanged(height: CGFloat) {
    func build(height: CGFloat) {
        //: msgInputView.snp.updateConstraints { make in
        msgInputView.snp.updateConstraints { make in
            //: make.height.equalTo(height)
            make.height.equalTo(height)
        }
        //: DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 0.3) {
        DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 0.3) {
            //: self.msgView.parent(true)
            self.msgView.parent(true)
        }
    }

    //: @objc override func func__TapGestureRecognizer(_ tap: UITapGestureRecognizer) {
    @objc override func memory(_ tap: UITapGestureRecognizer) {
        //: super.func__TapGestureRecognizer(tap)
        super.memory(tap)
        //: if self.isFrom == .LiveRoom {
        if self.isFrom == .LiveRoom {
            //: let location = tap.location(ofTouch: 0, in: self.contentView)
            let location = tap.location(ofTouch: 0, in: self.contentView)
            //: if !contentView.point(inside: location, with: nil) {
            if !contentView.point(inside: location, with: nil) {
                // 点击列表以外区域，移除视图（包括聊天列表）
                //: half_dismissViewAnimation(true)
                singleFile(true)
            }
        }
    }

    /// 开始录音操作
    //: func func__starRecordBtnDown() {
    func pointThread() {
        //: if TalkingVoiceMsgPlayManager.shared.playStatus == .Playing {
        if LargeObjectProtocol.shared.playStatus == .Playing {
            //: TalkingVoiceMsgPlayManager.shared.stopAudioPlayer()
            LargeObjectProtocol.shared.narrowPlayer()
        }
    }
}

// MARK: - 动效

//: extension TalkingPrivateChatController {
extension BorderInsularViewDelegate {
    /// 插入礼物消息，播放动效
    /// - Parameters:
    ///   - extral: 送礼接口返回结果
    ///   - isFromInputView: 是否来自输入框送礼入口
    //: func func__insertGiftMessageWithExtral(extral: Dictionary<String, Any?>, isFromInputView: Bool) {
    func general(extral: [String: Any?], isFromInputView: Bool) {
        //: var extralInfo = extral
        var extralInfo = extral
        //: var msgInfoDic: Dictionary<String, Any> = extralInfo["msgInfo"] as! Dictionary<String, Any>
        var msgInfoDic: [String: Any] = extralInfo[(String(dataReadMsg.suffix(7)))] as! [String: Any]
        //: msgInfoDic["msgType"] = "gift"
        msgInfoDic[String(bytes: noti_filterPath.map{shoreCounteraction(bright: $0)}, encoding: .utf8)!] = (data_youUrl.replacingOccurrences(of: "bottom", with: "ft"))
        //: extralInfo["msgInfo"] = msgInfoDic
        extralInfo[(String(dataReadMsg.suffix(7)))] = msgInfoDic

        //: let cellData = TUISocialChatManager.handleTXMessageSend(withExtral: extralInfo as [String: Any], isSender: true, isTip: false)
        let cellData = CivilityReactiveCompatible.smartMailSenderBlockTipHandleTurn(withExtral: extralInfo as [String: Any], isSender: true, isTip: false)
        //: self.msgView.sendCustomMessage(cellData)
        self.msgView.element(cellData)
        //: var msgIncome: NSNumber = NSNumber.init(floatLiteral: 0)
        var msgIncome = NSNumber(floatLiteral: 0)
        //: if msgInfoDic.keys.contains("msgIncome") {
        if msgInfoDic.keys.contains((String(showExploreValue) + k_bowlId.replacingOccurrences(of: "say", with: "e"))) {
            //: msgIncome = msgInfoDic["msgIncome"] as! NSNumber
            msgIncome = msgInfoDic[(String(showExploreValue) + k_bowlId.replacingOccurrences(of: "say", with: "e"))] as! NSNumber
        }
        //: self.msgView.changeCustomMsg(cellData, msgId: msgInfoDic["msgId"] as? String, msgIncome: msgIncome, status: .Msg_Status_Succ)
        self.msgView.outerNameStatus(cellData, modify: msgInfoDic[(String(k_earnValue.prefix(5)))] as? String, storage: msgIncome, commonStatus: .Msg_Status_Succ)
        
        //: sendGiftByMeToEfffecct(giftDic: extral as [String: Any])
        awakened(giftDic: extral as [String: Any])
        //: if extralInfo.keys.contains("totalMfCoin") {
        if extralInfo.keys.contains((kWealthyPath.replacingOccurrences(of: "effective", with: "to") + "lMfCoin")) {
            //: let totalMfCoin: NSNumber = extralInfo["totalMfCoin"] as! NSNumber
            let totalMfCoin: NSNumber = extralInfo[(kWealthyPath.replacingOccurrences(of: "effective", with: "to") + "lMfCoin")] as! NSNumber
            //: FacultyReactiveCompatible.share.loginUserMode.mf_coin = totalMfCoin.stringValue
            FacultyReactiveCompatible.share.loginUserMode.mf_coin = totalMfCoin.stringValue
        }
        //: if isFromInputView {
        if isFromInputView {
            //: msgInputView.giftView.updateGiftInfo(needReload: false, mf_coin: FacultyReactiveCompatible.share.loginUserMode.mf_coin)
            msgInputView.giftView.pictureUp(needReload: false, mf_coin: FacultyReactiveCompatible.share.loginUserMode.mf_coin)
        }
    }

    //: func initCachEffectData() {
    func taskMin() {
        //: giftEffectView.initCachAnimatData(tagetID: msgView.targetID)
        giftEffectView.containment(tagetID: msgView.targetID)
    }

    //: func sendGiftByMeToEfffecct(giftDic: Dictionary<String, Any>) {
    func awakened(giftDic: [String: Any]) {
        //: giftEffectView.dealGiftAnimatData(dict: giftDic)
        giftEffectView.query(dict: giftDic)
    }

    //: @objc func addGiftEffectModelArr(giftArr: Array<Any>) {
    @objc func panStopHold(giftArr: [Any]) {
        //: giftEffectView.addGiftAnimatModelArr(modelArr: giftArr)
        giftEffectView.close(modelArr: giftArr)
    }

    //: @objc func getChatVCTargetId() ->String {
    @objc func logical() -> String {
        //: return msgView.targetID
        return msgView.targetID
    }
}

// MARK: - 音视频通话Event

//: extension TalkingPrivateChatController {
extension BorderInsularViewDelegate {
    /// 检查权限后发起音频通话
    //: func checkAndCallVoice() {
    func bottom() {
        //: videoCallManager.checkAndCallVoice {[weak self] succeed in
        videoCallManager.queryUpEnableForComplection { [weak self] succeed in
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: guard succeed else { return }
            guard succeed else { return }
            //: self.updateSession()
            self.flaunt()
        }
    }

    /// 检测权限后发起视频通话
    //: func checkAndCallVideo() {
    func rating() {
        //: videoCallManager.checkAndCallVideo {[weak self] succeed in
        videoCallManager.advertising { [weak self] succeed in
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: guard succeed else { return }
            guard succeed else { return }
            //: self.updateSession()
            self.flaunt()
        }
    }
}

// MARK: - Event

//: extension TalkingPrivateChatController {
extension BorderInsularViewDelegate {
    //: @objc private func feedbackBtnClick() {
    @objc private func executeRange() {
        //: let vc = TalkingFeedbackVC.init()
        let vc = AttachViewController()
        //: getNavigationController()?.pushViewController(vc, animated: true)
        shorePast()?.pushViewController(vc, animated: true)
    }

    /// 更多按钮事件
    //: private func moreBtnClick() {
    private func suspendAcross() {
        //: let vc = TalkingMunuPopView.init(frame: self.view.frame)
        let vc = ExploreDataSource(frame: self.view.frame)
        //: vc.initwithList(cellTitleList: ["View info".localized, "Notes".localized, "Report".localized])
        vc.footList(cellTitleList: [(String(k_shouldMessage) + String(noti_bowlRecoverValue.prefix(4)) + "info").localized, (String(dataHelpQueryedName.suffix(5))).localized, (String(main_beautyTheMsg)).localized])
        //: vc.cancelColor = .mottleResult()
        vc.cancelColor = .mottleResult()
        //: vc.munuBlock = { [weak self] index, str in
        vc.munuBlock = { [weak self] index, _ in
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: if index == 0 {
            if index == 0 {
                //: DisplayAperturePresenter.share.func__pushToUserDetailVC(uid: self.msgView.targetID)
                DisplayAperturePresenter.share.numbererest(uid: self.msgView.targetID)

                //: } else if index == 1 {
            } else if index == 1 { // 备注
                //: jumpToNotesView()
                underBackground()

                //: } else if index == 2 {
            } else if index == 2 { // 举报
                //: self.msgInputView.showOtherInputView = true
                self.msgInputView.showOtherInputView = true
                //: let reportView = TalkingReportAlertView.init(frame: UIScreen.main.bounds, type: .reportUserType, rId: self.msgView.targetID)
                let reportView = OccurrenceView(frame: UIScreen.main.bounds, type: .reportUserType, rId: self.msgView.targetID)
                //: reportView.showReportViewIn(view: nil)
                reportView.nearbyDisplay(view: nil)
                //: reportView.reportHideBlock = { _ in
                reportView.reportHideBlock = { _ in
                    //: self.msgInputView.showOtherInputView = false
                    self.msgInputView.showOtherInputView = false
                }
            }
        }
    }

    /// 跳转到备注页面
    //: private func jumpToNotesView() {
    private func underBackground() {
        //: self.msgInputView.showOtherInputView = true
        self.msgInputView.showOtherInputView = true
        //: let view = PrivateChatNotesView()
        let view = PriorityViewDelegate()
        //: view.show(self.userWrap.content, isTop: self.userWrap.top, toUid: self.msgView.targetID)
        view.picCap(self.userWrap.content, isTop: self.userWrap.top, toUid: self.msgView.targetID)
        //: view.dismissBlock = { content, isTop in
        view.dismissBlock = { content, isTop in
            //: self.msgInputView.showOtherInputView = false
            self.msgInputView.showOtherInputView = false
            //: self.userWrap.content = content
            self.userWrap.content = content
            //: self.userWrap.top = isTop
            self.userWrap.top = isTop
            //: self.notesTopView.refresh(self.userWrap)
            self.notesTopView.timing(self.userWrap)
        }
    }

    /// 返回按钮点击事件
    //: private func backButtonClick() {
    private func redundantBlind() {
        //: if self.isFrom == .LiveRoom {
        if self.isFrom == .LiveRoom {
            //: half_dismissViewAnimation(false)
            singleFile(false)
            //: } else {
        } else {
            //: getNavigationController()?.popViewController(animated: true)
            shorePast()?.popViewController(animated: true)
        }
    }

    /// 展示飘屏内容
    //: @objc private func showFloatScreen(notification: NSNotification) {
    @objc private func foursquare(notification: NSNotification) {
        //: if conversationType == .private {
        if conversationType == .private {
            //: let dataInfo = notification.userInfo as! [String: AnyObject]
            let dataInfo = notification.userInfo as! [String: AnyObject]
            //: if let model = TalkingGameFloatingScreenModel.deserialize(from: dataInfo as? Dictionary) {
            if let model = NameTheoryMeasurable.deserialize(from: dataInfo as? Dictionary) {
                //: self.floatScreenView.isUserInteractionEnabled = true
                self.floatScreenView.isUserInteractionEnabled = true
                //: self.floatScreenView.didReceive(model)
                self.floatScreenView.strip(model)
            }
        }
    }
}

// MARK: - VIP消息已读回执

//: extension TalkingPrivateChatController {
extension BorderInsularViewDelegate {
    /// 添加通知
    //: private func addReadPeceiptNotification() {
    private func counteractionNotification() {
        //: NotificationCenter.default.addObserver(self,
        NotificationCenter.default.addObserver(self,
                                               //: selector: #selector(func__msgReadReceipt(notification:)),
                                               selector: #selector(schedule(notification:)),
                                               //: name: CHAT_MSG_READ_RECEIPT_NOTIFICATION,
                                               name: appKeyPath,
                                               //: object: nil)
                                               object: nil)

        //: NotificationCenter.default.addObserver(self,
        NotificationCenter.default.addObserver(self,
                                               //: selector: #selector(func__refreshMsgList),
                                               selector: #selector(listProcessReload),
                                               //: name: USER_UPDATE_LOUNGEPLUSINFO_NOTIFICATION,
                                               name: userMaxStr,
                                               //: object: nil)
                                               object: nil)
    }

    /// 收到对端已读消息回执通知 -> 刷新列表
    //: @objc func func__msgReadReceipt(notification: NSNotification) {
    @objc func schedule(notification: NSNotification) {
        //: let obj = notification.object as! V2TIMMessageReceipt
        let obj = notification.object as! V2TIMMessageReceipt
        //: guard obj.userID == msgView.targetID else { return }
        guard obj.userID == msgView.targetID else { return }
        //: guard FacultyReactiveCompatible.share.loginUserMode.loungePlus == true else { return }
        guard FacultyReactiveCompatible.share.loginUserMode.loungePlus == true else { return }
        //: DispatchQueue.main.async {
        DispatchQueue.main.async {
            //: self.msgView.tableView.reloadData()
            self.msgView.tableView.reloadData()
        }
    }

    /// 订阅成功 -> 更改订阅状态 -> 刷新列表
    //: @objc private func func__refreshMsgList() {
    @objc private func listProcessReload() {
        //: FacultyReactiveCompatible.share.loginUserMode.loungePlus = true
        FacultyReactiveCompatible.share.loginUserMode.loungePlus = true
        //: userWrap.isRecharge = 1
        userWrap.isRecharge = 1
        //: msgView.isRecharge = userWrap.isRecharge
        msgView.isRecharge = userWrap.isRecharge
        //: refreshRechargeRequiredView()
        unitedNationsAgency()
        //: DispatchQueue.main.async {
        DispatchQueue.main.async {
            //: self.msgView.tableView.reloadData()
            self.msgView.tableView.reloadData()
        }
    }

    /// 判断是否插入已读回执引导充值VIP提示消息
    //: func func__privateVIPReadReceipt_insertTipsMsg() {
    func past() {
        //: DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 0.4) {
        DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 0.4) {
            //: guard let lastCellData = self.msgView.uiMsgs.lastObject as? AgoneCellData else { return }
            guard let lastCellData = self.msgView.uiMsgs.lastObject as? AgoneCellData else { return }
            //: if TalkingPrivateChatManager.needInsertVIPReadReceiptMsg(cellData: lastCellData) {
            if SpanChatManager.totalerest(cellData: lastCellData) {
                // 只做内存插入展示
                //: self.insertTipMsg_Nobalance(tipStr: "View read receipts?".localized, needInsertLocal: false)
                self.imbedPrivacy(tipStr: (String(k_bulletText.suffix(4)) + " read rec" + String(data_ofName.suffix(6))).localized, needInsertLocal: false)
            }
        }
    }
}

// MARK: - 选择图片

//: extension TalkingPrivateChatController: UIImagePickerControllerDelegate, UINavigationControllerDelegate {
extension BorderInsularViewDelegate: UIImagePickerControllerDelegate, UINavigationControllerDelegate {
    //: private func func__choicePhotos() {
    private func feedback() {
        //: TalkingPermissionTool.func__openAlbumServiceWithBlock(true) { (isOpen: Bool) in
        ActivityReactiveCompatible.oddCharm(true) { (isOpen: Bool) in
            //: if isOpen {
            if isOpen {
                //: let vc = TalkingImagePickTool.initImagePickerVc(maxCount: 1, allowPhoto: true, allowVideo: false)
                let vc = ThreeToolReactiveCompatible.interval(maxCount: 1, allowPhoto: true, allowVideo: false)
                //: vc.modalPresentationStyle = .fullScreen
                vc.modalPresentationStyle = .fullScreen
                //: self.present(vc, animated: true)
                self.present(vc, animated: true)
                //: vc.didFinishPickingPhotosHandle = { [weak self] ( photos: Array<UIImage>?,
                vc.didFinishPickingPhotosHandle = { [weak self] (photos: [UIImage]?,
                                                                 //: assets: Array<Any>?,
                                                                 _: [Any]?,
                                                                 //: isSelectOriginalPhoto: Bool?)  in
                                                                 _: Bool?) in
                        //: guard let self = self else {return}
                        guard let self = self else { return }
                        //: if photos!.count > 0 {
                        if photos!.count > 0 {
                            //: self.showUsePhoto(img: photos![0])
                            self.alive(img: photos![0])
                        }
                }
            }
        }
    }

    //: private func func__camaraPhotos() {
    private func formPhotos() {
        //: TalkingPermissionTool.func__openCaptureDeviceServiceWithBlock(true) { (isOpen: Bool) in
        ActivityReactiveCompatible.dataConverter(true) { (isOpen: Bool) in
            //: if isOpen {
            if isOpen {
                //: let imagePicker = UIImagePickerController()
                let imagePicker = UIImagePickerController()
                //: imagePicker.sourceType = .camera
                imagePicker.sourceType = .camera
                //: imagePicker.delegate = self
                imagePicker.delegate = self
                //: self.present(imagePicker, animated: true, completion: {})
                self.present(imagePicker, animated: true, completion: {})
            }
        }
    }

    //: func imagePickerController(_ picker: UIImagePickerController, didFinishPickingMediaWithInfo info: [UIImagePickerController.InfoKey: Any]) {
    func imagePickerController(_: UIImagePickerController, didFinishPickingMediaWithInfo info: [UIImagePickerController.InfoKey: Any]) {
        //: self.dismiss(animated: true) {
        self.dismiss(animated: true) {
            //: if let photo = info[UIImagePickerController.InfoKey.originalImage] as! UIImage? {
            if let photo = info[UIImagePickerController.InfoKey.originalImage] as! UIImage? {
                //: self.showUsePhoto(img: photo)
                self.alive(img: photo)
            }
        }
    }

    //: func imagePickerControllerDidCancel(_ picker: UIImagePickerController) {
    func imagePickerControllerDidCancel(_: UIImagePickerController) {
        //: self.dismiss(animated: true, completion: nil)
        self.dismiss(animated: true, completion: nil)
    }

    /// 展示阅后即焚+私密照片选项
    /// - Parameter img: 图片资源
    //: private func showUsePhoto(img: UIImage) {
    private func alive(img: UIImage) {
        //: guard conversationType == .private &&
        guard conversationType == .private &&
            //: FacultyReactiveCompatible.share.appStatus == AppSkinStatus.normal.rawValue else {
            FacultyReactiveCompatible.share.appStatus == PenConstantTarget.normal.rawValue
        else { // 非私聊 && 非审核模式
            //: let sendModel = AbTalkingChatMsgCustomSendModel()
            let sendModel = HintModel()
            //: sendModel.isBurn = "0"
            sendModel.isBurn = "0"
            //: sendModel.giftId = "0"
            sendModel.giftId = "0"
            //: self.func__sendPictureMsg(img: img, sendModel: sendModel)
            self.echogram(img: img, sendModel: sendModel)
            //: return
            return
        }

        //: let vc = TalkingMomentPhotosVC(images: [img], index: 0, type: .sendIntimatePhoto)
        let vc = SpanScreenVc(images: [img], index: 0, type: .sendIntimatePhoto)
        //: vc.sendIntimatePhotoBlock = {[weak self] isBurn, giftId in
        vc.sendIntimatePhotoBlock = { [weak self] isBurn, giftId in
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: let sendModel = AbTalkingChatMsgCustomSendModel()
            let sendModel = HintModel()
            //: sendModel.isBurn = isBurn
            sendModel.isBurn = isBurn
            //: sendModel.giftId = giftId
            sendModel.giftId = giftId
            //: self.func__sendPictureMsg(img: img, sendModel: sendModel)
            self.echogram(img: img, sendModel: sendModel)
        }
        //: getNavigationController()?.pushViewController(vc, animated: true)
        shorePast()?.pushViewController(vc, animated: true)
    }
}

// MARK: - 选择视频

//: extension TalkingPrivateChatController {
extension BorderInsularViewDelegate {
    /// 发送短视频
    //: private func func__sendShortVideo() {
    private func readyPostCircle() {
        // 检测用户短视频版本是否可用
        //: guard TalkingPrivateChatManager.checkShortVideo(userVersion: self.userWrap.version) else {
        guard SpanChatManager.thumb(userVersion: self.userWrap.version) else {
            //: let gender = self.userWrap.sex == Gender.male.rawValue ? "He".localized:"She".localized
            let gender = self.userWrap.sex == ATextLiteral.male.rawValue ? "He".localized : (String(showAssistantMessage.suffix(3))).localized
            //: self.micromicronProcess(showMsg: "%@ is using too low a version".localizedArguments(gender))
            self.micromicronProcess(showMsg: (String(const_serverSubjectHimValue.suffix(5)) + " using t" + String(noti_documentText.prefix(4)) + "ow a" + String(show_partnerUrl.prefix(4)) + "sion").extraPropertyLetter(gender))
            //: return
            return
        }

        //: self.openVideoPicker()
        self.someStandard()
    }

    /// 打开视频相册
    //: private func openVideoPicker() {
    private func someStandard() {
        //: let vc = TalkingImagePickTool.initImagePickerVc(maxCount: 1, allowPhoto: false, allowVideo: true)
        let vc = ThreeToolReactiveCompatible.interval(maxCount: 1, allowPhoto: false, allowVideo: true)
        //: vc.modalPresentationStyle = .fullScreen
        vc.modalPresentationStyle = .fullScreen
        //: self.present(vc, animated: true)
        self.present(vc, animated: true)
        //: vc.didFinishPickingVideoHandle = { [weak self] ( coverImage: UIImage?, assets: PHAsset?)  in
        vc.didFinishPickingVideoHandle = { [weak self] (_: UIImage?, assets: PHAsset?) in
            //: guard let self = self else {return}
            guard let self = self else { return }
            //: TalkingImagePickTool.getVideoPath(asset: assets) { filePath in
            ThreeToolReactiveCompatible.heapAbout(asset: assets) { filePath in
                //: guard filePath != nil else { return }
                guard filePath != nil else { return }
                //: let asset = AVURLAsset.init(url: filePath!)
                let asset = AVURLAsset(url: filePath!)
                //: let time = asset.duration
                let time = asset.duration
                //: let seconds = Int(roundf(Float(CMTimeGetSeconds(time))))
                let seconds = Int(roundf(Float(CMTimeGetSeconds(time))))
                //: if seconds >= 5 {
                if seconds >= 5 { // 压缩视频
                    //: self.CompressedVideo(url: filePath!)
                    self.priorityPitch(url: filePath!)
                    //: } else {
                } else {
                    //: DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 0.5) {
                    DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 0.5) { // 视频压缩取消
                        //: self.micromicronProcess(showMsg: "Shoot video at least 5 seconds".localized)
                        self.micromicronProcess(showMsg: (String(appMemberUrl.prefix(6)) + "video " + String(constReferWedStr.suffix(5)) + String(appElementGrayTitle.suffix(7)) + dataDeleteTitle.lowercased()).localized)
                    }
                }
            }
        }
    }

    /// 视频压缩
    /// - Parameter url: 本地路径
    //: private func CompressedVideo(url: URL) {
    private func priorityPitch(url: URL) {
        //: let style: TalkingVideoEditorStyle = (conversationType == .private) ? .intimateVideo : .normal
        let style: AEditorStyle = (conversationType == .private) ? .intimateVideo : .normal
        //: let videoVC = TalkingVideoEditorController(style: style, maxDuration: 60.0, videoURL: url) { videoInfo in
        let videoVC = SmartViewController(style: style, maxDuration: 60.0, videoURL: url) { videoInfo in
            //: guard let videoInfo = videoInfo else { return }
            guard let videoInfo = videoInfo else { return }
            // 压缩成功，实例化模型
            //: let sendModel = AbTalkingChatMsgCustomSendModel()
            let sendModel = HintModel()
            //: sendModel.giftId = videoInfo.giftId
            sendModel.giftId = videoInfo.giftId
            //: sendModel.isBurn = videoInfo.isBurn
            sendModel.isBurn = videoInfo.isBurn
            //: sendModel.localCoverImg = videoInfo.coverPath
            sendModel.localCoverImg = videoInfo.coverPath
            //: sendModel.localVideoUrl = videoInfo.videoPath
            sendModel.localVideoUrl = videoInfo.videoPath
            //: sendModel.duration = Int(roundf(Float(videoInfo.duration)))
            sendModel.duration = Int(roundf(Float(videoInfo.duration)))
            // 发送视频消息
            //: self.func__sendVideoMsg(msgData: nil, sendModel: sendModel)
            self.listener(msgData: nil, sendModel: sendModel)
        }

        //: ProgressHUD.dismiss()
        StrangerProgressHUD.barDismiss()
        //: getNavigationController()?.pushViewController(videoVC, animated: true)
        shorePast()?.pushViewController(videoVC, animated: true)
    }

    /// 上传视频
    /// - Parameters:
    ///   - msgData: cellData，此处只数据传递
    ///   - sendModel: 消息自定义本地模型
    //: private func cos_UploadVideo(msgData: AgoneCellData, sendModel: AbTalkingChatMsgCustomSendModel) {
    private func secure(msgData: AgoneCellData, sendModel: HintModel) {
        //: TalkingMomentVideoManager.shared.cos_uploadVideo(type: 3, coverPath: sendModel.localCoverImg, videoPath: sendModel.localVideoUrl) { coverUrl, videoUrl, succeed in
        ActivityBuildListener.shared.domain(type: 3, coverPath: sendModel.localCoverImg, videoPath: sendModel.localVideoUrl) { coverUrl, videoUrl, succeed in
            //: guard succeed else {
            guard succeed else {
                //: self.micromicronProcess(showMsg: "Upload failed".localized)
                self.micromicronProcess(showMsg: (String(const_bathroomUrl.prefix(7)) + String(kSwallowMessage)).localized)
                // 更改消息状态为失败
                //: self.msgView.changeCustomMsg(msgData, msgId: nil, msgIncome: NSNumber(value: 0), status: .Msg_Status_Fail)
                self.msgView.outerNameStatus(msgData, modify: nil, storage: NSNumber(value: 0), commonStatus: .Msg_Status_Fail)

                //: return
                return
            }

            /// 上传成功，刷新本地视频消息，调用发消息接口
            //: sendModel.coverImg = coverUrl
            sendModel.coverImg = coverUrl
            //: sendModel.videoUrl = videoUrl
            sendModel.videoUrl = videoUrl
            //: self.func__sendVideoMsg(msgData: msgData, sendModel: sendModel)
            self.listener(msgData: msgData, sendModel: sendModel)
        }
    }
}

// MARK: - 语音消息

//: extension TalkingPrivateChatController {
extension BorderInsularViewDelegate {
    //: func func__addVoiceNotification() {
    func tarNotification() {
        //: NotificationCenter.default.addObserver(self, selector: #selector(appDidBecomeActive),
        NotificationCenter.default.addObserver(self, selector: #selector(access),
                                               //: name: UIApplication.willEnterForegroundNotification,
                                               name: UIApplication.willEnterForegroundNotification,
                                               //: object: nil)
                                               object: nil)

        //: NotificationCenter.default.addObserver(self, selector: #selector(appDidEnterBack),
        NotificationCenter.default.addObserver(self, selector: #selector(renderBalance),
                                               //: name: UIApplication.didEnterBackgroundNotification,
                                               name: UIApplication.didEnterBackgroundNotification,
                                               //: object: nil)
                                               object: nil)

        //: NotificationCenter.default.addObserver(self,
        NotificationCenter.default.addObserver(self,
                                               //: selector: #selector(audioSessionInterrupted(notification:)),
                                               selector: #selector(inheritance(notification:)),
                                               //: name: AVAudioSession.interruptionNotification,
                                               name: AVAudioSession.interruptionNotification,
                                               //: object: nil)
                                               object: nil)

        //: NotificationCenter.default.addObserver(self, selector: #selector(insertGiftMsgNotif(notification:)),
        NotificationCenter.default.addObserver(self, selector: #selector(construct(notification:)),
                                               //: name: CHAT_ADD_CALL_RECORD_MSG_NOTIFICATION,
                                               name: dataProductionTitle,
                                               //: object: nil)
                                               object: nil)

        //: NotificationCenter.default.addObserver(self,
        NotificationCenter.default.addObserver(self,
                                               //: selector: #selector(newMsgIntimate(notification:)),
                                               selector: #selector(confirm(notification:)),
                                               //: name: CHAT_NEW_MSG_NOTIFICATION,
                                               name: notiSuccessKey,
                                               //: object: nil)
                                               object: nil)
    }

    //: func func__addOrRrmoveObserverForMsgPlayer(isAdd: Bool) {
    func usa(isAdd: Bool) {
        //: if isAdd {
        if isAdd {
            //: TalkingVoiceMsgPlayManager.shared.addDaskManagerDelegate()
            LargeObjectProtocol.shared.doingAliveUsed()
            //: } else {
        } else {
            //: TalkingVoiceMsgPlayManager.shared.removeDaskManagerDelegate()
            LargeObjectProtocol.shared.tar()
        }
    }

    //: @objc func appDidBecomeActive() {
    @objc func access() {
        //: func__addOrRrmoveObserverForMsgPlayer(isAdd: true)
        usa(isAdd: true)
    }

    //: @objc func appDidEnterBack() {
    @objc func renderBalance() {
        //: func__addOrRrmoveObserverForMsgPlayer(isAdd: false)
        usa(isAdd: false)
    }

    //: @objc func audioSessionInterrupted(notification: NSNotification) {
    @objc func inheritance(notification: NSNotification) {
        //: guard let userInfo = notification.userInfo, let reasonValue = userInfo[AVAudioSessionInterruptionTypeKey] as? UInt else { return }
        guard let userInfo = notification.userInfo, let reasonValue = userInfo[AVAudioSessionInterruptionTypeKey] as? UInt else { return }
        //: switch reasonValue {
        switch reasonValue {
        //: case AVAudioSession.InterruptionType.began.rawValue:
        case AVAudioSession.InterruptionType.began.rawValue: // Began
            //: func__addOrRrmoveObserverForMsgPlayer(isAdd: false)
            usa(isAdd: false)
        //: break
        //: case AVAudioSession.InterruptionType.ended.rawValue:
        case AVAudioSession.InterruptionType.ended.rawValue: // End
            //: func__addOrRrmoveObserverForMsgPlayer(isAdd: true)
            usa(isAdd: true)
        //: break
        //: default:
        default:
            //: break
            break
        }
    }

    //: func func__DidTouchVoiceCellNeedPlayWithCurrentModel(cellData: TagCellData, cellView: TalkingChatAudioMsgCell) {
    func party(cellData: TagCellData, cellView: RetainViewCell) {
        //: guard TalkingSocketManager.shared.isGeneral == false else {
        guard ConfigureAxisService.shared.isGeneral == false else {
            //: self.micromicronProcess(showMsg: kMessage_videoTalking_limit)
            self.micromicronProcess(showMsg: notiSinceKey)
            //: return
            return
        }

        //: let message = cellData.innerMessage
        let message = cellData.innerMessage

        //: for indexModel in self.msgView.uiMsgs {
        for indexModel in self.msgView.uiMsgs {
            //: if (indexModel as? AgoneCellData)!.isKind(of: TagCellData.self) == false {
            if (indexModel as? AgoneCellData)!.isKind(of: TagCellData.self) == false {
                //: continue
                continue
            }
            //: let audioModel = indexModel as! TagCellData
            let audioModel = indexModel as! TagCellData
            //: audioModel.voiceModel.isPlayingStatus = 2
            audioModel.voiceModel.isPlayingStatus = 2
            //: audioModel.voiceModel.activityShowStatus = 2
            audioModel.voiceModel.activityShowStatus = 2
            // 红点提示隐藏
            //: if (audioModel.isEqual(cellData)) {
            if audioModel.isEqual(cellData) {
                //: cellView.voiceTrackPoint.isHidden = true
                cellView.voiceTrackPoint.isHidden = true
            }
        }
        //: if message != nil {
        if message != nil {
            //: cellData.voiceModel.db_voiceCacheWrap = DBUserInfoManager.cache_getAudioDB(message: message!)
            cellData.voiceModel.db_voiceCacheWrap = DeployMatrixDelegate.molarityMessage(message: message!)
        }
        //: TalkingVoiceMsgPlayManager.shared.stopAudioPlayer()
        LargeObjectProtocol.shared.narrowPlayer()

        //: if TalkingVoiceMsgPlayManager.shared.clickCellId == cellData.voiceModel.db_voiceCacheWrap.msgId {
        if LargeObjectProtocol.shared.clickCellId == cellData.voiceModel.db_voiceCacheWrap.msgId {
            //: printLog(message: "播放状态:\(TalkingVoiceMsgPlayManager.shared.playStatus)")
            printLog(message: (showPenKey.capitalized) + "\(LargeObjectProtocol.shared.playStatus)")
            //: if TalkingVoiceMsgPlayManager.shared.playStatus != .Unknown {
            if LargeObjectProtocol.shared.playStatus != .Unknown {
                //: TalkingVoiceMsgPlayManager.shared.playStatus = .Unknown
                LargeObjectProtocol.shared.playStatus = .Unknown
                //: return
                return
            }
        }
        // 指定播放的cell
        //: TalkingVoiceMsgPlayManager.shared.clickCellId = cellData.voiceModel.db_voiceCacheWrap.msgId
        LargeObjectProtocol.shared.clickCellId = cellData.voiceModel.db_voiceCacheWrap.msgId
        //: TalkingVoiceMsgPlayManager.shared.playStatus = .Waiting
        LargeObjectProtocol.shared.playStatus = .Waiting
        //: var waitArray = [cellData.voiceModel]
        var waitArray = [cellData.voiceModel]
        //: if cellData.voiceModel.db_voiceCacheWrap.db_isRead == "0" {
        if cellData.voiceModel.db_voiceCacheWrap.db_isRead == "0" {
            //: let currentIndex = self.msgView.uiMsgs.index(of: cellData)
            let currentIndex = self.msgView.uiMsgs.index(of: cellData)
            //: for index in currentIndex+1 ..< self.msgView.uiMsgs.count {
            for index in currentIndex + 1 ..< self.msgView.uiMsgs.count {
                //: let subModel: AgoneCellData = self.msgView.uiMsgs.object(at: index) as! AgoneCellData
                let subModel: AgoneCellData = self.msgView.uiMsgs.object(at: index) as! AgoneCellData
                //: let subMessage = subModel.innerMessage
                let subMessage = subModel.innerMessage
                //: if subModel.isKind(of: TagCellData.self) {
                if subModel.isKind(of: TagCellData.self) {
                    //: let audioModel = subModel as! TagCellData
                    let audioModel = subModel as! TagCellData
                    //: let voiceModel = audioModel.voiceModel
                    let voiceModel = audioModel.voiceModel
                    //: if subMessage != nil {
                    if subMessage != nil {
                        //: voiceModel.db_voiceCacheWrap = DBUserInfoManager.cache_getAudioDB(message: subMessage!)
                        voiceModel.db_voiceCacheWrap = DeployMatrixDelegate.molarityMessage(message: subMessage!)
                    }
                    //: if (voiceModel.db_voiceCacheWrap.db_isRead == "0") {
                    if voiceModel.db_voiceCacheWrap.db_isRead == "0" {
                        //: waitArray.append(voiceModel)
                        waitArray.append(voiceModel)
                    }
                    //: cellData.voiceModel.db_voiceCacheWrap.db_isRead = "1"
                    cellData.voiceModel.db_voiceCacheWrap.db_isRead = "1"
                    //: cellView.voiceTrackPoint.isHidden = true
                    cellView.voiceTrackPoint.isHidden = true
                    //: WCDBVoiceMsgTable.db_updateVoiceMsg(cellData.voiceModel.db_voiceCacheWrap)
                    DrawButtonMechanism.deadlineMsg(cellData.voiceModel.db_voiceCacheWrap)
                }
            }
        }
        //: TalkingVoiceMsgPlayManager.shared.starPlayMsg(msgArr: waitArray)
        LargeObjectProtocol.shared.user(msgArr: waitArray)
    }
}

// MARK: - 用户相关

//: extension TalkingPrivateChatController {
extension BorderInsularViewDelegate {
    /// 请求聊天用户信息 V1.8.0 提前到进入页面之前，先判断是否封号
    //: func func__requestChatInfo() {
    func doingdResume() {
        //: guard conversationType == .private || conversationType == .service else { return }
        guard conversationType == .private || conversationType == .service else { return }

        //: self.func__updateUIWithUserWrap()
        self.leave()
        //: self.msgView.tableView.reloadData()
        self.msgView.tableView.reloadData()
        // 判断是否需要插入一键打招呼消息
        //: if (self.quickGreetingTextDict?.count ?? 0 > 0) || (self.quickGreetingAudioDict?.count ?? 0 > 0) {
        if (self.quickGreetingTextDict?.count ?? 0 > 0) || (self.quickGreetingAudioDict?.count ?? 0 > 0) {
            //: self.func_sendQuickGreetingMsg(textDict: self.quickGreetingTextDict, audioDict: self.quickGreetingAudioDict)
            self.fieldDict(textDict: self.quickGreetingTextDict, audioDict: self.quickGreetingAudioDict)
        }
        //: if self.msgView.uiMsgs.count > 0 && self.userWrap.existSess == false {
        if self.msgView.uiMsgs.count > 0, self.userWrap.existSess == false {
            //: for model in self.msgView.uiMsgs {
            for model in self.msgView.uiMsgs {
                //: let indexModel = model as? AgoneCellData
                let indexModel = model as? AgoneCellData
                //: if indexModel?.isKind(of: TagCellData.self) == true || indexModel?.isKind(of: ApartCellData.self) == true || indexModel?.isKind(of: AgoArrayCellData.self) == true || indexModel?.isKind(of: SaverCellData.self) == true {
                if indexModel?.isKind(of: TagCellData.self) == true || indexModel?.isKind(of: ApartCellData.self) == true || indexModel?.isKind(of: AgoArrayCellData.self) == true || indexModel?.isKind(of: SaverCellData.self) == true {
                    //: self.userWrap.existSess = true
                    self.userWrap.existSess = true
                    //: break
                    break
                }
            }
        }
        // 判断是否需要插入音视频提示消息
        //: self.func__privateChatVideo_insertTipMsg()
        self.beak()
        // 判断是否需要插入官方客服提示消息
        //: self.func__customerServiceCenter_insertTipMsg()
        self.nanometreOrFingertip()
        /// 判断是否插入已读回执引导充值VIP提示消息
        //: self.func__privateVIPReadReceipt_insertTipsMsg()
        self.past()
    }

    /// 刷新UI
    //: func func__updateUIWithUserWrap() {
    func leave() {
        //: guard userWrap.uid.count > 0 else { return }
        guard userWrap.uid.count > 0 else { return }
        //: bgImgeView.setUrlImage(urlStr: userWrap.headPic)
        bgImgeView.notFit(urlStr: userWrap.headPic)
        //: navView.private_updateNavView(userModel: userWrap, isFrom: self.isFrom)
        navView.teteATete(userModel: userWrap, isFrom: self.isFrom)
        /// 亲密度头像
        //: self.showFirstIntimateView()
        self.menuContact()

        //: if sendGreetBtn.isHidden == false {
        if sendGreetBtn.isHidden == false {
            //: sendGreetBtn.isHidden = userWrap.sex == FacultyReactiveCompatible.share.loginUserMode.sex
            sendGreetBtn.isHidden = userWrap.sex == FacultyReactiveCompatible.share.loginUserMode.sex
        }
        //: if beautyPhotosBtn.isHidden == false {
        if beautyPhotosBtn.isHidden == false {
            //: beautyPhotosBtn.isHidden = userWrap.sex == FacultyReactiveCompatible.share.loginUserMode.sex
            beautyPhotosBtn.isHidden = userWrap.sex == FacultyReactiveCompatible.share.loginUserMode.sex
        }
        // 备注置顶
        //: notesTopView.refresh(userWrap)
        notesTopView.timing(userWrap)
    }

    //: func func__getFillDraftText() {
    func daily() {
        //: let userID = "c2c_"+msgView.targetID
        let userID = (String(const_sectionTitle.suffix(4))) + msgView.targetID
        //: V2TIMManager.sharedInstance().getConversation(userID) { [weak self] conv in
        V2TIMManager.sharedInstance().getConversation(userID) { [weak self] conv in
            //: guard let self = self else {return}
            guard let self = self else { return }
            //: DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 0.5) {
            DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 0.5) {
                //: let draftText = conv?.draftText ?? ""
                let draftText = conv?.draftText ?? ""
                //: if !draftText.isEmptyString {
                if !draftText.isEmptyString {
                    //: self.msgInputView.inputTextView.text = conv?.draftText
                    self.msgInputView.inputTextView.text = conv?.draftText
                    //: self.msgInputView.inputTextView.becomeFirstResponder()
                    self.msgInputView.inputTextView.becomeFirstResponder()
                }
            }
            //: } fail: { code, desc in
        } fail: { _, _ in
        }
    }

    //: func func__saveFillDraftText() {
    func bird() {
        //: let userID = "c2c_"+msgView.targetID
        let userID = (String(const_sectionTitle.suffix(4))) + msgView.targetID
        //: V2TIMManager.sharedInstance().setConversationDraft(userID, draftText: msgInputView.inputTextView.text!) {
        V2TIMManager.sharedInstance().setConversationDraft(userID, draftText: msgInputView.inputTextView.text!) {
            //: } fail: { code, desc in
        } fail: { _, _ in
        }
    }
}

// MARK: - 提示消息

//: extension TalkingPrivateChatController {
extension BorderInsularViewDelegate {
    //: func func__TipsNotification() {
    func detectByPress() {
        //: NotificationCenter.default.addObserver(self, selector: #selector(insertTipsNotif(notification:)),
        NotificationCenter.default.addObserver(self, selector: #selector(length(notification:)),
                                               //: name: CHAT_TIPS_TEXT_NOTIFICATION,
                                               name: noti_trustAgeCameraMsg,
                                               //: object: nil)
                                               object: nil)
    }

    //: @objc func insertTipsNotif(notification: NSNotification) -> Void {
    @objc func length(notification: NSNotification) {
        //: let userinfo = notification.userInfo as! [String: AnyObject]
        let userinfo = notification.userInfo as! [String: AnyObject]
        //: let celldata: TalkingChatTipsMsgCellData  = userinfo["msg"] as! TalkingChatTipsMsgCellData
        let celldata: ComposableAnimatedPromise = userinfo[(kSpotKey.replacingOccurrences(of: "elect", with: "g"))] as! ComposableAnimatedPromise

        //: self.msgView.uiMsgs.add(celldata)
        self.msgView.uiMsgs.add(celldata)
        //: self.msgView.tableView.reloadData()
        self.msgView.tableView.reloadData()
        //: self.msgView.parent(true)
        self.msgView.parent(true)
    }

    /// 插入提示消息
    /// - Parameters:
    ///   - tipStr: 提示消息文本
    ///   - needInsertLocal: 是否需要插入本地数据库（默认：是）
    //: func insertTipMsg_Nobalance(tipStr: String, needInsertLocal: Bool = true) {
    func imbedPrivacy(tipStr: String, needInsertLocal: Bool = true) {
        //: let msgInfo: [String: Any] = ["content": tipStr,
        let msgInfo: [String: Any] = [String(bytes: dataDefineValue.map{fromMystery(desk: $0)}, encoding: .utf8)!: tipStr,
                                      //: "msgType": "tips"]
                                      String(bytes: noti_filterPath.map{shoreCounteraction(bright: $0)}, encoding: .utf8)!: (kExtentValue.replacingOccurrences(of: "refuse", with: "ip"))]
        //: var message: AgoneCellData?
        var message: AgoneCellData?
        //: if needInsertLocal == true {
        if needInsertLocal == true { // 需要插入本地
            //: message = TUISocialChatManager.insertTXMessage(withExtral: msgInfo, toUid: msgView.targetID, isSender: true, isTip: true)
            message = CivilityReactiveCompatible.effect(withExtral: msgInfo, toUid: msgView.targetID, isSender: true, isTip: true)
            //: } else {
        } else {
            //: message = TUISocialChatManager.handleTXMessageSend(withExtral: msgInfo, isSender: true, isTip: true)
            message = CivilityReactiveCompatible.smartMailSenderBlockTipHandleTurn(withExtral: msgInfo, isSender: true, isTip: true)
        }

        //: guard let message = message else { return }
        guard let message = message else { return }
        //: self.msgView.uiMsgs.add(message)
        self.msgView.uiMsgs.add(message)
        //: self.msgView.tableView.reloadData()
        self.msgView.tableView.reloadData()
        //: self.msgView.parent(true)
        self.msgView.parent(true)
    }

    /// 官方客服插入系统提示消息
    //: func func__customerServiceCenter_insertTipMsg() {
    func nanometreOrFingertip() {
        //: guard conversationType == .service else { return }
        guard conversationType == .service else { return }
        //: TalkingPrivateChatManager.chat_customerServiceCenter_insertTipMsg { tipsStr in
        SpanChatManager.seatStrokeMessage { tipsStr in
            //: guard tipsStr != nil else { return }
            guard tipsStr != nil else { return }
            // 客服提示消息，只做内存插入展示
            //: self.insertTipMsg_Nobalance(tipStr: tipsStr!, needInsertLocal: false)
            self.imbedPrivacy(tipStr: tipsStr!, needInsertLocal: false)
        }
    }

    /// 判断是否需要插入免费提示消息
    //: func func__privateAttachSys_insertTipMsg(str: String) {
    func hidden(str: String) {
        //: guard conversationType == .private else { return }
        guard conversationType == .private else { return }
        //: self.insertTipMsg_Nobalance(tipStr: str)
        self.imbedPrivacy(tipStr: str)
    }

    /// 判断是否需要插入音视频通话提示消息
    //: func func__privateChatVideo_insertTipMsg() {
    func beak() {
        //: guard conversationType == .private else { return }
        guard conversationType == .private else { return }
        //: TalkingPrivateChatManager.chat_privateChatVideo_insertTipMsg(toUid: self.userWrap.uid, msgView: self.msgView) {
        SpanChatManager.descriptionHandler(toUid: self.userWrap.uid, msgView: self.msgView) {
            // 插入音视频提示消息
            //: let str = self.userWrap.nickname
            let str = self.userWrap.nickname
            //: self.insertTipMsg_Nobalance(tipStr: "You can use the video chat feature to give %@ a call".localizedArguments(str))
            self.imbedPrivacy(tipStr: String(bytes: show_stageUrl.map{selfPropelledVehicle(direction: $0)}, encoding: .utf8)!.extraPropertyLetter(str))
        }
    }

    /// 通话礼物消息
    //: @objc func insertGiftMsgNotif(notification: NSNotification) {
    @objc func construct(notification: NSNotification) {
        //: let dict = notification.userInfo
        let dict = notification.userInfo
        //: let cellData: AgoneCellData = dict?["cellData"] as! AgoneCellData
        let cellData: AgoneCellData = dict?[(String(data_banMsg) + String(show_changeMessage))] as! AgoneCellData
        //: self.msgView.uiMsgs.add(cellData)
        self.msgView.uiMsgs.add(cellData)
        //: self.msgView.tableView.reloadData()
        self.msgView.tableView.reloadData()
        //: self.msgView.parent(true)
        self.msgView.parent(true)
    }
}

// MARK: - 回复文案

//: extension TalkingPrivateChatController: PrivateChatReplyTipsProtocol {
extension BorderInsularViewDelegate: PocketTipsProtocol {
    //: func func__setReplyTipsView() {
    func beginView() {
        //: let key = "\(FacultyReactiveCompatible.share.loginUserMode.userID)_PrivateChatTipsTitle_\(String(self.msgView.targetID))"
        let key = "\(FacultyReactiveCompatible.share.loginUserMode.userID)" + (String(const_serverData) + String(appTipUserValue)) + "\(String(self.msgView.targetID))"
        //: let isShow: Bool = UserDefaults.standard.object(forKey: key) as? Bool ?? false
        let isShow: Bool = UserDefaults.standard.object(forKey: key) as? Bool ?? false
        //: if !isShow && FacultyReactiveCompatible.share.appStatus == AppSkinStatus.normal.rawValue {
        if !isShow && FacultyReactiveCompatible.share.appStatus == PenConstantTarget.normal.rawValue {
            //: contentView.addSubview(replyTipsView!)
            contentView.addSubview(replyTipsView!)
            //: replyTipsView?.snp.makeConstraints { make in
            replyTipsView?.snp.makeConstraints { make in
                //: make.bottom.equalTo(msgInputView.snp.top)
                make.bottom.equalTo(msgInputView.snp.top)
                //: make.leading.trailing.equalToSuperview()
                make.leading.trailing.equalToSuperview()
                //: make.height.equalTo(50)
                make.height.equalTo(50)
            }
            //: msgView.view.snp.remakeConstraints { make in
            msgView.view.snp.remakeConstraints { make in
                //: make.leading.trailing.equalToSuperview()
                make.leading.trailing.equalToSuperview()
                //: make.top.equalTo(navView.snp.bottom)
                make.top.equalTo(navView.snp.bottom)
                //: make.bottom.equalTo(replyTipsView!.snp.top)
                make.bottom.equalTo(replyTipsView!.snp.top)
            }
            //: self.replyTipsView?.setTipsStrView()
            self.replyTipsView?.keyView()
            // 修改banner位置
            //: changeBannerViewFream(isMiniView: self.bannerView.isMiniView)
            bannerEye(isMiniView: self.bannerView.isMiniView)
            //: } else {
        } else {
            //: removeReplyTipsView()
            reward()
        }
    }

    //: func seleteReplyTipsMessage(str: String) {
    func gross(str: String) {
        //: self.userWrap.existSess = true
        self.userWrap.existSess = true
        //: func__sendTextMsg(msgStr: str, quoteModel: nil)
        flag(msgStr: str, quoteModel: nil)
        //: removeReplyTipsView()
        reward()
    }

    //: func removeReplyTipsView() {
    func reward() {
        //: if conversationType == .private {
        if conversationType == .private {
            //: if self.replyTipsView != nil {
            if self.replyTipsView != nil {
                //: self.replyTipsView?.removeFromSuperview()
                self.replyTipsView?.removeFromSuperview()
                //: self.replyTipsView = nil
                self.replyTipsView = nil
            }
            //: msgView.view.snp.remakeConstraints { make in
            msgView.view.snp.remakeConstraints { make in
                //: make.leading.trailing.equalToSuperview()
                make.leading.trailing.equalToSuperview()
                //: make.top.equalTo(navView.snp.bottom)
                make.top.equalTo(navView.snp.bottom)
                //: make.bottom.equalTo(msgInputView.snp.top)
                make.bottom.equalTo(msgInputView.snp.top)
            }
            //: changeBannerViewFream(isMiniView: self.bannerView.isMiniView)
            bannerEye(isMiniView: self.bannerView.isMiniView)
        }
    }
}

// MARK: - 问题回答消息

//: extension TalkingPrivateChatController {
extension BorderInsularViewDelegate {
    /// 判断问题回答消息遮罩是否展示
    //: private func refreshRechargeRequiredView() {
    private func unitedNationsAgency() {
        //: guard FacultyReactiveCompatible.share.loginUserMode.sex == Gender.male.rawValue else { return }
        guard FacultyReactiveCompatible.share.loginUserMode.sex == ATextLiteral.male.rawValue else { return }
        //: if userWrap.isRecharge == 1 || userWrap.isOfficial {
        if userWrap.isRecharge == 1 || userWrap.isOfficial { // 已充值过
            //: quickReplayView.isHidden = true
            quickReplayView.isHidden = true
            //: rechargeView.isHidden = true
            rechargeView.isHidden = true

            //: } else {
        } else { // 未充值
            //: rechargeView.isHidden = false
            rechargeView.isHidden = false
            //: self.checkQuestionQuickReplayView()
            self.queryOffAcross()
        }
    }

    /// 检查快捷回复视图是否展示
    //: private func checkQuestionQuickReplayView() {
    private func queryOffAcross() {
        //: guard userWrap.isOfficial == false else { return }
        guard userWrap.isOfficial == false else { return }
        // 检查男性侧快捷回复是否展示（延时为了拿到uiMsgs数据）
        //: DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 1) {
        DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 1) {
            //: TalkingPrivateChatManager.chat_onNewMessage_checkQuestionQuickReplayMsg(isRecharge: self.userWrap.isRecharge == 1, msgView: self.msgView) { msgInfo in
            SpanChatManager.blue(isRecharge: self.userWrap.isRecharge == 1, msgView: self.msgView) { msgInfo in
                //: guard let msgInfo = msgInfo else { return }
                guard let msgInfo = msgInfo else { return }
                //: DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 1) {
                DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 1) {
                    //: self.quickReplayView.isHidden = false
                    self.quickReplayView.isHidden = false
                    //: self.quickReplayView.refreshQuickReplayView(msgInfo: msgInfo)
                    self.quickReplayView.content(msgInfo: msgInfo)
                }
            }
        }
    }
}

// MARK: - 半屏页面展示/隐藏动画

//: extension TalkingPrivateChatController {
extension BorderInsularViewDelegate {
    /// 半屏展示动画
    //: private func half_showViewAnimation() {
    private func timingEnablee() {
        //: guard self.isFrom == .LiveRoom else { return }
        guard self.isFrom == .LiveRoom else { return }
        //: NotificationCenter.default.post(name: LIVE_HALF_VIEW_SHOW, object: nil, userInfo: nil)
        NotificationCenter.default.post(name: notiAccountName, object: nil, userInfo: nil)
        //: self.view.frame = CGRect(x: 0, y: ScreenHeight-HalfViewTopMargin, width: ScreenWidth, height: ScreenHeight)
        self.view.frame = CGRect(x: 0, y: kTopUrl - data_windowMsg, width: main_assetUrl, height: kTopUrl)
        //: UIView.animate(withDuration: 0.25) {
        UIView.animate(withDuration: 0.25) {
            //: self.view.frame = CGRect(x: 0,
            self.view.frame = CGRect(x: 0,
                                     //: y: 0,
                                     y: 0,
                                     //: width: ScreenWidth,
                                     width: main_assetUrl,
                                     //: height: ScreenHeight)
                                     height: kTopUrl)
        }
    }

    /// 半屏页消失动画
    /// - Parameter removeChatList: 是否需要移除聊天列表
    //: private func half_dismissViewAnimation(_ removeChatList: Bool) {
    private func singleFile(_ removeChatList: Bool) {
        //: guard self.isFrom == .LiveRoom else { return }
        guard self.isFrom == .LiveRoom else { return }

        //: self.view.endEditing(true)
        self.view.endEditing(true)
        // 移除聊天列表
        //: if removeChatList == true, let currentVC = self.currentViewController() {
        if removeChatList == true, let currentVC = self.field() {
            //: for vc in currentVC.children {
            for vc in currentVC.children {
                //: if vc.isKind(of: TalkingChatListViewController.self) {
                if vc.isKind(of: TagViewController.self) {
                    //: vc.removeFromParent()
                    vc.removeFromParent()
                    //: vc.view.removeFromSuperview()
                    vc.view.removeFromSuperview()
                    //: break
                    break
                }
            }
        }

        // 移除私聊页
        //: UIView.animate(withDuration: 0.25) {
        UIView.animate(withDuration: 0.25) {
            //: self.view.frame = CGRect(x: 0,
            self.view.frame = CGRect(x: 0,
                                     //: y: ScreenHeight-HalfViewTopMargin,
                                     y: kTopUrl - data_windowMsg,
                                     //: width: ScreenWidth,
                                     width: main_assetUrl,
                                     //: height: ScreenHeight)
                                     height: kTopUrl)
            //: } completion: { finish in
        } completion: { _ in
            //: self.removeFromParent()
            self.removeFromParent()
            //: self.view.removeFromSuperview()
            self.view.removeFromSuperview()
        }
    }
}

// MARK: - Layout

//: extension TalkingPrivateChatController {
extension BorderInsularViewDelegate {
    /// 创建UI
    //: private func createUI() {
    private func share() {
        //: if conversationType == .private {
        if conversationType == .private {
            //: msgView.view.backgroundColor = UIColor.clear
            msgView.view.backgroundColor = UIColor.clear
            //: msgView.tableView.backgroundColor = UIColor.clear
            msgView.tableView.backgroundColor = UIColor.clear
            //: } else {
        } else {
            //: msgView.view.backgroundColor = UIColor.clear
            msgView.view.backgroundColor = UIColor.clear
            //: msgView.tableView.backgroundColor = UIColor(hex: "F5F5F5")
            msgView.tableView.backgroundColor = UIColor(hex: (String(main_anotherName) + constPetitionMsg.capitalized))
        }
        //: view.backgroundColor = (self.isFrom == .LiveRoom) ? UIColor.clear:UIColor.white
        view.backgroundColor = (self.isFrom == .LiveRoom) ? UIColor.clear : UIColor.white
        //: view.addSubview(contentView)
        view.addSubview(contentView)

        //: if conversationType == .private {
        if conversationType == .private {
            //: contentView.addSubview(bgImgeView)
            contentView.addSubview(bgImgeView)
            //: contentView.addSubview(bgView)
            contentView.addSubview(bgView)
            //: contentView.addSubview(coverView)
            contentView.addSubview(coverView)
            //: self.addChild(msgView)
            self.addChild(msgView)
            //: contentView.addSubview(msgView.view)
            contentView.addSubview(msgView.view)
            //: contentView.addSubview(beautyPhotosBtn)
            contentView.addSubview(beautyPhotosBtn)
            //: contentView.addSubview(sendGreetBtn)
            contentView.addSubview(sendGreetBtn)
            //: if self.isFrom == .Normal {
            if self.isFrom == .Normal {
                //: contentView.addSubview(bannerView)
                contentView.addSubview(bannerView)
            }
            //: contentView.addSubview(floatScreenView)
            contentView.addSubview(floatScreenView)
            //: contentView.addSubview(giftEffectView)
            contentView.addSubview(giftEffectView)
            //: contentView.addSubview(navView)
            contentView.addSubview(navView)
            //: contentView.addSubview(notesTopView)
            contentView.addSubview(notesTopView)
            //: view.addSubview(msgInputView)
            view.addSubview(msgInputView)
            //: view.addSubview(rechargeView)
            view.addSubview(rechargeView)
            //: view.addSubview(quickReplayView)
            view.addSubview(quickReplayView)

            //: } else if conversationType == .service {
        } else if conversationType == .service {
            //: self.addChild(msgView)
            self.addChild(msgView)
            //: contentView.addSubview(msgView.view)
            contentView.addSubview(msgView.view)
            //: contentView.addSubview(navView)
            contentView.addSubview(navView)
            //: view.addSubview(msgInputView)
            view.addSubview(msgInputView)

            //: } else {
        } else {
            //: title = "System notification".localized
            title = (String(dataRowModeMsg) + String(mainAgainName.suffix(5)) + "ific" + k_guessMsg.replacingOccurrences(of: "ago", with: "on")).localized
            //: contentView.addSubview(feedView)
            contentView.addSubview(feedView)
            //: feedView.addSubview(feedbackBtn)
            feedView.addSubview(feedbackBtn)
            //: self.addChild(msgView)
            self.addChild(msgView)
            //: contentView.addSubview(msgView.view)
            contentView.addSubview(msgView.view)
        }
    }

    /// 约束
    //: private func createUIConstraints() {
    private func nameConstraints() {
        //: contentView.snp.makeConstraints { make in
        contentView.snp.makeConstraints { make in
            //: make.bottom.leading.trailing.equalToSuperview()
            make.bottom.leading.trailing.equalToSuperview()
            //: make.top.equalTo((self.isFrom == .LiveRoom) ? HalfViewTopMargin:0)
            make.top.equalTo((self.isFrom == .LiveRoom) ? data_windowMsg : 0)
        }

        //: if self.isFrom == .LiveRoom {
        if self.isFrom == .LiveRoom {
            //: contentView.layoutIfNeeded()
            contentView.layoutIfNeeded()
            //: contentView.Corner(width: contentView.width,
            contentView.cornerPopular(width: contentView.width,
                               //: height: contentView.height,
                               height: contentView.height,
                               //: corners: [.topLeft, .topRight],
                               corners: [.topLeft, .topRight],
                               //: cornerRadii: .init(width: 8, height: 8))
                               cornerRadii: .init(width: 8, height: 8))
        }

        //: if conversationType == .private {
        if conversationType == .private {
            //: bgView.snp.makeConstraints { make in
            bgView.snp.makeConstraints { make in
                //: make.edges.equalToSuperview()
                make.edges.equalToSuperview()
            }
            //: bgImgeView.snp.makeConstraints { make in
            bgImgeView.snp.makeConstraints { make in
                //: make.edges.equalToSuperview()
                make.edges.equalToSuperview()
            }
            //: coverView.snp.makeConstraints { make in
            coverView.snp.makeConstraints { make in
                //: make.edges.equalToSuperview()
                make.edges.equalToSuperview()
            }
            //: navView.snp.makeConstraints { make in
            navView.snp.makeConstraints { make in
                //: make.leading.trailing.top.equalToSuperview()
                make.leading.trailing.top.equalToSuperview()
                //: make.height.equalTo((self.isFrom == .LiveRoom) ? NavigationBarHeight:StatusBarNavigationBarHeight)
                make.height.equalTo((self.isFrom == .LiveRoom) ? show_allPlayId : main_sendData)
            }
            //: notesTopView.snp.makeConstraints { make in
            notesTopView.snp.makeConstraints { make in
                //: make.top.equalTo(navView.snp.bottom)
                make.top.equalTo(navView.snp.bottom)
                //: make.leading.equalTo(10)
                make.leading.equalTo(10)
                //: make.trailing.equalTo(-10)
                make.trailing.equalTo(-10)
                //: make.height.equalTo(36)
                make.height.equalTo(36)
            }
            //: msgInputView.snp.makeConstraints { make in
            msgInputView.snp.makeConstraints { make in
                //: make.leading.trailing.bottom.equalTo(view)
                make.leading.trailing.bottom.equalTo(view)
                //: make.height.equalTo(msgInputView.BackViewHeight)
                make.height.equalTo(msgInputView.BackViewHeight)
            }
            //: msgView.view.snp.makeConstraints { make in
            msgView.view.snp.makeConstraints { make in
                //: make.leading.trailing.equalToSuperview()
                make.leading.trailing.equalToSuperview()
                //: make.top.equalTo(navView.snp.bottom)
                make.top.equalTo(navView.snp.bottom)
                //: if beautyPhotosBtn.isHidden {
                if beautyPhotosBtn.isHidden {
                    //: make.bottom.equalTo(msgInputView.snp.top)
                    make.bottom.equalTo(msgInputView.snp.top)
                    //: } else {
                } else {
                    //: make.bottom.equalTo(beautyPhotosBtn.snp.top).offset(-5)
                    make.bottom.equalTo(beautyPhotosBtn.snp.top).offset(-5)
                }
            }
            //: giftEffectView.snp.makeConstraints { make in
            giftEffectView.snp.makeConstraints { make in
                //: make.edges.equalToSuperview()
                make.edges.equalToSuperview()
            }
            //: sendGreetBtn.snp.makeConstraints { make in
            sendGreetBtn.snp.makeConstraints { make in
                //: make.bottom.equalTo(beautyPhotosBtn)
                make.bottom.equalTo(beautyPhotosBtn)
                //: make.leading.equalTo(10)
                make.leading.equalTo(10)
                //: make.size.equalTo(beautyPhotosBtn)
                make.size.equalTo(beautyPhotosBtn)
            }
            //: beautyPhotosBtn.snp.makeConstraints { make in
            beautyPhotosBtn.snp.makeConstraints { make in
                //: make.bottom.equalTo(msgInputView.snp.top).offset(-8)
                make.bottom.equalTo(msgInputView.snp.top).offset(-8)
                //: make.leading.equalTo(sendGreetBtn.snp.trailing).offset(9)
                make.leading.equalTo(sendGreetBtn.snp.trailing).offset(9)
                //: make.width.equalTo(134)
                make.width.equalTo(134)
                //: make.height.equalTo(30)
                make.height.equalTo(30)
            }
            //: floatScreenView.snp.makeConstraints { make in
            floatScreenView.snp.makeConstraints { make in
                //: make.top.equalTo(navView.snp.bottom)
                make.top.equalTo(navView.snp.bottom)
                //: make.leading.trailing.equalToSuperview()
                make.leading.trailing.equalToSuperview()
                //: make.height.equalTo(40)
                make.height.equalTo(40)
            }
            //: rechargeView.snp.makeConstraints { make in
            rechargeView.snp.makeConstraints { make in
                //: make.leading.trailing.bottom.equalToSuperview()
                make.leading.trailing.bottom.equalToSuperview()
                //: make.height.equalTo(rechargeView.ContentView_H)
                make.height.equalTo(rechargeView.ContentView_H)
            }
            //: quickReplayView.snp.makeConstraints { make in
            quickReplayView.snp.makeConstraints { make in
                //: make.leading.equalTo(24)
                make.leading.equalTo(24)
                //: make.width.equalTo(270)
                make.width.equalTo(270)
                //: make.bottom.equalTo(rechargeView.snp.top).offset(-13)
                make.bottom.equalTo(rechargeView.snp.top).offset(-13)
            }
            //: if self.isFrom == .Normal {
            if self.isFrom == .Normal {
                //: bannerView.snp.makeConstraints { make in
                bannerView.snp.makeConstraints { make in
                    //: make.trailing.equalToSuperview()
                    make.trailing.equalToSuperview()
                    //: make.bottom.equalTo(beautyPhotosBtn.snp.top)
                    make.bottom.equalTo(beautyPhotosBtn.snp.top)
                    //: make.height.equalTo(106)
                    make.height.equalTo(106)
                    //: make.width.equalTo(93)
                    make.width.equalTo(93)
                }
            }

            //: } else if conversationType == .service {
        } else if conversationType == .service {
            //: navView.snp.makeConstraints { make in
            navView.snp.makeConstraints { make in
                //: make.leading.trailing.top.equalToSuperview()
                make.leading.trailing.top.equalToSuperview()
                //: make.height.equalTo((self.isFrom == .LiveRoom) ? NavigationBarHeight:StatusBarNavigationBarHeight)
                make.height.equalTo((self.isFrom == .LiveRoom) ? show_allPlayId : main_sendData)
            }
            //: msgInputView.snp.makeConstraints { make in
            msgInputView.snp.makeConstraints { make in
                //: make.leading.trailing.bottom.equalTo(view)
                make.leading.trailing.bottom.equalTo(view)
                //: make.height.equalTo(msgInputView.BackViewHeight)
                make.height.equalTo(msgInputView.BackViewHeight)
            }
            //: msgView.view.snp.makeConstraints { make in
            msgView.view.snp.makeConstraints { make in
                //: make.leading.trailing.equalToSuperview()
                make.leading.trailing.equalToSuperview()
                //: make.top.equalTo(navView.snp.bottom)
                make.top.equalTo(navView.snp.bottom)
                //: make.bottom.equalTo(msgInputView.snp.top)
                make.bottom.equalTo(msgInputView.snp.top)
            }

            //: } else {
        } else {
            //: feedView.snp.makeConstraints { make in
            feedView.snp.makeConstraints { make in
                //: make.leading.trailing.bottom.equalTo((view))
                make.leading.trailing.bottom.equalTo(view)
                //: make.height.equalTo(56+kDeviceSafeBottomHeight)
                make.height.equalTo(56 + data_cornerMessage)
            }
            //: feedbackBtn.snp.makeConstraints { make in
            feedbackBtn.snp.makeConstraints { make in
                //: make.leading.trailing.top.equalTo(feedView)
                make.leading.trailing.top.equalTo(feedView)
                //: make.height.equalTo(56)
                make.height.equalTo(56)
            }
            //: msgView.view.snp.makeConstraints { make in
            msgView.view.snp.makeConstraints { make in
                //: make.leading.trailing.top.equalTo((view))
                make.leading.trailing.top.equalTo(view)
                //: make.bottom.equalTo(feedView.snp.top)
                make.bottom.equalTo(feedView.snp.top)
            }
        }
    }

    /// 事件绑定
    //: func bindInteraction() {
    func contact() {
        //: navView.backButton.rx.tap
        navView.backButton.rx.tap
            //: .subscribe(onNext: { [weak self] in
            .subscribe(onNext: { [weak self] in
                //: guard let self = self else {return}
                guard let self = self else { return }
                //: self.backButtonClick()
                self.redundantBlind()
                //: })
            })
            //: .disposed(by: disposeBag)
            .disposed(by: disposeBag)
        //: navView.moreButton.rx.controlEvent(.touchUpInside)
        navView.moreButton.rx.controlEvent(.touchUpInside)
            //: .subscribe(onNext: { [weak self] in
            .subscribe(onNext: { [weak self] in
                //: guard let self = self else {return}
                guard let self = self else { return }
                //: self.view.endEditing(true)
                self.view.endEditing(true)
                //: self.moreBtnClick()
                self.suspendAcross()
                //: })
            })
            //: .disposed(by: disposeBag)
            .disposed(by: disposeBag)
        //: navView.videoCallBtn.rx.controlEvent(.touchUpInside)
        navView.videoCallBtn.rx.controlEvent(.touchUpInside)
            //: .subscribe(onNext: { [weak self] in
            .subscribe(onNext: { [weak self] in
                //: guard let self = self else {return}
                guard let self = self else { return }
                //: self.view.endEditing(true)
                self.view.endEditing(true)
                //: self.func__selectMoreFunAction(actionType: .VideoCall)
                self.componentBy(actionType: .VideoCall)
                //: uploadRecord.uploadRecordEvent(eventID: ClickChatVideoButton, toUid: self.userWrap.uid )
                kSaveKey.putAvailable(eventID: k_backData, toUid: self.userWrap.uid)
                //: })
            })
            //: .disposed(by: disposeBag)
            .disposed(by: disposeBag)
    }
}
