
//: Declare String Begin

/*: "init(coder:) has not been implemented" :*/
fileprivate let constGiveBotMsg:[UInt8] = [0x64,0x65,0x74,0x6e,0x65,0x6d,0x65,0x6c,0x70,0x6d,0x69,0x20,0x6e,0x65,0x65,0x62,0x20,0x74,0x6f,0x6e,0x20,0x73,0x61,0x68,0x20,0x29,0x3a,0x72,0x65,0x64,0x6f,0x63,0x28,0x74,0x69,0x6e,0x69]

/*: "uid" :*/
fileprivate let dataRobotBreadWhenTitle:[UInt8] = [0x64,0x69,0x75]

/*: "headPic" :*/
fileprivate let show_favoriteData:[Character] = ["h","e","a","d","P","i","c"]

/*: "nickname" :*/
fileprivate let show_dimensionName:[Character] = ["n","i","c","k","n","a","m","e"]

/*: "birthday" :*/
fileprivate let kSecondEquallyStr:[Character] = ["b","i","r","t","h"]
fileprivate let app_fullyPath:String = "dtooy"

/*: "videoUrl" :*/
fileprivate let data_pitchNarrowPushValue:String = "customde"

/*: "cover" :*/
fileprivate let k_mixValue:String = "cobookr"

/*: "location" :*/
fileprivate let show_secretCapEscapeText:String = "locatcrusho"
fileprivate let const_thereName:[Character] = ["n"]

/*: "cmd" :*/
fileprivate let dataPostTitle:[UInt8] = [0xed,0xe3,0xea]

private func matchControl(calendar num: UInt8) -> UInt8 {
    return num ^ 142
}

/*: "refuseCall" :*/
fileprivate let appCollectionAgreeContent:String = "rinstancefu"
fileprivate let appNighData:String = "seCalllog direct mine carpet hidden"

/*: "data" :*/
fileprivate let dataSplitKey:[UInt8] = [0x99,0x9c,0x89,0x9c]

private func flashCoal(like num: UInt8) -> UInt8 {
    return num ^ 253
}

/*: "Call interruption" :*/
fileprivate let kAccelerateMsg:[Character] = ["C","a","l","l"," ","i","n","t"]
fileprivate let app_proAverageValue:String = "sherru"

/*: "stopCall" :*/
fileprivate let userMiniStr:String = "sclub"
fileprivate let mainFindWellMyUrl:String = "mated sharedpCall"

/*: "logId" :*/
fileprivate let dataNothingFormat:[UInt8] = [0x51,0x52,0x5a,0x74,0x59]

private func blackPopulation(arc num: UInt8) -> UInt8 {
    return num ^ 61
}

/*: "cancelCall" :*/
fileprivate let mainPocketPath:[Character] = ["c","a","n","c","e","l","C","a","l"]
fileprivate let show_allId:[Character] = ["l"]

/*: "No answer" :*/
fileprivate let const_brotherName:[Character] = ["N","o"," ","a"]
fileprivate let dataOffeStr:String = "nswephase"

/*: "duration" :*/
fileprivate let user_procedureData:[Character] = ["d","u","r","a","t","i","o","n"]

/*: "She has hung up" :*/
fileprivate let userBellyFastTitle:String = "She hrequired data mine home arrow"
fileprivate let data_yesNurseValue:String = "recommendation price nearas hu"

/*: "cancelSex" :*/
fileprivate let k_findId:String = "calivec"

/*: "He has hung up" :*/
fileprivate let app_dancingValue:String = "He hasoperation belly shape"

/*: "endType" :*/
fileprivate let data_feedCommitteeUrl:[Character] = ["e","n","d","T","y"]
fileprivate let showSolutionPath:String = "preason"

/*: "stopSex" :*/
fileprivate let const_resourcePath:String = "spark"
fileprivate let show_tireText:[Character] = ["t","o","p","S","e","x"]

/*: "Insufficient gold coins, call has ended" :*/
fileprivate let showFarStr:[UInt8] = [0x17,0x30,0x2d,0x2b,0x38,0x38,0x37,0x3d,0x37,0x3b,0x30,0x2a,0x7e,0x39,0x31,0x32,0x3a,0x7e,0x3d,0x31,0x37,0x30,0x2d,0x72,0x7e,0x3d,0x3f,0x32,0x32,0x7e,0x36,0x3f,0x2d,0x7e,0x3b,0x30,0x3a,0x3b,0x3a]

private func symbolicDancing(necessary num: UInt8) -> UInt8 {
    return num ^ 94
}

/*: "The other hung up the phone" :*/
fileprivate let mainAboveMsg:[Character] = ["T","h","e"," ","o","t","h","e","r"," ","h","u","n","g"," ","u","p"," ","t","h","e"," "]
fileprivate let k_sheMessage:[Character] = ["p","h","o","n","e"]

/*: "type" :*/
fileprivate let appAllId:String = "tholdpe"

/*: "jumpUrl" :*/
fileprivate let main_pastFormat:[Character] = ["j","u","m","p","U"]
fileprivate let showSecurityUrl:String = "RL"

/*: "Cancel" :*/
fileprivate let userAppealMsg:[Character] = ["C","a","n","c","e","l"]

/*: "Get gold coins" :*/
fileprivate let const_problemUrl:[Character] = ["G","e","t"," ","g","o","l","d"," ","c","o","i","n"]
fileprivate let const_pureNaturalMessage:String = "bread"

/*: "refuseSex" :*/
fileprivate let notiEnoughMsg:String = "east"
fileprivate let user_blankFormat:[Character] = ["e","f","u","s","e","S","e","x"]

/*: "onAcceptCall" :*/
fileprivate let kTalkValue:[Character] = ["o","n","A","c"]
fileprivate let showSwitchlyMessage:[Character] = ["c","e","p","t","C","a","l","l"]

/*: "acceptCall" :*/
fileprivate let showGoText:[Character] = ["a","c","c","e"]
fileprivate let k_letterPath:String = "stick bowl peerptCall"

/*: "Calls are cancelled" :*/
fileprivate let userBadKey:[Character] = ["C","a","l","l","s"," ","a","r","e"," ","c"]
fileprivate let mainHouseSuccessTitle:String = "anceskiped"

/*: "Continue matching with other users？" :*/
fileprivate let showValidPath:[UInt8] = [0xf8,0xd4,0xd5,0xcf,0xd2,0xd5,0xce,0xde,0x9b,0xd6,0xda,0xcf,0xd8,0xd3,0xd2,0xd5,0xdc,0x9b,0xcc,0xd2,0xcf,0xd3,0x9b,0xd4,0xcf,0xd3,0xde,0xc9,0x9b,0xce,0xc8,0xde,0xc9,0xc8,0x54,0x7,0x24]

private func taskDescend(format num: UInt8) -> UInt8 {
    return num ^ 187
}

/*: "Yes" :*/
fileprivate let userAirTitle:String = "operating rank statement waitYes"

/*: "The call has ended" :*/
fileprivate let show_videoId:[Character] = ["T","h","e"," ","c","a","l","l"," ","h","a","s"," ","e","n"]
fileprivate let constShPath:[Character] = ["d","e","d"]

//: Declare String End

// __DEBUG__
// __CLOSE_PRINT__
//
//  AdvancedErrorDelegate.swift
//  AbroadTalking
//
//  Created by young on 2022/11/8.
//

//: import CallKit.CXCallObserver
import CallKit.CXCallObserver
//: import UIKit
import UIKit

// 通话监听(必须为全局属性，否则无法接听)
//: let videoCallHelper = CXCallObserver()
let show_scaleStr = CXCallObserver()
//: class TalkingVideoChatViewController: TalkingBaseViewController {
class AdvancedErrorDelegate: SafetyFactorViewController {
    //: var chatModel: TalkingVideoChatModel?
    var chatModel: ControlChatModel? // 视频通话模型
    //: private var player: TalkingCallBGMPlayer?
    private var player: PanelPlayListener? // 拨打中背景音乐
    //: private var timeOutTimer: Timer?
    private var timeOutTimer: Timer? // 未接听超时定时器
    //: private var expiredDate = Date()
    private var expiredDate = Date() // 失效时间
    //: private var miniView: TalkingVideoMiniView?
    private var miniView: FountainPenMiniView? // 视频通话小窗口
    //: private var isFromMiniView = false
    private var isFromMiniView = false // 是否点击小窗口进入
    //: private var reportView: TalkingVideoFloatScreenView?
    private var reportView: FriendshipView? // 视频举报弹窗

    //: override init(nibName nibNameOrNil: String?, bundle nibBundleOrNil: Bundle?) {
    override init(nibName _: String?, bundle _: Bundle?) {
        //: super.init(nibName: nil, bundle: nil)
        super.init(nibName: nil, bundle: nil)
        // 提前绑定delegate
        //: TalkingSocketManager.shared.callerDelegate = self
        ConfigureAxisService.shared.callerDelegate = self
        //: TalkingSocketManager.shared.calledPartyDelegate = self
        ConfigureAxisService.shared.calledPartyDelegate = self
        //: TalkingSocketManager.shared.errorDelegate = self
        ConfigureAxisService.shared.errorDelegate = self
    }

    //: required init?(coder: NSCoder) {
    required init?(coder _: NSCoder) {
        //: fatalError("init(coder:) has not been implemented")
        fatalError(String(bytes: constGiveBotMsg.reversed(), encoding: .utf8)!)
    }

    //: override func viewDidAppear(_ animated: Bool) {
    override func viewDidAppear(_ animated: Bool) {
        //: super.viewDidAppear(animated)
        super.viewDidAppear(animated)
        //: let nav = self.navigationController as! TalkingNavigationController
        let nav = self.navigationController as! StrangerControllerDelegate
        //: nav.protectedPushVc = nil
        nav.protectedPushVc = nil

        //: UIApplication.shared.isIdleTimerDisabled = true
        UIApplication.shared.isIdleTimerDisabled = true // 设备不会进入休眠状态
        //: self.startBGM()
        self.inStock()
        //: if self.chatModel?.videoStage == .Recieved {
        if self.chatModel?.videoStage == .Recieved { // 接收方验证权限
            //: checkVideoAuth()
            aGlobal()
            //: } else if self.chatModel?.videoStage == .Calling {
        } else if self.chatModel?.videoStage == .Calling { // 直接创建通话中的界面（包括重连）
            // 通话小窗口进入无需重复创建
            //: guard isFromMiniView == false else { return }
            guard isFromMiniView == false else { return }
            //: createCallingRoom(logId: self.chatModel?.logId)
            entry(logId: self.chatModel?.logId)
        }
    }

    //: override func viewWillAppear(_ animated: Bool) {
    override func viewWillAppear(_ animated: Bool) {
        //: super.viewWillAppear(animated)
        super.viewWillAppear(animated)
        //: if self.miniView != nil {
        if self.miniView != nil { // 移除小窗口
            //: self.miniView?.removeFromSuperview()
            self.miniView?.removeFromSuperview()
            //: self.miniView = nil
            self.miniView = nil
            //: TalkingSocketManager.shared.currGeneralVC = nil
            ConfigureAxisService.shared.currGeneralVC = nil
        }
        //: interactionView.initCachEffectData()
        interactionView.connectednessBindRawData()
    }

    //: override func viewDidLoad() {
    override func viewDidLoad() {
        //: super.viewDidLoad()
        super.viewDidLoad()
        //: self.hideNavi = true
        self.hideNavi = true
        //: setupSubviews()
        isCircle()
        //: setupSubViewsConstraint()
        displayNor()
        //: initNotificationObserver()
        loop()
        //: req_callGetUserInfo()
        someoneBy()
        //: timeoutRules()
        elementRandom()
        //: setUpBGMPlayer()
        ban()
        //: if self.chatModel?.videoStage != .Recieved {
        if self.chatModel?.videoStage != .Recieved {
            //: self.startPreview()
            self.fallCustom()
        }
    }

    //: override func loadView() {
    override func loadView() {
        //: if #available(iOS 13.2, *) {
        if #available(iOS 13.2, *) {
            //: if FacultyReactiveCompatible.share.appConfigMode.disableShootScreen {
            if FacultyReactiveCompatible.share.appConfigMode.disableShootScreen {
                //: super.loadView()
                super.loadView()
                //: } else {
            } else {
                //: let bgView = MAScreenShieldView(frame: CGRect(x: 0, y: 0, width: ScreenWidth, height: ScreenHeight))
                let bgView = ShieldView(frame: CGRect(x: 0, y: 0, width: main_assetUrl, height: kTopUrl))
                //: self.view = bgView
                self.view = bgView
            }
            //: } else {
        } else {
            //: super.loadView()
            super.loadView()
        }
    }

    //: deinit {
    deinit {
        //: UIApplication.shared.isIdleTimerDisabled = false
        UIApplication.shared.isIdleTimerDisabled = false
        //: NotificationCenter.default.removeObserver(self)
        NotificationCenter.default.removeObserver(self)
        //: TalkingSocketManager.shared.isGeneral = false
        ConfigureAxisService.shared.isGeneral = false
        //: TalkingSocketManager.shared.isCalling = false
        ConfigureAxisService.shared.isCalling = false
        //: self.stopVCAllActivties()
        self.begin()
    }

    // MARK: - Lazy Load

    /// 视频通话背景
    //: private lazy var bgVideoView: TalkingBGVideoView = {
    private lazy var bgVideoView: BearVideoView = {
        //: let v = TalkingBGVideoView()
        let v = BearVideoView()
        //: v.showCoverView(show: self.chatModel?.videoStage != .Calling)
        v.executeThan(show: self.chatModel?.videoStage != .Calling)
        //: return v
        return v
        //: }()
    }()

    /// 视频通话操作按钮
    //: private lazy var interactionView: Talking1v1VideoInteractionView = {
    private lazy var interactionView: RealiseViewDelegate = {
        //: let v = Talking1v1VideoInteractionView(frame: .zero, chatModel: self.chatModel)
        let v = RealiseViewDelegate(frame: .zero, chatModel: self.chatModel)
        //: v.delegate = self
        v.delegate = self
        //: return v
        return v
        //: }()
    }()

    // 视频通话小窗口
    //: private lazy var windowView: TalkingVideoWindowView = {
    private lazy var windowView: WillWindowView = {
        //: let v = TalkingVideoWindowView()
        let v = WillWindowView()
        //: var x = ScreenWidth-15-actualWidth(w: 125)
        var x = main_assetUrl - 15 - actualWidth(w: 125)
        //: if LanguageManager.shared.direction == .rightToLeft {
        if LocalRouterDescription.shared.direction == .rightToLeft {
            //: x = 15
            x = 15
        }
        //: v.frame = CGRect(x: x, y: StatusBarHeight+actualHeight(h: 25)+26+35, width: actualWidth(w: 125), height: actualHeight(h: 170))
        v.frame = CGRect(x: x, y: show_textNextFormat + actualHeight(h: 25) + 26 + 35, width: actualWidth(w: 125), height: actualHeight(h: 170))
        //: v.isHidden = self.chatModel?.videoStage != .Calling
        v.isHidden = self.chatModel?.videoStage != .Calling
        //: v.block = { [weak self] in
        v.block = { [weak self] in
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: self.changeRenderView()
            self.becomeView()
        }
        //: return v
        return v
        //: }()
    }()

    //: private lazy var videoManager: TalkingVideoChatManager = {
    private lazy var videoManager: FactorChatManager = {
        //: let m = TalkingVideoChatManager()
        let m = FactorChatManager()
        //: m.infoModel = self.chatModel
        m.infoModel = self.chatModel
        // 视图传递给manager
        //: m.bgVideoView = self.bgVideoView.renderView
        m.bgVideoView = self.bgVideoView.renderView
        //: m.bgView = self.bgVideoView.bgView
        m.bgView = self.bgVideoView.bgView
        //: m.windowVideoView = self.windowView.renderView
        m.windowVideoView = self.windowView.renderView
        //: return m
        return m
        //: }()
    }()
}

// MARK: - Request && 刷新UI

//: extension TalkingVideoChatViewController {
extension AdvancedErrorDelegate {
    /// 查询接收者信息
    //: func req_callGetUserInfo() {
    func someoneBy() {
        //: guard let pairUid = chatModel?.pairUid else { return }
        guard let pairUid = chatModel?.pairUid else { return }
        //: TalkingVideoChatManager.req_callGetUserInfo(uid: pairUid) { [weak self] succeed, result, errorModel in
        FactorChatManager.designReceive(uid: pairUid) { [weak self] succeed, result, _ in
            //: guard succeed == true else { return }
            guard succeed == true else { return }
            //: guard let self = self else { return }
            guard let self = self else { return }

            //: let json = JSON(result ?? [String: Any]())
            let json = JSON(result ?? [String: Any]())

            //: self.chatModel?.pairUid = json["uid"].stringValue
            self.chatModel?.pairUid = json[String(bytes: dataRobotBreadWhenTitle.reversed(), encoding: .utf8)!].stringValue
            //: self.chatModel?.pairHeadPic = json["headPic"].stringValue
            self.chatModel?.pairHeadPic = json[(String(show_favoriteData))].stringValue
            //: self.chatModel?.pairNickName = json["nickname"].stringValue
            self.chatModel?.pairNickName = json[(String(show_dimensionName))].stringValue
            //: self.chatModel?.pairBirthday = json["birthday"].stringValue
            self.chatModel?.pairBirthday = json[(String(kSecondEquallyStr) + app_fullyPath.replacingOccurrences(of: "too", with: "a"))].stringValue
            //: self.chatModel?.videoUrl = json["videoUrl"].stringValue
            self.chatModel?.videoUrl = json[(data_pitchNarrowPushValue.replacingOccurrences(of: "custom", with: "vi") + "oUrl")].stringValue
            //: self.chatModel?.cover = json["cover"].stringValue
            self.chatModel?.cover = json[(k_mixValue.replacingOccurrences(of: "book", with: "ve"))].stringValue
            //: self.chatModel?.location = json["location"].stringValue
            self.chatModel?.location = json[(show_secretCapEscapeText.replacingOccurrences(of: "crush", with: "i") + String(const_thereName))].stringValue

            // 刷新通话视图
            //: self.interactionView.afterGetInfo()
            self.interactionView.server()
            //: self.videoBG_refreshViewWithCameraStatus()
            self.reserveCon()
        }
    }

    /// 根据摄像头是否开启状态，刷新视频通话背景
    //: private func videoBG_refreshViewWithCameraStatus() {
    private func reserveCon() {
        //: var bgHeadPic: String?
        var bgHeadPic: String?
        //: var bgShowName: String?
        var bgShowName: String?
        //: var bgOpenCamera = VideoCameraStatus.Open
        var bgOpenCamera = ArrayCameraStatus.Open
        //: var windowHeadPic: String?
        var windowHeadPic: String?
        //: var windowOpenCamera = VideoCameraStatus.Open
        var windowOpenCamera = ArrayCameraStatus.Open

        //: if self.chatModel?.renderPostion == .SelfOnWindow {
        if self.chatModel?.renderPostion == .SelfOnWindow { // 自己在窗口
            //: bgHeadPic = self.chatModel?.pairHeadPic
            bgHeadPic = self.chatModel?.pairHeadPic
            //: bgShowName = self.chatModel?.pairNickName
            bgShowName = self.chatModel?.pairNickName
            //: bgOpenCamera = self.videoManager.config.remoteCameraStatus
            bgOpenCamera = self.videoManager.config.remoteCameraStatus
            //: windowHeadPic = self.chatModel?.iHeadPic
            windowHeadPic = self.chatModel?.iHeadPic
            //: windowOpenCamera = self.videoManager.config.openCamera ? .Open:.Closed
            windowOpenCamera = self.videoManager.config.openCamera ? .Open : .Closed

            //: } else {
        } else { // 自己在大背景
            //: bgHeadPic = self.chatModel?.iHeadPic
            bgHeadPic = self.chatModel?.iHeadPic
            //: bgShowName = self.chatModel?.iNickName
            bgShowName = self.chatModel?.iNickName
            //: bgOpenCamera = self.videoManager.config.openCamera ? .Open:.Closed
            bgOpenCamera = self.videoManager.config.openCamera ? .Open : .Closed
            //: windowHeadPic = self.chatModel?.pairHeadPic
            windowHeadPic = self.chatModel?.pairHeadPic
            //: windowOpenCamera = self.videoManager.config.remoteCameraStatus
            windowOpenCamera = self.videoManager.config.remoteCameraStatus
        }

        //: self.bgVideoView.refreshBgView(urlStr: bgHeadPic, showName: bgShowName, isOpenCamera: bgOpenCamera)
        self.bgVideoView.assessmentRefresh(urlStr: bgHeadPic, showName: bgShowName, isOpenCamera: bgOpenCamera)
        //: self.windowView.refreshBgView(urlStr: windowHeadPic, isOpenCamera: windowOpenCamera)
        self.windowView.that(urlStr: windowHeadPic, isOpenCamera: windowOpenCamera)
    }
}

// MARK: - Event

//: extension TalkingVideoChatViewController {
extension AdvancedErrorDelegate {
    /// 权限校验，播放BGM
    //: private func checkVideoAuth() {
    private func aGlobal() {
        //: TalkingPermissionTool.checkCameraAndMicrophone { isOpen in
        ActivityReactiveCompatible.counteraction { isOpen in
            //: guard isOpen == true else {
            guard isOpen == true else {
                //: self.func__showStatusBarErrorMsg(showMsg: kMessage_permissions_error)
                self.micromicronProcess(showMsg: showPackageId)
                //: TalkingSocketManager.shared.sendMessage(info: ["cmd": "refuseCall", "data": ["uid": self.chatModel?.pairUid]])
                ConfigureAxisService.shared.attentionCut(info: [String(bytes: dataPostTitle.map{matchControl(calendar: $0)}, encoding: .utf8)!: (appCollectionAgreeContent.replacingOccurrences(of: "instance", with: "e") + String(appNighData.prefix(6))), String(bytes: dataSplitKey.map{flashCoal(like: $0)}, encoding: .utf8)!: [String(bytes: dataRobotBreadWhenTitle.reversed(), encoding: .utf8)!: self.chatModel?.pairUid]])
                //: self.popCurrentViewController()
                self.cap()
                //: return
                return
            }

            //: self.startPreview()
            self.fallCustom()
            //: self.stopBGM()
            self.attribute()
            //: self.player = TalkingCallBGMPlayer.callBGMPlayer(type: .Response)
            self.player = PanelPlayListener.explain(type: .Response)
            //: self.startBGM()
            self.inStock()
        }
    }

    /// 预览视频画面
    //: private func startPreview() {
    private func fallCustom() {
        //: self.videoManager.startPreview()
        self.videoManager.oval()
    }

    /// 释放当前页面所有资源
    //: private func stopVCAllActivties() {
    private func begin() {
        //: if self.timeOutTimer != nil {
        if self.timeOutTimer != nil {
            //: self.timeOutTimer?.invalidate()
            self.timeOutTimer?.invalidate()
            //: self.timeOutTimer = nil
            self.timeOutTimer = nil
        }

        //: self.stopBGM()
        self.attribute()
        //: TalkingVideoDanmuManager.danmu_releaseAllResource()
        AfterAdvancedDanmuManager.shapeForce()
        //: TalkingSocketManager.shared.callerDelegate = nil
        ConfigureAxisService.shared.callerDelegate = nil
        //: TalkingSocketManager.shared.calledPartyDelegate = nil
        ConfigureAxisService.shared.calledPartyDelegate = nil
        //: TalkingSocketManager.shared.errorDelegate = nil
        ConfigureAxisService.shared.errorDelegate = nil
    }

    // 将当前视图从栈中移除
    //: private func popCurrentViewController(animated: Bool = true) {
    private func cap(animated: Bool = true) {
        //: self.stopBGM()
        self.attribute()

        //: if self.navigationController?.topViewController == self {
        if self.navigationController?.topViewController == self {
            //: self.navigationController?.popViewController(animated: animated)
            self.navigationController?.popViewController(animated: animated)
            //: } else {
        } else {
            //: if var vcArr = self.navigationController?.viewControllers {
            if var vcArr = self.navigationController?.viewControllers {
                //: if let index = vcArr.firstIndex(of: self) {
                if let index = vcArr.firstIndex(of: self) {
                    //: vcArr.remove(at: index)
                    vcArr.remove(at: index)
                    //: self.navigationController?.setViewControllers(vcArr, animated: false)
                    self.navigationController?.setViewControllers(vcArr, animated: false)
                }
            }
        }
    }

    /// 视频通话双方窗口转换
    //: private func changeRenderView() {
    private func becomeView() {
        //: if self.chatModel?.renderPostion == .selfOnBg {
        if self.chatModel?.renderPostion == .selfOnBg {
            //: self.chatModel?.renderPostion = .SelfOnWindow
            self.chatModel?.renderPostion = .SelfOnWindow
            //: } else if self.chatModel?.renderPostion == .SelfOnWindow {
        } else if self.chatModel?.renderPostion == .SelfOnWindow {
            //: self.chatModel?.renderPostion = .selfOnBg
            self.chatModel?.renderPostion = .selfOnBg
        }

        //: self.videoManager.changeRenderView()
        self.videoManager.blink()
        //: self.videoBG_refreshViewWithCameraStatus()
        self.reserveCon()
    }

    /// 创建视频通话房间
    //: private func createTalkRoom() {
    private func withinThat() {
        //: self.windowView.isHidden = self.chatModel?.videoStage != .Calling
        self.windowView.isHidden = self.chatModel?.videoStage != .Calling
        //: self.videoManager.createRoomAndStartPublish()
        self.videoManager.atIncrease()
        //: self.videoBG_refreshViewWithCameraStatus()
        self.reserveCon()
        // 展示投诉说明飘屏（男性）
        //: self.show_videoFloatScreenAnimation()
        self.pastAnimation()
    }

    /// 展示投诉说明飘屏（男性）
    //: private func show_videoFloatScreenAnimation() {
    private func pastAnimation() {
        //: guard FacultyReactiveCompatible.share.loginUserMode.sex == Gender.male.rawValue else { return }
        guard FacultyReactiveCompatible.share.loginUserMode.sex == ATextLiteral.male.rawValue else { return }
        //: guard reportView == nil else { return }
        guard reportView == nil else { return }

        //: reportView = TalkingVideoFloatScreenView()
        reportView = FriendshipView()
        //: view.addSubview(reportView!)
        view.addSubview(reportView!)
        //: reportView?.show_animation()
        reportView?.showMagnituderyToALesserExtentParty()
    }
}

// MARK: - 通知

//: extension TalkingVideoChatViewController {
extension AdvancedErrorDelegate {
    /// 初始化通知
    //: private func initNotificationObserver() {
    private func loop() {
        // 监听 App 从后台切前台
        //: NotificationCenter.default.addObserver(self,
        NotificationCenter.default.addObserver(self,
                                               //: selector: #selector(appWillEnterForeground(notif:)),
                                               selector: #selector(price(notif:)),
                                               //: name: UIApplication.willEnterForegroundNotification,
                                               name: UIApplication.willEnterForegroundNotification,
                                               //: object: nil)
                                               object: nil)

        // 监听 对方摄像头是否可用
        //: NotificationCenter.default.addObserver(self,
        NotificationCenter.default.addObserver(self,
                                               //: selector: #selector(updateCameraStatus(notification:)),
                                               selector: #selector(sort(notification:)),
                                               //: name: VIDEO_CHAT_OTHER_CAMERA_AVAILABLE_NOTIFICATION,
                                               name: show_purchaseFormat,
                                               //: object: nil)
                                               object: nil)

        // socket断网重连监听
        //: NotificationCenter.default.addObserver(self,
        NotificationCenter.default.addObserver(self,
                                               //: selector: #selector(reachabilityChanged(note:)),
                                               selector: #selector(postConvert(note:)),
                                               //: name: SOCKET_IS_OPEN_NOTIFICATION,
                                               name: k_tableStr,
                                               //: object: nil)
                                               object: nil)

        //: videoCallHelper.setDelegate(self, queue: DispatchQueue.main)
        show_scaleStr.setDelegate(self, queue: DispatchQueue.main)
    }

    /// app进入前台
    //: @objc private func appWillEnterForeground(notif: Notification) {
    @objc private func price(notif _: Notification) {
        //: guard self.chatModel?.videoStage != .Calling else { return }
        guard self.chatModel?.videoStage != .Calling else { return }
        //: let date = Date()
        let date = Date()
        //: let interval = date.timeIntervalSince(self.expiredDate)
        let interval = date.timeIntervalSince(self.expiredDate)
        //: if interval >= 0 {
        if interval >= 0 {
            //: popCurrentViewController()
            cap()
        }
    }

    /// 断网重连socket
    //: @objc private func reachabilityChanged(note: Notification) {
    @objc private func postConvert(note _: Notification) {
        //: guard TalkingSocketManager.shared.needReconnect == true else { return }
        guard ConfigureAxisService.shared.needReconnect == true else { return }
        //: TalkingSocketManager.shared.callerDelegate = self
        ConfigureAxisService.shared.callerDelegate = self
        //: TalkingSocketManager.shared.calledPartyDelegate = self
        ConfigureAxisService.shared.calledPartyDelegate = self
        //: TalkingSocketManager.shared.errorDelegate = self
        ConfigureAxisService.shared.errorDelegate = self
        //: self.videoManager.restartTalkWhenDisconnectedAndNetAvailable()
        self.videoManager.restart()
    }

    /// 根据对方摄像头状态更新UI
    /// - Parameter notification: 通知
    //: @objc private func updateCameraStatus(notification: NSNotification) {
    @objc private func sort(notification _: NSNotification) {
        //: self.videoBG_refreshViewWithCameraStatus()
        self.reserveCon()
    }
}

// MARK: - CXCallObserverDelegate【通话监听】

//: extension TalkingVideoChatViewController: CXCallObserverDelegate {
extension AdvancedErrorDelegate: CXCallObserverDelegate {
    //: func callObserver(_ callObserver: CXCallObserver, callChanged call: CXCall) {
    func callObserver(_: CXCallObserver, callChanged call: CXCall) {
        //: if call.hasConnected == true {
        if call.hasConnected == true { // 通话中
            //: self.func__showStatusBarErrorMsg(showMsg: "Call interruption")
            self.micromicronProcess(showMsg: (String(kAccelerateMsg) + app_proAverageValue.replacingOccurrences(of: "she", with: "e") + "ption"))
            //: switch(self.chatModel?.videoStage) {
            switch self.chatModel?.videoStage {
            //: case .Calling:
            case .Calling:
                //: TalkingSocketManager.shared.sendMessage(info: ["cmd": "stopCall", "data": ["logId": self.chatModel?.logId]])
                ConfigureAxisService.shared.attentionCut(info: [String(bytes: dataPostTitle.map{matchControl(calendar: $0)}, encoding: .utf8)!: (userMiniStr.replacingOccurrences(of: "club", with: "to") + String(mainFindWellMyUrl.suffix(5))), String(bytes: dataSplitKey.map{flashCoal(like: $0)}, encoding: .utf8)!: [String(bytes: dataNothingFormat.map{blackPopulation(arc: $0)}, encoding: .utf8)!: self.chatModel?.logId]])
            //: case .Waiting:
            case .Waiting:
                //: TalkingSocketManager.shared.sendMessage(info: ["cmd": "cancelCall", "data": ["logId": self.chatModel?.logId]])
                ConfigureAxisService.shared.attentionCut(info: [String(bytes: dataPostTitle.map{matchControl(calendar: $0)}, encoding: .utf8)!: (String(mainPocketPath) + String(show_allId)), String(bytes: dataSplitKey.map{flashCoal(like: $0)}, encoding: .utf8)!: [String(bytes: dataNothingFormat.map{blackPopulation(arc: $0)}, encoding: .utf8)!: self.chatModel?.logId]])
            //: case .Recieved:
            case .Recieved:
                //: TalkingSocketManager.shared.sendMessage(info: ["cmd": "refuseCall", "data": ["uid": self.chatModel?.pairUid]])
                ConfigureAxisService.shared.attentionCut(info: [String(bytes: dataPostTitle.map{matchControl(calendar: $0)}, encoding: .utf8)!: (appCollectionAgreeContent.replacingOccurrences(of: "instance", with: "e") + String(appNighData.prefix(6))), String(bytes: dataSplitKey.map{flashCoal(like: $0)}, encoding: .utf8)!: [String(bytes: dataRobotBreadWhenTitle.reversed(), encoding: .utf8)!: self.chatModel?.pairUid]])
            //: case .none:
            case .none:
                //: break
                break
            }

            //: if miniView != nil {
            if miniView != nil {
                //: self.miniView?.dismissWhenCallEnd()
                self.miniView?.towardPhone()
                //: TalkingSocketManager.shared.currGeneralVC = nil
                ConfigureAxisService.shared.currGeneralVC = nil
            }
            //: popCurrentViewController()
            cap()
        }
    }
}

// MARK: - 通话音乐

//: extension TalkingVideoChatViewController {
extension AdvancedErrorDelegate {
    /// 初始化视频通话背景音乐
    //: private func setUpBGMPlayer() {
    private func ban() {
        //: guard self.chatModel?.videoStage != .Calling else { return }
        guard self.chatModel?.videoStage != .Calling else { return }

        //: let type = self.chatModel!.isCaller == true ? CallBGMPlayerType.Call : CallBGMPlayerType.Response
        let type = self.chatModel!.isCaller == true ? ReportedTransformable.Call : ReportedTransformable.Response
        //: self.player = TalkingCallBGMPlayer.callBGMPlayer(type: type)
        self.player = PanelPlayListener.explain(type: type)
    }

    /// 开始播放背景音乐
    //: private func startBGM() {
    private func inStock() {
        //: guard self.chatModel?.videoStage != .Calling else { return }
        guard self.chatModel?.videoStage != .Calling else { return }
        //: guard self.chatModel?.resumeTalk != true else { return }
        guard self.chatModel?.resumeTalk != true else { return }

        //: self.player?.play()
        self.player?.highlightFail()
    }

    /// 停止播放背景音乐
    //: private func stopBGM() {
    private func attribute() {
        //: guard self.player != nil else { return }
        guard self.player != nil else { return }
        //: self.player?.stop()
        self.player?.labelForeNet()
        //: self.player = nil
        self.player = nil
    }
}

// MARK: - 定时器

//: extension TalkingVideoChatViewController {
extension AdvancedErrorDelegate {
    /// 超时定时器
    //: private func timeoutRules() {
    private func elementRandom() {
        //: guard self.chatModel?.videoStage != .Calling else { return }
        guard self.chatModel?.videoStage != .Calling else { return }

        //: let timeout = self.chatModel?.isCaller == true ? VIDEO_1v1_CALL_TIMEOUT : self.chatModel?.remainAnswerCallTime()
        let timeout = self.chatModel?.isCaller == true ? user_currencyPath : self.chatModel?.screenTime()
        //: if timeout! <= 0 {
        if timeout! <= 0 {
            //: buildTalkTimeOutAction()
            star()
        }
        //: self.timeOutTimer = Timer.scheduledTimer(withTimeInterval: timeout!, block: { [weak self] timer in
        self.timeOutTimer = Timer.scheduledTimer(withTimeInterval: timeout!, block: { [weak self] _ in
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: self.buildTalkTimeOutAction()
            self.star()
            //: }, repeats: false)
        }, repeats: false)

        //: self.expiredDate = Date().addingTimeInterval(60)
        self.expiredDate = Date().addingTimeInterval(60)
    }

    /// 倒计时超时逻辑处理
    //: private func buildTalkTimeOutAction() {
    private func star() {
        //: guard !TalkingSocketManager.shared.isGeneral else { return }
        guard !ConfigureAxisService.shared.isGeneral else { return }

        //: var socketDict: [String: Any]
        var socketDict: [String: Any]
        //: if self.chatModel?.isCaller == true {
        if self.chatModel?.isCaller == true { // 拨打方
            //: self.func__showStatusBarErrorMsg(showMsg: "No answer".localized)
            self.micromicronProcess(showMsg: (String(const_brotherName) + dataOffeStr.replacingOccurrences(of: "phase", with: "r")).localized)
            //: socketDict = ["cmd": "cancelCall", "data": ["logId": self.chatModel?.logId]]
            socketDict = [String(bytes: dataPostTitle.map{matchControl(calendar: $0)}, encoding: .utf8)!: (String(mainPocketPath) + String(show_allId)), String(bytes: dataSplitKey.map{flashCoal(like: $0)}, encoding: .utf8)!: [String(bytes: dataNothingFormat.map{blackPopulation(arc: $0)}, encoding: .utf8)!: self.chatModel?.logId]]
            //: } else {
        } else { // 接收方
            //: socketDict = ["cmd": "refuseCall", "data": ["uid": self.chatModel?.pairUid]]
            socketDict = [String(bytes: dataPostTitle.map{matchControl(calendar: $0)}, encoding: .utf8)!: (appCollectionAgreeContent.replacingOccurrences(of: "instance", with: "e") + String(appNighData.prefix(6))), String(bytes: dataSplitKey.map{flashCoal(like: $0)}, encoding: .utf8)!: [String(bytes: dataRobotBreadWhenTitle.reversed(), encoding: .utf8)!: self.chatModel?.pairUid]]
        }
        //: TalkingSocketManager.shared.sendMessage(info: socketDict)
        ConfigureAxisService.shared.attentionCut(info: socketDict)
        //: popCurrentViewController()
        cap()
    }
}

// MARK: - resume talk【恢复通话】

//: extension TalkingVideoChatViewController {
extension AdvancedErrorDelegate {
    /// 恢复视频通话，更新chatModel
    /// - Parameter resumeData: 数据
    //: func updateInfoViaResumeTalkData(resumeData: [String: Any]) {
    func componentPartThan(resumeData: [String: Any]) {
        //: let json = JSON(resumeData)
        let json = JSON(resumeData)
        //: let logId = json["logId"].intValue
        let logId = json[String(bytes: dataNothingFormat.map{blackPopulation(arc: $0)}, encoding: .utf8)!].intValue
        //: let duration = json["duration"].intValue
        let duration = json[(String(user_procedureData))].intValue
        //: let interval = Date().timeIntervalSince1970
        let interval = Date().timeIntervalSince1970
        //: let startTalkTime = floor(interval) - Double(duration)
        let startTalkTime = floor(interval) - Double(duration)
        //: self.chatModel?.startTime = startTalkTime
        self.chatModel?.startTime = startTalkTime
        //: self.interactionView.updateStartTime(startTime: startTalkTime)
        self.interactionView.disassemble(startTime: startTalkTime)

        //: if self.chatModel?.videoStage != .Calling {
        if self.chatModel?.videoStage != .Calling {
            //: self.chatModel?.logId = logId
            self.chatModel?.logId = logId
            //: self.chatModel?.videoStage = .Calling
            self.chatModel?.videoStage = .Calling
            //: self.interactionView.showViewsViaStage()
            self.interactionView.widespread()
            //: createTalkRoom()
            withinThat()
            //: self.stopBGM()
            self.attribute()
        }
    }

    /// 创建通话中房间
    /// - Parameter logId: 通话记录Id
    //: private func createCallingRoom(logId: Int?) {
    private func entry(logId: Int?) {
        //: guard let logId = logId else { return }
        guard let logId = logId else { return }

        //: self.chatModel?.logId = logId
        self.chatModel?.logId = logId
        //: self.chatModel?.videoStage = .Calling
        self.chatModel?.videoStage = .Calling
        //: self.chatModel?.renderPostion = .SelfOnWindow
        self.chatModel?.renderPostion = .SelfOnWindow

        //: self.videoManager.changeRenderView()
        self.videoManager.blink()
        //: self.bgVideoView.showCoverView(show: false)
        self.bgVideoView.executeThan(show: false)
        //: self.stopBGM()
        self.attribute()
        //: self.interactionView.showViewsViaStage()
        self.interactionView.widespread()
        //: self.createTalkRoom()
        self.withinThat()
    }
}

// MARK: - ConfirmCallerDelegate【拨打方】

//: extension TalkingVideoChatViewController: SocketManagerVideoChatCallerDelegate {
extension AdvancedErrorDelegate: ConfirmCallerDelegate {
    /// 拨打方拒绝了通话，接收方收到该指令（未通话）
    //: func socket_videoChat_cancelCall(data: [String: Any]) {
    func delineateBean(data: [String: Any]) {
        //: if self.miniView == nil {
        if self.miniView == nil {
            //: let json = JSON(data)
            let json = JSON(data)
            //: var typeStr = "She has hung up".localized
            var typeStr = (String(userBellyFastTitle.prefix(5)) + String(data_yesNurseValue.suffix(5)) + "ng up").localized
            //: if json["cancelSex"].stringValue == Gender.male.rawValue {
            if json[(k_findId.replacingOccurrences(of: "alive", with: "an") + "elSex")].stringValue == ATextLiteral.male.rawValue {
                //: typeStr = "He has hung up".localized
                typeStr = (String(app_dancingValue.prefix(6)) + " hung up").localized
            }
            //: self.func__showStatusBarErrorMsg(showMsg: typeStr)
            self.micromicronProcess(showMsg: typeStr)
            //: } else {
        } else {
            //: self.miniView?.dismissWhenCallEnd()
            self.miniView?.towardPhone()
            //: TalkingSocketManager.shared.currGeneralVC = nil
            ConfigureAxisService.shared.currGeneralVC = nil
        }

        //: popCurrentViewController()
        cap()
    }

    /// 对方接受了来电，建立视频通话
    //: func socket_videoChat_acceptCall(data: [String: Any]) {
    func curve(data: [String: Any]) {
        //: let json = JSON(data)
        let json = JSON(data)
        //: createCallingRoom(logId: json["logId"].intValue)
        entry(logId: json[String(bytes: dataNothingFormat.map{blackPopulation(arc: $0)}, encoding: .utf8)!].intValue)
    }

    /// 对方结束了通话（通话中）
    //: func socket_videoChat_stopCall(data: [String: Any]) {
    func wrapData(data: [String: Any]) {
        //: TalkingAlertShow.hideAlert()
        PersonAlertShow.quantityerrupt()
        //: let json = JSON(data)
        let json = JSON(data)

        //: let endType = json["endType"].intValue
        let endType = json[(String(data_feedCommitteeUrl) + showSolutionPath.replacingOccurrences(of: "reason", with: "e"))].intValue
        //: var typeStr = "She has hung up".localized
        var typeStr = (String(userBellyFastTitle.prefix(5)) + String(data_yesNurseValue.suffix(5)) + "ng up").localized
        //: if json["stopSex"].stringValue == Gender.male.rawValue {
        if json[(const_resourcePath.replacingOccurrences(of: "spark", with: "s") + String(show_tireText))].stringValue == ATextLiteral.male.rawValue {
            //: typeStr = "He has hung up".localized
            typeStr = (String(app_dancingValue.prefix(6)) + " hung up").localized
        }

        //: if endType == 1 {
        if endType == 1 { // 付费方余额不足
            //: if FacultyReactiveCompatible.share.appStatus == AppSkinStatus.normal.rawValue {
            if FacultyReactiveCompatible.share.appStatus == PenConstantTarget.normal.rawValue {
                //: typeStr = "Insufficient gold coins, call has ended".localized
                typeStr = String(bytes: showFarStr.map{symbolicDancing(necessary: $0)}, encoding: .utf8)!.localized
                //: } else {
            } else {
                //: typeStr = "The other hung up the phone".localized
                typeStr = (String(mainAboveMsg) + String(k_sheMessage)).localized
            }
        }
        //: if self.miniView == nil {
        if self.miniView == nil {
            //: self.func__showStatusBarErrorMsg(showMsg: typeStr)
            self.micromicronProcess(showMsg: typeStr)
            //: } else {
        } else {
            //: self.miniView?.dismissWhenCallEnd()
            self.miniView?.towardPhone()
            //: TalkingSocketManager.shared.currGeneralVC = nil
            ConfigureAxisService.shared.currGeneralVC = nil
        }

        //: self.interactionView.func_giftViewDismiss()
        self.interactionView.funcCounterrupt()
        //: popCurrentViewController()
        cap()
    }

    /// 余额不足提醒
    //: func socket_videoChat_onMissCoin(msg: String, data: [String: Any]) {
    func noKit(msg: String, data: [String: Any]) {
        //: guard FacultyReactiveCompatible.share.appStatus == AppSkinStatus.normal.rawValue else { return }
        guard FacultyReactiveCompatible.share.appStatus == PenConstantTarget.normal.rawValue else { return }

        //: let index = data["type"] as? String ?? ""
        let index = data[(appAllId.replacingOccurrences(of: "hold", with: "y"))] as? String ?? ""
        //: if index == "2" {
        if index == "2" {
//            if self.interactionView.isShowRecharge {
//                return
//            }
            //: guard let jumpUrl = data["jumpUrl"] as? String  else { return }
            guard let jumpUrl = data[(String(main_pastFormat) + showSecurityUrl.lowercased())] as? String else { return }
            //: let httpsUrl = FacultyReactiveCompatible.share.appConfigMode.urlH5Domain+jumpUrl
            let httpsUrl = FacultyReactiveCompatible.share.appConfigMode.urlH5Domain + jumpUrl
            //: var config = TalkingWebConfig.init()
            var config = CalendarConfig()
            //: config.isDiscountsView = true
            config.isDiscountsView = true
            //: DisplayAperturePresenter.share.func__pushToWebVC(urlStr: httpsUrl, webConfig: config)
            DisplayAperturePresenter.share.lifeStyle(urlStr: httpsUrl, webConfig: config)
            //: } else {
        } else {
            //: TalkingAlertShow.alert(title: nil,
            PersonAlertShow.panAngleKind(title: nil,
                                          //: message: msg,
                                          message: msg,
                                          //: leftBtnTitle: "Cancel".localized,
                                          leftBtnTitle: (String(userAppealMsg)).localized,
                                          //: rightBtnTitle: "Get gold coins".localized) {
                                          rightBtnTitle: (String(const_problemUrl) + const_pureNaturalMessage.replacingOccurrences(of: "bread", with: "s")).localized)
            {
                //: TalkingAlertShow.hideAlert()
                PersonAlertShow.quantityerrupt()
                //: } rightBlock: {
            } rightBlock: {
                //: DisplayAperturePresenter.share.func__jumpToWebRecharge(clickEvent: clickLess1minButton, sufficient: false)
                DisplayAperturePresenter.share.bilge(clickEvent: noti_packageMessage, sufficient: false)
            }
        }
    }

    /// 收到礼物消息展示
    //: @objc func receiveGiftPlay(giftArr: Array<Any>) {
    @objc func cradleArr(giftArr: [Any]) {
        //: interactionView.addGiftEffectModelArr(giftArr: giftArr)
        interactionView.countoArr(giftArr: giftArr)
    }
}

// MARK: - SeparatePartyDelegate【接收方】

//: extension TalkingVideoChatViewController: SocketManagerVideoChatCalledPartyDelegate {
extension AdvancedErrorDelegate: SeparatePartyDelegate {
    /// 接收方拒绝了通话，拨打方收到该指令（未通话）
    /// - Parameter data: 数据
    //: func socket_videoChat_refuseCall(data: [String: Any]) {
    func countCity(data: [String: Any]) {
        //: if self.miniView == nil {
        if self.miniView == nil {
            //: let json = JSON(data)
            let json = JSON(data)
            //: var typeStr = "She has hung up".localized
            var typeStr = (String(userBellyFastTitle.prefix(5)) + String(data_yesNurseValue.suffix(5)) + "ng up").localized
            //: if json["refuseSex"].stringValue == Gender.male.rawValue {
            if json[(notiEnoughMsg.replacingOccurrences(of: "east", with: "r") + String(user_blankFormat))].stringValue == ATextLiteral.male.rawValue {
                //: typeStr = "He has hung up".localized
                typeStr = (String(app_dancingValue.prefix(6)) + " hung up").localized
            }
            //: self.func__showStatusBarErrorMsg(showMsg: typeStr)
            self.micromicronProcess(showMsg: typeStr)
            //: } else {
        } else {
            //: self.miniView?.dismissWhenCallEnd()
            self.miniView?.towardPhone()
            //: TalkingSocketManager.shared.currGeneralVC = nil
            ConfigureAxisService.shared.currGeneralVC = nil
        }

        //: popCurrentViewController()
        cap()
    }

    /// 接收方接受了通话，改变状态为通话中，acceptCall回执
    //: func socket_videoChat_onAcceptCall(data: [String: Any]) {
    func starHideSort(data: [String: Any]) {
        //: let json = JSON(data)
        let json = JSON(data)
        //: createCallingRoom(logId: json["logId"].intValue)
        entry(logId: json[String(bytes: dataNothingFormat.map{blackPopulation(arc: $0)}, encoding: .utf8)!].intValue)
    }
}

// MARK: - FriendshipErrorDelegate【异常处理】

//: extension TalkingVideoChatViewController: TalkingSocketManagerErrorDelegate {
extension AdvancedErrorDelegate: FriendshipErrorDelegate {
    // 处理error情况
    //: func socket_didRecieveError(errorNo: Int, cmd: String, msg: String, data: [String: Any]) {
    func data(errorNo: Int, cmd: String, msg: String, data _: [String: Any]) {
        //: if cmd == "refuseCall" { // 对方拒绝了通话
        if cmd == (appCollectionAgreeContent.replacingOccurrences(of: "instance", with: "e") + String(appNighData.prefix(6))) { // 对方拒绝了通话
            //: if self.miniView == nil {
            if self.miniView == nil {
                //: self.func__showStatusBarErrorMsg(showMsg: "No answer".localized)
                self.micromicronProcess(showMsg: (String(const_brotherName) + dataOffeStr.replacingOccurrences(of: "phase", with: "r")).localized)
                //: } else {
            } else {
                //: self.miniView?.dismissWhenCallEnd()
                self.miniView?.towardPhone()
                //: TalkingSocketManager.shared.currGeneralVC = nil
                ConfigureAxisService.shared.currGeneralVC = nil
            }
            //: popCurrentViewController()
            cap()

            //: } else if cmd == "onAcceptCall" && errorNo == 101 { // 接听通话时，余额不足
        } else if cmd == (String(kTalkValue) + String(showSwitchlyMessage)) && errorNo == 101 { // 接听通话时，余额不足
            //: guard FacultyReactiveCompatible.share.appStatus == AppSkinStatus.normal.rawValue else { return }
            guard FacultyReactiveCompatible.share.appStatus == PenConstantTarget.normal.rawValue else { return }
            //: DisplayAperturePresenter.share.func__jumpToWebRecharge(sufficient: false)
            DisplayAperturePresenter.share.bilge(sufficient: false)

            //: } else if cmd == "onAcceptCall" && errorNo == 102 { // 对方已取消通话
        } else if cmd == (String(kTalkValue) + String(showSwitchlyMessage)) && errorNo == 102 { // 对方已取消通话
            //: self.func__showStatusBarErrorMsg(showMsg: msg)
            self.micromicronProcess(showMsg: msg)
            //: popCurrentViewController()
            cap()
        }
    }
}

// MARK: - PeerViewDelegate【通话按钮操作事件】

//: extension TalkingVideoChatViewController: Talking1v1VideoInteractionViewDelegate {
extension AdvancedErrorDelegate: PeerViewDelegate {
    /// 接听视频通话
    //: func interactionView_shouldAcceptCall() {
    func finishOutline() {
        //: TalkingSocketManager.shared.sendMessage(info: ["cmd": "acceptCall", "data": ["uid": self.chatModel?.pairUid]])
        ConfigureAxisService.shared.attentionCut(info: [String(bytes: dataPostTitle.map{matchControl(calendar: $0)}, encoding: .utf8)!: (String(showGoText) + String(k_letterPath.suffix(6))), String(bytes: dataSplitKey.map{flashCoal(like: $0)}, encoding: .utf8)!: [String(bytes: dataRobotBreadWhenTitle.reversed(), encoding: .utf8)!: self.chatModel?.pairUid]])
    }

    /// 拨打方结束通话
    //: func interactionView_shouldEndCall() {
    func entitle() {
        //: if self.chatModel?.videoStage == .Waiting {
        if self.chatModel?.videoStage == .Waiting { // 拨打方取消【未通话】
            //: self.func__showStatusBarErrorMsg(showMsg: "Calls are cancelled".localized)
            self.micromicronProcess(showMsg: (String(userBadKey) + mainHouseSuccessTitle.replacingOccurrences(of: "skip", with: "ll")).localized)
            //: TalkingSocketManager.shared.sendMessage(info: ["cmd": "cancelCall", "data": ["logId": self.chatModel?.logId]])
            ConfigureAxisService.shared.attentionCut(info: [String(bytes: dataPostTitle.map{matchControl(calendar: $0)}, encoding: .utf8)!: (String(mainPocketPath) + String(show_allId)), String(bytes: dataSplitKey.map{flashCoal(like: $0)}, encoding: .utf8)!: [String(bytes: dataNothingFormat.map{blackPopulation(arc: $0)}, encoding: .utf8)!: self.chatModel?.logId]])
            //: } else if self.chatModel?.videoStage == .Recieved {
        } else if self.chatModel?.videoStage == .Recieved { // 接收方取消【未通话】
            //: self.func__showStatusBarErrorMsg(showMsg: "Calls are cancelled".localized)
            self.micromicronProcess(showMsg: (String(userBadKey) + mainHouseSuccessTitle.replacingOccurrences(of: "skip", with: "ll")).localized)
            //: TalkingSocketManager.shared.sendMessage(info: ["cmd": "refuseCall", "data": ["uid": self.chatModel?.pairUid]])
            ConfigureAxisService.shared.attentionCut(info: [String(bytes: dataPostTitle.map{matchControl(calendar: $0)}, encoding: .utf8)!: (appCollectionAgreeContent.replacingOccurrences(of: "instance", with: "e") + String(appNighData.prefix(6))), String(bytes: dataSplitKey.map{flashCoal(like: $0)}, encoding: .utf8)!: [String(bytes: dataRobotBreadWhenTitle.reversed(), encoding: .utf8)!: self.chatModel?.pairUid]])
            //: } else {
        } else { // 【通话中】
            //: if chatModel?.isRandomVCPush ?? false {
            if chatModel?.isRandomVCPush ?? false {
                //: TalkingAlertShow.alert(title: nil, message: "Continue matching with other users？".localized, leftBtnTitle: "No".localized, rightBtnTitle: "Yes".localized, leftBlock: {
                PersonAlertShow.panAngleKind(title: nil, message: String(bytes: showValidPath.map{taskDescend(format: $0)}, encoding: .utf8)!.localized, leftBtnTitle: "No".localized, rightBtnTitle: (String(userAirTitle.suffix(3))).localized, leftBlock: {
                    //: self.randowCallIsEndTool(isNewRandow: false)
                    self.privatenessScene(isNewRandow: false)
                    //: }, rightBlock: {
                }, rightBlock: {
                    //: self.randowCallIsEndTool(isNewRandow: true)
                    self.privatenessScene(isNewRandow: true)
                    //: })
                })
                //: return
                return
                    //: } else {
            } else {
                //: self.func__showStatusBarErrorMsg(showMsg: "The call has ended".localized)
                self.micromicronProcess(showMsg: (String(show_videoId) + String(constShPath)).localized)
                //: TalkingSocketManager.shared.sendMessage(info: ["cmd": "stopCall", "data": ["logId": self.chatModel?.logId]])
                ConfigureAxisService.shared.attentionCut(info: [String(bytes: dataPostTitle.map{matchControl(calendar: $0)}, encoding: .utf8)!: (userMiniStr.replacingOccurrences(of: "club", with: "to") + String(mainFindWellMyUrl.suffix(5))), String(bytes: dataSplitKey.map{flashCoal(like: $0)}, encoding: .utf8)!: [String(bytes: dataNothingFormat.map{blackPopulation(arc: $0)}, encoding: .utf8)!: self.chatModel?.logId]])
            }
        }

        //: popCurrentViewController()
        cap()
    }

    /// 随机匹配通话挂断时
    //: func randowCallIsEndTool(isNewRandow: Bool) {
    func privatenessScene(isNewRandow: Bool) {
        //: self.func__showStatusBarErrorMsg(showMsg: "The call has ended".localized)
        self.micromicronProcess(showMsg: (String(show_videoId) + String(constShPath)).localized)
        //: TalkingSocketManager.shared.sendMessage(info: ["cmd": "stopCall", "data": ["logId": self.chatModel?.logId]])
        ConfigureAxisService.shared.attentionCut(info: [String(bytes: dataPostTitle.map{matchControl(calendar: $0)}, encoding: .utf8)!: (userMiniStr.replacingOccurrences(of: "club", with: "to") + String(mainFindWellMyUrl.suffix(5))), String(bytes: dataSplitKey.map{flashCoal(like: $0)}, encoding: .utf8)!: [String(bytes: dataNothingFormat.map{blackPopulation(arc: $0)}, encoding: .utf8)!: self.chatModel?.logId]])

        //: if !isNewRandow{
        if !isNewRandow {
            //: if let targetVC = navigationController?.viewControllers.first(where: { $0 is RandomMatchVC }) {
            if let targetVC = navigationController?.viewControllers.first(where: { $0 is LiteralMatchVc }) {
                //: navigationController?.viewControllers = [targetVC]
                navigationController?.viewControllers = [targetVC]
                //: navigationController?.popToViewController(targetVC, animated: false)
                navigationController?.popToViewController(targetVC, animated: false)
            }
            //: }else {
        } else {
            //: popCurrentViewController()
            cap()
            //: NotificationCenter.default.post(name: MATCHVIDEOCALL_END_NOTIFICATION, object: nil)
            NotificationCenter.default.post(name: show_lastTableValue, object: nil)
        }
    }

    /// 旋转摄像头
    //: func interactionView_shouldChangeCamera(isFront: Bool) {
    func noneParent(isFront: Bool) {
        //: self.videoManager.useFrontCamera(isFront)
        self.videoManager.evaluate(isFront)
    }

    /// 开启/关闭摄像头
    //: func interactionView_shouldForbiddenCamera(forbidden: Bool) {
    func goldfishBowl(forbidden: Bool) {
        //: if self.chatModel?.videoStage == .Recieved {
        if self.chatModel?.videoStage == .Recieved {
            //: self.bgVideoView.showCoverView(show: !forbidden)
            self.bgVideoView.executeThan(show: !forbidden)
        }
        //: self.videoManager.useCamera(!forbidden)
        self.videoManager.reuse(!forbidden)
        //: self.videoBG_refreshViewWithCameraStatus()
        self.reserveCon()
    }

    /// 展示视频通话小窗口
    //: func interactionView_shouldDisplayMiniWindow() {
    func futurism() {
        //: TalkingSocketManager.shared.currGeneralVC = self
        ConfigureAxisService.shared.currGeneralVC = self
        //: self.miniView = TalkingVideoMiniView.buildVideoMiniView()
        self.miniView = FountainPenMiniView.markBy()
        //: self.videoManager.smallRenderView = self.miniView?.renderView
        self.videoManager.smallRenderView = self.miniView?.renderView
        //: self.videoManager.enterSmallRenderMode()
        self.videoManager.capture()
        //: self.miniView?.tapGestureBlock = { [weak self] in
        self.miniView?.tapGestureBlock = { [weak self] in // 还原回通话界面
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: self.videoManager.exitSmallRenderMode()
            self.videoManager.failBy()
            //: self.isFromMiniView = true
            self.isFromMiniView = true
            //: self.currentViewController()?.navigationController?.pushViewController(self, animated: true)
            self.field()?.navigationController?.pushViewController(self, animated: true)
        }
        //: popCurrentViewController()
        cap()
    }

    /// 投诉成功
    //: func interactionView_reportSucceed() {
    func inner() {
        //: if reportView?.superview != nil {
        if reportView?.superview != nil {
            //: reportView?.removeFromSuperview()
            reportView?.removeFromSuperview()
        }
    }
}

// MARK: - Layout

//: extension TalkingVideoChatViewController {
extension AdvancedErrorDelegate {
    /// 添加视图
    //: private func setupSubviews() {
    private func isCircle() {
        //: self.view.layer.masksToBounds = true
        self.view.layer.masksToBounds = true
        //: self.view.addSubview(bgVideoView)
        self.view.addSubview(bgVideoView)
        //: self.view.addSubview(interactionView)
        self.view.addSubview(interactionView)
        //: self.interactionView.addWindowView(view: self.windowView)
        self.interactionView.condensedMilk(view: self.windowView)
    }

    /// 添加约束
    //: private func setupSubViewsConstraint() {
    private func displayNor() {
        //: bgVideoView.snp.makeConstraints { make in
        bgVideoView.snp.makeConstraints { make in
            //: make.edges.equalToSuperview()
            make.edges.equalToSuperview()
        }
        //: interactionView.snp.makeConstraints { make in
        interactionView.snp.makeConstraints { make in
            //: make.edges.equalToSuperview()
            make.edges.equalToSuperview()
        }
    }
}
