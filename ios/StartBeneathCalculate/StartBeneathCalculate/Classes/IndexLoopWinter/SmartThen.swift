
//: Declare String Begin

/*: "fee" :*/
fileprivate let dataTingFeedMessage:[Character] = ["f","e","e"]

/*: "VIPFee" :*/
fileprivate let notiIdentifyRandomBalloonId:[Character] = ["V"]
fileprivate let k_soundChangeTitle:String = "merge method perform collection imageIPFee"

/*: "chattingNum" :*/
fileprivate let user_scoreText:String = "chapriority"

/*: "You're already in her live room" :*/
fileprivate let k_acrossContent:[UInt8] = [0x90,0xa6,0xbc,0xee,0xbb,0xac,0xe9,0xa8,0xa5,0xbb,0xac,0xa8,0xad,0xb0,0xe9,0xa0,0xa7,0xe9,0xa1,0xac,0xbb,0xe9,0xa5,0xa0,0xbf,0xac,0xe9,0xbb,0xa6,0xa6,0xa4]

private func uropygialGland(frank num: UInt8) -> UInt8 {
    return num ^ 201
}

/*: "toUid" :*/
fileprivate let const_advancedText:[UInt8] = [0x64,0x69,0x55,0x6f,0x74]

/*: "momentId" :*/
fileprivate let k_grabTitle:[UInt8] = [0xaa,0xac,0xaa,0xa2,0xab,0xb1,0x86,0xa1]

fileprivate func switchicialLine(attribute num: UInt8) -> UInt8 {
    let value = Int(num) - 61
    if value < 0 {
        return UInt8(value + 256)
    } else {
        return UInt8(value)
    }
}

/*: "extra" :*/
fileprivate let userPanMsg:[UInt8] = [0x99,0x84,0x88,0x8e,0x9d]

private func smartForward(we num: UInt8) -> UInt8 {
    return num ^ 252
}

/*: "uid" :*/
fileprivate let user_themeStr:[UInt8] = [0x64,0x69,0x75]

/*: "onlineStatus" :*/
fileprivate let noti_boldValue:[UInt8] = [0xe3,0xe2,0xe0,0xe5,0xe2,0xe9,0xdf,0xf8,0xed,0xf8,0xf9,0xff]

private func scaleOfMeasurement(define num: UInt8) -> UInt8 {
    return num ^ 140
}

/*: "isNewUser" :*/
fileprivate let kComplyValue:[UInt8] = [0xad,0xb7,0x8a,0xa1,0xb3,0x91,0xb7,0xa1,0xb6]

private func haveFresh(nest num: UInt8) -> UInt8 {
    return num ^ 196
}

/*: "userStatus" :*/
fileprivate let app_stretchStr:[UInt8] = [0xda,0xd8,0xca,0xd7,0xb8,0xd9,0xc6,0xd9,0xda,0xd8]

fileprivate func miniDark(lift num: UInt8) -> UInt8 {
    let value = Int(num) - 101
    if value < 0 {
        return UInt8(value + 256)
    } else {
        return UInt8(value)
    }
}

/*: "Account is restricted！" :*/
fileprivate let const_bronzePath:String = "media mode saveAcco"
fileprivate let app_containName:String = "unt ivantage error ingredient nurse arrow"
fileprivate let show_receptionBodyStr:String = "sufficientri"

/*: "Failed to enter the chat room. Please try again later" :*/
fileprivate let k_includeTitle:[UInt8] = [0xc9,0xe4,0xec,0xef,0xe8,0xe7,0xa3,0xf7,0xf2,0xa3,0xe8,0xf1,0xf7,0xe8,0xf5,0xa3,0xf7,0xeb,0xe8,0xa3,0xe6,0xeb,0xe4,0xf7,0xa3,0xf5,0xf2,0xf2,0xf0,0xb1,0xa3,0xd3,0xef,0xe8,0xe4,0xf6,0xe8,0xa3,0xf7,0xf5,0xfc,0xa3,0xe4,0xea,0xe4,0xec,0xf1,0xa3,0xef,0xe4,0xf7,0xe8,0xf5]

fileprivate func passJudgment(available num: UInt8) -> UInt8 {
    let value = Int(num) - 131
    if value < 0 {
        return UInt8(value + 256)
    } else {
        return UInt8(value)
    }
}

/*: "url" :*/
fileprivate let mainNumberMessage:String = "urstream"

/*: "userDetail" :*/
fileprivate let userTeemMsg:String = "main scoreuserD"
fileprivate let kMagnitudeMsg:String = "espellil"

/*: "&type=6" :*/
fileprivate let data_plantName:String = "&type=6heap read"

/*: "&type=7" :*/
fileprivate let app_occurData:String = "toward harassment simultaneously&type=7"

/*: "amount" :*/
fileprivate let app_checkUrl:[UInt8] = [0xf1,0xfd,0xff,0xe5,0xfe,0xe4]

/*: "https://" :*/
fileprivate let appPanValue:[Character] = ["h","t","t"]
fileprivate let kCameraName:String = "ps://share request presentation expansion dialog"

/*: "http://" :*/
fileprivate let noti_literalContent:[Character] = ["h","t","t","p",":","/","/"]

/*: "transparency" :*/
fileprivate let appNumberryTitle:[Character] = ["t","r","a","n","s"]
fileprivate let kSureStr:String = "PARENCY"

/*: "version= :*/
fileprivate let noti_playValue:String = "version=feature eastern"

/*: &packageId= :*/
fileprivate let noti_arrayTitle:[Character] = ["&","p"]
fileprivate let userGenerateTitle:[Character] = ["a","c","k","a","g","e","I","d","="]

/*: &bundleId= :*/
fileprivate let main_featureValue:[Character] = ["&","b","u","n","d","l","e"]
fileprivate let noti_yieldMsg:String = "Id=adjustment stroke for announcement"

//: Declare String End

// __DEBUG__
// __CLOSE_PRINT__
//
//  SmartThen.swift
//  AbroadTalking
//
//  Created by Hemming on 2022/8/31.
//

//: import UIKit
import UIKit

//: public class SmartThen: NSObject {
public class SmartThen: NSObject {
    //: @objc static public let share = SmartThen()
    @objc public static let share = SmartThen()
    //: private override init() {}
    override private init() {}
}

//: extension SmartThen {
extension SmartThen {
    // MARK: - 随机视频入口

    //: func func__pushToRandomVideoVC(isBeginRand: Bool) {
    func recto(isBeginRand: Bool) {
        //: uploadRecord.uploadRecordEvent(eventID: ClickInRandomvideo)
        kSaveKey.putAvailable(eventID: user_beginName)
        //: guard TalkingSocketManager.shared.isGeneral == false else {
        guard AlsoThen.shared.isGeneral == false else {
            //: self.func__showStatusBarErrorMsg(showMsg: kMessage_videoTalking_limit)
            self.micromicronProcess(showMsg: notiSinceKey)
            //: return
            return
        }
        //: TalkingPermissionTool.checkCameraAndMicrophone { isOpen in
        ActivityReactiveCompatible.counteraction { isOpen in
            //: guard isOpen == true else { return }
            guard isOpen == true else { return }
            //: TalkingQuickVideoManager.req_videoMatchCheck() { succeed, result, errorModel in
            FactorReactiveCompatible.sourceDay { succeed, result, _ in
                //: guard succeed else { return }
                guard succeed else { return }
                //: let json = JSON(result ?? [String: Any]())
                let json = JSON(result ?? [String: Any]())
                //: let vc = TalkingRandomVideoViewController()
                let vc = LiteralViewController()
                //: vc.fee = json["fee"].intValue
                vc.fee = json[(String(dataTingFeedMessage))].intValue
                //: vc.vipFee = json["VIPFee"].intValue
                vc.vipFee = json[(String(notiIdentifyRandomBalloonId) + String(k_soundChangeTitle.suffix(5)))].intValue
                //: vc.chattingNum = json["chattingNum"].intValue
                vc.chattingNum = json[(user_scoreText.replacingOccurrences(of: "priority", with: "tt") + "ingNum")].intValue
                //: vc.isBeginRand = isBeginRand
                vc.isBeginRand = isBeginRand
                //: vc.isFirstRandow = isBeginRand
                vc.isFirstRandow = isBeginRand
                //: self.currentViewController()?.navigationController?.pushViewController(vc, animated: true)
                self.field()?.navigationController?.pushViewController(vc, animated: true)
            }
        }
    }

    // MARK: - 跳转个人资料

    //: func func__pushToUserDetailVC(uid: String?) {
    func numbererest(uid: String?) {
        //: guard let currentVC = self.currentViewController() else { return }
        guard let currentVC = self.field() else { return }
        //: if let profileVC = currentVC as? TalkingUserDetailViewController {
        if let profileVC = currentVC as? ReadyViewDelegate {
            // 同一用户的资料页已经在顶部，只刷新
            //: if profileVC.uid == uid {
            if profileVC.uid == uid {
                //: profileVC.requestAllData()
                profileVC.magnitudery()
                //: return
                return
            }
        }

        //: let vc = TalkingUserDetailViewController(tempUid: uid)
        let vc = ReadyViewDelegate(tempUid: uid)
        //: getNavigationController()?.pushViewController(vc, animated: true)
        shorePast()?.pushViewController(vc, animated: true)
    }

    // MARK: - 观众侧进入直播间

    /// 观众侧跳转到直播间
    /// - Parameters:
    ///   - uid: 主播id
    ///   - enterType: 入口
    //: func func_audiencePushToLiveRoomVC(uid: String, enterType: LiveEnterType) {
    func simpleAlong(uid: String, enterType: AvailableEnterType) {
        // 同一直播间只能进一次
        //: guard String(TalkingLiveManager.shared().liveRoomModel.streamerInfo.uid) != uid else {
        guard String(BeReactiveCompatible.clearExplain().liveRoomModel.streamerInfo.uid) != uid else {
            //: self.func__showStatusBarErrorMsg(showMsg: "You're already in her live room".localized)
            self.micromicronProcess(showMsg: String(bytes: k_acrossContent.map{uropygialGland(frank: $0)}, encoding: .utf8)!.localized)
            //: return
            return
        }
        // 校验接口，通过后进入直播间
        //: TalkingAudienceManager().req_enterLiveRoom(streamerUid: uid) { succeed, result, errorModel in
        FriendshipThen().awakeCompletion(streamerUid: uid) { succeed, result, _ in
            //: guard succeed else { return }
            guard succeed else { return }
            //: guard let livemodel = TalkingLiveRoomModel.deserialize(from: result as? Dictionary) else { return }
            guard let livemodel = AlsoHandyJSON.deserialize(from: result as? Dictionary) else { return }
            // 退出之前的直播间
            //: let currentVC = self.func__getCurrentActivityVC()
            let currentVC = self.stepFind()
            //: if let vcArr = currentVC?.navigationController?.viewControllers {
            if let vcArr = currentVC?.navigationController?.viewControllers {
                //: for index in 0..<(vcArr.count) {
                for index in 0 ..< (vcArr.count) {
                    //: let vc = vcArr[index]
                    let vc = vcArr[index]
                    //: if vc.isKind(of: TalkingLivePullStreamsViewController.self) == true {
                    if vc.isKind(of: ReportedViewDelegate.self) == true {
                        //: (vc as! TalkingLivePullStreamsViewController).popCurrentViewController()
                        (vc as! ReportedViewDelegate).route()
                    }
                }
            }

            // 进入新直播间
            //: TalkingLiveManager.shared().liveRoomModel = livemodel
            BeReactiveCompatible.clearExplain().liveRoomModel = livemodel
            //: let liveVc = TalkingLivePullStreamsViewController()
            let liveVc = ReportedViewDelegate()
            //: liveVc.type = enterType
            liveVc.type = enterType
            //: currentVC?.navigationController?.pushViewController(liveVc, animated: true)
            currentVC?.navigationController?.pushViewController(liveVc, animated: true)
            // 埋点
            //: switch enterType {
            switch enterType {
            //: case .match:
            case .match:
                //: uploadRecord.uploadRecordEvent(eventID: ClickMatchLiveWindowNoP, toUid: uid)
                kSaveKey.putAvailable(eventID: user_liveData, toUid: uid)
            //: case .userDetail:
            case .userDetail:
                //: uploadRecord.uploadRecordEvent(eventID: ClickProfileLiveWindowNoP, toUid: uid)
                kSaveKey.putAvailable(eventID: appSucceedTopStr, toUid: uid)
            //: case .unknown: break
            case .unknown: break
            }
        }
    }

    // MARK: - 跳转聊天

    /// 跳转到消息列表页
    /// - Parameter isHalf: 是否半屏展示
    //: func func__pushToChatListVC(isHalfView: Bool = false) {
    func followView(isHalfView: Bool = false) {
        //: let vc = TalkingChatListViewController(isHalfView: isHalfView)
        let vc = TagViewController(isHalfView: isHalfView)
        //: let currentVC = func__getCurrentActivityVC()
        let currentVC = stepFind()
        //: if isHalfView == false {
        if isHalfView == false {
            //: currentVC?.navigationController?.pushViewController(vc, animated: true)
            currentVC?.navigationController?.pushViewController(vc, animated: true)
            //: } else {
        } else {
            //: currentVC?.addChild(vc)
            currentVC?.addChild(vc)
            //: currentVC?.view.addSubview(vc.view)
            currentVC?.view.addSubview(vc.view)
        }
    }

    /// 跳转到消息聊天页
    /// - Parameters:
    ///   - chatID: 对方Id
    ///   - isFrom: 私聊页入口
    /// - Returns: 消息聊天页

    //: func func__pushToPriveteChatVC(chatID: String, isFrom: PrivateChatIsFromEnum = .Normal, completion: ((_ vc: TalkingPrivateChatController) -> Void)? = nil ) {
    func aboveCompletion(chatID: String, isFrom: ArrayAdvancedFromEnum = .Normal, completion: ((_ vc: BorderInsularViewDelegate) -> Void)? = nil) {
        //: guard chatID.count > 0 else { return }
        guard chatID.count > 0 else { return }

        //: if chatID != MacroBorderDefineThen.getXiaoMiID() {
        if chatID != MacroBorderDefineThen.seethe() { // 系统消息
            //: ProgressHUD.show()
            StrangerProgressHUD.greenDirect()
            //: let param = ["toUid": chatID, "momentId": "0", "extra": "1"]
            let param = [String(bytes: const_advancedText.reversed(), encoding: .utf8)!: chatID, String(bytes: k_grabTitle.map{switchicialLine(attribute: $0)}, encoding: .utf8)!: "0", String(bytes: userPanMsg.map{smartForward(we: $0)}, encoding: .utf8)!: "1"]
            //: TalkingChatRequestTool.req_refreshUserChatInfo(param: param) { [weak self] succeed, result, errorModel in
            PouchReactiveCompatible.frontEnd(param: param) { [weak self] succeed, result, _ in
                //: ProgressHUD.dismiss()
                StrangerProgressHUD.barDismiss()
                //: guard let self = self else {return}
                guard let self = self else { return }
                //: guard succeed else { return }
                guard succeed else { return }

                //: let userWrap = ConsumerPenHandyJSON.func__transformedChatinfo(userDic: result as! Dictionary<String, Any>)
                let userWrap = ConsumerPenHandyJSON.advancedVertical(userDic: result as! [String: Any])
                //: DBUserInfoManager.cache_updateMsgUserInfoAndPostNotif(with: userWrap)
                InertGasThen.blocked(with: userWrap)
                // 更新消息列表用户在线状态
                //: let userInfo = ["uid": chatID,
                let userInfo = [String(bytes: user_themeStr.reversed(), encoding: .utf8)!: chatID,
                                //: "onlineStatus": userWrap.onlineStatus,
                                String(bytes: noti_boldValue.map{scaleOfMeasurement(define: $0)}, encoding: .utf8)!: userWrap.onlineStatus,
                                //: "isNewUser":userWrap.isNewUser,
                                String(bytes: kComplyValue.map{haveFresh(nest: $0)}, encoding: .utf8)!: userWrap.isNewUser,
                                //: "userStatus": userWrap.userStatus]
                                String(bytes: app_stretchStr.map{miniDark(lift: $0)}, encoding: .utf8)!: userWrap.userStatus]
                //: NotificationCenter.default.post(name: MSGLIST_UPDATE_ONLINESTATUS_NOTIFICATION,
                NotificationCenter.default.post(name: show_deviceFormat,
                                                //: object: nil,
                                                object: nil,
                                                //: userInfo: userInfo)
                                                userInfo: userInfo)
                //: if userWrap.userStatus == 3 {
                if userWrap.userStatus == 3 { /// 被封号
                    //: self.func__showStatusBarErrorMsg(showMsg: "Account is restricted！".localized)
                    self.micromicronProcess(showMsg: (String(const_bronzePath.suffix(4)) + String(app_containName.prefix(5)) + "s re" + show_receptionBodyStr.replacingOccurrences(of: "sufficient", with: "st") + "cted！").localized)
                    //: return
                    return
                }
                //: DispatchQueue.main.async {
                DispatchQueue.main.async {
                    //: let chatVC = self.func__canPushToPriveteChatVC(chatID: chatID, isFrom: isFrom, userWrap: userWrap)
                    let chatVC = self.attributeShrink(chatID: chatID, isFrom: isFrom, userWrap: userWrap)
                    //: completion?(chatVC)
                    completion?(chatVC)
                }
            }
            //: }else{
        } else {
            //: DispatchQueue.main.async {
            DispatchQueue.main.async {
                //: let chatVC = self.func__canPushToPriveteChatVC(chatID: chatID, isFrom: isFrom, userWrap: ConsumerPenHandyJSON())
                let chatVC = self.attributeShrink(chatID: chatID, isFrom: isFrom, userWrap: ConsumerPenHandyJSON())
                //: completion?(chatVC)
                completion?(chatVC)
            }
        }
    }

    //: func func__canPushToPriveteChatVC(chatID: String, isFrom: PrivateChatIsFromEnum = .Normal, userWrap: ConsumerPenHandyJSON ) -> TalkingPrivateChatController {
    func attributeShrink(chatID: String, isFrom: ArrayAdvancedFromEnum = .Normal, userWrap: ConsumerPenHandyJSON) -> BorderInsularViewDelegate {
        //: let currentVC = func__getCurrentActivityVC()
        let currentVC = stepFind()
        //: if isFrom != .LiveRoom, let vcArr = currentVC?.navigationController?.viewControllers {
        if isFrom != .LiveRoom, let vcArr = currentVC?.navigationController?.viewControllers {
            //: for index in 0..<(vcArr.count) {
            for index in 0 ..< (vcArr.count) {
                //: if vcArr[index] .isKind(of: TalkingPrivateChatController.self) == true {
                if vcArr[index].isKind(of: BorderInsularViewDelegate.self) == true {
                    //: let vc = vcArr[index] as! TalkingPrivateChatController
                    let vc = vcArr[index] as! BorderInsularViewDelegate
                    //: if vc.msgView.targetID == chatID {
                    if vc.msgView.targetID == chatID {
                        //: currentVC?.navigationController?.popToViewController(vc, animated: true)
                        currentVC?.navigationController?.popToViewController(vc, animated: true)
                        //: return vc
                        return vc
                    }
                }
            }
        }

        //: let vc = TalkingPrivateChatController(chatID: chatID, isFrom: isFrom, userWrap: userWrap)
        let vc = BorderInsularViewDelegate(chatID: chatID, isFrom: isFrom, userWrap: userWrap)
        //: if isFrom == .LiveRoom {
        if isFrom == .LiveRoom {
            //: currentVC?.addChild(vc)
            currentVC?.addChild(vc)
            //: currentVC?.view.addSubview(vc.view)
            currentVC?.view.addSubview(vc.view)
            //: } else {
        } else {
            //: currentVC?.navigationController?.pushViewController(vc, animated: true)
            currentVC?.navigationController?.pushViewController(vc, animated: true)
        }
        //: return vc
        return vc
    }

    // MARK: - 跳转群聊

    //: func func__pushToGroupChat(groupId: String) {
    func patentId(groupId: String) {
        //: guard groupId.count > 0 else { return }
        guard groupId.count > 0 else { return }
        //: ProgressHUD.show()
        StrangerProgressHUD.greenDirect()
        //: TalkingChatRequestTool.req_checkChatRoom(roomId: groupId) { succeed, result, errorModel in
        PouchReactiveCompatible.combine(roomId: groupId) { succeed, result, errorModel in
            //: ProgressHUD.dismiss()
            StrangerProgressHUD.barDismiss()
            //: guard succeed == true else {
            guard succeed == true else {
                //: if errorModel?.errorCode == 106 {
                if errorModel?.errorCode == 106 {
                    //: } else {
                } else {
                    //: self.func__showStatusBarErrorMsg(showMsg: errorModel?.errorMsg ?? "")
                    self.micromicronProcess(showMsg: errorModel?.errorMsg ?? "")
                    //: if errorModel?.errorCode == 103 {
                    if errorModel?.errorCode == 103 { // 不在家族里
                    }
                }

                //: return
                return
            }

            //: guard let data = result as? [String: Any] else {
            guard let data = result as? [String: Any] else {
                //: return
                return
            }

            //: if MacroBorderDefineThen.isGroupChat(groupId) {
            if MacroBorderDefineThen.climbAcross(groupId) {
                //: } else {
            } else { // 公共聊天室
                //: V2TIMManager.sharedInstance().joinGroup(groupId, msg: nil) {
                V2TIMManager.sharedInstance().joinGroup(groupId, msg: nil) {
                    //: guard self.func__checkIsGroupChatVC(roomId: groupId) == false else { return }
                    guard self.blankId(roomId: groupId) == false else { return }
                    //: let vc = TalkingGroupChatController(groupType: GroupType_Meeting, infoData: data)
                    let vc = UncompressChatController(groupType: GroupType_Meeting, infoData: data)
                    //: self.func__getCurrentActivityVC()?.navigationController?.pushViewController(vc, animated: true)
                    self.stepFind()?.navigationController?.pushViewController(vc, animated: true)

                    //: } fail: { code, desc in
                } fail: { code, _ in
                    //: if code == 10013 {
                    if code == 10013 { // 程序异常退出，未退出聊天室
                        //: guard self.func__checkIsGroupChatVC(roomId: groupId) == false else { return }
                        guard self.blankId(roomId: groupId) == false else { return }
                        //: let vc = TalkingGroupChatController(groupType: GroupType_Meeting, infoData: data)
                        let vc = UncompressChatController(groupType: GroupType_Meeting, infoData: data)
                        //: self.func__getCurrentActivityVC()?.navigationController?.pushViewController(vc, animated: true)
                        self.stepFind()?.navigationController?.pushViewController(vc, animated: true)
                        //: } else {
                    } else {
                        //: self.func__showStatusBarErrorMsg(showMsg: "Failed to enter the chat room. Please try again later".localized)
                        self.micromicronProcess(showMsg: String(bytes: k_includeTitle.map{passJudgment(available: $0)}, encoding: .utf8)!.localized)
                    }
                }
            }
        }
    }

    /// 检测堆栈中是否有群聊控制器，有则跳转到群聊页
    /// - Parameter roomId: 房间Id
    /// - Returns: 检测结果（true: 有; false: 无）
    //: private func func__checkIsGroupChatVC(roomId: String) -> Bool {
    private func blankId(roomId: String) -> Bool {
        //: let currentVC = self.func__getCurrentActivityVC()
        let currentVC = self.stepFind()
        //: if let vcArr = currentVC?.navigationController?.viewControllers {
        if let vcArr = currentVC?.navigationController?.viewControllers {
            //: for index in 0..<(vcArr.count) {
            for index in 0 ..< (vcArr.count) {
                //: if vcArr[index].isKind(of: TalkingGroupChatController.self) == true {
                if vcArr[index].isKind(of: UncompressChatController.self) == true {
                    //: let vc = vcArr[index] as! TalkingGroupChatController
                    let vc = vcArr[index] as! UncompressChatController
                    //: if vc.infoModel.roomId == roomId {
                    if vc.infoModel.roomId == roomId {
                        //: currentVC?.navigationController?.popToViewController(vc, animated: true)
                        currentVC?.navigationController?.popToViewController(vc, animated: true)
                        //: return true
                        return true
                    }
                }
            }
        }

        //: return false
        return false
    }

    // MARK: - 任意用户的动态列表

    //: func func__pushUserPostListWithUid(uid: String) {
    func yesterdayCos(uid _: String) {}

    // MARK: - 反馈feebook

    //: func func__pushFeedbackVC() {
    func tipVc() {
        //: let currentVC = func__getCurrentActivityVC()
        let currentVC = stepFind()
        //: let vc = TalkingFeedbackVC.init()
        let vc = AttachViewController()
        //: currentVC?.navigationController?.pushViewController(vc, animated: true)
        currentVC?.navigationController?.pushViewController(vc, animated: true)
    }

    // MARK: - 消息跳转

    //: func func__actionPushForMessage(jumpModel: TalkingMessageJumpModel) {
    func questionable(jumpModel: PhoneMessageTransformable) {
        //: if jumpModel.jumpKey == "url" {
        if jumpModel.jumpKey == (mainNumberMessage.replacingOccurrences(of: "stream", with: "l")) {
            //: func__pushToWebVC(urlStr: jumpModel.url)
            lifeStyle(urlStr: jumpModel.url)
            //: } else if jumpModel.jumpKey == "userDetail"{
        } else if jumpModel.jumpKey == (String(userTeemMsg.suffix(5)) + kMagnitudeMsg.replacingOccurrences(of: "spell", with: "ta")) {}
    }

    // MARK: - 会员订阅网页

    //: @objc public func func__pushToSubscribePageWebVC() {
    @objc public func subscribe() {
        //: SmartThen.share.func__pushToWebVC(webViewType: .SubscribePage)
        SmartThen.share.joinType(webViewType: .SubscribePage)
    }
}

// MARK: - 跳转H5

//: extension SmartThen {
extension SmartThen {
    // 跳转全屏H5入口
    //: func func__pushToWebVC(webViewType: WebUrlType) {
    func joinType(webViewType: BorderBasicType) {
        //: func__pushToWebVC(webViewType: webViewType, webConfig: TalkingWebConfig())
        constituent(webViewType: webViewType, webConfig: CalendarConfig())
    }

    /// 系统主动拨打弹窗，余额不足跳转充值/订阅页
    //: func func__jumpVideoCallToWebRecharge(popupCallIndex: Int, appendParams: String = "&type=6") {
    func manualMid(popupCallIndex: Int, appendParams: String = (String(data_plantName.prefix(7)))) {
        //: let view = TalkingInsufficientBalanceView.init()
        let view = ShrewdView()
        //: view.show()
        view.innerShow()
        //: view.openBtnClock = { [weak self] in
        view.openBtnClock = { [weak self] in
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: if popupCallIndex == 1 {
            if popupCallIndex == 1 { // 半屏充值页
                //: self.func__pushToHalfWebVC(webViewType: .RechargeHalfPage)
                self.gallery(webViewType: .RechargeHalfPage)
                //: } else if popupCallIndex == 2 {
            } else if popupCallIndex == 2 { // 会员订阅弹窗
                //: func__pushToSubscribeAlert(appendParams: appendParams)
                thumbParams(appendParams: appendParams)
            }
        }
    }

    /// 余额不足跳转充值/订阅页
    /// - Parameters:
    ///   - clickEvent: 埋点事件
    ///   - sufficient: 余额是否充足（默认true）
    //: func func__jumpToWebRecharge(clickEvent: String = "", sufficient: Bool = true, appendParams: String = "&type=7") {
    func bilge(clickEvent: String = "", sufficient: Bool = true, appendParams: String = (String(app_occurData.suffix(7)))) {
        //: let payWinType = FacultyReactiveCompatible.share.appUserConfigMode.payWinType
        let payWinType = FacultyReactiveCompatible.share.appUserConfigMode.payWinType
        // 样式：半屏充值页 || (已订阅 && 余额不足)

        //: let view = TalkingInsufficientBalanceView.init()
        let view = ShrewdView()
        //: view.show()
        view.innerShow()
        //: view.openBtnClock = { [weak self] in
        view.openBtnClock = { [weak self] in
            //: guard let self = self else {
            guard let self = self else {
                //: return
                return
            }
            //: if payWinType == 1 || (FacultyReactiveCompatible.share.loginUserMode.loungePlus && sufficient == false) {
            if payWinType == 1 || (FacultyReactiveCompatible.share.loginUserMode.loungePlus && sufficient == false) { // 半屏充值页
                //: self.func__pushToHalfWebVC(webViewType: .RechargeHalfPage, clickEvent: clickEvent)
                self.gallery(webViewType: .RechargeHalfPage, clickEvent: clickEvent)
                //: } else if payWinType == 2 {
            } else if payWinType == 2 { // 会员订阅弹窗
                //: func__pushToSubscribeAlert(appendParams: appendParams)
                thumbParams(appendParams: appendParams)
            }
        }
    }

    /// 跳转会员订阅弹窗
    /// - Parameter appendParams: url拼接参数（假视频流程拼接："&type=6；默认流程："&type=7"）
    //: func func__pushToSubscribeAlert(appendParams: String = "&type=7") {
    func thumbParams(appendParams: String = (String(app_occurData.suffix(7)))) {
        //: var urlStr = TalkingWebManager.getFullUrl(urlType: .SubscribeAlert)
        var urlStr = GuideThen.hire(urlType: .SubscribeAlert)
        //: urlStr.append(appendParams)
        urlStr.append(appendParams)
        //: SmartThen.share.func__pushToWebVC(urlStr: urlStr)
        SmartThen.share.lifeStyle(urlStr: urlStr)
        // 曝光用户数埋点
        //: uploadRecord.uploadRecordEvent(eventID: subscribtionRecordButton, parameterStr: ["amount": 0])
        kSaveKey.parameterStr(eventID: const_pingPath, parameterStr: [String(bytes: app_checkUrl.map{$0^144}, encoding: .utf8)!: 0])
    }

    /// 跳转半屏H5入口
    /// - Parameters:
    ///   - webViewType: 网页URL
    ///   - clickEvent: 埋点事件
    //: func func__pushToHalfWebVC(webViewType: WebUrlType, clickEvent: String = "") {
    func gallery(webViewType: BorderBasicType, clickEvent: String = "") {
        //: var config = TalkingWebConfig.init()
        var config = CalendarConfig()
        //: config.showLoadingView = false
        config.showLoadingView = false
        //: config.isHalfView = true
        config.isHalfView = true
        //: func__pushToWebVC(webViewType: webViewType, webConfig: config)
        constituent(webViewType: webViewType, webConfig: config)
        // 充值埋点
        //: if webViewType == .RechargeHalfPage {
        if webViewType == .RechargeHalfPage {
            //: RECHARGE_CLICK_EVENT = clickEvent
            show_depthLastSelectValue = clickEvent
            //: if clickEvent.count > 0 {
            if clickEvent.count > 0 {
                //: uploadRecord.uploadRecordEvent(eventID: clickEvent, parameterStr: ["amount": 0])
                kSaveKey.parameterStr(eventID: clickEvent, parameterStr: [String(bytes: app_checkUrl.map{$0^144}, encoding: .utf8)!: 0])
            }
        }
    }

    /// 跳转到充值优惠弹窗
    //: func func_pushToDiscountRechargeView() {
    func onConvert() {
        //: guard !FacultyReactiveCompatible.share.appUserConfigMode.discountUrl.isEmpty else { return }
        guard !FacultyReactiveCompatible.share.appUserConfigMode.discountUrl.isEmpty else { return }
        //: let httpsUrl = FacultyReactiveCompatible.share.appConfigMode.urlH5Domain + FacultyReactiveCompatible.share.appUserConfigMode.discountUrl
        let httpsUrl = FacultyReactiveCompatible.share.appConfigMode.urlH5Domain + FacultyReactiveCompatible.share.appUserConfigMode.discountUrl
        //: let httpUrl = httpsUrl.replacingOccurrences(of: "https://", with: "http://")
        let httpUrl = httpsUrl.replacingOccurrences(of: (String(appPanValue) + String(kCameraName.prefix(5))), with: (String(noti_literalContent)))
        //: SmartThen.share.func__pushToWebVC(urlStr: httpUrl)
        SmartThen.share.lifeStyle(urlStr: httpUrl)
    }

    //: func func__pushToWebVC(webViewType: WebUrlType, webConfig: TalkingWebConfig) {
    func constituent(webViewType: BorderBasicType, webConfig: CalendarConfig) {
        //: let urlStr = TalkingWebManager.getFullUrl(urlType: webViewType)
        let urlStr = GuideThen.hire(urlType: webViewType)
        //: func__pushToWebView(urlStr: urlStr, webViewType: webViewType, webConfig: webConfig)
        lengthAfterConfig(urlStr: urlStr, webViewType: webViewType, webConfig: webConfig)
    }

    //: func func__pushToWebVC(urlStr: String?, webConfig: TalkingWebConfig? = nil) {
    func lifeStyle(urlStr: String?, webConfig: CalendarConfig? = nil) {
        //: let config = (webConfig == nil) ? TalkingWebConfig():webConfig
        let config = (webConfig == nil) ? CalendarConfig() : webConfig
        //: func__pushToWebView(urlStr: urlStr, webViewType: nil, webConfig: config!)
        lengthAfterConfig(urlStr: urlStr, webViewType: nil, webConfig: config!)
    }

    //: func func__pushToWebView(urlStr: String?, webViewType: WebUrlType?, webConfig: TalkingWebConfig) {
    func lengthAfterConfig(urlStr: String?, webViewType: BorderBasicType?, webConfig: CalendarConfig) {
        //: guard urlStr != nil && urlStr != "" else { return }
        guard urlStr != nil, urlStr != "" else { return }
        //: var config = webConfig
        var config = webConfig

        // 有值且大于0，半屏按照比例展示
        //: if config.widthHeight ?? 0 > 0 {
        if config.widthHeight ?? 0 > 0 {
            //: config.isHalfView = true
            config.isHalfView = true
        }
        // 解析url中带的参数
        //: let urlParams = JSON(urlStr!.urlParameters ?? [:])
        let urlParams = JSON(urlStr!.urlParameters ?? [:])
        //: if urlParams["transparency"].stringValue == "1" { // 透明背景
        if urlParams[(String(appNumberryTitle) + kSureStr.lowercased())].stringValue == "1" { // 透明背景
            //: config.clearBgColor = true
            config.clearBgColor = true
        }

        // url拼接参数
        //: let allowCharSet: CharacterSet = CharacterSet.init(charactersIn: "`%^{}\"[]|\\<>").inverted
        let allowCharSet = CharacterSet(charactersIn: "`%^{}" + "\"" + "[]|\\<>").inverted
        //: var urlStr = urlStr!.addingPercentEncoding(withAllowedCharacters: allowCharSet)!
        var urlStr = urlStr!.addingPercentEncoding(withAllowedCharacters: allowCharSet)!
        //: let otherParams = "version=\(AppNetVersion)&packageId=\(PackageID)&bundleId=\(AppBundle)"
        let otherParams = (String(noti_playValue.prefix(8))) + "\(data_serviceFormat)" + (String(noti_arrayTitle) + String(userGenerateTitle)) + "\(showViewValue)" + (String(main_featureValue) + String(noti_yieldMsg.prefix(3))) + "\(const_onMsg)"
        //: if urlStr.contains("?") {
        if urlStr.contains("?") {
            //: urlStr = "\(urlStr)&\(otherParams)"
            urlStr = "\(urlStr)&\(otherParams)"
            //: } else {
        } else {
            //: urlStr = "\(urlStr)?\(otherParams)"
            urlStr = "\(urlStr)?\(otherParams)"
        }

        //: let vc = TalkingWebViewController.init(urlString: urlStr, webViewType: webViewType, webConfig: config)
        let vc = RecognizeViewController(urlString: urlStr, webViewType: webViewType, webConfig: config)

        // 展示网页
        //: if let currentVC = func__getCurrentActivityVC() {
        if let currentVC = stepFind() {
            // 如果当前视图是模态上来的，跳转到下级时只能使用模态效果跳转
            //: var currentVCIsPresent = false
            var currentVCIsPresent = false
            //: if currentVC.isKind(of: TalkingWebViewController.self) &&
            if currentVC.isKind(of: RecognizeViewController.self) &&
                //: (currentVC as! TalkingWebViewController).isModal == true {
                (currentVC as! RecognizeViewController).isModal == true
            {
                //: currentVCIsPresent = true
                currentVCIsPresent = true
            }

            //: if config.isHalfView {
            if config.isHalfView { // 半屏使用present
                //: if config.widthHeight == nil {
                if config.widthHeight == nil {
                    //: vc.tranConfig.frameOfPresentedView = CGRect(x: 0, y: 0, width: ScreenWidth, height: ScreenHeight*2/3)
                    vc.tranConfig.frameOfPresentedView = CGRect(x: 0, y: 0, width: main_assetUrl, height: kTopUrl * 2 / 3)
                    //: vc.tranConfig.configCorner(corners: [.topLeft, .topRight], radius: 18)
                    vc.tranConfig.goldBalance(corners: [.topLeft, .topRight], radius: 18)
                    //: } else {
                } else {
                    //: vc.tranConfig.frameOfPresentedView = CGRect(x: 0, y: 0, width: ScreenWidth, height: ScreenWidth/config.widthHeight!)
                    vc.tranConfig.frameOfPresentedView = CGRect(x: 0, y: 0, width: main_assetUrl, height: main_assetUrl / config.widthHeight!)
                }
                //: vc.isModal = true
                vc.isModal = true
                //: currentVC.present(vc, animated: true)
                currentVC.present(vc, animated: true)

                //: } else if config.clearBgColor || currentVCIsPresent {
            } else if config.clearBgColor || currentVCIsPresent { // 透明背景只能用present
                //: vc.tranConfig.frameOfPresentedView = CGRect(x: 0, y: 0, width: ScreenWidth, height: ScreenHeight)
                vc.tranConfig.frameOfPresentedView = CGRect(x: 0, y: 0, width: main_assetUrl, height: kTopUrl)
                //: vc.isModal = true
                vc.isModal = true
                //: currentVC.present(vc, animated: true)
                currentVC.present(vc, animated: true)

                //: } else {
            } else {
                //: vc.isModal = false
                vc.isModal = false
                //: currentVC.navigationController?.pushViewController(vc, animated: true)
                currentVC.navigationController?.pushViewController(vc, animated: true)
            }
        }
    }
}

// MARK: - 获取当前视图、导航

//: extension SmartThen {
public extension SmartThen {
    //: @objc public func func__getCurrentActivityVC() -> UIViewController? {
    @objc func stepFind() -> UIViewController? {
        //: return currentViewController()
        return field()
    }
}

//: struct TalkingMessageJumpModel: HandyJSON {
struct PhoneMessageTransformable: HandyJSON {
    //: var jumpKey: String?
    var jumpKey: String? // 评论的id
    //: var jumpUid: String?
    var jumpUid: String?
    //: var url: String?
    var url: String?
}
