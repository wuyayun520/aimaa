
//: Declare String Begin

/*: "bg_message_top" :*/
fileprivate let app_affectTitle:[Character] = ["b","g","_","m","e","s"]
fileprivate let k_accountingContent:String = "simplege"

/*: "777777" :*/
fileprivate let data_awakeKey:String = "777777"

/*: "Messages" :*/
fileprivate let notiOutlinePath:[Character] = ["M","e","s","s","a","g","e"]
fileprivate let data_reachName:String = "presentation"

/*: "Who like me" :*/
fileprivate let dataCreationMsg:[Character] = ["W","h","o"," ","l","i","k","e"," ","m","e"]

/*: "Call" :*/
fileprivate let userSpreadMsg:String = "Callsum very"

/*: "#FF2348" :*/
fileprivate let mainWidespreadName:[Character] = ["#","F","F","2","3","4","8"]

/*: "Current network unavailable" :*/
fileprivate let notiRefKey:[Character] = ["C","u","r","r","e","n","t"," ","n","e","t","w","o","r","k"," ","u","n","a","v","a","i","l"]
fileprivate let main_resumeMsg:String = "acellle"

/*: "icon_yidu_pre" :*/
fileprivate let mainOrganizationPath:String = "control screenicon_"

/*: "transform.rotation" :*/
fileprivate let constNetName:[Character] = ["t","r","a","n","s","f","o","r","m"]
fileprivate let mainHoneyRandomData:[Character] = [".","r","o","t","a","t","i","o","n"]

/*: "transform.scale" :*/
fileprivate let main_redData:String = "current"
fileprivate let k_windowValue:String = "rplainnsf"

/*: "zoom&shake" :*/
fileprivate let show_exceptionMessage:String = "ZOOM"
fileprivate let main_contactRoutineValue:String = "&shaketotal resistance currency"

/*: "yyyy-MM-dd" :*/
fileprivate let notiCorrectComplexName:String = "yyyy-club series weak thread"
fileprivate let user_putMsg:String = "MM-ddhome glass"

/*: "Allow %@ to send you notifications?" :*/
fileprivate let const_screenFormat:[UInt8] = [0xb3,0x9e,0x9e,0x9d,0x85,0xd2,0xd7,0xb2,0xd2,0x86,0x9d,0xd2,0x81,0x97,0x9c,0x96,0xd2,0x8b,0x9d,0x87,0xd2,0x9c,0x9d,0x86,0x9b,0x94,0x9b,0x91,0x93,0x86,0x9b,0x9d,0x9c,0x81,0xcd]

private func standOut(tire num: UInt8) -> UInt8 {
    return num ^ 242
}

/*: "Cancel" :*/
fileprivate let mainClipBeName:String = "paper"
fileprivate let notiLengthTagKey:String = "inventoryncel"

/*: "Settings" :*/
fileprivate let show_pauseId:[Character] = ["S"]
fileprivate let const_willMessage:String = "ettiprovidergs"

/*: "badNumber" :*/
fileprivate let kOptionStr:String = "push tolerancebadNumber"

/*: "isConnection" :*/
fileprivate let main_maleFormat:String = "isConnemoon body via"
fileprivate let mainRemoveUrl:String = "climitedon"

/*: "networkStatus" :*/
fileprivate let show_temporaryPath:[Character] = ["n","e","t","w","o"]
fileprivate let dataDailySomeoneKey:String = "partner first spec templaterkSt"

/*: "unreadMessageNum" :*/
fileprivate let noti_stripValue:[Character] = ["u","n","r","e","a","d","M","e","s","s"]
fileprivate let notiAskKey:String = "ageNumsuccessfully uniform mated transform"

/*: "Do you want to mark all messages as read?" :*/
fileprivate let data_dateValue:[UInt8] = [0xbb,0x90,0xdf,0x86,0x90,0x8a,0xdf,0x88,0x9e,0x91,0x8b,0xdf,0x8b,0x90,0xdf,0x92,0x9e,0x8d,0x94,0xdf,0x9e,0x93,0x93,0xdf,0x92,0x9a,0x8c,0x8c,0x9e,0x98,0x9a,0x8c,0xdf,0x9e,0x8c,0xdf,0x8d,0x9a,0x9e,0x9b,0xc0]

private func heartMenuSequence(match num: UInt8) -> UInt8 {
    return num ^ 255
}

/*: "消息列表一键已读失败：code: :*/
fileprivate let dataAskGuideUnionTitle:[Character] = ["消","\u{606f}","列","表","一","键","已","读","失","败","：","c","o"]
fileprivate let noti_tabLifePath:String = "de:zz opportunity topic shirt"

/*: , desc: :*/
fileprivate let mainFrameValue:String = ", desc:group clock"

//: Declare String End

// __DEBUG__
// __CLOSE_PRINT__
//
//  AgencyReactiveCompatible.swift
//  AbroadTalking
//
//  Created by Hemming on 2022/1/20.
//

//: import UIKit
import UIKit

//: class TalkingMessageViewController: TalkingBaseViewController {
class AgencyReactiveCompatible: SafetyFactorViewController {
    //: var isNoChangeTop = false
    var isNoChangeTop = false
    //: private let disposeBag = DisposeBag()
    private let disposeBag = DisposeBag()
    //: private var seleteIndex = 0
    private var seleteIndex = 0

    //: override func viewDidAppear(_ animated: Bool) {
    override func viewDidAppear(_ animated: Bool) {
        //: super.viewDidAppear(animated)
        super.viewDidAppear(animated)
        //: setIsShowCleanAmimat()
        placement()
    }

    //: override func viewWillAppear(_ animated: Bool) {
    override func viewWillAppear(_ animated: Bool) {
        //: super.viewWillAppear(animated)
        super.viewWillAppear(animated)
    }

    //: override func viewDidLoad() {
    override func viewDidLoad() {
        //: super.viewDidLoad()
        super.viewDidLoad()
        //: self.hideNavi = true
        self.hideNavi = true
        //: createUI()
        squareHighlight()
        //: bindInteraction()
        spreadAcross()
        //: func__turnOnSystemNotification()
        everyWantHide()
    }

    // MARK: - Lazy load

    // 背景
    //: private lazy var colorImageView: UIImageView = {
    private lazy var colorImageView: UIImageView = {
        //: let colorV = UIImageView.init(frame: CGRect(x: 0, y: 0, width: ScreenWidth, height: 100+StatusBarNavigationBarHeight))
        let colorV = UIImageView(frame: CGRect(x: 0, y: 0, width: main_assetUrl, height: 100 + main_sendData))
        //: colorV.image = UIImage.BundleImageNamed(name: "bg_message_top")
        colorV.image = UIImage.adName(name: (String(app_affectTitle) + k_accountingContent.replacingOccurrences(of: "simple", with: "sa") + "_top"))
        //: return colorV
        return colorV
        //: }()
    }()

    //: private lazy var noticeView: TalkingNoticeTipView = {
    private lazy var noticeView: PriorityActionThen = {
        //: let V = TalkingNoticeTipView()
        let V = PriorityActionThen()
        //: V.isHidden = true
        V.isHidden = true
        //: return V
        return V
        //: }()
    }()

    //: private lazy var slideVC: QYSlideNavigationViewController = {
    private lazy var slideVC: TraceViewController = {
        //: let vc = QYSlideNavigationViewController()
        let vc = TraceViewController()
        //: vc.topDistance = 0
        vc.topDistance = 0
        //: vc.controllerItems = controllerItems
        vc.controllerItems = controllerItems
        //: vc.titleItems = titleItems
        vc.titleItems = titleItems
        //: vc.rightSpace = 60
        vc.rightSpace = 60
        //: vc.titleViewBackgroundColor = .clear
        vc.titleViewBackgroundColor = .clear
        //: vc.titleFont = .pingfangFont(type: .Medium, fontSize: 16)
        vc.titleFont = .recipeDecision(type: .Medium, fontSize: 16)
        //: vc.selectTitleFont = .pingfangFont(type: .Medium, fontSize: 18)
        vc.selectTitleFont = .recipeDecision(type: .Medium, fontSize: 18)
        //: vc.normalColor = UIColor(hex: "777777")!
        vc.normalColor = UIColor(hex: (data_awakeKey.capitalized))!
        //: vc.selectedColor = UIColor.mottleResult()
        vc.selectedColor = UIColor.mottleResult()
        //: vc.hasBttomTabBar = true
        vc.hasBttomTabBar = true
        //: vc.delegate = self
        vc.delegate = self
        //: vc.scrollEnabled = false
        vc.scrollEnabled = false
        //: return vc
        return vc
        //: }()
    }()

    //: private lazy var controllerItems: [TalkingBaseViewController] = {
    private lazy var controllerItems: [SafetyFactorViewController] = {
        //: var arr = [self.chatListVC, TalkingWhoLikeViewController()]
        var arr = [self.chatListVC, SmartReactiveCompatible()]
        //: if FacultyReactiveCompatible.share.loginUserMode.callTabSwitch == 1 {
        if FacultyReactiveCompatible.share.loginUserMode.callTabSwitch == 1 {
            //: arr.insert(TalkingVideoCallVC(), at: arr.count-1)
            arr.insert(MotionViewController(), at: arr.count - 1)
        }
        //: return arr
        return arr

        //: }()
    }()

    //: private lazy var chatListVC: TalkingChatListViewController = {
    private lazy var chatListVC: TagViewController = {
        //: let vc = TalkingChatListViewController()
        let vc = TagViewController()
        //: return vc
        return vc
        //: }()
    }()

    //: private lazy var titleItems: [String] = {
    private lazy var titleItems: [String] = {
        //: var arr = ["Messages".localized, "Who like me".localized]
        var arr = [(String(notiOutlinePath) + data_reachName.replacingOccurrences(of: "presentation", with: "s")).localized, (String(dataCreationMsg)).localized]
        //: if FacultyReactiveCompatible.share.loginUserMode.callTabSwitch == 1 {
        if FacultyReactiveCompatible.share.loginUserMode.callTabSwitch == 1 {
            //: arr.insert("Call".localized, at: arr.count-1)
            arr.insert((String(userSpreadMsg.prefix(4))).localized, at: arr.count - 1)
        }
        //: return arr
        return arr
        //: }()
    }()

    // 断网提醒
    //: private lazy var netTipLabel: UILabel = {
    private lazy var netTipLabel: UILabel = {
        //: let tipView = UILabel.init(frame: CGRect(x: 0, y: StatusBarNavigationBarHeight, width: ScreenWidth, height: 30))
        let tipView = UILabel(frame: CGRect(x: 0, y: main_sendData, width: main_assetUrl, height: 30))
        //: tipView.font = UIFont.fontRaw(fontSize: 14)
        tipView.font = UIFont.fontRaw(fontSize: 14)
        //: tipView.textColor = .white
        tipView.textColor = .white
        //: tipView.backgroundColor = UIColor.init(hex: "#FF2348")?.withAlphaComponent(0.8)
        tipView.backgroundColor = UIColor(hex: (String(mainWidespreadName)))?.withAlphaComponent(0.8)
        //: tipView.textAlignment = .center
        tipView.textAlignment = .center
        //: tipView.text = "Current network unavailable".localized
        tipView.text = (String(notiRefKey) + main_resumeMsg.replacingOccurrences(of: "cell", with: "b")).localized
        //: tipView.isHidden = true
        tipView.isHidden = true
        //: return tipView
        return tipView
        //: }()
    }()

    // message红点角标
    //: private lazy var msgNumLabel: BadgeLab = {
    private lazy var msgNumLabel: AvailableReactiveCompatible = {
        //: let numbLabel = BadgeLab()
        let numbLabel = AvailableReactiveCompatible()
        //: numbLabel.isHidden = true
        numbLabel.isHidden = true
        //: return numbLabel
        return numbLabel
        //: }()
    }()

    // whoLikeMe红点角标
    //: private lazy var badgLB: BadgeLab = {
    private lazy var badgLB: AvailableReactiveCompatible = {
        //: let numbLabel = BadgeLab()
        let numbLabel = AvailableReactiveCompatible()
        //: numbLabel.isHidden = true
        numbLabel.isHidden = true
        //: return numbLabel
        return numbLabel
        //: }()
    }()

    // 一键清除
    //: private lazy var cleanBtn: UIButton = {
    private lazy var cleanBtn: UIButton = {
        //: let btn = UIButton(type: .custom)
        let btn = UIButton(type: .custom)
        //: btn.backgroundColor = .clear
        btn.backgroundColor = .clear
        //: btn.setImage(UIImage.BundleImageNamed(name: "icon_yidu_pre"), for: .normal)
        btn.setImage(UIImage.adName(name: (String(mainOrganizationPath.suffix(5)) + "yidu_pre")), for: .normal)
        //: btn.setImage(UIImage.BundleImageNamed(name: "icon_yidu_pre"), for: .highlighted)
        btn.setImage(UIImage.adName(name: (String(mainOrganizationPath.suffix(5)) + "yidu_pre")), for: .highlighted)
        //: return btn
        return btn
        //: }()
    }()
}

// MARK: - ConfirmNavigationDelegate

//: extension TalkingMessageViewController: QYSlideNavigationDelegate {
extension AgencyReactiveCompatible: ConfirmNavigationDelegate {
    //: func selectedViewController(_ viewController: UIViewController, index: Int) {
    func beginBy(_ viewController: UIViewController, index: Int) {
        //: self.seleteIndex = Int(index)
        self.seleteIndex = Int(index)
        //: if viewController.isKind(of: TalkingChatListViewController.self) {
        if viewController.isKind(of: TagViewController.self) {
            //: self.cleanBtn.isHidden = false
            self.cleanBtn.isHidden = false
            //: setIsShowCleanAmimat()
            placement()

            //: } else if viewController.isKind(of: TalkingWhoLikeViewController.self) {
        } else if viewController.isKind(of: SmartReactiveCompatible.self) {
            //: self.cleanBtn.isHidden = true
            self.cleanBtn.isHidden = true
            //: uploadRecord.uploadRecordEvent(eventID: ClickWLikeMeTabNoP)
            kSaveKey.putAvailable(eventID: user_nameId)
        }
    }
}

// MARK: - Event

//: extension TalkingMessageViewController {
extension AgencyReactiveCompatible {
    //: func setIsTopAll() {
    func gossipAll() {
        /// 默认选中all
        //: if seleteIndex == 0 {
        if seleteIndex == 0 {
            //: let vc = slideVC.controllerItems.first as? TalkingChatListViewController
            let vc = slideVC.controllerItems.first as? TagViewController
            //: vc?.resetToTopItemView()
            vc?.moveOuter()
            // 刷新在线状态
            //: vc?.refreshOnlineStatus(isFirst: true)
            vc?.beanVoice(isFirst: true)
        }
    }

    /// 无网络提示
    //: func func__checkNetTipState() {
    func funPresentation() {
        //: if FacultyReactiveCompatible.share.networkStatus != .Unavailable && IonV2Listener.shared.isConnection {
        if FacultyReactiveCompatible.share.networkStatus != .Unavailable, IonV2Listener.shared.isConnection {
            //: netTipLabel.isHidden = true
            netTipLabel.isHidden = true
            //: } else {
        } else {
            //: netTipLabel.isHidden = false
            netTipLabel.isHidden = false
        }
    }

    /// message红点
    //: func func__messagebadgLBState(num: Int) {
    func rawOverall(num: Int) {
        //: if num > 0 {
        if num > 0 {
            //: msgNumLabel.isHidden = false
            msgNumLabel.isHidden = false
            //: if num > 99 {
            if num > 99 {
                //: msgNumLabel.text = "99+"
                msgNumLabel.text = "99+"
                //: } else {
            } else {
                //: msgNumLabel.text = String(num)
                msgNumLabel.text = String(num)
            }
            //: } else {
        } else {
            //: msgNumLabel.isHidden = true
            msgNumLabel.isHidden = true
        }
    }

    /// 一键清除动画
    //: func setIsShowCleanAmimat() {
    func placement() {
        //: let unreadMsgCount = FacultyReactiveCompatible.share.unreadMessageNum
        let unreadMsgCount = FacultyReactiveCompatible.share.unreadMessageNum
        //: let isClean: Bool = UserDefaults.standard.object(forKey: isShowCleanAnimatCacheKey) as? Bool ?? false
        let isClean: Bool = UserDefaults.standard.object(forKey: mainWindowKey) as? Bool ?? false
        //: if unreadMsgCount >= 10 && !isClean {
        if unreadMsgCount >= 10, !isClean {
            //: self.cleanBtnBtnAnimat()
            self.noneThenAnimat()
        }
    }

    //: func cleanBtnBtnAnimat() {
    func noneThenAnimat() {
        //: let degreesToRadians = 180.0 * .pi
        let degreesToRadians = 180.0 * .pi
        //: let angle1 = 0 / degreesToRadians
        let angle1 = 0 / degreesToRadians
        //: let angle2 = 100 / degreesToRadians
        let angle2 = 100 / degreesToRadians
        //: let angle3 = -100 / degreesToRadians
        let angle3 = -100 / degreesToRadians
        //: let angle4 = 100 / degreesToRadians
        let angle4 = 100 / degreesToRadians
        //: let angle5 = -100 / degreesToRadians
        let angle5 = -100 / degreesToRadians
        //: let angle6 = 0 / degreesToRadians
        let angle6 = 0 / degreesToRadians

        //: let du1: NSNumber = 0
        let du1: NSNumber = 0
        //: let du2: NSNumber = 0.13
        let du2: NSNumber = 0.13
        //: let du3: NSNumber = 0.3
        let du3: NSNumber = 0.3
        //: let du4: NSNumber = 0.47
        let du4: NSNumber = 0.47
        //: let du5: NSNumber = 0.8
        let du5: NSNumber = 0.8
        //: let du6: NSNumber = 1.0
        let du6: NSNumber = 1.0

        // 旋转
        //: let roate = CAKeyframeAnimation.init(keyPath: "transform.rotation")
        let roate = CAKeyframeAnimation(keyPath: (String(constNetName) + String(mainHoneyRandomData)))
        //: roate.values = [(angle1), (angle2), (angle3), (angle4), (angle5), (angle6)]
        roate.values = [angle1, angle2, angle3, angle4, angle5, angle6]
        //: roate.keyTimes = [(du1), (du2), (du3), (du4), (du5), (du6)]
        roate.keyTimes = [du1, du2, du3, du4, du5, du6]

        // 缩放
        //: let zoom = CAKeyframeAnimation.init(keyPath: "transform.scale")
        let zoom = CAKeyframeAnimation(keyPath: (main_redData.replacingOccurrences(of: "current", with: "t") + k_windowValue.replacingOccurrences(of: "plain", with: "a") + "orm.scale"))
        //: zoom.values = [(1.0), (1.2), (1.0)]
        zoom.values = [1.0, 1.2, 1.0]
        //: zoom.keyTimes = [(0), (0.5), (1.0)]
        zoom.keyTimes = [0, 0.5, 1.0]

        //
        //: let groupAnnimation = CAAnimationGroup.init()
        let groupAnnimation = CAAnimationGroup()
        //: groupAnnimation.autoreverses = false
        groupAnnimation.autoreverses = false
        //: groupAnnimation.animations = [roate, zoom]
        groupAnnimation.animations = [roate, zoom]
        //: groupAnnimation.duration = 1.0
        groupAnnimation.duration = 1.0
        //: groupAnnimation.repeatCount = 2
        groupAnnimation.repeatCount = 2
        //: groupAnnimation.isRemovedOnCompletion = false
        groupAnnimation.isRemovedOnCompletion = false
        //: groupAnnimation.fillMode = .forwards
        groupAnnimation.fillMode = .forwards
        //: cleanBtn.layer.add(groupAnnimation, forKey: "zoom&shake")
        cleanBtn.layer.add(groupAnnimation, forKey: (show_exceptionMessage.lowercased() + String(main_contactRoutineValue.prefix(6))))
    }

    /// 检查系统通知是否开启
    //: private func func__turnOnSystemNotification() {
    private func everyWantHide() {
        //: TalkingPermissionTool.checkPushNotification { isEnable in
        ActivityReactiveCompatible.effectIndicator { isEnable in

            //: DispatchQueue.main.async {
            DispatchQueue.main.async {
                //: if isEnable {
                if isEnable {
                    //: self.noticeView.isHidden = true
                    self.noticeView.isHidden = true
                    //: self.func__hideNotificationTipView(hide: true)
                    self.viewOnceDisable(hide: true)
                    //: }else{
                } else {
                    /// 页面顶部提示
                    //: let today = NSDate.getTimeString(date: Date(), dateFormat: "yyyy-MM-dd")
                    let today = NSDate.populationProfile(date: Date(), dateFormat: (String(notiCorrectComplexName.prefix(5)) + String(user_putMsg.prefix(5))))
                    //: if let closeDateStr = Defaults.string(forKey: TalkingMessageTabCloseNotificationsKey), today == closeDateStr {
                    if let closeDateStr = mainNameNearbyTitle.string(forKey: noti_warnMsg), today == closeDateStr {
                        //: self.func__hideNotificationTipView(hide: true)
                        self.viewOnceDisable(hide: true)
                        //: }else{
                    } else {
                        //: self.func__hideNotificationTipView(hide: false)
                        self.viewOnceDisable(hide: false)
                    }
                    /// 弹框逻辑
                    //: let isShow = Defaults.bool(forKey: TalkingMessageTabTurnOnNotificationsKey)
                    let isShow = mainNameNearbyTitle.bool(forKey: appMineMsg)
                    //: if !isShow {
                    if !isShow {
                        //: Defaults.set(true, forKey: TalkingMessageTabTurnOnNotificationsKey)
                        mainNameNearbyTitle.set(true, forKey: appMineMsg)
                        //: TalkingAlertShow.alert(title: nil,
                        PersonAlertShow.panAngleKind(title: nil,
                                                      //: message: "Allow %@ to send you notifications?".localizedArguments(AppName),
                                                      message: String(bytes: const_screenFormat.map{standOut(tire: $0)}, encoding: .utf8)!.extraPropertyLetter(appCollectionKey),
                                                      //: leftBtnTitle: "Cancel".localized,
                                                      leftBtnTitle: (mainClipBeName.replacingOccurrences(of: "paper", with: "C") + notiLengthTagKey.replacingOccurrences(of: "inventory", with: "a")).localized,
                                                      //: rightBtnTitle: "Settings".localized) {
                                                      rightBtnTitle: (String(show_pauseId) + const_willMessage.replacingOccurrences(of: "provider", with: "n")).localized)
                        {
                            //: TalkingAlertShow.hideAlert()
                            PersonAlertShow.quantityerrupt()
                            //: return
                            //: } rightBlock: {
                        } rightBlock: {
                            //: if let settingsUrl = URL(string: UIApplication.openSettingsURLString) {
                            if let settingsUrl = URL(string: UIApplication.openSettingsURLString) {
                                //: UIApplication.shared.open(settingsUrl)
                                UIApplication.shared.open(settingsUrl)
                            }
                        }
                    }
                }
            }
        }
    }

    //: private func func__hideNotificationTipView(hide: Bool) {
    private func viewOnceDisable(hide: Bool) {
        //: DispatchQueue.main.async {
        DispatchQueue.main.async {
            //: if hide{
            if hide {
                //: self.noticeView.isHidden = true
                self.noticeView.isHidden = true
                //: self.slideVC.view.snp.updateConstraints { make in
                self.slideVC.view.snp.updateConstraints { make in
                    //: make.top.equalToSuperview().offset(StatusBarHeight)
                    make.top.equalToSuperview().offset(show_textNextFormat)
                }
                //: self.slideVC.viewHeight = ScreenHeight - StatusBarHeight - TabBarViewHeight
                self.slideVC.viewHeight = kTopUrl - show_textNextFormat - data_ageOfMessage
                //: }else{
            } else {
                //: self.noticeView.isHidden = false
                self.noticeView.isHidden = false
                //: self.slideVC.view.snp.updateConstraints { make in
                self.slideVC.view.snp.updateConstraints { make in
                    //: make.top.equalToSuperview().offset(StatusBarHeight + self.noticeView.viewHeight)
                    make.top.equalToSuperview().offset(show_textNextFormat + self.noticeView.viewHeight)
                }
                //: self.slideVC.viewHeight = ScreenHeight - self.noticeView.bottom - TabBarViewHeight
                self.slideVC.viewHeight = kTopUrl - self.noticeView.bottom - data_ageOfMessage
            }
        }
    }
}

// MARK: - 通知

//: extension TalkingMessageViewController {
extension AgencyReactiveCompatible {
    /// who like me 红点通知
    //: @objc func showbadgNumberNotif(notification: NSNotification) {
    @objc func adopt(notification: NSNotification) {
        //: let userinfo = notification.userInfo as![String: AnyObject]
        let userinfo = notification.userInfo as! [String: AnyObject]
        //: let badNumber: Int  = userinfo["badNumber"] as! Int
        let badNumber: Int = userinfo[(String(kOptionStr.suffix(9)))] as! Int
        //: badgLB.isHidden = false
        badgLB.isHidden = false
        //: if badNumber > 99 {
        if badNumber > 99 {
            //: badgLB.text = "99+"
            badgLB.text = "99+"
            //: } else if badNumber<=0 {
        } else if badNumber <= 0 {
            //: badgLB.isHidden = true
            badgLB.isHidden = true
            //: badgLB.text = "0"
            badgLB.text = "0"
            //: } else {
        } else {
            //: badgLB.text = String(badNumber)
            badgLB.text = String(badNumber)
        }
    }
}

// MARK: - Layout

//: extension TalkingMessageViewController {
extension AgencyReactiveCompatible {
    /// UI
    //: private func createUI() {
    private func squareHighlight() {
        //: view.backgroundColor = .white
        view.backgroundColor = .white

        //: view.addSubview(colorImageView)
        view.addSubview(colorImageView)
        //: view.addSubview(noticeView)
        view.addSubview(noticeView)
        //: self.addChild(slideVC)
        self.addChild(slideVC)

        //: self.view.addSubview(slideVC.view)
        self.view.addSubview(slideVC.view)
        //: slideVC.view.backgroundColor = UIColor.clear
        slideVC.view.backgroundColor = UIColor.clear
        //: slideVC.view.snp.makeConstraints { make in
        slideVC.view.snp.makeConstraints { make in
            //: make.top.equalToSuperview().offset(StatusBarHeight)
            make.top.equalToSuperview().offset(show_textNextFormat)
            //: make.leading.trailing.equalToSuperview()
            make.leading.trailing.equalToSuperview()
            //: make.bottom.equalToSuperview()
            make.bottom.equalToSuperview()
        }

        //: for btn in slideVC.allBtn {
        for btn in slideVC.allBtn {
            //: let curenbrn: UIButton = btn
            let curenbrn: UIButton = btn
            //: if curenbrn.titleLabel!.text == "Who like me".localized {
            if curenbrn.titleLabel!.text == (String(dataCreationMsg)).localized {
                //: (curenbrn as AnyObject).addSubview(badgLB)
                (curenbrn as AnyObject).addSubview(badgLB)
                //: badgLB.snp.makeConstraints { make in
                badgLB.snp.makeConstraints { make in
                    //: make.leading.equalTo(curenbrn.titleLabel!.snp.trailing).offset(-5)
                    make.leading.equalTo(curenbrn.titleLabel!.snp.trailing).offset(-5)
                    //: make.top.equalTo(curenbrn).offset(7)
                    make.top.equalTo(curenbrn).offset(7)
                    //: make.height.equalTo(16)
                    make.height.equalTo(16)
                    //: make.width.greaterThanOrEqualTo(16)
                    make.width.greaterThanOrEqualTo(16)
                }
                //: badgLB.isHidden = true
                badgLB.isHidden = true
                //: } else if (curenbrn.titleLabel!.text == "Messages".localized) {
            } else if curenbrn.titleLabel!.text == (String(notiOutlinePath) + data_reachName.replacingOccurrences(of: "presentation", with: "s")).localized {
                //: (curenbrn as AnyObject).addSubview(msgNumLabel)
                (curenbrn as AnyObject).addSubview(msgNumLabel)
                //: msgNumLabel.snp.makeConstraints { make in
                msgNumLabel.snp.makeConstraints { make in
                    //: make.leading.equalTo(curenbrn.titleLabel!.snp.trailing).offset(-5)
                    make.leading.equalTo(curenbrn.titleLabel!.snp.trailing).offset(-5)
                    //: make.top.equalTo(curenbrn).offset(7)
                    make.top.equalTo(curenbrn).offset(7)
                    //: make.height.equalTo(16)
                    make.height.equalTo(16)
                    //: make.width.greaterThanOrEqualTo(16)
                    make.width.greaterThanOrEqualTo(16)
                }
            }
        }

        //: view.addSubview(cleanBtn)
        view.addSubview(cleanBtn)
        //: cleanBtn.snp.makeConstraints { make in
        cleanBtn.snp.makeConstraints { make in
            //: make.top.equalTo(slideVC.view.snp.top)
            make.top.equalTo(slideVC.view.snp.top)
            //: make.trailing.equalTo(-5)
            make.trailing.equalTo(-5)
            //: make.size.equalTo(CGSize(width: 44, height: 44))
            make.size.equalTo(CGSize(width: 44, height: 44))
        }

        //: view.addSubview(netTipLabel)
        view.addSubview(netTipLabel)
        //: netTipLabel.snp.makeConstraints { make in
        netTipLabel.snp.makeConstraints { make in
            //: make.top.equalTo(slideVC.view.snp.top).offset(44)
            make.top.equalTo(slideVC.view.snp.top).offset(44)
            //: make.leading.trailing.equalToSuperview()
            make.leading.trailing.equalToSuperview()
            //: make.height.equalTo(30)
            make.height.equalTo(30)
        }
    }

    /// 绑定事件
    //: private func bindInteraction() {
    private func spreadAcross() {
        //: IonV2Listener.shared.rx
        IonV2Listener.shared.rx
            //: .observeWeakly(Bool.self, "isConnection")
            .observeWeakly(Bool.self, (String(main_maleFormat.prefix(7)) + mainRemoveUrl.replacingOccurrences(of: "limited", with: "ti")))
            //: .subscribe(onNext: { [weak self] (value) in
            .subscribe(onNext: { [weak self] _ in
                //: guard let self = self else { return }
                guard let self = self else { return }
                //: self.func__checkNetTipState()
                self.funPresentation()
                //: })
            })
            //: .disposed(by: disposeBag)
            .disposed(by: disposeBag)

        //: FacultyReactiveCompatible.share.rx.observeWeakly(Int.self, "networkStatus")
        FacultyReactiveCompatible.share.rx.observeWeakly(Int.self, (String(show_temporaryPath) + String(dataDailySomeoneKey.suffix(4)) + "atus"))
            //: .subscribe(onNext: { [weak self]  (value) in
            .subscribe(onNext: { [weak self] _ in
                //: guard let self = self else { return }
                guard let self = self else { return }
                //: self.func__checkNetTipState()
                self.funPresentation()
                //: })
            })
            //: .disposed(by: disposeBag)
            .disposed(by: disposeBag)

        // 展示全部红点
        //: FacultyReactiveCompatible.share.rx.observeWeakly(Int.self, "unreadMessageNum")
        FacultyReactiveCompatible.share.rx.observeWeakly(Int.self, (String(noti_stripValue) + String(notiAskKey.prefix(6))))
            //: .subscribe(onNext: { [weak self] (value) in
            .subscribe(onNext: { [weak self] value in
                //: guard let self = self else { return }
                guard let self = self else { return }
                //: let valueInt = value ?? 0
                let valueInt = value ?? 0
                //: self.func__messagebadgLBState(num: valueInt)
                self.rawOverall(num: valueInt)
                //: })
            })
            //: .disposed(by: disposeBag)
            .disposed(by: disposeBag)

        //: cleanBtn.rx.controlEvent(.touchUpInside)
        cleanBtn.rx.controlEvent(.touchUpInside)
            //: .subscribe(onNext: {
            .subscribe(onNext: {
                //: let config = ShowAlertConfig()
                let config = InstAlertConfig()
                //: config.alignment = .center
                config.alignment = .center

                //: TalkingAlertShow.customAlert(message: "Do you want to mark all messages as read?".localized, leftBtnTitle: "Cancel".localized, rightBtnTitle: "OK".localized, leftBlock: {
                PersonAlertShow.customOfConfig(message: String(bytes: data_dateValue.map{heartMenuSequence(match: $0)}, encoding: .utf8)!.localized, leftBtnTitle: (mainClipBeName.replacingOccurrences(of: "paper", with: "C") + notiLengthTagKey.replacingOccurrences(of: "inventory", with: "a")).localized, rightBtnTitle: "OK".localized, leftBlock: {
                    //: TalkingAlertShow.hideAlert()
                    PersonAlertShow.quantityerrupt()

                    //: }, rightBlock: {
                }, rightBlock: {
                    //: V2TIMManager.sharedInstance().markAllMessage {
                    V2TIMManager.sharedInstance().markAllMessage {
                        //: UserDefaults.standard.set(true, forKey: isShowCleanAnimatCacheKey)
                        UserDefaults.standard.set(true, forKey: mainWindowKey)
                        //: } fail: { code, desc in
                    } fail: { code, desc in
                        //: UploadLogTool.writeLog(msg: "消息列表一键已读失败：code:\(code), desc:\(String(describing: desc))")
                        MigrationThen.pathAge(msg: (String(dataAskGuideUnionTitle) + String(noti_tabLifePath.prefix(3))) + "\(code)" + (String(mainFrameValue.prefix(7))) + "\(String(describing: desc))")
                    }
                    //: }, config: config)
                }, config: config)

                //: })
            })
            //: .disposed(by: disposeBag)
            .disposed(by: disposeBag)

        // who like me 红点通知
        //: NotificationCenter.default.addObserver(self,
        NotificationCenter.default.addObserver(self,
                                               //: selector: #selector(showbadgNumberNotif(notification:)),
                                               selector: #selector(adopt(notification:)),
                                               //: name: WHOLIKEME_BADGENUMBER_NOTIF,
                                               name: show_lastData,
                                               //: object: nil)
                                               object: nil)

        //: noticeView.closeBtnBlock = { [weak self] ()  -> Void in
        noticeView.closeBtnBlock = { [weak self] () in
            //: guard let self = self else {return}
            guard let self = self else { return }
            //: let today = NSDate.getTimeString(date: Date(), dateFormat: "yyyy-MM-dd")
            let today = NSDate.populationProfile(date: Date(), dateFormat: (String(notiCorrectComplexName.prefix(5)) + String(user_putMsg.prefix(5))))
            //: Defaults.set(today, forKey: TalkingMessageTabCloseNotificationsKey)
            mainNameNearbyTitle.set(today, forKey: noti_warnMsg)
            //: self.func__hideNotificationTipView(hide: true)
            self.viewOnceDisable(hide: true)
        }

        //: NotificationCenter.default.rx.notification( UIApplication.willEnterForegroundNotification)
        NotificationCenter.default.rx.notification(UIApplication.willEnterForegroundNotification)
            //: .subscribe(onNext: { [weak self] (notification) in
            .subscribe(onNext: { [weak self] _ in
                //: guard let self = self else { return }
                guard let self = self else { return }
                //: self.func__turnOnSystemNotification()
                self.everyWantHide()
                //: }).disposed(by: disposeBag)
            }).disposed(by: disposeBag)
    }
}
