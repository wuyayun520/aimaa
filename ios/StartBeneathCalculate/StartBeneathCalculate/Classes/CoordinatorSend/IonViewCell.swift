
//: Declare String Begin

/*: "formatType" :*/
fileprivate let const_yellowPath:[Character] = ["f","o","r","m","a","t","T","y","p"]
fileprivate let notiGrossRelatedPath:String = "mention"

/*: "plain" :*/
fileprivate let notiBorderMessage:String = "intoain"

/*: "init(coder:) has not been implemented" :*/
fileprivate let show_wellValue:[UInt8] = [0x66,0x61,0x66,0x7b,0x27,0x6c,0x60,0x6b,0x6a,0x7d,0x35,0x26,0x2f,0x67,0x6e,0x7c,0x2f,0x61,0x60,0x7b,0x2f,0x6d,0x6a,0x6a,0x61,0x2f,0x66,0x62,0x7f,0x63,0x6a,0x62,0x6a,0x61,0x7b,0x6a,0x6b]

private func indigenousTick(channel num: UInt8) -> UInt8 {
    return num ^ 15
}

/*: "#FF2348" :*/
fileprivate let app_highlightValue:String = "coordinate lift identical form#FF23"
fileprivate let app_taskKey:[Character] = ["4","8"]

/*: "icon_lounge" :*/
fileprivate let k_policemanKey:String = "icon_distant hormone weak project"

/*: "icon_home_v" :*/
fileprivate let userFeedStr:[Character] = ["i"]
fileprivate let main_brushUrl:String = "privacy recommendationcon_h"

/*: "icon_chat_list_new" :*/
fileprivate let k_seaIncreaseName:[Character] = ["i","c","o","n","_","c","h","a","t","_","l","i","s","t","_"]
fileprivate let app_strikePath:String = "newindow"

/*: "Official" :*/
fileprivate let user_manageSeaData:String = "shore license formulaOffi"

/*: "#30D00B" :*/
fileprivate let show_priceName:String = "ignore access it tolerance mated#30D00B"

/*: "iv_message_honey" :*/
fileprivate let main_spreadSaveValue:String = "coat eff succeediv_messag"
fileprivate let k_holdName:String = "e_honeyrecording leading along weak"

/*: "#FF60B9" :*/
fileprivate let user_clearlyUrl:String = "heart heritage#FF60B9"

/*: "#EEEEEE" :*/
fileprivate let main_regionContent:[Character] = ["#","E","E","E","E","E","E"]

/*: "opacity" :*/
fileprivate let constExpressionFormat:[Character] = ["o"]
fileprivate let showDisappearId:[Character] = ["p","a","c","i","t","y"]

/*: "path" :*/
fileprivate let dataWrittenText:[Character] = ["p","a","t","h"]

/*: "extra" :*/
fileprivate let const_woundStr:[Character] = ["e","x","t","r","a"]

/*: "msgInfo" :*/
fileprivate let show_trafficId:[Character] = ["m","s","g","I","n","f","o"]

/*: "content" :*/
fileprivate let mainGameMsg:String = "cheavy"
fileprivate let showPullStr:[Character] = ["n","t","e","n","t"]

/*: "messageType" :*/
fileprivate let data_accountingFormat:String = "meflag"
fileprivate let constCalendarMessage:String = "sub"

/*: "[Reply messages to get points]" :*/
fileprivate let main_cropPath:String = "strip instruction index already[Reply"
fileprivate let userEscapeName:[Character] = ["s"," ","t","o"," ","g","e","t"," ","p","o","i","n","t","s","]"]

/*: "title" :*/
fileprivate let app_operateKey:[Character] = ["t","i","t","l","e"]

/*: "img" :*/
fileprivate let main_generalData:[Character] = ["i","m","g"]

/*: "[Photo]" :*/
fileprivate let showPanBodyTitle:String = "channel timing target female[Phot"
fileprivate let show_launchRegainStr:String = "o]written simultaneously automatic affect"

/*: "[Audio]" :*/
fileprivate let k_handUrl:[Character] = ["["]
fileprivate let showBathroomFormat:String = "limit presentationAudio]"

/*: "rYMsgType" :*/
fileprivate let constDepthStr:[Character] = ["r","Y","M","s","g","T"]
fileprivate let k_strangerStr:[Character] = ["y","p","e"]

/*: "GJ:CallCustom" :*/
fileprivate let showUponStr:String = "GJ:thumb raw equally original"
fileprivate let dataCuriosityName:[Character] = ["C","a","l","l","C","u","s","t","o","m"]

/*: "callCmd" :*/
fileprivate let dataFormerId:String = "be stretch care commercial representationcallC"
fileprivate let main_dimensionData:[Character] = ["m","d"]

/*: "missCall" :*/
fileprivate let kCoatValue:[Character] = ["m","i","s","s","C","a","l","l"]

/*: "[Missed Call]" :*/
fileprivate let main_teamName:String = "retirement daily security system complex[Missed "
fileprivate let data_submitAlleyId:String = "every windowCall]"

/*: "logType" :*/
fileprivate let main_cellId:String = "logTypeinfo immediate"

/*: "[Voice Call]" :*/
fileprivate let k_resumeFormat:[Character] = ["[","V","o","i","c","e"," ","C","a","l","l"]
fileprivate let kTailRobotFormat:String = "]"

/*: "[Video Call]" :*/
fileprivate let const_alongsideFormat:String = "[Vidbeginning time cloud prepare"

/*: "video" :*/
fileprivate let app_stepUrl:[Character] = ["v","i","d","e","o"]

/*: "contentType" :*/
fileprivate let constAppointmentValue:String = "receptiononte"

/*: "VideoMsg" :*/
fileprivate let constScanTitle:String = "Videher coal stand"

/*: "[Short Video]" :*/
fileprivate let show_hereValue:String = "calculation illegal just effect[Short"
fileprivate let data_onlyStr:[Character] = ["o","]"]

/*: "match" :*/
fileprivate let userPaperTitle:String = "matcround"

/*: "[Cupid messaging]" :*/
fileprivate let notiAnimaData:[Character] = ["[","C","u","p","i","d"," ","m","e"]
fileprivate let app_mailName:[Character] = ["s","s","a","g","i","n","g","]"]

/*: "GJ:Sess:TxtCustom" :*/
fileprivate let data_signatureId:[Character] = ["G","J",":","S","e","s"]
fileprivate let noti_normalUrl:[Character] = ["s",":","T","x"]
fileprivate let main_deviceMsg:[Character] = ["t","C","u","s","t","o","m"]

/*: "isSend" :*/
fileprivate let user_priorityMessage:[Character] = ["i","s","S","e","n","d"]

/*: "Sent Gift" :*/
fileprivate let appCurFormat:[Character] = ["S"]
fileprivate let constResValue:String = "ent find transaction country climb"

/*: "Receive Gift" :*/
fileprivate let app_produceValue:String = "already expression pattern max otherRecei"
fileprivate let notiRenderTitle:String = "ve Giftphoto sight desk"

/*: "uid" :*/
fileprivate let showThatRelateUnlessTitle:[Character] = ["u","i","d"]

/*: "gift" :*/
fileprivate let mainMergeUrl:String = "gifsomeone"

/*: "name" :*/
fileprivate let noti_growthFormat:String = "naplante"

/*: "num" :*/
fileprivate let mainForthPhaseFormat:String = "NUM"

/*: "imageUri" :*/
fileprivate let k_smartUrl:String = "imageUrpad reader yes"
fileprivate let show_greenKey:[Character] = ["i"]

/*: "imageData" :*/
fileprivate let constPhysicalUnderMsg:[Character] = ["i","m","a","g","e","D","a"]
fileprivate let kAffectFormat:String = "hirea"

/*: "<img>" :*/
fileprivate let show_bellyDevoteOriginallyStr:[Character] = ["<","i","m","g",">"]

/*: "</img>" :*/
fileprivate let mainPointKey:String = "and brother resume</img>"

/*: "wantGift" :*/
fileprivate let user_processingStr:[Character] = ["w","a"]
fileprivate let noti_randomValue:String = "access rear rugntGift"

/*: "roundNum" :*/
fileprivate let const_dayPath:[Character] = ["r","o","u","n","d","N","u","m"]

/*: "[Question]" :*/
fileprivate let mainDoingceInsertId:[Character] = ["[","Q","u","e"]
fileprivate let constComeMsg:String = "match donstion]"

/*: "intimatePhoto" :*/
fileprivate let app_mustBeanPriceText:[Character] = ["i","n","t","i","m","a","t","e"]
fileprivate let main_inventoryPitchText:[Character] = ["P","h","o","t","o"]

/*: "audioData" :*/
fileprivate let appComplexUrl:String = "afunio"

/*: "audioUri" :*/
fileprivate let k_bothText:[Character] = ["a","u","d","i","o","U","r","i"]

/*: "draftContent" :*/
fileprivate let userGameMinePath:[Character] = ["d","r"]
fileprivate let const_joinTitle:String = "draw show sign beneath solutionaftCon"

/*: "[draft]%@" :*/
fileprivate let data_pocketFatherValue:[Character] = ["[","d","r","a","f","t","]","%","@"]

/*: "99999+" :*/
fileprivate let app_capacityStr:[UInt8] = [0xba,0xba,0xba,0xba,0xba,0xa8]

/*: "New" :*/
fileprivate let userUniqueText:[Character] = ["N","e","w"]

/*: "Account is restricted！" :*/
fileprivate let notiImagineCancelName:String = "Accountsum highlight to flag identity"
fileprivate let noti_holderData:String = " is resstage design application"
fileprivate let k_songContent:String = "tricted！grade extent force medal read"

/*: "%@ Online" :*/
fileprivate let dataBoardContent:String = "user top operation%@ O"

/*: "Messages not been replied here" :*/
fileprivate let const_goodMsg:String = "Messagcondition device"
fileprivate let showConveyKey:String = "ot bsatisfy year wait"
fileprivate let const_entryMessage:String = "rebetterl"
fileprivate let main_adUrl:String = "hscreenrscreen"

//: Declare String End

// __DEBUG__
// __CLOSE_PRINT__
//
//  IonViewCell.swift
//  AbroadTalking
//
//  Created by Hemming on 2022/9/12.
//

//: import UIKit
import UIKit

//: let RCSystemFormatType                 = "formatType"
let const_maxTitle = (String(const_yellowPath) + notiGrossRelatedPath.replacingOccurrences(of: "mention", with: "e"))
//: let RCSystemFormatTypePlain            = "plain"
let user_serviceAgentId = (notiBorderMessage.replacingOccurrences(of: "into", with: "pl"))

//: class TalkingChatListTableCell: UITableViewCell {
class IonViewCell: UITableViewCell {
    //: required init?(coder: NSCoder) {
    required init?(coder: NSCoder) {
        //: super.init(coder: coder)
        super.init(coder: coder)
        //: fatalError("init(coder:) has not been implemented")
        fatalError(String(bytes: show_wellValue.map{indigenousTick(channel: $0)}, encoding: .utf8)!)
    }

    //: override init(style: UITableViewCell.CellStyle, reuseIdentifier: String?) {
    override init(style: UITableViewCell.CellStyle, reuseIdentifier: String?) {
        //: super.init(style: style, reuseIdentifier: reuseIdentifier)
        super.init(style: style, reuseIdentifier: reuseIdentifier)
        //: createCellUI()
        crossStreet()
        //: createCellUIConstraints()
        post()
    }

    // MARK: - Lazy load

    //: private lazy var headImgView: UIImageView = {
    private lazy var headImgView: UIImageView = {
        //: let imageView = UIImageView.init()
        let imageView = UIImageView()
        //: imageView.layer.cornerRadius = 30
        imageView.layer.cornerRadius = 30
        //: imageView.clipsToBounds = true
        imageView.clipsToBounds = true
        //: imageView.backgroundColor = UIColor.white
        imageView.backgroundColor = UIColor.white
        //: imageView.contentMode = .scaleAspectFill
        imageView.contentMode = .scaleAspectFill
        //: return imageView
        return imageView
        //: }()
    }()

    //: private lazy var iconBorder: UIButton = {
    private lazy var iconBorder: UIButton = {
        //: let btn = UIButton.init()
        let btn = UIButton()
        //: btn.imageView?.contentMode = .scaleAspectFill
        btn.imageView?.contentMode = .scaleAspectFill
        //: btn.isUserInteractionEnabled = false
        btn.isUserInteractionEnabled = false
        //: self.addSubview(btn)
        self.addSubview(btn)
        //: return btn
        return btn
        //: }()
    }()

    //: private lazy var badgeLab: UILabel = {
    private lazy var badgeLab: UILabel = {
        //: let label = UILabel.init()
        let label = UILabel()
        //: label.font = .pingfangFont(type: .Medium, fontSize: 13)
        label.font = .recipeDecision(type: .Medium, fontSize: 13)
        //: label.textAlignment = .center
        label.textAlignment = .center
        //: label.textColor = .white
        label.textColor = .white
        //: label.backgroundColor = UIColor(hex: "#FF2348")
        label.backgroundColor = UIColor(hex: (String(app_highlightValue.suffix(5)) + String(app_taskKey)))
        //: label.layer.cornerRadius = 10
        label.layer.cornerRadius = 10
        //: label.layer.masksToBounds = true
        label.layer.masksToBounds = true
        //: label.layer.borderColor = UIColor.white.cgColor
        label.layer.borderColor = UIColor.white.cgColor
        //: label.layer.borderWidth = 1
        label.layer.borderWidth = 1
        //: label.isHidden = true
        label.isHidden = true
        //: return label
        return label
        //: }()
    }()

    //: private lazy var nickNameLabel: UILabel = {
    private lazy var nickNameLabel: UILabel = {
        //: let label = UILabel.init()
        let label = UILabel()
        //: label.font = .pingfangFont(type: .Medium, fontSize: 18)
        label.font = .recipeDecision(type: .Medium, fontSize: 18)
        //: label.textColor = .mottleResult()
        label.textColor = .mottleResult()
        //: return label
        return label
        //: }()
    }()

    //: private lazy var msgShowLabel: UILabel = {
    private lazy var msgShowLabel: UILabel = {
        //: let label = UILabel.init()
        let label = UILabel()
        //: label.font = .pingfangFont(type: .Regular, fontSize: 15)
        label.font = .recipeDecision(type: .Regular, fontSize: 15)
        //: label.textColor = .appValueDetailColor()
        label.textColor = .keepRid()
        //: return label
        return label
        //: }()
    }()

    //: private lazy var timeLab: UILabel = {
    private lazy var timeLab: UILabel = {
        //: let label = UILabel.init()
        let label = UILabel()
        //: label.font = .pingfangFont(type: .Medium, fontSize: 12)
        label.font = .recipeDecision(type: .Medium, fontSize: 12)
        //: label.textColor = .appValueDetailColor()
        label.textColor = .keepRid()
        //: return label
        return label
        //: }()
    }()

    //: lazy var loungeImgV: UIImageView = {
    lazy var loungeImgV: UIImageView = {
        //: let img = UIImageView()
        let img = UIImageView()
        //: img.image = UIImage.BundleImageNamed(name: "icon_lounge")
        img.image = UIImage.adName(name: (String(k_policemanKey.prefix(5)) + "lounge"))
        //: return img
        return img
        //: }()
    }()

    //: private lazy var authImgV: UIImageView = {
    private lazy var authImgV: UIImageView = {
        //: let imgV = UIImageView()
        let imgV = UIImageView()
        //: imgV.image = UIImage.BundleImageNamed(name: "icon_home_v")
        imgV.image = UIImage.adName(name: (String(userFeedStr) + String(main_brushUrl.suffix(5)) + "ome_v"))
        //: return imgV
        return imgV
        //: }()
    }()

    //: private lazy var newImgV: TalkingButton = {
    private lazy var newImgV: SeparateControlReactiveCompatible = {
        //: let imgV = TalkingButton()
        let imgV = SeparateControlReactiveCompatible()
        //: imgV.setBackgroundImage(UIImage.BundleImageNamed(name: "icon_chat_list_new"), for: .normal)
        imgV.setBackgroundImage(UIImage.adName(name: (String(k_seaIncreaseName) + app_strikePath.replacingOccurrences(of: "window", with: "w"))), for: .normal)
        //: return imgV
        return imgV
        //: }()
    }()

    //: private lazy var officialImgV: UIButton = {
    private lazy var officialImgV: UIButton = {
        //: let img = UIButton()
        let img = UIButton()
        //: img.setBackgroundColor(color: UIColor.msgTipsColor(), forState: .normal)
        img.askPath(color: UIColor.supernumerary(), forState: .normal)
        //: img.setTitle("Official".localized, for: .normal)
        img.setTitle((String(user_manageSeaData.suffix(4)) + "cial").localized, for: .normal)
        //: img.setTitleColor(.white, for: .normal)
        img.setTitleColor(.white, for: .normal)
        //: img.titleLabel?.font = UIFont.fontRaw(fontSize: 11)
        img.titleLabel?.font = UIFont.fontRaw(fontSize: 11)
        //: img.layer.cornerRadius = 8
        img.layer.cornerRadius = 8
        //: img.layer.masksToBounds = true
        img.layer.masksToBounds = true
        //: return img
        return img
        //: }()
    }()

    //: private lazy var onlineStatus: UIView = {
    private lazy var onlineStatus: UIView = {
        //: let v = UIView()
        let v = UIView()
        //: v.backgroundColor = UIColor(hex: "#30D00B")
        v.backgroundColor = UIColor(hex: (String(show_priceName.suffix(7))))
        //: v.layer.cornerRadius = 5
        v.layer.cornerRadius = 5
        //: v.layer.borderColor = UIColor.white.cgColor
        v.layer.borderColor = UIColor.white.cgColor
        //: v.layer.borderWidth = 1
        v.layer.borderWidth = 1
        //: v.isHidden = true
        v.isHidden = true
        //: return v
        return v
        //: }()
    }()

    //: private lazy var imtimateBtn: TalkingButton = {
    private lazy var imtimateBtn: SeparateControlReactiveCompatible = {
        //: let btn = TalkingButton.init()
        let btn = SeparateControlReactiveCompatible()
        //: btn.setImage(UIImage.BundleImageNamed(name: "iv_message_honey"), for: .normal)
        btn.setImage(UIImage.adName(name: (String(main_spreadSaveValue.suffix(9)) + String(k_holdName.prefix(7)))), for: .normal)
        //: btn.spaceBetweenTitleAndImage = 2
        btn.spaceBetweenTitleAndImage = 2
        //: btn.titleLabel?.font = .pingfangFont(type: .Medium, fontSize: 12)
        btn.titleLabel?.font = .recipeDecision(type: .Medium, fontSize: 12)
        //: btn.setTitleColor(UIColor.init(hex: "#FF60B9"), for: .normal)
        btn.setTitleColor(UIColor(hex: (String(user_clearlyUrl.suffix(7)))), for: .normal)
        //: btn.contentHorizontalAlignment = .right
        btn.contentHorizontalAlignment = .right
        //: if LanguageManager.shared.direction == .rightToLeft {
        if TaLanguageManager.shared.direction == .rightToLeft {
            //: btn.contentHorizontalAlignment = .left
            btn.contentHorizontalAlignment = .left
        }
        //: return btn
        return btn
        //: }()
    }()
}

//: extension TalkingChatListTableCell {
extension IonViewCell {
    //: override func touchesBegan(_ touches: Set<UITouch>, with event: UIEvent?) {
    override func touchesBegan(_ touches: Set<UITouch>, with event: UIEvent?) {
        //: let animationColor = UIColor.init(hex: "#EEEEEE")
        let animationColor = UIColor(hex: (String(main_regionContent)))
        //: let animationDuration: Double = 0.4
        let animationDuration = 0.4

        //: super.touchesBegan(touches, with: event)
        super.touchesBegan(touches, with: event)
        //: let coverView = UIView(frame: bounds)
        let coverView = UIView(frame: bounds)
        //: coverView.autoresizingMask = [.flexibleWidth, .flexibleHeight]
        coverView.autoresizingMask = [.flexibleWidth, .flexibleHeight]
        //: coverView.backgroundColor = UIColor.clear
        coverView.backgroundColor = UIColor.clear
        //: self.addSubview(coverView)
        self.addSubview(coverView)

        //: let touch = touches.first!
        let touch = touches.first!
        //: let point = touch.location(in: self)
        let point = touch.location(in: self)

        //: let ourTouchView = UIView(frame: CGRect(x: point.x - 5, y: point.y - 5, width: 10, height: 10))
        let ourTouchView = UIView(frame: CGRect(x: point.x - 5, y: point.y - 5, width: 10, height: 10))

        //: let circleMaskPathInitial = UIBezierPath(ovalIn: ourTouchView.frame)
        let circleMaskPathInitial = UIBezierPath(ovalIn: ourTouchView.frame)
        //: let pathRect = self.bounds.inset(by: UIEdgeInsets(top: 1, left: 1, bottom: 1, right: 1))
        let pathRect = self.bounds.inset(by: UIEdgeInsets(top: 1, left: 1, bottom: 1, right: 1))
        //: let circleMaskPathFinal = UIBezierPath(roundedRect: pathRect, cornerRadius: 5)
        let circleMaskPathFinal = UIBezierPath(roundedRect: pathRect, cornerRadius: 5)

        //: let rippleLayer = CAShapeLayer()
        let rippleLayer = CAShapeLayer()
        //: rippleLayer.opacity = 1.0
        rippleLayer.opacity = 1.0
        //: rippleLayer.fillColor = animationColor?.cgColor
        rippleLayer.fillColor = animationColor?.cgColor
        //: rippleLayer.path = circleMaskPathFinal.cgPath
        rippleLayer.path = circleMaskPathFinal.cgPath
        //: coverView.layer.addSublayer(rippleLayer)
        coverView.layer.addSublayer(rippleLayer)

        // fade up
        //: let fadeUp = CABasicAnimation(keyPath: "opacity")
        let fadeUp = CABasicAnimation(keyPath: (String(constExpressionFormat) + String(showDisappearId)))
        //: fadeUp.beginTime = CACurrentMediaTime()
        fadeUp.beginTime = CACurrentMediaTime()
        //: fadeUp.duration = 0.1
        fadeUp.duration = 0.1
        //: fadeUp.toValue = 0.8
        fadeUp.toValue = 0.8
        //: fadeUp.timingFunction = CAMediaTimingFunction(name: CAMediaTimingFunctionName.easeOut)
        fadeUp.timingFunction = CAMediaTimingFunction(name: CAMediaTimingFunctionName.easeOut)
        //: fadeUp.fillMode = CAMediaTimingFillMode.forwards
        fadeUp.fillMode = CAMediaTimingFillMode.forwards
        //: fadeUp.isRemovedOnCompletion = false
        fadeUp.isRemovedOnCompletion = false
        //: rippleLayer.add(fadeUp, forKey: nil)
        rippleLayer.add(fadeUp, forKey: nil)

        // fade down
        //: let fade = CABasicAnimation(keyPath: "opacity")
        let fade = CABasicAnimation(keyPath: (String(constExpressionFormat) + String(showDisappearId)))
        //: fade.beginTime = CACurrentMediaTime() + animationDuration * 0.60
        fade.beginTime = CACurrentMediaTime() + animationDuration * 0.60
        //: fade.duration = 0.1
        fade.duration = 0.1
        //: fade.toValue = 0
        fade.toValue = 0
        //: fade.timingFunction = CAMediaTimingFunction(name: CAMediaTimingFunctionName.easeOut)
        fade.timingFunction = CAMediaTimingFunction(name: CAMediaTimingFunctionName.easeOut)
        //: fade.fillMode = CAMediaTimingFillMode.forwards
        fade.fillMode = CAMediaTimingFillMode.forwards
        //: fade.isRemovedOnCompletion = false
        fade.isRemovedOnCompletion = false
        //: rippleLayer.add(fade, forKey: nil)
        rippleLayer.add(fade, forKey: nil)

        // change path
        //: CATransaction.begin()
        CATransaction.begin()
        //: let maskLayerAnimation = CABasicAnimation(keyPath: "path")
        let maskLayerAnimation = CABasicAnimation(keyPath: (String(dataWrittenText)))
        //: maskLayerAnimation.fromValue = circleMaskPathInitial.cgPath
        maskLayerAnimation.fromValue = circleMaskPathInitial.cgPath
        //: maskLayerAnimation.toValue = circleMaskPathFinal.cgPath
        maskLayerAnimation.toValue = circleMaskPathFinal.cgPath
        //: maskLayerAnimation.beginTime = CACurrentMediaTime()
        maskLayerAnimation.beginTime = CACurrentMediaTime()
        //: maskLayerAnimation.duration = 0.1
        maskLayerAnimation.duration = 0.1
        //: maskLayerAnimation.timingFunction = CAMediaTimingFunction(name: CAMediaTimingFunctionName.easeOut)
        maskLayerAnimation.timingFunction = CAMediaTimingFunction(name: CAMediaTimingFunctionName.easeOut)
        //: CATransaction.setCompletionBlock({
        CATransaction.setCompletionBlock {
            //: coverView.removeFromSuperview()
            coverView.removeFromSuperview()
            //: })
        }
        //: rippleLayer.add(maskLayerAnimation, forKey: "path")
        rippleLayer.add(maskLayerAnimation, forKey: (String(dataWrittenText)))
        //: CATransaction.commit()
        CATransaction.commit()
    }
}

// MARK: - Public Event

//: extension TalkingChatListTableCell {
extension IonViewCell {
    /// 刷新cell
    /// - Parameter model: 模型
    //: func refreshCell(model: TalkingConversationModel?) {
    func compare(model: MultistageRocketConversationModel?) {
        //: guard let model = model else { return }
        guard let model = model else { return }
        //: let isOther = (model.chatType == .system || model.chatType == .service ||
        let isOther = (model.chatType == .system || model.chatType == .service ||
            //: model.chatType == .group || model.chatType == .moreMsg)
            model.chatType == .group || model.chatType == .moreMsg)
        // 更新昵称、头像、vip标识
        //: updateUserInfo(userInfo: model.gj_userInfo, model: model, isOther: isOther)
        fullOther(userInfo: model.gj_userInfo, model: model, isOther: isOther)
        // 未读消息数
        //: updateUnreadNumber(unreadCount: Int(model.unreadCount))
        choroidCoat(unreadCount: Int(model.unreadCount))
        // 详情消息
        //: handleC2CMessage(model: model)
        indigenous(model: model)

        //: if model.chatType == .group || model.chatType == .moreMsg {
        if model.chatType == .group || model.chatType == .moreMsg {
            //: self.accessoryType = .disclosureIndicator
            self.accessoryType = .disclosureIndicator // 添加箭头
            //: } else {
        } else {
            //: self.accessoryType = .none
            self.accessoryType = .none
        }
        // 时间
        //: timeLab.text = nil
        timeLab.text = nil
        //: if isOther == false && model.hasTimestamp {
        if isOther == false, model.hasTimestamp {
            //: let timestamp: Date? = model.timestamp as Date?
            let timestamp: Date? = model.timestamp as Date?
            //: if timestamp != nil && timestamp!.timeIntervalSince1970 > NSTimeIntervalSince1970 {
            if timestamp != nil, timestamp!.timeIntervalSince1970 > NSTimeIntervalSince1970 {
                //: timeLab.text = NSDate.tx_messageTimeString(showDetail: false, date: timestamp!)
                timeLab.text = NSDate.underMinimum(showDetail: false, date: timestamp!)
            }
        }
        // 置顶背景色
        //: if model.isPinned == true {
        if model.isPinned == true {
            //: contentView.backgroundColor = UIColor(hex: "#EEEEEE")
            contentView.backgroundColor = UIColor(hex: (String(main_regionContent)))
            //: } else {
        } else {
            //: contentView.backgroundColor = (FacultyReactiveCompatible.share.appStatus == AppSkinStatus.special.rawValue) ? .clear:.white
            contentView.backgroundColor = (FacultyReactiveCompatible.share.appStatus == PenConstantTarget.special.rawValue) ? .clear : .white
        }
        //: self.backgroundColor = contentView.backgroundColor
        self.backgroundColor = contentView.backgroundColor
    }

    /// 处理最新消息
    //: static func handleShowC2CDetailTextOfMessage(model: TalkingConversationModel) -> NSMutableAttributedString? {
    static func slipUp(model: MultistageRocketConversationModel) -> NSMutableAttributedString? {
//        messageType 消息类型：1系统消息 2官方小秘 3自动打招呼 4主动打招呼 5付费消息 6普通消息 7主动打招呼未回复 8.主打招呼回复 9，自动打招呼未回复 10, 自动打招呼回复11.发送礼物消息 12互相关注消息 13 取消关注 22家族邀请跳转消息 25索要礼物 29问题回答消息
        //: var detailAttrString: NSMutableAttributedString?
        var detailAttrString: NSMutableAttributedString?
        //: let change = false
        let change = false
        //: var speColor = [NSValue: UIColor]()
        var speColor = [NSValue: UIColor]()
        //: let showUnread = model.isShowMsgUnread && model.unreadCount > 0
        let showUnread = model.isShowMsgUnread && model.unreadCount > 0
        //: let direction: TMsgDirection = (model.listShowMessage?.isSelf ?? false) ? .MsgDirectionOutgoing:.MsgDirectionIncoming
        let direction: BeAdditiveArithmetic = (model.listShowMessage?.isSelf ?? false) ? .MsgDirectionOutgoing : .MsgDirectionIncoming
        //: var voice_msgInfo: JSON?
        var voice_msgInfo: JSON?

        //: guard model.draftText.isEmptyString else {
        guard model.draftText.isEmptyString else { // 草稿
            //: detailAttrString = self.getDraftString(draft: &model.draftText)
            detailAttrString = self.textQuantity(draft: &model.draftText)
            //: detailAttrString?.addAttribute(.font, value: UIFont.pingfangFont(type: .Regular, fontSize: 15), range: NSRange(location: 0, length: detailAttrString?.length ?? 0))
            detailAttrString?.addAttribute(.font, value: UIFont.recipeDecision(type: .Regular, fontSize: 15), range: NSRange(location: 0, length: detailAttrString?.length ?? 0))
            //: return detailAttrString
            return detailAttrString
        }

        //: let extra = String(data: (model.listShowMessage?.customElem.data)!, encoding: .utf8)
        let extra = String(data: (model.listShowMessage?.customElem.data)!, encoding: .utf8)
        //: let json = JSON(parseJSON: extra!)
        let json = JSON(parseJSON: extra!)
        //: let extraDic = json["extra"]
        let extraDic = json[(String(const_woundStr))]
        //: let msgInfo = extraDic["msgInfo"]
        let msgInfo = extraDic[(String(show_trafficId))]
        //: var content = msgInfo["content"].stringValue.deleteHtmlTips()
        var content = msgInfo[(mainGameMsg.replacingOccurrences(of: "heavy", with: "o") + String(showPullStr))].stringValue.forceRoot()
        //: let messageType = msgInfo["messageType"].intValue
        let messageType = msgInfo[(data_accountingFormat.replacingOccurrences(of: "flag", with: "ss") + "ageTyp" + constCalendarMessage.replacingOccurrences(of: "sub", with: "e"))].intValue
        //: let formatTypeStr = extraDic["formatType"].stringValue
        let formatTypeStr = extraDic[(String(const_yellowPath) + notiGrossRelatedPath.replacingOccurrences(of: "mention", with: "e"))].stringValue

        //: if TalkingPrivateChatManager.isReplyToGetPointsMsg(direction, messageType, msgTime: model.listShowMessage?.timestamp) {
        if SpanChatManager.attributeTime(direction, messageType, msgTime: model.listShowMessage?.timestamp) { // 回复获取积分消息
            //: let content = "[Reply messages to get points]".localized
            let content = (String(main_cropPath.suffix(6)) + " message" + String(userEscapeName)).localized
            //: let color  = UIColor.commercialMessage()
            let color = UIColor.commercialMessage()
            //: detailAttrString = NSMutableAttributedString(string: content)
            detailAttrString = NSMutableAttributedString(string: content)
            //: speColor = [NSValue(range: NSRange(location: 0, length: detailAttrString?.length ?? 0)): color]
            speColor = [NSValue(range: NSRange(location: 0, length: detailAttrString?.length ?? 0)): color]

            //: } else if model.chatType == .system {
        } else if model.chatType == .system { // 官方消息，只展示文本
            //: var content = extraDic["title"].stringValue
            var content = extraDic[(String(app_operateKey))].stringValue
            //: let sysContent = extraDic["content"].stringValue
            let sysContent = extraDic[(mainGameMsg.replacingOccurrences(of: "heavy", with: "o") + String(showPullStr))].stringValue
            //: let sysImg = extraDic["img"].stringValue
            let sysImg = extraDic[(String(main_generalData))].stringValue
            //: if content.count > 0 || sysContent.count > 0 || sysImg.count > 0 {
            if content.count > 0 || sysContent.count > 0 || sysImg.count > 0 { // 跳转系统消息
                //: if content.count == 0 {
                if content.count == 0 {
                    //: content.append(sysContent)
                    content.append(sysContent)
                }
                //: if sysImg.count > 0 {
                if sysImg.count > 0 {
                    //: content.append("[Photo]".localized)
                    content.append((String(showPanBodyTitle.suffix(5)) + String(show_launchRegainStr.prefix(2))).localized)
                }

                //: } else {
            } else { // 默认系统消息
                //: content = msgInfo["content"].stringValue
                content = msgInfo[(mainGameMsg.replacingOccurrences(of: "heavy", with: "o") + String(showPullStr))].stringValue
                //: if content.isEmptyString {
                if content.isEmptyString {
                    //: content = json["content"].stringValue
                    content = json[(mainGameMsg.replacingOccurrences(of: "heavy", with: "o") + String(showPullStr))].stringValue
                }
                //: content = content.deleteHtmlTips()
                content = content.forceRoot()
            }
            //: detailAttrString = NSMutableAttributedString(string: content)
            detailAttrString = NSMutableAttributedString(string: content)

            //: } else if CircularReactiveCompatible.checkVoiceJsonMsg(msginfo: msgInfo.dictionaryValue) {
        } else if CircularReactiveCompatible.simultaneously(msginfo: msgInfo.dictionaryValue) { // 语音
            //: voice_msgInfo = msgInfo
            voice_msgInfo = msgInfo
            //: content = "[Audio]".localized
            content = (String(k_handUrl) + String(showBathroomFormat.suffix(6))).localized
            //: detailAttrString = NSMutableAttributedString(string: content)
            detailAttrString = NSMutableAttributedString(string: content)

            //: } else if json["rYMsgType"].stringValue == "GJ:CallCustom" { // 音视频通话
        } else if json[(String(constDepthStr) + String(k_strangerStr))].stringValue == (String(showUponStr.prefix(3)) + String(dataCuriosityName)) { // 音视频通话
            //: if msgInfo["callCmd"].stringValue == "missCall" { // 未接来电
            if msgInfo[(String(dataFormerId.suffix(5)) + String(main_dimensionData))].stringValue == (String(kCoatValue)) { // 未接来电
                //: content = "[Missed Call]".localized
                content = (String(main_teamName.suffix(8)) + String(data_submitAlleyId.suffix(5))).localized
                //: } else {
            } else {
                //: if msgInfo["logType"].intValue == 1 { // 音频
                if msgInfo[(String(main_cellId.prefix(7)))].intValue == 1 { // 音频
                    //: content = "[Voice Call]".localized
                    content = (String(k_resumeFormat) + kTailRobotFormat.capitalized).localized
                    //: } else {
                } else {
                    //: content = "[Video Call]".localized
                    content = (String(const_alongsideFormat.prefix(4)) + "eo Call]").localized
                }
            }

            //: detailAttrString = NSMutableAttributedString(string: content)
            detailAttrString = NSMutableAttributedString(string: content)

            //: } else if msgInfo["video"].exists() || msgInfo["contentType"].stringValue == "VideoMsg" { // 短视频
        } else if msgInfo[(String(app_stepUrl))].exists() || msgInfo[(constAppointmentValue.replacingOccurrences(of: "reception", with: "c") + "ntType")].stringValue == (String(constScanTitle.prefix(4)) + "oMsg") { // 短视频
            //: content = "[Short Video]".localized
            content = (String(show_hereValue.suffix(6)) + " Vide" + String(data_onlyStr)).localized
            //: detailAttrString = NSMutableAttributedString(string: content)
            detailAttrString = NSMutableAttributedString(string: content)

            //: } else if (extraDic["match"].stringValue == "1" || msgInfo["match"].stringValue == "1") { // 匹配消息
        } else if extraDic[(userPaperTitle.replacingOccurrences(of: "round", with: "h"))].stringValue == "1" || msgInfo[(userPaperTitle.replacingOccurrences(of: "round", with: "h"))].stringValue == "1" { // 匹配消息
            //: content = "[Cupid messaging]".localized
            content = (String(notiAnimaData) + String(app_mailName)).localized
            //: detailAttrString = NSMutableAttributedString(string: content)
            detailAttrString = NSMutableAttributedString(string: content)

            //: } else if json["rYMsgType"].stringValue == "GJ:Sess:TxtCustom" { // 提示消息
        } else if json[(String(constDepthStr) + String(k_strangerStr))].stringValue == (String(data_signatureId) + String(noti_normalUrl) + String(main_deviceMsg)) { // 提示消息
            //: content = json["content"].stringValue
            content = json[(mainGameMsg.replacingOccurrences(of: "heavy", with: "o") + String(showPullStr))].stringValue
            //: detailAttrString = NSMutableAttributedString(string: content)
            detailAttrString = NSMutableAttributedString(string: content)

            //: } else if messageType == 11 {
        } else if messageType == 11 { // 送礼
            //: let isSend = msgInfo["isSend"].string
            let isSend = msgInfo[(String(user_priorityMessage))].string
            //: var string1: String?
            var string1: String?
            //: if isSend != nil {
            if isSend != nil {
                //: string1 = isSend!.boolValue ? "Sent Gift".localized:"Receive Gift".localized
                string1 = isSend!.boolValue ? (String(appCurFormat) + String(constResValue.prefix(4)) + "Gift").localized : (String(app_produceValue.suffix(5)) + String(notiRenderTitle.prefix(7))).localized
                //: } else {
            } else {
                //: let uid = msgInfo["uid"].stringValue
                let uid = msgInfo[(String(showThatRelateUnlessTitle))].stringValue
                //: string1 = (uid == FacultyReactiveCompatible.share.loginUserMode.userID) ? "Sent Gift".localized:"Receive Gift".localized
                string1 = (uid == FacultyReactiveCompatible.share.loginUserMode.userID) ? (String(appCurFormat) + String(constResValue.prefix(4)) + "Gift").localized : (String(app_produceValue.suffix(5)) + String(notiRenderTitle.prefix(7))).localized
            }
            //: let name = extraDic["gift"]["name"].stringValue
            let name = extraDic[(mainMergeUrl.replacingOccurrences(of: "someone", with: "t"))][(noti_growthFormat.replacingOccurrences(of: "plant", with: "m"))].stringValue
            //: let num = extraDic["gift"]["num"].stringValue
            let num = extraDic[(mainMergeUrl.replacingOccurrences(of: "someone", with: "t"))][(mainForthPhaseFormat.lowercased())].stringValue

            //: content = "\(string1!) \(name) x\(num)"
            content = "\(string1!) \(name) x\(num)"
            //: detailAttrString = NSMutableAttributedString(string: content)
            detailAttrString = NSMutableAttributedString(string: content)

            //: } else if messageType == 4 {
        } else if messageType == 4 { // 主动打招呼
//            let uid = msgInfo["uid"].stringValue
//            let toUid = msgInfo["toUid"].stringValue
            //: detailAttrString = NSMutableAttributedString(string: content)
            detailAttrString = NSMutableAttributedString(string: content)

            //: } else if messageType == 3 {
        } else if messageType == 3 { // 自动打招呼
            //: let imageUri = msgInfo["imageUri"].stringValue
            let imageUri = msgInfo[(String(k_smartUrl.prefix(7)) + String(show_greenKey))].stringValue
            //: let imageData = msgInfo["imageData"].stringValue
            let imageData = msgInfo[(String(constPhysicalUnderMsg) + kAffectFormat.replacingOccurrences(of: "hire", with: "t"))].stringValue

            //: if CircularReactiveCompatible.checkVoiceJsonMsg(msginfo: msgInfo.dictionaryValue) {
            if CircularReactiveCompatible.simultaneously(msginfo: msgInfo.dictionaryValue) {
                //: content = "[Audio]".localized
                content = (String(k_handUrl) + String(showBathroomFormat.suffix(6))).localized
                //: } else if (imageUri.count > 0 || imageData.count > 0) {
            } else if imageUri.count > 0 || imageData.count > 0 {
                //: content = "[Photo]".localized
                content = (String(showPanBodyTitle.suffix(5)) + String(show_launchRegainStr.prefix(2))).localized
            }
            //: detailAttrString = NSMutableAttributedString(string: content)
            detailAttrString = NSMutableAttributedString(string: content)

            //: } else if formatTypeStr == RCSystemFormatTypePlain {
        } else if formatTypeStr == user_serviceAgentId {
            //: var content  = msgInfo["content"].stringValue
            var content = msgInfo[(mainGameMsg.replacingOccurrences(of: "heavy", with: "o") + String(showPullStr))].stringValue
            //: var imageUrl = extraDic["imageUri"].stringValue
            var imageUrl = extraDic[(String(k_smartUrl.prefix(7)) + String(show_greenKey))].stringValue
            //: let imageData = extraDic["imageData"].stringValue
            let imageData = extraDic[(String(constPhysicalUnderMsg) + kAffectFormat.replacingOccurrences(of: "hire", with: "t"))].stringValue
            //: if imageUrl.count == 0 || imageData.count > 0 {
            if imageUrl.count == 0 || imageData.count > 0 {
                //: if content.hasPrefix("<img>") && content.hasSuffix("</img>") {
                if content.hasPrefix((String(show_bellyDevoteOriginallyStr))), content.hasSuffix((String(mainPointKey.suffix(6)))) {
                    //: var str = NSString(string: content)
                    var str = NSString(string: content)
                    //: str = str.replacingOccurrences(of: "<img>", with: "") as NSString
                    str = str.replacingOccurrences(of: (String(show_bellyDevoteOriginallyStr)), with: "") as NSString
                    //: str = str.replacingOccurrences(of: "</img>", with: "") as NSString
                    str = str.replacingOccurrences(of: (String(mainPointKey.suffix(6))), with: "") as NSString
                    //: imageUrl = str as String
                    imageUrl = str as String
                }
            }

            //: if (imageUrl.count > 0 || imageData.count > 0) {
            if imageUrl.count > 0 || imageData.count > 0 {
                //: content = "[Photo]".localized
                content = (String(showPanBodyTitle.suffix(5)) + String(show_launchRegainStr.prefix(2))).localized
                //: } else {
            } else {
                //: content = content.deleteHtmlTips()
                content = content.forceRoot()
            }

            //: detailAttrString = NSMutableAttributedString(string: content)
            detailAttrString = NSMutableAttributedString(string: content)

            //: } else if messageType == 25 {
        } else if messageType == 25 { // 索要礼物
            //: if let wantGift  = msgInfo["wantGift"].dictionary {
            if let wantGift = msgInfo[(String(user_processingStr) + String(noti_randomValue.suffix(6)))].dictionary {
                //: let content = wantGift["content"]?.string ?? ""
                let content = wantGift[(mainGameMsg.replacingOccurrences(of: "heavy", with: "o") + String(showPullStr))]?.string ?? ""
                //: detailAttrString = NSMutableAttributedString(string: content)
                detailAttrString = NSMutableAttributedString(string: content)
            }

            //: } else if messageType == 29, direction == .MsgDirectionIncoming, msgInfo["roundNum"].intValue == 1 { // 问题回答消息
        } else if messageType == 29, direction == .MsgDirectionIncoming, msgInfo[(String(const_dayPath))].intValue == 1 { // 问题回答消息
            //: content = "[Question]".localized
            content = (String(mainDoingceInsertId) + String(constComeMsg.suffix(6))).localized
            //: detailAttrString = NSMutableAttributedString(string: content)
            detailAttrString = NSMutableAttributedString(string: content)

            //: } else {
        } else {
            // 图片
            //: let imageUri             = msgInfo["imageUri"].stringValue
            let imageUri = msgInfo[(String(k_smartUrl.prefix(7)) + String(show_greenKey))].stringValue
            //: let imageData            = msgInfo["imageData"].stringValue
            let imageData = msgInfo[(String(constPhysicalUnderMsg) + kAffectFormat.replacingOccurrences(of: "hire", with: "t"))].stringValue
            //: let intimatePhotoDict    = msgInfo["intimatePhoto"].dictionaryValue // 私密照片
            let intimatePhotoDict = msgInfo[(String(app_mustBeanPriceText) + String(main_inventoryPitchText))].dictionaryValue // 私密照片
            //: if (imageUri.count > 0 || imageData.count > 0 || intimatePhotoDict.keys.count > 0) {
            if imageUri.count > 0 || imageData.count > 0 || intimatePhotoDict.keys.count > 0 {
                //: content = "[Photo]".localized
                content = (String(showPanBodyTitle.suffix(5)) + String(show_launchRegainStr.prefix(2))).localized
            }
            // 语音消息
            //: if CircularReactiveCompatible.checkVoiceJsonMsg(msginfo: msgInfo.dictionaryValue) {
            if CircularReactiveCompatible.simultaneously(msginfo: msgInfo.dictionaryValue) {
                //: content = "[Audio]".localized
                content = (String(k_handUrl) + String(showBathroomFormat.suffix(6))).localized
            }

            //: detailAttrString = NSMutableAttributedString(string: content)
            detailAttrString = NSMutableAttributedString(string: content)
        }

        //: if (!change) {
        if !change {
            //: if detailAttrString?.string == "[Audio]".localized {
            if detailAttrString?.string == (String(k_handUrl) + String(showBathroomFormat.suffix(6))).localized {
                //: let sendUid = model.sender
                let sendUid = model.sender
                //: let mid = FacultyReactiveCompatible.share.loginUserMode.userID
                let mid = FacultyReactiveCompatible.share.loginUserMode.userID
                //: if sendUid == mid {
                if sendUid == mid { // 我发送的
                    //: } else {
                } else { // 别人发的
                    //: var downurl = ""
                    var downurl = ""
                    //: if (voice_msgInfo?["audioData"].stringValue.isEmptyString) != nil {
                    if (voice_msgInfo?[(appComplexUrl.replacingOccurrences(of: "fun", with: "ud") + "Data")].stringValue.isEmptyString) != nil {
                        //: downurl = voice_msgInfo!["audioData"].stringValue
                        downurl = voice_msgInfo![(appComplexUrl.replacingOccurrences(of: "fun", with: "ud") + "Data")].stringValue
                        //: } else if (voice_msgInfo?["audioUri"].stringValue.isEmptyString) != nil {
                    } else if (voice_msgInfo?[(String(k_bothText))].stringValue.isEmptyString) != nil {
                        //: downurl = voice_msgInfo!["audioUri"].stringValue
                        downurl = voice_msgInfo![(String(k_bothText))].stringValue
                    }
                }
                //: let unreadColor = UIColor.appValueDetailColor()
                let unreadColor = UIColor.keepRid()
                //: detailAttrString?.addAttribute(.foregroundColor, value: unreadColor, range: NSRange(location: 0, length: detailAttrString?.length ?? 0))
                detailAttrString?.addAttribute(.foregroundColor, value: unreadColor, range: NSRange(location: 0, length: detailAttrString?.length ?? 0))

                //: } else {
            } else {
                //: let unreadColor = UIColor.appValueDetailColor()
                let unreadColor = UIColor.keepRid()
                //: detailAttrString?.addAttribute(.foregroundColor, value: unreadColor, range: NSRange(location: 0, length: detailAttrString?.length ?? 0))
                detailAttrString?.addAttribute(.foregroundColor, value: unreadColor, range: NSRange(location: 0, length: detailAttrString?.length ?? 0))
            }
        }
        //: if speColor.keys.count > 0 {
        if speColor.keys.count > 0 {
            //: for (key, value) in speColor {
            for (key, value) in speColor {
                //: detailAttrString?.addAttribute(.foregroundColor, value: value, range: key.rangeValue)
                detailAttrString?.addAttribute(.foregroundColor, value: value, range: key.rangeValue)
            }
        }
        //: detailAttrString?.addAttribute(.font, value: UIFont.pingfangFont(type: .Regular, fontSize: 15), range: NSRange(location: 0, length: detailAttrString?.length ?? 0))
        detailAttrString?.addAttribute(.font, value: UIFont.recipeDecision(type: .Regular, fontSize: 15), range: NSRange(location: 0, length: detailAttrString?.length ?? 0))

        //: return detailAttrString
        return detailAttrString
    }

    /// 处理草稿消息
    //: static func getDraftString(draft: inout String) -> NSMutableAttributedString {
    static func textQuantity(draft: inout String) -> NSMutableAttributedString {
        //: if draft.contains("draftContent") {
        if draft.contains((String(userGameMinePath) + String(const_joinTitle.suffix(6)) + "tent")) {
            //: let dict = NSDictionary.dictionary(withJsonString: draft)
            let dict = NSDictionary.beginShoo(withJsonString: draft)
            //: draft = "\(String(describing: dict?["draftContent"]))"
            draft = "\(String(describing: dict?[(String(userGameMinePath) + String(const_joinTitle.suffix(6)) + "tent")]))"
        }
        //: let draftString = NSMutableAttributedString.init(string: "[draft]%@".localizedArguments(draft))
        let draftString = NSMutableAttributedString(string: (String(data_pocketFatherValue)).extraPropertyLetter(draft))
        //: draftString.addAttribute(.foregroundColor, value: UIColor.msgTipsColor(), range: NSRange(location: 0, length: 7))
        draftString.addAttribute(.foregroundColor, value: UIColor.supernumerary(), range: NSRange(location: 0, length: 7))
        //: return draftString
        return draftString
    }
}

// MARK: - Private Event

//: extension TalkingChatListTableCell {
extension IonViewCell {
    /// 更新昵称、头像
    /// - Parameters:
    ///   - userInfo: 数据库信息
    ///   - model: tx数据
    ///   - isOther: 是否其他消息
    //: private func updateUserInfo(userInfo: ConsumerPenHandyJSON?, model: TalkingConversationModel, isOther: Bool) {
    private func fullOther(userInfo: ConsumerPenHandyJSON?, model: MultistageRocketConversationModel, isOther: Bool) {
        // 在线状态
        //: onlineStatus.isHidden = (model.listOnlineStatus == 0)
        onlineStatus.isHidden = (model.listOnlineStatus == 0)
        //: newImgV.isHidden = !model.isNewUser
        newImgV.isHidden = !model.isNewUser
        // 是否存在用户信息
        //: if userInfo == nil || userInfo?.uid == MacroBorderDefineThen.getXiaoMiID() || userInfo?.uid == MacroBorderDefineThen.getCustomerServiceID() {
        if userInfo == nil || userInfo?.uid == MacroBorderDefineThen.seethe() || userInfo?.uid == MacroBorderDefineThen.receiveRemove() {
            //: nickNameLabel.textColor = .mottleResult()
            nickNameLabel.textColor = .mottleResult()
            //: nickNameLabel.text = model.showName
            nickNameLabel.text = model.showName
            //: nickNameLabel.snp.updateConstraints { make in
            nickNameLabel.snp.updateConstraints { make in
                //: make.trailing.lessThanOrEqualToSuperview().offset(-40)
                make.trailing.lessThanOrEqualToSuperview().offset(-40)
            }
            //: if model.chatType == .group || model.chatType == .moreMsg {
            if model.chatType == .group || model.chatType == .moreMsg {
                //: headImgView.image = UIImage.BundleImageNamed(name: model.faceUrl)
                headImgView.image = UIImage.adName(name: model.faceUrl)
                //: } else {
            } else {
                //: headImgView.setUrlImage(urlStr: model.faceUrl, placeImg: UIImage.placeSquareBigImgBanner())
                headImgView.sortAge(urlStr: model.faceUrl, placeImg: UIImage.ridMale())
            }
            //: iconBorder.isHidden = true
            iconBorder.isHidden = true
            //: loungeImgV.isHidden = true
            loungeImgV.isHidden = true
            //: authImgV.isHidden = true
            authImgV.isHidden = true
            //: officialImgV.isHidden = true
            officialImgV.isHidden = true
            //: imtimateBtn.isHidden = true
            imtimateBtn.isHidden = true
            //: return
            return
        }
        //: guard let userInfo = userInfo else {
        guard let userInfo = userInfo else {
            //: return
            return
        }
        // 昵称
        //: nickNameLabel.textColor = userInfo.loungePlus ? .areaEmphasis():.mottleResult()
        nickNameLabel.textColor = userInfo.loungePlus ? .areaEmphasis() : .mottleResult()
        //: nickNameLabel.text = userInfo.nickname.count > 0 ? userInfo.nickname:model.userID
        nickNameLabel.text = userInfo.nickname.count > 0 ? userInfo.nickname : model.userID
        // 头像
        //: headImgView.setUrlImage(urlStr: userInfo.headPic, placeImg: UIImage.placeHolderImage(sex: userInfo.sex))
        headImgView.sortAge(urlStr: userInfo.headPic, placeImg: UIImage.gingerSnap(sex: userInfo.sex))
        // VIP头像框
        //: if isOther == false && !userInfo.headPicFrame.isEmptyString {
        if isOther == false, !userInfo.headPicFrame.isEmptyString {
            //: iconBorder.isHidden = false
            iconBorder.isHidden = false
            //: iconBorder.setHeadFrameUrlImage(urlStr: userInfo.headPicFrame)
            iconBorder.slash(urlStr: userInfo.headPicFrame)
            //: } else {
        } else {
            //: iconBorder.isHidden = true
            iconBorder.isHidden = true
        }
        //: imtimateBtn.setTitle(model.totalIntimate > 99999 ? "99999+": "\(model.totalIntimate)", for: .normal)
        imtimateBtn.setTitle(model.totalIntimate > 99999 ? String(bytes: app_capacityStr.map{$0^131}, encoding: .utf8)! : "\(model.totalIntimate)", for: .normal)
        //: imtimateBtn.isHidden = model.totalIntimate <= 0
        imtimateBtn.isHidden = model.totalIntimate <= 0
        //: if model.totalIntimate > 0 {
        if model.totalIntimate > 0 {
            //: let size = imtimateBtn.titleLabel?.text?.boundingRect(with: CGSize(width: CGFloat(MAXFLOAT), height: 0.0), options: .usesLineFragmentOrigin, attributes: [NSAttributedString.Key.font: imtimateBtn.titleLabel?.font ?? .pingfangFont(type: .Medium, fontSize: 12)], context: nil)
            let size = imtimateBtn.titleLabel?.text?.boundingRect(with: CGSize(width: CGFloat(MAXFLOAT), height: 0.0), options: .usesLineFragmentOrigin, attributes: [NSAttributedString.Key.font: imtimateBtn.titleLabel?.font ?? .recipeDecision(type: .Medium, fontSize: 12)], context: nil)
            //: imtimateBtn.snp.updateConstraints { make in
            imtimateBtn.snp.updateConstraints { make in
                //: make.width.equalTo((size?.width ?? 50) + 20)
                make.width.equalTo((size?.width ?? 50) + 20)
            }
        }
        // 是否其他消息
        //: if isOther == false {
        if isOther == false {
            //: loungeImgV.isHidden = !userInfo.loungePlus
            loungeImgV.isHidden = !userInfo.loungePlus
            //: authImgV.isHidden = !userInfo.tpAuth
            authImgV.isHidden = !userInfo.tpAuth
            //: officialImgV.isHidden = !userInfo.isOfficial
            officialImgV.isHidden = !userInfo.isOfficial
            //: } else {
        } else {
            //: loungeImgV.isHidden = true
            loungeImgV.isHidden = true
            //: authImgV.isHidden = true
            authImgV.isHidden = true
            //: officialImgV.isHidden = true
            officialImgV.isHidden = true
        }

        //: nickNameLabel.snp.updateConstraints { make in
        nickNameLabel.snp.updateConstraints { make in
            //: make.trailing.lessThanOrEqualToSuperview().offset( isOther == true ? -40 : -130)
            make.trailing.lessThanOrEqualToSuperview().offset(isOther == true ? -40 : -130)
        }
        //: loungeImgV.snp.updateConstraints { make in
        loungeImgV.snp.updateConstraints { make in
            //: make.leading.equalTo(nickNameLabel.snp.trailing).offset(loungeImgV.isHidden == true ? 0:4)
            make.leading.equalTo(nickNameLabel.snp.trailing).offset(loungeImgV.isHidden == true ? 0 : 4)
            //: make.width.equalTo(loungeImgV.isHidden == true ? 0:20)
            make.width.equalTo(loungeImgV.isHidden == true ? 0 : 20)
        }
        //: authImgV.snp.updateConstraints { make in
        authImgV.snp.updateConstraints { make in
            //: make.leading.equalTo(loungeImgV.snp.trailing).offset(authImgV.isHidden == true ? 0:4)
            make.leading.equalTo(loungeImgV.snp.trailing).offset(authImgV.isHidden == true ? 0 : 4)
            //: make.width.equalTo(authImgV.isHidden == true ? 0:16)
            make.width.equalTo(authImgV.isHidden == true ? 0 : 16)
        }
        //: let text = "New".localized
        let text = (String(userUniqueText)).localized
        //: let rect = text.boundingRect(with: .zero, options: .usesLineFragmentOrigin, attributes: [.font: UIFont.pingfangMediumFont(fontSize: 11)], context: nil)
        let rect = text.boundingRect(with: .zero, options: .usesLineFragmentOrigin, attributes: [.font: UIFont.originally(fontSize: 11)], context: nil)
        //: newImgV.snp.updateConstraints { make in
        newImgV.snp.updateConstraints { make in
            //: make.leading.equalTo(authImgV.snp.trailing).offset(newImgV.isHidden == true ? 0:4)
            make.leading.equalTo(authImgV.snp.trailing).offset(newImgV.isHidden == true ? 0 : 4)
            //: make.width.equalTo(newImgV.isHidden == true ? 0:rect.width+10)
            make.width.equalTo(newImgV.isHidden == true ? 0 : rect.width + 10)
        }
        //: officialImgV.snp.remakeConstraints { make in
        officialImgV.snp.remakeConstraints { make in
            //: make.centerY.equalTo(nickNameLabel)
            make.centerY.equalTo(nickNameLabel)
            //: make.height.equalTo(15)
            make.height.equalTo(15)
            //: make.leading.equalTo(newImgV.snp.trailing).offset(officialImgV.isHidden == true ? 0:4)
            make.leading.equalTo(newImgV.snp.trailing).offset(officialImgV.isHidden == true ? 0 : 4)
            //: if officialImgV.isHidden {
            if officialImgV.isHidden {
                //: make.width.equalTo(0)
                make.width.equalTo(0)
                //: } else {
            } else {
                //: make.width.greaterThanOrEqualTo(50)
                make.width.greaterThanOrEqualTo(50)
            }
        }
    }

    /// 更新未读数
    /// - Parameter unreadCount: 未读消息数
    //: private func updateUnreadNumber(unreadCount: Int) {
    private func choroidCoat(unreadCount: Int) {
        //: guard unreadCount > 0 else {
        guard unreadCount > 0 else {
            //: self.badgeLab.isHidden = true
            self.badgeLab.isHidden = true
            //: return
            return
        }

        //: self.badgeLab.isHidden = false
        self.badgeLab.isHidden = false
        //: var unreadCountStr = String(unreadCount)
        var unreadCountStr = String(unreadCount)
        //: if unreadCount > 99 {
        if unreadCount > 99 {
            //: unreadCountStr = String(unreadCount) + "+"
            unreadCountStr = String(unreadCount) + "+"
            //: badgeLab.text = "99+"
            badgeLab.text = "99+"
            //: } else {
        } else {
            //: badgeLab.text = unreadCountStr
            badgeLab.text = unreadCountStr
        }
        //: var width = badgeLab.text!.size(withAttributes: [.font: self.badgeLab.font!]).width + 12
        var width = badgeLab.text!.size(withAttributes: [.font: self.badgeLab.font!]).width + 12
        //: if width < 20 { width = 20 }
        if width < 20 { width = 20 }
        //: badgeLab.snp.updateConstraints { make in
        badgeLab.snp.updateConstraints { make in
            //: make.width.greaterThanOrEqualTo(width)
            make.width.greaterThanOrEqualTo(width)
        }
    }

    /// 处理详情消息
    //: private func handleC2CMessage(model: TalkingConversationModel) {
    private func indigenous(model: MultistageRocketConversationModel) {
        //: if model.userStatus == 5 {
        if model.userStatus == 5 {
            //: let str = "Account is restricted！".localized
            let str = (String(notiImagineCancelName.prefix(7)) + String(noti_holderData.prefix(7)) + String(k_songContent.prefix(8))).localized
            //: let attributes: [NSAttributedString.Key: Any] = [
            let attributes: [NSAttributedString.Key: Any] = [
                //: NSAttributedString.Key.font: UIFont.pingfangFont(type: .Regular, fontSize: 15),
                NSAttributedString.Key.font: UIFont.recipeDecision(type: .Regular, fontSize: 15),
                //: NSAttributedString.Key.foregroundColor: UIColor.appValueDetailColor()
                NSAttributedString.Key.foregroundColor: UIColor.keepRid(),
            ]
            //: msgShowLabel.attributedText = NSAttributedString(string: str, attributes: attributes)
            msgShowLabel.attributedText = NSAttributedString(string: str, attributes: attributes)
            //: return
            return
        }
        //: if model.listShowMessage != nil {
        if model.listShowMessage != nil {
            //: msgShowLabel.attributedText = TalkingChatListTableCell.handleShowC2CDetailTextOfMessage(model: model)
            msgShowLabel.attributedText = IonViewCell.slipUp(model: model)
            //: } else if model.chatType == .group {
        } else if model.chatType == .group { // 公共聊天室入口
            //: let str = "%@ Online".localizedArguments(model.num)
            let str = (String(dataBoardContent.suffix(4)) + "nline").extraPropertyLetter(model.num)
            //: let attributes: [NSAttributedString.Key: Any] = [
            let attributes: [NSAttributedString.Key: Any] = [
                //: NSAttributedString.Key.font: UIFont.pingfangFont(type: .Regular, fontSize: 15),
                NSAttributedString.Key.font: UIFont.recipeDecision(type: .Regular, fontSize: 15),
                //: NSAttributedString.Key.foregroundColor: UIColor.appValueDetailColor()
                NSAttributedString.Key.foregroundColor: UIColor.keepRid(),
            ]
            //: msgShowLabel.attributedText = NSAttributedString(string: str, attributes: attributes)
            msgShowLabel.attributedText = NSAttributedString(string: str, attributes: attributes)

            //: } else if model.chatType == .moreMsg {
        } else if model.chatType == .moreMsg { // 更多消息入口，无数据时，占位文案
            //: let str = "Messages not been replied here".localized
            let str = (String(const_goodMsg.prefix(6)) + "es n" + String(showConveyKey.prefix(4)) + "een " + const_entryMessage.replacingOccurrences(of: "better", with: "p") + "ied " + main_adUrl.replacingOccurrences(of: "screen", with: "e")).localized
            //: let attributes: [NSAttributedString.Key: Any] = [
            let attributes: [NSAttributedString.Key: Any] = [
                //: NSAttributedString.Key.font: UIFont.pingfangFont(type: .Regular, fontSize: 15),
                NSAttributedString.Key.font: UIFont.recipeDecision(type: .Regular, fontSize: 15),
                //: NSAttributedString.Key.foregroundColor: UIColor.appValueDetailColor()
                NSAttributedString.Key.foregroundColor: UIColor.keepRid(),
            ]
            //: msgShowLabel.attributedText = NSAttributedString(string: str, attributes: attributes)
            msgShowLabel.attributedText = NSAttributedString(string: str, attributes: attributes)

            //: } else {
        } else { // 草稿箱
            //: var attrStr: NSMutableAttributedString?
            var attrStr: NSMutableAttributedString?
            //: if !model.draftText.isEmptyString {
            if !model.draftText.isEmptyString {
                //: attrStr = TalkingChatListTableCell.getDraftString(draft: &model.draftText)
                attrStr = IonViewCell.textQuantity(draft: &model.draftText)
            }
            //: attrStr?.addAttribute(.font, value: UIFont.pingfangFont(type: .Regular, fontSize: 15), range: NSRange(location: 0, length: attrStr?.length ?? 0))
            attrStr?.addAttribute(.font, value: UIFont.recipeDecision(type: .Regular, fontSize: 15), range: NSRange(location: 0, length: attrStr?.length ?? 0))
            //: msgShowLabel.attributedText = attrStr
            msgShowLabel.attributedText = attrStr
        }
    }
}

// MARK: - UI

//: extension TalkingChatListTableCell {
extension IonViewCell {
    //: private func createCellUI() {
    private func crossStreet() {
        //: contentView.addSubview(headImgView)
        contentView.addSubview(headImgView)
        //: contentView.addSubview(iconBorder)
        contentView.addSubview(iconBorder)
        //: contentView.addSubview(onlineStatus)
        contentView.addSubview(onlineStatus)
        //: contentView.addSubview(badgeLab)
        contentView.addSubview(badgeLab)
        //: contentView.addSubview(nickNameLabel)
        contentView.addSubview(nickNameLabel)
        //: contentView.addSubview(loungeImgV)
        contentView.addSubview(loungeImgV)
        //: contentView.addSubview(authImgV)
        contentView.addSubview(authImgV)
        //: contentView.addSubview(newImgV)
        contentView.addSubview(newImgV)
        //: contentView.addSubview(officialImgV)
        contentView.addSubview(officialImgV)
        //: contentView.addSubview(msgShowLabel)
        contentView.addSubview(msgShowLabel)
        //: contentView.addSubview(timeLab)
        contentView.addSubview(timeLab)
        //: contentView.addSubview(imtimateBtn)
        contentView.addSubview(imtimateBtn)
    }

    //: private func createCellUIConstraints() {
    private func post() {
        //: headImgView.snp.makeConstraints { make in
        headImgView.snp.makeConstraints { make in
            //: make.centerY.equalToSuperview()
            make.centerY.equalToSuperview()
            //: make.leading.equalTo(15)
            make.leading.equalTo(15)
            //: make.width.height.equalTo(60)
            make.width.height.equalTo(60)
        }
        //: iconBorder.snp.makeConstraints { make in
        iconBorder.snp.makeConstraints { make in
            //: make.centerY.equalToSuperview()
            make.centerY.equalToSuperview()
            //: make.leading.equalTo(10)
            make.leading.equalTo(10)
            //: make.width.height.equalTo(69.5)
            make.width.height.equalTo(69.5)
        }
        //: onlineStatus.snp.makeConstraints { make in
        onlineStatus.snp.makeConstraints { make in
            //: make.trailing.equalTo(headImgView.snp.trailing).offset(-3)
            make.trailing.equalTo(headImgView.snp.trailing).offset(-3)
            //: make.bottom.equalTo(headImgView.snp.bottom).offset(-3)
            make.bottom.equalTo(headImgView.snp.bottom).offset(-3)
            //: make.width.height.equalTo(10)
            make.width.height.equalTo(10)
        }
        //: badgeLab.snp.makeConstraints { make in
        badgeLab.snp.makeConstraints { make in
            //: make.top.equalTo(self.headImgView.snp.top).offset(-5)
            make.top.equalTo(self.headImgView.snp.top).offset(-5)
            //: make.trailing.equalTo(self.headImgView.snp.trailing).offset(6)
            make.trailing.equalTo(self.headImgView.snp.trailing).offset(6)
            //: make.height.equalTo(20)
            make.height.equalTo(20)
            //: make.width.greaterThanOrEqualTo(20)
            make.width.greaterThanOrEqualTo(20)
        }
        //: nickNameLabel.snp.makeConstraints { make in
        nickNameLabel.snp.makeConstraints { make in
            //: make.top.equalTo(headImgView).offset(6)
            make.top.equalTo(headImgView).offset(6)
            //: make.leading.equalTo(headImgView.snp.trailing).offset(10)
            make.leading.equalTo(headImgView.snp.trailing).offset(10)
            //: make.trailing.lessThanOrEqualToSuperview().offset(-40)
            make.trailing.lessThanOrEqualToSuperview().offset(-40)
            //: make.height.equalTo(21)
            make.height.equalTo(21)
        }
        //: loungeImgV.snp.makeConstraints { make in
        loungeImgV.snp.makeConstraints { make in
            //: make.centerY.equalTo(nickNameLabel)
            make.centerY.equalTo(nickNameLabel)
            //: make.leading.equalTo(nickNameLabel.snp.trailing).offset(4)
            make.leading.equalTo(nickNameLabel.snp.trailing).offset(4)
            //: make.height.width.equalTo(20)
            make.height.width.equalTo(20)
        }
        //: authImgV.snp.makeConstraints { make in
        authImgV.snp.makeConstraints { make in
            //: make.centerY.equalTo(nickNameLabel)
            make.centerY.equalTo(nickNameLabel)
            //: make.leading.equalTo(loungeImgV.snp.trailing).offset(4)
            make.leading.equalTo(loungeImgV.snp.trailing).offset(4)
            //: make.width.equalTo(16)
            make.width.equalTo(16)
            //: make.height.equalTo(16)
            make.height.equalTo(16)
        }
        //: newImgV.snp.makeConstraints { make in
        newImgV.snp.makeConstraints { make in
            //: make.centerY.equalTo(nickNameLabel)
            make.centerY.equalTo(nickNameLabel)
            //: make.leading.equalTo(authImgV.snp.trailing).offset(4)
            make.leading.equalTo(authImgV.snp.trailing).offset(4)
            //: make.width.equalTo(26)
            make.width.equalTo(26)
            //: if LanguageManager.shared.direction == .rightToLeft {
            if TaLanguageManager.shared.direction == .rightToLeft {
                //: make.height.equalTo(16)
                make.height.equalTo(16)
                //: } else {
            } else {
                //: make.height.equalTo(14)
                make.height.equalTo(14)
            }
        }
        //: officialImgV.snp.makeConstraints { make in
        officialImgV.snp.makeConstraints { make in
            //: make.centerY.equalTo(nickNameLabel)
            make.centerY.equalTo(nickNameLabel)
            //: make.leading.equalTo(newImgV.snp.trailing).offset(4)
            make.leading.equalTo(newImgV.snp.trailing).offset(4)
            //: make.width.greaterThanOrEqualTo(50)
            make.width.greaterThanOrEqualTo(50)
            //: make.height.equalTo(16)
            make.height.equalTo(16)
        }
        //: timeLab.snp.makeConstraints { make in
        timeLab.snp.makeConstraints { make in
            //: make.centerY.equalTo(nickNameLabel)
            make.centerY.equalTo(nickNameLabel)
            //: make.trailing.equalToSuperview().offset(-15)
            make.trailing.equalToSuperview().offset(-15)
        }
        //: imtimateBtn.snp.makeConstraints { make in
        imtimateBtn.snp.makeConstraints { make in
            //: make.centerY.equalTo(msgShowLabel)
            make.centerY.equalTo(msgShowLabel)
            //: make.width .equalTo(50)
            make.width.equalTo(50)
            //: make.trailing.equalToSuperview().offset(-15)
            make.trailing.equalToSuperview().offset(-15)
        }
        //: msgShowLabel.snp.makeConstraints { make in
        msgShowLabel.snp.makeConstraints { make in
            //: make.bottom.equalTo(headImgView.snp.bottom).offset(-6)
            make.bottom.equalTo(headImgView.snp.bottom).offset(-6)
            //: make.leading.equalTo(nickNameLabel)
            make.leading.equalTo(nickNameLabel)
            //: make.trailing.equalTo(imtimateBtn.snp.leading).offset(-10)
            make.trailing.equalTo(imtimateBtn.snp.leading).offset(-10)
        }
    }
}
