
//: Declare String Begin

/*: "SOCKET_ADDR_KEY" :*/
fileprivate let k_completeName:[Character] = ["S","O","C","K","E","T","_","A","D"]
fileprivate let appRestrictionValue:String = "DR_KEYindigenous generate address delivery"

/*: "SOCKET_VIDEOCHAT_REQUESTCALL_NOTIFICATION" :*/
fileprivate let main_fenceKey:[UInt8] = [0x3c,0x20,0x2c,0x24,0x2a,0x3b,0x30,0x39,0x26,0x2b,0x2a,0x20,0x2c,0x27,0x2e,0x3b,0x30,0x3d,0x2a,0x3e,0x3a,0x2a,0x3c,0x3b,0x2c,0x2e,0x23,0x23,0x30,0x21,0x20,0x3b,0x26,0x29,0x26,0x2c,0x2e,0x3b,0x26,0x20,0x21]

private func boundLose(owner num: UInt8) -> UInt8 {
    return num ^ 111
}

/*: "RECIVE_FLOATSCREEN_NOTIFICATION" :*/
fileprivate let const_haveMsg:[UInt8] = [0x1c,0xb,0xd,0x7,0x18,0xb,0x11,0x8,0x2,0x1,0xf,0x1a,0x1d,0xd,0x1c,0xb,0xb,0x0,0x11,0x0,0x1,0x1a,0x7,0x8,0x7,0xd,0xf,0x1a,0x7,0x1,0x0]

private func coreGroupEntitle(popular num: UInt8) -> UInt8 {
    return num ^ 78
}

/*: "SOCKET 连接成功." :*/
fileprivate let notiHeapLowerAccessData:String = "frontOCKE"
fileprivate let const_failMessage:String = "t"
fileprivate let kWithoutMessage:String = " 连接成功."

/*: "SOCKET 连接失败, error: :*/
fileprivate let const_interestBodyFormat:[Character] = ["S","O","C"]
fileprivate let kStatId:[Character] = ["K","E","T"," ","连","接","\u{5931}","败",","," ","e","r","r","o","r",":"]

/*: "SOCKET 失败 :*/
fileprivate let userResignDiskName:[Character] = ["S","O","C","K","E"]
fileprivate let userShapeCommonValue:String = "side extentT 失败"

/*: 次, 切换过Socket域名重新尝试，地址： :*/
fileprivate let appSpendData:String = "次, 切换\u{8fc7}"
fileprivate let show_fragmentPath:String = "t域\u{540d}重新"
fileprivate let kStartTitle:String = "尝试\u{ff0c}地址："

/*: "SOCKET 已关闭, code: :*/
fileprivate let show_textMessage:[Character] = ["S","O","C","K","E","T"," ","已","关","闭"]
fileprivate let notiTeemColumnKey:[Character] = [","," ","c","o","d","e",":"]

/*: , reason: :*/
fileprivate let app_multipleData:String = ", reacollection bolt inventory premium recipe"

/*: /?sid= :*/
fileprivate let k_alreadyName:String = "/?sid=stretch wed image origin"

/*: &uid= :*/
fileprivate let showGuessYouMsg:String = "&uid=challenge elect"

/*: &platform=iphone&version= :*/
fileprivate let notiPlantPath:String = "&plaodd available lack modify warning"
fileprivate let user_barName:String = "ipmilitaryon"
fileprivate let dataRestrictionPath:String = "ion=import storage select"

/*: &packageId= :*/
fileprivate let data_sameMessage:String = "&pacfirst bean date"
fileprivate let show_disappearUrbanApproximatelyTitle:[Character] = ["="]

/*: "1v1server. :*/
fileprivate let user_inkPickHeavyTitle:[Character] = ["1","v","1","s","e","r","v"]
fileprivate let k_relieveSecureMigrationMsg:[Character] = ["e","r","."]

/*: .com:7766" :*/
fileprivate let noti_randomId:String = "particular consider crop.com"

/*: "43.129.185.194:7767" :*/
fileprivate let kLeadingKey:String = "43.129."
fileprivate let noti_laterGoingStr:String = "phase85.phase"
fileprivate let appYesterdayPairPath:String = "94:7767"

/*: "SOCKET 网络reachabilityChanged监听，断网关闭." :*/
fileprivate let showMedicPingProgressPath:[UInt8] = [0x30,0x2c,0x20,0x28,0x26,0x37,0x43,0x84,0xde,0xf2,0x84,0xd8,0xff,0x11,0x6,0x2,0x0,0xb,0x2,0x1,0xa,0xf,0xa,0x17,0x1a,0x20,0xb,0x2,0xd,0x4,0x6,0x7,0x84,0xf8,0xf2,0x86,0xf3,0xcf,0x8c,0xdf,0xef,0x85,0xf5,0xce,0x84,0xde,0xf2,0x86,0xe6,0xd0,0x8a,0xf4,0xce,0x4d]

private func menuResumeImage(activity num: UInt8) -> UInt8 {
    return num ^ 99
}

/*: "cmd" :*/
fileprivate let appAdviceSkipValue:[Character] = ["c","m","d"]

/*: "ping" :*/
fileprivate let dataBothTitle:String = "broadcasting"

/*: "callHeartbeat" :*/
fileprivate let dataReplyMsg:[Character] = ["c","a","l","l","H","e","a","r","t","b","e","a","t"]

/*: "recievedDate" :*/
fileprivate let dataDealUrl:String = "RECI"
fileprivate let mainCutStr:[Character] = ["D","a","t","e"]

/*: "liveHeartBeat" :*/
fileprivate let user_readyValue:String = "whole"
fileprivate let constEnoughMessage:String = "strip submitveHear"

/*: "🔗 关闭 socket" :*/
fileprivate let dataDatabaseUrl:String = "properly"
fileprivate let userEstimatedTitle:[Character] = [" ","关","闭"," ","s","o","c","k","e","t"]

/*: "The user logs out" :*/
fileprivate let k_levelToolPath:String = "commercial beyondThe user"
fileprivate let showTillStr:[Character] = [" ","l","o","g","s"," ","o","u","t"]

/*: "SOCKET  :*/
fileprivate let userPersonName:[Character] = ["S","O","C","K","E","T"," "]

/*: 重连. 重试次数： :*/
fileprivate let kReceptionDoingstMapMessage:String = "重连. "
fileprivate let dataCoordinatorText:String = "\u{ff1a}"

/*: "SOCKET 连接正常 不需要重新连接." :*/
fileprivate let mainScoreFormat:String = "SOCKET sort bolt she nothing suspend"
fileprivate let noti_screenStr:String = "连接正\u{5e38} \u{4e0d}"
fileprivate let dataCurPath:String = "需要重新\u{8fde}接."

/*: "SOCKET 正在连接 不需要重新连接." :*/
fileprivate let showSampleMaximumMessage:String = "flash link mic coverSOCKET "
fileprivate let show_fireMessage:String = "\u{6b63}在连接 不"

/*: "&resume=1" :*/
fileprivate let const_expansionValue:String = "&resuretirement ingredient filter"
fileprivate let dataTireOwnName:[Character] = ["m","e","=","1"]

/*: "&lang= :*/
fileprivate let app_constantSaveHalfUrl:[Character] = ["&"]
fileprivate let show_satisfyMessage:[Character] = ["l","a","n","g","="]

/*: "SOCKET 开始连接, 地址： :*/
fileprivate let kStorageData:[Character] = ["S","O","C","K","E","T"," ","开","始","\u{8fde}","接",","," ","\u{5730}","址","："]

/*: "onNetDisabled" :*/
fileprivate let dataSheKey:[Character] = ["o","n","N","e","t","D","i","s","a","b","l","e","d"]

/*: "errno" :*/
fileprivate let appExtraValue:[Character] = ["e","r","r","n","o"]

/*: "data" :*/
fileprivate let show_timeKey:String = "dbrushtbrush"

/*: "SOCKET 连接保活cmd: ping." :*/
fileprivate let mainNoteFormat:[Character] = ["S","O","C","K","E","T"," ","连","接","保","活"]
fileprivate let main_termsKey:[Character] = ["c","m","d",":"," ","p","i","n","g","."]

/*: "SOCKET trtc保活cmd: callHeartbeat." :*/
fileprivate let appDirectName:[UInt8] = [0x7b,0x77,0x6b,0x73,0x6d,0x7c,0x48,0x9c,0x9a,0x9c,0x8b,0xc,0xe7,0xc5,0xe,0xdc,0xe3,0x8b,0x95,0x8c,0x62,0x48,0x8b,0x89,0x94,0x94,0x70,0x8d,0x89,0x9a,0x9c,0x8a,0x8d,0x89,0x9c,0x56]

fileprivate func layerAdmin(announcement num: UInt8) -> UInt8 {
    let value = Int(num) - 40
    if value < 0 {
        return UInt8(value + 256)
    } else {
        return UInt8(value)
    }
}

/*: "SOCKET live保活cmd: liveHeartBeat." :*/
fileprivate let appAssetBreadId:[UInt8] = [0x1e,0x2,0xe,0x6,0x8,0x19,0x6d,0x21,0x24,0x3b,0x28,0xa9,0xf2,0xd0,0xab,0xf9,0xf6,0x2e,0x20,0x29,0x77,0x6d,0x21,0x24,0x3b,0x28,0x5,0x28,0x2c,0x3f,0x39,0xf,0x28,0x2c,0x39,0x63]

private func popProvide(sort num: UInt8) -> UInt8 {
    return num ^ 77
}

/*: "onP" :*/
fileprivate let const_timingStockName:[Character] = ["o","n","P"]

/*: "msg" :*/
fileprivate let dataGinRunningMsg:String = "mssolution"

/*: "🔗 处理消息：cmdName: :*/
fileprivate let const_bounceTitle:[Character] = ["🔗"," ","处","理","消","\u{606f}","：","c","m","d","N","a","m","e",":"]

/*: , errNo: :*/
fileprivate let data_meltSharedName:String = ", errNup his previous reader inventory"
fileprivate let data_complexId:[Character] = ["o",":"]

/*: , msg: :*/
fileprivate let dataRestoreValue:[Character] = [","," ","m","s","g"]
fileprivate let notiDragUrl:[Character] = [":"]

/*: , data: :*/
fileprivate let user_perspectiveMessage:[Character] = [","," ","d","a","t","a"]
fileprivate let const_feedbackData:String = "meet"

/*: "SOCKET 接收异常消息：cmdName: :*/
fileprivate let appHundredTitle:String = "rewardCKE"
fileprivate let showTableMiddlePath:String = "\u{5e38}消\u{606f}："
fileprivate let const_cantId:String = "cmdName:curve someone"

/*: "onReceivedGift" :*/
fileprivate let app_cleanMsg:String = "onRecstyle image merge"
fileprivate let show_parentData:String = "disabled information anima calculateeivedGift"

/*: "onVideoMatch" :*/
fileprivate let constPocketBoundName:[Character] = ["o","n","V","i","d","e","o","M"]
fileprivate let userChannelName:String = "ATCH"

/*: "onRequestCall" :*/
fileprivate let const_blendExplainForceKey:String = "need bear earlyonReq"
fileprivate let noti_globName:[Character] = ["a","l","l"]

/*: "onCancelCall" :*/
fileprivate let notiCreateId:String = "vital traffic carpetonCa"
fileprivate let kMiniValue:String = "NCEL"

/*: "onRefuseCall" :*/
fileprivate let appMakeEnvironmentTitle:String = "creation sibling sufficient programonRef"

/*: "onAcceptCall" :*/
fileprivate let constIncentiveMessage:[Character] = ["o","n","A","c","c","e","p"]
fileprivate let mainLoadName:[Character] = ["t","C","a","l","l"]

/*: "onStopCall" :*/
fileprivate let k_tickStr:String = "voicen"
fileprivate let dataSuspendKey:String = "name certain migrationStopCall"

/*: "freeCallTimes" :*/
fileprivate let appServerName:String = "fliterale"
fileprivate let userPassUrl:String = "impanels"

/*: "mfCoin" :*/
fileprivate let user_sweepName:[Character] = ["m","f","C","o","i","n"]

/*: "onMissCoin" :*/
fileprivate let dataClotMessage:String = "recent regiononMis"
fileprivate let data_successfulMsg:String = "sCoinletter trade coordinator"

/*: "acceptCall" :*/
fileprivate let main_withoutTellName:String = "accprogressp"
fileprivate let noti_advicePath:String = "family superiortCall"

/*: "stopCall" :*/
fileprivate let main_rugSweepId:String = "stscorep"

/*: "requestCall" :*/
fileprivate let showSegmentPath:String = "rburnquburn"
fileprivate let notiTunMsg:[Character] = ["s","t","C","a","l","l"]

/*: "[Invite you to a video call...]" :*/
fileprivate let notiDeskId:[UInt8] = [0xf6,0xe4,0xc3,0xdb,0xc4,0xd9,0xc8,0x8d,0xd4,0xc2,0xd8,0x8d,0xd9,0xc2,0x8d,0xcc,0x8d,0xdb,0xc4,0xc9,0xc8,0xc2,0x8d,0xce,0xcc,0xc1,0xc1,0x83,0x83,0x83,0xf0]

/*: "type" :*/
fileprivate let noti_commonName:String = "tyresolutione"

/*: "[Invite you to a voice call...]" :*/
fileprivate let constRemindAgainContent:[UInt8] = [0x30,0x22,0x5,0x1d,0x2,0x1f,0xe,0x4b,0x12,0x4,0x1e,0x4b,0x1f,0x4,0x4b,0xa,0x4b,0x1d,0x4,0x2,0x8,0xe,0x4b,0x8,0xa,0x7,0x7,0x45,0x45,0x45,0x36]

/*: "uid" :*/
fileprivate let noti_accordingUrl:[Character] = ["u","i","d"]

/*: "nickname" :*/
fileprivate let app_tagText:[Character] = ["n","i","c","k","n","a","m","e"]

/*: "headPic" :*/
fileprivate let noti_flexibleKey:[Character] = ["h","e","a","d","P","i","c"]

/*: "onCallResume" :*/
fileprivate let notiBreastStr:[Character] = ["o","n","C","a","l","l","R","e"]
fileprivate let userProcedurePath:String = "sucommende"

/*: "cancelCall" :*/
fileprivate let show_themeLibraryFormat:[Character] = ["c","a","n","c","e","l"]
fileprivate let user_wordItValue:String = "Callingredient clip female us quantity"

/*: "refuseCall" :*/
fileprivate let show_seeUrl:String = "feedbackfus"
fileprivate let user_heritageData:String = "nor"

/*: "onSendProgramGift" :*/
fileprivate let const_bookTitle:String = "onSenstrong complete opportunity follow square"
fileprivate let dataDecideFormat:String = "native m crop op generaldProg"

/*: "onGameFs" :*/
fileprivate let showWithStr:[Character] = ["o","n","G","a","m","e","F"]
fileprivate let showEquallyProvideUrl:[Character] = ["s"]

/*: "broadStatus" :*/
fileprivate let showLockMsg:String = "involvement shirtbroadSta"
fileprivate let user_readingKey:String = "tselect"

/*: "onCallChatMsg" :*/
fileprivate let showLowStr:String = "shrink contrast circleonCal"

/*: "onUserBlock" :*/
fileprivate let constConvertPath:String = "onUspic press maximum portrait"
fileprivate let showFireReportedTitle:String = "network normal checkerBlock"

/*: "isRecharge" :*/
fileprivate let notiLogicalName:String = "reject"
fileprivate let notiLiterTitle:String = "stand father minuteRecharge"

/*: "source" :*/
fileprivate let show_farReceiveStr:[Character] = ["s","o","u","r","c","e"]

/*: "invitePop" :*/
fileprivate let notiPureMessage:String = "form applyinvitePop"

/*: "videoPop" :*/
fileprivate let const_shallMessage:String = "videoPopagenda degree track message"

/*: "coin" :*/
fileprivate let dataDailyRefreshUrl:String = "cmethodn"

//: Declare String End

// __DEBUG__
// __CLOSE_PRINT__
//
//  AlsoThen.swift
//  AbroadTalking
//
//  Created by young on 2022/9/13.
//

//: import SocketRocket
import SocketRocket
//: import UIKit
import UIKit

// 保活心跳包ping的时间间隔
//: let SOCKET_PING_INTERVAL = 60.0
let show_outputText = 60.0
// 视频通话心跳包时间间隔
//: let SOCKET_CALL_INTERVAL = 20.0
let kAgeTitle = 20.0
// 直播心跳包时间间隔
//: let SOCKET_LIVE_INTERVAL = 20.0
let notiCollectionMsg = 20.0
// 失败重试次数
//: let SOCKET_ADDR_RETRY_COUNT = 3
let noti_logFormat = 3
// socket连接超时时间
//: let SOCKET_MANAGER_CONNECT_TIMEOUT = 30.0
let noti_cornerFormat = 30.0
// socket连接成功缓存key
//: let SOCKET_ADDR_KEY = "SOCKET_ADDR_KEY"
let const_contentSystemPath = (String(k_completeName) + String(appRestrictionValue.prefix(6)))

// 错误码
//: let SOCKET_MANAGER_UNCONNECTED_CODE = 5001
let main_okStr = 5001
//: let SOCKET_MANAGER_SELF_BUSY_CODE   = 5002
let dataSendMsg = 5002
//: let SOCKET_WITHOUT_NET_CODE         = 5003
let dataGreetId = 5003
//: let SOCKET_MANAGER_USER_LOGOUT_CLOSED_CODE = 1002
let data_giftPath = 1002

// 接收方收到音视频通话通知
//: public let SOCKET_VIDEOCHAT_REQUESTCALL_NOTIFICATION = NSNotification.Name(rawValue: "SOCKET_VIDEOCHAT_REQUESTCALL_NOTIFICATION")
public let user_styleMethodMsg = NSNotification.Name(rawValue: String(bytes: main_fenceKey.map{boundLose(owner: $0)}, encoding: .utf8)!)
// 游戏飘屏通知
//: public let RECIVE_FLOATSCREEN_NOTIFICATION = NSNotification.Name(rawValue: "RECIVE_FLOATSCREEN_NOTIFICATION")
public let show_lessKey = NSNotification.Name(rawValue: String(bytes: const_haveMsg.map{coreGroupEntitle(popular: $0)}, encoding: .utf8)!)

// MARK: - protocol

// MARK: - socket失败代理

/// socket收到失败消息
//: protocol TalkingSocketManagerErrorDelegate: NSObjectProtocol {
protocol FriendshipErrorDelegate: NSObjectProtocol {
    /// socket收到失败消息
    /// - Parameters:
    ///   - errorNo: 失败编码
    ///   - cmd: 命令
    ///   - msg: 失败信息
    ///  - data: 数据
    //: func socket_didRecieveError(errorNo: Int, cmd: String, msg: String, data: [String: Any])
    func data(errorNo: Int, cmd: String, msg: String, data: [String: Any])
}

// MARK: - 视频速配代理

/// 视频速配代理（男）
//: protocol SocketManagerVideoMatchDelegate: NSObjectProtocol {
protocol BearMatchDelegate: NSObjectProtocol {
    /// 视频速配成功
    /// - Parameter data: 速配数据
    //: func socket_match_onVideoMatch(data: [String: Any])
    func standard(data: [String: Any])

    /// 匹配成功，可以拨打通话
    /// - Parameter data: 速配数据
    //: func socket_match_onRequestCall(data: [String: Any])
    func widespreadData(data: [String: Any])
}

// MARK: - 音视频通话代理

/// 视频通话【拨打方】代理
//: protocol SocketManagerVideoChatCallerDelegate: NSObjectProtocol {
protocol ConfirmCallerDelegate: NSObjectProtocol {
    /// 接收方接受了来电
    /// - Parameter data: 数据
    //: func socket_videoChat_acceptCall(data: [String: Any])
    func curve(data: [String: Any])

    /// 接收方挂断了通话（通话中）
    /// - Parameter data: 数据
    //: func socket_videoChat_stopCall(data: [String: Any])
    func wrapData(data: [String: Any])

    /// 拨打方拒绝了通话，接收方收到该指令（未通话）
    /// - Parameter data: 数据
    //: func socket_videoChat_cancelCall(data: [String: Any])
    func delineateBean(data: [String: Any])

    /// 通话余额不足提醒
    /// - Parameters:
    ///   - msg: 提醒信息
    ///   - data: 数据
    //: func socket_videoChat_onMissCoin(msg: String, data: [String: Any])
    func noKit(msg: String, data: [String: Any])
}

/// 视频通话【接收方】代理
//: @objc protocol SocketManagerVideoChatCalledPartyDelegate: NSObjectProtocol {
@objc protocol SeparatePartyDelegate: NSObjectProtocol {
    /// 接收方拒绝了通话，拨打方收到该指令（未通话）
    /// - Parameter data: 数据
    //: func socket_videoChat_refuseCall(data: [String: Any])
    func countCity(data: [String: Any])

    /// 接收方接受了通话，改变状态为通话中，acceptCall回执
    /// - Parameter data: 数据
    //: func socket_videoChat_onAcceptCall(data: [String: Any])
    func starHideSort(data: [String: Any])
}

//: class TalkingSocketManager: NSObject {
class AlsoThen: NSObject {
    //: enum SocketRetryType: String {
    enum BeJump: String {
        //: case AppLogin = "用户登录"
        case AppLogin = "用户登录"
        //: case NetWork = "网络重连"
        case NetWork = "网络重连"
        //: case EnterForeground = "回到APP"
        case EnterForeground = "回到APP"
        //: case SocketFail = "连接失败"
        case SocketFail = "连接失败"
        //: case SocketClose = "连接关闭"
        case SocketClose = "连接关闭"
        //: case SocketSendMsg = "发送消息失败"
        case SocketSendMsg = "发送消息失败"
    }

    /// 音视频最小化
    //: var currGeneralVC: AnyObject?
    var currGeneralVC: AnyObject? // 当前正在通话的音视频视图
    //: var videoMiniPosition = CGPoint(
    var videoMiniPosition = CGPoint( // 音视频小窗口默认位置
        //: x: LanguageManager.shared.direction == .rightToLeft ? 0:ScreenWidth,
        x: TaLanguageManager.shared.direction == .rightToLeft ? 0 : main_assetUrl,
        //: y: StatusBarHeight+190
        y: show_textNextFormat + 190
    )
    //: var liveMiniPosition = CGPoint(
    var liveMiniPosition = CGPoint( // 直播小窗口默认位置
        //: x: LanguageManager.shared.direction == .rightToLeft ? 0:ScreenWidth,
        x: TaLanguageManager.shared.direction == .rightToLeft ? 0 : main_assetUrl,
        //: y: StatusBarHeight+190
        y: show_textNextFormat + 190
    )
    //: var containerView: UIView?
    var containerView: UIView? // 小窗口容器

    /// delegate
    //: weak var errorDelegate: TalkingSocketManagerErrorDelegate?
    weak var errorDelegate: FriendshipErrorDelegate? // error !=0 的异常处理
    //: weak var videoMatchDelegate: SocketManagerVideoMatchDelegate?
    weak var videoMatchDelegate: BearMatchDelegate? // 视频通话速配
    //: weak var callerDelegate: SocketManagerVideoChatCallerDelegate?
    weak var callerDelegate: ConfirmCallerDelegate? // 视频通话【拨打方】
    //: weak var calledPartyDelegate: SocketManagerVideoChatCalledPartyDelegate?
    weak var calledPartyDelegate: SeparatePartyDelegate? // 视频通话【接收方】

    //: private var webSocket: SRWebSocket?
    private var webSocket: SRWebSocket?
    //: private var keepAliveTimer: Timer?
    private var keepAliveTimer: Timer? // socket保活心跳包
    //: private var connectUrlStr: String?
    private var connectUrlStr: String? // socket连接地址
    //: private var linkIndex = 0
    private var linkIndex = 0 // 当前连接地址索引
    //: private var linkAddrArr: [String]?
    private var linkAddrArr: [String]? // 连接地址数组
    //: private var failureCount = 0
    private var failureCount = 0 // 连接失败次数
    //: private let disposeBag = DisposeBag()
    private let disposeBag = DisposeBag()

    /// talking
    //: var needReconnect = false
    var needReconnect = false // 断网重连socket
    
    @objc dynamic var isGeneral = false // 是否视频通话中
    //: @objc dynamic var isCalling = false
    @objc dynamic var isCalling = false // 是否呼叫中
    //: private var talkTimer: Timer?
    private var talkTimer: Timer? // 视频通话心跳包
    //: private var liveTimer: Timer?
    private var liveTimer: Timer? // 直播心跳包
    //: private var unHandleCallData: [String: Any]?
    private var unHandleCallData: [String: Any]? // 在后台收到的接听消息
    //: private var cancelCall = false
    private var cancelCall = false // 拨打方是否停止呼叫（还未打通）
    //: var isFreeCall = false
    var isFreeCall = false // 是否发起免费通话

    // singleton
    //: static let shared = TalkingSocketManager()
    static let shared = AlsoThen()
    //: override func copy() -> Any { return self }
    override func copy() -> Any { return self }
    //: override func mutableCopy() -> Any { return self }
    override func mutableCopy() -> Any { return self }
    //: private override init() {
    override private init() {
        //: super.init()
        super.init()
        // 网络状态监听
        //: NotificationCenter.default.addObserver(self,
        NotificationCenter.default.addObserver(self,
                                               //: selector: #selector(reachabilityChanged(note:)),
                                               selector: #selector(operations(note:)),
                                               //: name: .reachabilityChanged,
                                               name: .reachabilityChanged,
                                               //: object: nil)
                                               object: nil)

        //: NotificationCenter.default.addObserver(self,
        NotificationCenter.default.addObserver(self,
                                               //: selector: #selector(checkWebsocket),
                                               selector: #selector(generalWebsocket),
                                               //: name: UIApplication.willEnterForegroundNotification,
                                               name: UIApplication.willEnterForegroundNotification,
                                               //: object: nil)
                                               object: nil)
    }

    //: deinit {
    deinit {
        //: NotificationCenter.default.removeObserver(self)
        NotificationCenter.default.removeObserver(self)
    }
}

// MARK: - SRWebSocketDelegate

//: extension TalkingSocketManager: SRWebSocketDelegate {
extension AlsoThen: SRWebSocketDelegate {
    //: func webSocketDidOpen(_ webSocket: SRWebSocket!) {
    func webSocketDidOpen(_ webSocket: SRWebSocket!) {
        //: guard self.webSocket == webSocket else { return }
        guard self.webSocket == webSocket else { return }
        //: UploadLogTool.writeLog(msg: "SOCKET 连接成功.")
        MigrationThen.pathAge(msg: (notiHeapLowerAccessData.replacingOccurrences(of: "front", with: "S") + const_failMessage.uppercased() + kWithoutMessage.capitalized))
        //: sendPing()
        form()
        //: NotificationCenter.default.post(name: SOCKET_IS_OPEN_NOTIFICATION,
        NotificationCenter.default.post(name: k_tableStr,
                                        //: object: nil,
                                        object: nil,
                                        //: userInfo: nil)
                                        userInfo: nil)
        // 还原断网重连状态
        //: self.needReconnect = false
        self.needReconnect = false

        //: self.failureCount = 0
        self.failureCount = 0

        // 保存成功连接的记录
        //: if self.linkIndex < self.linkAddrArr?.count ?? 0 {
        if self.linkIndex < self.linkAddrArr?.count ?? 0 {
            //: UserDefaults.standard.set(self.linkAddrArr?[self.linkIndex], forKey: SOCKET_ADDR_KEY)
            UserDefaults.standard.set(self.linkAddrArr?[self.linkIndex], forKey: const_contentSystemPath)
        }
        // 创建音视频最小化窗口容器
        //: mini_createContainerView()
        hiView()
    }

    //: func webSocket(_ webSocket: SRWebSocket!, didFailWithError error: Error!) {
    func webSocket(_ webSocket: SRWebSocket!, didFailWithError error: Error!) {
        //: UploadLogTool.writeLog(msg: "SOCKET 连接失败, error:\(error.localizedDescription).")
        MigrationThen.pathAge(msg: (String(const_interestBodyFormat) + String(kStatId)) + "\(error.localizedDescription).")
        //: guard self.webSocket == webSocket else { return }
        guard self.webSocket == webSocket else { return }

        //: guard let status = FacultyReactiveCompatible.share.reachability?.connection, status != .unavailable else {
        guard let status = FacultyReactiveCompatible.share.reachability?.connection, status != .unavailable else {
            //: return
            return
        }
        //: if self.failureCount == SOCKET_ADDR_RETRY_COUNT {
        if self.failureCount == noti_logFormat {
            //: self.failureCount = 0
            self.failureCount = 0
            //: self.linkIndex = (self.linkIndex == 0) ? 1:0
            self.linkIndex = (self.linkIndex == 0) ? 1 : 0
            //: self.connectUrlStr = getWebSocketPath(isRetry: true)
            self.connectUrlStr = beVia(isRetry: true)
            //: UploadLogTool.writeLog(msg: "SOCKET 失败\(SOCKET_ADDR_RETRY_COUNT)次, 切换过Socket域名重新尝试，地址：\(connectUrlStr ?? "").")
            MigrationThen.pathAge(msg: (String(userResignDiskName) + String(userShapeCommonValue.suffix(4))) + "\(noti_logFormat)" + (appSpendData + "Socke" + show_fragmentPath + kStartTitle) + "\(connectUrlStr ?? "").")
            //: } else {
        } else {
            //: self.failureCount += 1
            self.failureCount += 1
        }
        //: self.reconnectWebSocket(timeInterval: 2, type: .SocketFail)
        self.option(timeInterval: 2, type: .SocketFail)
    }

    //: func webSocket(_ webSocket: SRWebSocket!, didReceiveMessage message: Any!) {
    func webSocket(_ webSocket: SRWebSocket!, didReceiveMessage message: Any!) {
        //: guard self.webSocket == webSocket else { return }
        guard self.webSocket == webSocket else { return }
        //: self.handleReceiveMessage(message: message as? String)
        self.supreme(message: message as? String)
    }

    //: func webSocket(_ webSocket: SRWebSocket!, didCloseWithCode code: Int, reason: String!, wasClean: Bool) {
    func webSocket(_ webSocket: SRWebSocket!, didCloseWithCode code: Int, reason: String!, wasClean _: Bool) {
        //: UploadLogTool.writeLog(msg: "SOCKET 已关闭, code:\(code), reason:\(String(describing: reason)).")
        MigrationThen.pathAge(msg: (String(show_textMessage) + String(notiTeemColumnKey)) + "\(code)" + (String(app_multipleData.prefix(5)) + "son:") + "\(String(describing: reason)).")
        //: guard self.webSocket == webSocket else { return }
        guard self.webSocket == webSocket else { return }

        //: self.closeWebSocket(userLoginOut: false)
        self.photographicCamera(userLoginOut: false)
        //: if code == SOCKET_MANAGER_USER_LOGOUT_CLOSED_CODE {
        if code == data_giftPath {
            //: return
            return
        }
        //: if let status = FacultyReactiveCompatible.share.reachability?.connection, status != .unavailable {
        if let status = FacultyReactiveCompatible.share.reachability?.connection, status != .unavailable {
            //: self.reconnectWebSocket(timeInterval: 5, type: .SocketClose)
            self.option(timeInterval: 5, type: .SocketClose)
        }
    }

    /// 获取socket地址
    //: func getWebSocketPath(isRetry: Bool) -> String? {
    func beVia(isRetry: Bool) -> String? {
        //: var linkAddr = ""
        var linkAddr = ""
        //: self.linkAddrArr = FacultyReactiveCompatible.share.appConfigMode.ws
        self.linkAddrArr = FacultyReactiveCompatible.share.appConfigMode.ws
        //: if self.linkAddrArr == nil || self.linkAddrArr?.count == 0 {
        if self.linkAddrArr == nil || self.linkAddrArr?.count == 0 {
            //: self.linkAddrArr = defaultWSArr()
            self.linkAddrArr = actionArr()
        }
        //: let lastAddr = UserDefaults.standard.string(forKey: SOCKET_ADDR_KEY)
        let lastAddr = UserDefaults.standard.string(forKey: const_contentSystemPath)
        //: if lastAddr == nil || isRetry {
        if lastAddr == nil || isRetry {
            //: if let temLinkAddr = self.linkAddrArr?[safe: self.linkIndex] {
            if let temLinkAddr = self.linkAddrArr?[safe: self.linkIndex] {
                //: linkAddr = temLinkAddr
                linkAddr = temLinkAddr
                //: } else {
            } else {
                //: self.linkIndex = 0
                self.linkIndex = 0
                //: linkAddr = self.linkAddrArr!.first!
                linkAddr = self.linkAddrArr!.first!
            }
            //: } else {
        } else {
            //: if let index = self.linkAddrArr!.firstIndex(of: lastAddr!) {
            if let index = self.linkAddrArr!.firstIndex(of: lastAddr!) {
                //: self.linkIndex = index
                self.linkIndex = index
                //: linkAddr = lastAddr!
                linkAddr = lastAddr!
                //: }else{
            } else {
                //: self.linkIndex = 0
                self.linkIndex = 0
                //: linkAddr = self.linkAddrArr!.first!
                linkAddr = self.linkAddrArr!.first!
            }
        }

        //: let uid = FacultyReactiveCompatible.share.loginUid
        let uid = FacultyReactiveCompatible.share.loginUid
        //: let sid = FacultyReactiveCompatible.share.loginSessionId
        let sid = FacultyReactiveCompatible.share.loginSessionId
        //: let address = "ws://\(linkAddr)/?sid=\(sid)&uid=\(uid)&platform=iphone&version=\(AppNetVersion)&packageId=\(PackageID)"
        let address = "ws://\(linkAddr)" + (String(k_alreadyName.prefix(6))) + "\(sid)" + (String(showGuessYouMsg.prefix(5))) + "\(uid)" + (String(notiPlantPath.prefix(4)) + "tform=" + user_barName.replacingOccurrences(of: "military", with: "h") + "e&vers" + String(dataRestrictionPath.prefix(4))) + "\(data_serviceFormat)" + (String(data_sameMessage.prefix(4)) + "kageId" + String(show_disappearUrbanApproximatelyTitle)) + "\(showViewValue)"
        //: return address
        return address
    }

    /// 默认地址
    //: func defaultWSArr() -> [String] {
    func actionArr() -> [String] {
        //: return Environment_Formal ? ["1v1server.\(ReplaceUrlDomain).com:7766"] : ["43.129.185.194:7767"]
        return dataPopText ? [(String(user_inkPickHeavyTitle) + String(k_relieveSecureMigrationMsg)) + "\(app_frameName)" + (String(noti_randomId.suffix(4)) + ":7766")] : [(kLeadingKey.capitalized + noti_laterGoingStr.replacingOccurrences(of: "phase", with: "1") + appYesterdayPairPath.capitalized)]
    }
}

// MARK: - NotificationCenter

//: extension TalkingSocketManager {
extension AlsoThen {
    /// 网络监听通知
    //: @objc func reachabilityChanged(note: Notification) {
    @objc func operations(note: Notification) {
        //: let reachability = note.object as? Reachability
        let reachability = note.object as? Reachability
        //: if reachability?.connection == .unavailable {
        if reachability?.connection == .unavailable {
            // 断网, 关闭socket
            //: UploadLogTool.writeLog(msg: "SOCKET 网络reachabilityChanged监听，断网关闭.")
            MigrationThen.pathAge(msg: String(bytes: showMedicPingProgressPath.map{menuResumeImage(activity: $0)}, encoding: .utf8)!)
            //: self.closeWebSocket(userLoginOut: false)
            self.photographicCamera(userLoginOut: false)
            //: } else {
        } else {
            // 网络原因重连
            //: self.reconnectWebSocket(timeInterval: 2.0, type: .NetWork)
            self.option(timeInterval: 2.0, type: .NetWork)
        }
    }

    /// 检测socket链接状态
    //: @objc func checkWebsocket() {
    @objc func generalWebsocket() {
        //: self.reconnectWebSocket(timeInterval: 2.0, type: .EnterForeground)
        self.option(timeInterval: 2.0, type: .EnterForeground)
        // 处理通话逻辑
        //: self.handleCallData()
        self.accumulation()
    }
}

// MARK: - Event

//: extension TalkingSocketManager {
extension AlsoThen {
    /// socket保活心跳包
    //: private func sendPing() {
    private func form() {
        //: self.keepAliveTimer = Timer.scheduledTimer(withTimeInterval: SOCKET_PING_INTERVAL, block: { timer in
        self.keepAliveTimer = Timer.scheduledTimer(withTimeInterval: show_outputText, block: { _ in
            //: self.sendMessage(info: ["cmd": "ping"])
            self.attentionCut(info: [(String(appAdviceSkipValue)): (dataBothTitle.replacingOccurrences(of: "broadcast", with: "p"))])
            //: }, repeats: true)
        }, repeats: true)
    }

    /// 开启视频通话心跳包
    //: private func startTalkPing() {
    private func liveStart() {
        //: endTalkingPing()
        loadBig()

        //: self.talkTimer = Timer.scheduledTimer(withTimeInterval: SOCKET_CALL_INTERVAL, block: {  timer in
        self.talkTimer = Timer.scheduledTimer(withTimeInterval: kAgeTitle, block: { _ in
            /// 不在通话中，不在音视频通话页，没有开启最小化时
            //: if !self.isGeneral || (self.currentViewController()?.isKind(of: TalkingVideoChatViewController.self) == false  && self.currentViewController()?.isKind(of: TalkingVoiceChatViewController.self) == false ) && (self.currGeneralVC?.isKind(of: TalkingVideoChatViewController.self) == false  && self.currGeneralVC?.isKind(of: TalkingVoiceChatViewController.self) == false ) {
            if !self.isGeneral || (self.field()?.isKind(of: AdvancedErrorDelegate.self) == false && self.field()?.isKind(of: MileageThen.self) == false) && (self.currGeneralVC?.isKind(of: AdvancedErrorDelegate.self) == false && self.currGeneralVC?.isKind(of: MileageThen.self) == false) {
                //: return
                return
            }
            //: self.sendMessage(info: ["cmd": "callHeartbeat"])
            self.attentionCut(info: [(String(appAdviceSkipValue)): (String(dataReplyMsg))])
            //: }, repeats: true)
        }, repeats: true)
    }

    /// 结束视频通话心跳包
    //: func endTalkingPing() {
    func loadBig() {
        //: if self.talkTimer != nil {
        if self.talkTimer != nil {
            //: self.talkTimer?.invalidate()
            self.talkTimer?.invalidate()
            //: self.talkTimer = nil
            self.talkTimer = nil
        }
    }

    /// 处理通话逻辑
    //: func handleCallData() {
    func accumulation() {
        //: if self.cancelCall == true {
        if self.cancelCall == true { // 拨打方已停止呼叫
            //: self.cancelCall = false
            self.cancelCall = false
            //: self.unHandleCallData = nil
            self.unHandleCallData = nil
            //: return
            return
        }

        //: if self.unHandleCallData != nil {
        if self.unHandleCallData != nil {
            //: let recievedTime = self.unHandleCallData!["recievedDate"] as! Date
            let recievedTime = self.unHandleCallData![(dataDealUrl.lowercased() + "eved" + String(mainCutStr))] as! Date
            //: let currentTime = Date()
            let currentTime = Date()
            //: if currentTime.timeIntervalSince(recievedTime) < VIDEO_1v1_RECIEVED_CALL_TIMEOUT {
            if currentTime.timeIntervalSince(recievedTime) < noti_storageId {
                //: FacultyReactiveCompatible.share.recieve1v1TalkRequest(info: self.unHandleCallData!)
                FacultyReactiveCompatible.share.conserveFit(info: self.unHandleCallData!)
            }
            //: self.unHandleCallData = nil
            self.unHandleCallData = nil
        }
    }
}

// MARK: - 直播心跳包

//: extension TalkingSocketManager {
extension AlsoThen {
    /// 开启直播心跳包
    //: func startLivePing() {
    func lookCharacter() {
        //: endLivePing()
        runCountoReply()
        //: self.liveTimer = Timer.scheduledTimer(withTimeInterval: SOCKET_LIVE_INTERVAL, block: { timer in
        self.liveTimer = Timer.scheduledTimer(withTimeInterval: notiCollectionMsg, block: { _ in
            //: self.sendMessage(info: ["cmd": "liveHeartBeat"])
            self.attentionCut(info: [(String(appAdviceSkipValue)): (user_readyValue.replacingOccurrences(of: "whole", with: "li") + String(constEnoughMessage.suffix(6)) + "tBeat")])
            //: }, repeats: true)
        }, repeats: true)
    }

    /// 结束直播心跳包
    //: func endLivePing() {
    func runCountoReply() {
        //: if self.liveTimer != nil {
        if self.liveTimer != nil {
            //: self.liveTimer?.invalidate()
            self.liveTimer?.invalidate()
            //: self.liveTimer = nil
            self.liveTimer = nil
        }
    }
}

// MARK: - 音视频窗口最小化

//: extension TalkingSocketManager {
extension AlsoThen {
    /// 创建窗口最小化容器
    //: private func mini_createContainerView() {
    private func hiView() {
        //: guard self.containerView == nil else { return }
        guard self.containerView == nil else { return }

        //: let windowView = TalkingMiniContainerView()
        let windowView = MotionView()
        //: let keyWindow = MacroBorderDefineThen.getWindow()
        let keyWindow = MacroBorderDefineThen.frameworkWindow()
        //: windowView.frame = keyWindow.bounds
        windowView.frame = keyWindow.bounds
        //: windowView.backgroundColor = .clear
        windowView.backgroundColor = .clear
        //: keyWindow.addSubview(windowView)
        keyWindow.addSubview(windowView)
        //: self.containerView = windowView
        self.containerView = windowView
        //: self.containerView?.isHidden = !self.isGeneral
        self.containerView?.isHidden = !self.isGeneral
    }

    /// 移除窗口最小化容器
    //: private func mini_removeContainerView() {
    private func weltanschauungIndex() {
        //: guard self.containerView != nil else { return }
        guard self.containerView != nil else { return }
        //: self.containerView?.removeAllSubviews()
        self.containerView?.removeAllSubviews()
        //: self.containerView?.removeFromSuperview()
        self.containerView?.removeFromSuperview()
        //: self.containerView = nil
        self.containerView = nil
    }
}

// MARK: - Socket

//: extension TalkingSocketManager {
extension AlsoThen {
    /// 关闭socket
    /// - Parameter userLoginOut: 是否是用户退出
    //: func closeWebSocket(userLoginOut: Bool) {
    func photographicCamera(userLoginOut: Bool) {
        //: printLog(message: "🔗 关闭 socket")
        printLog(message: (dataDatabaseUrl.replacingOccurrences(of: "properly", with: "🔗") + String(userEstimatedTitle)))
        //: if self.webSocket != nil {
        if self.webSocket != nil {
            //: self.webSocket?.delegate = nil
            self.webSocket?.delegate = nil
            //: if userLoginOut == true {
            if userLoginOut == true {
                //: self.webSocket?.close(withCode: SOCKET_MANAGER_USER_LOGOUT_CLOSED_CODE, reason: "The user logs out")
                self.webSocket?.close(withCode: data_giftPath, reason: (String(k_levelToolPath.suffix(8)) + String(showTillStr)))
                //: } else {
            } else {
                //: self.webSocket?.close()
                self.webSocket?.close()
            }
            //: self.webSocket = nil
            self.webSocket = nil
        }

        //: if self.keepAliveTimer != nil {
        if self.keepAliveTimer != nil {
            //: self.keepAliveTimer?.invalidate()
            self.keepAliveTimer?.invalidate()
            //: self.keepAliveTimer = nil
            self.keepAliveTimer = nil
        }

        //: endTalkingPing()
        loadBig()

        //: if userLoginOut == true {
        if userLoginOut == true {
            //: self.connectUrlStr = nil
            self.connectUrlStr = nil
            // 销毁最小化窗口
            //: mini_removeContainerView()
            weltanschauungIndex()
        }
    }

    /// 更新socket连接
    //: func updateWebSocket() {
    func applicationSocket() {
        //: self.connectUrlStr = TalkingSocketManager.shared.getWebSocketPath(isRetry: false)
        self.connectUrlStr = AlsoThen.shared.beVia(isRetry: false)
        //: self.reconnectWebSocket(timeInterval: 0, type: .AppLogin)
        self.option(timeInterval: 0, type: .AppLogin)
    }

    /// 重连socket
    //: private func reconnectWebSocket(timeInterval: TimeInterval, type: SocketRetryType) {
    private func option(timeInterval: TimeInterval, type: BeJump) {
        //: DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + timeInterval) {
        DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + timeInterval) {
            //: UploadLogTool.writeLog(msg: "SOCKET \(type.rawValue)重连. 重试次数：\(self.failureCount), 地址：\(self.connectUrlStr ?? "").")
            MigrationThen.pathAge(msg: (String(userPersonName)) + "\(type.rawValue)" + (kReceptionDoingstMapMessage.capitalized + "重试次数" + dataCoordinatorText) + "\(self.failureCount), 地址：\(self.connectUrlStr ?? "").")
            //: self.connectWebSocket(urlStr: self.connectUrlStr)
            self.backPitch(urlStr: self.connectUrlStr)
        }
    }

    /// 连接socket
    //: private func connectWebSocket(urlStr: String?) {
    private func backPitch(urlStr: String?) {
        //: guard urlStr?.isEmpty == false else { return }
        guard urlStr?.isEmpty == false else { return }

        //: if urlStr == self.connectUrlStr && self.webSocket != nil && (self.webSocket?.readyState == .OPEN || self.webSocket?.readyState == .CONNECTING) {
        if urlStr == self.connectUrlStr, self.webSocket != nil, self.webSocket?.readyState == .OPEN || self.webSocket?.readyState == .CONNECTING {
            //: if (self.webSocket?.readyState == .OPEN) {
            if self.webSocket?.readyState == .OPEN {
                //: UploadLogTool.writeLog(msg: "SOCKET 连接正常 不需要重新连接.")
                MigrationThen.pathAge(msg: (String(mainScoreFormat.prefix(7)) + noti_screenStr + dataCurPath))
                //: } else {
            } else {
                //: UploadLogTool.writeLog(msg: "SOCKET 正在连接 不需要重新连接.")
                MigrationThen.pathAge(msg: (String(showSampleMaximumMessage.suffix(7)) + show_fireMessage + "需要\u{91cd}新\u{8fde}接."))
            }
            //: return
            return
        }

        //: self.connectUrlStr = urlStr
        self.connectUrlStr = urlStr

        //: var newStr = urlStr!
        var newStr = urlStr!
        //: if self.isGeneral {
        if self.isGeneral {
            //: newStr = newStr.appending("&resume=1")
            newStr = newStr.appending((String(const_expansionValue.prefix(5)) + String(dataTireOwnName)))
        }
        //: newStr = newStr.appending("&lang=\(TalkingRequestAddrTool.share.interfaceLang)")
        newStr = newStr.appending((String(app_constantSaveHalfUrl) + String(show_satisfyMessage)) + "\(RocketRecognizeAddrTool.share.interfaceLang)")
        //: closeWebSocket(userLoginOut: false)
        photographicCamera(userLoginOut: false)

        //: let url = URL(string: newStr)
        let url = URL(string: newStr)
        //: var request = URLRequest(url: url!)
        var request = URLRequest(url: url!)
        //: request.timeoutInterval = SOCKET_MANAGER_CONNECT_TIMEOUT
        request.timeoutInterval = noti_cornerFormat
        //: self.webSocket = SRWebSocket(urlRequest: request)
        self.webSocket = SRWebSocket(urlRequest: request)
        //: self.webSocket?.delegate = self
        self.webSocket?.delegate = self
        //: self.webSocket?.open()
        self.webSocket?.open()
        //: UploadLogTool.writeLog(msg: "SOCKET 开始连接, 地址：\(newStr).")
        MigrationThen.pathAge(msg: (String(kStorageData)) + "\(newStr).")
    }

    /// 发送消息
    /// - Parameter info: 消息体
    /// - Returns: 发送结果
    //: @discardableResult
    @discardableResult
    //: func sendMessage(info: [String: Any]?) -> Bool {
    func attentionCut(info: [String: Any]?) -> Bool {
        //: guard info != nil else { return false }
        guard info != nil else { return false }

        // 1. 判断是否有网络连接
        //: if FacultyReactiveCompatible.share.reachability?.connection == nil || FacultyReactiveCompatible.share.reachability?.connection == .unavailable {
        if FacultyReactiveCompatible.share.reachability?.connection == nil || FacultyReactiveCompatible.share.reachability?.connection == .unavailable {
            //: let info: [String: Any] = ["cmd": "onNetDisabled",
            let info: [String: Any] = [(String(appAdviceSkipValue)): (String(dataSheKey)),
                                       //: "errno": "\(SOCKET_WITHOUT_NET_CODE)",
                                       (String(appExtraValue)): "\(dataGreetId)",
                                       //: "data": [:]]
                                       (show_timeKey.replacingOccurrences(of: "brush", with: "a")): [:]]
            //: let data = try? JSONSerialization.data(withJSONObject: info, options: [])
            let data = try? JSONSerialization.data(withJSONObject: info, options: [])
            //: let message = String(data: data!, encoding: String.Encoding.utf8)
            let message = String(data: data!, encoding: String.Encoding.utf8)
            //: handleReceiveMessage(message: message)
            supreme(message: message)
            //: return false
            return false
        }

        // 2. 尝试发送消息
        //: if self.webSocket?.readyState == SRReadyState.OPEN {
        if self.webSocket?.readyState == SRReadyState.OPEN {
            //: let data = try? JSONSerialization.data(withJSONObject: info!, options: [])
            let data = try? JSONSerialization.data(withJSONObject: info!, options: [])
            //: let message = String(data: data!, encoding: String.Encoding.utf8)
            let message = String(data: data!, encoding: String.Encoding.utf8)
            //: if info!["cmd"] as? String == "ping" {
            if info![(String(appAdviceSkipValue))] as? String == (dataBothTitle.replacingOccurrences(of: "broadcast", with: "p")) {
                //: UploadLogTool.writeLog(msg: "SOCKET 连接保活cmd: ping.")
                MigrationThen.pathAge(msg: (String(mainNoteFormat) + String(main_termsKey)))
                //: } else if info!["cmd"] as? String == "callHeartbeat" {
            } else if info![(String(appAdviceSkipValue))] as? String == (String(dataReplyMsg)) {
                //: UploadLogTool.writeLog(msg: "SOCKET trtc保活cmd: callHeartbeat.")
                MigrationThen.pathAge(msg: String(bytes: appDirectName.map{layerAdmin(announcement: $0)}, encoding: .utf8)!)
                //: } else if info!["cmd"] as? String == "liveHeartBeat" {
            } else if info![(String(appAdviceSkipValue))] as? String == (user_readyValue.replacingOccurrences(of: "whole", with: "li") + String(constEnoughMessage.suffix(6)) + "tBeat") {
                //: UploadLogTool.writeLog(msg: "SOCKET live保活cmd: liveHeartBeat.")
                MigrationThen.pathAge(msg: String(bytes: appAssetBreadId.map{popProvide(sort: $0)}, encoding: .utf8)!)
            }
            //: self.webSocket?.send(message)
            self.webSocket?.send(message)
            //: return true
            return true

            //: } else {
        } else {
            //: if info!["cmd"] as? String == "ping" {
            if info![(String(appAdviceSkipValue))] as? String == (dataBothTitle.replacingOccurrences(of: "broadcast", with: "p")) {
                //: let info: [String: Any] = ["cmd": "onP",
                let info: [String: Any] = [(String(appAdviceSkipValue)): (String(const_timingStockName)),
                                           //: "errno": "\(SOCKET_MANAGER_UNCONNECTED_CODE)",
                                           (String(appExtraValue)): "\(main_okStr)",
                                           //: "data": [:]]
                                           (show_timeKey.replacingOccurrences(of: "brush", with: "a")): [:]]
                //: let data = try? JSONSerialization.data(withJSONObject: info, options: [])
                let data = try? JSONSerialization.data(withJSONObject: info, options: [])
                //: let message = String(data: data!, encoding: String.Encoding.utf8)
                let message = String(data: data!, encoding: String.Encoding.utf8)
                //: handleReceiveMessage(message: message)
                supreme(message: message)
            }
            //: self.reconnectWebSocket(timeInterval: 5.0, type: .SocketSendMsg)
            self.option(timeInterval: 5.0, type: .SocketSendMsg)
            //: return false
            return false
        }
    }

    /// 处理接收到的消息
    /// - Parameter message: 消息体
    //: private func handleReceiveMessage(message: String?) {
    private func supreme(message: String?) {
        //: guard message != nil else { return }
        guard message != nil else { return }

        //: let json = JSON(parseJSON: message!)
        let json = JSON(parseJSON: message!)
        //: let cmdName = json["cmd"].stringValue
        let cmdName = json[(String(appAdviceSkipValue))].stringValue
        //: let errorNo = json["errno"].intValue
        let errorNo = json[(String(appExtraValue))].intValue
        //: let msg = json["msg"].stringValue
        let msg = json[(dataGinRunningMsg.replacingOccurrences(of: "solution", with: "g"))].stringValue
        //: let data = json["data"].dictionaryObject ?? [:]
        let data = json[(show_timeKey.replacingOccurrences(of: "brush", with: "a"))].dictionaryObject ?? [:]

        //: printLog(message: "🔗 处理消息：cmdName:\(cmdName), errNo:\(errorNo), msg:\(msg), data:\(data)")
        printLog(message: (String(const_bounceTitle)) + "\(cmdName)" + (String(data_meltSharedName.prefix(6)) + String(data_complexId)) + "\(errorNo)" + (String(dataRestoreValue) + String(notiDragUrl)) + "\(msg)" + (String(user_perspectiveMessage) + const_feedbackData.replacingOccurrences(of: "meet", with: ":")) + "\(data)")

        // 处理异常
        //: guard errorNo == 0 else {
        guard errorNo == 0 else {
            //: UploadLogTool.writeLog(msg: "SOCKET 接收异常消息：cmdName:\(cmdName), errNo:\(errorNo), msg:\(msg), data:\(data).")
            MigrationThen.pathAge(msg: (appHundredTitle.replacingOccurrences(of: "reward", with: "SO") + "T 接收异" + showTableMiddlePath + String(const_cantId.prefix(8))) + "\(cmdName)" + (String(data_meltSharedName.prefix(6)) + String(data_complexId)) + "\(errorNo)" + (String(dataRestoreValue) + String(notiDragUrl)) + "\(msg)" + (String(user_perspectiveMessage) + const_feedbackData.replacingOccurrences(of: "meet", with: ":")) + "\(data).")
            //: if errorNo == 444 {
            if errorNo == 444 {
                //: FacultyReactiveCompatible.share.loginUserMode.freeCallTimes = 0
                FacultyReactiveCompatible.share.loginUserMode.freeCallTimes = 0
            }
            //: self.errorDelegate?.socket_didRecieveError(errorNo: errorNo, cmd: cmdName, msg: msg, data: data)
            self.errorDelegate?.data(errorNo: errorNo, cmd: cmdName, msg: msg, data: data)
            //: return
            return
        }

        // 处理正常接收的消息
        //: if cmdName == "onReceivedGift" { // 礼物
        if cmdName == (String(app_cleanMsg.prefix(5)) + String(show_parentData.suffix(9))) { // 礼物
            //: NotificationCenter.default.post(name: USER_RECIEVED_GIFT_NOTIFICATION, object: data)
            NotificationCenter.default.post(name: showAccuracyText, object: data)

            //: } else if cmdName == "onVideoMatch" { // 视频速配
        } else if cmdName == (String(constPocketBoundName) + userChannelName.lowercased()) { // 视频速配
            //: self.videoMatchDelegate?.socket_match_onVideoMatch(data: data)
            self.videoMatchDelegate?.standard(data: data)

            //: } else if cmdName == "onRequestCall" { // 拨打方发出requestCall回执
        } else if cmdName == (String(const_blendExplainForceKey.suffix(5)) + "uestC" + String(noti_globName)) { // 拨打方发出requestCall回执
            //: self.isCalling = true
            self.isCalling = true
            // 视频速配 || 音视频通话
            //: self.videoMatchDelegate?.socket_match_onRequestCall(data: data)
            self.videoMatchDelegate?.widespreadData(data: data)

            //: } else if cmdName == "onCancelCall" { // 拨打方发出cancelCall回执，目前无需处理
        } else if cmdName == (String(notiCreateId.suffix(4)) + kMiniValue.lowercased() + "Call") { // 拨打方发出cancelCall回执，目前无需处理
            //: } else if cmdName == "onRefuseCall" { // 接收方发送refuseCall回执，目前无需处理
        } else if cmdName == (String(appMakeEnvironmentTitle.suffix(5)) + "useCall") { // 接收方发送refuseCall回执，目前无需处理
            //: } else if cmdName == "onAcceptCall" { // 接收方发送acceptCall回执
        } else if cmdName == (String(constIncentiveMessage) + String(mainLoadName)) { // 接收方发送acceptCall回执
            //: self.isGeneral = true
            self.isGeneral = true
            //: self.isCalling = false
            self.isCalling = false
            //: startTalkPing()
            liveStart()
            //: self.calledPartyDelegate?.socket_videoChat_onAcceptCall(data: data)
            self.calledPartyDelegate?.starHideSort(data: data)

            //: } else if cmdName == "onStopCall" { // 接收方发送stopCall回执
        } else if cmdName == (k_tickStr.replacingOccurrences(of: "voice", with: "o") + String(dataSuspendKey.suffix(8))) { // 接收方发送stopCall回执
            //: self.isGeneral = false
            self.isGeneral = false
            //: self.isCalling = false
            self.isCalling = false
            //: endTalkingPing()
            loadBig()
            //: if let freeCallTimes = data["freeCallTimes"] as? Int {
            if let freeCallTimes = data[(appServerName.replacingOccurrences(of: "literal", with: "re") + "CallT" + userPassUrl.replacingOccurrences(of: "panel", with: "e"))] as? Int {
                //: FacultyReactiveCompatible.share.loginUserMode.freeCallTimes = freeCallTimes
                FacultyReactiveCompatible.share.loginUserMode.freeCallTimes = freeCallTimes
            }
            // 更新余额
            //: FacultyReactiveCompatible.share.loginUserMode.mf_coin = json["data"]["mfCoin"].stringValue
            FacultyReactiveCompatible.share.loginUserMode.mf_coin = json[(show_timeKey.replacingOccurrences(of: "brush", with: "a"))][(String(user_sweepName))].stringValue

            //: inviteCallPopViewEndShow(data: data)
            examine(data: data)
            //: NotificationCenter.default.post(name: VIDEOCALL_END_CLOSEDISCOUNTS_NOTIFICATION, object: nil)
            NotificationCenter.default.post(name: appEventUrl, object: nil)
            //: self.isFreeCall = false
            self.isFreeCall = false
            //: } else if cmdName == "onMissCoin" { // 余额不足提醒
        } else if cmdName == (String(dataClotMessage.suffix(5)) + String(data_successfulMsg.prefix(5))) { // 余额不足提醒
            //: self.callerDelegate?.socket_videoChat_onMissCoin(msg: msg, data: data)
            self.callerDelegate?.noKit(msg: msg, data: data)

            //: } else if cmdName == "acceptCall" { // 接收方接受了来电，改变状态为通话中,开始推拉流
        } else if cmdName == (main_withoutTellName.replacingOccurrences(of: "progress", with: "e") + String(noti_advicePath.suffix(5))) { // 接收方接受了来电，改变状态为通话中,开始推拉流
            //: self.isGeneral = true
            self.isGeneral = true
            //: self.isCalling = false
            self.isCalling = false
            //: startTalkPing()
            liveStart()
            //: self.callerDelegate?.socket_videoChat_acceptCall(data: data)
            self.callerDelegate?.curve(data: data)

            //: } else if cmdName == "stopCall" { // 接收方/拨打方 挂断了通话
        } else if cmdName == (main_rugSweepId.replacingOccurrences(of: "score", with: "o") + "Call") { // 接收方/拨打方 挂断了通话
            //: self.isGeneral = false
            self.isGeneral = false
            //: self.isCalling = false
            self.isCalling = false
            //: endTalkingPing()
            loadBig()
            //: self.callerDelegate?.socket_videoChat_stopCall(data: data)
            self.callerDelegate?.wrapData(data: data)
            // 更新余额
            //: FacultyReactiveCompatible.share.loginUserMode.mf_coin = json["data"]["mfCoin"].stringValue
            FacultyReactiveCompatible.share.loginUserMode.mf_coin = json[(show_timeKey.replacingOccurrences(of: "brush", with: "a"))][(String(user_sweepName))].stringValue
            //: if let freeCallTimes = data["freeCallTimes"] as? Int {
            if let freeCallTimes = data[(appServerName.replacingOccurrences(of: "literal", with: "re") + "CallT" + userPassUrl.replacingOccurrences(of: "panel", with: "e"))] as? Int {
                //: FacultyReactiveCompatible.share.loginUserMode.freeCallTimes = freeCallTimes
                FacultyReactiveCompatible.share.loginUserMode.freeCallTimes = freeCallTimes
            }
            //: inviteCallPopViewEndShow(data: data)
            examine(data: data)
            //: NotificationCenter.default.post(name: VIDEOCALL_END_CLOSEDISCOUNTS_NOTIFICATION, object: nil)
            NotificationCenter.default.post(name: appEventUrl, object: nil)
            //: self.isFreeCall = false
            self.isFreeCall = false

            //: } else if cmdName == "requestCall" { // 接收方收到了拨打方的申请通话
        } else if cmdName == (showSegmentPath.replacingOccurrences(of: "burn", with: "e") + String(notiTunMsg)) { // 接收方收到了拨打方的申请通话
            //: self.isCalling = true
            self.isCalling = true
            // 发送通知，告知外界处理逻辑
            //: NotificationCenter.default.post(name: SOCKET_VIDEOCHAT_REQUESTCALL_NOTIFICATION, object: self)
            NotificationCenter.default.post(name: user_styleMethodMsg, object: self)

            //: if UIApplication.shared.applicationState != .background {
            if UIApplication.shared.applicationState != .background { // 应用在前台
                //: FacultyReactiveCompatible.share.recieve1v1TalkRequest(info: data)
                FacultyReactiveCompatible.share.conserveFit(info: data)
                //: } else {
            } else {
                //: self.cancelCall = false
                self.cancelCall = false
                //: self.unHandleCallData = data
                self.unHandleCallData = data
                //: self.unHandleCallData?["recievedDate"] = Date()
                self.unHandleCallData?[(dataDealUrl.lowercased() + "eved" + String(mainCutStr))] = Date()

                // 应用在后台，发送本地推送告知用户
                //: var body = "[Invite you to a video call...]".localized
                var body = String(bytes: notiDeskId.map{$0^173}, encoding: .utf8)!.localized
                //: if json["data"]["type"].intValue == 1 { // 音频
                if json[(show_timeKey.replacingOccurrences(of: "brush", with: "a"))][(noti_commonName.replacingOccurrences(of: "resolution", with: "p"))].intValue == 1 { // 音频
                    //: body = "[Invite you to a voice call...]".localized
                    body = String(bytes: constRemindAgainContent.map{$0^107}, encoding: .utf8)!.localized
                }
                //: AppDelegateHelper.pushLocalNotification(uid: String(json["data"]["uid"].intValue),
                RetainThen.joint(uid: String(json[(show_timeKey.replacingOccurrences(of: "brush", with: "a"))][(String(noti_accordingUrl))].intValue),
                                        //: title: data["nickname"] as? String,
                                        title: data[(String(app_tagText))] as? String,
                                        //: body: body,
                                        body: body,
                                        //: imageUrl: data["headPic"] as? String)
                                        imageUrl: data[(String(noti_flexibleKey))] as? String)
                // 播放来电音乐
                //: AppDelegateHelper.shared.startPlayBGM()
                RetainThen.shared.produce()
            }

            //: } else if cmdName == "onCallResume" { // 接收方/发送方 恢复通话中的页面，并开始推拉流
        } else if cmdName == (String(notiBreastStr) + userProcedurePath.replacingOccurrences(of: "commend", with: "m")) { // 接收方/发送方 恢复通话中的页面，并开始推拉流
            //: self.isGeneral = true
            self.isGeneral = true
            //: self.isCalling = false
            self.isCalling = false
            //: startTalkPing()
            liveStart()

            //: var resumeData = data
            var resumeData = data
            //: resumeData["onCallResume"] = "1"
            resumeData[(String(notiBreastStr) + userProcedurePath.replacingOccurrences(of: "commend", with: "m"))] = "1"
            //: FacultyReactiveCompatible.share.recieve1v1TalkResumeRequest(info: resumeData)
            FacultyReactiveCompatible.share.whiteSomeone(info: resumeData)

            //: } else if cmdName == "cancelCall" { // 拨打方拒绝了通话，接收方收到该指令
        } else if cmdName == (String(show_themeLibraryFormat) + String(user_wordItValue.prefix(4))) { // 拨打方拒绝了通话，接收方收到该指令
            //: self.isGeneral = false
            self.isGeneral = false
            //: self.isCalling = false
            self.isCalling = false
            //: self.cancelCall = true
            self.cancelCall = true
            //: self.isFreeCall = false
            self.isFreeCall = false
            //: endTalkingPing()
            loadBig()
            //: self.callerDelegate?.socket_videoChat_cancelCall(data: data)
            self.callerDelegate?.delineateBean(data: data)
            // 应用在后台
            //: if UIApplication.shared.applicationState == .background {
            if UIApplication.shared.applicationState == .background {
                // 移除来电本地推送
                //: AppDelegateHelper.local_removeDeliveredNotification(identifier: String(json["data"]["uid"].intValue))
                RetainThen.micCon(identifier: String(json[(show_timeKey.replacingOccurrences(of: "brush", with: "a"))][(String(noti_accordingUrl))].intValue))
                // 停止播放来电音乐
                //: AppDelegateHelper.shared.stopPlayBGM()
                RetainThen.shared.mated()
            }

            //: } else if cmdName == "refuseCall" { // 接收方拒绝了通话，拨打方收到该指令
        } else if cmdName == (show_seeUrl.replacingOccurrences(of: "feedback", with: "re") + "eCal" + user_heritageData.replacingOccurrences(of: "nor", with: "l")) { // 接收方拒绝了通话，拨打方收到该指令
            //: self.isGeneral = false
            self.isGeneral = false
            //: self.isCalling = false
            self.isCalling = false
            //: self.isFreeCall = false
            self.isFreeCall = false
            //: endTalkingPing()
            loadBig()
            //: self.calledPartyDelegate?.socket_videoChat_refuseCall(data: data)
            self.calledPartyDelegate?.countCity(data: data)

            //: } else if cmdName == "onSendProgramGift" { // 视频通话节目送礼成功tost
        } else if cmdName == (String(const_bookTitle.prefix(5)) + String(dataDecideFormat.suffix(5)) + "ramGift") { // 视频通话节目送礼成功tost
            //: self.func__showStatusBarSuccessMsg(showMsg: msg)
            self.pointOfReferenceMedia(showMsg: msg)

            //: } else if cmdName == "onGameFs" { // 游戏中奖飘屏
        } else if cmdName == (String(showWithStr) + String(showEquallyProvideUrl)) { // 游戏中奖飘屏
            //: let broadStatus = data["broadStatus"] as? Int ?? -1
            let broadStatus = data[(String(showLockMsg.suffix(8)) + user_readingKey.replacingOccurrences(of: "select", with: "us"))] as? Int ?? -1
            //: if FacultyReactiveCompatible.share.appStatus == AppSkinStatus.normal.rawValue {
            if FacultyReactiveCompatible.share.appStatus == PenConstantTarget.normal.rawValue {
                //: if broadStatus == 1 {
                if broadStatus == 1 {
                    //: NotificationCenter.default.post(name: RECIVE_FLOATSCREEN_NOTIFICATION, object: self, userInfo: data)
                    NotificationCenter.default.post(name: show_lessKey, object: self, userInfo: data)
                    //: NotificationCenter.default.post(name: USER_RECIEVED_GIFT_NOTIFICATION, object: data)
                    NotificationCenter.default.post(name: showAccuracyText, object: data)
                    //: } else if broadStatus == -1 {
                } else if broadStatus == -1 {
                    //: NotificationCenter.default.post(name: RECIVE_FLOATSCREEN_NOTIFICATION, object: self, userInfo: data)
                    NotificationCenter.default.post(name: show_lessKey, object: self, userInfo: data)
                }
            }

            //: } else if cmdName == "onCallChatMsg" { // 收到视频通话中的聊天消息
        } else if cmdName == (String(showLowStr.suffix(5)) + "lChatMsg") { // 收到视频通话中的聊天消息
            //: TalkingVideoDanmuManager.shared().onRecvDanmuNewMsg(msg: data)
            AfterAdvancedDanmuManager.object().range(msg: data)

            //: } else if cmdName == "onUserBlock" { // 用户被封号
        } else if cmdName == (String(constConvertPath.prefix(4)) + String(showFireReportedTitle.suffix(7))) { // 用户被封号
            //: NotificationCenter.default.post(name: DID_LOGIN_OUT_SUCCESS_NOTIFICATION, object: nil, userInfo: nil)
            NotificationCenter.default.post(name: main_contentMessage, object: nil, userInfo: nil)
        }
    }
}

// MARK: - 处理业务逻辑

//: extension TalkingSocketManager {
extension AlsoThen {
    /// 邀请通话通话结束之后，判断弹窗类型
    //: private func inviteCallPopViewEndShow(data: [String: Any]) {
    private func examine(data: [String: Any]) {
        // 展示评分弹窗
        //: AppStoreRatingManager.shared.requestQuickRating()
        JumpRatingManager.shared.across()
        //: guard FacultyReactiveCompatible.share.loginUserMode.sex == Gender.male.rawValue else {
        guard FacultyReactiveCompatible.share.loginUserMode.sex == ATextLiteral.male.rawValue else {
            //: return
            return
        }
        // 弹出优惠充值弹窗
        //: if (data["isRecharge"]) as? Bool == false {
        if data[(notiLogicalName.replacingOccurrences(of: "reject", with: "is") + String(notiLiterTitle.suffix(8)))] as? Bool == false {
            //: SmartThen.share.func_pushToDiscountRechargeView()
            SmartThen.share.onConvert()
        }
        // 半屏充值弹窗场景
        //: let source = data["source"] as? String
        let source = data[(String(show_farReceiveStr))] as? String
        //: guard source == "invitePop" || source == "videoPop" else {
        guard source == (String(notiPureMessage.suffix(9))) || source == (String(const_shallMessage.prefix(8))) else {
            //: return
            return
        }
        //: if (data["coin"] as? Int) ?? 0 < 60 {
        if (data[(dataDailyRefreshUrl.replacingOccurrences(of: "method", with: "oi"))] as? Int) ?? 0 < 60 {
            //: SmartThen.share.func__pushToHalfWebVC(webViewType: .RechargeHalfPage)
            SmartThen.share.gallery(webViewType: .RechargeHalfPage)
        }
    }
}
