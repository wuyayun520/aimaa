
//: Declare String Begin

/*: "cmd" :*/
fileprivate let userHostValue:[UInt8] = [0x4f,0x59,0x50]

fileprivate func pushCancelTrack(reply num: UInt8) -> UInt8 {
    let value = Int(num) + 20
    if value > 255 {
        return UInt8(value - 256)
    } else {
        return UInt8(value)
    }
}

/*: "cancelCall" :*/
fileprivate let show_teemStr:String = "sourcenc"

/*: "data" :*/
fileprivate let constNaturalFormat:[UInt8] = [0x19,0x1c,0x9,0x1c]

private func beYourselfJust(week num: UInt8) -> UInt8 {
    return num ^ 125
}

/*: "logId" :*/
fileprivate let appCornerTitle:String = "tell pill physicallogId"

/*: "#322870" :*/
fileprivate let main_creamValue:[Character] = ["#"]
fileprivate let showNorKey:[Character] = ["3","2","2","8","7","0"]

/*: "Tap to start video chatting" :*/
fileprivate let appTopicName:String = "decrease launchTap t"
fileprivate let constModifyRequestPanelId:String = "become file moonart v"
fileprivate let const_errValue:String = "sample run stranger maker cha"

/*: "icon_close_nor" :*/
fileprivate let main_normallyUrl:String = "area usericon_clo"
fileprivate let user_blendData:[Character] = ["s","e","_","n","o","r"]

/*: "icon_views_number" :*/
fileprivate let user_mailFormat:String = "event ion lens warning iicon_vie"
fileprivate let kCommercialId:String = "ws_pipe construct"
fileprivate let kComeMessage:String = "nuincreaseber"

/*: "%@ chatting" :*/
fileprivate let notiBeginningMsg:[Character] = ["%","@"," ","c","h"]
fileprivate let show_simpleStressFunUrl:String = "ATTING"

/*: "icon_lounge" :*/
fileprivate let notiCountoValue:String = "pill member conveyicon_l"
fileprivate let kConsequenceName:String = "ounglass"

/*: "icon_rank_coin" :*/
fileprivate let showRunningName:[Character] = ["i","c","o"]
fileprivate let userCosGrabName:String = "going currently define long movien_ran"

/*: "vip_btn_top" :*/
fileprivate let app_thereData:String = "girl add fixed title diskvip_b"

/*: "btn_nearby_settings_nor" :*/
fileprivate let const_fashionName:String = "btn_mask dismiss bind country cap"
fileprivate let const_meltMessage:String = "recognize escape ease increase sceneby_s"
fileprivate let showSaveBetweenUrl:String = "output input reward lenss_no"
fileprivate let dataRobotAbsolutelyId:String = "formation"

/*: "%@ Coins/min" :*/
fileprivate let user_weekKey:[Character] = ["%","@"," ","C","o","i","n"]
fileprivate let user_successPath:String = "appearance magnitudes/min"

/*: "img_match_connecting" :*/
fileprivate let user_runName:[Character] = ["i","m","g","_","m","a","t","c","h","_","c","o","n","n","e","c","t","i","n","g"]

/*: "#9F80FF" :*/
fileprivate let userScanMsg:[Character] = ["#","9","F","8","0","F","F"]

/*: "get json error" :*/
fileprivate let show_incentiveFormat:[Character] = ["g","e","t"," ","j","s"]
fileprivate let app_canEncouragingTitle:String = "ON"
fileprivate let noti_customerUrl:String = "listener dynamics replace function error"

/*: "matchId" :*/
fileprivate let kMeUrl:String = "bass activity white invest sweepmatchId"

/*: "errno" :*/
fileprivate let app_pastName:[UInt8] = [0x75,0x62,0x62,0x7e,0x7f]

private func clingQuestion(entry num: UInt8) -> UInt8 {
    return num ^ 16
}

/*: "VIPFee" :*/
fileprivate let notiBeneathId:String = "unknown into say moveVIPFee"

/*: "Matching now" :*/
fileprivate let dataHerMsg:String = "Matchidetail literal"
fileprivate let const_skipName:[Character] = ["o","w"]

/*: "userTouch" :*/
fileprivate let notiShouldPath:String = "userToufence carrier"
fileprivate let k_behindMessage:String = "ccream"

/*: "mf/freeCall/getVideoInfo" :*/
fileprivate let mainHalfAlleyKey:String = "mf/frfamily statin within box"
fileprivate let userChallengeValue:String = "l/getVreception with"

/*: "fromType" :*/
fileprivate let k_heFollowStr:[UInt8] = [0xd0,0xc4,0xd9,0xdb,0xe2,0xcf,0xc6,0xd3]

private func goingCharmStaff(below num: UInt8) -> UInt8 {
    return num ^ 182
}

/*: "You've been barred from receiving calls" :*/
fileprivate let notiMaximumKey:[UInt8] = [0x8a,0xbc,0xa6,0xf4,0xa5,0xb6,0xf3,0xb1,0xb6,0xb6,0xbd,0xf3,0xb1,0xb2,0xa1,0xa1,0xb6,0xb7,0xf3,0xb5,0xa1,0xbc,0xbe,0xf3,0xa1,0xb6,0xb0,0xb6,0xba,0xa5,0xba,0xbd,0xb4,0xf3,0xb0,0xb2,0xbf,0xbf,0xa0]

private func biologicalLock(greet num: UInt8) -> UInt8 {
    return num ^ 211
}

/*: "source" :*/
fileprivate let kWillTitle:[UInt8] = [0xef,0xf3,0xe9,0xee,0xff,0xf9]

private func allowBroken(dynamics num: UInt8) -> UInt8 {
    return num ^ 156
}

/*: "type" :*/
fileprivate let notiFallKey:[UInt8] = [0x57,0x5c,0x53,0x48]

fileprivate func tooHundred(down num: UInt8) -> UInt8 {
    let value = Int(num) - 227
    if value < 0 {
        return UInt8(value + 256)
    } else {
        return UInt8(value)
    }
}

/*: "uid" :*/
fileprivate let constSafetyKey:[UInt8] = [0xb3,0xaf,0xa2]

private func thumbSeaBorder(currency num: UInt8) -> UInt8 {
    return num ^ 198
}

/*: "fromFreeCall" :*/
fileprivate let appExistingName:String = "frorecommendation"

/*: "requestCall" :*/
fileprivate let userVoiceKey:String = "reqliteral"
fileprivate let dataScenarioPath:String = "verbal"

/*: "onVideoMatch" :*/
fileprivate let k_momentData:String = "onVideoholder run produce refuse big"
fileprivate let kHeartId:String = "Matchaverage warn formula inner"

/*: "[Automatic video] Receive video call incoming" :*/
fileprivate let app_measurementName:[UInt8] = [0xcc,0xd6,0xe2,0xe3,0xf8,0xfa,0xf6,0xe3,0xfe,0xf4,0xb7,0xe1,0xfe,0xf3,0xf2,0xf8,0xca,0xb7,0xc5,0xf2,0xf4,0xf2,0xfe,0xe1,0xf2,0xb7,0xe1,0xfe,0xf3,0xf2,0xf8,0xb7,0xf4,0xf6,0xfb,0xfb,0xb7,0xfe,0xf9,0xf4,0xf8,0xfa,0xfe,0xf9,0xf0]

/*: "isRandomVCPush" :*/
fileprivate let appPopMessage:String = "isRanattention salt distant auto ta"
fileprivate let appLargeData:[Character] = ["P","u","s","h"]

/*: "refuseCall" :*/
fileprivate let const_goParaData:[Character] = ["r","e","f","u","s","e","C","a","l","l"]

/*: "onCancelCall" :*/
fileprivate let constJudgeStr:[Character] = ["o","n","C","a","n","c","e","l","C"]
fileprivate let showShapeBackyardUrl:[Character] = ["a","l","l"]

/*: "retryAfter" :*/
fileprivate let main_ingredientName:[Character] = ["r","e","t","r"]
fileprivate let const_natureData:String = "yAfterutilize assessment"

/*: "onRequestCall" :*/
fileprivate let dataPaperEastStr:[Character] = ["o","n"]
fileprivate let k_findText:[Character] = ["R","e","q","u","e","s","t","C","a","l","l"]

/*: "failType" :*/
fileprivate let noti_resTapUrl:String = "failTypeat listener spend precedent"

/*: "callWaitingTime" :*/
fileprivate let dataPlayerMsg:[Character] = ["c","a","l","l","W","a","i","t","i","n","g","T","i","m"]
fileprivate let user_costAgeValue:[Character] = ["e"]

/*: "toUid" :*/
fileprivate let mainProFormat:[Character] = ["t","o","U","i","d"]

/*: "Free 1 min" :*/
fileprivate let notiSpecKey:[Character] = ["F","r","e","e"," ","1"," "]
fileprivate let constStaffPath:[Character] = ["m","i","n"]

/*: "%@/min" :*/
fileprivate let appNarrowMsg:String = "agency within reported west%@/min"

//: Declare String End

// __DEBUG__
// __CLOSE_PRINT__
//
//  LiteralViewController.swift
//  AbroadTalking
//
//  Created by young on 2023/8/22.
//

//: import UIKit
import UIKit

//: class TalkingRandomVideoViewController: TalkingBaseViewController {
class LiteralViewController: SafetyFactorViewController {
    //: var fee = 0
    var fee = 0 // 原价
    //: var vipFee = 0
    var vipFee = 0 // VIP价格
    //: var chattingNum = 0
    var chattingNum = 0 // 当前匹配人数
    //: private var matchId: Int = 0
    private var matchId: Int = 0 // 速配批次Id
    //: private var waitingTimer: Timer?
    private var waitingTimer: Timer? // 呼叫等待定时（等待15s）
    //: private var retryAfterTimer: Timer?
    private var retryAfterTimer: Timer? // 匹配失败重试定时器
    //: private var logId = 0
    private var logId = 0 // 通话id
    //: private var isShowFreeVideo = false
    private var isShowFreeVideo = false // 是否已展示免费视频挽留弹窗
    //: var beginTime: TimeInterval?
    var beginTime: TimeInterval?
    //: private var dispatchWorkItem: DispatchWorkItem?
    private var dispatchWorkItem: DispatchWorkItem?
    //: private var closeBtnWorkItem: DispatchWorkItem?
    private var closeBtnWorkItem: DispatchWorkItem?
    //: var isFirstRandow = true
    var isFirstRandow = true
    //: var isBeginRand = false
    var isBeginRand = false

    //: override func viewWillAppear(_ animated: Bool) {
    override func viewWillAppear(_ animated: Bool) {
        //: super.viewWillAppear(animated)
        super.viewWillAppear(animated)
        //: DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 1.0) {
        DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 1.0) {
            //: if !self.isFirstRandow {
            if !self.isFirstRandow {
                //: self.refreshVideoCallPrice()
                self.soundKeep()
            }
        }
        //: self.resetCamera()
        self.camera()
    }

    //: override func viewDidLoad() {
    override func viewDidLoad() {
        //: super.viewDidLoad()
        super.viewDidLoad()
        //: self.hideNavi = true
        self.hideNavi = true

        //: setupSubviews()
        blindSide()
        //: setupSubViewsConstraint()
        sayConstraint()
        //: bindInteraction()
        feedbackStreet()
        //: videoManager.startPreview(isMatch: true)
        videoManager.oval(isMatch: true)
        //: setPlayer(isRandon: false)
        duringCounter(isRandon: false)
        //: if isBeginRand {
        if isBeginRand {
            //: start_randomVideo()
            early()
        }
    }

    //: deinit {
    deinit {
        //: if waitingTimer != nil {
        if waitingTimer != nil {
            //: TalkingSocketManager.shared.sendMessage(info: ["cmd": "cancelCall", "data": ["logId": logId]])
            AlsoThen.shared.attentionCut(info: [String(bytes: userHostValue.map{pushCancelTrack(reply: $0)}, encoding: .utf8)!: (show_teemStr.replacingOccurrences(of: "source", with: "ca") + "elCall"), String(bytes: constNaturalFormat.map{beYourselfJust(week: $0)}, encoding: .utf8)!: [(String(appCornerTitle.suffix(5))): logId]])
        }
        //: NotificationCenter.default.removeObserver(self)
        NotificationCenter.default.removeObserver(self)
        //: destroyWaitingTimer()
        edge()
        //: if TalkingSocketManager.shared.isGeneral == false {
        if AlsoThen.shared.isGeneral == false {
            //: TalkingSocketManager.shared.videoMatchDelegate = nil
            AlsoThen.shared.videoMatchDelegate = nil
            //: TalkingSocketManager.shared.callerDelegate = nil
            AlsoThen.shared.callerDelegate = nil
            //: TalkingSocketManager.shared.calledPartyDelegate = nil
            AlsoThen.shared.calledPartyDelegate = nil
            //: TalkingSocketManager.shared.errorDelegate = nil
            AlsoThen.shared.errorDelegate = nil
        }
    }

    // MARK: - Lazy Load

    //: private lazy var videoManager: TalkingVideoChatManager = {
    private lazy var videoManager: FactorChatManager = {
        //: let m = TalkingVideoChatManager()
        let m = FactorChatManager()
        //: let info = TalkingVideoChatModel()
        let info = ControlChatModel()
        //: info.videoStage = .Calling
        info.videoStage = .Calling
        //: info.renderPostion = .selfOnBg
        info.renderPostion = .selfOnBg
        //: m.infoModel = info
        m.infoModel = info
        // 视图传递给manager
        //: m.bgVideoView = self.renderView
        m.bgVideoView = self.renderView
        //: return m
        return m
        //: }()
    }()

    // 视频画面渲染视图
    //: private lazy var renderView: UIView = {
    private lazy var renderView: UIView = {
        //: let v = UIView()
        let v = UIView()
        //: v.backgroundColor = .mottleResult()
        v.backgroundColor = .mottleResult()
        //: return v
        return v
        //: }()
    }()

    //: private lazy var gradientBtn: UIButton = {
    private lazy var gradientBtn: UIButton = {
        //: let btn = UIButton()
        let btn = UIButton()
        //: btn.addTarget(self, action: #selector(start_randomVideo), for: .touchUpInside)
        btn.addTarget(self, action: #selector(early), for: .touchUpInside)
        //: btn.layer.masksToBounds = true
        btn.layer.masksToBounds = true
        // 渐变
        //: let layer = CAGradientLayer()
        let layer = CAGradientLayer()
        //: layer.frame = CGRect(x: 0, y: 0, width: ScreenWidth+1, height: ScreenHeight+1)
        layer.frame = CGRect(x: 0, y: 0, width: main_assetUrl + 1, height: kTopUrl + 1)
        //: layer.startPoint = CGPoint(x: 0.5, y: 0)
        layer.startPoint = CGPoint(x: 0.5, y: 0)
        //: layer.endPoint = CGPoint(x: 0.5, y: 1)
        layer.endPoint = CGPoint(x: 0.5, y: 1)
        //: layer.colors = [UIColor(white: 0, alpha: 0.15).cgColor, UIColor(hex: "#322870")!.cgColor]
        layer.colors = [UIColor(white: 0, alpha: 0.15).cgColor, UIColor(hex: (String(main_creamValue) + String(showNorKey)))!.cgColor]
        //: layer.locations = [0, 1]
        layer.locations = [0, 1]
        //: btn.layer.addSublayer(layer)
        btn.layer.addSublayer(layer)
        //: return btn
        return btn
        //: }()
    }()

    //: private lazy var svgaPlayer: SVGAPlayer = {
    private lazy var svgaPlayer: SVGAPlayer = {
        //: let player = SVGAPlayer.init()
        let player = SVGAPlayer()
        //: player.loops = 0
        player.loops = 0
        //: player.clearsAfterStop = false
        player.clearsAfterStop = false
        //: player.isUserInteractionEnabled = false
        player.isUserInteractionEnabled = false
        //: player.contentMode = .scaleAspectFill
        player.contentMode = .scaleAspectFill

        //: return player
        return player
        //: }()
    }()

    //: private lazy var tipsLab: UILabel = {
    private lazy var tipsLab: UILabel = {
        //: let lab = UILabel()
        let lab = UILabel()
        //: lab.textColor = .white
        lab.textColor = .white
        //: lab.font = UIFont.boldSystemFont(ofSize: 20)
        lab.font = UIFont.boldSystemFont(ofSize: 20)
        //: lab.textAlignment = .center
        lab.textAlignment = .center
        //: lab.text = "Tap to start video chatting".localized
        lab.text = (String(appTopicName.suffix(5)) + "o st" + String(constModifyRequestPanelId.suffix(5)) + "ideo" + String(const_errValue.suffix(4)) + "tting").localized
        //: return lab
        return lab
        //: }()
    }()

    //: private lazy var closeBtn: UIButton = {
    private lazy var closeBtn: UIButton = {
        //: let btn = UIButton()
        let btn = UIButton()
        //: btn.setImage(UIImage.BundleImageNamed(name: "icon_close_nor"), for: .normal)
        btn.setImage(UIImage.adName(name: (String(main_normallyUrl.suffix(8)) + String(user_blendData))), for: .normal)
        //: btn.addTarget(self, action: #selector(closeButtonClick), for: .touchUpInside)
        btn.addTarget(self, action: #selector(offPureClick), for: .touchUpInside)
        //: return btn
        return btn
        //: }()
    }()

    //: private lazy var chattingNumBtn: TalkingButton = {
    private lazy var chattingNumBtn: SeparateControlReactiveCompatible = {
        //: let btn = TalkingButton()
        let btn = SeparateControlReactiveCompatible()
        //: btn.setImage(UIImage.BundleImageNamed(name: "icon_views_number"), for: .normal)
        btn.setImage(UIImage.adName(name: (String(user_mailFormat.suffix(8)) + String(kCommercialId.prefix(3)) + kComeMessage.replacingOccurrences(of: "increase", with: "m"))), for: .normal)
        //: btn.setImage(UIImage.BundleImageNamed(name: "icon_views_number"), for: .disabled)
        btn.setImage(UIImage.adName(name: (String(user_mailFormat.suffix(8)) + String(kCommercialId.prefix(3)) + kComeMessage.replacingOccurrences(of: "increase", with: "m"))), for: .disabled)
        //: btn.spaceBetweenTitleAndImage = 5
        btn.spaceBetweenTitleAndImage = 5
        //: btn.isEnabled = false
        btn.isEnabled = false
        //: btn.setTitleColor(UIColor.white, for: .normal)
        btn.setTitleColor(UIColor.white, for: .normal)
        //: btn.titleLabel?.font = UIFont.pingfangFont(type: .Regular, fontSize: 15)
        btn.titleLabel?.font = UIFont.recipeDecision(type: .Regular, fontSize: 15)
        //: btn.setTitle("%@ chatting".localizedArguments(chattingNum), for: .normal)
        btn.setTitle((String(notiBeginningMsg) + show_simpleStressFunUrl.lowercased()).extraPropertyLetter(chattingNum), for: .normal)
        //: return btn
        return btn
        //: }()
    }()

    //: private lazy var priceStackView: UIStackView = {
    private lazy var priceStackView: UIStackView = {
        //: let v = UIStackView()
        let v = UIStackView()
        //: v.axis = .horizontal
        v.axis = .horizontal
        //: v.alignment = .center
        v.alignment = .center
        //: v.spacing = 4
        v.spacing = 4
        //: v.isUserInteractionEnabled = false
        v.isUserInteractionEnabled = false
        //: return v
        return v
        //: }()
    }()

    //: private lazy var vipIcon: UIImageView = {
    private lazy var vipIcon: UIImageView = {
        //: let img = UIImageView()
        let img = UIImageView()
        //: img.image = UIImage.BundleImageNamed(name: "icon_lounge")
        img.image = UIImage.adName(name: (String(notiCountoValue.suffix(6)) + kConsequenceName.replacingOccurrences(of: "glass", with: "ge")))
        //: img.contentMode = .scaleAspectFill
        img.contentMode = .scaleAspectFill
        //: return img
        return img
        //: }()
    }()

    //: private lazy var goldCoinIcon: UIImageView = {
    private lazy var goldCoinIcon: UIImageView = {
        //: let img = UIImageView()
        let img = UIImageView()
        //: img.image = UIImage.BundleImageNamed(name: "icon_rank_coin")
        img.image = UIImage.adName(name: (String(showRunningName) + String(userCosGrabName.suffix(5)) + "k_coin"))
        //: return img
        return img
        //: }()
    }()

    //: private lazy var priceLab: UILabel = {
    private lazy var priceLab: UILabel = {
        //: let lab = UILabel()
        let lab = UILabel()
        //: lab.textColor = .white
        lab.textColor = .white
        //: lab.font = UIFont.pingfangFont(type: .Regular, fontSize: 15)
        lab.font = UIFont.recipeDecision(type: .Regular, fontSize: 15)
        //: return lab
        return lab
        //: }()
    }()

    //: private lazy var vipTopImgView: UIImageView = {
    private lazy var vipTopImgView: UIImageView = {
        //: let imgV = UIImageView()
        let imgV = UIImageView()
        //: imgV.isHidden = true
        imgV.isHidden = true
        //: imgV.image  = UIImage.BundleImageNamed(name: "vip_btn_top")
        imgV.image = UIImage.adName(name: (String(app_thereData.suffix(5)) + "tn_top"))
        //: return imgV
        return imgV
        //: }()
    }()

    // 会员折扣价
    //: private lazy var vipPriceBtn: TalkingButton = {
    private lazy var vipPriceBtn: SeparateControlReactiveCompatible = {
        //: let btn = TalkingButton()
        let btn = SeparateControlReactiveCompatible()
        //: btn.backgroundColor = UIColor.white.withAlphaComponent(0.1)
        btn.backgroundColor = UIColor.white.withAlphaComponent(0.1)
        //: btn.layer.cornerRadius = actualWidth(w: 27)
        btn.layer.cornerRadius = actualWidth(w: 27)
        //: btn.clipsToBounds = true
        btn.clipsToBounds = true
        //: btn.addTarget(self, action: #selector(maleVipPriceButtonClick), for: .touchUpInside)
        btn.addTarget(self, action: #selector(adminPic), for: .touchUpInside)
        //: btn.isHidden = true
        btn.isHidden = true
        //: btn.setBackgroundImage(UIImage.BundleImageNamed(name: "btn_nearby_settings_nor"), for: .normal)
        btn.setBackgroundImage(UIImage.adName(name: (String(const_fashionName.prefix(4)) + "near" + String(const_meltMessage.suffix(4)) + "etting" + String(showSaveBetweenUrl.suffix(4)) + dataRobotAbsolutelyId.replacingOccurrences(of: "formation", with: "r"))), for: .normal)
        //: return btn
        return btn
        //: }()
    }()

    //: private lazy var vipPriceImg: UIImageView = {
    private lazy var vipPriceImg: UIImageView = {
        //: let imgV = UIImageView()
        let imgV = UIImageView()
        //: imgV.image  = UIImage.BundleImageNamed(name: "icon_rank_coin")
        imgV.image = UIImage.adName(name: (String(showRunningName) + String(userCosGrabName.suffix(5)) + "k_coin"))
        //: return imgV
        return imgV
        //: }()
    }()

    //: private lazy var vipPriceLab: UILabel = {
    private lazy var vipPriceLab: UILabel = {
        //: let lab = UILabel()
        let lab = UILabel()
        //: lab.textColor = .areaEmphasis()
        lab.textColor = .areaEmphasis()
        //: lab.text = "%@ Coins/min".localizedArguments(vipFee)
        lab.text = (String(user_weekKey) + String(user_successPath.suffix(5))).extraPropertyLetter(vipFee)
        //: lab.font = UIFont.pingfangFont(type: .Medium, fontSize: 15)
        lab.font = UIFont.recipeDecision(type: .Medium, fontSize: 15)
        //: return lab
        return lab
        //: }()
    }()

    //: private lazy var norPriceLab: UILabel = {
    private lazy var norPriceLab: UILabel = {
        //: let lab = UILabel()
        let lab = UILabel()
        //: return lab
        return lab
        //: }()
    }()

    // 免费视频挽留弹窗
    //: private lazy var freeVideoAlert: TalkingLiveAlertView = {
    private lazy var freeVideoAlert: PositionArrayView = //: return PositionArrayView(frame: .zero, type: .freeVideo)
        .init(frame: .zero, type: .freeVideo)
    //: }()

    //: private lazy var connectingImg: UIImageView = {
    private lazy var connectingImg: UIImageView = {
        //: let imgV = UIImageView()
        let imgV = UIImageView()
        //: imgV.image  = UIImage.BundleImageNamed(name: "img_match_connecting")
        imgV.image = UIImage.adName(name: (String(user_runName)))
        //: let view = UIActivityIndicatorView.init()
        let view = UIActivityIndicatorView()
        //: view.color = UIColor(hex: "#9F80FF")
        view.color = UIColor(hex: (String(userScanMsg)))
        //: imgV.addSubview(view)
        imgV.addSubview(view)
        //: view.snp.makeConstraints { make in
        view.snp.makeConstraints { make in
            //: make.bottom.equalToSuperview().offset(-12)
            make.bottom.equalToSuperview().offset(-12)
            //: make.centerX.equalToSuperview()
            make.centerX.equalToSuperview()
            //: make.size.equalTo(20)
            make.size.equalTo(20)
        }
        //: view.startAnimating()
        view.startAnimating()
        //: imgV.isHidden = true
        imgV.isHidden = true
        //: return imgV
        return imgV
        //: }()
    }()
}

// MARK: - Request & 数据处理

//: extension TalkingRandomVideoViewController {
extension LiteralViewController {
    //: private func setPlayer(isRandon: Bool) {
    private func duringCounter(isRandon: Bool) {
        //: let url = SVGAEffectTool.default.getZipEffectPath(type: isRandon ? .Random_video_bg : .Random_video_bg_nor )
        let url = TaEffectTool.default.towardPath(type: isRandon ? .Random_video_bg : .Random_video_bg_nor)
        //: do {
        do {
            //: let data = try Data(contentsOf: url)
            let data = try Data(contentsOf: url)
            //: let parser = SVGAParser.init()
            let parser = SVGAParser()
            //: parser.parse(with: data, cacheKey: "") { [weak self] videoItem in
            parser.parse(with: data, cacheKey: "") { [weak self] videoItem in
                //: guard let self = self else {return}
                guard let self = self else { return }
                //: svgaPlayer.videoItem = videoItem
                svgaPlayer.videoItem = videoItem
                //: svgaPlayer.startAnimation()
                svgaPlayer.startAnimation()
            }
            //: } catch {
        } catch {
            //: printLog(message: "get json error")
            printLog(message: (String(show_incentiveFormat) + app_canEncouragingTitle.lowercased() + String(noti_customerUrl.suffix(6))))
        }
    }

    /// 开始随机视频（男）
    /// - Parameters:
    ///   - cmd: 事件名
    ///   - errno: cmd的错误码
    //: private func req_randomVideoToMatch(cmd: String, errno: String) {
    private func okByInformation(cmd: String, errno: String) {
        //: beginTime = Date().timeIntervalSince1970
        beginTime = Date().timeIntervalSince1970
        //: let params = ["matchId": self.matchId,
        let params = [(String(kMeUrl.suffix(7))): self.matchId,
                      //: "cmd": cmd,
                      String(bytes: userHostValue.map{pushCancelTrack(reply: $0)}, encoding: .utf8)!: cmd,
                      //: "errno": errno] as [String: Any]
                      String(bytes: app_pastName.map{clingQuestion(entry: $0)}, encoding: .utf8)!: errno] as [String: Any]
        //: TalkingQuickVideoManager.req_randomVideoToMatch(params: params) { succeed, result, errorModel in
        FactorReactiveCompatible.againCompletion(params: params) { succeed, result, _ in
            //: guard succeed == true else {
            guard succeed == true else {
                //: self.stop_randomVideo()
                self.titleTag()
                //: return
                return
            }
            //: let json = JSON(result ?? [String: Any]())
            let json = JSON(result ?? [String: Any]())
            //: self.matchId = json["matchId"].intValue
            self.matchId = json[(String(kMeUrl.suffix(7)))].intValue
        }
    }

    /// 更新VIP价格
    //: @objc private func req_updateVIPFee() {
    @objc private func restoreThroughLegalFee() {
        //: TalkingQuickVideoManager.req_videoMatchCheck() { succeed, result, errorModel in
        FactorReactiveCompatible.sourceDay { succeed, result, _ in
            //: guard succeed else { return }
            guard succeed else { return }
            //: let json = JSON(result ?? [String: Any]())
            let json = JSON(result ?? [String: Any]())
            //: self.vipFee = json["VIPFee"].intValue
            self.vipFee = json[(String(notiBeneathId.suffix(6)))].intValue
            //: self.refreshVideoCallPrice()
            self.soundKeep()
        }
    }
}

// MARK: - Event

//: extension TalkingRandomVideoViewController {
extension LiteralViewController {
    /// 男性侧vip价格按钮点击事件
    //: @objc private func maleVipPriceButtonClick() {
    @objc private func adminPic() {
        //: SmartThen.share.func__pushToSubscribeAlert()
        SmartThen.share.thumbParams()
    }

    /// 关闭按钮点击事件
    //: @objc func closeButtonClick() {
    @objc func offPureClick() {
        //: dispatchWorkItem?.cancel()
        dispatchWorkItem?.cancel()
        //: showCloseBtn()
        per()
        //: uploadRecord.uploadRecordEvent(eventID: ClickRandomvideoCancelNoP)
        kSaveKey.putAvailable(eventID: show_trustFormat)

        //: if FacultyReactiveCompatible.share.appConfigMode.displayMatchV4PopUp, FacultyReactiveCompatible.share.loginUserMode.freeCallTimes > 0,
        if FacultyReactiveCompatible.share.appConfigMode.displayMatchV4PopUp, FacultyReactiveCompatible.share.loginUserMode.freeCallTimes > 0,
           //: isShowFreeVideo == false {
           isShowFreeVideo == false
        {
            //: self.freeVideoAlert.show()
            self.freeVideoAlert.equalShow()
            //: isShowFreeVideo = true
            isShowFreeVideo = true
            //: return
            return
        }
        //: currentViewController()?.navigationController?.popViewController(animated: true)
        field()?.navigationController?.popViewController(animated: true)
    }

    /// 开始随机视频
    //: @objc private func start_randomVideo() {
    @objc private func early() {
        //: guard TalkingSocketManager.shared.isGeneral == false else {
        guard AlsoThen.shared.isGeneral == false else {
            //: self.func__showStatusBarErrorMsg(showMsg: kMessage_videoTalking_limit)
            self.micromicronProcess(showMsg: notiSinceKey)
            //: return
            return
        }
        //: guard gradientBtn.isEnabled == true else { return }
        guard gradientBtn.isEnabled == true else { return }
        //: afterShowClose()
        demonstrateAfterOpen()
        //: self.matchId = 0
        self.matchId = 0 // 重置匹配池
        //: gradientBtn.isEnabled = false
        gradientBtn.isEnabled = false
        //: tipsLab.text = "Matching now".localized
        tipsLab.text = (String(dataHerMsg.prefix(6)) + "ng n" + String(const_skipName)).localized
        //: setPlayer(isRandon: true)
        duringCounter(isRandon: true)
        //: priceStackView.isHidden = true
        priceStackView.isHidden = true
        //: vipPriceBtn.isHidden = true
        vipPriceBtn.isHidden = true
        //: vipTopImgView.isHidden = true
        vipTopImgView.isHidden = true
        //: if FacultyReactiveCompatible.share.loginUserMode.randomVideoType == 2 && FacultyReactiveCompatible.share.loginUserMode.robotVideoNum > 0 {
        if FacultyReactiveCompatible.share.loginUserMode.randomVideoType == 2, FacultyReactiveCompatible.share.loginUserMode.robotVideoNum > 0 {
            //: uploadRecord.uploadRecordEvent(eventID: ClickRobotRandomButton)
            kSaveKey.putAvailable(eventID: appTimeSincePath)
            //: req_getVideoInfo()
            belowPair()
            //: } else {
        } else {
            //: uploadRecord.uploadRecordEvent(eventID: ClickRandomvideoCallNoP)
            kSaveKey.putAvailable(eventID: notiAssetMessage)

            //: TalkingSocketManager.shared.videoMatchDelegate = self
            AlsoThen.shared.videoMatchDelegate = self
            //: TalkingSocketManager.shared.callerDelegate = self
            AlsoThen.shared.callerDelegate = self
            //: TalkingSocketManager.shared.calledPartyDelegate = self
            AlsoThen.shared.calledPartyDelegate = self
            //: TalkingSocketManager.shared.errorDelegate = self
            AlsoThen.shared.errorDelegate = self
            //: restart_randomVideo()
            pursuingErrno()
        }
    }

    /// 重新开始视频速配
    /// - Parameters:
    ///   - cmd: 事件名（默认用户主动点击）
    ///   - errno: cmd的错误码
    //: private func restart_randomVideo(cmd: String = "userTouch", errno: String = "0") {
    private func pursuingErrno(cmd: String = (String(notiShouldPath.prefix(7)) + k_behindMessage.replacingOccurrences(of: "cream", with: "h")), errno: String = "0") {
        // 停止定时器
        //: destroyWaitingTimer()
        edge()
        //: req_randomVideoToMatch(cmd: cmd, errno: errno)
        okByInformation(cmd: cmd, errno: errno)
    }

    /// 停止随机视频
    //: private func stop_randomVideo() {
    private func titleTag() {
        // 停止定时器
        //: destroyWaitingTimer()
        edge()
        //: showCloseBtn()
        per()

        //: gradientBtn.isEnabled = true
        gradientBtn.isEnabled = true
        //: tipsLab.text = "Tap to start video chatting".localized
        tipsLab.text = (String(appTopicName.suffix(5)) + "o st" + String(constModifyRequestPanelId.suffix(5)) + "ideo" + String(const_errValue.suffix(4)) + "tting").localized
        //: setPlayer(isRandon: false)
        duringCounter(isRandon: false)
        //: self.isFirstRandow = false
        self.isFirstRandow = false
        //: if TalkingSocketManager.shared.isGeneral == false {
        if AlsoThen.shared.isGeneral == false {
            //: TalkingSocketManager.shared.videoMatchDelegate = nil
            AlsoThen.shared.videoMatchDelegate = nil
            //: TalkingSocketManager.shared.callerDelegate = nil
            AlsoThen.shared.callerDelegate = nil
            //: TalkingSocketManager.shared.calledPartyDelegate = nil
            AlsoThen.shared.calledPartyDelegate = nil
            //: TalkingSocketManager.shared.errorDelegate = nil
            AlsoThen.shared.errorDelegate = nil
        }
        //: refreshVideoCallPrice()
        soundKeep()
    }

    /// 销毁定时器
    //: private func destroyWaitingTimer() {
    private func edge() {
        //: if self.waitingTimer != nil {
        if self.waitingTimer != nil {
            //: self.waitingTimer?.invalidate()
            self.waitingTimer?.invalidate()
            //: self.waitingTimer = nil
            self.waitingTimer = nil
        }

        //: if self.retryAfterTimer != nil {
        if self.retryAfterTimer != nil {
            //: self.retryAfterTimer?.invalidate()
            self.retryAfterTimer?.invalidate()
            //: self.retryAfterTimer = nil
            self.retryAfterTimer = nil
        }
    }

    /// 获取用户主动拨打视频信息
    //: @objc private func req_getVideoInfo() {
    @objc private func belowPair() {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = ReportedThen()
        //: reqModel.requestPath = "mf/freeCall/getVideoInfo"
        reqModel.requestPath = (String(mainHalfAlleyKey.prefix(5)) + "eeCal" + String(userChallengeValue.prefix(6)) + "ideoInfo")
        //: reqModel.requestType = .GET
        reqModel.requestType = .GET
        //: reqModel.params = ["fromType": "1"]
        reqModel.params = [String(bytes: k_heFollowStr.map{goingCharmStaff(below: $0)}, encoding: .utf8)!: "1"]
        //: reqModel.showErrorStatusBar = false
        reqModel.showErrorStatusBar = false
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        appAdjustData.kitEspecial(model: reqModel) { succeed, result, errorModel in

            //: guard succeed == true else {
            guard succeed == true else {
                //: self.stop_randomVideo()
                self.titleTag()
                //: if errorModel?.errorCode ?? 1 > 0 {
                if errorModel?.errorCode ?? 1 > 0 {
                    //: FacultyReactiveCompatible.share.loginUserMode.randomVideoType = 1
                    FacultyReactiveCompatible.share.loginUserMode.randomVideoType = 1
                    //: self.start_randomVideo()
                    self.early()
                    //: return
                    return
                }
                //: self.func__showStatusBarErrorMsg(showMsg: errorModel?.errorMsg ?? "")
                self.micromicronProcess(showMsg: errorModel?.errorMsg ?? "")
                //: return
                return
            }
            //: guard let result = result as? Dictionary<String, Any> else {
            guard let result = result as? [String: Any] else {
                //: return
                return
            }
            //: self.connectingImg.isHidden = false
            self.connectingImg.isHidden = false
            //: if let model = TalkingVideoInitivCallModel.deserialize(from: result as? Dictionary) {
            if let model = FactorModelType.deserialize(from: result as? Dictionary) {
                /// 预加载
                //: let  player = TalkingVideoPlayerManager.init()
                let player = AttentionThen()
                //: player.setMute(bEnable: true)
                player.setMuteEmpower(bEnable: true)
                //: player.playerWithUrlAndVideoView(url: model.callVideoUrl, view: UIView.init())
                player.find(url: model.callVideoUrl, view: UIView())

                //: TalkingVideoInitivCallManager.shared.videoCallModel = model
                StrangerManager.shared.videoCallModel = model

                //: TalkingPermissionTool.checkCameraAndMicrophone { [weak self] isOpen in
                ActivityReactiveCompatible.counteraction { [weak self] isOpen in
                    //: guard let self = self else { return }
                    guard let self = self else { return }
                    //: guard isOpen == true else { return }
                    guard isOpen == true else { return }

                    //: self.putThrough()
                    self.electricGlowAt()
                }
            }
        }
    }

    /// 接通电话
    //: private func putThrough() {
    private func electricGlowAt() {
        //: uploadRecord.uploadRecordEvent(eventID: clickVideoAcceptButton)
        kSaveKey.putAvailable(eventID: noti_maxRequestFormat)
        /// 视频通话被禁用
        //: guard TalkingVideoInitivCallManager.shared.videoCallModel.isBan == 0 else {
        guard StrangerManager.shared.videoCallModel.isBan == 0 else {
            //: self.func__showStatusBarErrorMsg(showMsg: "You've been barred from receiving calls".localized)
            self.micromicronProcess(showMsg: String(bytes: notiMaximumKey.map{biologicalLock(greet: $0)}, encoding: .utf8)!.localized)
            //: self.connectingImg.isHidden = true
            self.connectingImg.isHidden = true
            //: return
            return
        }
        //: self.dispatchWorkItem = DispatchWorkItem {
        self.dispatchWorkItem = DispatchWorkItem {
            //: self.connectingImg.isHidden = true
            self.connectingImg.isHidden = true
            //: TalkingVideoInitivCallManager.shared.req_userOperation(type: 1) { succeed, result, errorModel in
            StrangerManager.shared.charmCompletion(type: 1) { succeed, _, _ in
                //: guard succeed == true else {
                guard succeed == true else {
                    //: return
                    return
                }
                //: guard succeed == true else {
                guard succeed == true else {
                    //: self.stop_randomVideo()
                    self.titleTag()
                    //: return
                    return
                }
                //: let vc = TalkingVideoInitivCallVC()
                let vc = StateBoundaryViewController()
                //: vc.isRandomVCPush = true
                vc.isRandomVCPush = true
                //: self.currentViewController()?.navigationController?.pushViewController(vc, animated: true)
                self.field()?.navigationController?.pushViewController(vc, animated: true)
                //: self.stop_randomVideo()
                self.titleTag()
            }
        }
        // 添加延时任务
        //: DispatchQueue.main.asyncAfter(deadline: .now() + 3.0, execute: self.dispatchWorkItem!)
        DispatchQueue.main.asyncAfter(deadline: .now() + 3.0, execute: self.dispatchWorkItem!)
    }

    /// 匹配时x秒显示关闭按钮
    //: private func afterShowClose() {
    private func demonstrateAfterOpen() {
        //: if FacultyReactiveCompatible.share.appUserConfigMode.matchCancelBtnDelayTime > 0 {
        if FacultyReactiveCompatible.share.appUserConfigMode.matchCancelBtnDelayTime > 0 {
            //: self.closeBtn.isHidden = true
            self.closeBtn.isHidden = true
            //: closeBtnWorkItem = DispatchWorkItem { [weak self] in
            closeBtnWorkItem = DispatchWorkItem { [weak self] in
                //: guard let self = self else { return }
                guard let self = self else { return }
                //: self.closeBtn.isHidden = false
                self.closeBtn.isHidden = false
            }
            //: DispatchQueue.main.asyncAfter(deadline: .now() + Double(FacultyReactiveCompatible.share.appUserConfigMode.matchCancelBtnDelayTime), execute: closeBtnWorkItem!)
            DispatchQueue.main.asyncAfter(deadline: .now() + Double(FacultyReactiveCompatible.share.appUserConfigMode.matchCancelBtnDelayTime), execute: closeBtnWorkItem!)
        }
    }

    //: private func showCloseBtn() {
    private func per() {
        //: closeBtnWorkItem?.cancel()
        closeBtnWorkItem?.cancel()
        //: self.closeBtn.isHidden = false
        self.closeBtn.isHidden = false
    }

    //: private func resetCamera() {
    private func camera() {
        //: guard TalkingSocketManager.shared.isGeneral == false else {
        guard AlsoThen.shared.isGeneral == false else {
            //: return
            return
        }
        //: DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 2.0) {
        DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 2.0) {
            //: self.videoManager.infoModel?.videoStage = .Waiting
            self.videoManager.infoModel?.videoStage = .Waiting
            //: self.videoManager.config.openCamera = true
            self.videoManager.config.openCamera = true
            //: self.videoManager.isOpenTRTCPreview = false
            self.videoManager.isOpenTRTCPreview = false
            //: self.videoManager.startPreview()
            self.videoManager.oval()
        }
    }

    /// 匹配通话结束时
    //: @objc private func closeTool() {
    @objc private func flush() {
        //: DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 2) {
        DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 2) {
            //: self.start_randomVideo()
            self.early()
        }
    }
}

// MARK: - BearMatchDelegate

//: extension TalkingRandomVideoViewController: SocketManagerVideoMatchDelegate {
extension LiteralViewController: BearMatchDelegate {
    /// 视频速配成功
    //: func socket_match_onVideoMatch(data: [String: Any]) {
    func standard(data: [String: Any]) {
        // 拨打视频通话
        //: guard let model = TalkingMatchResultModel.deserialize(from: data) else { return }
        guard let model = GenerationResultModel.deserialize(from: data) else { return }
        //: self.matchId = model.matchId
        self.matchId = model.matchId
        //: var callData: [String: Any] = ["matchId": model.matchId,
        var callData: [String: Any] = [(String(kMeUrl.suffix(7))): model.matchId,
                                       //: "source": model.source,
                                       String(bytes: kWillTitle.map{allowBroken(dynamics: $0)}, encoding: .utf8)!: model.source,
                                       //: "type": model.type,
                                       String(bytes: notiFallKey.map{tooHundred(down: $0)}, encoding: .utf8)!: model.type,
                                       //: "uid": model.uid]
                                       String(bytes: constSafetyKey.map{thumbSeaBorder(currency: $0)}, encoding: .utf8)!: model.uid]
        //: let index = EnableFreeCallType.randow
        let index = UnderlyingHashableRepresentation.randow
        //: if FacultyReactiveCompatible.share.loginUserMode.freeCallTimes > 0 && FacultyReactiveCompatible.share.appUserConfigMode.enableFreeCallType & index.rawValue == index.rawValue {
        if FacultyReactiveCompatible.share.loginUserMode.freeCallTimes > 0 && FacultyReactiveCompatible.share.appUserConfigMode.enableFreeCallType & index.rawValue == index.rawValue {
            //: callData.updateValue(1, forKey: "fromFreeCall")
            callData.updateValue(1, forKey: (appExistingName.replacingOccurrences(of: "recommendation", with: "m") + "FreeCall"))
            //: TalkingSocketManager.shared.isFreeCall = true
            AlsoThen.shared.isFreeCall = true
        }
        //: let info: [String: Any] = ["cmd": "requestCall", "data": callData]
        let info: [String: Any] = [String(bytes: userHostValue.map{pushCancelTrack(reply: $0)}, encoding: .utf8)!: (userVoiceKey.replacingOccurrences(of: "literal", with: "u") + "estCal" + dataScenarioPath.replacingOccurrences(of: "verbal", with: "l")), String(bytes: constNaturalFormat.map{beYourselfJust(week: $0)}, encoding: .utf8)!: callData]
        //: TalkingSocketManager.shared.sendMessage(info: info)
        AlsoThen.shared.attentionCut(info: info)
        // 15s未接听主动挂断
        //: waitingTimer = Timer.scheduledTimer(withTimeInterval: TimeInterval(15), block: { [weak self] timer in
        waitingTimer = Timer.scheduledTimer(withTimeInterval: TimeInterval(15), block: { [weak self] _ in
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: TalkingSocketManager.shared.sendMessage(info: ["cmd": "cancelCall", "data": ["logId": self.logId]])
            AlsoThen.shared.attentionCut(info: [String(bytes: userHostValue.map{pushCancelTrack(reply: $0)}, encoding: .utf8)!: (show_teemStr.replacingOccurrences(of: "source", with: "ca") + "elCall"), String(bytes: constNaturalFormat.map{beYourselfJust(week: $0)}, encoding: .utf8)!: [(String(appCornerTitle.suffix(5))): self.logId]])
            //: self.restart_randomVideo(cmd: "onVideoMatch")
            self.pursuingErrno(cmd: (String(k_momentData.prefix(7)) + String(kHeartId.prefix(5))))
            //: }, repeats: false)
        }, repeats: false)
        //: connectingImg.isHidden = false
        connectingImg.isHidden = false
    }

    //: func socket_match_onRequestCall(data: [String: Any]) {
    func widespreadData(data: [String: Any]) {
        //: let json = JSON(data)
        let json = JSON(data)
        //: self.logId = json["logId"].intValue
        self.logId = json[(String(appCornerTitle.suffix(5)))].intValue
    }
}

// MARK: - ConfirmCallerDelegate【视频拨打方处理】

//: extension TalkingRandomVideoViewController: SocketManagerVideoChatCallerDelegate {
extension LiteralViewController: ConfirmCallerDelegate {
    /// 拨打方拒绝了通话，接收方收到该指令（未通话）
    //: func socket_videoChat_cancelCall(data: [String: Any]) {}
    func delineateBean(data _: [String: Any]) {}

    /// 对方结束了通话（通话中）
    //: func socket_videoChat_stopCall(data: [String: Any]) {}
    func wrapData(data _: [String: Any]) {}

    /// 余额不足提醒
    //: func socket_videoChat_onMissCoin(msg: String, data: [String: Any]) {}
    func noKit(msg _: String, data _: [String: Any]) {}

    /// 对方接受了来电，建立视频通话
    //: func socket_videoChat_acceptCall(data: [String: Any]) {
    func curve(data: [String: Any]) {
        // 应用在后台，先提醒用户
        //: if UIApplication.shared.applicationState == .background {
        if UIApplication.shared.applicationState == .background {
            //: AppDelegateHelper.pushLocalNotification(body: "[Automatic video] Receive video call incoming".localized)
            RetainThen.joint(body: String(bytes: app_measurementName.map{$0^151}, encoding: .utf8)!.localized)
        }
        //: let model = TalkingMatchResultModel.deserialize(from: data)
        let model = GenerationResultModel.deserialize(from: data)
        //: if let uid = model?.uid {
        if let uid = model?.uid {
            //: uploadSuccessTime(toUid: String(uid))
            searchion(toUid: String(uid))
        }
        //: connectingImg.isHidden = true
        connectingImg.isHidden = true
        //: stop_randomVideo()
        titleTag()
        // 开启视频通话时先销毁当前预览画面
        //: videoManager.shouldReleaseAllResource()
        videoManager.overFoot()
        //: var newData = data
        var newData = data
        //: newData["isRandomVCPush"] = true
        newData[(String(appPopMessage.prefix(5)) + "domVC" + String(appLargeData))] = true
        //: FacultyReactiveCompatible.share.start1v1TalkCall(info: newData, situation: .Calling)
        FacultyReactiveCompatible.share.restaurateurSituation(info: newData, situation: .Calling)
    }
}

// MARK: - SeparatePartyDelegate【接收到回执代理】

//: extension TalkingRandomVideoViewController: SocketManagerVideoChatCalledPartyDelegate {
extension LiteralViewController: SeparatePartyDelegate {
    /// 接收方拒绝了通话，拨打方收到该指令（未通话）
    //: func socket_videoChat_refuseCall(data: [String: Any]) {
    func countCity(data _: [String: Any]) {
        //: restart_randomVideo(cmd: "refuseCall")
        pursuingErrno(cmd: (String(const_goParaData)))
        //: connectingImg.isHidden = true
        connectingImg.isHidden = true
    }

    /// 接收方接受了通话，改变状态为通话中，acceptCall回执
    //: func socket_videoChat_onAcceptCall(data: [String: Any]) {}
    func starHideSort(data _: [String: Any]) {}
}

// MARK: - FriendshipErrorDelegate

//: extension TalkingRandomVideoViewController: TalkingSocketManagerErrorDelegate {
extension LiteralViewController: FriendshipErrorDelegate {
    //: func socket_didRecieveError(errorNo: Int, cmd: String, msg: String, data: [String: Any]) {
    func data(errorNo: Int, cmd: String, msg: String, data: [String: Any]) {
        //: connectingImg.isHidden = true
        connectingImg.isHidden = true
        //: if cmd == "onCancelCall" {  // 取消通话异常
        if cmd == (String(constJudgeStr) + String(showShapeBackyardUrl)) { // 取消通话异常
            //: stop_randomVideo()
            titleTag()
            //: return
            return
        }
        //: if cmd == "onVideoMatch", errorNo == 404 { // 匹配池用完
        if cmd == (String(k_momentData.prefix(7)) + String(kHeartId.prefix(5))), errorNo == 404 { // 匹配池用完
            //: stop_randomVideo()
            titleTag()
            //: let json = JSON(data)
            let json = JSON(data)
            //: self.matchId = json["matchId"].intValue
            self.matchId = json[(String(kMeUrl.suffix(7)))].intValue
            //: self.func__showStatusBarErrorMsg(showMsg: msg)
            self.micromicronProcess(showMsg: msg)
            //: uploadFailTime(type: 2)
            bringCookie(type: 2)
            //: return
            return
        }
        //: if cmd == "onVideoMatch", errorNo == 449 { // 匹配池用完，延时重试
        if cmd == (String(k_momentData.prefix(7)) + String(kHeartId.prefix(5))), errorNo == 449 { // 匹配池用完，延时重试
            //: let json = JSON(data)
            let json = JSON(data)
            //: retryAfterTimer = Timer.scheduledTimer(withTimeInterval: TimeInterval(json["retryAfter"].intValue), block: { [weak self] timer in
            retryAfterTimer = Timer.scheduledTimer(withTimeInterval: TimeInterval(json[(String(main_ingredientName) + String(const_natureData.prefix(6)))].intValue), block: { [weak self] _ in
                //: guard let self = self else { return }
                guard let self = self else { return }
                //: self.matchId = json["matchId"].intValue
                self.matchId = json[(String(kMeUrl.suffix(7)))].intValue
                //: self.restart_randomVideo(cmd: cmd, errno: String(errorNo))
                self.pursuingErrno(cmd: cmd, errno: String(errorNo))
                //: }, repeats: false)
            }, repeats: false)
            //: return
            return
        }
        //: if cmd == "onRequestCall", errorNo == VideoChatErrorCode.MoneyLack.rawValue { // 余额不足
        if cmd == (String(dataPaperEastStr) + String(k_findText)), errorNo == GoBasicType.MoneyLack.rawValue { // 余额不足
            //: stop_randomVideo()
            titleTag()
            //: guard FacultyReactiveCompatible.share.appStatus == AppSkinStatus.normal.rawValue else { return }
            guard FacultyReactiveCompatible.share.appStatus == PenConstantTarget.normal.rawValue else { return }
            //: SmartThen.share.func__jumpToWebRecharge(clickEvent: clickRandomvideoButton, sufficient: false)
            SmartThen.share.bilge(clickEvent: user_likeKey, sufficient: false)
            //: uploadFailTime(type: 1)
            bringCookie(type: 1)
            //: return
            return
        }

        //: restart_randomVideo(cmd: cmd, errno: String(errorNo))
        pursuingErrno(cmd: cmd, errno: String(errorNo))
    }
}

// MARK: - 埋点

//: extension TalkingRandomVideoViewController {
extension LiteralViewController {
    //: func uploadFailTime(type: Int) {
    func bringCookie(type: Int) {
        //: let nowtime = Date().timeIntervalSince1970
        let nowtime = Date().timeIntervalSince1970
        //: let time = Int(nowtime - (beginTime ?? Date().timeIntervalSince1970))
        let time = Int(nowtime - (beginTime ?? Date().timeIntervalSince1970))
        //: var parm = Dictionary<String, Any>()
        var parm = [String: Any]()
        //: parm["failType"] = type
        parm[(String(noti_resTapUrl.prefix(8)))] = type
        //: parm["callWaitingTime"] = time
        parm[(String(dataPlayerMsg) + String(user_costAgeValue))] = time
        //: uploadRecord.uploadRecordEvent(eventID: RandomvideoFailwaitTime, parameterStr: parm)
        kSaveKey.parameterStr(eventID: constChangeStr, parameterStr: parm)
    }

    //: func uploadSuccessTime(toUid: String) {
    func searchion(toUid: String) {
        //: let nowtime = Date().timeIntervalSince1970
        let nowtime = Date().timeIntervalSince1970
        //: let time = Int(nowtime - (beginTime ?? Date().timeIntervalSince1970))
        let time = Int(nowtime - (beginTime ?? Date().timeIntervalSince1970))
        //: var parm = Dictionary<String, Any>()
        var parm = [String: Any]()
        //: parm["callWaitingTime"] = time
        parm[(String(dataPlayerMsg) + String(user_costAgeValue))] = time
        //: parm["toUid"] = toUid
        parm[(String(mainProFormat))] = toUid
        //: uploadRecord.uploadRecordEvent(eventID: RandomvideowaitTime, parameterStr: parm)
        kSaveKey.parameterStr(eventID: kScreenUrl, parameterStr: parm)
    }
}

// MARK: - Layout

//: extension TalkingRandomVideoViewController {
extension LiteralViewController {
    /// 刷新视频通话价格
    //: @objc private func refreshVideoCallPrice() {
    @objc private func soundKeep() {
        //: guard FacultyReactiveCompatible.share.appStatus == AppSkinStatus.normal.rawValue else {
        guard FacultyReactiveCompatible.share.appStatus == PenConstantTarget.normal.rawValue else {
            //: priceStackView.isHidden = true
            priceStackView.isHidden = true
            //: vipPriceBtn.isHidden = true
            vipPriceBtn.isHidden = true
            //: vipTopImgView.isHidden = true
            vipTopImgView.isHidden = true
            //: return
            return
        }

        //: vipIcon.isHidden = !FacultyReactiveCompatible.share.loginUserMode.loungePlus
        vipIcon.isHidden = !FacultyReactiveCompatible.share.loginUserMode.loungePlus
        //: priceStackView.isHidden = false
        priceStackView.isHidden = false
        //: let indexType = EnableFreeCallType.randow
        let indexType = UnderlyingHashableRepresentation.randow
        //: if FacultyReactiveCompatible.share.loginUserMode.freeCallTimes > 0 && FacultyReactiveCompatible.share.appUserConfigMode.enableFreeCallType & indexType.rawValue == indexType.rawValue {
        if FacultyReactiveCompatible.share.loginUserMode.freeCallTimes > 0, FacultyReactiveCompatible.share.appUserConfigMode.enableFreeCallType & indexType.rawValue == indexType.rawValue { // 有免费次数
            //: if FacultyReactiveCompatible.share.appUserConfigMode.callBtnStyle == 1 {
            if FacultyReactiveCompatible.share.appUserConfigMode.callBtnStyle == 1 {
                //: priceLab.text = "Free 1 min".localized
                priceLab.text = (String(notiSpecKey) + String(constStaffPath)).localized
                //: } else {
            } else {
                //: priceStackView.isHidden = true
                priceStackView.isHidden = true
            }
            //: vipPriceBtn.isHidden = true
            vipPriceBtn.isHidden = true
            //: vipTopImgView.isHidden = true
            vipTopImgView.isHidden = true

            //: } else {
        } else { // 无免费次数, 展示价格
            //: let price = FacultyReactiveCompatible.share.loginUserMode.loungePlus ? vipFee:fee
            let price = FacultyReactiveCompatible.share.loginUserMode.loungePlus ? vipFee : fee
            //: priceLab.text = "%@/min".localizedArguments(price)
            priceLab.text = (String(appNarrowMsg.suffix(6))).extraPropertyLetter(price)

            //: let attr = NSMutableAttributedString(string: "%@/min".localizedArguments(price), attributes: [.strikethroughStyle : NSUnderlineStyle.single.rawValue, .foregroundColor: UIColor.white.withAlphaComponent(0.5), .font: UIFont.pingfangMediumFont(fontSize: 10)])
            let attr = NSMutableAttributedString(string: (String(appNarrowMsg.suffix(6))).extraPropertyLetter(price), attributes: [.strikethroughStyle: NSUnderlineStyle.single.rawValue, .foregroundColor: UIColor.white.withAlphaComponent(0.5), .font: UIFont.originally(fontSize: 10)])
            //: norPriceLab.attributedText = attr
            norPriceLab.attributedText = attr
            //: vipPriceBtn.isHidden = FacultyReactiveCompatible.share.loginUserMode.loungePlus
            vipPriceBtn.isHidden = FacultyReactiveCompatible.share.loginUserMode.loungePlus
            //: vipTopImgView.isHidden = FacultyReactiveCompatible.share.loginUserMode.loungePlus
            vipTopImgView.isHidden = FacultyReactiveCompatible.share.loginUserMode.loungePlus

            //: priceStackView.isHidden = !FacultyReactiveCompatible.share.loginUserMode.loungePlus
            priceStackView.isHidden = !FacultyReactiveCompatible.share.loginUserMode.loungePlus
        }
    }

    /// 添加视图
    //: private func setupSubviews() {
    private func blindSide() {
        //: view.addSubview(renderView)
        view.addSubview(renderView)
        //: view.addSubview(gradientBtn)
        view.addSubview(gradientBtn)
        //: view.addSubview(svgaPlayer)
        view.addSubview(svgaPlayer)
        //: view.addSubview(tipsLab)
        view.addSubview(tipsLab)
        //: view.addSubview(chattingNumBtn)
        view.addSubview(chattingNumBtn)
        //: view.addSubview(priceStackView)
        view.addSubview(priceStackView)
        //: priceStackView.addArrangedSubview(vipIcon)
        priceStackView.addArrangedSubview(vipIcon)
        //: priceStackView.addArrangedSubview(goldCoinIcon)
        priceStackView.addArrangedSubview(goldCoinIcon)
        //: priceStackView.addArrangedSubview(priceLab)
        priceStackView.addArrangedSubview(priceLab)
        //: view.addSubview(vipPriceBtn)
        view.addSubview(vipPriceBtn)
        //: view.addSubview(vipTopImgView)
        view.addSubview(vipTopImgView)
        //: vipPriceBtn.addSubview(vipPriceImg)
        vipPriceBtn.addSubview(vipPriceImg)
        //: vipPriceBtn.addSubview(vipPriceLab)
        vipPriceBtn.addSubview(vipPriceLab)
        //: vipPriceBtn.addSubview(norPriceLab)
        vipPriceBtn.addSubview(norPriceLab)

        //: view.addSubview(closeBtn)
        view.addSubview(closeBtn)
        //: view.addSubview(connectingImg)
        view.addSubview(connectingImg)
    }

    /// 添加约束
    //: private func setupSubViewsConstraint() {
    private func sayConstraint() {
        //: renderView.snp.makeConstraints { make in
        renderView.snp.makeConstraints { make in
            //: make.edges.equalToSuperview()
            make.edges.equalToSuperview()
        }
        //: gradientBtn.snp.makeConstraints { make in
        gradientBtn.snp.makeConstraints { make in
            //: make.edges.equalToSuperview()
            make.edges.equalToSuperview()
        }
        //: svgaPlayer.snp.makeConstraints { make in
        svgaPlayer.snp.makeConstraints { make in
            //: make.leading.equalTo(-8)
            make.leading.equalTo(-8)
            //: make.trailing.equalTo(8)
            make.trailing.equalTo(8)
            //: make.centerY.equalToSuperview()
            make.centerY.equalToSuperview()
            //: make.height.equalTo(actualWidth(w: 395))
            make.height.equalTo(actualWidth(w: 395))
        }
        //: tipsLab.snp.makeConstraints { make in
        tipsLab.snp.makeConstraints { make in
            //: make.bottom.equalTo(svgaPlayer.snp.bottom).offset(-40)
            make.bottom.equalTo(svgaPlayer.snp.bottom).offset(-40)
            //: make.width.centerX.equalToSuperview()
            make.width.centerX.equalToSuperview()
        }
        //: chattingNumBtn.snp.makeConstraints { make in
        chattingNumBtn.snp.makeConstraints { make in
            //: make.top.equalTo(tipsLab.snp.bottom).offset(35)
            make.top.equalTo(tipsLab.snp.bottom).offset(35)
            //: make.centerX.equalToSuperview()
            make.centerX.equalToSuperview()
        }

        //: priceStackView.snp.makeConstraints { make in
        priceStackView.snp.makeConstraints { make in
            //: make.top.equalTo(chattingNumBtn.snp.bottom).offset(8)
            make.top.equalTo(chattingNumBtn.snp.bottom).offset(8)
            //: make.centerX.equalToSuperview()
            make.centerX.equalToSuperview()
            //: make.height.equalTo(31)
            make.height.equalTo(31)
            //: make.trailing.lessThanOrEqualToSuperview()
            make.trailing.lessThanOrEqualToSuperview()
        }
        //: vipIcon.snp.makeConstraints { make in
        vipIcon.snp.makeConstraints { make in
            //: make.width.height.equalTo(15)
            make.width.height.equalTo(15)
        }
        //: goldCoinIcon.snp.makeConstraints { make in
        goldCoinIcon.snp.makeConstraints { make in
            //: make.width.height.equalTo(actualWidth(w: 15))
            make.width.height.equalTo(actualWidth(w: 15))
        }
        //: priceLab.snp.makeConstraints { make in
        priceLab.snp.makeConstraints { make in
            //: make.height.equalToSuperview()
            make.height.equalToSuperview()
        }

        //: vipPriceBtn.snp.makeConstraints { make in
        vipPriceBtn.snp.makeConstraints { make in
            //: make.top.equalTo(priceStackView.snp.bottom).offset(16)
            make.top.equalTo(priceStackView.snp.bottom).offset(16)
            //: make.centerX.equalToSuperview()
            make.centerX.equalToSuperview()
            //: make.size.equalTo(CGSize(width: actualWidth(w: 240), height: actualWidth(w: 54)))
            make.size.equalTo(CGSize(width: actualWidth(w: 240), height: actualWidth(w: 54)))
        }
        //: vipTopImgView.snp.makeConstraints { make in
        vipTopImgView.snp.makeConstraints { make in
            //: make.top.equalTo(vipPriceBtn.snp.top).offset(-12)
            make.top.equalTo(vipPriceBtn.snp.top).offset(-12)
            //: make.leading.equalTo(vipPriceBtn.snp.leading)
            make.leading.equalTo(vipPriceBtn.snp.leading)
            //: make.size.equalTo(CGSize(width: actualWidth(w: 32), height: actualWidth(w: 24)))
            make.size.equalTo(CGSize(width: actualWidth(w: 32), height: actualWidth(w: 24)))
        }

        //: vipPriceImg.snp.makeConstraints { make in
        vipPriceImg.snp.makeConstraints { make in
            //: make.top.equalToSuperview().offset(actualWidth(w: 12))
            make.top.equalToSuperview().offset(actualWidth(w: 12))
            //: make.width.height.equalTo(15)
            make.width.height.equalTo(15)
            //: make.trailing.equalTo(vipPriceLab.snp.leading).offset(-2)
            make.trailing.equalTo(vipPriceLab.snp.leading).offset(-2)
        }

        //: vipPriceLab.snp.makeConstraints { make in
        vipPriceLab.snp.makeConstraints { make in
            //: make.centerY.equalTo(vipPriceImg)
            make.centerY.equalTo(vipPriceImg)
            //: make.centerX.equalToSuperview().offset(8)
            make.centerX.equalToSuperview().offset(8)
        }
        //: norPriceLab.snp.makeConstraints { make in
        norPriceLab.snp.makeConstraints { make in
            //: make.top.equalTo(vipPriceImg.snp.bottom).offset(5)
            make.top.equalTo(vipPriceImg.snp.bottom).offset(5)
            //: make.centerX.equalToSuperview()
            make.centerX.equalToSuperview()
        }

        //: closeBtn.snp.makeConstraints { make in
        closeBtn.snp.makeConstraints { make in
            //: make.top.equalTo(StatusBarHeight+6)
            make.top.equalTo(show_textNextFormat + 6)
            //: make.trailing.equalTo(-10)
            make.trailing.equalTo(-10)
            //: make.width.height.equalTo(actualWidth(w: 44))
            make.width.height.equalTo(actualWidth(w: 44))
        }

        //: connectingImg.snp.makeConstraints { make in
        connectingImg.snp.makeConstraints { make in
            //: make.center.equalToSuperview()
            make.center.equalToSuperview()
            //: make.size.equalTo(CGSize(width: actualWidth(w: 262), height: actualWidth(w: 102)))
            make.size.equalTo(CGSize(width: actualWidth(w: 262), height: actualWidth(w: 102)))
        }
    }

    /// 绑定事件
    //: private func bindInteraction() {
    private func feedbackStreet() {
        // index/getconfig接口回调刷新UI
        //: NotificationCenter.default.addObserver(self,
        NotificationCenter.default.addObserver(self,
                                               //: selector: #selector(refreshVideoCallPrice),
                                               selector: #selector(soundKeep),
                                               //: name: UPDATE_INDEX_GETCONFIG_NOTIFICATION,
                                               name: appInputListValue,
                                               //: object: nil)
                                               object: nil)
        // 个人信息更新成功刷新UI
        //: NotificationCenter.default.addObserver(self,
        NotificationCenter.default.addObserver(self,
                                               //: selector: #selector(req_updateVIPFee),
                                               selector: #selector(restoreThroughLegalFee),
                                               //: name: USER_GETMYINFO_SUCCEED_NOTIFICATION,
                                               name: appDescriptionKey,
                                               //: object: nil)
                                               object: nil)

        //: NotificationCenter.default.addObserver(self, selector: #selector(closeTool), name: MATCHVIDEOCALL_END_NOTIFICATION, object: nil)
        NotificationCenter.default.addObserver(self, selector: #selector(flush), name: show_lastTableValue, object: nil)
    }
}
