
//: Declare String Begin

/*: "mf/business/giftList" :*/
fileprivate let constDescendName:String = "function privacy tillmf/b"
fileprivate let user_holderOnstKey:String = "destroy remainingess/gif"
fileprivate let appAdCrowMsg:[Character] = ["t","L","i","s","t"]

/*: "type" :*/
fileprivate let noti_guideName:[UInt8] = [0xdb,0xd6,0xdf,0xca]

/*: "mf/chatRoom/giftList" :*/
fileprivate let app_mixKey:[Character] = ["m","f","/","c","h","a","t"]
fileprivate let show_exitUrl:String = "cell pass demandRoom/g"

/*: "live/giftList" :*/
fileprivate let user_norName:String = "lpatentve"
fileprivate let noti_micReportedTitle:String = "sseek"

/*: "IM/getMessageList" :*/
fileprivate let k_runData:String = "since recipeIM/ge"
fileprivate let k_afterMessage:String = "clock rangegeLi"
fileprivate let const_duringFormat:String = "who"

/*: "mf/business/sendMsg" :*/
fileprivate let const_penNighTitle:String = "mf/buhormone mm click"
fileprivate let dataFrontPath:String = "mask east acquire periodss/send"
fileprivate let k_withinId:String = "last maker helpMsg"

/*: "mf/chatRoom/sendMsg" :*/
fileprivate let showPipePath:String = "album problem alongside bitmf/cha"
fileprivate let noti_upperUrl:String = "sstroke"
fileprivate let kSubBoltFormat:[Character] = ["n","d","M","s","g"]

/*: "mf/business/sendGift" :*/
fileprivate let userAboutData:[Character] = ["m","f","/","b","u","s","i","n","e","s","s","/","s","e","n","d","G","i","f"]
fileprivate let app_cosName:String = "uniform"

/*: "mf/chatRoom/sendGift" :*/
fileprivate let userPairVidMarchContent:String = "lay automatic remarkmf/c"
fileprivate let noti_iqOperatingKey:String = "oom/senunusual eye long currently"
fileprivate let dataCaptureFormat:[Character] = ["d","G","i","f","t"]

/*: "live/sendGift" :*/
fileprivate let mainCommitteeStr:[Character] = ["l","i","v","e","/","s","e","n","d","G","i"]
fileprivate let userInstanceBottomData:[Character] = ["f","t"]

/*: "mf/user/getChatInfo" :*/
fileprivate let user_unusualUrl:String = "mf/usefoot blind apply three mp"
fileprivate let app_exceptionKey:String = "r/gebrother walk"
fileprivate let k_betweenMsg:String = "response sea invitation haulInfo"

/*: "IM/getMessageUserInfoList" :*/
fileprivate let data_partyName:String = "satisfy end address song quitIM/ge"
fileprivate let const_intoValue:String = "black little girl commit performageUse"
fileprivate let main_teamData:String = "isreason"

/*: "mf/crush/whoLikeMeList" :*/
fileprivate let noti_menuValue:String = "mf/cparty cover common"
fileprivate let main_instructionContent:String = "/whoLwest degree shore lose in"
fileprivate let user_weekMsg:String = "evaluate playeList"

/*: "mf/crush/ignore" :*/
fileprivate let app_resValue:String = "grade mystery fall fixed dragmf/cru"
fileprivate let constScreenData:String = "sh/ipick business guard"

/*: "mf/crush/like" :*/
fileprivate let mainVoiceData:[Character] = ["m","f","/","c","r","u","s","h","/"]
fileprivate let data_stageMeetingValue:String = "likfeature"

/*: "mf/user/getRelation" :*/
fileprivate let showTimeValue:String = "inner sum heritagemf/user"
fileprivate let app_operatingData:[Character] = ["/","g","e","t","R","e","l","a","t","i","o","n"]

/*: "user/attention" :*/
fileprivate let noti_extraName:[UInt8] = [0x5,0x3,0x15,0x2,0x5f,0x11,0x4,0x4,0x15,0x1e,0x4,0x19,0x1f,0x1e]

private func ballClub(draw num: UInt8) -> UInt8 {
    return num ^ 112
}

/*: "user/removeAttention" :*/
fileprivate let user_bucketValue:[Character] = ["u","s","e","r","/","r","e","m","o","v","e","A","t","t"]
fileprivate let appResponseFormat:[Character] = ["e","n","t","i","o","n"]

/*: "follow Success" :*/
fileprivate let notiConstraintName:String = "normallyllow"
fileprivate let user_containText:String = "link"

/*: "unfollow Success" :*/
fileprivate let constRegularValue:[Character] = ["u","n","f","o","l"]
fileprivate let app_assistantId:[Character] = ["l","o","w"," ","S","u"]
fileprivate let show_trustBeanValue:[Character] = ["c","c","e","s","s"]

/*: "translate/msg" :*/
fileprivate let userQuitStr:String = "TRANSLATE"
fileprivate let k_borderName:String = "don scan currently/msg"

/*: "msgId" :*/
fileprivate let show_styleMessage:[UInt8] = [0x64,0x49,0x67,0x73,0x6d]

/*: "targetLng" :*/
fileprivate let data_keepTitle:[UInt8] = [0xa3,0xb6,0xa5,0xb0,0xb2,0xa3,0x9b,0xb9,0xb0]

/*: "mf/greet/getAutoGreetContent" :*/
fileprivate let kHostRemoteKey:String = "mf/galbum warn big constant"
fileprivate let user_nothingKey:String = "getAutoGrdraw provider transfer"
fileprivate let constPocketName:String = "eetConitem like bag encouraging"

/*: "mf/greet/getIntimatePhoto" :*/
fileprivate let showHelloPath:String = "certain now dialogmf/gre"
fileprivate let showInstructionJoinUrl:String = "tIntinothing letter according cancel upon"
fileprivate let k_readerValue:String = "bulletto"

/*: "toUid" :*/
fileprivate let show_suspendMessage:[UInt8] = [0x5c,0x47,0x7d,0x41,0x4c]

private func gestureThree(succeed num: UInt8) -> UInt8 {
    return num ^ 40
}

/*: "mf/chatRoom/info" :*/
fileprivate let user_approachId:[Character] = ["m","f","/","c","h","a","t","R"]
fileprivate let constHeavyName:[Character] = ["o","o","m","/","i","n","f","o"]

/*: "mf/chatRoom/list" :*/
fileprivate let dataAcrossContent:[Character] = ["m","f","/","c","h","a"]
fileprivate let appScribeAttributeData:[Character] = ["t","R","o","o","m","/","l","i","s","t"]

/*: "roomId" :*/
fileprivate let constScriptId:[UInt8] = [0x64,0x49,0x6d,0x6f,0x6f,0x72]

/*: "needExtraInfo" :*/
fileprivate let data_winterFormat:[UInt8] = [0x82,0x89,0x89,0x88,0xa9,0x94,0x98,0x9e,0x8d,0xa5,0x82,0x8a,0x83]

/*: "mf/chatRoom/check" :*/
fileprivate let kCollectZzData:[Character] = ["m","f","/","c","h","a","t","R"]
fileprivate let main_lawnContent:String = "oom/checkdetect mini regular"

/*: "mf/chatRoom/joinOrQuit" :*/
fileprivate let user_timeFormat:[Character] = ["m","f","/","c","h","a","t","R","o","o","m","/","j","o"]
fileprivate let main_cramText:String = "bronze"
fileprivate let main_flashCompareAbsTitle:[Character] = ["O","r","Q","u","i","t"]

/*: "mf/chatRoom/members" :*/
fileprivate let appSuccessName:String = "maker resource coal instance skipmf/ch"
fileprivate let notiPlantKey:String = "relieve unless flowatR"
fileprivate let show_offerPath:String = "day directoroom/"

/*: "mf/user/queryStatus" :*/
fileprivate let show_clearlyValue:[Character] = ["m","f","/","u","s","e"]
fileprivate let mainGalleryName:String = "chance enabler/qu"
fileprivate let userCoverTakeName:String = "tdarkus"

/*: "toUids" :*/
fileprivate let app_pageValue:[UInt8] = [0x4d,0x48,0x2e,0x42,0x3d,0x4c]

fileprivate func doingAnalyze(nearby num: UInt8) -> UInt8 {
    let value = Int(num) + 39
    if value > 255 {
        return UInt8(value - 256)
    } else {
        return UInt8(value)
    }
}

/*: "call/getCallTab" :*/
fileprivate let show_ownHelloSourceUrl:String = "pack bit sequencecall/"
fileprivate let constStackKey:String = "numb midlTab"

/*: "mf/user/remark" :*/
fileprivate let showValidWithinName:String = "mill extra construct tool outsidemf/"
fileprivate let notiPassingPath:[Character] = ["u","s","e","r","/","r","e","m","a","r","k"]

/*: "content" :*/
fileprivate let mainFinishText:[UInt8] = [0x42,0x4e,0x4f,0x55,0x44,0x4f,0x55]

private func arrayUpper(multi num: UInt8) -> UInt8 {
    return num ^ 33
}

/*: "top" :*/
fileprivate let app_draftPath:[UInt8] = [0x70,0x6f,0x74]

//: Declare String End

// __DEBUG__
// __CLOSE_PRINT__
//
//  PouchReactiveCompatible.swift
//  AbroadTalking
//
//  Created by Charlotte on 2022/9/7.
//

//: import UIKit
import UIKit

//: public class TalkingChatRequestTool: NSObject {
public class PouchReactiveCompatible: NSObject {
    //: public typealias RequestEndBlock = (_ t: Bool) -> Void
    public typealias RequestEndBlock = (_ t: Bool) -> Void
    //: typealias FinishBlocks = (_ succeed: Bool, _ result: NSDictionary) -> Void
    typealias FinishBlocks = (_ succeed: Bool, _ result: NSDictionary) -> Void
    //: public typealias RequestMsgUserListBlock = (_ succeed: Bool, _ dataArr: [ConsumerPenHandyJSON]) -> Void
    public typealias RequestMsgUserListBlock = (_ succeed: Bool, _ dataArr: [ConsumerPenHandyJSON]) -> Void

    /// 私信礼物面板
    /// - Parameters:
    ///   - type: 0=默认， 1=私密照片，2=私密视频,3=节目单
    ///   - completion: 回调
    //: class func req_ChatGiftList(type: String, completion: @escaping FinishBlock) {
    class func chatPerformCompletion(type: String, completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = AgileCrucialTransformer()
        //: reqModel.requestPath = "mf/business/giftList"
        reqModel.requestPath = (String(constDescendName.suffix(4)) + "usin" + String(user_holderOnstKey.suffix(7)) + String(appAdCrowMsg))
        //: reqModel.params = ["type": type]
        reqModel.params = [String(bytes: noti_guideName.map{$0^175}, encoding: .utf8)!: type]
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        appAdjustData.kitEspecial(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 聊天室礼物面板
    /// - Parameters：
    ///   - completion: 回调
    //: class func req_ChatRoomGiftList(type: String, completion: @escaping FinishBlock) {
    class func storageConversation(type _: String, completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = AgileCrucialTransformer()
        //: reqModel.requestPath = "mf/chatRoom/giftList"
        reqModel.requestPath = (String(app_mixKey) + String(show_exitUrl.suffix(6)) + "iftList")
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        appAdjustData.kitEspecial(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 直播礼物面板
    /// - Parameters：
    ///   - completion: 回调
    //: class func req_LiveGiftList(type: String, completion: @escaping FinishBlock) {
    class func headingCompletion(type _: String, completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = AgileCrucialTransformer()
        //: reqModel.requestPath = "live/giftList"
        reqModel.requestPath = (user_norName.replacingOccurrences(of: "patent", with: "i") + "/giftLi" + noti_micReportedTitle.replacingOccurrences(of: "seek", with: "t"))
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        appAdjustData.kitEspecial(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 请求系统消息
    /// - Parameter completion: 回调
    //: @objc class func req_sysMessageList(completion: @escaping FinishBlocks) {
    @objc class func selectlistCompletion(completion: @escaping FinishBlocks) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = AgileCrucialTransformer()
        //: reqModel.requestPath = "IM/getMessageList"
        reqModel.requestPath = (String(k_runData.suffix(5)) + "tMessa" + String(k_afterMessage.suffix(4)) + const_duringFormat.replacingOccurrences(of: "who", with: "st"))
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        appAdjustData.kitEspecial(model: reqModel) { succeed, result, _ in
            //: if succeed {
            if succeed {
                //: completion(true, result as! NSDictionary)
                completion(true, result as! NSDictionary)
            }
        }
    }

    /// 私聊发送消息
    /// - Parameters:
    ///   - param: 传参
    ///   - completion: 回调
    //: class func req_SendPrivete(param: Dictionary<String, Any>, completion: @escaping FinishBlock) {
    class func modeNetwork(param: [String: Any], completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = AgileCrucialTransformer()
        //: reqModel.requestType = .POST
        reqModel.requestType = .POST
        //: reqModel.requestPath = "mf/business/sendMsg"
        reqModel.requestPath = (String(const_penNighTitle.prefix(5)) + "sine" + String(dataFrontPath.suffix(7)) + String(k_withinId.suffix(3)))
        //: reqModel.params = param
        reqModel.params = param
        //: reqModel.showErrorStatusBar = false
        reqModel.showErrorStatusBar = false
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        appAdjustData.kitEspecial(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 公共聊天室发送消息
    /// - Parameters:
    ///   - param: 传参
    ///   - completion: 回调
    //: class func req_SendRoomMsg(param: Dictionary<String, Any>, completion: @escaping FinishBlock) {
    class func beginningBeauty(param: [String: Any], completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel()
        let reqModel = AgileCrucialTransformer()
        //: reqModel.requestType = .POST
        reqModel.requestType = .POST
        //: reqModel.requestPath = "mf/chatRoom/sendMsg"
        reqModel.requestPath = (String(showPipePath.suffix(6)) + "tRoom/" + noti_upperUrl.replacingOccurrences(of: "stroke", with: "e") + String(kSubBoltFormat))
        //: reqModel.params = param
        reqModel.params = param
        //: reqModel.showErrorStatusBar = false
        reqModel.showErrorStatusBar = false
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        appAdjustData.kitEspecial(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 送礼物
    /// - Parameters:
    ///   - param: 参数
    ///   - completion: 回调
    //: class func req_SendGiftPrivete( param: Dictionary<String, Any>, completion: @escaping FinishBlock) {
    class func streetSmartCompletion(param: [String: Any], completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = AgileCrucialTransformer()
        //: reqModel.requestType = .POST
        reqModel.requestType = .POST
        //: reqModel.requestPath = "mf/business/sendGift"
        reqModel.requestPath = (String(userAboutData) + app_cosName.replacingOccurrences(of: "uniform", with: "t"))
        //: reqModel.params = param
        reqModel.params = param
        //: reqModel.showErrorStatusBar = false
        reqModel.showErrorStatusBar = false
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        appAdjustData.kitEspecial(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 聊天室送礼物
    /// - Parameters:
    ///   - param: 参数
    ///   - completion: 回调
    //: class func req_SendGiftChatRoom( param: Dictionary<String, Any>, completion: @escaping FinishBlock) {
    class func consequence(param: [String: Any], completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = AgileCrucialTransformer()
        //: reqModel.requestType = .POST
        reqModel.requestType = .POST
        //: reqModel.requestPath = "mf/chatRoom/sendGift"
        reqModel.requestPath = (String(userPairVidMarchContent.suffix(4)) + "hatR" + String(noti_iqOperatingKey.prefix(7)) + String(dataCaptureFormat))
        //: reqModel.params = param
        reqModel.params = param
        //: reqModel.showErrorStatusBar = false
        reqModel.showErrorStatusBar = false
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        appAdjustData.kitEspecial(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 直播送礼物
    /// - Parameters:
    ///   - param: 参数
    ///   - completion: 回调
    //: class func req_SendGiftLive( param: Dictionary<String, Any>, completion: @escaping FinishBlock) {
    class func make(param: [String: Any], completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = AgileCrucialTransformer()
        //: reqModel.requestType = .POST
        reqModel.requestType = .POST
        //: reqModel.requestPath = "live/sendGift"
        reqModel.requestPath = (String(mainCommitteeStr) + String(userInstanceBottomData))
        //: reqModel.params = param
        reqModel.params = param
        //: reqModel.showErrorStatusBar = false
        reqModel.showErrorStatusBar = false
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        appAdjustData.kitEspecial(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 请求聊天用户信息
    /// - Parameters:
    //: class func req_refreshUserChatInfo( param: Dictionary<String, Any>, completion: @escaping FinishBlock) {
    class func frontEnd(param: [String: Any], completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = AgileCrucialTransformer()
        //: reqModel.requestPath = "mf/user/getChatInfo"
        reqModel.requestPath = (String(user_unusualUrl.prefix(6)) + String(app_exceptionKey.prefix(4)) + "tChat" + String(k_betweenMsg.suffix(4)))
        //: reqModel.params = param
        reqModel.params = param
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        appAdjustData.kitEspecial(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 请求用户消息列表
    /// - Parameters:
    //: @objc class public func req_refreshMsgUserListInfo( param: Dictionary<String, Any>, completion: @escaping RequestMsgUserListBlock) {
    @objc public class func maximumRequireListener(param: [String: Any], completion: @escaping RequestMsgUserListBlock) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = AgileCrucialTransformer()
        //: reqModel.requestPath = "IM/getMessageUserInfoList"
        reqModel.requestPath = (String(data_partyName.suffix(5)) + "tMess" + String(const_intoValue.suffix(6)) + "rInfoL" + main_teamData.replacingOccurrences(of: "reason", with: "t"))
        //: reqModel.params = param
        reqModel.params = param
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        appAdjustData.kitEspecial(model: reqModel) { succeed, result, _ in
            //: guard succeed else {
            guard succeed else {
                //: completion(succeed, [])
                completion(succeed, [])
                //: return
                return
            }

            //: if let datas = Array<ConsumerPenHandyJSON>.deserialize(from: result as? Array) {
            if let datas = Array<ConsumerPenHandyJSON>.deserialize(from: result as? Array) {
                //: completion(succeed, datas as! [ConsumerPenHandyJSON])
                completion(succeed, datas as! [ConsumerPenHandyJSON])
            }
        }
    }

    /// 请求用户消息列表
    //: @objc class public func req_giftEffectDataUrl(giftUrl: String, path: String, completion: @escaping RequestEndBlock) {
    @objc public class func disembarrass(giftUrl: String, path: String, completion: @escaping RequestEndBlock) {
        //: GJ.startGiftZipRequest(giftUrl: giftUrl, path: path) { succeed, result, errorModel in
        appAdjustData.outputCompletion(giftUrl: giftUrl, path: path) { succeed, _, _ in
            //: completion(succeed)
            completion(succeed)
        }
    }

    /// 谁心动我 - 列表
    /// - Parameters:
    ///   - completion: 回调
    //: class func req_whoLikeMeList(params: Dictionary<String, Any>, completion: @escaping FinishBlock) {
    class func sortBy(params: [String: Any], completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = AgileCrucialTransformer()
        //: reqModel.requestPath = "mf/crush/whoLikeMeList"
        reqModel.requestPath = (String(noti_menuValue.prefix(4)) + "rush" + String(main_instructionContent.prefix(5)) + "ikeM" + String(user_weekMsg.suffix(5)))
        //: reqModel.params = params
        reqModel.params = params
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        appAdjustData.kitEspecial(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 谁心动我 - nope拒绝操作
    /// - Parameters:
    ///   - completion: 回调
    //: class func req_whoLikeMeIgnore(params: Dictionary<String, Any>, completion: @escaping FinishBlock) {
    class func saintJohn(params: [String: Any], completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = AgileCrucialTransformer()
        //: reqModel.requestPath = "mf/crush/ignore"
        reqModel.requestPath = (String(app_resValue.suffix(6)) + String(constScreenData.prefix(4)) + "gnore")
        //: reqModel.params = params
        reqModel.params = params
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        appAdjustData.kitEspecial(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 谁心动我 - like操作
    /// - Parameters:
    ///   - completion: 回调
    //: class func req_whoLikeMelike(params: Dictionary<String, Any>, completion: @escaping FinishBlock) {
    class func cellMark(params: [String: Any], completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = AgileCrucialTransformer()
        //: reqModel.requestPath = "mf/crush/like"
        reqModel.requestPath = (String(mainVoiceData) + data_stageMeetingValue.replacingOccurrences(of: "feature", with: "e"))
        //: reqModel.params = params
        reqModel.params = params
        //: reqModel.requestType = .POST
        reqModel.requestType = .POST
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        appAdjustData.kitEspecial(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 关注列表
    /// - Parameters:
    ///   - completion: 回调
    //: class func req_atationList(params: Dictionary<String, Any>, completion: @escaping FinishBlock) {
    class func actual(params: [String: Any], completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = AgileCrucialTransformer()
        //: reqModel.requestPath = "mf/user/getRelation"
        reqModel.requestPath = (String(showTimeValue.suffix(7)) + String(app_operatingData))
        //: reqModel.params = params
        reqModel.params = params
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        appAdjustData.kitEspecial(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 关注操作
    /// - Parameters:
    /// - isAttention: 关注/取消
    ///   - completion: 回调
    //: class func req_atationTool(isAttention: Bool, params: Dictionary<String, Any>, completion: @escaping FinishBlock) {
    class func attentionProgress(isAttention: Bool, params: [String: Any], completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = AgileCrucialTransformer()
        //: let path = isAttention==true ? "user/attention" :"user/removeAttention"
        let path = isAttention == true ? String(bytes: noti_extraName.map{ballClub(draw: $0)}, encoding: .utf8)! : (String(user_bucketValue) + String(appResponseFormat))
        //: reqModel.requestPath = path
        reqModel.requestPath = path
        //: reqModel.params = params
        reqModel.params = params
        //: reqModel.showErrorStatusBar = false
        reqModel.showErrorStatusBar = false
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        appAdjustData.kitEspecial(model: reqModel) { succeed, result, errorModel in
            // 统一处理toast
            //: if succeed {
            if succeed {
                //: if isAttention {
                if isAttention {
                    //: object.func__showStatusBarSuccessMsg(showMsg: "follow Success".localized)
                    noti_assetMessage.pointOfReferenceMedia(showMsg: (notiConstraintName.replacingOccurrences(of: "normally", with: "fo") + " Succes" + user_containText.replacingOccurrences(of: "link", with: "s")).localized)
                    //: } else {
                } else {
                    //: object.func__showStatusBarSuccessMsg(showMsg: "unfollow Success".localized)
                    noti_assetMessage.pointOfReferenceMedia(showMsg: (String(constRegularValue) + String(app_assistantId) + String(show_trustBeanValue)).localized)
                }

                //: } else {
            } else {
                //: object.func__showStatusBarErrorMsg(showMsg: errorModel?.errorMsg ?? "")
                noti_assetMessage.micromicronProcess(showMsg: errorModel?.errorMsg ?? "")
            }
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 文本翻译
    /// - Parameters:
    ///   - msgId: 消息ID
    ///   - type=0表示私聊消息，type=1表示聊天室消息
    ///   - completion: 回调
    //: @objc class public func req_translateMsg(msgId: String, type: Int, completion: @escaping (_ succeed: Bool, _ result: NSDictionary, _ errorCode: Int) -> Void) {
    @objc public class func facility(msgId: String, type: Int, completion: @escaping (_ succeed: Bool, _ result: NSDictionary, _ errorCode: Int) -> Void) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = AgileCrucialTransformer()
        //: reqModel.requestPath = "translate/msg"
        reqModel.requestPath = (userQuitStr.lowercased() + String(k_borderName.suffix(4)))
        //: reqModel.params = ["msgId": msgId, "targetLng": AnimateUnaryAllocator.getSystemLangCode(), "type": "\(type)"]
        reqModel.params = [String(bytes: show_styleMessage.reversed(), encoding: .utf8)!: msgId, String(bytes: data_keepTitle.map{$0^215}, encoding: .utf8)!: AnimateUnaryAllocator.canny(), String(bytes: noti_guideName.map{$0^175}, encoding: .utf8)!: "\(type)"]
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        appAdjustData.kitEspecial(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result as? NSDictionary ?? [:], errorModel?.errorCode ?? 0)
            completion(succeed, result as? NSDictionary ?? [:], errorModel?.errorCode ?? 0)
        }
    }

    /// 获取一键打招呼内容
    ///   - completion: 回调
    //: class func req_getAutoGreetContent(completion: @escaping FinishBlock) {
    class func mugshot(completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = AgileCrucialTransformer()
        //: reqModel.requestPath = "mf/greet/getAutoGreetContent"
        reqModel.requestPath = (String(kHostRemoteKey.prefix(4)) + "reet/" + String(user_nothingKey.prefix(9)) + String(constPocketName.prefix(6)) + "tent")
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        appAdjustData.kitEspecial(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 获取随机私密照片
    ///   - completion: 回调
    //: class func req_getIntimatePhoto(toUid: String, completion: @escaping FinishBlock) {
    class func squelchDownFromCompletionJust(toUid: String, completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = AgileCrucialTransformer()
        //: reqModel.requestPath = "mf/greet/getIntimatePhoto"
        reqModel.requestPath = (String(showHelloPath.suffix(6)) + "et/ge" + String(showInstructionJoinUrl.prefix(5)) + "mateP" + k_readerValue.replacingOccurrences(of: "bullet", with: "ho"))
        //: reqModel.params = ["toUid": toUid]
        reqModel.params = [String(bytes: show_suspendMessage.map{gestureThree(succeed: $0)}, encoding: .utf8)!: toUid]
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        appAdjustData.kitEspecial(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 获取聊天广场在线人数和家族信息
    /// - Parameters:
    ///   - completion: 回调
    //: class func req_getChatRoomInfo(completion: @escaping FinishBlock) {
    class func walkInMinimize(completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = AgileCrucialTransformer()
        //: reqModel.requestPath = "mf/chatRoom/info"
        reqModel.requestPath = (String(user_approachId) + String(constHeavyName))
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        appAdjustData.kitEspecial(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 聊天广场列表
    /// - Parameters:
    ///   - completion: 回调
    //: class func req_getChatRoomList(completion: @escaping FinishBlock) {
    class func putUp(completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = AgileCrucialTransformer()
        //: reqModel.requestPath = "mf/chatRoom/list"
        reqModel.requestPath = (String(dataAcrossContent) + String(appScribeAttributeData))
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        appAdjustData.kitEspecial(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 检测聊天室
    /// - Parameters:
    ///   - roomId: 聊天室Id
    ///   - completion: 回调
    //: class func req_checkChatRoom(roomId: String, completion: @escaping FinishBlock) {
    class func combine(roomId: String, completion: @escaping FinishBlock) {
        //: let params = ["roomId": roomId, "needExtraInfo": "1"]
        let params = [String(bytes: constScriptId.reversed(), encoding: .utf8)!: roomId, String(bytes: data_winterFormat.map{$0^236}, encoding: .utf8)!: "1"]
        //: let reqModel = TalkingRequestModel()
        let reqModel = AgileCrucialTransformer()
        //: reqModel.requestPath = "mf/chatRoom/check"
        reqModel.requestPath = (String(kCollectZzData) + String(main_lawnContent.prefix(9)))
        //: reqModel.params = params
        reqModel.params = params
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        appAdjustData.kitEspecial(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 进入或退出聊天室
    /// - Parameters:
    ///   - params: 参数
    ///   - completion: 回调
    //: class func req_chatRoomJoinOrQuit(params: [String: Any], completion: @escaping FinishBlock) {
    class func feeWeek(params: [String: Any], completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel()
        let reqModel = AgileCrucialTransformer()
        //: reqModel.requestPath = "mf/chatRoom/joinOrQuit"
        reqModel.requestPath = (String(user_timeFormat) + main_cramText.replacingOccurrences(of: "bronze", with: "in") + String(main_flashCompareAbsTitle))
        //: reqModel.params = params
        reqModel.params = params
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        appAdjustData.kitEspecial(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 聊天室在线名单
    /// - Parameters:
    ///   - completion: 回调
    //: class func req_getChatRoomMembers(params: Dictionary<String, Any>, completion: @escaping FinishBlock) {
    class func playAgree(params: [String: Any], completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = AgileCrucialTransformer()
        //: reqModel.requestPath = "mf/chatRoom/members"
        reqModel.requestPath = (String(appSuccessName.suffix(5)) + String(notiPlantKey.suffix(3)) + String(show_offerPath.suffix(4)) + "members")
        //: reqModel.params = params
        reqModel.params = params
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        appAdjustData.kitEspecial(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 批量查询消息列表用户在线状态
    /// - Parameters:
    ///   - toUids: 用户ids
    ///   - completion: 回调
    //: @objc class public func req_queryStatus(toUids: String, completion: @escaping (_ succeed: Bool, _ result: [[String: Any]]?) -> Void) {
    @objc public class func blendOmit(toUids: String, completion: @escaping (_ succeed: Bool, _ result: [[String: Any]]?) -> Void) {
        //: let reqModel = TalkingRequestModel()
        let reqModel = AgileCrucialTransformer()
        //: reqModel.requestType = .POST
        reqModel.requestType = .POST
        //: reqModel.requestPath = "mf/user/queryStatus"
        reqModel.requestPath = (String(show_clearlyValue) + String(mainGalleryName.suffix(4)) + "eryS" + userCoverTakeName.replacingOccurrences(of: "dark", with: "at"))
        //: reqModel.params = ["toUids": toUids, "type": 1]
        reqModel.params = [String(bytes: app_pageValue.map{doingAnalyze(nearby: $0)}, encoding: .utf8)!: toUids, String(bytes: noti_guideName.map{$0^175}, encoding: .utf8)!: 1]
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        appAdjustData.kitEspecial(model: reqModel) { succeed, result, _ in
            //: completion(succeed, result as? [[String: Any]] ?? [])
            completion(succeed, result as? [[String: Any]] ?? [])
        }
    }

    /// 拨打通话记录
    /// - Parameters:
    ///   - completion: 回调
    //: class func req_getCallTab(params: Dictionary<String, Any>, completion: @escaping FinishBlock) {
    class func should(params: [String: Any], completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = AgileCrucialTransformer()
        //: reqModel.requestPath = "call/getCallTab"
        reqModel.requestPath = (String(show_ownHelloSourceUrl.suffix(5)) + "getCal" + String(constStackKey.suffix(4)))
        //: reqModel.requestType = .POST
        reqModel.requestType = .POST
        //: reqModel.params = params
        reqModel.params = params
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        appAdjustData.kitEspecial(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 新增/修改备注
    /// - Parameters:
    ///   - content: 备注内容
    ///   - isTop: 是否置顶，1：是，2：否
    ///   - toUid: 对方uid
    ///   - completion: 回调
    //: class func req_remark(content: String, isTop: Int, toUid: String, completion: @escaping FinishBlock) {
    class func audiogram(content: String, isTop: Int, toUid: String, completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel()
        let reqModel = AgileCrucialTransformer()
        //: reqModel.requestPath = "mf/user/remark"
        reqModel.requestPath = (String(showValidWithinName.suffix(3)) + String(notiPassingPath))
        //: reqModel.requestType = .POST
        reqModel.requestType = .POST
        //: reqModel.params = ["content": content, "top": isTop, "toUid": toUid]
        reqModel.params = [String(bytes: mainFinishText.map{arrayUpper(multi: $0)}, encoding: .utf8)!: content, String(bytes: app_draftPath.reversed(), encoding: .utf8)!: isTop, String(bytes: show_suspendMessage.map{gestureThree(succeed: $0)}, encoding: .utf8)!: toUid]
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        appAdjustData.kitEspecial(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }
}
