
//: Declare String Begin

/*: "msgType" :*/
fileprivate let dataMovementKey:String = "ad window become multiplemsgType"

/*: "tips" :*/
fileprivate let dataAreaName:String = "markips"

/*: "extra" :*/
fileprivate let mainComplexSitKey:String = "extwhole"

/*: "msgInfo" :*/
fileprivate let noti_herLoseValue:String = "bread reply partner capture somemsgInfo"

/*: "user" :*/
fileprivate let mainPickPath:[Character] = ["u","s","e","r"]

/*: "uid" :*/
fileprivate let noti_confirmStr:String = "UID"

/*: "nickname" :*/
fileprivate let dataItUrl:[UInt8] = [0x99,0x9e,0x94,0x9c,0x99,0x96,0x9a,0x92]

private func warningEase(doc num: UInt8) -> UInt8 {
    return num ^ 247
}

/*: "headPic" :*/
fileprivate let kAcrossFormat:[UInt8] = [0x4d,0x40,0x44,0x41,0x75,0x4c,0x46]

private func nearbyInstruction(tool num: UInt8) -> UInt8 {
    return num ^ 37
}

/*: "age" :*/
fileprivate let const_countryName:[UInt8] = [0x65,0x67,0x61]

/*: "gift" :*/
fileprivate let dataOuterFormat:String = "gistock"

/*: "isGroup" :*/
fileprivate let kGivingValue:String = "isGroupresource phase gold"

/*: "WantGift" :*/
fileprivate let constMidMessage:[Character] = ["W","a","n","t","G","i","f","t"]

/*: "txt" :*/
fileprivate let const_deadlineKey:[Character] = ["t","x","t"]

/*: "img" :*/
fileprivate let data_lanceUrl:[Character] = ["i","m","g"]

/*: "video" :*/
fileprivate let noti_stressStr:String = "videbound"

/*: "audio" :*/
fileprivate let mainLabKey:String = "audovalo"

/*: "messageType" :*/
fileprivate let notiStatusId:String = "melarge"
fileprivate let userSayData:[Character] = ["e"]

/*: "contentType" :*/
fileprivate let main_stillWoundUrl:String = "clessnt"
fileprivate let app_computeValue:String = "imagepe"

/*: "ImgMsg" :*/
fileprivate let k_identityStr:String = "alive"
fileprivate let notiAboveValue:String = "mgMsgletter detect reserve"

/*: "imageUri" :*/
fileprivate let noti_vantageText:[Character] = ["i","m","a","g","e","U","r","i"]

/*: "type" :*/
fileprivate let kAcrossName:[Character] = ["t","y","p","e"]

/*: "pic" :*/
fileprivate let dataApproximatelyOkMessage:String = "piboard"

/*: "isBurn" :*/
fileprivate let show_roundStageMessage:[Character] = ["i","s","B","u","r","n"]

/*: "giftId" :*/
fileprivate let kEarnKey:[Character] = ["g","i","f","t","I","d"]

/*: "params" :*/
fileprivate let appWeekAvailableId:String = "paramcare"

/*: "audioLength" :*/
fileprivate let dataCertainValue:String = "aucelli"

/*: "len" :*/
fileprivate let show_incomeStr:String = "lbassn"

/*: "audioSandbox" :*/
fileprivate let dataScribeId:String = "audioSbutton distribution rem asset that"

/*: "senderId" :*/
fileprivate let user_plainKey:String = "ssafetyd"

/*: "AudioMsg" :*/
fileprivate let mainSquareKey:String = "Audiheritage two"

/*: "content" :*/
fileprivate let noti_piMessage:[Character] = ["c","o","n","t","e","n","t"]

/*: "TxtMsg" :*/
fileprivate let showPartBasicFormat:String = "TxtMsggather her"

/*: "message" :*/
fileprivate let data_ridExceptContent:String = "organization"
fileprivate let user_shallTitle:[Character] = ["s","s","a","g","e"]

/*: "quoteMsgId" :*/
fileprivate let appArenaSpirePath:[Character] = ["q","u","o","t","e","M","s"]
fileprivate let app_heMessage:String = "gIdeven command"

/*: "quoteMsgInfo" :*/
fileprivate let appAdvertisingMessage:String = "conversationuot"
fileprivate let dataNorName:String = "eMsgInfoenough spring found escape"

/*: "replayId" :*/
fileprivate let notiSparkPath:[Character] = ["r","e","p","l","a","y","I","d"]

/*: "rYMsgType" :*/
fileprivate let noti_dotKey:[Character] = ["r","Y","M","s","g","T","y","p","e"]

/*: "GJ:Sess:AutoGreet:TxtCustom" :*/
fileprivate let mainMobileUrl:[Character] = ["G","J",":","S","e","s","s",":","A","u","t","o","G","r","e","e","t",":","T","x","t"]
fileprivate let show_universalText:[Character] = ["C","u","s","t","o","m"]

/*: "changeDirection" :*/
fileprivate let showFenceUrl:String = "CHANG"
fileprivate let show_donMessage:[Character] = ["t","i","o","n"]

/*: "system" :*/
fileprivate let userSkipKey:String = "sysmaxm"

/*: "title" :*/
fileprivate let app_connectPath:String = "iontle"

/*: "icon" :*/
fileprivate let data_smoothPath:String = "announcementcon"

/*: "all" :*/
fileprivate let show_centerTitle:String = "carell"

/*: "icon_chats_pcr" :*/
fileprivate let appEnableerPath:[Character] = ["i","c","o","n","_","c","h"]
fileprivate let main_cityKey:String = "previous vertical try insideats_pcr"

/*: "status" :*/
fileprivate let main_recommendationFormat:[Character] = ["s","t","a","t","u","s"]

/*: "msgIncome" :*/
fileprivate let const_calculateKey:[Character] = ["m","s","g","I","n","c"]
fileprivate let appMedicSelectionMessage:String = "omtraffic"

/*: "msgId" :*/
fileprivate let showFindMessage:String = "sufficient expressionmsgId"

/*: "SuiteStretchCount" :*/
fileprivate let const_modelValue:String = "TMsgproject window storage memory"
fileprivate let showWalkKey:String = "lateStsecond expression beginning info"
fileprivate let mainMineFormat:String = "asiblings"

/*: "TMsgTranslatedContent" :*/
fileprivate let constRidTitle:[Character] = ["T","M","s","g","T","r","a","n","s"]
fileprivate let showNearUrl:String = "lataveraged"

/*: "formatType" :*/
fileprivate let userLanceFormat:String = "FORMAT"

/*: "plain" :*/
fileprivate let main_corePath:String = "plshouldin"

/*: "imageData" :*/
fileprivate let noti_expansionText:String = "imageDaobject ta protocol"
fileprivate let show_statinFeeUrl:[Character] = ["t","a"]

/*: "VideoMsg" :*/
fileprivate let app_sectionSendName:[Character] = ["V","i","d"]
fileprivate let k_degreeStr:[Character] = ["e","o","M","s","g"]

/*: "match" :*/
fileprivate let user_connectMsg:String = "mathide"

/*: "userIntro" :*/
fileprivate let appSwitchceGoingUrl:String = "USE"
fileprivate let showPunishUrl:String = "rIntrothrough man"

/*: "targetId" :*/
fileprivate let showEachValue:String = "agent reduce he remaining brushtargetId"

/*: "yyyy-MM-dd HH:mm:ss" :*/
fileprivate let user_entitleStr:String = "path turn very still processyyyy-"
fileprivate let noti_processingName:String = "following reader lose conditionHH:m"

/*: "sendTime" :*/
fileprivate let constPinUrl:[Character] = ["s","e","n","d","T","i","m"]
fileprivate let appDeviceStr:String = "star"

/*: "isRead" :*/
fileprivate let userExtraMsg:[Character] = ["i","s","R","e","a"]
fileprivate let notiFoundMsg:String = "language"

/*: "audioData" :*/
fileprivate let dataSearchionMessage:[Character] = ["a","u","d","i","o","D","a","t","a"]

/*: "audioUri" :*/
fileprivate let notiMatedPath:String = "awithindi"

//: Declare String End

// __DEBUG__
// __CLOSE_PRINT__
//
//  CivilityReactiveCompatible.swift

//: import Foundation
import Foundation

// MARK: - 枚举定义

//: @objc public enum TXMessageContentType: UInt {
@objc public enum AggregationQuickLookable: UInt {
    //: case TXMessageContentType_text = 1
    case TXMessageContentType_text = 1
    //: case TXMessageContentType_picture
    case TXMessageContentType_picture
    //: case TXMessageContentType_gift
    case TXMessageContentType_gift
    //: case TXMessageContentType_askforGift
    case TXMessageContentType_askforGift
    //: case TXMessageContentType_voice
    case TXMessageContentType_voice
    //: case TXMessageContentType_local
    case TXMessageContentType_local
    //: case TXMessageContentType_tips
    case TXMessageContentType_tips
    //: case TXMessageContentType_userCard
    case TXMessageContentType_userCard
    //: case TXMessageContentType_video
    case TXMessageContentType_video
}

// MARK: - CivilityReactiveCompatible

//: @objcMembers public class TUISocialChatManager: NSObject {
@objcMembers public class CivilityReactiveCompatible: NSObject {
    // MARK: - 公共方法

    /// 获取消息插入的时间戳 毫秒
    //: public class func getMessageInsertTime() -> Int64 {
    public class func enquiryProductInstance() -> Int64 {
        //: let curDate = Date().timeIntervalSince1970
        let curDate = Date().timeIntervalSince1970
        //: let msInterval = Int64(floor(curDate * 1000))
        let msInterval = Int64(floor(curDate * 1000))
        //: var deltaTime = Int64(V2TIMManager.sharedInstance().getServerTime())
        var deltaTime = Int64(V2TIMManager.sharedInstance().getServerTime()) // 秒为单位
        //: if deltaTime < 1 {
        if deltaTime < 1 {
            //: deltaTime = 0
            deltaTime = 0
        }
        //: let timeStamp = msInterval - deltaTime
        let timeStamp = msInterval - deltaTime
        //: return timeStamp
        return timeStamp
    }

    // MARK: - 腾讯IM消息处理

    /// 腾讯IM消息插入
    //: public class func insertTXMessage(withExtral extral: [String: Any], toUid: String, isSender: Bool, isTip: Bool) -> AgoneCellData? {
    public class func effect(withExtral extral: [String: Any], toUid: String, isSender: Bool, isTip: Bool) -> AgoneCellData? {
        //: let data = handleTXMessageSend(withExtral: extral, isSender: isSender, isTip: isTip)
        let data = smartMailSenderBlockTipHandleTurn(withExtral: extral, isSender: isSender, isTip: isTip)
        //: if let data = data {
        if let data = data {
            //: ListenLayoutDelivery.shared.func__handleMsg(targetId: toUid, msgCellData: data, isPrivete: true)
            ListenLayoutDelivery.shared.disposeOf(targetId: toUid, msgCellData: data, isPrivete: true)
        }
        //: return data
        return data
    }

    /// 腾讯IM发送消息组装
    //: public class func handleTXMessageSend(withExtral extral: [String: Any], isSender: Bool, isTip: Bool) -> AgoneCellData? {
    public class func smartMailSenderBlockTipHandleTurn(withExtral extral: [String: Any], isSender: Bool, isTip: Bool) -> AgoneCellData? {
        //: if isTip {
        if isTip {
            //: var extralDict = extral
            var extralDict = extral
            //: extralDict["msgType"] = "tips"
            extralDict[(String(dataMovementKey.suffix(7)))] = (dataAreaName.replacingOccurrences(of: "mark", with: "t"))
            //: let extraDict = ["extra": extralDict]
            let extraDict = [(mainComplexSitKey.replacingOccurrences(of: "whole", with: "ra")): extralDict]
            //: let extra = parseTXMessageJsonStr(data: extraDict)
            let extra = situation(data: extraDict)
            //: let data = TalkingChatTipsMsgCellData(direction: isSender ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
            let data = ComposableAnimatedPromise(direction: isSender ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
            //: let cData = extra.data(using: .utf8)
            let cData = extra.data(using: .utf8)
            //: data.innerMessage = V2TIMManager.sharedInstance().createCustomMessage(cData)
            data.innerMessage = V2TIMManager.sharedInstance().createCustomMessage(cData)
            //: data.innerMessage.isExcludedFromLastMessage = true
            data.innerMessage.isExcludedFromLastMessage = true
            //: let model = AbTalkingChatMsgBaseModel()
            let model = ReadModel()
            //: if let tipModel = try? AbTalkingChatMsgTipsModel(dictionary: extralDict) {
            if let tipModel = try? OccurModel(dictionary: extralDict) {
                //: model.tips = tipModel
                model.tips = tipModel
            }
            //: data.msgModel = model
            data.msgModel = model
            //: return data
            return data
        }

        //: var tempDict = extral
        var tempDict = extral
        //: guard let msgInfo = extral["msgInfo"] as? [String: Any] else {
        guard let msgInfo = extral[(String(noti_herLoseValue.suffix(7)))] as? [String: Any] else {
            //: return nil
            return nil
        }

        //: tempDict["user"] = [
        tempDict[(String(mainPickPath))] = [
            //: "uid": FacultyReactiveCompatible.share.loginUserMode.userID,
            (noti_confirmStr.lowercased()): FacultyReactiveCompatible.share.loginUserMode.userID,
            //: "nickname": FacultyReactiveCompatible.share.loginUserMode.nickname,
            String(bytes: dataItUrl.map{warningEase(doc: $0)}, encoding: .utf8)!: FacultyReactiveCompatible.share.loginUserMode.nickname,
            //: "headPic": FacultyReactiveCompatible.share.loginUserMode.headPic,
            String(bytes: kAcrossFormat.map{nearbyInstruction(tool: $0)}, encoding: .utf8)!: FacultyReactiveCompatible.share.loginUserMode.headPic,
            //: "age": FacultyReactiveCompatible.share.loginUserMode.age,
            String(bytes: const_countryName.reversed(), encoding: .utf8)!: FacultyReactiveCompatible.share.loginUserMode.age,
        ]

        //: let extraDict = ["extra": tempDict]
        let extraDict = [(mainComplexSitKey.replacingOccurrences(of: "whole", with: "ra")): tempDict]
        //: let extraString = parseTXMessageJsonStr(data: extraDict)
        let extraString = situation(data: extraDict)

        // 礼物
        //: if msgInfo["msgType"] as? String == "gift" {
        if msgInfo[(String(dataMovementKey.suffix(7)))] as? String == (dataOuterFormat.replacingOccurrences(of: "stock", with: "ft")) {
            //: var data: AgoneCellData
            var data: AgoneCellData
            //: if msgInfo["isGroup"] as? String == "1" {  // 群聊
            if msgInfo[(String(kGivingValue.prefix(7)))] as? String == "1" { // 群聊
                //: data = TalkingGroupChatGiftMsgCellData(direction: isSender ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
                data = CompartmentReactiveCompatible(direction: isSender ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
                //: } else {
            } else {
                //: data = RangeCellData(direction: isSender ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
                data = RangeCellData(direction: isSender ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
            }

            //: let cData = extraString.data(using: .utf8)
            let cData = extraString.data(using: .utf8)
            //: data.innerMessage = V2TIMManager.sharedInstance().createCustomMessage(cData)
            data.innerMessage = V2TIMManager.sharedInstance().createCustomMessage(cData)
            //: if let model = try? AbTalkingChatMsgBaseModel(dictionary: tempDict) {
            if let model = try? ReadModel(dictionary: tempDict) {
                //: data.msgModel = model
                data.msgModel = model
            }
            //: return data
            return data
        }

        // 索要礼物
        //: if msgInfo["msgType"] as? String == "WantGift" {
        if msgInfo[(String(dataMovementKey.suffix(7)))] as? String == (String(constMidMessage)) {
            //: let data = CommandTypeTag(direction: isSender ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
            let data = CommandTypeTag(direction: isSender ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
            //: let cData = extraString.data(using: .utf8)
            let cData = extraString.data(using: .utf8)
            //: data.innerMessage = V2TIMManager.sharedInstance().createCustomMessage(cData)
            data.innerMessage = V2TIMManager.sharedInstance().createCustomMessage(cData)
            //: if let model = try? AbTalkingChatMsgBaseModel(dictionary: tempDict) {
            if let model = try? ReadModel(dictionary: tempDict) {
                //: data.msgModel = model
                data.msgModel = model
            }
            //: return data
            return data
        }

        // 文字
        //: if msgInfo["msgType"] as? String == "txt" {
        if msgInfo[(String(dataMovementKey.suffix(7)))] as? String == (String(const_deadlineKey)) {
            //: var data: AgoneCellData
            var data: AgoneCellData
            //: if msgInfo["isGroup"] as? String == "1" {  // 群聊
            if msgInfo[(String(kGivingValue.prefix(7)))] as? String == "1" { // 群聊
                //: data = WillCellData(direction: isSender ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
                data = WillCellData(direction: isSender ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
                //: } else {
            } else {
                //: data = AgoArrayCellData(direction: isSender ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
                data = AgoArrayCellData(direction: isSender ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
            }
            //: let cData = extraString.data(using: .utf8)
            let cData = extraString.data(using: .utf8)
            //: data.innerMessage = V2TIMManager.sharedInstance().createCustomMessage(cData)
            data.innerMessage = V2TIMManager.sharedInstance().createCustomMessage(cData)
            //: if let model = try? AbTalkingChatMsgBaseModel(dictionary: tempDict) {
            if let model = try? ReadModel(dictionary: tempDict) {
                //: data.msgModel = model
                data.msgModel = model
            }
            //: return data
            return data
        }

        // 图片
        //: if msgInfo["msgType"] as? String == "img" {
        if msgInfo[(String(dataMovementKey.suffix(7)))] as? String == (String(data_lanceUrl)) {
            //: let data = ApartCellData(direction: isSender ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
            let data = ApartCellData(direction: isSender ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
            //: let cData = extraString.data(using: .utf8)
            let cData = extraString.data(using: .utf8)
            //: data.innerMessage = V2TIMManager.sharedInstance().createCustomMessage(cData)
            data.innerMessage = V2TIMManager.sharedInstance().createCustomMessage(cData)
            //: if let model = try? AbTalkingChatMsgBaseModel(dictionary: tempDict) {
            if let model = try? ReadModel(dictionary: tempDict) {
                //: data.msgModel = model
                data.msgModel = model
            }
            //: return data
            return data
        }

        // 视频
        //: if msgInfo["msgType"] as? String == "video" {
        if msgInfo[(String(dataMovementKey.suffix(7)))] as? String == (noti_stressStr.replacingOccurrences(of: "bound", with: "o")) {
            //: let data = SaverCellData(direction: isSender ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
            let data = SaverCellData(direction: isSender ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
            //: let cData = extraString.data(using: .utf8)
            let cData = extraString.data(using: .utf8)
            //: data.innerMessage = V2TIMManager.sharedInstance().createCustomMessage(cData)
            data.innerMessage = V2TIMManager.sharedInstance().createCustomMessage(cData)
            //: if let model = try? AbTalkingChatMsgBaseModel(dictionary: tempDict) {
            if let model = try? ReadModel(dictionary: tempDict) {
                //: data.msgModel = model
                data.msgModel = model
            }
            //: return data
            return data
        }

        // 语音
        //: if msgInfo["msgType"] as? String == "audio" {
        if msgInfo[(String(dataMovementKey.suffix(7)))] as? String == (mainLabKey.replacingOccurrences(of: "oval", with: "i")) {
            //: let data = TagCellData(direction: isSender ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
            let data = TagCellData(direction: isSender ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
            //: let cData = extraString.data(using: .utf8)
            let cData = extraString.data(using: .utf8)
            //: data.innerMessage = V2TIMManager.sharedInstance().createCustomMessage(cData)
            data.innerMessage = V2TIMManager.sharedInstance().createCustomMessage(cData)
            //: if let model = try? AbTalkingChatMsgBaseModel(dictionary: tempDict) {
            if let model = try? ReadModel(dictionary: tempDict) {
                //: data.msgModel = model
                data.msgModel = model
            }
            //: return data
            return data
        }

        // 打招呼
        //: if extral["gift"] != nil && (msgInfo["messageType"] as? Int) == 4 {
        if extral[(dataOuterFormat.replacingOccurrences(of: "stock", with: "ft"))] != nil && (msgInfo[(notiStatusId.replacingOccurrences(of: "large", with: "ss") + "ageTyp" + String(userSayData))] as? Int) == 4 {
            //: let data = AgoArrayCellData(direction: isSender ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
            let data = AgoArrayCellData(direction: isSender ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
            //: let cData = extraString.data(using: .utf8)
            let cData = extraString.data(using: .utf8)
            //: data.innerMessage = V2TIMManager.sharedInstance().createCustomMessage(cData)
            data.innerMessage = V2TIMManager.sharedInstance().createCustomMessage(cData)
            //: if let model = try? AbTalkingChatMsgBaseModel(dictionary: tempDict) {
            if let model = try? ReadModel(dictionary: tempDict) {
                //: data.msgModel = model
                data.msgModel = model
            }
            //: return data
            return data
        }

        //: return nil
        return nil
    }

    /// 腾讯IM重发消息组装 （只有私聊在用）
    //: public class func handleTXMessageReSend(withData cellData: AgoneCellData) -> AgoneCellData? {
    public class func set(withData cellData: AgoneCellData) -> AgoneCellData? {
        // 图片
        //: if let reCellData = cellData as? ApartCellData {
        if let reCellData = cellData as? ApartCellData {
            //: let paths = NSSearchPathForDirectoriesInDomains(.cachesDirectory, .userDomainMask, true)
            let paths = NSSearchPathForDirectoriesInDomains(.cachesDirectory, .userDomainMask, true)
            //: var filePath = paths.first!
            var filePath = paths.first!
            //: filePath = (filePath as NSString).appendingPathComponent(AnimateUnaryAllocator.getImageFileName())
            filePath = (filePath as NSString).appendingPathComponent(AnimateUnaryAllocator.accordingEmpty())
            //: filePath = (filePath as NSString).appendingPathComponent((reCellData.imageUrl! as NSString).lastPathComponent)
            filePath = (filePath as NSString).appendingPathComponent((reCellData.imageUrl! as NSString).lastPathComponent)

            //: if !FileManager.default.fileExists(atPath: filePath) {
            if !FileManager.default.fileExists(atPath: filePath) {
                //: return nil
                return nil
            }

            //: var dic: [String: Any] = [:]
            var dic: [String: Any] = [:]
            //: var params: [String: Any] = [:]
            var params: [String: Any] = [:]
            //: var msgInfo: [String: Any] = [:]
            var msgInfo: [String: Any] = [:]
            //: msgInfo["contentType"] = "ImgMsg"
            msgInfo[(main_stillWoundUrl.replacingOccurrences(of: "less", with: "o") + "entT" + app_computeValue.replacingOccurrences(of: "image", with: "y"))] = (k_identityStr.replacingOccurrences(of: "alive", with: "I") + String(notiAboveValue.prefix(5)))
            //: msgInfo["msgType"] = "img"
            msgInfo[(String(dataMovementKey.suffix(7)))] = (String(data_lanceUrl))
            //: msgInfo["imageUri"] = filePath
            msgInfo[(String(noti_vantageText))] = filePath
            //: dic["msgInfo"] = msgInfo
            dic[(String(noti_herLoseValue.suffix(7)))] = msgInfo

            //: params["type"] = "ImgMsg"
            params[(String(kAcrossName))] = (k_identityStr.replacingOccurrences(of: "alive", with: "I") + String(notiAboveValue.prefix(5)))
            //: params["pic"] = filePath
            params[(dataApproximatelyOkMessage.replacingOccurrences(of: "board", with: "c"))] = filePath

            // 重发私密照片
            //: let isBurn = reCellData.msgModel.msgInfo.customSend.isBurn
            let isBurn = reCellData.msgModel.msgInfo.customSend.isBurn
            //: if !isBurn.isEmpty {
            if !isBurn.isEmpty {
                //: params["isBurn"] = isBurn
                params[(String(show_roundStageMessage))] = isBurn
                //: } else {
            } else {
                //: params["isBurn"] = "0"
                params[(String(show_roundStageMessage))] = "0"
            }

            //: let giftId = reCellData.msgModel.msgInfo.customSend.giftId
            let giftId = reCellData.msgModel.msgInfo.customSend.giftId
            //: if !giftId.isEmpty {
            if !giftId.isEmpty {
                //: params["giftId"] = giftId
                params[(String(kEarnKey))] = giftId
                //: } else {
            } else {
                //: params["giftId"] = "0"
                params[(String(kEarnKey))] = "0"
            }

            //: dic["params"] = params
            dic[(appWeekAvailableId.replacingOccurrences(of: "care", with: "s"))] = params

            //: let data = handleTXMessageSend(withExtral: dic, isSender: true, isTip: false)
            let data = smartMailSenderBlockTipHandleTurn(withExtral: dic, isSender: true, isTip: false)
            //: data?.status = .Msg_Status_Fail
            data?.status = .Msg_Status_Fail
            //: return data
            return data
        }

        // 视频
        //: if let reCellData = cellData as? SaverCellData {
        if let reCellData = cellData as? SaverCellData {
            //: reCellData.status = .Msg_Status_Fail
            reCellData.status = .Msg_Status_Fail
            //: return reCellData
            return reCellData
        }

        // 语音
        //: if let reCellData = cellData as? TagCellData {
        if let reCellData = cellData as? TagCellData {
            //: let voiceModel = DBUserInfoManager.cache_getAudioDB(message: reCellData.innerMessage)
            let voiceModel = DeployMatrixDelegate.molarityMessage(message: reCellData.innerMessage)
            //: var dic: [String: Any] = [:]
            var dic: [String: Any] = [:]
            //: var msgInfo: [String: Any] = [:]
            var msgInfo: [String: Any] = [:]
            //: var params: [String: Any] = [:]
            var params: [String: Any] = [:]

            //: if !voiceModel.db_audioLength.isEmpty {
            if !voiceModel.db_audioLength.isEmpty {
                //: msgInfo["audioLength"] = voiceModel.db_audioLength
                msgInfo[(dataCertainValue.replacingOccurrences(of: "cell", with: "d") + "oLength")] = voiceModel.db_audioLength
                //: params["len"] = voiceModel.db_audioLength
                params[(show_incomeStr.replacingOccurrences(of: "bass", with: "e"))] = voiceModel.db_audioLength
            }
            //: if !voiceModel.db_filePath.isEmpty {
            if !voiceModel.db_filePath.isEmpty {
                //: msgInfo["audioSandbox"] = voiceModel.db_filePath
                msgInfo[(String(dataScribeId.prefix(6)) + "andbox")] = voiceModel.db_filePath
                //: params["audioSandbox"] = voiceModel.db_filePath
                params[(String(dataScribeId.prefix(6)) + "andbox")] = voiceModel.db_filePath
            }
            //: msgInfo["msgType"] = "audio"
            msgInfo[(String(dataMovementKey.suffix(7)))] = (mainLabKey.replacingOccurrences(of: "oval", with: "i"))
            //: msgInfo["senderId"] = FacultyReactiveCompatible.share.loginUserMode.userID
            msgInfo[(user_plainKey.replacingOccurrences(of: "safety", with: "en") + "erId")] = FacultyReactiveCompatible.share.loginUserMode.userID
            //: dic["msgInfo"] = msgInfo
            dic[(String(noti_herLoseValue.suffix(7)))] = msgInfo

            //: params["type"] = "AudioMsg"
            params[(String(kAcrossName))] = (String(mainSquareKey.prefix(4)) + "oMsg")
            //: dic["params"] = params
            dic[(appWeekAvailableId.replacingOccurrences(of: "care", with: "s"))] = params

            //: let data = handleTXMessageSend(withExtral: dic, isSender: true, isTip: false)
            let data = smartMailSenderBlockTipHandleTurn(withExtral: dic, isSender: true, isTip: false)
            //: data?.status = .Msg_Status_Fail
            data?.status = .Msg_Status_Fail
            //: return data
            return data
        }

        // 文本
        //: if let reCellData = cellData as? AgoArrayCellData {
        if let reCellData = cellData as? AgoArrayCellData {
            //: var dic: [String: Any] = [:]
            var dic: [String: Any] = [:]
            //: var params: [String: Any] = [:]
            var params: [String: Any] = [:]
            //: var msgInfo: [String: Any] = [:]
            var msgInfo: [String: Any] = [:]

            //: msgInfo["content"] = reCellData.contentStr ?? ""
            msgInfo[(String(noti_piMessage))] = reCellData.contentStr ?? ""
            //: msgInfo["msgType"] = "txt"
            msgInfo[(String(dataMovementKey.suffix(7)))] = (String(const_deadlineKey))
            //: dic["msgInfo"] = msgInfo
            dic[(String(noti_herLoseValue.suffix(7)))] = msgInfo
            //: params["type"] = "TxtMsg"
            params[(String(kAcrossName))] = (String(showPartBasicFormat.prefix(6)))
            //: params["message"] = reCellData.contentStr ?? ""
            params[(data_ridExceptContent.replacingOccurrences(of: "organization", with: "me") + String(user_shallTitle))] = reCellData.contentStr ?? ""

            //: if reCellData.msgModel.quoteMsgInfo.isQuoteMsg() == true {
            if reCellData.msgModel.quoteMsgInfo.hide() == true { // 引用消息重发
                //: params["quoteMsgId"] = reCellData.msgModel.quoteMsgInfo.msgId
                params[(String(appArenaSpirePath) + String(app_heMessage.prefix(3)))] = reCellData.msgModel.quoteMsgInfo.msgId
                //: dic["quoteMsgInfo"] = reCellData.msgModel.quoteMsgInfo
                dic[(appAdvertisingMessage.replacingOccurrences(of: "conversation", with: "q") + String(dataNorName.prefix(8)))] = reCellData.msgModel.quoteMsgInfo
                    //: .toDictionary()
                    .toDictionary()
            }

            //: if let replayId = reCellData.msgModel.params["replayId"] { // 问题回答消息重发
            if let replayId = reCellData.msgModel.params[(String(notiSparkPath))] { // 问题回答消息重发
                //: params["replayId"] = replayId
                params[(String(notiSparkPath))] = replayId
            }

            //: dic["params"] = params
            dic[(appWeekAvailableId.replacingOccurrences(of: "care", with: "s"))] = params

            //: let data = handleTXMessageSend(withExtral: dic, isSender: true, isTip: false)
            let data = smartMailSenderBlockTipHandleTurn(withExtral: dic, isSender: true, isTip: false)
            //: data?.status = .Msg_Status_Fail
            data?.status = .Msg_Status_Fail
            //: return data
            return data
        }

        //: return nil
        return nil
    }

    /// 腾讯IM接收消息组装
    //: public class func handleTXReceiveMessage(withExtral dic: [String: Any], cusData: Data, tempModel: V2TIMMessage) -> AgoneCellData? {
    public class func stack(withExtral dic: [String: Any], cusData: Data, tempModel: V2TIMMessage) -> AgoneCellData? {
        //: var cellData: AgoneCellData?
        var cellData: AgoneCellData?
        //: let extraDict = dic["extra"] as? [String: Any]
        let extraDict = dic[(mainComplexSitKey.replacingOccurrences(of: "whole", with: "ra"))] as? [String: Any]
        //: let msgInfoDict = extraDict?["msgInfo"] as? [String: Any]
        let msgInfoDict = extraDict?[(String(noti_herLoseValue.suffix(7)))] as? [String: Any]
        //: let user = dic["user"] as? [String: Any] ?? [:]
        let user = dic[(String(mainPickPath))] as? [String: Any] ?? [:]

        //: guard let model = try? AbTalkingChatMsgBaseModel(dictionary: extraDict) else {
        guard let model = try? ReadModel(dictionary: extraDict) else {
            //: return nil
            return nil
        }
        //: var isSelf = (model.user.uid == FacultyReactiveCompatible.share.loginUserMode.userID.intValue)
        var isSelf = (model.user.uid == FacultyReactiveCompatible.share.loginUserMode.userID.intValue)

        // 后台代发送的自动打招呼消息
        //: var isAutoSendMsg = false
        var isAutoSendMsg = false
        //: if model.msgInfo.uid.intValue == FacultyReactiveCompatible.share.loginUserMode.userID.intValue &&
        if model.msgInfo.uid.intValue == FacultyReactiveCompatible.share.loginUserMode.userID.intValue &&
            //: model.msgInfo.messageType.rawValue == 3 {
            model.msgInfo.messageType.rawValue == 3
        {
            //: isSelf = true
            isSelf = true
            //: isAutoSendMsg = true
            isAutoSendMsg = true
        }

        //: if dic["rYMsgType"] as? String == "GJ:Sess:AutoGreet:TxtCustom" {
        if dic[(String(noti_dotKey))] as? String == (String(mainMobileUrl) + String(show_universalText)) {
            //: isSelf = msgInfoDict?["changeDirection"] as? Bool ?? false
            isSelf = msgInfoDict?[(showFenceUrl.lowercased() + "eDirec" + String(show_donMessage))] as? Bool ?? false
        }

        //: let contentType = messageCellType(dic)
        let contentType = prompt(dic)

        // 系统消息
        //: if extraDict?["msgType"] as? String == "system" ||
        if extraDict?[(String(dataMovementKey.suffix(7)))] as? String == (userSkipKey.replacingOccurrences(of: "max", with: "te")) ||
            //: contentType == .TXMessageContentType_tips {
            contentType == .TXMessageContentType_tips
        {
            //: let model = AbTalkingChatMsgBaseModel()
            let model = ReadModel()
            //: let tipModel = try? AbTalkingChatMsgTipsModel(dictionary: extraDict)
            let tipModel = try? OccurModel(dictionary: extraDict)
            //: cellData = TalkingChatTipsMsgCellData(direction: isSelf ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
            cellData = ComposableAnimatedPromise(direction: isSelf ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
            //: if let content = dic["content"] as? String {
            if let content = dic[(String(noti_piMessage))] as? String {
                //: tipModel?.content = content
                tipModel?.content = content
            }
            //: if tipModel != nil {
            if tipModel != nil {
                //: model.tips = tipModel!
                model.tips = tipModel!
            }
            //: cellData?.msgModel = model
            cellData?.msgModel = model
            //: return cellData
            return cellData
        }

        // 其他消息类型的创建...
        //: switch contentType {
        switch contentType {
        //: case .TXMessageContentType_picture:
        case .TXMessageContentType_picture:
            //: cellData = ApartCellData(direction: isSelf ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
            cellData = ApartCellData(direction: isSelf ? .MsgDirectionOutgoing : .MsgDirectionIncoming)

        //: case .TXMessageContentType_video:
        case .TXMessageContentType_video:
            //: cellData = SaverCellData(direction: isSelf ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
            cellData = SaverCellData(direction: isSelf ? .MsgDirectionOutgoing : .MsgDirectionIncoming)

        //: case .TXMessageContentType_gift:
        case .TXMessageContentType_gift:
            //: if let groupID = tempModel.groupID, !groupID.isEmpty {
            if let groupID = tempModel.groupID, !groupID.isEmpty { // 群聊gift
                //: cellData = TalkingGroupChatGiftMsgCellData(direction: isSelf ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
                cellData = CompartmentReactiveCompatible(direction: isSelf ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
                //: } else {
            } else { // 私聊gift
                //: cellData = RangeCellData(direction: isSelf ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
                cellData = RangeCellData(direction: isSelf ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
            }

        //: case .TXMessageContentType_askforGift:
        case .TXMessageContentType_askforGift: // 索要礼物
            //: let tempDic = parseTXMessageData(data: cusData)
            let tempDic = manage(data: cusData)
            //: if let extraData = tempDic["extra"] as? [String: Any],
            if let extraData = tempDic[(mainComplexSitKey.replacingOccurrences(of: "whole", with: "ra"))] as? [String: Any],
               //: let msgInfoData = extraData["msgInfo"] as? [String: Any],
               let msgInfoData = extraData[(String(noti_herLoseValue.suffix(7)))] as? [String: Any],
               //: !msgInfoData.isEmpty {
               !msgInfoData.isEmpty
            {
                //: if let info = try? AbTalkingChatMsgInfoModel(dictionary: msgInfoData) {
                if let info = try? DisplaceModel(dictionary: msgInfoData) {
                    //: model.msgInfo = info
                    model.msgInfo = info
                }
            }
            //: cellData = CommandTypeTag(direction: isSelf ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
            cellData = CommandTypeTag(direction: isSelf ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
            //: cellData?.msgModel = model
            cellData?.msgModel = model

        //: case .TXMessageContentType_voice:
        case .TXMessageContentType_voice:
            //: cellData = TagCellData(direction: isSelf ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
            cellData = TagCellData(direction: isSelf ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
            //: if let audioCellData = cellData as? TagCellData {
            if let audioCellData = cellData as? TagCellData {
                //: if audioCellData.voiceModel.db_voiceCacheWrap.msgId.isEmpty {
                if audioCellData.voiceModel.db_voiceCacheWrap.msgId.isEmpty {
                    //: let mainKey = checkPrimaryKey(withMsgModel: tempModel)
                    let mainKey = expression(withMsgModel: tempModel)
                    //: var cacheWrap = WCDBVoiceMsgTable.db_getVoiceMsg(with: mainKey)
                    var cacheWrap = DrawButtonMechanism.familyLayer(with: mainKey)
                    //: if cacheWrap == nil {
                    if cacheWrap == nil {
                        //: cacheWrap = WCDBVoiceMsgTable.db_inserVoiceMsgToDB(packageDict(withTXMessage: tempModel))
                        cacheWrap = DrawButtonMechanism.exceptAction(arena(withTXMessage: tempModel))
                        //: audioCellData.voiceModel.db_voiceCacheWrap = cacheWrap!
                        audioCellData.voiceModel.db_voiceCacheWrap = cacheWrap!
                        //: } else {
                    } else {
                        //: audioCellData.voiceModel.db_voiceCacheWrap = cacheWrap!
                        audioCellData.voiceModel.db_voiceCacheWrap = cacheWrap!
                    }
                }
            }

        //: case .TXMessageContentType_local:
        case .TXMessageContentType_local:
            //: cellData = TalkingChatMatchMsgCellData(direction: isSelf ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
            cellData = SubjectCellData(direction: isSelf ? .MsgDirectionOutgoing : .MsgDirectionIncoming)

        //: default:
        default:
            //: let title = extraDict?["title"] as? String ?? ""
            let title = extraDict?[(app_connectPath.replacingOccurrences(of: "ion", with: "ti"))] as? String ?? ""
            //: let content = extraDict?["content"] as? String ?? ""
            let content = extraDict?[(String(noti_piMessage))] as? String ?? ""
            //: let img = extraDict?["img"] as? String ?? ""
            let img = extraDict?[(String(data_lanceUrl))] as? String ?? ""
            //: if !title.isEmpty || !content.isEmpty || !img.isEmpty {
            if !title.isEmpty || !content.isEmpty || !img.isEmpty { // 系统新消息
                //: cellData = TalkingChatSystemNotifJumpMsgCellData(direction: isSelf ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
                cellData = PersonCellData(direction: isSelf ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
                //: cellData?.extraDict = extraDict
                cellData?.extraDict = extraDict
                //: } else if let groupID = tempModel.groupID, !groupID.isEmpty {
            } else if let groupID = tempModel.groupID, !groupID.isEmpty { // 群聊text
                //: cellData = WillCellData(direction: isSelf ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
                cellData = WillCellData(direction: isSelf ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
                //: } else {
            } else { // 私聊text
                //: cellData = AgoArrayCellData(direction: isSelf ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
                cellData = AgoArrayCellData(direction: isSelf ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
            }
        }

        // 设置用户信息
        //: if model.user.nickname.isEmpty {
        if model.user.nickname.isEmpty {
            //: var userDict = user
            var userDict = user
            //: userDict.merge(msgInfoDict ?? [:]) { (_, new) in new }
            userDict.merge(msgInfoDict ?? [:]) { _, new in new }
            //: if let userModel = try? AbTalkingChatMsgUserModel(dictionary: userDict) {
            if let userModel = try? LifetimePointModel(dictionary: userDict) {
                //: if userModel.uid == 0 {
                if userModel.uid == 0 {
                    //: userModel.uid = userDict["id"] as? Int ?? 0
                    userModel.uid = userDict["id"] as? Int ?? 0
                }
                //: if userModel.headPic.isEmpty == true {
                if userModel.headPic.isEmpty == true {
                    //: userModel.headPic = userDict["icon"] as? String ?? ""
                    userModel.headPic = userDict[(data_smoothPath.replacingOccurrences(of: "announcement", with: "i"))] as? String ?? ""
                }

                //: if isAutoSendMsg {
                if isAutoSendMsg {
                    //: userModel.uid = model.msgInfo.toUid.intValue
                    userModel.uid = model.msgInfo.toUid.intValue
                    //: userModel.headPic =
                    userModel.headPic =
                        //: FacultyReactiveCompatible.share.loginUserMode.headPic ?? ""
                        FacultyReactiveCompatible.share.loginUserMode.headPic ?? ""
                }
                //: model.user = userModel
                model.user = userModel
            }
        }

        //: if model.msgInfo == nil {
        if model.msgInfo == nil {
            //: let msgInfoModel = AbTalkingChatMsgInfoModel()
            let msgInfoModel = DisplaceModel()
            //: msgInfoModel.content = dic["content"] as? String ?? ""
            msgInfoModel.content = dic[(String(noti_piMessage))] as? String ?? ""
            //: msgInfoModel.jumps = extraDict?["extra"] as? [Any] ?? []
            msgInfoModel.jumps = extraDict?[(mainComplexSitKey.replacingOccurrences(of: "whole", with: "ra"))] as? [Any] ?? []
            //: model.msgInfo = msgInfoModel
            model.msgInfo = msgInfoModel
        }

        // 聊天室送礼消息
        //: if contentType == .TXMessageContentType_gift && tempModel.groupID != nil && !tempModel.groupID!.isEmpty {
        if contentType == .TXMessageContentType_gift && tempModel.groupID != nil && !tempModel.groupID!.isEmpty {
            //: if model.toUser.uid.isEmpty {
            if model.toUser.uid.isEmpty { // 送礼所有人
                //: let toUserInfo = AbTalkingChatMsgToUserModel()
                let toUserInfo = ProfessionalModel()
                //: toUserInfo.uid = "all"
                toUserInfo.uid = (show_centerTitle.replacingOccurrences(of: "care", with: "a"))
                //: toUserInfo.nickname = model.gift.sendNum
                toUserInfo.nickname = model.gift.sendNum
                //: toUserInfo.headPic = "icon_chats_pcr"
                toUserInfo.headPic = (String(appEnableerPath) + String(main_cityKey.suffix(7)))
                //: model.toUser = toUserInfo
                model.toUser = toUserInfo
            }
        }
        //: cellData?.msgModel = model
        cellData?.msgModel = model

        // 本地自定义字段处理
        //: let customStr = String(data: cusData, encoding: .utf8) ?? ""
        let customStr = String(data: cusData, encoding: .utf8) ?? ""
        //: let customDic = NSDictionary.dictionary(withJsonString: customStr) ?? [:]
        let customDic = NSDictionary.beginShoo(withJsonString: customStr) ?? [:]
        //: let statusStr = customDic["status"] as? String ?? "0"
        let statusStr = customDic[(String(main_recommendationFormat))] as? String ?? "0"
        //: let status = TMsgStatus(rawValue: UInt(statusStr) ?? 0) ?? .Msg_Status_Init
        let status = TMsgStatus(rawValue: UInt(statusStr) ?? 0) ?? .Msg_Status_Init
        //: cellData?.status = status
        cellData?.status = status
        //: cellData?.msgIncome = CGFloat(customDic["msgIncome"] as? Float ?? 0)
        cellData?.msgIncome = CGFloat(customDic[(String(const_calculateKey) + appMedicSelectionMessage.replacingOccurrences(of: "traffic", with: "e"))] as? Float ?? 0)
        //: cellData?.messageType = msgInfoDict?["messageType"] as? Int ?? 0
        cellData?.messageType = msgInfoDict?[(notiStatusId.replacingOccurrences(of: "large", with: "ss") + "ageTyp" + String(userSayData))] as? Int ?? 0

        //: if let msgId = customDic["msgId"] as? String {
        if let msgId = customDic[(String(showFindMessage.suffix(5)))] as? String {
            //: cellData?.msgModel.msgInfo.msgId = msgId
            cellData?.msgModel.msgInfo.msgId = msgId
        }

        // 文本翻译
        //: if let translateStatus = customDic["TMsgTranslateStatus"] as? Int {
        if let translateStatus = customDic[(String(const_modelValue.prefix(4)) + "Trans" + String(showWalkKey.prefix(6)) + mainMineFormat.replacingOccurrences(of: "sibling", with: "tu"))] as? Int {
            //: cellData?.translateStatus = TMsgTranslateStatus(rawValue: UInt(translateStatus)) ?? .MsgTranslate_init
            cellData?.translateStatus = SuiteStretchCount(rawValue: UInt(translateStatus)) ?? .MsgTranslate_init
        }
        //: if let translatedContent = customDic["TMsgTranslatedContent"] as? String {
        if let translatedContent = customDic[(String(constRidTitle) + showNearUrl.replacingOccurrences(of: "average", with: "e") + "Content")] as? String {
            //: cellData?.translatedContent = translatedContent
            cellData?.translatedContent = translatedContent
        }

        // 私密照片Cache
        //: if contentType == .TXMessageContentType_picture {
        if contentType == .TXMessageContentType_picture {
            //: let dict = AbTalkingChatMsgIntimatePhotoModel.loadLocalIntimatePhotoDict()
            let dict = CarryModel.lengthGroup()
            //: if let photoData = dict[tempModel.msgID as Any] as? String {
            if let photoData = dict[tempModel.msgID as Any] as? String {
                //: cellData?.msgModel.msgInfo.intimatePhoto = AbTalkingChatMsgIntimatePhotoModel(string: photoData, error: nil)
                cellData?.msgModel.msgInfo.intimatePhoto = CarryModel(string: photoData, error: nil)
            }
        }

        // 私密视频Cache
        //: if contentType == .TXMessageContentType_video {
        if contentType == .TXMessageContentType_video {
            //: let dict = AbTalkingChatMsgVideoModel.loadLocalIntimateVideoDict()
            let dict = OthersModel.video()
            //: if let videoData = dict[tempModel.msgID as Any] as? String {
            if let videoData = dict[tempModel.msgID as Any] as? String {
                //: cellData?.msgModel.msgInfo.video = AbTalkingChatMsgVideoModel(string: videoData, error: nil)
                cellData?.msgModel.msgInfo.video = OthersModel(string: videoData, error: nil)
            }
        }

        // 索要礼物Cache
        //: if contentType == .TXMessageContentType_askforGift {
        if contentType == .TXMessageContentType_askforGift {
            //: let arr = WantGiftModel.loadAskForGiftArr()
            let arr = SourceModel.duringAsk()
            //: if arr.contains(tempModel.msgID as Any) {
            if arr.contains(tempModel.msgID as Any) { // 已发送
                //: cellData?.msgModel.msgInfo.wantGift.sendStatus = 1
                cellData?.msgModel.msgInfo.wantGift.sendStatus = 1
            }
        }

        //: return cellData
        return cellData
    }

    // MARK: - 辅助方法

    /// 根据字典获取消息类型
    //: public class func messageCellType(_ extraDic: [String: Any]) -> TXMessageContentType {
    public class func prompt(_ extraDic: [String: Any]) -> AggregationQuickLookable {
        //: let dic = extraDic["extra"] as? [String: Any]
        let dic = extraDic[(mainComplexSitKey.replacingOccurrences(of: "whole", with: "ra"))] as? [String: Any]
        //: let msgInfo = dic?["msgInfo"] as? [String: Any]
        let msgInfo = dic?[(String(noti_herLoseValue.suffix(7)))] as? [String: Any]
        //: guard let model = try? AbTalkingChatMsgBaseModel(dictionary: dic) else {
        guard let model = try? ReadModel(dictionary: dic) else {
            //: return .TXMessageContentType_text
            return .TXMessageContentType_text
        }

        // 系统消息 官方小密
        //: if dic?["formatType"] as? String == "plain" {
        if dic?[(userLanceFormat.lowercased() + "Type")] as? String == (main_corePath.replacingOccurrences(of: "should", with: "a")) {
            //: let imageUri = dic?["imageUri"] as? String ?? ""
            let imageUri = dic?[(String(noti_vantageText))] as? String ?? ""
            //: let imageData = dic?["imageData"] as? String ?? ""
            let imageData = dic?[(String(noti_expansionText.prefix(7)) + String(show_statinFeeUrl))] as? String ?? ""
            //: let isImageStyle = !imageData.isEmpty || !imageUri.isEmpty
            let isImageStyle = !imageData.isEmpty || !imageUri.isEmpty
            //: if isImageStyle {
            if isImageStyle {
                //: return .TXMessageContentType_picture
                return .TXMessageContentType_picture
            }
        }

        // 图片||私密照片
        //: if !model.msgInfo.imageUri.isEmpty || !model.msgInfo.imageData.isEmpty || !model.msgInfo.intimatePhoto.originUrl.isEmpty {
        if !model.msgInfo.imageUri.isEmpty || !model.msgInfo.imageData.isEmpty || !model.msgInfo.intimatePhoto.originUrl.isEmpty {
            //: return .TXMessageContentType_picture
            return .TXMessageContentType_picture
        }

        // 接收的视频消息 || 本人发送的视频消息
        //: if !model.msgInfo.video.videoUrl.isEmpty || model.msgInfo.contentType == "VideoMsg" {
        if !model.msgInfo.video.videoUrl.isEmpty || model.msgInfo.contentType == (String(app_sectionSendName) + String(k_degreeStr)) {
            //: return .TXMessageContentType_video
            return .TXMessageContentType_video
        }

        // 礼物，非打招呼消息
        //: if !model.gift.name.isEmpty && model.msgInfo.messageType.rawValue != 4 {
        if !model.gift.name.isEmpty && model.msgInfo.messageType.rawValue != 4 {
            //: return .TXMessageContentType_gift
            return .TXMessageContentType_gift
        }

        //: let isVoiceStyle = checkVoiceMsg(msginfo: msgInfo ?? [:])
        let isVoiceStyle = subMsginfo(msginfo: msgInfo ?? [:])
        //: if isVoiceStyle {
        if isVoiceStyle {
            //: return .TXMessageContentType_voice
            return .TXMessageContentType_voice
        }

        //: if (dic?["match"] as? Int) == 1 || (msgInfo?["match"] as? Int) == 1 {
        if (dic?[(user_connectMsg.replacingOccurrences(of: "hide", with: "ch"))] as? Int) == 1 || (msgInfo?[(user_connectMsg.replacingOccurrences(of: "hide", with: "ch"))] as? Int) == 1 {
            // 匹配消息
            //: return .TXMessageContentType_local
            return .TXMessageContentType_local
        }

        //: if dic?["msgType"] as? String == "tips" || (dic?["messageType"] as? Int) == 17 {
        if dic?[(String(dataMovementKey.suffix(7)))] as? String == (dataAreaName.replacingOccurrences(of: "mark", with: "t")) || (dic?[(notiStatusId.replacingOccurrences(of: "large", with: "ss") + "ageTyp" + String(userSayData))] as? Int) == 17 {
            // 提示消息,包含超时未回复
            //: return .TXMessageContentType_tips
            return .TXMessageContentType_tips
        }

        //: if (dic?["formatType"] as? String == "mf") && ((dic?["messageType"] as? Int) == 15 || (dic?["messageType"] as? Int) == 16) {
        if (dic?[(userLanceFormat.lowercased() + "Type")] as? String == "mf") && ((dic?[(notiStatusId.replacingOccurrences(of: "large", with: "ss") + "ageTyp" + String(userSayData))] as? Int) == 15 || (dic?[(notiStatusId.replacingOccurrences(of: "large", with: "ss") + "ageTyp" + String(userSayData))] as? Int) == 16) {
            // 提示消息，打招呼提示送出神秘礼物,对方设置价格消息提示
            //: return .TXMessageContentType_tips
            return .TXMessageContentType_tips
        }

        //: if dic?["msgType"] as? String == "userIntro" {
        if dic?[(String(dataMovementKey.suffix(7)))] as? String == (appSwitchceGoingUrl.lowercased() + String(showPunishUrl.prefix(6))) {
            //: return .TXMessageContentType_userCard
            return .TXMessageContentType_userCard
        }

        //: if msgInfo?["msgType"] as? String == "WantGift" || (msgInfo?["messageType"] as? Int) == 25 {
        if msgInfo?[(String(dataMovementKey.suffix(7)))] as? String == (String(constMidMessage)) || (msgInfo?[(notiStatusId.replacingOccurrences(of: "large", with: "ss") + "ageTyp" + String(userSayData))] as? Int) == 25 {
            //: return .TXMessageContentType_askforGift
            return .TXMessageContentType_askforGift
        }

        //: return .TXMessageContentType_text
        return .TXMessageContentType_text
    }

    // MARK: - 语音消息处理

    /// 从TX的model里提取参数
    //: public class func packageDict(withTXMessage message: V2TIMMessage) -> [String: Any] {
    public class func arena(withTXMessage message: V2TIMMessage) -> [String: Any] {
        //: let extra = String(data: message.customElem.data, encoding: .utf8) ?? ""
        let extra = String(data: message.customElem.data, encoding: .utf8) ?? ""
        //: let dic = NSDictionary.dictionary(withJsonString: extra) ?? [:]
        let dic = NSDictionary.beginShoo(withJsonString: extra) ?? [:]
        //: let extraData = dic["extra"] as? [String: Any]
        let extraData = dic[(mainComplexSitKey.replacingOccurrences(of: "whole", with: "ra"))] as? [String: Any]
        //: let msgInfo = extraData?["msgInfo"] as? [String: Any]
        let msgInfo = extraData?[(String(noti_herLoseValue.suffix(7)))] as? [String: Any]
        //: let user = extraData?["user"] as? [String: Any]
        let user = extraData?[(String(mainPickPath))] as? [String: Any]

        //: let senderId = msgInfo?["senderId"] as? String ?? ""
        let senderId = msgInfo?[(user_plainKey.replacingOccurrences(of: "safety", with: "en") + "erId")] as? String ?? ""
        //: var info = msgInfo
        var info = msgInfo

        // 自己发送的和接收的数据结构不一样
        //: if senderId == FacultyReactiveCompatible.share.loginUserMode.userID {
        if senderId == FacultyReactiveCompatible.share.loginUserMode.userID {
            //: info?["senderId"] = senderId
            info?[(user_plainKey.replacingOccurrences(of: "safety", with: "en") + "erId")] = senderId
            //: info?["targetId"] = message.userID ?? (user?["targetId"] as? String ?? "")
            info?[(String(showEachValue.suffix(8)))] = message.userID ?? (user?[(String(showEachValue.suffix(8)))] as? String ?? "")
            //: } else {
        } else {
            //: info?["senderId"] = "\(msgInfo?["uid"] as? Int ?? 0)"
            info?[(user_plainKey.replacingOccurrences(of: "safety", with: "en") + "erId")] = "\(msgInfo?[(noti_confirmStr.lowercased())] as? Int ?? 0)"
            //: info?["targetId"] = "\(msgInfo?["uid"] as? Int ?? 0)"
            info?[(String(showEachValue.suffix(8)))] = "\(msgInfo?[(noti_confirmStr.lowercased())] as? Int ?? 0)"
        }

        //: if msgInfo?.keys.contains("msgId") ?? false {
        if msgInfo?.keys.contains((String(showFindMessage.suffix(5)))) ?? false {
            //: info?["msgId"] = "\(msgInfo?["msgId"] ?? "")"
            info?[(String(showFindMessage.suffix(5)))] = "\(msgInfo?[(String(showFindMessage.suffix(5)))] ?? "")"
            //: } else {
        } else {
            //: let customStr = String(data: message.localCustomData, encoding: .utf8) ?? ""
            let customStr = String(data: message.localCustomData, encoding: .utf8) ?? ""
            //: let customDic = NSDictionary.dictionary(withJsonString: customStr) ?? [:]
            let customDic = NSDictionary.beginShoo(withJsonString: customStr) ?? [:]
            //: if customDic["msgId"] != nil {
            if customDic[(String(showFindMessage.suffix(5)))] != nil {
                //: info?["msgId"] = "\(customDic["msgId"] ?? "")"
                info?[(String(showFindMessage.suffix(5)))] = "\(customDic[(String(showFindMessage.suffix(5)))] ?? "")"
                //: } else {
            } else {
                //: let txMsgId = !message.msgID.isEmpty ? message.msgID : "\(Int64(Date().timeIntervalSince1970))"
                let txMsgId = !message.msgID.isEmpty ? message.msgID : "\(Int64(Date().timeIntervalSince1970))"
                //: info?["msgId"] = txMsgId
                info?[(String(showFindMessage.suffix(5)))] = txMsgId
            }
        }

        //: if msgInfo?.keys.contains("audioSandbox") ?? false {
        if msgInfo?.keys.contains((String(dataScribeId.prefix(6)) + "andbox")) ?? false {
            //: info?["audioSandbox"] = "\(msgInfo?["audioSandbox"] ?? "")"
            info?[(String(dataScribeId.prefix(6)) + "andbox")] = "\(msgInfo?[(String(dataScribeId.prefix(6)) + "andbox")] ?? "")"
        }
        //: if msgInfo?.keys.contains("audioLength") ?? false {
        if msgInfo?.keys.contains((dataCertainValue.replacingOccurrences(of: "cell", with: "d") + "oLength")) ?? false {
            //: info?["audioLength"] = "\(msgInfo?["audioLength"] ?? "")"
            info?[(dataCertainValue.replacingOccurrences(of: "cell", with: "d") + "oLength")] = "\(msgInfo?[(dataCertainValue.replacingOccurrences(of: "cell", with: "d") + "oLength")] ?? "")"
        }

        //: let formatter = DateFormatter()
        let formatter = DateFormatter()
        //: formatter.dateFormat = "yyyy-MM-dd HH:mm:ss"
        formatter.dateFormat = (String(user_entitleStr.suffix(5)) + "MM-dd " + String(noti_processingName.suffix(4)) + "m:ss")
        //: info?["sendTime"] = formatter.string(from: message.timestamp ?? Date())
        info?[(String(constPinUrl) + appDeviceStr.replacingOccurrences(of: "star", with: "e"))] = formatter.string(from: message.timestamp ?? Date())
        //: info?["isRead"] = senderId == FacultyReactiveCompatible.share.loginUserMode.userID ? "1" : "0"
        info?[(String(userExtraMsg) + notiFoundMsg.replacingOccurrences(of: "language", with: "d"))] = senderId == FacultyReactiveCompatible.share.loginUserMode.userID ? "1" : "0"

        //: return info ?? [:]
        return info ?? [:]
    }

    //: public class func checkPrimaryKey(withMsgModel tempModel: V2TIMMessage) -> String {
    public class func expression(withMsgModel tempModel: V2TIMMessage) -> String {
        //: let v2Info = packageDict(withTXMessage: tempModel)
        let v2Info = arena(withTXMessage: tempModel)
        //: let key = "\(v2Info["msgId"] ?? "")"
        let key = "\(v2Info[(String(showFindMessage.suffix(5)))] ?? "")"
        //: print("key = \(key)")
        //: return key
        return key
    }

    // MARK: - JSON处理方法

    //: @objc public class func parseTXMessageData(data: Data) -> [String: Any] {
    @objc public class func manage(data: Data) -> [String: Any] {
        //: do {
        do {
            //: let dict = try JSONSerialization.jsonObject(
            let dict = try JSONSerialization.jsonObject(
                //: with: data, options: .mutableContainers)
                with: data, options: .mutableContainers
            )
            //: return dict as? [String: Any] ?? [:]
            return dict as? [String: Any] ?? [:]
            //: } catch {
        } catch {
            //: return [:]
            return [:]
        }
    }

    //: @objc public class func parseTXMessageJsonStr(data: [String: Any]) -> String {
    @objc public class func situation(data: [String: Any]) -> String {
        //: do {
        do {
            //: let jsonData = try JSONSerialization.data(
            let jsonData = try JSONSerialization.data(
                //: withJSONObject: data, options: [])
                withJSONObject: data, options: []
            )
            //: return String(data: jsonData, encoding: .utf8) ?? ""
            return String(data: jsonData, encoding: .utf8) ?? ""
            //: } catch {
        } catch {
            //: return ""
            return ""
        }
    }

    // MARK: - 语音消息检测

    //: @objc public class func checkVoiceMsg(msginfo: [String: Any]) -> Bool {
    @objc public class func subMsginfo(msginfo: [String: Any]) -> Bool {
        //: if let msgType = msginfo["msgType"] as? String, msgType == "audio" {
        if let msgType = msginfo[(String(dataMovementKey.suffix(7)))] as? String, msgType == (mainLabKey.replacingOccurrences(of: "oval", with: "i")) {
            //: return true
            return true
        }
        //: if let contentType = msginfo["contentType"] as? String,
        if let contentType = msginfo[(main_stillWoundUrl.replacingOccurrences(of: "less", with: "o") + "entT" + app_computeValue.replacingOccurrences(of: "image", with: "y"))] as? String,
           //: contentType == "AudioMsg"
           contentType == (String(mainSquareKey.prefix(4)) + "oMsg")
        {
            //: return true
            return true
        }
        //: if let audioData = msginfo["audioData"] as? String, !audioData.isEmpty {
        if let audioData = msginfo[(String(dataSearchionMessage))] as? String, !audioData.isEmpty {
            //: return true
            return true
        }
        //: if let audioUri = msginfo["audioUri"] as? String, !audioUri.isEmpty {
        if let audioUri = msginfo[(notiMatedPath.replacingOccurrences(of: "within", with: "u") + "oUri")] as? String, !audioUri.isEmpty {
            //: return true
            return true
        }
        //: return false
        return false
    }
}
