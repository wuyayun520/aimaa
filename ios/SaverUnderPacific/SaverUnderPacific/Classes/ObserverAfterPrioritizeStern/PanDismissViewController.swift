
//: Declare String Begin

/*: "init(coder:) has not been implemented" :*/
fileprivate let k_subName:[UInt8] = [0x4d,0x4a,0x4d,0x50,0xc,0x47,0x4b,0x40,0x41,0x56,0x1e,0xd,0x4,0x4c,0x45,0x57,0x4,0x4a,0x4b,0x50,0x4,0x46,0x41,0x41,0x4a,0x4,0x4d,0x49,0x54,0x48,0x41,0x49,0x41,0x4a,0x50,0x41,0x40]

/*: "roomId" :*/
fileprivate let const_earnPactPath:String = "structure noneroomId"

/*: "type" :*/
fileprivate let main_contextKey:[UInt8] = [0x65,0x70,0x79,0x74]

/*: "extra" :*/
fileprivate let show_femaleUniqueUrl:String = "exfarmerra"

/*: "jumpKey" :*/
fileprivate let const_renderBindData:[UInt8] = [0x58,0x47,0x5f,0x42,0x79,0x57,0x4b]

private func showTiming(yes num: UInt8) -> UInt8 {
    return num ^ 50
}

/*: "Send Gift" :*/
fileprivate let mainPremiumResolutionPath:[Character] = ["S","e","n","d"," ","G","i","f","t"]

/*: "View info" :*/
fileprivate let app_refugeeKeyTitle:String = "guide within execute tingView "
fileprivate let noti_capFormat:[Character] = ["i","n","f","o"]

/*: "Report" :*/
fileprivate let show_sinkString:[Character] = ["R","e","p","o","r","t"]

/*: "uid" :*/
fileprivate let appLackProductionMessage:[UInt8] = [0xc1,0xdd,0xd0]

private func borderLive(gift num: UInt8) -> UInt8 {
    return num ^ 180
}

/*: "name" :*/
fileprivate let kParticleName:[UInt8] = [0x88,0x87,0x8b,0x83]

private func wenChain(deal num: UInt8) -> UInt8 {
    return num ^ 230
}

/*: "Can't send blank message" :*/
fileprivate let kSoundSinceMsg:[Character] = ["C","a","n","\'","t"," ","s","e","n","d"," ","b","l"]
fileprivate let main_factTitle:[Character] = ["a"]
fileprivate let constFitValue:[Character] = ["n","k"," ","m","e","s","s","a","g","e"]

/*: "user" :*/
fileprivate let notiMemberFormat:[Character] = ["u","s","e","r"]

/*: "content" :*/
fileprivate let data_remainStr:[UInt8] = [0xaa,0xa6,0xa7,0xbd,0xac,0xa7,0xbd]

private func immediateBring(promise num: UInt8) -> UInt8 {
    return num ^ 201
}

/*: "msgType" :*/
fileprivate let appFollowId:String = "malongside"
fileprivate let app_placeFormat:[Character] = ["g","T","y","p","e"]

/*: "txt" :*/
fileprivate let k_checkBorderId:String = "riotxriot"

/*: "isGroup" :*/
fileprivate let showCanBusinessMessage:[Character] = ["i","s","G","r","o","u"]
fileprivate let data_packageMessage:[Character] = ["p"]

/*: "jumps" :*/
fileprivate let k_recognizeKey:[Character] = ["j","u","m","p","s"]

/*: "message" :*/
fileprivate let constMarkDownId:[UInt8] = [0x6e,0x66,0x70,0x70,0x62,0x64,0x66]

private func labYou(flag num: UInt8) -> UInt8 {
    return num ^ 3
}

/*: "TxtMsg" :*/
fileprivate let dataEveryValue:[Character] = ["T","x","t","M","s","g"]

/*: "toUid" :*/
fileprivate let mainFailUrl:String = "toUidstrategy area"

/*: "msgInfo" :*/
fileprivate let dataAspectCollectionPath:[Character] = ["m","s","g","I","n","f","o"]

/*: "params" :*/
fileprivate let user_reflectString:[UInt8] = [0x73,0x6d,0x61,0x72,0x61,0x70]

/*: "contentType" :*/
fileprivate let constFunId:[UInt8] = [0xd1,0xdd,0xdc,0xc6,0xd7,0xdc,0xc6,0xe6,0xcb,0xc2,0xd7]

private func activeHere(route num: UInt8) -> UInt8 {
    return num ^ 178
}

/*: "ImgMsg" :*/
fileprivate let constExactlyPath:[Character] = ["I","m","g","M","s","g"]

/*: "img" :*/
fileprivate let appFamilyRegulateName:[Character] = ["i","m","g"]

/*: "imageUri" :*/
fileprivate let kHerUrl:[UInt8] = [0x69,0x72,0x55,0x65,0x67,0x61,0x6d,0x69]

/*: "pic" :*/
fileprivate let dataObjectMsg:String = "pwhyc"

/*: "msgId" :*/
fileprivate let main_lessRemoteId:[Character] = ["m","s","g","I","d"]

/*: "totalMfCoin" :*/
fileprivate let notiFieldFormat:String = "totamix"
fileprivate let k_roundingString:String = "group correctMfCoin"

/*: "giftId" :*/
fileprivate let mainSinkMsg:String = "giftIresign float which sale careful"
fileprivate let main_movementMsg:[Character] = ["d"]

/*: "giftNum" :*/
fileprivate let show_matchMsg:String = "fit samplegiftNu"
fileprivate let app_continuingPath:String = "M"

/*: "pkgItemsetId" :*/
fileprivate let kCornerName:[Character] = ["p","k","g","I","t","e","m","s","e","t","I","d"]

/*: "msg" :*/
fileprivate let user_aroundId:String = "msaria"

/*: "tips" :*/
fileprivate let const_writePath:String = "touterps"

/*: "gift" :*/
fileprivate let main_eagerRevenueMessage:[Character] = ["g","i","f","t"]

/*: "nickname" :*/
fileprivate let main_disappearName:[Character] = ["n","i","c","k","n","a","m","e"]

/*: "headPic" :*/
fileprivate let show_styleStr:[UInt8] = [0x4f,0x42,0x46,0x43,0x77,0x4e,0x44]

private func priorityLogic(appropriate num: UInt8) -> UInt8 {
    return num ^ 39
}

/*: "All Numbers" :*/
fileprivate let main_mageAccountStr:[Character] = ["A","l","l"," ","N","u","m","b","e","r"]
fileprivate let userAttentionGivingString:String = "po"

/*: "sendNum" :*/
fileprivate let noti_mountMessage:String = "sendNumprocess tolerance"

/*: "toUser" :*/
fileprivate let main_sincePath:String = "white give lock clear blindtoUse"
fileprivate let dataCreateerMessage:String = "point"

/*: "msgIncome" :*/
fileprivate let user_leaderFindId:[Character] = ["m","s","g","I","n"]
fileprivate let userNoteMsg:[Character] = ["c","o","m","e"]

/*: "isConnection" :*/
fileprivate let show_wouldMessage:String = "isConneroll weak target clip event"
fileprivate let constIonName:String = "challengetion"

//: Declare String End

// __DEBUG__
// __CLOSE_PRINT__
//
//  PanDismissViewController.swift
//  AbroadTalking
//
//  Created by young on 2023/4/4.
//

//: import UIKit
import UIKit

//: class TalkingGroupChatController: TalkingBaseViewController {
class PanDismissViewController: EasyChainSensor {
    //: var infoModel = TalkingChatRoomInfoModel()
    var infoModel = IdentityTask() // 群聊信息
    //: private let disposeBag = DisposeBag()
    private let disposeBag = DisposeBag()
    //: private var tipsTimer: Timer?
    private var tipsTimer: Timer? // 提示消息定时器
    //: private var conversationData = TalkingConversationModel()
    private var conversationData = FormSubtleModel() // 会话cellData
    //: private var conversationType: PacificUserType = .group
    private var conversationType: PacificUserType = .group // 会话类型
    //: private var atFlag = false
    private var atFlag = false // 记录是否@

    //: init(groupType: String, infoData: [String: Any]) {
    init(groupType: String, infoData: [String: Any]) {
        //: super.init(nibName: nil, bundle: nil)
        super.init(nibName: nil, bundle: nil)
        //: hideNavi = true
        hideNavi = true
        //: if let model = TalkingChatRoomInfoModel.deserialize(from: infoData) {
        if let model = IdentityTask.deserialize(from: infoData) {
            //: infoModel = model
            infoModel = model
        }
        //: conversationData.groupID = infoModel.roomId
        conversationData.groupID = infoModel.roomId
        //: conversationData.groupType = groupType
        conversationData.groupType = groupType
        //: if groupType == GroupType_Meeting {
        if groupType == GroupType_Meeting {
            //: conversationType = .meeting
            conversationType = .meeting
        }
    }

    //: required init?(coder: NSCoder) {
    required init?(coder: NSCoder) {
        //: fatalError("init(coder:) has not been implemented")
        fatalError(String(bytes: k_subName.map{$0^36}, encoding: .utf8)!)
    }

    //: override func loadView() {
    override func loadView() {
        //: if #available(iOS 13.2, *) {
        if #available(iOS 13.2, *) {
            //: if MarginExamineer.share.appConfigMode.disableShootScreen {
            if MarginExamineer.share.appConfigMode.disableShootScreen {
                //: super.loadView()
                super.loadView()
                //: } else {
            } else {
                //: let bgView = MAScreenShieldView(frame: CGRect(x: 0, y: 0, width: ScreenWidth, height: ScreenHeight))
                let bgView = OccasionAll(frame: CGRect(x: 0, y: 0, width: show_errorChangeFormat, height: appLoadMessage))
                //: self.view = bgView
                self.view = bgView
            }
            //: } else {
        } else {
            //: super.loadView()
            super.loadView()
        }
    }

    //: override func viewDidLoad() {
    override func viewDidLoad() {
        //: super.viewDidLoad()
        super.viewDidLoad()
        //: createUI()
        offer()
        //: createUIConstraints()
        slowMention()
        //: bindInteraction()
        picture()
        //: addTapGestureRecognizer()
        reservePanel()
        //: func__chatRoom_insertTipMsg()
        evaluate()
        //: chatSettings()
        broadcast()

        //: if conversationType == .private {
        if conversationType == .private {
            //: initCachEffectData()
            secure()
        }
    }

    //: override func viewDidAppear(_ animated: Bool) {
    override func viewDidAppear(_ animated: Bool) {
        //: super.viewDidAppear(animated)
        super.viewDidAppear(animated)
        //: if self.atFlag {
        if self.atFlag {
            //: self.msgInputView.inputTextView.becomeFirstResponder()
            self.msgInputView.inputTextView.becomeFirstResponder()
            //: self.atFlag = false
            self.atFlag = false
        }
    }

    //: override func viewWillDisappear(_ animated: Bool) {
    override func viewWillDisappear(_ animated: Bool) {
        //: super.viewWillDisappear(animated)
        super.viewWillDisappear(animated)
        //: view.endEditing(true)
        view.endEditing(true)
    }

    //: deinit {
    deinit {
        //: if self.conversationData.groupType == GroupType_Meeting {
        if self.conversationData.groupType == GroupType_Meeting {
            //: V2TIMManager.sharedInstance().quitGroup(self.conversationData.groupID, succ: nil, fail: nil)
            V2TIMManager.sharedInstance().quitGroup(self.conversationData.groupID, succ: nil, fail: nil)
        }
        //: self.func__joinOrQuitRoom(join: false)
        self.native(join: false)
        //: self.destroyTipsTimer()
        self.everyYield()
    }

    // MARK: - Lazy Load

    //: private lazy var msgView: TUIMessageController = {
    private lazy var msgView: DeployViewController = {
        //: let msgVC = TUIMessageController.init()
        let msgVC = DeployViewController()
        //: msgVC.delegate = self
        msgVC.delegate = self
        //: msgVC.view.backgroundColor = UIColor.clear
        msgVC.view.backgroundColor = UIColor.clear
        //: msgVC.tableView.backgroundColor = .clear
        msgVC.tableView.backgroundColor = .clear
        //: msgVC.tableView.register(TalkingGroupChatTextMsgCell.self, forCellReuseIdentifier: TalkingGroupChatTextMsgCell.className())
        msgVC.tableView.register(DoingFramework.self, forCellReuseIdentifier: DoingFramework.className())
        //: msgVC.tableView.register(TalkingChatImageMsgCell.self, forCellReuseIdentifier: TalkingChatImageMsgCell.className())
        msgVC.tableView.register(PageFactor.self, forCellReuseIdentifier: PageFactor.className())
        //: msgVC.tableView.register(TalkingGroupChatGiftMsgCell.self, forCellReuseIdentifier: TalkingGroupChatGiftMsgCell.className())
        msgVC.tableView.register(StageView.self, forCellReuseIdentifier: StageView.className())
        //: msgVC.tableView.register(TalkingChatTipsMsgCell.self, forCellReuseIdentifier: TalkingChatTipsMsgCell.className())
        msgVC.tableView.register(EagerSpeed.self, forCellReuseIdentifier: EagerSpeed.className())
        //: msgVC.tableView.register(TUISystemMessageCell.self, forCellReuseIdentifier: BlackBy.TSystemMessageCell_ReuseId)
        msgVC.tableView.register(TensionIndependent.self, forCellReuseIdentifier: BlackBy.TSystemMessageCell_ReuseId)
        //: msgVC.currentChatType = conversationType
        msgVC.currentChatType = conversationType
        //: return msgVC
        return msgVC
        //: }()
    }()

    //: private lazy var msgInputView: TalkingChatInputView = {
    private lazy var msgInputView: WorkSend = {
        //: let view = TalkingChatInputView(type: .GroupChat)
        let view = WorkSend(type: .GroupChat)
        //: view.chatRoomID = infoModel.roomId
        view.chatRoomID = infoModel.roomId
        //: view.delegate = self
        view.delegate = self
        //: return view
        return view
        //: }()
    }()

    //: private lazy var giftEffectView: TalkingPrivateChatAnimatView = {
    private lazy var giftEffectView: ThresholdView = {
        //: let effectView = TalkingPrivateChatAnimatView.init()
        let effectView = ThresholdView()
        //: effectView.contentMode = .scaleAspectFill
        effectView.contentMode = .scaleAspectFill
        //: return effectView
        return effectView
        //: }()
    }()

    //: private lazy var giftTrackView: TalkingGiftTrackView = {
    private lazy var giftTrackView: AgileClean = {
        //: let trackView = TalkingGiftTrackView.init()
        let trackView = AgileClean()
        //: trackView.isUserInteractionEnabled = false
        trackView.isUserInteractionEnabled = false
        //: return trackView
        return trackView
        //: }()
    }()

    //: private lazy var navView: TalkingChatNavTitleView = {
    private lazy var navView: RouteConsumer = {
        //: let navView = TalkingChatNavTitleView(type: conversationType)
        let navView = RouteConsumer(type: conversationType)
        //: navView.titleLabel.text = infoModel.name
        navView.titleLabel.text = infoModel.name
        //: return navView
        return navView
        //: }()
    }()

    //: private lazy var someoneView: TalkingSomeoneAtMeView = {
    private lazy var someoneView: ComputePainter = {
        //: let v = TalkingSomeoneAtMeView()
        let v = ComputePainter()
        //: v.isHidden = true
        v.isHidden = true
        //: return v
        return v
        //: }()
    }()

    //: private lazy var mentionInfoArr: [[String: Any]] = {
    private lazy var mentionInfoArr: [[String: Any]] = //: return [[String: Any]]()
        .init()
    //: }()
}

// MARK: - Load Data

//: extension TalkingGroupChatController {
extension PanDismissViewController {
    /// 群聊设置
    //: private func chatSettings() {
    private func broadcast() {
        //: TalkingGroupChatManager.share.chatRoomVC = self
        HalfLength.share.chatRoomVC = self

        //: func__joinOrQuitRoom(join: true)
        native(join: true)
    }

    /// 加入或退出聊天室
    /// - Parameter join: 是否加入
    //: private func func__joinOrQuitRoom(join: Bool) {
    private func native(join: Bool) {
        //: let params = ["roomId": self.conversationData.groupID, "type": (join == true ? 1:2)] as [String: Any]
        let params = [(String(const_earnPactPath.suffix(6))): self.conversationData.groupID, String(bytes: main_contextKey.reversed(), encoding: .utf8)!: join == true ? 1 : 2] as [String: Any]
        //: TalkingChatRequestTool.req_chatRoomJoinOrQuit(params: params) { succeed, result, errorModel in
        PriorMedia.publishTo(params: params) { succeed, result, errorModel in
            //: guard succeed else { return }
            guard succeed else { return }
//            guard let data = result as? [String: Any] else {
//                return
//            }
//
//            if join {
//
//            }
        }
    }
}

// MARK: - AdjustLength

//: extension TalkingGroupChatController: TMessageControllerDelegate {
extension PanDismissViewController: AdjustLength {
    //: func messageController(_ controller: TUIMessageController!, onNewMessage data: V2TIMMessage!) -> AlongCrucial! {
    func flexible(_ controller: DeployViewController!, heap data: V2TIMMessage!) -> AlongCrucial! {
        //: let dic = NeedSpecify.parseTXMessageData(data: data.customElem.data)
        let dic = NeedSpecify.onStrategy(data: data.customElem.data)
        //: let cellData = TUISocialChatManager.handleTXReceiveMessage(withExtral: dic, cusData: data.localCustomData, tempModel: data)
        let cellData = IndependentObserve.moreBloc(withExtral: dic, cusData: data.localCustomData, tempModel: data)

        //: return cellData
        return cellData
    }

    //: func messageController(_ controller: TUIMessageController!, onShowMessageData data: AlongCrucial!) -> BlackBy! {
    func nearCompose(_ controller: DeployViewController!, shadow data: AlongCrucial!) -> BlackBy! {
        //: if data.isKind(of: TalkingGroupChatTextMsgCellData.self) {
        if data.isKind(of: GlobalDuring.self) {
            //: let cell = TalkingGroupChatTextMsgCell.init(style: .default, reuseIdentifier: TalkingGroupChatTextMsgCell.className())
            let cell = DoingFramework(style: .default, reuseIdentifier: DoingFramework.className())
            //: let cellData = data as! TalkingGroupChatTextMsgCellData
            let cellData = data as! GlobalDuring
            //: cell.fill(with: cellData)
            cell.satisfyAssociated(with: cellData)
            //: cellData.atBlock = { [weak self] uid in
            cellData.atBlock = { [weak self] uid in
                //: guard let self = self else { return }
                guard let self = self else { return }
                //: self.menu_goToUserDetailController(uid: uid)
                self.asset(uid: uid)
            }
            //: cell.transBlock = { [weak self] in
            cell.transBlock = { [weak self] in
                //: guard let self = self else { return }
                guard let self = self else { return }
                //: self.msgView.translateMsg(with: cellData)
                self.msgView.exit(cellData)
            }

            //: return cell
            return cell
        }

        //: if data.isKind(of: TalkingChatImageMsgCellData.self) {
        if data.isKind(of: DelayComposition.self) {
            //: let cell = TalkingChatImageMsgCell.init(style: .default, reuseIdentifier: TalkingChatImageMsgCell.className())
            let cell = PageFactor(style: .default, reuseIdentifier: PageFactor.className())
            //: let cellData = data as! TalkingChatImageMsgCellData
            let cellData = data as! DelayComposition
            //: cell.fill(with: cellData)
            cell.satisfyAssociated(with: cellData)
            //: return cell
            return cell
        }

        //: if data.isKind(of: TalkingGroupChatGiftMsgCellData.self) {
        if data.isKind(of: MultiLastSine.self) {
            //: let cell = TalkingGroupChatGiftMsgCell.init(style: .default, reuseIdentifier: TalkingGroupChatGiftMsgCell.className())
            let cell = StageView(style: .default, reuseIdentifier: StageView.className())
            //: let cellData = data as! TalkingGroupChatGiftMsgCellData
            let cellData = data as! MultiLastSine
            //: cell.fill(with: cellData)
            cell.satisfyAssociated(with: cellData)

            //: return cell
            return cell
        }

        //: if data.isKind(of: TalkingChatTipsMsgCellData.self) {
        if data.isKind(of: ToAdjust.self) {
            //: let cell = TalkingChatTipsMsgCell.init(style: .default, reuseIdentifier: TalkingChatTipsMsgCell.className())
            let cell = EagerSpeed(style: .default, reuseIdentifier: EagerSpeed.className())
            //: let cellData = data as! TalkingChatTipsMsgCellData
            let cellData = data as! ToAdjust
            //: cell.fill(with: cellData)
            cell.satisfyAssociated(with: cellData)
            //: return cell
            return cell
        }

        //: return nil
        return nil
    }

    //: func messageController(_ controller: TUIMessageController!, onSelectMessageAvatar cell: BlackBy!) {
    func isolate(forDelay controller: DeployViewController!, user cell: BlackBy!) {
        //: if let userModel = cell.messageData?.msgModel.user {
        if let userModel = cell.messageData?.msgModel.user {
            //: if userModel.uid == Int(MarginExamineer.share.loginUid) {
            if userModel.uid == Int(MarginExamineer.share.loginUid) {
                //: menu_goToUserDetailController(uid: userModel.uid )
                asset(uid: userModel.uid)
                //: }else{
            } else {
                //: menu_showMunuPopView(user: userModel)
                soundFromItem(user: userModel)
            }
        }
    }

    //: func messageController(_ controller: TUIMessageController!, onSelectMessageContent cell: BlackBy!) {
    func evaluation(_ controller: DeployViewController!, name cell: BlackBy!) {
        //: if cell .isKind(of: TalkingChatImageMsgCell.self) {
        if cell.isKind(of: PageFactor.self) { // 图片
            //: let imageCellData = cell.messageData as! TalkingChatImageMsgCellData
            let imageCellData = cell.messageData as! DelayComposition
            //: let libraryPath = NSSearchPathForDirectoriesInDomains(.cachesDirectory, .userDomainMask, true).first!
            let libraryPath = NSSearchPathForDirectoriesInDomains(.cachesDirectory, .userDomainMask, true).first!
            //: var filePath = (libraryPath as NSString).appendingPathComponent(imageFileName)
            var filePath = (libraryPath as NSString).appendingPathComponent(show_viewBottomName)
            //: filePath = (filePath as NSString).appendingPathComponent((imageCellData.imageUrl! as NSString).lastPathComponent)
            filePath = (filePath as NSString).appendingPathComponent((imageCellData.imageUrl! as NSString).lastPathComponent)

            //: var model = TalkingMomentPhotoModel.init()
            var model = AreaModel()
            //: if FileManager.default.fileExists(atPath: filePath) {
            if FileManager.default.fileExists(atPath: filePath) {
                //: let data = NSData.init(contentsOfFile: filePath)
                let data = NSData(contentsOfFile: filePath)
                //: model.imagePic = UIImage(data: data! as Data)
                model.imagePic = UIImage(data: data! as Data)
                //: } else {
            } else {
                //: if imageCellData.imageData != nil {
                if imageCellData.imageData != nil {
                    //: model.imagePic = imageCellData.imageData
                    model.imagePic = imageCellData.imageData
                    //: } else {
                } else {
                    //: model.imgUrl =  imageCellData.imageUrl
                    model.imgUrl = imageCellData.imageUrl
                }
            }
            //: var currenModel = TalkingMomentModel.init()
            var currenModel = StatusModel()
            //: var tempArr: [TalkingMomentPhotoModel] = []
            var tempArr: [AreaModel] = []
            //: tempArr.append(model)
            tempArr.append(model)
            //: currenModel.pic = tempArr
            currenModel.pic = tempArr
            //: let vc = TalkingMomentPhotosVC.init(momentModel: currenModel, index: 0, type: .normal)
            let vc = QuantityegrityViewController(momentModel: currenModel, index: 0, type: .normal)
            //: self.currentViewController()?.navigationController?.pushViewController(vc, animated: true)
            self.myAcross()?.navigationController?.pushViewController(vc, animated: true)

            //: } else if cell.isKind(of: TalkingGroupChatTextMsgCell.self) {
        } else if cell.isKind(of: DoingFramework.self) {
            //: let textCell = cell as! TalkingGroupChatTextMsgCell
            let textCell = cell as! DoingFramework
            //: let textData = textCell.messageData
            let textData = textCell.messageData
            //: let dic = NeedSpecify.parseTXMessageData(data: (textData?.innerMessage.customElem.data)!)
            let dic = NeedSpecify.onStrategy(data: (textData?.innerMessage.customElem.data)!)
            //: guard let extraDic = dic["extra"] as? Dictionary<String, Any> else {
            guard let extraDic = dic[(show_femaleUniqueUrl.replacingOccurrences(of: "farmer", with: "t"))] as? [String: Any] else {
                //: return
                return
            }
            //: if extraDic.keys.contains("jumpKey") {
            if extraDic.keys.contains(String(bytes: const_renderBindData.map{showTiming(yes: $0)}, encoding: .utf8)!) {
                //: if let jumpModel = JSONDeserializer<TalkingMessageJumpModel>.deserializeFrom(dict: extraDic, designatedPath: nil) {
                if let jumpModel = JSONDeserializer<PopModel>.deserializeFrom(dict: extraDic, designatedPath: nil) {
                    //: OriginMediumWill.share.func__actionPushForMessage(jumpModel: jumpModel)
                    OriginMediumWill.share.brokenDisabled(jumpModel: jumpModel)
                }
            }

            //: } else if cell.isKind(of: TalkingChatTipsMsgCell.self) {
        } else if cell.isKind(of: EagerSpeed.self) {
            //: let tipCell = cell as! TalkingChatTipsMsgCell
            let tipCell = cell as! EagerSpeed
            //: let tipData = tipCell.messageData
            let tipData = tipCell.messageData
            //: let dic = NeedSpecify.parseTXMessageData(data: (tipData?.innerMessage.customElem.data)!)
            let dic = NeedSpecify.onStrategy(data: (tipData?.innerMessage.customElem.data)!)
            //: guard let extraDic = dic["extra"] as? Dictionary<String, Any> else {
            guard let extraDic = dic[(show_femaleUniqueUrl.replacingOccurrences(of: "farmer", with: "t"))] as? [String: Any] else {
                //: return
                return
            }
            //: if extraDic.keys.contains("jumpKey") {
            if extraDic.keys.contains(String(bytes: const_renderBindData.map{showTiming(yes: $0)}, encoding: .utf8)!) {
                //: if let jumpModel = JSONDeserializer<TalkingMessageJumpModel>.deserializeFrom(dict: extraDic, designatedPath: nil) {
                if let jumpModel = JSONDeserializer<PopModel>.deserializeFrom(dict: extraDic, designatedPath: nil) {
                    //: OriginMediumWill.share.func__actionPushForMessage(jumpModel: jumpModel)
                    OriginMediumWill.share.brokenDisabled(jumpModel: jumpModel)
                }
            }
        }
    }

    //: func messageController(_ controller: TUIMessageController!, onRetryMessageContent cell: BlackBy!) {
    func stackHalf(_ controller: DeployViewController!, thanMobile cell: BlackBy!) {
        //: func__sendPriveteMsg(msgData: cell.messageData!, reSend: true)
        universalMaximumPrevious(msgData: cell.messageData!, reSend: true)
    }
}

// MARK: - Event

//: extension TalkingGroupChatController {
extension PanDismissViewController {
    /// 销毁倒计时
    //: private func destroyTipsTimer() {
    private func everyYield() {
        //: if tipsTimer != nil {
        if tipsTimer != nil {
            //: tipsTimer?.invalidate()
            tipsTimer?.invalidate()
            //: tipsTimer = nil
            tipsTimer = nil
        }
    }

    /// 点击用户头像，展示操作面板
    /// - Parameter user: 用户模型
    //: private func menu_showMunuPopView(user: AbTalkingChatMsgUserModel) {
    private func soundFromItem(user: NextModel) {
        //: view.endEditing(true)
        view.endEditing(true)
        //: let vc = TalkingMunuPopView.init(frame: self.view.frame)
        let vc = PinSecond(frame: self.view.frame)
        //: vc.title = user.nickname
        vc.title = user.nickname
        //: vc.initwithList(cellTitleList: ["Send Gift".localized, "@", "View info".localized, "Report".localized])
        vc.techniqueCapacity(cellTitleList: [(String(mainPremiumResolutionPath)).localized, "@", (String(app_refugeeKeyTitle.suffix(5)) + String(noti_capFormat)).localized, (String(show_sinkString)).localized])
        //: vc.munuBlock = { [weak self] index, str in
        vc.munuBlock = { [weak self] index, str in
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: if index == 0 {
            if index == 0 {
                //: self.menu_sentGiftAction(user: user)
                self.sharedVisible(user: user)
                //: } else if index == 1 {
            } else if index == 1 {
                //: self.menu_atTa(user: user)
                self.pageDevice(user: user)
                //: } else if index == 2 {
            } else if index == 2 {
                //: self.menu_goToUserDetailController(uid: user.uid)
                self.asset(uid: user.uid)
                //: } else if index == 3 {
            } else if index == 3 {
                //: self.menu_reportAction(uid: String(user.uid))
                self.iconSatisfy(uid: String(user.uid))
            }
        }
    }

    /// 送礼
    /// - Parameter user: 用户模型
    //: private func menu_sentGiftAction(user: AbTalkingChatMsgUserModel) {
    private func sharedVisible(user: NextModel) {
        //: self.msgInputView.clickgiftBtn()
        self.msgInputView.phase()
        //: let model = TalkingChatRoomMemberModel()
        let model = ReduceModel()
        //: model.uid = "\(user.uid)"
        model.uid = "\(user.uid)"
        //: model.nickname = user.nickname
        model.nickname = user.nickname
        //: model.headPic = user.headPic
        model.headPic = user.headPic
        //: model.sex = user.sex
        model.sex = user.sex
        //: model.age = Int(user.age) ?? 0
        model.age = Int(user.age) ?? 0
        //: self.msgInputView.giftView.didClickSelectedRow(model: model)
        self.msgInputView.giftView.onicialUponTriggerLast(model: model)
    }

    /// @Ta
    /// - Parameter user: 用户模型
    //: private func menu_atTa(user: AbTalkingChatMsgUserModel) {
    private func pageDevice(user: NextModel) {
        //: let dict: [String: Any] = ["uid": String(user.uid), "name": user.nickname]
        let dict: [String: Any] = [String(bytes: appLackProductionMessage.map{borderLive(gift: $0)}, encoding: .utf8)!: String(user.uid), String(bytes: kParticleName.map{wenChain(deal: $0)}, encoding: .utf8)!: user.nickname]
        //: self.msgInputView.receiveAtInfo(range: nil, dict: dict)
        self.msgInputView.within(range: nil, dict: dict)
        //: self.msgInputView.inputTextView.becomeFirstResponder()
        self.msgInputView.inputTextView.becomeFirstResponder()
    }

    /// 访问用户详情
    /// - Parameter uid: 用户Id
    //: private func menu_goToUserDetailController(uid: Int) {
    private func asset(uid: Int) {
        //: OriginMediumWill.share.func__pushToUserDetailVC(uid: String(uid))
        OriginMediumWill.share.petAcrossAm(uid: String(uid))
    }

    /// 举报
    /// - Parameter uid: 用户Id
    //: private func menu_reportAction(uid: String) {
    private func iconSatisfy(uid: String) {
        //: self.msgInputView.showOtherInputView = true
        self.msgInputView.showOtherInputView = true
        //: let reportView = TalkingReportAlertView.init(frame: UIScreen.main.bounds, type: .reportUserType, rId: uid)
        let reportView = ReadExclusiveRateAppearanceView(frame: UIScreen.main.bounds, type: .reportUserType, rId: uid)
        //: reportView.showReportViewIn(view: nil)
        reportView.medal(view: nil)
        //: reportView.reportHideBlock = { [weak self] _ in
        reportView.reportHideBlock = { [weak self] _ in
            //: guard let self = self else {return}
            guard let self = self else { return }
            //: self.msgInputView.showOtherInputView = false
            self.msgInputView.showOtherInputView = false
            //: return
        }
    }
}

// MARK: - 发送消息

//: extension TalkingGroupChatController {
extension PanDismissViewController {
    /// 发送文本消息
    /// - Parameter msgStr: 文本

    //: internal func func__sendTextMsg(msgStr: String, quoteModel: AbTalkingChatMsgQuoteModel?) {
    func now(msgStr: String, quoteModel: LocationModel?) {
        //: let temStr = msgStr.trimmingCharacters(in: CharacterSet.whitespacesAndNewlines)
        let temStr = msgStr.trimmingCharacters(in: CharacterSet.whitespacesAndNewlines)
        //: if temStr.count == 0 {
        if temStr.count == 0 {
            //: func__showStatusBarErrorMsg(showMsg: "Can't send blank message".localized)
            exitMarker(showMsg: (String(kSoundSinceMsg) + String(main_factTitle) + String(constFitValue)).localized)
            //: return
            return
        }

        //: let uidStr = NSMutableString()
        let uidStr = NSMutableString()
        //: var jumps = [[String: String]]()
        var jumps = [[String: String]]()
        //: if msgInputView.atArr.count > 0 {
        if msgInputView.atArr.count > 0 { // 有@ 消息
            //: msgInputView.atArr.forEach { uid in
            msgInputView.atArr.forEach { uid in
                //: let dict = ["jumpKey": "user", "uid": uid]
                let dict = [String(bytes: const_renderBindData.map{showTiming(yes: $0)}, encoding: .utf8)!: (String(notiMemberFormat)), String(bytes: appLackProductionMessage.map{borderLive(gift: $0)}, encoding: .utf8)!: uid]
                //: uidStr.append(uid+",")
                uidStr.append(uid + ",")
                //: jumps.append(dict)
                jumps.append(dict)
            }
            //: uidStr.deleteCharacters(in: NSRange(location: uidStr.length-1, length: 1))
            uidStr.deleteCharacters(in: NSRange(location: uidStr.length - 1, length: 1))
        }

        //: var msgInfo: [String: Any] = ["content": temStr, "msgType": "txt", "isGroup": "1"]
        var msgInfo: [String: Any] = [String(bytes: data_remainStr.map{immediateBring(promise: $0)}, encoding: .utf8)!: temStr, (appFollowId.replacingOccurrences(of: "alongside", with: "s") + String(app_placeFormat)): (k_checkBorderId.replacingOccurrences(of: "riot", with: "t")), (String(showCanBusinessMessage) + String(data_packageMessage)): "1"]
        //: if jumps.count > 0 {
        if jumps.count > 0 {
            //: msgInfo["jumps"] = jumps
            msgInfo[(String(k_recognizeKey))] = jumps
        }

        // 接口参数
        //: var paramsDic: [String: Any] = ["message": temStr,
        var paramsDic: [String: Any] = [String(bytes: constMarkDownId.map{labYou(flag: $0)}, encoding: .utf8)!: temStr,
                                        //: "type": "TxtMsg",
                                        String(bytes: main_contextKey.reversed(), encoding: .utf8)!: (String(dataEveryValue)),
                                        //: "roomId": self.infoModel.roomId]
                                        (String(const_earnPactPath.suffix(6))): self.infoModel.roomId]
        //: if uidStr.length > 0 {
        if uidStr.length > 0 {
            //: paramsDic["toUid"] = uidStr
            paramsDic[(String(mainFailUrl.prefix(5)))] = uidStr
        }

        //: let dic = ["msgInfo": msgInfo, "params": paramsDic]
        let dic = [(String(dataAspectCollectionPath)): msgInfo, String(bytes: user_reflectString.reversed(), encoding: .utf8)!: paramsDic]
        //: if let cellData = TUISocialChatManager.handleTXMessageSend(withExtral: dic, isSender: true, isTip: false) {
        if let cellData = IndependentObserve.on(withExtral: dic, isSender: true, isTip: false) {
            //: func__sendPriveteMsg(msgData: cellData, reSend: false)
            universalMaximumPrevious(msgData: cellData, reSend: false)
        }
    }

    /// 发送图片消息
    /// - Parameter img: 图片
    //: private func func__sendPictureMsg(img: UIImage) {
    private func computeCancel(img: UIImage) {
        //: let imagePath = TalkingChatImageMsgCellData.getChatImagePath(image: img)
        let imagePath = DelayComposition.product(image: img)
        //: let msgInfo: [String: Any] = ["contentType": "ImgMsg", "msgType": "img", "imageUri": imagePath]
        let msgInfo: [String: Any] = [String(bytes: constFunId.map{activeHere(route: $0)}, encoding: .utf8)!: (String(constExactlyPath)), (appFollowId.replacingOccurrences(of: "alongside", with: "s") + String(app_placeFormat)): (String(appFamilyRegulateName)), String(bytes: kHerUrl.reversed(), encoding: .utf8)!: imagePath]
        // 接口参数
        //: let paramsDic: [String: Any] = ["pic": imagePath,
        let paramsDic: [String: Any] = [(dataObjectMsg.replacingOccurrences(of: "why", with: "i")): imagePath,
                                        //: "type": "ImgMsg",
                                        String(bytes: main_contextKey.reversed(), encoding: .utf8)!: (String(constExactlyPath)),
                                        //: "roomId": self.infoModel.roomId]
                                        (String(const_earnPactPath.suffix(6))): self.infoModel.roomId]
        //: let dic = ["msgInfo": msgInfo, "params": paramsDic]
        let dic = [(String(dataAspectCollectionPath)): msgInfo, String(bytes: user_reflectString.reversed(), encoding: .utf8)!: paramsDic]
        //: if let cellData = TUISocialChatManager.handleTXMessageSend(withExtral: dic, isSender: true, isTip: false) {
        if let cellData = IndependentObserve.on(withExtral: dic, isSender: true, isTip: false) {
            //: func__sendPriveteMsg(msgData: cellData, reSend: false)
            universalMaximumPrevious(msgData: cellData, reSend: false)
        }
    }

    /// 发送消息
    /// - Parameters:
    ///   - msgData: 消息数据结构
    ///   - reSend: 是否重发【目前未使用该字段】
    //: private func func__sendPriveteMsg(msgData: AlongCrucial, reSend: Bool) {
    private func universalMaximumPrevious(msgData: AlongCrucial, reSend: Bool) {
        //: let reachability = try? Reachability()
        let reachability = try? Reachability()
        //: if reachability?.connection == .unavailable {
        if reachability?.connection == .unavailable {
            //: func__showStatusBarErrorMsg(showMsg: kNetErrorMsg)
            exitMarker(showMsg: kRequestPath)
            //: return
            return
        }
        // 插入本地
        //: self.msgView.sendCustomMessage(msgData)
        self.msgView.normSave(msgData)
        // 处理请求参数
        //: var params: Dictionary<String, Any> = msgData.msgModel.params as! Dictionary<String, Any>
        var params: [String: Any] = msgData.msgModel.params as! [String: Any]
//        params["toUid"] = self.targetId
        //: if params.keys.contains("pic") { // 图片
        if params.keys.contains((dataObjectMsg.replacingOccurrences(of: "why", with: "i"))) { // 图片
            //: let imageStr: String = params["pic"] as! String
            let imageStr: String = params[(dataObjectMsg.replacingOccurrences(of: "why", with: "i"))] as! String
            //: do {
            do {
                //: let data: Data = try Data.init(contentsOf: URL.init(fileURLWithPath: imageStr))
                let data: Data = try Data(contentsOf: URL(fileURLWithPath: imageStr))
                //: params["pic"] = data
                params[(dataObjectMsg.replacingOccurrences(of: "why", with: "i"))] = data
                //: } catch {
            } catch {
                //: printLog(message: error)
                printLog(message: error)
            }
        }

        // 发送消息接口
        //: TalkingChatRequestTool.req_SendRoomMsg(param: params, completion: { succeed, result, errorModel in
        PriorMedia.duringNative(param: params, completion: { succeed, result, errorModel in
            //: if !succeed {
            if !succeed {
                //: self.msgView.changeCustomMsg(msgData, msgId: nil, msgIncome: NSNumber(value: 0), status: .Msg_Status_Fail)
                self.msgView.functional(msgData, tillEffectDensity: nil, flushBorder: NSNumber(value: 0), overShadow: .Msg_Status_Fail)
                //: self.dealSendMsgError(errorCode: errorModel!.errorCode, errorStr: errorModel!.errorMsg, isGift: true, isResend: false)
                self.propertyOfPlate(errorCode: errorModel!.errorCode, errorStr: errorModel!.errorMsg, isGift: true, isResend: false)
                //: return
                return
            }

            //: let dataDict = result as! [String: Any]
            let dataDict = result as! [String: Any]
            //: let msgInfo = dataDict["msgInfo"] as! [String: Any]
            let msgInfo = dataDict[(String(dataAspectCollectionPath))] as! [String: Any]

            // 更新本地消息信息
            //: self.msgView.changeCustomMsg(msgData, msgId: msgInfo["msgId"] as? String, msgIncome: NSNumber(value: 0), status: .Msg_Status_Succ)
            self.msgView.functional(msgData, tillEffectDensity: msgInfo[(String(main_lessRemoteId))] as? String, flushBorder: NSNumber(value: 0), overShadow: .Msg_Status_Succ)

            //: if dataDict.keys.contains("totalMfCoin") {
            if dataDict.keys.contains((notiFieldFormat.replacingOccurrences(of: "mix", with: "l") + String(k_roundingString.suffix(6)))) {
                //: let totalMfCoin: NSNumber = dataDict["totalMfCoin"] as! NSNumber
                let totalMfCoin: NSNumber = dataDict[(notiFieldFormat.replacingOccurrences(of: "mix", with: "l") + String(k_roundingString.suffix(6)))] as! NSNumber
                //: MarginExamineer.share.loginUserMode.mf_coin = totalMfCoin.stringValue
                MarginExamineer.share.loginUserMode.mf_coin = totalMfCoin.stringValue
            }
            //: })
        })
    }

    /// 处理发送失败情况
    //: private func dealSendMsgError(errorCode: Int, errorStr: String, isGift: Bool, isResend: Bool) {
    private func propertyOfPlate(errorCode: Int, errorStr: String, isGift: Bool, isResend: Bool) {
        //: if errorCode == 50203 {
        if errorCode == 50203 {
            //: guard MarginExamineer.share.loginUserMode.status != 1 else {
            guard MarginExamineer.share.loginUserMode.status != 1 else {
                //: if !errorStr.isEmptyString {
                if !errorStr.isEmptyString {
                    //: func__showStatusBarErrorMsg(showMsg: errorStr)
                    exitMarker(showMsg: errorStr)
                }
                //: return
                return
            }

            //: OriginMediumWill.share.func__jumpToWebRecharge(clickEvent: clickSendingiftsButton, sufficient: false)
            OriginMediumWill.share.resumeUniversal(clickEvent: notiProgressId, sufficient: false)
            //: msgInputView.giftView.dismissView()
            msgInputView.giftView.fadePhone()
            //: insertTipMsg_Nobalance(tipStr: errorStr)
            face(tipStr: errorStr)
            //: }else if errorCode == 10888 {
        } else if errorCode == 10888 {
            //: let view = TalkingLiveVipSubscribePopView.init(frame: CGRect(x: 0, y: 0, width: ScreenWidth, height: ScreenHeight))
            let view = ProjectionView(frame: CGRect(x: 0, y: 0, width: show_errorChangeFormat, height: appLoadMessage))
            //: view.show()
            view.someModify()
            //: } else {
        } else {
            //: if !errorStr.isEmptyString {
            if !errorStr.isEmptyString {
                //: func__showStatusBarErrorMsg(showMsg: errorStr)
                exitMarker(showMsg: errorStr)
            }
        }
    }
}

// MARK: - 输入框

//: extension TalkingGroupChatController: ChatInputViewDelegate {
extension PanDismissViewController: PerformSum {
    //: func func__askforGiftMsg(giftModel: TalkingRoomGiftModel, giftNum: String) {
    func medium(giftModel: EvaluationMenuRate, giftNum: String) {}

    /// 输入框输入内容停顿超过1秒
    //: func func_inputContentIntervalOneSecond() {}
    func beauty() {}

    /// 输入框@ 在线列表用户
    //: func func__selectMentionUser(range: NSRange) {
    func yield(range: NSRange) {
        //: let vc = TalkingChatRoomOnlineListController()
        let vc = PopPriorViewController()
        //: vc.roomID = self.infoModel.roomId
        vc.roomID = self.infoModel.roomId
        //: vc.titleType = .callNumber
        vc.titleType = .callNumber
        //: vc.selectedBlock = {[weak self] dict in
        vc.selectedBlock = { [weak self] dict in
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: self.atFlag = true
            self.atFlag = true
            //: self.msgInputView.receiveAtInfo(range: range, dict: dict)
            self.msgInputView.within(range: range, dict: dict)
            //: self.navigationController?.popViewController(animated: true)
            self.navigationController?.popViewController(animated: true)
        }
        //: self.navigationController?.pushViewController(vc, animated: true)
        self.navigationController?.pushViewController(vc, animated: true)
    }

    //: func func__selectMoreFunAction(actionType: MoreActionType) {
    func over(actionType: MobileExercise) {
        //: switch actionType {
        switch actionType {
        //: case .Photo:
        case .Photo:
            //: func__choicePhotos()
            basicFor()

        //: default:
        default:
            //: break
            break
        }
    }

    //: func func__sendGiftMsg(giftModel: TalkingRoomGiftModel, giftNum: String) {}
    func selecter(giftModel: EvaluationMenuRate, giftNum: String) {}

    //: func func__sendChatRoomGiftMsg(giftModel: TalkingRoomGiftModel, giftNum: String, model: TalkingChatRoomMemberModel) {
    func stepEager(giftModel: EvaluationMenuRate, giftNum: String, model: ReduceModel) {
        //: let reachability = try? Reachability()
        let reachability = try? Reachability()
        //: if reachability?.connection == .unavailable {
        if reachability?.connection == .unavailable {
            //: func__showStatusBarErrorMsg(showMsg: kNetErrorMsg)
            exitMarker(showMsg: kRequestPath)
            //: return
            return
        }
        //: var params: Dictionary<String, Any> = [:]
        var params: [String: Any] = [:]
        //: params["toUid"] =  model.uid
        params[(String(mainFailUrl.prefix(5)))] = model.uid
        //: params["giftId"] = giftModel.gid
        params[(String(mainSinkMsg.prefix(5)) + String(main_movementMsg))] = giftModel.gid
        //: params["giftNum"] = giftNum
        params[(String(show_matchMsg.suffix(6)) + app_continuingPath.lowercased())] = giftNum
        //: params["roomId"] = infoModel.roomId
        params[(String(const_earnPactPath.suffix(6)))] = infoModel.roomId
        //: if giftModel.pkgItemsetId != nil {
        if giftModel.pkgItemsetId != nil {
            //: params["pkgItemsetId"] = giftModel.pkgItemsetId
            params[(String(kCornerName))] = giftModel.pkgItemsetId
        }

        // 送礼接口
        //: TalkingChatRequestTool.req_SendGiftChatRoom(param: params, completion: { succeed, result, errorModel in
        PriorMedia.anonymousDoing(param: params, completion: { succeed, result, errorModel in
            //: guard succeed else {
            guard succeed else {
                //: self.dealSendMsgError(errorCode: errorModel!.errorCode, errorStr: errorModel!.errorMsg, isGift: true, isResend: false)
                self.propertyOfPlate(errorCode: errorModel!.errorCode, errorStr: errorModel!.errorMsg, isGift: true, isResend: false)
                //: return
                return
            }

            // 更新本地礼物背包
            //: if giftModel.pkgItemsetId != nil {
            if giftModel.pkgItemsetId != nil {
                //: self.msgInputView.giftView.bags_removeGiftPackage(pkgItemsetId: giftModel.pkgItemsetId!, num: Int(giftNum) ?? 0)
                self.msgInputView.giftView.strategyBy(pkgItemsetId: giftModel.pkgItemsetId!, num: Int(giftNum) ?? 0)
            }
            //: self.func__insertGiftMessageWithExtral(extral: result as! Dictionary<String, Any?>, model: model, isFromInputView: true)
            self.server(extral: result as! [String: Any?], model: model, isFromInputView: true)
            //: })
        })
    }

    //: func heightToBottomChanged(heightToBottom: CGFloat) {
    func primary(heightToBottom: CGFloat) {
        //: msgInputView.snp.updateConstraints { make in
        msgInputView.snp.updateConstraints { make in
            //: make.bottom.equalTo(self.view).offset(-heightToBottom)
            make.bottom.equalTo(self.view).offset(-heightToBottom)
        }
        //: someoneView.snp.updateConstraints { make in
        someoneView.snp.updateConstraints { make in
            //: make.bottom.equalTo(msgInputView.snp.top).offset(-103)
            make.bottom.equalTo(msgInputView.snp.top).offset(-103)
        }
        //: DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 0.3) {
        DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 0.3) {
            //: self.msgView.paintAcross(true)
            self.msgView.paintAcross(true)
        }
    }

    //: func inputViewHeightChanged(height: CGFloat) {
    func phoneStructure(height: CGFloat) {
        //: self.msgInputView.snp.updateConstraints { make in
        self.msgInputView.snp.updateConstraints { make in
            //: make.height.equalTo(height)
            make.height.equalTo(height)
        }
        //: self.view.needsUpdateConstraints()
        self.view.needsUpdateConstraints()
        //: self.view.updateConstraintsIfNeeded()
        self.view.updateConstraintsIfNeeded()
        //: UIView.animate(withDuration: 0.3, animations: {
        UIView.animate(withDuration: 0.3, animations: {
            //: self.view.layoutIfNeeded()
            self.view.layoutIfNeeded()
            //: })
        })
        //: DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 0.3) {
        DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 0.3) {
            //: self.msgView.paintAcross(true)
            self.msgView.paintAcross(true)
        }
    }

    //: @objc override func func__TapGestureRecognizer(_ tap: UITapGestureRecognizer) {
    @objc override func purchase(_ tap: UITapGestureRecognizer) {
        //: super.func__TapGestureRecognizer(tap)
        super.purchase(tap)
    }

    //: func func__starRecordBtnDown() {}
    func occasionBan() {}

    //: func func__sendVoiceMsg(voicePath: String, voiceTime: Int) {}
    func displayGate(voicePath: String, voiceTime: Int) {}
}

// MARK: - 选择图片

//: extension TalkingGroupChatController: UIImagePickerControllerDelegate, UINavigationControllerDelegate {
extension PanDismissViewController: UIImagePickerControllerDelegate, UINavigationControllerDelegate {
    //: private func func__choicePhotos() {
    private func basicFor() {
        //: TalkingPermissionTool.func__openAlbumServiceWithBlock(true) { (isOpen: Bool) in
        MultiplePoint.outLimited(true) { (isOpen: Bool) in
            //: if isOpen {
            if isOpen {
                //: let vc = TalkingImagePickTool.initImagePickerVc(maxCount: 1, allowPhoto: true, allowVideo: false)
                let vc = ZonePosition.exAccelerateReliability(maxCount: 1, allowPhoto: true, allowVideo: false)
                //: vc.modalPresentationStyle = .fullScreen
                vc.modalPresentationStyle = .fullScreen
                //: OriginMediumWill.share.func__getCurrentActivityVC()?.present(vc, animated: true)
                OriginMediumWill.share.halfSplit()?.present(vc, animated: true)
                //: vc.didFinishPickingPhotosHandle = { [weak self] ( photos: Array<UIImage>?,
                vc.didFinishPickingPhotosHandle = { [weak self] (photos: [UIImage]?,
                                                                 //: assets: Array<Any>?,
                                                                 assets: [Any]?,
                                                                 //: isSelectOriginalPhoto: Bool?)  in
                                                                 isSelectOriginalPhoto: Bool?) in
                        //: guard let self = self else {return}
                        guard let self = self else { return }
                        //: if photos!.count > 0 {
                        if photos!.count > 0 {
                            //: self.func__sendPictureMsg(img: photos![0])
                            self.computeCancel(img: photos![0])
                        }
                }
            }
        }
    }

    //: private func func__camaraPhotos() {
    private func transform() {
        //: TalkingPermissionTool.func__openCaptureDeviceServiceWithBlock(true) { (isOpen: Bool) in
        MultiplePoint.closeicialLine(true) { (isOpen: Bool) in
            //: if isOpen {
            if isOpen {
                //: let imagePicker = UIImagePickerController()
                let imagePicker = UIImagePickerController()
                //: imagePicker.sourceType = .camera
                imagePicker.sourceType = .camera
                //: imagePicker.delegate = self
                imagePicker.delegate = self
                //: self.present(imagePicker, animated: true, completion: {})
                self.present(imagePicker, animated: true, completion: {})
            }
        }
    }

    //: func imagePickerController(_ picker: UIImagePickerController, didFinishPickingMediaWithInfo info: [UIImagePickerController.InfoKey: Any]) {
    func imagePickerController(_ picker: UIImagePickerController, didFinishPickingMediaWithInfo info: [UIImagePickerController.InfoKey: Any]) {
        //: self.dismiss(animated: true) {
        self.dismiss(animated: true) {
            //: if let photo = info[UIImagePickerController.InfoKey.originalImage] as! UIImage? {
            if let photo = info[UIImagePickerController.InfoKey.originalImage] as! UIImage? {
                //: self.func__sendPictureMsg(img: photo)
                self.computeCancel(img: photo)
            }
        }
    }

    //: func imagePickerControllerDidCancel(_ picker: UIImagePickerController) {
    func imagePickerControllerDidCancel(_ picker: UIImagePickerController) {
        //: self.dismiss(animated: true, completion: nil)
        self.dismiss(animated: true, completion: nil)
    }
}

// MARK: - 处理@ 消息

//: extension TalkingGroupChatController {
extension PanDismissViewController {
    /// 接收到群聊@ 消息
    /// - Parameter info: @消息数据
    //: func handleMentionMsg(info: [String: Any]) {
    func halfAndTransactionDescription(info: [String: Any]) {
        //: DispatchQueue.main.async {
        DispatchQueue.main.async {
            //: guard info["roomId"] as! String == self.conversationData.groupID else { return }
            guard info[(String(const_earnPactPath.suffix(6)))] as! String == self.conversationData.groupID else { return }
            //: if self.mentionInfoArr.count >= 5 {
            if self.mentionInfoArr.count >= 5 { // 只保留最新5条@ 消息
                //: self.mentionInfoArr.removeFirst()
                self.mentionInfoArr.removeFirst()
            }
            //: self.mentionInfoArr.append(info)
            self.mentionInfoArr.append(info)
            //: self.someoneView.isHidden = self.mentionInfoArr.count > 0 ? false:true
            self.someoneView.isHidden = self.mentionInfoArr.count > 0 ? false : true
        }
    }
}

// MARK: - 提示消息

//: extension TalkingGroupChatController {
extension PanDismissViewController {
    //: func func__TipsNotification() {
    func confirmInReduce() {
        //: NotificationCenter.default.addObserver(self,
        NotificationCenter.default.addObserver(self,
                                               //: selector: #selector(insertTipsNotif(notification:)),
                                               selector: #selector(mic(notification:)),
                                               //: name: CHAT_TIPS_TEXT_NOTIFICATION,
                                               name: notiSquareFeedbackSourceString,
                                               //: object: nil)
                                               object: nil)
    }

    //: @objc func insertTipsNotif(notification: NSNotification) {
    @objc func mic(notification: NSNotification) {
        //: let userinfo = notification.userInfo as! [String: AnyObject]
        let userinfo = notification.userInfo as! [String: AnyObject]
        //: let celldata: TalkingChatTipsMsgCellData  = userinfo["msg"] as! TalkingChatTipsMsgCellData
        let celldata: ToAdjust = userinfo[(user_aroundId.replacingOccurrences(of: "aria", with: "g"))] as! ToAdjust

        //: self.msgView.uiMsgs.add(celldata)
        self.msgView.uiMsgs.add(celldata)
        //: self.msgView.tableView.reloadData()
        self.msgView.tableView.reloadData()
        //: self.msgView.paintAcross(true)
        self.msgView.paintAcross(true)
    }

    /// 插入提示消息
    /// - Parameters:
    ///   - tipStr: 提示消息文本
    ///   - needInsertLocal: 是否需要插入本地数据库（默认：是）
    //: func insertTipMsg_Nobalance(tipStr: String, needInsertLocal: Bool = true) {
    func face(tipStr: String, needInsertLocal: Bool = true) {
        //: let msgInfo = ["content": tipStr, "msgType": "tips"]
        let msgInfo = [String(bytes: data_remainStr.map{immediateBring(promise: $0)}, encoding: .utf8)!: tipStr, (appFollowId.replacingOccurrences(of: "alongside", with: "s") + String(app_placeFormat)): (const_writePath.replacingOccurrences(of: "outer", with: "i"))]
        //: var message: AlongCrucial?
        var message: AlongCrucial?
        //: if needInsertLocal == true {
        if needInsertLocal == true { // 需要插入本地
            //: } else {
        } else {
            //: message = TUISocialChatManager.handleTXMessageSend(withExtral: msgInfo, isSender: true, isTip: true)
            message = IndependentObserve.on(withExtral: msgInfo, isSender: true, isTip: true)
        }

        //: guard let message = message else { return }
        guard let message = message else { return }
        //: self.msgView.uiMsgs.add(message)
        self.msgView.uiMsgs.add(message)
        //: self.msgView.tableView.reloadData()
        self.msgView.tableView.reloadData()
        //: self.msgView.paintAcross(true)
        self.msgView.paintAcross(true)
    }

    /// 公共聊天室插入系统提示消息
    //: func func__chatRoom_insertTipMsg() {
    func evaluate() {
        // 延时，内存插入
        //: tipsTimer = Timer.scheduledTimer(withTimeInterval: 5, repeats: false, block: {[weak self] time in
        tipsTimer = Timer.scheduledTimer(withTimeInterval: 5, repeats: false, block: { [weak self] time in
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: guard MarginExamineer.share.appUserConfigMode.rTips.count > 0 else { return }
            guard MarginExamineer.share.appUserConfigMode.rTips.count > 0 else { return }
            //: self.insertTipMsg_Nobalance(tipStr: MarginExamineer.share.appUserConfigMode.rTips, needInsertLocal: false)
            self.face(tipStr: MarginExamineer.share.appUserConfigMode.rTips, needInsertLocal: false)
            //: })
        })
    }
}

// MARK: - 动效

//: extension TalkingGroupChatController {
extension PanDismissViewController {
    /// 插入礼物消息，播放动效
    /// - Parameters:
    ///   - extral: 送礼接口返回结果
    ///   - isFromInputView: 是否来自输入框送礼入口
    //: func func__insertGiftMessageWithExtral(extral: Dictionary<String, Any?>, model: TalkingChatRoomMemberModel, isFromInputView: Bool) {
    func server(extral: [String: Any?], model: ReduceModel, isFromInputView: Bool) {
        //: var extralInfo = extral
        var extralInfo = extral
        //: var msgInfoDic: Dictionary<String, Any> = extralInfo["msgInfo"] as! Dictionary<String, Any>
        var msgInfoDic: [String: Any] = extralInfo[(String(dataAspectCollectionPath))] as! [String: Any]
        /// 用于本地判断celldata类型
        //: msgInfoDic["isGroup"] = "1"
        msgInfoDic[(String(showCanBusinessMessage) + String(data_packageMessage))] = "1"
        //: msgInfoDic["msgType"] = "gift"
        msgInfoDic[(appFollowId.replacingOccurrences(of: "alongside", with: "s") + String(app_placeFormat))] = (String(main_eagerRevenueMessage))
        //: extralInfo["msgInfo"] = msgInfoDic
        extralInfo[(String(dataAspectCollectionPath))] = msgInfoDic

        //: var toUser = ["uid": model.uid,
        var toUser = [String(bytes: appLackProductionMessage.map{borderLive(gift: $0)}, encoding: .utf8)!: model.uid,
                      //: "nickname": model.nickname,
                      (String(main_disappearName)): model.nickname,
                      //: "headPic": model.headPic]
                      String(bytes: show_styleStr.map{priorityLogic(appropriate: $0)}, encoding: .utf8)!: model.headPic]

        //: if model.nickname == "All Numbers".localized {
        if model.nickname == (String(main_mageAccountStr) + userAttentionGivingString.replacingOccurrences(of: "po", with: "s")).localized {
            //: if extralInfo.keys.contains("gift") != false {
            if extralInfo.keys.contains((String(main_eagerRevenueMessage))) != false {
                //: let giftdic = extralInfo["gift"] as! Dictionary<String, Any>
                let giftdic = extralInfo[(String(main_eagerRevenueMessage))] as! [String: Any]
                //: toUser["nickname"] = "\(giftdic["sendNum"] ?? "1")"
                toUser[(String(main_disappearName))] = "\(giftdic[(String(noti_mountMessage.prefix(7)))] ?? "1")"
            }
        }
        //: extralInfo["toUser"] = toUser
        extralInfo[(String(main_sincePath.suffix(5)) + dataCreateerMessage.replacingOccurrences(of: "point", with: "r"))] = toUser

        //: let cellData = TUISocialChatManager.handleTXMessageSend(withExtral: extralInfo as [String: Any], isSender: true, isTip: false)
        let cellData = IndependentObserve.on(withExtral: extralInfo as [String: Any], isSender: true, isTip: false)
        //: self.msgView.sendCustomMessage(cellData)
        self.msgView.normSave(cellData)
        //: var msgIncome: NSNumber = NSNumber.init(floatLiteral: 0)
        var msgIncome = NSNumber(floatLiteral: 0)
        //: if msgInfoDic.keys.contains("msgIncome") {
        if msgInfoDic.keys.contains((String(user_leaderFindId) + String(userNoteMsg))) {
            //: msgIncome = msgInfoDic["msgIncome"] as! NSNumber
            msgIncome = msgInfoDic[(String(user_leaderFindId) + String(userNoteMsg))] as! NSNumber
        }
        //: self.msgView.changeCustomMsg(cellData, msgId: msgInfoDic["msgId"] as? String, msgIncome: msgIncome, status: .Msg_Status_Succ)
        self.msgView.functional(cellData, tillEffectDensity: msgInfoDic[(String(main_lessRemoteId))] as? String, flushBorder: msgIncome, overShadow: .Msg_Status_Succ)
        
        //: sendGiftByMeToEfffecct(giftDic: extral as [String: Any], model: model)
        serviceDown(giftDic: extral as [String: Any], model: model)
        //: if extralInfo.keys.contains("totalMfCoin") {
        if extralInfo.keys.contains((notiFieldFormat.replacingOccurrences(of: "mix", with: "l") + String(k_roundingString.suffix(6)))) {
            //: let totalMfCoin: NSNumber = extralInfo["totalMfCoin"] as! NSNumber
            let totalMfCoin: NSNumber = extralInfo[(notiFieldFormat.replacingOccurrences(of: "mix", with: "l") + String(k_roundingString.suffix(6)))] as! NSNumber
            //: MarginExamineer.share.loginUserMode.mf_coin = totalMfCoin.stringValue
            MarginExamineer.share.loginUserMode.mf_coin = totalMfCoin.stringValue
        }
        //: if isFromInputView {
        if isFromInputView {
            //: msgInputView.giftView.updateGiftInfo(needReload: false, mf_coin: MarginExamineer.share.loginUserMode.mf_coin)
            msgInputView.giftView.color(needReload: false, mf_coin: MarginExamineer.share.loginUserMode.mf_coin)
        }
        //: msgInputView.giftView.saveRecentMembers()
        msgInputView.giftView.mid()
    }

    //: func initCachEffectData() {
    func secure() {
        //: giftEffectView.initCachAnimatData(tagetID: infoModel.roomId)
        giftEffectView.titleure(tagetID: infoModel.roomId)
    }

    //: func sendGiftByMeToEfffecct(giftDic: Dictionary<String, Any>, model: TalkingChatRoomMemberModel) {
    func serviceDown(giftDic: [String: Any], model: ReduceModel) {
        //: guard let giftModel = giftEffectView.chatRoomDisposeReceiveGiftMsg(giftMessageDic: (giftDic as NSDictionary) as! Dictionary<String, Any>, model: model) else { return }
        guard let giftModel = giftEffectView.brownFramework(giftMessageDic: (giftDic as NSDictionary) as! [String: Any], model: model) else { return }
        //: if giftTrackView.giftSendItem.count == 0 {
        if giftTrackView.giftSendItem.count == 0 {
            //: let arrM = NSMutableArray.init()
            let arrM = NSMutableArray()
            //: for model in TalkingChatGiftManager.share.getSendingItems() {
            for model in CommonLog.share.light() {
                //: arrM.add(model)
                arrM.add(model)
            }
            //: giftTrackView.giftSendItem = arrM as! [TalkingGiftNumArrModel]
            giftTrackView.giftSendItem = arrM as! [SClip]
        }
        //: giftTrackView.func__didReceiveGiftMsgModel(model: giftModel)
        giftTrackView.appearanceFill(model: giftModel)

        //: giftEffectView.dealGiftAnimatData(dict: giftDic)
        giftEffectView.onIndex(dict: giftDic)
    }

    //: @objc func addGiftEffectModelArr(giftArr: Array<Any>) {
    @objc func impactSame(giftArr: [Any]) {
        //: if giftTrackView.giftSendItem.count == 0 {
        if giftTrackView.giftSendItem.count == 0 {
            //: let arrM = NSMutableArray.init()
            let arrM = NSMutableArray()
            //: for model in TalkingChatGiftManager.share.getSendingItems() {
            for model in CommonLog.share.light() {
                //: arrM.add(model)
                arrM.add(model)
            }
            //: giftTrackView.giftSendItem = arrM as! [TalkingGiftNumArrModel]
            giftTrackView.giftSendItem = arrM as! [SClip]
        }
        //: for i in 0..<giftArr.count {
        for i in 0 ..< giftArr.count {
            //: let giftModel = giftArr[i]
            let giftModel = giftArr[i]
            //: giftTrackView.func__didReceiveGiftMsgModel(model: giftModel as! TalkingGiftMsgAnimatModel)
            giftTrackView.appearanceFill(model: giftModel as! FutureMendSumerval)
        }

        //: giftEffectView.addGiftAnimatModelArr(modelArr: giftArr)
        giftEffectView.who(modelArr: giftArr)
    }
}

// MARK: - Layout

//: extension TalkingGroupChatController {
extension PanDismissViewController {
    /// 创建UI
    //: private func createUI() {
    private func offer() {
        //: self.addChild(msgView)
        self.addChild(msgView)
        //: view.addSubview(msgView.view)
        view.addSubview(msgView.view)
        //: view.addSubview(someoneView)
        view.addSubview(someoneView)
        //: view.addSubview(giftEffectView)
        view.addSubview(giftEffectView)
        //: view.insertSubview(giftTrackView, belowSubview: giftEffectView)
        view.insertSubview(giftTrackView, belowSubview: giftEffectView)
        //: view.addSubview(msgInputView)
        view.addSubview(msgInputView)
        //: view.addSubview(navView)
        view.addSubview(navView)
        //: msgView.targetID = self.conversationData.groupID
        msgView.targetID = self.conversationData.groupID
    }

    /// 约束
    //: private func createUIConstraints() {
    private func slowMention() {
        //: navView.snp.makeConstraints { make in
        navView.snp.makeConstraints { make in
            //: make.leading.trailing.top.equalTo(view)
            make.leading.trailing.top.equalTo(view)
            //: make.height.equalTo(StatusBarNavigationBarHeight)
            make.height.equalTo(k_underSubtleTitle)
        }
        //: msgInputView.snp.makeConstraints { make in
        msgInputView.snp.makeConstraints { make in
            //: make.leading.trailing.bottom.equalTo(view)
            make.leading.trailing.bottom.equalTo(view)
            //: make.height.equalTo(msgInputView.BackViewHeight)
            make.height.equalTo(msgInputView.BackViewHeight)
        }
        //: msgView.view.snp.makeConstraints { make in
        msgView.view.snp.makeConstraints { make in
            //: make.leading.trailing.equalTo(view)
            make.leading.trailing.equalTo(view)
            //: make.top.equalTo(StatusBarNavigationBarHeight)
            make.top.equalTo(k_underSubtleTitle)
            //: make.bottom.equalTo(msgInputView.snp.top)
            make.bottom.equalTo(msgInputView.snp.top)
        }
        //: someoneView.snp.makeConstraints { make in
        someoneView.snp.makeConstraints { make in
            //: make.bottom.equalTo(msgInputView.snp.top).offset(-103)
            make.bottom.equalTo(msgInputView.snp.top).offset(-103)
            //: make.trailing.equalToSuperview()
            make.trailing.equalToSuperview()
            //: make.size.equalTo(CGSize(width: 130, height: 37))
            make.size.equalTo(CGSize(width: 130, height: 37))
        }
        //: giftEffectView.snp.makeConstraints { make in
        giftEffectView.snp.makeConstraints { make in
            //: make.leading.trailing.top.bottom.equalTo(view)
            make.leading.trailing.top.bottom.equalTo(view)
        }
        //: giftTrackView.snp.makeConstraints { make in
        giftTrackView.snp.makeConstraints { make in
            //: make.leading.equalTo(view)
            make.leading.equalTo(view)
            //: make.height.equalTo(180)
            make.height.equalTo(180)
            //: make.bottom.equalTo(-actualHeight(h: 220)-kDeviceSafeBottomHeight)
            make.bottom.equalTo(-actualHeight(h: 220) - const_closedAddSegmentPath)
        }
    }

    /// 事件绑定
    //: func bindInteraction() {
    func picture() {
        // 返回
        //: navView.backButton.rx.tap.subscribe(onNext: { [weak self] in
        navView.backButton.rx.tap.subscribe(onNext: { [weak self] in
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: self.navigationController?.popViewController(animated: true)
            self.navigationController?.popViewController(animated: true)
            //: }).disposed(by: disposeBag)
        }).disposed(by: disposeBag)

        // 在线列表
        //: navView.onlineListBtn.rx.tap.subscribe(onNext: { [weak self] in
        navView.onlineListBtn.rx.tap.subscribe(onNext: { [weak self] in
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: let vc = TalkingChatRoomOnlineListController()
            let vc = PopPriorViewController()
            //: vc.roomID = self.infoModel.roomId
            vc.roomID = self.infoModel.roomId
            //: vc.titleType = .normal
            vc.titleType = .normal
            //: self.navigationController?.pushViewController(vc, animated: true)
            self.navigationController?.pushViewController(vc, animated: true)
            //: }).disposed(by: disposeBag)
        }).disposed(by: disposeBag)

        // 群聊someone@ me 点击事件
        //: someoneView.mentionBtn.rx.tap
        someoneView.mentionBtn.rx.tap
            //: .throttle(.milliseconds(1000), scheduler: MainScheduler.instance)
            .throttle(.milliseconds(1000), scheduler: MainScheduler.instance)
            //: .subscribe(onNext: { [weak self] in
            .subscribe(onNext: { [weak self] in
                //: guard let self = self else { return }
                guard let self = self else { return }
                // 找到当前消息，滚动到指定位置
                //: guard let dict = self.mentionInfoArr.first else { return }
                guard let dict = self.mentionInfoArr.first else { return }
                //: for (index, cellData) in self.msgView.uiMsgs.enumerated() {
                for (index, cellData) in self.msgView.uiMsgs.enumerated() {
                    //: if (cellData as! AlongCrucial).isKind(of: TalkingGroupChatTextMsgCellData.self) {
                    if (cellData as! AlongCrucial).isKind(of: GlobalDuring.self) {
                        //: let groupCellData = cellData as! TalkingGroupChatTextMsgCellData
                        let groupCellData = cellData as! GlobalDuring
                        //: if groupCellData.msgID == dict["msgId"] as! String {
                        if groupCellData.msgID == dict[(String(main_lessRemoteId))] as! String {
                            //: self.msgView.tableView.scroll(toRow: UInt(index), inSection: 0, at: .bottom, animated: true)
                            self.msgView.tableView.scroll(toRow: UInt(index), inSection: 0, at: .bottom, animated: true)
                            //: self.mentionInfoArr.removeFirst()
                            self.mentionInfoArr.removeFirst()
                            //: self.someoneView.isHidden = self.mentionInfoArr.count > 0 ? false:true
                            self.someoneView.isHidden = self.mentionInfoArr.count > 0 ? false : true
                        }
                    }
                }
                //: }).disposed(by: disposeBag)
            }).disposed(by: disposeBag)

        // 被挤下线时，退出群聊
        //: EditChannel.shared.rx
        EditChannel.shared.rx
            //: .observeWeakly(Bool.self, "isConnection")
            .observeWeakly(Bool.self, (String(show_wouldMessage.prefix(7)) + constIonName.replacingOccurrences(of: "challenge", with: "c")))
            //: .subscribe(onNext: { [weak self] (value) in
            .subscribe(onNext: { [weak self] value in
                //: guard let self = self else { return }
                guard let self = self else { return }
                //: if value == false {
                if value == false {
                    //: self.navigationController?.popViewController(animated: true)
                    self.navigationController?.popViewController(animated: true)
                }
                //: }).disposed(by: disposeBag)
            }).disposed(by: disposeBag)
    }
}
