
//: Declare String Begin

/*: "init(coder:) has not been implemented" :*/
fileprivate let appDensityName:[UInt8] = [0x9c,0xa1,0x9c,0xa7,0x5b,0x96,0xa2,0x97,0x98,0xa5,0x6d,0x5c,0x53,0x9b,0x94,0xa6,0x53,0xa1,0xa2,0xa7,0x53,0x95,0x98,0x98,0xa1,0x53,0x9c,0xa0,0xa3,0x9f,0x98,0xa0,0x98,0xa1,0xa7,0x98,0x97]

fileprivate func tapMarker(robot num: UInt8) -> UInt8 {
    let value = Int(num) - 51
    if value < 0 {
        return UInt8(value + 256)
    } else {
        return UInt8(value)
    }
}

/*: "There's no posts yet." :*/
fileprivate let show_somethingTitle:[Character] = ["T","h","e","r","e"]
fileprivate let showConstantEnvironmentFormat:String = "\'s n"
fileprivate let show_buttTitle:String = "farmer disabled perceptionts yet."

/*: "uid" :*/
fileprivate let showUnctionPath:[Character] = ["u","i","d"]

/*: "page" :*/
fileprivate let constEmptyAgileMessage:String = "penablege"

/*: "list" :*/
fileprivate let show_conBrushConstantKey:[Character] = ["l","i","s","t"]

/*: "nickname" :*/
fileprivate let show_evolutionErrorKey:String = "nictill"

/*: "age" :*/
fileprivate let appInjurySearchStr:String = "sortge"

/*: "sex" :*/
fileprivate let appPortraitFormat:[Character] = ["s","e","x"]

/*: "isTPAuth" :*/
fileprivate let main_fileRemainingId:[Character] = ["i"]
fileprivate let data_exitStr:String = "give exceptionsTPAuth"

/*: "headPic" :*/
fileprivate let noti_objectStr:String = "headPicloop para err"

/*: "pinCount" :*/
fileprivate let noti_reducePath:String = "pinCouidentify movement automatic"
fileprivate let k_monsterString:String = "nexplain"

/*: "model" :*/
fileprivate let main_methodIdentifyMessage:String = "mtalkdel"

/*: "Unpin from profile" :*/
fileprivate let appAlreadyNervousPossessValue:[Character] = ["U","n","p","i","n"," ","f","r","o"]
fileprivate let show_behindCoverId:[Character] = ["m"," ","p","r","o","f","i","l","e"]

/*: "Delete Post" :*/
fileprivate let user_alwaysChildCanTitle:String = "super pass provider administrative finalDel"
fileprivate let constBanMessage:String = "state givingete Post"

/*: "Pin to profile" :*/
fileprivate let show_productConversionUrl:String = "connect blue end samplePin t"
fileprivate let user_offPopularMessage:String = "ofipersone"

/*: "momentId" :*/
fileprivate let showSpellId:String = "automatic new customermomentId"

/*: "status" :*/
fileprivate let dataAddressRecoverTitle:[Character] = ["s","t","a","t","u","s"]

/*: "Your post has been pinned" :*/
fileprivate let user_plusCurrentlyKey:[Character] = ["Y","o","u","r"," ","p","o","s","t"," "]
fileprivate let k_factorName:String = "has bad wander basic bounce"
fileprivate let dataDifficultHelloStr:String = "INNED"

/*: "Your post has been Unpinned" :*/
fileprivate let appGoingVersionValue:String = "help application normally she removeYour p"
fileprivate let dataHomeHiUrl:String = "ost cloud button titi"
fileprivate let kCouldMsg:String = "illegal automatic mode called loseeen U"

/*: "Posts" :*/
fileprivate let kRatingData:String = "cap ret let reasonPosts"

//: Declare String End

// __DEBUG__
// __CLOSE_PRINT__
//
//  EstimatedFeatureDisabled.swift
//  AbroadTalking
//
//  Created by Charlotte on 2022/9/22.
//

//: import UIKit
import UIKit

// MARK: - 属性声明 & 生命周期方法

//: class TalkingUserMomentVC: TalkingBaseViewController {
class EstimatedFeatureDisabled: EasyChainSensor {
    //: var uid = ""
    var uid = ""
    //: var pageIndex = 0
    var pageIndex = 0
    //: var disposeBag = DisposeBag()
    var disposeBag = DisposeBag()
    //: var dataSourceArr: [TalkingMomentModel] = []
    var dataSourceArr: [StatusModel] = []

    //: init(uid: String) {
    init(uid: String) {
        //: super.init(nibName: nil, bundle: nil)
        super.init(nibName: nil, bundle: nil)
        //: self.uid = uid
        self.uid = uid
    }

    //: required init?(coder: NSCoder) {
    required init?(coder: NSCoder) {
        //: fatalError("init(coder:) has not been implemented")
        fatalError(String(bytes: appDensityName.map{tapMarker(robot: $0)}, encoding: .utf8)!)
    }

    //: override func viewDidLoad() {
    override func viewDidLoad() {
        //: super.viewDidLoad()
        super.viewDidLoad()
        //: setupSubviews()
        brown()
        //: setupSubViewsConstraint()
        destroy()
        //: bindInteraction()
        resignAcross()
        //: reqData()
        ignore()
    }

    //: deinit {
    deinit {
        //: NotificationCenter.default.removeObserver(self)
        NotificationCenter.default.removeObserver(self)
    }

    //: lazy var mainTableView: UITableView = {
    lazy var mainTableView: UITableView = {
        //: let table = UITableView(frame: CGRect(x: 0, y: 0, width: ScreenWidth, height: ScreenHeight), style: UITableView.Style.plain)
        let table = UITableView(frame: CGRect(x: 0, y: 0, width: show_errorChangeFormat, height: appLoadMessage), style: UITableView.Style.plain)
        //: table.backgroundColor = UIColor.clear
        table.backgroundColor = UIColor.clear
        //: if #available(iOS 11.0, *) {
        if #available(iOS 11.0, *) {
            //: table.contentInsetAdjustmentBehavior = .never
            table.contentInsetAdjustmentBehavior = .never
            //: } else {
        } else {
            //: self.automaticallyAdjustsScrollViewInsets = false
            self.automaticallyAdjustsScrollViewInsets = false
        }
        //: table.showsVerticalScrollIndicator = true
        table.showsVerticalScrollIndicator = true
        //: table.estimatedRowHeight = 0
        table.estimatedRowHeight = 0
        //: table.estimatedSectionFooterHeight = 0
        table.estimatedSectionFooterHeight = 0
        //: table.estimatedSectionHeaderHeight = 0
        table.estimatedSectionHeaderHeight = 0
        //: table.separatorStyle = .none
        table.separatorStyle = .none
        //: table.autoresizingMask = [.flexibleWidth, .flexibleHeight]
        table.autoresizingMask = [.flexibleWidth, .flexibleHeight]
        //: return table
        return table
        //: }()
    }()

    //: lazy var emptyView: EmptyView = {
    lazy var emptyView: EmptyView = {
        //: var style = EmptyStyle()
        var style = MarginSignificantHard()
        //: style.TipsTitle = "There's no posts yet."
        style.TipsTitle = (String(show_somethingTitle) + showConstantEnvironmentFormat + "o pos" + String(show_buttTitle.suffix(7)))
        //: let view = EmptyView.init(frame: self.view.frame, style: style)
        let view = EmptyView(frame: self.view.frame, style: style)
        //: return view
        return view
        //: }()
    }()
}

// MARK: - Request & 数据处理

//: extension TalkingUserMomentVC {
extension EstimatedFeatureDisabled {
    //: func reqData() {
    func ignore() {
        //: var dict = Dictionary<String, Any>()
        var dict = [String: Any]()
        //: dict["uid"] = uid
        dict[(String(showUnctionPath))] = uid
        //: dict["page"] = pageIndex
        dict[(constEmptyAgileMessage.replacingOccurrences(of: "enable", with: "a"))] = pageIndex
        //: TalkingMomentRequestTool.req_UserMomentList(params: dict) { succeed, result, errorModel in
        WhenCluster.pastFire(params: dict) { succeed, result, errorModel in

            //: self.mainTableView.endRefresh()
            self.mainTableView.bringParent()
            //: if succeed {
            if succeed {
                //: guard let dict = result as? Dictionary<String, Any> else {
                guard let dict = result as? [String: Any] else {
                    //: if self.pageIndex > 0 {
                    if self.pageIndex > 0 {
                        //: self.mainTableView.mj_footer?.endRefreshingWithNoMoreData()
                        self.mainTableView.mj_footer?.endRefreshingWithNoMoreData()
                    }
                    //: return
                    return
                }

                //: guard let array = dict["list"] as? Array<Any> else {
                guard let array = dict[(String(show_conBrushConstantKey))] as? [Any] else {
                    //: self.mainTableView.mj_footer?.endRefreshingWithNoMoreData()
                    self.mainTableView.mj_footer?.endRefreshingWithNoMoreData()
                    //: return
                    return
                }
                //: var dataArr: [TalkingMomentModel] = []
                var dataArr: [StatusModel] = []
                //: for i in 0..<array.count {
                for i in 0 ..< array.count {
                    //: if var model = JSONDeserializer<TalkingMomentModel>.deserializeFrom(dict: array[i] as? Dictionary<String, Any>, designatedPath: nil) {
                    if var model = JSONDeserializer<StatusModel>.deserializeFrom(dict: array[i] as? [String: Any], designatedPath: nil) {
                        //: model.nickname = dict["nickname"] as? String
                        model.nickname = dict[(show_evolutionErrorKey.replacingOccurrences(of: "till", with: "k") + "name")] as? String
                        //: model.age = dict["age"] as? Int
                        model.age = dict[(appInjurySearchStr.replacingOccurrences(of: "sort", with: "a"))] as? Int
                        //: model.sex = dict["sex"] as? String
                        model.sex = dict[(String(appPortraitFormat))] as? String
                        //: model.uid = dict["uid"] as? String
                        model.uid = dict[(String(showUnctionPath))] as? String
                        //: model.isTPAuth = dict["isTPAuth"] as? Bool ?? false
                        model.isTPAuth = dict[(String(main_fileRemainingId) + String(data_exitStr.suffix(7)))] as? Bool ?? false
                        //: model.headPic = dict["headPic"] as? String
                        model.headPic = dict[(String(noti_objectStr.prefix(7)))] as? String
                        //: model.pinCount = dict["pinCount"] as! Int
                        model.pinCount = dict[(String(noti_reducePath.prefix(6)) + k_monsterString.replacingOccurrences(of: "explain", with: "t"))] as! Int
                        //: model.caculateItemHeight()
                        model.cornerDelicate()
                        //: if model.uid == MarginExamineer.share.loginUserMode.userID {
                        if model.uid == MarginExamineer.share.loginUserMode.userID {
                            //: model.isUserDetail = true
                            model.isUserDetail = true
                        }
                        //: dataArr.append(model)
                        dataArr.append(model)
                    }
                }

                //: if dataArr.count < 20 {
                if dataArr.count < 20 { // 不足一页，隐藏更多
                    //: self.mainTableView.mj_footer?.endRefreshingWithNoMoreData()
                    self.mainTableView.mj_footer?.endRefreshingWithNoMoreData()
                }

                //: if self.pageIndex == 0 {
                if self.pageIndex == 0 {
                    //: self.dataSourceArr = dataArr
                    self.dataSourceArr = dataArr
                    //: if dataArr.count>0 {
                    if dataArr.count > 0 {
                        //: self.emptyView.removeFromSuperview()
                        self.emptyView.removeFromSuperview()
                    }
                    //: } else {
                } else {
                    //: self.dataSourceArr.append(contentsOf: dataArr)
                    self.dataSourceArr.append(contentsOf: dataArr)
                }

                //: self.mainTableView.reloadData()
                self.mainTableView.reloadData()
                //: } else {
            } else {
                //: self.func__showStatusBarErrorMsg(showMsg: errorModel!.errorMsg )
                self.exitMarker(showMsg: errorModel!.errorMsg)
            }
        }
    }
}

// MARK: - 事件方法

//: extension TalkingUserMomentVC {
extension EstimatedFeatureDisabled {
    //: func headerRefresh() {
    func observe() {
        //: pageIndex = 0
        pageIndex = 0
        //: mainTableView.mj_header?.beginRefreshing()
        mainTableView.mj_header?.beginRefreshing()
        //: reqData()
        ignore()
    }

    //: func footerRefresh() {
    func schedule() {
        //: pageIndex += 1
        pageIndex += 1
        //: reqData()
        ignore()
    }

    //: @objc func updateLikeNumber(notification: NSNotification) -> Void {
    @objc func temp(notification: NSNotification) {
        //: let userinfo = notification.userInfo as![String: AnyObject]
        let userinfo = notification.userInfo as! [String: AnyObject]
        //: var model: TalkingMomentModel = userinfo["model"] as! TalkingMomentModel
        var model: StatusModel = userinfo[(main_methodIdentifyMessage.replacingOccurrences(of: "talk", with: "o"))] as! StatusModel
        //: if model.uid == MarginExamineer.share.loginUserMode.userID {
        if model.uid == MarginExamineer.share.loginUserMode.userID {
            //: model.isUserDetail = true
            model.isUserDetail = true
        }
        //: for i in 0..<self.dataSourceArr.count {
        for i in 0 ..< self.dataSourceArr.count {
            //: let tmodel = self.dataSourceArr[i]
            let tmodel = self.dataSourceArr[i]
            //: if model.mid == tmodel.mid {
            if model.mid == tmodel.mid {
                //: self.dataSourceArr[i] = model
                self.dataSourceArr[i] = model
                //: self.mainTableView.reloadRows(at: [IndexPath.init(row: i, section: 0)], with: .none)
                self.mainTableView.reloadRows(at: [IndexPath(row: i, section: 0)], with: .none)
                //: return
                return
            }
        }
    }

    //: func deleteCell(model: TalkingMomentModel, index: Int) {
    func display(model: StatusModel, index: Int) {
        //: let vc = TalkingMunuPopView.init(frame: self.view.frame)
        let vc = PinSecond(frame: self.view.frame)

        //: if model.pinCount<3 {
        if model.pinCount < 3 {
            //: if model.pinStatus {
            if model.pinStatus {
                //: vc.initwithList(cellTitleList: ["Unpin from profile".localized, "Delete Post".localized])
                vc.techniqueCapacity(cellTitleList: [(String(appAlreadyNervousPossessValue) + String(show_behindCoverId)).localized, (String(user_alwaysChildCanTitle.suffix(3)) + String(constBanMessage.suffix(8))).localized])
                //: } else {
            } else {
                //: vc.initwithList(cellTitleList: ["Pin to profile".localized, "Delete Post".localized])
                vc.techniqueCapacity(cellTitleList: [(String(show_productConversionUrl.suffix(5)) + "o pr" + user_offPopularMessage.replacingOccurrences(of: "person", with: "l")).localized, (String(user_alwaysChildCanTitle.suffix(3)) + String(constBanMessage.suffix(8))).localized])
            }
            //: } else {
        } else {
            //: if model.pinStatus {
            if model.pinStatus {
                //: vc.initwithList(cellTitleList: ["Unpin from profile".localized, "Delete Post".localized])
                vc.techniqueCapacity(cellTitleList: [(String(appAlreadyNervousPossessValue) + String(show_behindCoverId)).localized, (String(user_alwaysChildCanTitle.suffix(3)) + String(constBanMessage.suffix(8))).localized])
                //: } else {
            } else {
                //: vc.initwithList(cellTitleList: ["Delete Post".localized])
                vc.techniqueCapacity(cellTitleList: [(String(user_alwaysChildCanTitle.suffix(3)) + String(constBanMessage.suffix(8))).localized])
            }
        }

        //: vc.munuBlock = { [weak self] index, str in
        vc.munuBlock = { [weak self] index, str in
            //: guard let self = self else {return}
            guard let self = self else { return }

            //: if str == "Unpin from profile".localized {
            if str == (String(appAlreadyNervousPossessValue) + String(show_behindCoverId)).localized {
                //: self.topMoment(isTop: 0, model: model)
                self.format(isTop: 0, model: model)
                //: } else if str == "Pin to profile".localized {
            } else if str == (String(show_productConversionUrl.suffix(5)) + "o pr" + user_offPopularMessage.replacingOccurrences(of: "person", with: "l")).localized {
                //: self.topMoment(isTop: 1, model: model)
                self.format(isTop: 1, model: model)
                //: } else if str == "Delete Post".localized {
            } else if str == (String(user_alwaysChildCanTitle.suffix(3)) + String(constBanMessage.suffix(8))).localized {
                //: ProgressHUD.show()
                BeforeImagePhase.theGemRoll()
                //: var dict = Dictionary<String, Any>()
                var dict = [String: Any]()
                //: dict["id"] = model.mid
                dict["id"] = model.mid
                //: TalkingMomentRequestTool.req_DeleteMoment(params: dict) { succeed, result, errorModel in
                WhenCluster.flex(params: dict) { succeed, result, errorModel in
                    //: ProgressHUD.dismiss()
                    BeforeImagePhase.totalGap()
                    //: if succeed {
                    if succeed {
                        //: if index<self.dataSourceArr.count {
                        if index < self.dataSourceArr.count {
                            //: self.disposeBag = DisposeBag()
                            self.disposeBag = DisposeBag()
                            //: self.mainTableView.mj_header?.beginRefreshing()
                            self.mainTableView.mj_header?.beginRefreshing()
                            //: } else if self.dataSourceArr.count == 1 {
                        } else if self.dataSourceArr.count == 1 {
                            //: self.dataSourceArr.removeAll()
                            self.dataSourceArr.removeAll()
                            //: self.disposeBag = DisposeBag()
                            self.disposeBag = DisposeBag()
                            //: self.view.addSubview(self.emptyView)
                            self.view.addSubview(self.emptyView)
                        }
                        //: } else {
                    } else {
                        //: self.func__showStatusBarErrorMsg(showMsg: errorModel!.errorMsg)
                        self.exitMarker(showMsg: errorModel!.errorMsg)
                    }
                }
            }
        }
    }

    //: func topMoment(isTop: Int, model: TalkingMomentModel) {
    func format(isTop: Int, model: StatusModel) {
        //: ProgressHUD.show()
        BeforeImagePhase.theGemRoll()
        //: var dict = Dictionary<String, Any>()
        var dict = [String: Any]()
        //: dict["momentId"] = model.mid
        dict[(String(showSpellId.suffix(8)))] = model.mid
        //: dict["status"] = isTop
        dict[(String(dataAddressRecoverTitle))] = isTop
        //: TalkingMomentRequestTool.req_TopMoment(params: dict) { succeed, result, errorModel in
        WhenCluster.receive(params: dict) { succeed, result, errorModel in
            //: ProgressHUD.dismiss()
            BeforeImagePhase.totalGap()
            //: if succeed {
            if succeed {
                //: let str = isTop==1 ? "Your post has been pinned".localized : "Your post has been Unpinned".localized
                let str = self.getString(isTop)
                //: self.func__showStatusBarSuccessMsg(showMsg: str)
                self.nearEasy(showMsg: str)
                //: self.disposeBag = DisposeBag()
                self.disposeBag = DisposeBag()
                //: self.mainTableView.mj_header?.beginRefreshing()
                self.mainTableView.mj_header?.beginRefreshing()
                //: } else {
            } else {
                //: self.func__showStatusBarErrorMsg(showMsg: errorModel!.errorMsg)
                self.exitMarker(showMsg: errorModel!.errorMsg)
            }
        }
    }
    
    private func getString(_ isTop: Int) -> String {
        let str = isTop == 1 ? (String(user_plusCurrentlyKey) + String(k_factorName.prefix(4)) + "been p" + dataDifficultHelloStr.lowercased()).localized : (String(appGoingVersionValue.suffix(6)) + String(dataHomeHiUrl.prefix(4)) + "has b" + String(kCouldMsg.suffix(5)) + "npinned").localized
        return str
    }
}

// MARK: - UITableViewDelegate,UITableViewdataSourceArr

//: extension TalkingUserMomentVC: UITableViewDelegate, UITableViewDataSource {
extension EstimatedFeatureDisabled: UITableViewDelegate, UITableViewDataSource {
    //: func numberOfSections(in tableView: UITableView) -> Int {
    func numberOfSections(in tableView: UITableView) -> Int {
        //: return 1
        return 1
    }

    //: func tableView(_ tableView: UITableView, numberOfRowsInSection section: Int) -> Int {
    func tableView(_ tableView: UITableView, numberOfRowsInSection section: Int) -> Int {
        //: return self.dataSourceArr.count
        return self.dataSourceArr.count
    }

    //: func tableView(_ tableView: UITableView, heightForRowAt indexPath: IndexPath) -> CGFloat {
    func tableView(_ tableView: UITableView, heightForRowAt indexPath: IndexPath) -> CGFloat {
        //: let model = self.dataSourceArr[indexPath.row]
        let model = self.dataSourceArr[indexPath.row]
        //: return model.itemHeight ?? 56
        return model.itemHeight ?? 56
    }

    //: func tableView(_ tableView: UITableView, cellForRowAt indexPath: IndexPath) -> UITableViewCell {
    func tableView(_ tableView: UITableView, cellForRowAt indexPath: IndexPath) -> UITableViewCell {
        //: let cell = TalkingMomentItemCell(style: .default, reuseIdentifier: TalkingMomentItemCell.className(), isMyHost: true)
        let cell = BuildView(style: .default, reuseIdentifier: BuildView.className(), isMyHost: true)
        //: let model: TalkingMomentModel = self.dataSourceArr[indexPath.row]
        let model: StatusModel = self.dataSourceArr[indexPath.row]
        //: cell.configCell(model: model)
        cell.design(model: model)
        //: cell.userInfoView.deleteBtn.rx.tap.subscribe(onNext: { [weak self] in
        cell.userInfoView.deleteBtn.rx.tap.subscribe(onNext: { [weak self] in
            //: guard let self = self else {return}
            guard let self = self else { return }
            //: self.deleteCell(model: model, index: indexPath.row)
            self.display(model: model, index: indexPath.row)
            //: }).disposed(by: cell.cellDisposeBag )
        }).disposed(by: cell.cellDisposeBag)
        //: return cell
        return cell
    }

    //: func tableView(_ tableView: UITableView, willDisplay cell: UITableViewCell, forRowAt indexPath: IndexPath) {
    func tableView(_ tableView: UITableView, willDisplay cell: UITableViewCell, forRowAt indexPath: IndexPath) {}

    //: func tableView(_ tableView: UITableView, didSelectRowAt indexPath: IndexPath) {
    func tableView(_ tableView: UITableView, didSelectRowAt indexPath: IndexPath) {
        //: let model = self.dataSourceArr[indexPath.row]
        let model = self.dataSourceArr[indexPath.row]
        //: let vc =  TalkingMomentDetailVC.init(model: model)
        let vc = OriginViewController(model: model)
        //: vc.delegete = { [weak self] in
        vc.delegete = { [weak self] in
            //: guard let self = self else {return}
            guard let self = self else { return }
            //: if indexPath.row<self.dataSourceArr.count {
            if indexPath.row < self.dataSourceArr.count {
                //: self.dataSourceArr.remove(at: indexPath.row)
                self.dataSourceArr.remove(at: indexPath.row)
                //: self.mainTableView.reloadData()
                self.mainTableView.reloadData()
                //: self.disposeBag = DisposeBag()
                self.disposeBag = DisposeBag()
            }
        }
        //: self.navigationController?.pushViewController(vc, animated: true)
        self.navigationController?.pushViewController(vc, animated: true)
    }
}

// MARK: - Layout

//: extension TalkingUserMomentVC {
extension EstimatedFeatureDisabled {
    // 添加视图
    //: private func setupSubviews() {
    private func brown() {
        //: self.view.backgroundColor = UIColor.appBgColor()
        self.view.backgroundColor = UIColor.alongSupposed()
        //: self.title = "Posts".localized
        self.title = (String(kRatingData.suffix(5))).localized
        //: self.view.addSubview(emptyView)
        self.view.addSubview(emptyView)

        //: self.view.addSubview(mainTableView)
        self.view.addSubview(mainTableView)
        //: mainTableView.register(TalkingMomentItemCell.self, forCellReuseIdentifier: TalkingMomentItemCell.className())
        mainTableView.register(BuildView.self, forCellReuseIdentifier: BuildView.className())
        //: mainTableView.dataSource = self
        mainTableView.dataSource = self
        //: mainTableView.delegate = self
        mainTableView.delegate = self
        //: self.view.bringSubviewToFront(emptyView)
        self.view.bringSubviewToFront(emptyView)
    }

    // 添加约束
    //: private func setupSubViewsConstraint() {
    private func destroy() {
        //: mainTableView.snp.makeConstraints { make in
        mainTableView.snp.makeConstraints { make in
            //: make.top.equalTo(self.view).offset(12)
            make.top.equalTo(self.view).offset(12)
            //: make.bottom.equalTo(self.view.snp.bottom).offset(-12)
            make.bottom.equalTo(self.view.snp.bottom).offset(-12)
            //: make.leading.trailing.equalTo(self.view)
            make.leading.trailing.equalTo(self.view)
        }
    }

    // 添加事件
    //: private func bindInteraction() {
    private func resignAcross() {
        //: mainTableView.addHeaderRefresh { [weak self] in
        mainTableView.factory { [weak self] in
            //: guard let self = self else {return}
            guard let self = self else { return }
            //: self.headerRefresh()
            self.observe()
        }
        //: mainTableView.addFooterRefresh { [weak self] in
        mainTableView.unction { [weak self] in
            //: guard let self = self else {return}
            guard let self = self else { return }
            //: self.footerRefresh()
            self.schedule()
        }

        //: emptyView.emptyBlock = { [weak self] in
        emptyView.emptyBlock = { [weak self] in
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: self.mainTableView.mj_header?.beginRefreshing()
            self.mainTableView.mj_header?.beginRefreshing()
        }
        //: NotificationCenter.default.addObserver(self, selector: #selector(updateLikeNumber(notification:)), name: UPDATE_LIKE_NUMBER_NOTIFICATION, object: nil)
        NotificationCenter.default.addObserver(self, selector: #selector(temp(notification:)), name: userLikeTitle, object: nil)
    }
}
