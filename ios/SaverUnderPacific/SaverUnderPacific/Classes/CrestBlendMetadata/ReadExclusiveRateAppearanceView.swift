
//: Declare String Begin

/*: "init(coder:) has not been implemented" :*/
fileprivate let constLaterMessage:[UInt8] = [0x64,0x65,0x74,0x6e,0x65,0x6d,0x65,0x6c,0x70,0x6d,0x69,0x20,0x6e,0x65,0x65,0x62,0x20,0x74,0x6f,0x6e,0x20,0x73,0x61,0x68,0x20,0x29,0x3a,0x72,0x65,0x64,0x6f,0x63,0x28,0x74,0x69,0x6e,0x69]

/*: "Cancel" :*/
fileprivate let app_scientificMessage:String = "Cancelrobot depth today precede quit"

/*: "Submit" :*/
fileprivate let mainWelcomePopularFormat:String = "domain large complySubmi"
fileprivate let constAmTitle:[Character] = ["t"]

/*: "Please tell us your problems                   " :*/
fileprivate let const_weeklyTitle:[UInt8] = [0x20,0x20,0x20,0x20,0x20,0x20,0x20,0x20,0x20,0x20,0x20,0x20,0x20,0x20,0x20,0x20,0x20,0x20,0x20,0x73,0x6d,0x65,0x6c,0x62,0x6f,0x72,0x70,0x20,0x72,0x75,0x6f,0x79,0x20,0x73,0x75,0x20,0x6c,0x6c,0x65,0x74,0x20,0x65,0x73,0x61,0x65,0x6c,0x50]

/*: "You need to choose at least one." :*/
fileprivate let const_placePath:[UInt8] = [0x46,0x70,0x6a,0x3f,0x71,0x7a,0x7a,0x7b,0x3f,0x6b,0x70,0x3f,0x7c,0x77,0x70,0x70,0x6c,0x7a,0x3f,0x7e,0x6b,0x3f,0x73,0x7a,0x7e,0x6c,0x6b,0x3f,0x70,0x71,0x7a,0x31]

private func scheduleMic(lost num: UInt8) -> UInt8 {
    return num ^ 31
}

/*: "Please tell us your problems." :*/
fileprivate let constBoyfriendStr:String = "Pleaself increase"
fileprivate let userReduceValue:String = "ll us merge owner everything"
fileprivate let app_curStr:[Character] = ["y","o","u","r"," ","p","r","o","b","l","e","m","s","."]

/*: "app/userReport" :*/
fileprivate let show_putStr:[Character] = ["a","p","p","/","u","s","e","r","R","e","p","o"]
fileprivate let k_brokenDiskFormat:String = "rtunnel"

/*: "reason" :*/
fileprivate let show_operateTitle:String = "logicason"

/*: "desc" :*/
fileprivate let appDrawingScreenMessage:[UInt8] = [0xb2,0xb3,0xa5,0xb5]

private func statusDeemDoinge(broken num: UInt8) -> UInt8 {
    return num ^ 214
}

/*: "mf/moment/report" :*/
fileprivate let k_scanFormat:String = "presentation himmf/momen"
fileprivate let dataAmBurnData:String = "t/rmargin ago even"
fileprivate let mainProjectionPath:[Character] = ["e","p","o","r","t"]

/*: "momentId" :*/
fileprivate let data_galleryName:[UInt8] = [0x2f,0x2d,0x2f,0x27,0x2c,0x36,0xb,0x26]

private func literalYes(related num: UInt8) -> UInt8 {
    return num ^ 66
}

/*: "mf/momentReply/report" :*/
fileprivate let constDeployStr:[Character] = ["m","f"]
fileprivate let dataPartyStr:[Character] = ["/","m","o","m","e","n","t","R","e","p","l","y","/","r","e","p","o","r","t"]

/*: "app/videoReport" :*/
fileprivate let showVolumeTitle:[Character] = ["a","p","p","/","v"]
fileprivate let user_mediumOriginalKey:String = "ideoRperiod riot route carrier"

/*: "logId" :*/
fileprivate let noti_modelEstimatedPath:[UInt8] = [0x64,0x49,0x67,0x6f,0x6c]

/*: "Submitted successfully" :*/
fileprivate let appVersusData:String = "Submivoice better more sort portrait"
fileprivate let dataProvisionFormat:[Character] = ["u","c","c","e","s","s","f","u","l","l","y"]

/*: "Nudity" :*/
fileprivate let data_agoValue:[Character] = ["N","u","d","i","t","y"]

/*: "Violence" :*/
fileprivate let kItemMsg:String = "Violenskirt our"
fileprivate let noti_bassData:String = "equipment"

/*: "Harassment" :*/
fileprivate let appShrimpString:[Character] = ["H","a","r","a","s","s","m","e","n","t"]

/*: "Suicide or Self-injury" :*/
fileprivate let main_powderId:[Character] = ["S","u","i","c","i","d","e"," ","o","r"," ","S","e","l","f","-","i","n","j","u","r","y"]

/*: "False Information" :*/
fileprivate let showSubstantialStr:String = "Falseanother if with select situation"
fileprivate let show_angleKey:String = "iosupposed"

/*: "Spam" :*/
fileprivate let app_clickTitle:String = "Spamprice bind"

/*: "Hate Speech" :*/
fileprivate let show_conditionMsg:String = "Hate us eager injury within"
fileprivate let const_ignoreDropArkId:String = "maxi"

/*: "Terrorism" :*/
fileprivate let app_logString:String = "Terrobackground distinction composition"
fileprivate let user_popMessage:[Character] = ["r","i","s","m"]

/*: "Sale of Illegal or Regulated Goods" :*/
fileprivate let notiQualityPiValue:[UInt8] = [0x56,0x64,0x6f,0x68,0x23,0x72,0x69,0x23,0x4c,0x6f,0x6f,0x68,0x6a,0x64,0x6f,0x23,0x72,0x75,0x23,0x55,0x68,0x6a,0x78,0x6f,0x64,0x77,0x68,0x67,0x23,0x4a,0x72,0x72,0x67,0x76]

fileprivate func sessionCover(us num: UInt8) -> UInt8 {
    let value = Int(num) + 253
    if value > 255 {
        return UInt8(value - 256)
    } else {
        return UInt8(value)
    }
}

/*: "Sending disgusting messages" :*/
fileprivate let main_ownerTitle:[Character] = ["S","e","n","d","i","n","g"," ","d","i"]
fileprivate let user_sensorName:String = "sgmake"
fileprivate let userEagerBubbleString:String = "farmer editor question age facesting "

/*: "Using photos of nudity or drugs" :*/
fileprivate let noti_limitedValue:[UInt8] = [0x74,0x52,0x48,0x4f,0x46,0x1,0x51,0x49,0x4e,0x55,0x4e,0x52,0x1,0x4e,0x47,0x1,0x4f,0x54,0x45,0x48,0x55,0x58,0x1,0x4e,0x53,0x1,0x45,0x53,0x54,0x46,0x52]

private func shapeCapture(slow num: UInt8) -> UInt8 {
    return num ^ 33
}

/*: "Harassing or sending ads" :*/
fileprivate let user_infoStr:String = "security be exitHara"
fileprivate let main_drownRashStr:String = "g stack remark priority theme create"
fileprivate let mainRefDenseMessage:String = "or senspell pop"

/*: "Defraud or using fake info" :*/
fileprivate let const_alongFormat:[Character] = ["D","e","f","r","a","u","d"]
fileprivate let k_crucialMessage:[Character] = [" ","o","r"," ","u","s","i","n","g"," ","f","a","k","e"," ","i","n","f","o"]

/*: "Others" :*/
fileprivate let user_attentionValue:[Character] = ["O","t","h","e","r"]
fileprivate let userObtainFormat:String = "own"

//: Declare String End

// __DEBUG__
// __CLOSE_PRINT__
//
//  ReadExclusiveRateAppearanceView.swift
//  AbroadTalking
//
//  Created by Bolo on 2022/9/14.
//

//: import Then
import Then
//: import UIKit
import UIKit

//: enum TalkingReportAlertType: CaseIterable {
enum NameSimilar: CaseIterable {
    //: case reportCommentType
    case reportCommentType // 举报动态
    //: case reportDynamicType
    case reportDynamicType
    //: case reportUserType
    case reportUserType // 举报用户
    //: case reportVideo
    case reportVideo // 举报视频通话
}

// 是否举报成功block
//: typealias ReportViewHideBlock = (_ isReportSucc: Bool) -> Void
typealias ReportViewHideBlock = (_ isReportSucc: Bool) -> Void

//: class TalkingReportAlertView: UIView {
class ReadExclusiveRateAppearanceView: UIView {
    //: var reportType: TalkingReportAlertType?
    var reportType: NameSimilar?
    //: var dataArray: Array<TalkingReportModel> = []
    var dataArray: [ChapterMonster] = []
    //: var selectedModel = TalkingReportModel()
    var selectedModel = ChapterMonster()
    //: var tempId: String = ""
    var tempId: String = ""
    //: var reportHideBlock: ReportViewHideBlock?
    var reportHideBlock: ReportViewHideBlock?
    //: let disposeBag = DisposeBag()
    let disposeBag = DisposeBag()

    //: public init(frame: CGRect, type: TalkingReportAlertType?, rId: String) {
    public init(frame: CGRect, type: NameSimilar?, rId: String) {
        //: tempId = rId
        tempId = rId
        //: reportType = type
        reportType = type
        //: super.init(frame: frame)
        super.init(frame: frame)
        //: setupSubviews()
        marker()
        //: setupSubViewsConstraint()
        alongside()
        //: setupReasons()
        towardSustainable()
        //: bindInteraction()
        constraint()
    }

    //: required init?(coder: NSCoder) {
    required init?(coder: NSCoder) {
        //: fatalError("init(coder:) has not been implemented")
        fatalError(String(bytes: constLaterMessage.reversed(), encoding: .utf8)!)
    }

    // MARK: - Lazy load

    //: lazy var cancelButton = UIButton().then {
    lazy var cancelButton = UIButton().then {
        //: $0.setTitle("Cancel".localized, for: .normal)
        $0.setTitle((String(app_scientificMessage.prefix(6))).localized, for: .normal)
        //: $0.setTitleColor(UIColor.appThemeColor(), for: .normal)
        $0.setTitleColor(UIColor.systemRequest(), for: .normal)
        //: $0.titleLabel?.font = UIFont.pingfangFont(type: .Medium, fontSize: 17)
        $0.titleLabel?.font = UIFont.yearNearby(type: .Medium, fontSize: 17)
        //: $0.backgroundColor = UIColor.white
        $0.backgroundColor = UIColor.white
        //: $0.layer.borderWidth = 1
        $0.layer.borderWidth = 1
        //: $0.layer.borderColor = UIColor.appThemeColor().cgColor
        $0.layer.borderColor = UIColor.systemRequest().cgColor
        //: $0.layer.cornerRadius = 25
        $0.layer.cornerRadius = 25
        //: $0.clipsToBounds = true
        $0.clipsToBounds = true
        //: $0.addTarget(self, action: #selector(registerCancelAction), for: .touchUpInside)
        $0.addTarget(self, action: #selector(errRemind), for: .touchUpInside)
    }

    //: lazy var submitButton = UIButton().then {
    lazy var submitButton = UIButton().then {
        //: $0.setTitle("Submit".localized, for: .normal)
        $0.setTitle((String(mainWelcomePopularFormat.suffix(5)) + String(constAmTitle)).localized, for: .normal)
        //: $0.setTitleColor(UIColor.white, for: .normal)
        $0.setTitleColor(UIColor.white, for: .normal)
        //: $0.titleLabel?.font = UIFont.pingfangFont(type: .Medium, fontSize: 17)
        $0.titleLabel?.font = UIFont.yearNearby(type: .Medium, fontSize: 17)
        //: $0.setBackgroundImage(UIImage.imageGradientColor(colors: UIColor.appGradientColor(), size: CGSize(width: 126, height: 50)), for: .normal)
        $0.setBackgroundImage(UIImage.domainCover(colors: UIColor.running(), size: CGSize(width: 126, height: 50)), for: .normal)
        //: $0.layer.cornerRadius = 25
        $0.layer.cornerRadius = 25
        //: $0.clipsToBounds = true
        $0.clipsToBounds = true
        //: $0.addTarget(self, action: #selector(registerSubmitAction), for: .touchUpInside)
        $0.addTarget(self, action: #selector(connection), for: .touchUpInside)
    }

    //: lazy var tableFooterView = UIView().then {
    lazy var tableFooterView = UIView().then {
        //: $0.frame = CGRect(x: 0, y: 0, width: ScreenWidth-actualWidth(w: 108), height: actualHeight(h: 74))
        $0.frame = CGRect(x: 0, y: 0, width: show_errorChangeFormat - actualWidth(w: 108), height: actualHeight(h: 74))
        //: $0.backgroundColor = UIColor.RGBA(245, 245, 248, 1)
        $0.backgroundColor = UIColor.our(245, 245, 248, 1)
        //: $0.layer.masksToBounds = true
        $0.layer.masksToBounds = true
        //: $0.layer.cornerRadius = 5
        $0.layer.cornerRadius = 5
    }

    //: lazy var otherTextView = PlaceholderTextView(placeholder: "Please tell us your problems                   ".localized, placeholderColor: UIColor.appValueDetailColor(), frame: CGRect(x: 10, y: 10, width: ScreenWidth-actualWidth(w: 128), height: actualHeight(h: 54))).then {
    lazy var otherTextView = KindView(placeholder: String(bytes: const_weeklyTitle.reversed(), encoding: .utf8)!.localized, placeholderColor: UIColor.theProvision(), frame: CGRect(x: 10, y: 10, width: show_errorChangeFormat - actualWidth(w: 128), height: actualHeight(h: 54))).then {
        //: $0.backgroundColor = .clear
        $0.backgroundColor = .clear
        //: $0.placeholdertextView.backgroundColor = .clear
        $0.placeholdertextView.backgroundColor = .clear
        //: $0.limitWords = 200
        $0.limitWords = 200
        //: $0.isShowCountLabel = false
        $0.isShowCountLabel = false
        //: $0.isReturnHidden = true
        $0.isReturnHidden = true
        //: $0.plaleLabel.font = .pingfangFont(type: .Regular, fontSize: 16)
        $0.plaleLabel.font = .yearNearby(type: .Regular, fontSize: 16)
        //: $0.placeholdertextView.textColor = UIColor.appTitleColor()
        $0.placeholdertextView.textColor = UIColor.untilExpected()
        //: $0.placeholdertextView.font = .pingfangFont(type: .Regular, fontSize: 16)
        $0.placeholdertextView.font = .yearNearby(type: .Regular, fontSize: 16)
    }

    //: lazy var bgView = UIView().then {
    lazy var bgView = UIView().then {
        //: $0.backgroundColor = UIColor.RGBA(0, 0, 0, 0.6)
        $0.backgroundColor = UIColor.our(0, 0, 0, 0.6)
    }

    //: lazy var containerView = UIView().then {
    lazy var containerView = UIView().then {
        //: $0.backgroundColor = .white
        $0.backgroundColor = .white
        //: $0.layer.masksToBounds = true
        $0.layer.masksToBounds = true
        //: $0.layer.cornerRadius = 15
        $0.layer.cornerRadius = 15
    }

    //: lazy var tableView = UITableView.init(frame: .zero, style: .plain).then {
    lazy var tableView = UITableView(frame: .zero, style: .plain).then {
        //: $0.backgroundColor = .white
        $0.backgroundColor = .white
        //: $0.separatorStyle = .none
        $0.separatorStyle = .none
        //: $0.showsVerticalScrollIndicator = false
        $0.showsVerticalScrollIndicator = false
        //: $0.delegate = self
        $0.delegate = self
        //: $0.dataSource = self
        $0.dataSource = self
        //: if reportType != .reportCommentType, reportType != .reportVideo {
        if reportType != .reportCommentType, reportType != .reportVideo {
            //: $0.tableFooterView = self.tableFooterView
            $0.tableFooterView = self.tableFooterView
        }
        //: $0.register(TalkingReportViewCell.self, forCellReuseIdentifier: TalkingReportViewCell.className())
        $0.register(BorderView.self, forCellReuseIdentifier: BorderView.className())
    }
}

// MARK: - Bind && Event

//: extension TalkingReportAlertView {
extension ReadExclusiveRateAppearanceView {
    //: @objc func func__TapGestureRecognizer() {
    @objc func factor() {
        //: self.endEditing(true)
        self.endEditing(true)
    }

    //: public func showReportViewIn(view: UIView?) {
    public func medal(view: UIView?) {
        //: if view == nil {
        if view == nil {
            //: let window = MovementStructure.getWindow()
            let window = MovementStructure.levelScientific()
            //: window.addSubview(self)
            window.addSubview(self)
            //: return
            return
        }
        //: view?.addSubview(self)
        view?.addSubview(self)
    }

    /// 移除视图
    /// - Parameter succeed: 是否举报成功
    //: private func dismissReportView(succeed: Bool) {
    private func emptyRemark(succeed: Bool) {
        //: reportHideBlock?(succeed)
        reportHideBlock?(succeed)
        //: self.removeFromSuperview()
        self.removeFromSuperview()
    }

    /// 取消
    //: @objc func registerCancelAction() {
    @objc func errRemind() {
        //: self.dismissReportView(succeed: false)
        self.emptyRemark(succeed: false)
    }

    /// 提交举报内容
    //: @objc func registerSubmitAction() {
    @objc func connection() {
        //: if selectedModel.selected == false {
        if selectedModel.selected == false {
            //: self.func__showStatusBarErrorMsg(showMsg: "You need to choose at least one.".localized)
            self.exitMarker(showMsg: String(bytes: const_placePath.map{scheduleMic(lost: $0)}, encoding: .utf8)!.localized)
            //: return
            return
        }
        //: if reportType == .reportUserType, otherTextView.placeholdertextView.text.isEmptyString {
        if reportType == .reportUserType, otherTextView.placeholdertextView.text.isEmptyString {
            //: self.func__showStatusBarErrorMsg(showMsg: "Please tell us your problems.".localized)
            self.exitMarker(showMsg: (String(constBoyfriendStr.prefix(4)) + "se te" + String(userReduceValue.prefix(6)) + String(app_curStr)).localized)
            //: return
            return
        }

        //: var param: [String: String] = [:]
        var param: [String: String] = [:]
        //: var path: String = ""
        var path = ""

        //: switch self.reportType {
        switch self.reportType {
        //: case .reportUserType:
        case .reportUserType:
            //: path = "app/userReport"
            path = (String(show_putStr) + k_brokenDiskFormat.replacingOccurrences(of: "tunnel", with: "t"))
            //: param = ["id": tempId, "reason": selectedModel.type, "desc": otherTextView.placeholdertextView.text]
            param = ["id": tempId, (show_operateTitle.replacingOccurrences(of: "logic", with: "re")): selectedModel.type, String(bytes: appDrawingScreenMessage.map{statusDeemDoinge(broken: $0)}, encoding: .utf8)!: otherTextView.placeholdertextView.text]

        //: case .reportDynamicType:
        case .reportDynamicType:
            //: path = "mf/moment/report"
            path = (String(k_scanFormat.suffix(8)) + String(dataAmBurnData.prefix(3)) + String(mainProjectionPath))
            //: param = ["momentId": tempId, "reason": selectedModel.type]
            param = [String(bytes: data_galleryName.map{literalYes(related: $0)}, encoding: .utf8)!: tempId, (show_operateTitle.replacingOccurrences(of: "logic", with: "re")): selectedModel.type]

        //: case .reportCommentType:
        case .reportCommentType:
            //: path = "mf/momentReply/report"
            path = (String(constDeployStr) + String(dataPartyStr))
            //: param = ["id": tempId, "reason": selectedModel.type]
            param = ["id": tempId, (show_operateTitle.replacingOccurrences(of: "logic", with: "re")): selectedModel.type]

        //: case .reportVideo:
        case .reportVideo:
            //: path = "app/videoReport"
            path = (String(showVolumeTitle) + String(user_mediumOriginalKey.prefix(5)) + "eport")
            //: param = ["logId": tempId, "reason": selectedModel.type]
            param = [String(bytes: noti_modelEstimatedPath.reversed(), encoding: .utf8)!: tempId, (show_operateTitle.replacingOccurrences(of: "logic", with: "re")): selectedModel.type]

        //: default:
        default:
            //: break
            break
        }

        //: self.submitButton.isEnabled = false
        self.submitButton.isEnabled = false
        //: TalkingUserRequestManager.func__reportRequest(reqPath: path, reqParams: param) { succeed, result, errorModel in
        EnvironmentSource.nameure(reqPath: path, reqParams: param) { succeed, result, errorModel in
            //: self.submitButton.isEnabled = true
            self.submitButton.isEnabled = true
            //: if succeed == true {
            if succeed == true {
                //: self.func__showStatusBarSuccessMsg(showMsg: "Submitted successfully".localized)
                self.nearEasy(showMsg: (String(appVersusData.prefix(5)) + "tted s" + String(dataProvisionFormat)).localized)
                //: self.dismissReportView(succeed: true)
                self.emptyRemark(succeed: true)
            }
        }
    }
}

// MARK: - Delegate

//: extension TalkingReportAlertView: UITableViewDelegate, UITableViewDataSource {
extension ReadExclusiveRateAppearanceView: UITableViewDelegate, UITableViewDataSource {
    //: func numberOfSections(in tableView: UITableView) -> Int {
    func numberOfSections(in tableView: UITableView) -> Int {
        //: return 1
        return 1
    }

    //: func tableView(_ tableView: UITableView, numberOfRowsInSection section: Int) -> Int {
    func tableView(_ tableView: UITableView, numberOfRowsInSection section: Int) -> Int {
        //: return dataArray.count
        return dataArray.count
    }

    //: func tableView(_ tableView: UITableView, heightForRowAt indexPath: IndexPath) -> CGFloat {
    func tableView(_ tableView: UITableView, heightForRowAt indexPath: IndexPath) -> CGFloat {
        //: return actualHeight(h: 40)
        return actualHeight(h: 40)
    }

    //: func tableView(_ tableView: UITableView, cellForRowAt indexPath: IndexPath) -> UITableViewCell {
    func tableView(_ tableView: UITableView, cellForRowAt indexPath: IndexPath) -> UITableViewCell {
        //: let cell = tableView.dequeueReusableCell(withIdentifier: TalkingReportViewCell.className()) as! TalkingReportViewCell
        let cell = tableView.dequeueReusableCell(withIdentifier: BorderView.className()) as! BorderView
        //: let model = dataArray[indexPath.item]
        let model = dataArray[indexPath.item]
        //: cell.updateReportCell(model: model)
        cell.reduce(model: model)
        //: return cell
        return cell
    }

    //: func tableView(_ tableView: UITableView, didSelectRowAt indexPath: IndexPath) {
    func tableView(_ tableView: UITableView, didSelectRowAt indexPath: IndexPath) {
        //: for model in dataArray {
        for model in dataArray {
            //: model.selected = false
            model.selected = false
        }

        //: let itemModel = dataArray[indexPath.item]
        let itemModel = dataArray[indexPath.item]
        //: itemModel.selected = true
        itemModel.selected = true
        //: self.selectedModel = itemModel
        self.selectedModel = itemModel

        //: tableView.reloadData()
        tableView.reloadData()
    }
}

// MARK: - Layout

//: extension TalkingReportAlertView {
extension ReadExclusiveRateAppearanceView {
    /// 添加视图
    //: private func setupSubviews() {
    private func marker() {
        //: addSubview(bgView)
        addSubview(bgView)
        //: addSubview(containerView)
        addSubview(containerView)
        //: containerView.addSubview(tableView)
        containerView.addSubview(tableView)
        //: containerView.addSubview(cancelButton)
        containerView.addSubview(cancelButton)
        //: containerView.addSubview(submitButton)
        containerView.addSubview(submitButton)
        //: tableFooterView.addSubview(otherTextView)
        tableFooterView.addSubview(otherTextView)
    }

    /// 添加约束
    //: private func setupSubViewsConstraint() {
    private func alongside() {
        //: var height = actualHeight(h: 325)
        var height = actualHeight(h: 325)
        //: if reportType == .reportUserType {
        if reportType == .reportUserType {
            //: height = actualHeight(h: 375)
            height = actualHeight(h: 375)
            //: } else if reportType == .reportVideo {
        } else if reportType == .reportVideo {
            //: height = actualHeight(h: 338)
            height = actualHeight(h: 338)
        }
        //: let topY = (self.frame.height - height) / 2.0
        let topY = (self.frame.height - height) / 2.0

        //: bgView.snp.makeConstraints { make in
        bgView.snp.makeConstraints { make in
            //: make.leading.top.trailing.bottom.equalTo(0)
            make.leading.top.trailing.bottom.equalTo(0)
        }
        //: containerView.snp.makeConstraints { make in
        containerView.snp.makeConstraints { make in
            //: make.top.equalTo(topY)
            make.top.equalTo(topY)
            //: make.leading.equalTo(actualWidth(w: 40))
            make.leading.equalTo(actualWidth(w: 40))
            //: make.trailing.equalTo(actualWidth(w: -40))
            make.trailing.equalTo(actualWidth(w: -40))
            //: make.height.equalTo(height)
            make.height.equalTo(height)
        }
        //: tableView.snp.makeConstraints { make in
        tableView.snp.makeConstraints { make in
            //: make.leading.equalTo(actualWidth(w: 14))
            make.leading.equalTo(actualWidth(w: 14))
            //: make.trailing.equalTo(actualWidth(w: -14))
            make.trailing.equalTo(actualWidth(w: -14))
            //: make.top.equalTo(actualHeight(h: 36))
            make.top.equalTo(actualHeight(h: 36))
            //: if reportType == .reportVideo {
            if reportType == .reportVideo {
                //: make.bottom.equalTo(actualHeight(h: -62))
                make.bottom.equalTo(actualHeight(h: -62))
                //: } else {
            } else {
                //: make.bottom.equalTo(actualHeight(h: -82))
                make.bottom.equalTo(actualHeight(h: -82))
            }
        }
        //: cancelButton.snp.makeConstraints { make in
        cancelButton.snp.makeConstraints { make in
            //: make.leading.equalTo(actualWidth(w: 14))
            make.leading.equalTo(actualWidth(w: 14))
            //: make.bottom.equalTo(actualHeight(h: -16))
            make.bottom.equalTo(actualHeight(h: -16))
            //: make.size.equalTo(CGSize(width: 126, height: 50))
            make.size.equalTo(CGSize(width: 126, height: 50))
        }
        //: submitButton.snp.makeConstraints { make in
        submitButton.snp.makeConstraints { make in
            //: make.trailing.equalTo(actualWidth(w: -14))
            make.trailing.equalTo(actualWidth(w: -14))
            //: make.bottom.equalTo(actualHeight(h: -16))
            make.bottom.equalTo(actualHeight(h: -16))
            //: make.size.equalTo(CGSize(width: 126, height: 50))
            make.size.equalTo(CGSize(width: 126, height: 50))
        }
    }

    /// 添加数据
    //: private func setupReasons() {
    private func towardSustainable() {
        //: var reasonArray: [String] = []
        var reasonArray: [String] = []
        //: var type = 0
        var type = 0

        //: switch self.reportType {
        switch self.reportType {
        //: case .reportDynamicType, .reportCommentType:
        case .reportDynamicType, .reportCommentType:
            //: type = 6
            type = 6
            //: reasonArray = ["Nudity".localized,
            reasonArray = [(String(data_agoValue)).localized,
                           //: "Violence".localized,
                           (String(kItemMsg.prefix(6)) + noti_bassData.replacingOccurrences(of: "equipment", with: "ce")).localized,
                           //: "Harassment".localized,
                           (String(appShrimpString)).localized,
                           //: "Suicide or Self-injury".localized,
                           (String(main_powderId)).localized,
                           //: "False Information".localized,
                           (String(showSubstantialStr.prefix(5)) + " Informat" + show_angleKey.replacingOccurrences(of: "supposed", with: "n")).localized,
                           //: "Spam".localized,
                           (String(app_clickTitle.prefix(4))).localized,
                           //: "Hate Speech".localized,
                           (String(show_conditionMsg.prefix(5)) + "Speec" + const_ignoreDropArkId.replacingOccurrences(of: "maxi", with: "h")).localized,
                           //: "Terrorism".localized,
                           (String(app_logString.prefix(5)) + String(user_popMessage)).localized,
                           //: "Sale of Illegal or Regulated Goods".localized]
                           String(bytes: notiQualityPiValue.map{sessionCover(us: $0)}, encoding: .utf8)!.localized]

        //: case .reportUserType:
        case .reportUserType:
            //: type = 1
            type = 1
            //: reasonArray = ["Sending disgusting messages".localized,
            reasonArray = [(String(main_ownerTitle) + user_sensorName.replacingOccurrences(of: "make", with: "u") + String(userEagerBubbleString.suffix(6)) + "messages").localized,
                           //: "Using photos of nudity or drugs".localized,
                           String(bytes: noti_limitedValue.map{shapeCapture(slow: $0)}, encoding: .utf8)!.localized,
                           //: "Harassing or sending ads".localized,
                           (String(user_infoStr.suffix(4)) + "ssin" + String(main_drownRashStr.prefix(2)) + String(mainRefDenseMessage.prefix(6)) + "ding ads").localized,
                           //: "Defraud or using fake info".localized,
                           (String(const_alongFormat) + String(k_crucialMessage)).localized,
                           //: "Others".localized]
                           (String(user_attentionValue) + userObtainFormat.replacingOccurrences(of: "own", with: "s")).localized]

        //: case .reportVideo:
        case .reportVideo:
            //: if let reason = MarginExamineer.share.appConfigMode.videoReport["reason"] {
            if let reason = MarginExamineer.share.appConfigMode.videoReport[(show_operateTitle.replacingOccurrences(of: "logic", with: "re"))] {
                //: reasonArray = reason as! [String]
                reasonArray = reason as! [String]
            }

        //: default:
        default:
            //: type = 0
            type = 0
            //: reasonArray = []
            reasonArray = []
        }

        //: for i in 0..<reasonArray.count {
        for i in 0 ..< reasonArray.count {
            //: let model = TalkingReportModel()
            let model = ChapterMonster()
            //: model.reason = reasonArray[i]
            model.reason = reasonArray[i]
            //: if self.reportType == .reportVideo {
            if self.reportType == .reportVideo {
                //: model.type = "\(i)"
                model.type = "\(i)"
                //: } else {
            } else {
                //: model.type = "\(i+type)"
                model.type = "\(i + type)"
            }
            //: dataArray.append(model)
            dataArray.append(model)
        }

        //: tableView.reloadData()
        tableView.reloadData()
    }

    /// 添加事件
    //: private func bindInteraction() {
    private func constraint() {
        //: let tapGesture = UITapGestureRecognizer()
        let tapGesture = UITapGestureRecognizer()
        //: bgView.addGestureRecognizer(tapGesture)
        bgView.addGestureRecognizer(tapGesture)
        //: tapGesture.rx.event.bind { [weak self] recognizer in
        tapGesture.rx.event.bind { [weak self] recognizer in
            //: guard let self = self else {return}
            guard let self = self else { return }
            //: self.endEditing(true)
            self.endEditing(true)
        }
        //: .disposed(by: disposeBag)
        .disposed(by: disposeBag)
    }
}
