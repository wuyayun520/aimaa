
//: Declare String Begin

/*: ; build: :*/
fileprivate let app_includeTunnelString:String = "; buildbloc slide very cancel rover"
fileprivate let user_itString:[Character] = [":"]

/*: ; iOS  :*/
fileprivate let const_existPersonalStr:String = "press mask; iOS "

/*: "audio" :*/
fileprivate let noti_earlyYourselfString:String = "disclosedio"

/*: .wav" :*/
fileprivate let appVeryStr:[Character] = [".","w","a","v"]

/*: "audio/wav" :*/
fileprivate let show_reduceEntryUrl:String = "feedback impressionaudio/wav"

/*: .jpg" :*/
fileprivate let kSpecMessage:String = ".jpghung scene"

/*: "image/jpeg" :*/
fileprivate let noti_extraTailKey:[Character] = ["i"]
fileprivate let main_herePressValue:[Character] = ["m","a","g","e","/","j","p","e","g"]

/*: "://app." :*/
fileprivate let app_joinFormat:String = ":/"
fileprivate let app_peerPath:String = "/app.color presentation progress purple"

/*: "data" :*/
fileprivate let noti_tunPath:String = "dloga"

/*: ":null" :*/
fileprivate let data_materialPath:[Character] = [":","n","u","l","l"]

/*: "Request failed, Try again later" :*/
fileprivate let dataIndependentString:[UInt8] = [0x27,0x10,0x4,0x0,0x10,0x6,0x1,0x55,0x13,0x14,0x1c,0x19,0x10,0x11,0x59,0x55,0x21,0x7,0xc,0x55,0x14,0x12,0x14,0x1c,0x1b,0x55,0x19,0x14,0x1,0x10,0x7]

private func queryerFast(editor num: UInt8) -> UInt8 {
    return num ^ 117
}

/*: "json error" :*/
fileprivate let k_mValue:String = "jpackon"

/*: "platform=iphone&version= :*/
fileprivate let noti_clusterData:String = "PLATFO"
fileprivate let data_sparkTitle:String = "once log capturephone&ve"
fileprivate let user_regionDrawerFormat:String = "rsion=capable continuing statement generate"

/*: &packageId= :*/
fileprivate let show_coffeeStr:String = "indicator context agent&pac"
fileprivate let notiSpecifyId:[Character] = ["k","a","g","e","I","d","="]

/*: &bundleId= :*/
fileprivate let userProblemTransactionString:String = "instance confirm session&bund"

/*: &lang= :*/
fileprivate let k_stretchMessage:String = "&lang=trig against merge"

/*: "/route/slowindex" :*/
fileprivate let constSessionKey:[Character] = ["/"]
fileprivate let dataHiddenMsg:[Character] = ["r","o","u","t","e","/","s","l","o","w","i","n","d","e","x"]

/*: "/route/index" :*/
fileprivate let notiPoolStr:String = "identity distinction/rou"
fileprivate let constDraftExploreKey:String = "ndmemberx"

/*: "?path= :*/
fileprivate let kLocateTitleName:String = "painter"
fileprivate let app_completeId:String = "document movement which meetingpath="

/*: "&GJNonceStr= :*/
fileprivate let const_sineTitle:String = "remark down all spell bad&GJN"
fileprivate let appContinuingFormat:String = "="

/*: "&params= :*/
fileprivate let show_aSharedBridgeKey:String = "&pmirror vid"
fileprivate let show_selectionTitle:[Character] = ["a","r","a","m","s","="]

/*: "token" :*/
fileprivate let data_capableData:String = "stripken"

/*: "请求成功 :*/
fileprivate let main_crushFormat:[Character] = ["请","求","成","功"]

/*: "AAAAAAAA" :*/
fileprivate let k_trustHangValue:String = "aaaaaaaa"

/*: "下载完成 -  :*/
fileprivate let data_typicalPath:String = "下载"
fileprivate let noti_hickMessage:String = "完drag - "

/*: "/Documents/" :*/
fileprivate let k_thenTitle:String = "/Docutransition transition"
fileprivate let user_arableTitle:[Character] = ["m","e","n","t","s","/"]

/*: ".mp4" :*/
fileprivate let const_phoneMergeMessage:[Character] = [".","m","p","4"]

/*: "移动地址: :*/
fileprivate let app_coffeeFormat:[Character] = ["移","动","地","址",":"]

/*: "后台任务下载回来" :*/
fileprivate let app_objectPath:[Character] = ["后","台","任","\u{52a1}","下","载","回"]
fileprivate let showHappyStr:String = "observe"

/*: "下载成功 : :*/
fileprivate let showDayMsg:String = "下载\u{6210}功 :"

/*: "Net Error" :*/
fileprivate let notiDenyName:String = "processor sufficientNet E"

/*: "下载失败 : :*/
fileprivate let dataRemotePopularId:[Character] = ["下","载","\u{5931}","败"," ",":"]

//: Declare String End

// __DEBUG__
// __CLOSE_PRINT__
//
//  MessageMedia.swift
//  AbroadTalking
//
//  Created by Hemming on 2022/1/28.
//

//: import Alamofire
import Alamofire
//: import CoreMedia
import CoreMedia
//: import UIKit
import UIKit

// 默认请求超时时间
//: let REQUEST_TIMEOUT_INTERVAL = 10.0
let noti_untilFormat = 10.0

//: let userAgent = "\(AppName)/\(AppVersion) (\(AppBundle); build:\(AppBuildNumber); iOS \(UIDevice.current.systemVersion); \(UIDevice.modelName))"
let userMaleMessage = "\(appClosetoTitle)/\(show_prepareMsg) (\(showPartyUntilId)" + (String(app_includeTunnelString.prefix(7)) + String(user_itString)) + "\(main_isolateAccuracyData)" + (String(const_existPersonalStr.suffix(6))) + "\(UIDevice.current.systemVersion); \(UIDevice.modelName))"

//: typealias FinishBlock = (_ succeed: Bool, _ result: Any?, _ errorModel: TalkingErrorResponse?) -> Void
typealias FinishBlock = (_ succeed: Bool, _ result: Any?, _ errorModel: TellEdge?) -> Void

//: let GJ = TalkingRequestTool.init()
let notiSignatureItemMessage = MessageMedia()

//: @objc class TalkingRequestTool: NSObject, URLSessionDownloadDelegate {
@objc class MessageMedia: NSObject, URLSessionDownloadDelegate {
    //: public func startRequest(model: TalkingRequestModel, completion: @escaping FinishBlock) {
    public func lightForPath(model: ObtainMomentum, completion: @escaping FinishBlock) {
        //: if model.paramsContainBinaryObject() {
        if model.operateMemory() {
            //: self.startUploadDataRequest(model: model, completion: completion)
            self.ovalEnableEducate(model: model, completion: completion)
            //: } else {
        } else {
            //: self.startNormalRequest(model: model, completion: completion)
            self.sumegrationFromRapid(model: model, completion: completion)
        }
    }

    //: public func startUploadDataRequest(model: TalkingRequestModel, completion: @escaping FinishBlock) {
    public func ovalEnableEducate(model: ObtainMomentum, completion: @escaping FinishBlock) {
        //: let serverUrl = self.buildServerUrl(model: model)
        let serverUrl = self.skirtOf(model: model)
        //: let headers = self.getRequestHeader(model: model)
        let headers = self.sharedConMeNativeAngle(model: model)

        //: if model.requestType == .GET {
        if model.requestType == .GET {
            //: AF.request(serverUrl, method: .get, parameters: model.params, headers: headers).responseData { [self] responseData in
            AF.request(serverUrl, method: .get, parameters: model.params, headers: headers).responseData { [self] responseData in
                //: switch responseData.result {
                switch responseData.result {
                //: case .success:
                case .success:
                    //: func__requestSucess(model: model, response: responseData.response!, responseData: responseData.data!, completion: completion)
                    promptWould(model: model, response: responseData.response!, responseData: responseData.data!, completion: completion)
                //: break
                //: case .failure:
                case .failure:
                    //: completion(false, nil, TalkingErrorResponse.init(errorCode: RequestResultCode.NetError.rawValue, errorMsg: kNetErrorMsg))
                    completion(false, nil, TellEdge(errorCode: ModelAttribute.NetError.rawValue, errorMsg: kRequestPath))
                    //: if model.showErrorStatusBar {
                    if model.showErrorStatusBar {
                        //: func__showStatusBarErrorMsg(showMsg: kNetErrorMsg)
                        exitMarker(showMsg: kRequestPath)
                    }

                    //: break
                }
            }
            //: } else {
        } else {
            //: AF.upload(multipartFormData: { (multipartFormData) in
            AF.upload(multipartFormData: { multipartFormData in
                //: for (key, value) in model.params {
                for (key, value) in model.params {
                    //: if value is Data {
                    if value is Data {
                        //: if key == "audio" {
                        if key == (noti_earlyYourselfString.replacingOccurrences(of: "disclose", with: "au")) {
                            //: multipartFormData.append(value as! Data, withName: key, fileName: "\(NSDate.getCurrentTimeStamp())+\(PackageID).wav", mimeType: "audio/wav")
                            multipartFormData.append(value as! Data, withName: key, fileName: "\(NSDate.groupConvert())+\(k_perBounceString)" + (String(appVeryStr)), mimeType: (String(show_reduceEntryUrl.suffix(9))))
                            //: } else {
                        } else {
                            //: multipartFormData.append(value as! Data, withName: key, fileName: "\(key).jpg", mimeType: "image/jpeg")
                            multipartFormData.append(value as! Data, withName: key, fileName: "\(key)" + (String(kSpecMessage.prefix(4))), mimeType: (String(noti_extraTailKey) + String(main_herePressValue)))
                        }
                        //: } else {
                    } else {
                        //: let data_ = "\(value)".data(using: String.Encoding.utf8)!
                        let data_ = "\(value)".data(using: String.Encoding.utf8)!
                        //: multipartFormData.append(data_, withName: key)
                        multipartFormData.append(data_, withName: key)
                    }
                }
                //: }, to: serverUrl, method: .post, headers: headers).responseData { [self] responseData in
            }, to: serverUrl, method: .post, headers: headers).responseData { [self] responseData in
                //: switch responseData.result {
                switch responseData.result {
                //: case .success:
                case .success:
                    //: func__requestSucess(model: model, response: responseData.response!, responseData: responseData.data!, completion: completion)
                    promptWould(model: model, response: responseData.response!, responseData: responseData.data!, completion: completion)
                //: break
                //: case .failure:
                case .failure:
                    //: completion(false, nil, TalkingErrorResponse.init(errorCode: RequestResultCode.NetError.rawValue, errorMsg: kNetErrorMsg))
                    completion(false, nil, TellEdge(errorCode: ModelAttribute.NetError.rawValue, errorMsg: kRequestPath))
                    //: if model.showErrorStatusBar {
                    if model.showErrorStatusBar {
                        //: func__showStatusBarErrorMsg(showMsg: kNetErrorMsg)
                        exitMarker(showMsg: kRequestPath)
                    }

                    //: break
                }
            }
        }
    }

    //: public func startNormalRequest(model: TalkingRequestModel, completion: @escaping FinishBlock) {
    public func sumegrationFromRapid(model: ObtainMomentum, completion: @escaping FinishBlock) {
        //: let serverUrl = self.buildServerUrl(model: model)
        let serverUrl = self.skirtOf(model: model)
        //: let headers = self.getRequestHeader(model: model)
        let headers = self.sharedConMeNativeAngle(model: model)

        //: if model.requestType == .GET {
        if model.requestType == .GET {
            //: AF.request(serverUrl, method: .get, parameters: (Encryption_Request ? nil : model.params), headers: headers, requestModifier: { $0.timeoutInterval = REQUEST_TIMEOUT_INTERVAL }).responseData { [self] responseData in
            AF.request(serverUrl, method: .get, parameters: k_headDrawData ? nil : model.params, headers: headers, requestModifier: { $0.timeoutInterval = noti_untilFormat }).responseData { [self] responseData in
                //: switch responseData.result {
                switch responseData.result {
                //: case .success:
                case .success:
                    //: func__requestSucess(model: model, response: responseData.response!, responseData: responseData.data!, completion: completion)
                    promptWould(model: model, response: responseData.response!, responseData: responseData.data!, completion: completion)
                //: break
                //: case .failure:
                case .failure:
                    //: completion(false, nil, TalkingErrorResponse.init(errorCode: RequestResultCode.NetError.rawValue, errorMsg: kNetErrorMsg))
                    completion(false, nil, TellEdge(errorCode: ModelAttribute.NetError.rawValue, errorMsg: kRequestPath))
                    //: if model.showErrorStatusBar {
                    if model.showErrorStatusBar {
                        //: func__showStatusBarErrorMsg(showMsg: kNetErrorMsg)
                        exitMarker(showMsg: kRequestPath)
                    }
                    //: break
                }
            }
            //: } else {
        } else {
            //: AF.request(serverUrl, method: .post, parameters: model.params, headers: headers, requestModifier: { $0.timeoutInterval = REQUEST_TIMEOUT_INTERVAL }).responseData { [self] responseData in
            AF.request(serverUrl, method: .post, parameters: model.params, headers: headers, requestModifier: { $0.timeoutInterval = noti_untilFormat }).responseData { [self] responseData in
                //: switch responseData.result {
                switch responseData.result {
                //: case .success:
                case .success:
                    //: func__requestSucess(model: model, response: responseData.response!, responseData: responseData.data!, completion: completion)
                    promptWould(model: model, response: responseData.response!, responseData: responseData.data!, completion: completion)
                //: break
                //: case .failure:
                case .failure:
                    //: completion(false, nil, TalkingErrorResponse.init(errorCode: RequestResultCode.NetError.rawValue, errorMsg: kNetErrorMsg))
                    completion(false, nil, TellEdge(errorCode: ModelAttribute.NetError.rawValue, errorMsg: kRequestPath))
                    //: if model.showErrorStatusBar {
                    if model.showErrorStatusBar {
                        //: func__showStatusBarErrorMsg(showMsg: kNetErrorMsg)
                        exitMarker(showMsg: kRequestPath)
                    }

                    //: break
                }
            }
        }
    }

    //: func func__requestSucess(model: TalkingRequestModel, response: HTTPURLResponse, responseData: Data, completion: @escaping FinishBlock) {
    func promptWould(model: ObtainMomentum, response: HTTPURLResponse, responseData: Data, completion: @escaping FinishBlock) {
        //: let httpCookies = HTTPCookie.cookies(withResponseHeaderFields: response.allHeaderFields as! [String: String], for: response.url!)
        let httpCookies = HTTPCookie.cookies(withResponseHeaderFields: response.allHeaderFields as! [String: String], for: response.url!)
        //: if httpCookies.count > 0 {
        if httpCookies.count > 0 {
            //: TalkingRequestAddrTool.share.func__updateTalkingCookies(cookies: httpCookies)
            PresentLocal.share.whipEnd(cookies: httpCookies)
        }

        //: var responseJson = String(data: responseData, encoding: .utf8)
        var responseJson = String(data: responseData, encoding: .utf8)

        //: if Encryption_Request, model.requestServer.contains("://app.") {
        if k_headDrawData, model.requestServer.contains((app_joinFormat.capitalized + String(app_peerPath.prefix(5)))) {
            //: responseJson = responseJson?.aes256Decrypt(key: TalkingRequestAddrTool.share.encryKeyStr)
            responseJson = responseJson?.notCareful(key: PresentLocal.share.encryKeyStr)
        }

        //: responseJson = responseJson?.replacingOccurrences(of: "\"data\":null", with: "\"data\":{}")
        responseJson = responseJson?.replacingOccurrences(of: "\"" + (noti_tunPath.replacingOccurrences(of: "log", with: "at")) + "\"" + (String(data_materialPath)), with: "" + "\"" + (noti_tunPath.replacingOccurrences(of: "log", with: "at")) + "\"" + ":{}")
        //: if !Encryption_Request {
        if !k_headDrawData {
            //: responseJson = responseJson?.replacingOccurrences(of: ":null", with: ":\"\"")
            responseJson = responseJson?.replacingOccurrences(of: (String(data_materialPath)), with: ":" + "\"" + "\"")
        }
        //: if let responseModel = JSONDeserializer<TalkingBaseResponse>.deserializeFrom(json: responseJson) {
        if let responseModel = JSONDeserializer<ProAll>.deserializeFrom(json: responseJson) {
            //: if responseModel.errno == RequestResultCode.Normal.rawValue {
            if responseModel.errno == ModelAttribute.Normal.rawValue {
                //: completion(true, responseModel.data, nil)
                completion(true, responseModel.data, nil)
                //: } else {
            } else {
                //: completion(false, responseModel.data, TalkingErrorResponse.init(errorCode: responseModel.errno, errorMsg: responseModel.msg ?? ""))
                completion(false, responseModel.data, TellEdge(errorCode: responseModel.errno, errorMsg: responseModel.msg ?? ""))
                //: if model.showErrorStatusBar {
                if model.showErrorStatusBar {
                    //: func__showStatusBarErrorMsg(showMsg: responseModel.msg ?? "Request failed, Try again later".localized)
                    exitMarker(showMsg: responseModel.msg ?? String(bytes: dataIndependentString.map{queryerFast(editor: $0)}, encoding: .utf8)!.localized)
                }
                //: switch responseModel.errno {
                switch responseModel.errno {
                //: case RequestResultCode.NeedReLogin.rawValue:
                case ModelAttribute.NeedReLogin.rawValue:
                    //: NotificationCenter.default.post(name: DID_LOGIN_OUT_SUCCESS_NOTIFICATION, object: nil, userInfo: nil)
                    NotificationCenter.default.post(name: mainOperationPadString, object: nil, userInfo: nil)
                //: default:
                default:
                    //: break
                    break
                }
            }
            //: } else {
        } else {
            //: completion(false, nil, TalkingErrorResponse.init(errorCode: RequestResultCode.NetError.rawValue, errorMsg: "json error".localized))
            completion(false, nil, TellEdge(errorCode: ModelAttribute.NetError.rawValue, errorMsg: (k_mValue.replacingOccurrences(of: "pack", with: "s") + " error").localized))
        }
    }

    //: func buildServerUrl(model: TalkingRequestModel) -> String {
    func skirtOf(model: ObtainMomentum) -> String {
        //: var serverUrl: String = model.requestServer
        var serverUrl: String = model.requestServer

        //: var otherParams = "platform=iphone&version=\(AppNetVersion)&packageId=\(PackageID)&bundleId=\(AppBundle)&lang=\(TalkingRequestAddrTool.share.interfaceLang)"
        var otherParams = (noti_clusterData.lowercased() + "rm=i" + String(data_sparkTitle.suffix(8)) + String(user_regionDrawerFormat.prefix(6))) + "\(notiOriginalKey)" + (String(show_coffeeStr.suffix(4)) + String(notiSpecifyId)) + "\(k_perBounceString)" + (String(userProblemTransactionString.suffix(5)) + "leId=") + "\(showPartyUntilId)" + (String(k_stretchMessage.prefix(6))) + "\(PresentLocal.share.interfaceLang)"
        //: if Encryption_Request, serverUrl.contains("://app.") {
        if k_headDrawData, serverUrl.contains((app_joinFormat.capitalized + String(app_peerPath.prefix(5)))) {
            //: let allowCharSet: CharacterSet = CharacterSet.init(charactersIn: "?!@#$^&%*+,:;='\"`<>()[]{}/\\| ").inverted
            let allowCharSet = CharacterSet(charactersIn: "?!@#$^&%*+,:;='" + "\"" + "`<>()[]{}/\\| ").inverted
            //: if TalkingRequestAddrTool.share.slowPathArr.contains(model.requestPath) {
            if PresentLocal.share.slowPathArr.contains(model.requestPath) {
                //: serverUrl.append("/route/slowindex")
                serverUrl.append((String(constSessionKey) + String(dataHiddenMsg)))
                //: } else {
            } else {
                //: serverUrl.append("/route/index") // 如果使用IP需要加上app/
                serverUrl.append((String(notiPoolStr.suffix(4)) + "te/i" + constDraftExploreKey.replacingOccurrences(of: "member", with: "e"))) // 如果使用IP需要加上app/
            }

            //: let pathStr = model.requestPath.addingPercentEncoding(withAllowedCharacters: allowCharSet)!
            let pathStr = model.requestPath.addingPercentEncoding(withAllowedCharacters: allowCharSet)!
            //: serverUrl.append("?path=\(pathStr)")
            serverUrl.append((kLocateTitleName.replacingOccurrences(of: "painter", with: "?") + String(app_completeId.suffix(5))) + "\(pathStr)")
            //: serverUrl.append("&GJNonceStr=\(TalkingRequestAddrTool.share.randomStr)")
            serverUrl.append((String(const_sineTitle.suffix(4)) + "onceStr" + appContinuingFormat.capitalized) + "\(PresentLocal.share.randomStr)")

            //: if model.requestType == .GET {
            if model.requestType == .GET {
                //: for (key, value) in model.params {
                for (key, value) in model.params {
                    //: otherParams.append("&\(key)=\(value)")
                    otherParams.append("&\(key)=\(value)")
                }
            }
            //: if let encryStr = otherParams.aes256Encrypt(key: TalkingRequestAddrTool.share.encryKeyStr) {
            if let encryStr = otherParams.direct(key: PresentLocal.share.encryKeyStr) {
                //: let paramsStr = encryStr.addingPercentEncoding(withAllowedCharacters: allowCharSet)!
                let paramsStr = encryStr.addingPercentEncoding(withAllowedCharacters: allowCharSet)!
                //: serverUrl.append("&params=\(paramsStr)")
                serverUrl.append((String(show_aSharedBridgeKey.prefix(2)) + String(show_selectionTitle)) + "\(paramsStr)")
            }
            //: printLog(message: serverUrl)
            printLog(message: serverUrl)
            //: } else {
        } else {
            //: if !model.requestPath.isEmptyString {
            if !model.requestPath.isEmptyString {
                //: serverUrl.append("/\(model.requestPath)")
                serverUrl.append("/\(model.requestPath)")
            }
            //: serverUrl.append("?\(otherParams)")
            serverUrl.append("?\(otherParams)")
        }
        //: return serverUrl
        return serverUrl
    }

    /// 获取请求头参数
    /// - Parameter model: 请求模型
    /// - Returns: 请求头参数
    //: func getRequestHeader(model: TalkingRequestModel) -> HTTPHeaders {
    func sharedConMeNativeAngle(model: ObtainMomentum) -> HTTPHeaders {
        //: var headers = [HTTPHeader.userAgent(userAgent)]
        var headers = [HTTPHeader.userAgent(userMaleMessage)]
        //: if model.addHeaderToken.isEmpty == false {
        if model.addHeaderToken.isEmpty == false {
            //: let token = HTTPHeader(name: "token", value: model.addHeaderToken)
            let token = HTTPHeader(name: (data_capableData.replacingOccurrences(of: "strip", with: "to")), value: model.addHeaderToken)
            //: headers.append(token)
            headers.append(token)
        }
        //: return HTTPHeaders(headers)
        return HTTPHeaders(headers)
    }

    // MARK: - 系统后台下载

    //: func urlSessionBackgroundDownLoad(model: TalkingRequestModel) {
    func firstWithoutLocal(model: ObtainMomentum) {
        //: let serverUrl = self.buildServerUrl(model: model)
        let serverUrl = self.skirtOf(model: model)
        //: URLSession.shared.dataTask(with: URL.init(string: serverUrl)!) {(data, response, error) in
        URLSession.shared.dataTask(with: URL(string: serverUrl)!) { data, response, error in
            //: if error == nil {
            if error == nil {
                //: printLog(message: "请求成功\(String(describing: response))" )
                printLog(message: (String(main_crushFormat)) + "\(String(describing: response))")
            }
        }

//        let configuration1 = URLSessionConfiguration.default    // default模式下系统会创建一个持久化的缓存并在用户的钥匙串中存储证书
//        let configuration2 = URLSessionConfiguration.ephemeral  // 所有内容的生命周期都与session相同，当session无效时，所有内容自动释放。

        // 后台下载
        //: let configuration3 = URLSessionConfiguration.background(withIdentifier: "AAAAAAAA")  // 后台甚至APP已经关闭的时候仍然在传输数据的会话
        let configuration3 = URLSessionConfiguration.background(withIdentifier: (k_trustHangValue.uppercased())) // 后台甚至APP已经关闭的时候仍然在传输数据的会话
        //: configuration3.httpAdditionalHeaders = ["AA": "AA", "BB": "BB"] // 与请求一起发送的附加头文件的字典
        configuration3.httpAdditionalHeaders = ["AA": "AA", "BB": "BB"] // 与请求一起发送的附加头文件的字典
        //: configuration3.networkServiceType = .default
        configuration3.networkServiceType = .default // 网络服务的类型
        //: configuration3.allowsCellularAccess = true
        configuration3.allowsCellularAccess = true // 一个布尔值，用于确定是否应通过蜂窝网络进行连接
        //: configuration3.timeoutIntervalForRequest = 15
        configuration3.timeoutIntervalForRequest = 15
        //: configuration3.timeoutIntervalForResource = 15
        configuration3.timeoutIntervalForResource = 15
        //: let session = URLSession.init(configuration: configuration3, delegate: self, delegateQueue: OperationQueue.main)
        let session = URLSession(configuration: configuration3, delegate: self, delegateQueue: OperationQueue.main)
        //: session.downloadTask(with: URL.init(string: serverUrl)!).resume()
        session.downloadTask(with: URL(string: serverUrl)!).resume()
    }

    //: func urlSession(_ session: URLSession, downloadTask: URLSessionDownloadTask, didFinishDownloadingTo location: URL) {
    func urlSession(_ session: URLSession, downloadTask: URLSessionDownloadTask, didFinishDownloadingTo location: URL) {
        //: printLog(message: "下载完成 - \(location)")
        printLog(message: (data_typicalPath.capitalized + noti_hickMessage.replacingOccurrences(of: "drag", with: "成")) + "\(location)")
        //: let locationPath = location.path
        let locationPath = location.path
        //: let documnets = NSHomeDirectory() + "/Documents/" + NSDate.getCurrentTimeStamp() + ".mp4"
        let documnets = NSHomeDirectory() + (String(k_thenTitle.prefix(5)) + String(user_arableTitle)) + NSDate.groupConvert() + (String(const_phoneMergeMessage))
        //: printLog(message: "移动地址:\(documnets)")
        printLog(message: (String(app_coffeeFormat)) + "\(documnets)")
        //: let fileManager = FileManager.default
        let fileManager = FileManager.default
        //: do {
        do {
            //: try fileManager.moveItem(atPath: locationPath, toPath: documnets)
            try fileManager.moveItem(atPath: locationPath, toPath: documnets)
            //: } catch {
        } catch {
            // catch 中默认提供error信息, 当序列化不成功是, 返回error
            //: printLog(message: error)
            printLog(message: error)
        }
    }

    // 监听下载进度
    //: func urlSession(_ session: URLSession, downloadTask: URLSessionDownloadTask, didWriteData bytesWritten: Int64, totalBytesWritten: Int64, totalBytesExpectedToWrite: Int64) {
    func urlSession(_ session: URLSession, downloadTask: URLSessionDownloadTask, didWriteData bytesWritten: Int64, totalBytesWritten: Int64, totalBytesExpectedToWrite: Int64) {}

    //: func urlSessionDidFinishEvents(forBackgroundURLSession session: URLSession) {
    func urlSessionDidFinishEvents(forBackgroundURLSession session: URLSession) {
        //: printLog(message: "后台任务下载回来")
        printLog(message: (String(app_objectPath) + showHappyStr.replacingOccurrences(of: "observe", with: "来")))
        //: DispatchQueue.main.async {
        DispatchQueue.main.async {
            //: guard let backgroundHandle = AppDelegateHelper.shared.backgroundSessionCompletionHandler else { return }
            guard let backgroundHandle = CoordinatorPlatform.shared.backgroundSessionCompletionHandler else { return }
            //: backgroundHandle()
            backgroundHandle()
        }
    }

    // MARK: - 动画下载

    //: public func startGiftZipRequest(giftUrl: String, path: String, completion: @escaping FinishBlock) {
    public func betweenVariable(giftUrl: String, path: String, completion: @escaping FinishBlock) {
        //: let destination: DownloadRequest.Destination = { _, _ in
        let destination: DownloadRequest.Destination = { _, _ in
            //: let pathUrl = URL(fileURLWithPath: path)
            let pathUrl = URL(fileURLWithPath: path)
            //: return (pathUrl, [.removePreviousFile, .createIntermediateDirectories])
            return (pathUrl, [.removePreviousFile, .createIntermediateDirectories])
        }
        //: let giftUrlStr = giftUrl.replacingOccurrences(of: UrlDomin.urlAes256Decrypt(), with: ReplaceUrlDomain)
        let giftUrlStr = giftUrl.replacingOccurrences(of: app_lastMessage.experienced(), with: app_readTitle)
        //: AF.download(giftUrlStr, to: destination).downloadProgress { speed in
        AF.download(giftUrlStr, to: destination).downloadProgress { speed in
        }
        //: .responseData { response in
        .responseData { response in
            //: switch response.result {
            switch response.result {
            //: case .success(let json):
            case let .success(json):
                //: printLog(message: "下载成功 :\(json)")
                printLog(message: (showDayMsg) + "\(json)")
                //: completion(true, nil, TalkingErrorResponse.init(errorCode: RequestResultCode.Normal.rawValue, errorMsg: "Net Error".localized))
                completion(true, nil, TellEdge(errorCode: ModelAttribute.Normal.rawValue, errorMsg: (String(notiDenyName.suffix(5)) + "rror").localized))
            //: break
            //: case .failure(let errMsg):
            case let .failure(errMsg):
                //: completion(false, nil, TalkingErrorResponse.init(errorCode: RequestResultCode.NetError.rawValue, errorMsg: "Net Error".localized))
                completion(false, nil, TellEdge(errorCode: ModelAttribute.NetError.rawValue, errorMsg: (String(notiDenyName.suffix(5)) + "rror").localized))

                //: printLog(message: "下载失败 :\(errMsg)")
                printLog(message: (String(dataRemotePopularId)) + "\(errMsg)")
            }
        }
    }

    //: public func startVoiceRequest(voiceUrl: String, path: String, completion: @escaping FinishBlock) {
    public func restrictionPermission(voiceUrl: String, path: String, completion: @escaping FinishBlock) {
        //: let destination: DownloadRequest.Destination = { _, _ in
        let destination: DownloadRequest.Destination = { _, _ in
            //: let pathUrl = URL(fileURLWithPath: path)
            let pathUrl = URL(fileURLWithPath: path)
            //: return (pathUrl, [.removePreviousFile, .createIntermediateDirectories])
            return (pathUrl, [.removePreviousFile, .createIntermediateDirectories])
        }

        //: let voiceUrlStr = voiceUrl.replacingOccurrences(of: UrlDomin.urlAes256Decrypt(), with: ReplaceUrlDomain)
        let voiceUrlStr = voiceUrl.replacingOccurrences(of: app_lastMessage.experienced(), with: app_readTitle)
        //: AF.download(voiceUrlStr, to: destination).downloadProgress { speed in
        AF.download(voiceUrlStr, to: destination).downloadProgress { speed in
        }
        //: .responseData { response in
        .responseData { response in
            //: switch response.result {
            switch response.result {
            //: case .success(let json):
            case let .success(json):
                //: printLog(message: "下载成功 :\(json)")
                printLog(message: (showDayMsg) + "\(json)")
                //: completion(true, nil, TalkingErrorResponse.init(errorCode: RequestResultCode.Normal.rawValue, errorMsg: "Net Error".localized))
                completion(true, nil, TellEdge(errorCode: ModelAttribute.Normal.rawValue, errorMsg: (String(notiDenyName.suffix(5)) + "rror").localized))
            //: break
            //: case .failure(let errMsg):
            case let .failure(errMsg):
                //: completion(false, nil, TalkingErrorResponse.init(errorCode: RequestResultCode.NetError.rawValue, errorMsg: "Net Error".localized))
                completion(false, nil, TellEdge(errorCode: ModelAttribute.NetError.rawValue, errorMsg: (String(notiDenyName.suffix(5)) + "rror").localized))

                //: printLog(message: "下载失败 :\(errMsg)")
                printLog(message: (String(dataRemotePopularId)) + "\(errMsg)")
            }
        }
    }
}
