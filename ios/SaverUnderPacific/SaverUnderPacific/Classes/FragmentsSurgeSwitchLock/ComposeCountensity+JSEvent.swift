
//: Declare String Begin

/*: "WEBVIEW_THIRD_REFRESHCOIN_NOTIFICATION" :*/
fileprivate let k_gravityUrl:[UInt8] = [0x4e,0x4f,0x49,0x54,0x41,0x43,0x49,0x46,0x49,0x54,0x4f,0x4e,0x5f,0x4e,0x49,0x4f,0x43,0x48,0x53,0x45,0x52,0x46,0x45,0x52,0x5f,0x44,0x52,0x49,0x48,0x54,0x5f,0x57,0x45,0x49,0x56,0x42,0x45,0x57]

/*: "goodsId" :*/
fileprivate let app_deployRecognizeStr:String = "goodsIdsafe range net square"

/*: "source" :*/
fileprivate let constBuildKey:[Character] = ["s","o","u","r","c","e"]

/*: "type" :*/
fileprivate let data_speedMainName:String = "tcertaine"

/*: "title" :*/
fileprivate let app_additionalPath:[Character] = ["t","i","t","l","e"]

/*: "url" :*/
fileprivate let show_refValue:[Character] = ["u","r","l"]

/*: "money" :*/
fileprivate let showCareData:String = "monedestroy"

/*: "Other" :*/
fileprivate let user_environmentTitle:String = "Otherline collection temporary"

/*: "scene" :*/
fileprivate let kPureString:[Character] = ["s","c","e","n","e"]

/*: "show" :*/
fileprivate let userReportName:[Character] = ["s","h","o","w"]

/*: "target" :*/
fileprivate let dataLimitedFormat:String = "texclusiverget"

/*: "eventName" :*/
fileprivate let main_withinArrowTitle:String = "theme stat his ad beyondeventN"
fileprivate let appImpressionFormat:String = "criticale"

/*: "jsonString" :*/
fileprivate let notiMarginString:String = "jsonScity gem arc"

/*: "coin" :*/
fileprivate let data_mineSecretTitle:String = "cosegment"

/*: "uid" :*/
fileprivate let k_sinkKey:String = "ubad"

/*: "closeWeb" :*/
fileprivate let userServerStr:[Character] = ["c","l","o","s","e"]
fileprivate let notiHelpId:String = "Webdialog sibling"

/*: "未实现的js事件： :*/
fileprivate let mainStatementId:String = "未实现的jstotal be chapter mage"
fileprivate let k_orientationUrl:String = "事\u{4ef6}："

/*: "PurchaseClick" :*/
fileprivate let mainInfrastructureLawFormat:String = "migration boardPur"
fileprivate let app_ageStr:String = "chattachse"

/*: "Retry After or Go to " :*/
fileprivate let appBlankUrl:String = "bean personal constructRetr"
fileprivate let kObtainMsg:String = "ter orre butt alongside"
fileprivate let notiFormalData:[Character] = ["o"," "]

/*: "Feedback" :*/
fileprivate let appEaseCompleteFractionUrl:String = "refugee"
fileprivate let userContactPath:String = "eedreadingack"

/*: " to contact us" :*/
fileprivate let data_hourKey:String = " to coop drop appropriate customer broadcast"
fileprivate let dataDecisionData:[Character] = ["n","t","a","c","t"," ","u","s"]

/*: "Apple" :*/
fileprivate let dataPageUndertakeName:String = "sensor success return action greatApple"

/*: " apple支付充值失败： :*/
fileprivate let show_choiceDeployTitle:[Character] = ["\u{f8ff}"," ","a","p","p","l","e","\u{652f}","付","充","值"]
fileprivate let appLogCareFormat:String = "失败\u{ff1a}"

/*: "payResultCallback();" :*/
fileprivate let constScatterBelongTitle:String = "destination temp object want threepayResu"
fileprivate let mainYesterdayKey:[Character] = ["l","t","C","a"]
fileprivate let user_sKey:[Character] = ["l","l","b","a","c","k","(",")",";"]

/*: "USD" :*/
fileprivate let data_tempId:[Character] = ["U","S","D"]

/*: "amount" :*/
fileprivate let dataLastId:[UInt8] = [0xc0,0xcc,0xce,0xd4,0xcd,0xd3]

fileprivate func offerProtection(success num: UInt8) -> UInt8 {
    let value = Int(num) + 161
    if value > 255 {
        return UInt8(value - 256)
    } else {
        return UInt8(value)
    }
}

/*: "getSystemNotificationtStatus(true)" :*/
fileprivate let mainNeedKey:[UInt8] = [0x79,0x77,0x86,0x65,0x8b,0x85,0x86,0x77,0x7f,0x60,0x81,0x86,0x7b,0x78,0x7b,0x75,0x73,0x86,0x7b,0x81,0x80,0x86,0x65,0x86,0x73,0x86,0x87,0x85,0x3a,0x86,0x84,0x87,0x77,0x3b]

fileprivate func collectStat(commit num: UInt8) -> UInt8 {
    let value = Int(num) - 18
    if value < 0 {
        return UInt8(value + 256)
    } else {
        return UInt8(value)
    }
}

/*: "getSystemNotificationtStatus(false)" :*/
fileprivate let dataTheMixTitle:[UInt8] = [0x4,0x2,0x11,0xf0,0x16,0x10,0x11,0x2,0xa,0xeb,0xc,0x11,0x6,0x3,0x6,0x0,0xfe,0x11,0x6,0xc,0xb,0x11,0xf0,0x11,0xfe,0x11,0x12,0x10,0xc5,0x3,0xfe,0x9,0x10,0x2,0xc6]

fileprivate func subtleRecentInjury(replacement num: UInt8) -> UInt8 {
    let value = Int(num) + 99
    if value > 255 {
        return UInt8(value - 256)
    } else {
        return UInt8(value)
    }
}

//: Declare String End

// __DEBUG__
// __CLOSE_PRINT__
//
//  ComposeCountensity+JSEvent.swift
//  AbroadTalking
//
//  Created by young on 2022/9/6.
//

//: import UIKit
import UIKit
//: import WebKit
import WebKit

// 通知三方H5刷新金币
//: public let WEBVIEW_THIRD_REFRESHCOIN_NOTIFICATION = NSNotification.Name(rawValue: "WEBVIEW_THIRD_REFRESHCOIN_NOTIFICATION")
public let dataEquipmentFormat = NSNotification.Name(rawValue: String(bytes: k_gravityUrl.reversed(), encoding: .utf8)!)

//: extension TalkingWebViewController {
extension ComposeCountensity {
    /// 处理JS事件
    /// - Parameters:
    ///   - message: 消息
    //: func HandleScriptMessage(_ message: WKScriptMessage) {
    func practicalMaterial(_ message: WKScriptMessage) {
        //: let type = WebMethodType(rawValue: message.name)
        let type = HideInstead(rawValue: message.name)
        //: let json = JSON(message.body)
        let json = JSON(message.body)
        //: switch type {
        switch type {
        //: case .MethodType_Exit:
        case .MethodType_Exit:
            //: self.func__closeAction(sender: nil)
            self.bindOf(sender: nil)

        //: case .MethodType_needLogin:
        case .MethodType_needLogin:
            //: needLogin()
            second()

        //: case .MethodType_apPay:
        case .MethodType_apPay:
            //: applePay(productId: json["goodsId"].stringValue, source: json["source"].intValue, payType: .Pay)
            wen(productId: json[(String(app_deployRecognizeStr.prefix(7)))].stringValue, source: json[(String(constBuildKey))].intValue, payType: .Pay)

        //: case .MethodType_apPay_subscribe:
        case .MethodType_apPay_subscribe:
            //: if json["type"].stringValue == "6" {
            if json[(data_speedMainName.replacingOccurrences(of: "certain", with: "yp"))].stringValue == "6" {
                //: isVideoCallRechargeHalfPage = true
                isVideoCallRechargeHalfPage = true
                //: RECHARGE_CLICK_EVENT = subscribtionRecordButton
                noti_significantTitle = user_firstSingleFlexPath
                //: } else if json["type"].stringValue == "7" { // 点击订阅弹窗的订阅按钮
            } else if json[(data_speedMainName.replacingOccurrences(of: "certain", with: "yp"))].stringValue == "7" { // 点击订阅弹窗的订阅按钮
                //: RECHARGE_CLICK_EVENT = subscribtionRecordButton
                noti_significantTitle = user_firstSingleFlexPath
            }
            //: applePay_subscribe(type: json["type"].stringValue, productId: json["goodsId"].stringValue, payType: .Subscribe)
            acceptDecide(type: json[(data_speedMainName.replacingOccurrences(of: "certain", with: "yp"))].stringValue, productId: json[(String(app_deployRecognizeStr.prefix(7)))].stringValue, payType: .Subscribe)

        //: case .MethodType_changeMenuButton:
        case .MethodType_changeMenuButton:
            //: changeRightMenuButton(title: json["title"].stringValue, url: json["url"].stringValue)
            followEquipment(title: json[(String(app_additionalPath))].stringValue, url: json[(String(show_refValue))].stringValue)

        //: case .MethodType_refreshCoin:
        case .MethodType_refreshCoin:
            //: refreshCoin(price: Double(json["money"].stringValue) ?? 0, payMode: "Other", scene: json["scene"].stringValue)
            screening(price: Double(json[(showCareData.replacingOccurrences(of: "destroy", with: "y"))].stringValue) ?? 0, payMode: (String(user_environmentTitle.prefix(5))), scene: json[(String(kPureString))].stringValue)

        //: case .MethodType_appearsOnGiftBroadcast:
        case .MethodType_appearsOnGiftBroadcast:
            //: appearsOnGiftBroadcast(show: json["show"].boolValue)
            totalerval(show: json[(String(userReportName))].boolValue)

        //: case.MethodType_ToUrl:
        case .MethodType_ToUrl:
            //: openTourl(url: json.rawString() ?? "")
            normDeploy(url: json.rawString() ?? "")

        //: case .MethodType_customerService:
        case .MethodType_customerService:
            //: OriginMediumWill.share.func__pushToPriveteChatVC(chatID: MovementStructure.getCustomerServiceID())
            OriginMediumWill.share.drawingPreparation(chatID: MovementStructure.cartoonConnection())

        //: case.setRightMenuButton:
        case .setRightMenuButton:
            //: break
            break

        //: case .MethodType_taskCenter:
        case .MethodType_taskCenter:
            //: let target = json["target"].intValue
            let target = json[(dataLimitedFormat.replacingOccurrences(of: "exclusive", with: "a"))].intValue
            //: self.reloadInputViews()
            self.reloadInputViews()
            //: switch target {
            switch target {
            //: case 1:
            case 1: /// 签到领金币页
                //: OriginMediumWill.share.func__pushToSubscribePageWebVC()
                OriginMediumWill.share.direction()

            //: case 2:
            case 2: /// 个人信息编辑页面
                //: let vc = TalkingEditProfilesVC.init()
                let vc = StrengthFit()
                //: self.navigationController?.pushViewController(vc, animated: true)
                self.navigationController?.pushViewController(vc, animated: true)

            //: case 3: break
            case 3: break /// 真人认证页面

            //: case 4:
            case 4: /// 发布动态页面
                //: tabberSelete(type: TabBarItemType.Moment.rawValue)
                sir(type: StatusLiteText.Moment.rawValue)
                /// 防止动态首页广播还没创建
                //: DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 0.5) {
                DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 0.5) {
                    //: NotificationCenter.default.post(name: FREEBTN_UPLOAD_POST_NOTIFICATION,
                    NotificationCenter.default.post(name: noti_mobilePath,
                                                    //: object: nil,
                                                    object: nil,
                                                    //: userInfo: nil)
                                                    userInfo: nil)
                }
            //: case 5:
            case 5: /// 我的页面
                //: tabberSelete(type: TabBarItemType.Account.rawValue)
                sir(type: StatusLiteText.Account.rawValue)
            //: case 6:
            case 6: /// 首页
                //: tabberSelete(type: TabBarItemType.Social.rawValue)
                sir(type: StatusLiteText.Social.rawValue)
            //: case 7:
            case 7: /// 消息列表页
                //: tabberSelete(type: TabBarItemType.Message.rawValue)
                sir(type: StatusLiteText.Message.rawValue)
            //: case 8:
            case 8: /// Greet setting页
                //: break
                break
            //: case 9:
            case 9: /// 动态列表页
                //: tabberSelete(type: TabBarItemType.Moment.rawValue)
                sir(type: StatusLiteText.Moment.rawValue)
            //: default:
            default:
                //: break
                break
            }
        //: case .MethodType_systemNotification:
        case .MethodType_systemNotification:
            //: let type = json["type"].intValue
            let type = json[(data_speedMainName.replacingOccurrences(of: "certain", with: "yp"))].intValue
            //: changeNotifaStatus(type: type)
            pendingReflect(type: type)

        //: case .MethodType_onEvent:
        case .MethodType_onEvent:
            //: reportEvent(eventName: json["eventName"].stringValue, jsonStr: json["jsonString"].stringValue)
            instruction(eventName: json[(String(main_withinArrowTitle.suffix(6)) + appImpressionFormat.replacingOccurrences(of: "critical", with: "am"))].stringValue, jsonStr: json[(String(notiMarginString.prefix(5)) + "tring")].stringValue)

        //: case .MethodType_refreshCoinAll:
        case .MethodType_refreshCoinAll:
            //: req_refreshCoinAll(coin: json["coin"].stringValue)
            outsideCapture(coin: json[(data_mineSecretTitle.replacingOccurrences(of: "segment", with: "in"))].stringValue)

        //: case .MethodType_refreshPackage:
        case .MethodType_refreshPackage:
            //: req_refreshPackage()
            dataObtain()

        //: case .MethodType_vibrate:
        case .MethodType_vibrate:
            //: phoneVibrate()
            exhaust()

        //: case .MethodType_goFreeVideo:
        case .MethodType_goFreeVideo:
            //: AppManagerRequest.func__requestUserInfo { succeed, result, errorModel in
            ProjectBlack.wait { succeed, result, errorModel in
                //: if MarginExamineer.share.loginUserMode.jumpType == 1 {
                if MarginExamineer.share.loginUserMode.jumpType == 1 {
                    //: self.func__closeAction(sender: nil)
                    self.bindOf(sender: nil)
                    //: DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 0.5) {
                    DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 0.5) {
                        //: OriginMediumWill.share.func__pushToRandomVideoVC(isBeginRand: false)
                        OriginMediumWill.share.likeBox(isBeginRand: false)
                    }
                }
            }

        //: case .MethodType_goMessageDetail:
        case .MethodType_goMessageDetail:
            //: OriginMediumWill.share.func__pushToPriveteChatVC(chatID: json["uid"].stringValue)
            OriginMediumWill.share.drawingPreparation(chatID: json[(k_sinkKey.replacingOccurrences(of: "bad", with: "id"))].stringValue)

        //: case .MethodType_goPersonInfo:
        case .MethodType_goPersonInfo:
            //: OriginMediumWill.share.func__pushToUserDetailVC(uid: json["uid"].stringValue)
            OriginMediumWill.share.petAcrossAm(uid: json[(k_sinkKey.replacingOccurrences(of: "bad", with: "id"))].stringValue)

        //: case .MethodType_showRechargeDialog:
        case .MethodType_showRechargeDialog:
            //: OriginMediumWill.share.func__pushToHalfWebVC(webViewType: .RechargeHalfPage)
            OriginMediumWill.share.untilAmongForce(webViewType: .RechargeHalfPage)

        //: case .OpenRetainPopup:
        case .OpenRetainPopup:
            //: let view = TalkingRechargeDetentionView.init()
            let view = ResolveHidden()
            //: view.dtype = json["type"].intValue
            view.dtype = json[(data_speedMainName.replacingOccurrences(of: "certain", with: "yp"))].intValue
            //: view.closeWeb = json["closeWeb"].boolValue
            view.closeWeb = json[(String(userServerStr) + String(notiHelpId.prefix(3)))].boolValue
            //: view.show()
            view.trackItem()
            //: view.closWebClock = { [weak self] in
            view.closWebClock = { [weak self] in
                //: guard let self = self else {
                guard let self = self else {
                    //: return
                    return
                }
                //: self.func__closeAction(sender: nil)
                self.bindOf(sender: nil)
            }

        //: case .openBrowserWithUrl:
        case .openBrowserWithUrl:
            //: openToOutUrl(url: json.rawString() ?? "")
            exhaustDown(url: json.rawString() ?? "")
        //: case .none:
        case .none:
            //: printLog(message: "未实现的js事件：\(message.name)")
            printLog(message: (String(mainStatementId.prefix(6)) + k_orientationUrl) + "\(message.name)")
        }
    }

    // MARK: - Event

    /// 苹果订阅
    /// - Parameters:
    ///   - type: 订阅入口；1：首页banner，2：全屏充值页，3：半屏充值页，4：领取金币弹窗
    ///   - productId: 商品Id
    ///   - payType: 支付类型
    //: private func applePay_subscribe(type: String, productId: String, payType: ApplePayType) {
    private func acceptDecide(type: String, productId: String, payType: AssociateImplementation) {
        //: applePay(productId: productId, payType: payType)
        wen(productId: productId, payType: payType)
    }

    /// 苹果支付/订阅
    /// - Parameters:
    ///   - productId: productId: 商品Id
    ///   - source: 充值来源
    //: private func applePay(productId: String, source: Int = -1, payType: ApplePayType) {
    private func wen(productId: String, source: Int = -1, payType: AssociateImplementation) {
        //: if MarginExamineer.share.loginUid.isEmptyString {
        if MarginExamineer.share.loginUid.isEmptyString {
            //: return
            return
        }
        //: FirebaseAnalyticsManager.share.logEvent(name: "PurchaseClick")
        ProcessorReceive.share.joinOfShrimp(name: (String(mainInfrastructureLawFormat.suffix(3)) + app_ageStr.replacingOccurrences(of: "attach", with: "a") + "Click"))

        //: ProgressHUD.show()
        BeforeImagePhase.theGemRoll()
        //: var index = isVideoCallRechargeHalfPage ? 1: 0
        var index = isVideoCallRechargeHalfPage ? 1 : 0
        //: if source != -1 {
        if source != -1 {
            //: index = source
            index = source
        }
        //: AppleIAPManager.shared.iap_startPurchase(productId: productId, payType: payType, source: index) { status, reportMoney, payType in
        ProvisionCell.shared.fastDisclose(productId: productId, payType: payType, source: index) { status, reportMoney, payType in
            //: ProgressHUD.dismiss()
            BeforeImagePhase.totalGap()
            //: DispatchQueue.main.async { [self] in
            DispatchQueue.main.async { [self] in
                //: switch status {
                switch status {
                //: case .verityFail:
                case .verityFail:
                    //: ProgressHUD.toast( "Retry After or Go to \"Feedback\" to contact us".localized)
                    BeforeImagePhase.hunkLight((String(appBlankUrl.suffix(4)) + "y Af" + String(kObtainMsg.prefix(6)) + " Go t" + String(notiFormalData)) + "\"" + (appEaseCompleteFractionUrl.replacingOccurrences(of: "refugee", with: "F") + userContactPath.replacingOccurrences(of: "reading", with: "b")) + "\"" + (String(data_hourKey.prefix(6)) + String(dataDecisionData)).localized)
                //: case .veritySucceed:
                case .veritySucceed:
                    //: appleIAPVeritySucceed(price: reportMoney, payType: payType)
                    poloAlways(price: reportMoney, payType: payType)
                //: case .renewSucceed:
                case .renewSucceed:
                    //: let scene = (payType == .Subscribe) ? "1":"2"
                    let scene = (payType == .Subscribe) ? "1" : "2"
                    //: refreshCoin(price: reportMoney, payMode: "Apple", scene: scene)
                    screening(price: reportMoney, payMode: (String(dataPageUndertakeName.suffix(5))), scene: scene)
                //: default:
                default:
                    //: printLog(message: " apple支付充值失败：\(status.rawValue)")
                    printLog(message: (String(show_choiceDeployTitle) + appLogCareFormat) + "\(status.rawValue)")
                }
            }
        }
    }

    /// 设置导航右侧按钮
    /// - Parameters:
    ///   - title: 标题
    ///   - url: 地址
    //: private func changeRightMenuButton(title: String, url: String) {
    private func followEquipment(title: String, url: String) {
        //: let rightBtn = TalkingButton()
        let rightBtn = DistinctionButton()
        //: rightBtn.titleLabel?.font = .systemFont(ofSize: 14)
        rightBtn.titleLabel?.font = .systemFont(ofSize: 14)
        //: rightBtn.setTitle(title, for: .normal)
        rightBtn.setTitle(title, for: .normal)
        //: rightBtn.setTitleColor(.RGBA(51, 51, 51, 1), for: .normal)
        rightBtn.setTitleColor(.our(51, 51, 51, 1), for: .normal)
        //: rightBtn.addTarget(self, action: #selector(rightButtonClick), for: .touchUpInside)
        rightBtn.addTarget(self, action: #selector(chapterCommand), for: .touchUpInside)
        //: rightBtn.sizeToFit()
        rightBtn.sizeToFit()
        //: rightBtn.userInfo = ["url": url]
        rightBtn.userInfo = [(String(show_refValue)): url]
        //: self.navigationItem.rightBarButtonItem = UIBarButtonItem(customView: rightBtn)
        self.navigationItem.rightBarButtonItem = UIBarButtonItem(customView: rightBtn)
    }

    //: @objc func rightButtonClick(button: TalkingButton) {
    @objc func chapterCommand(button: DistinctionButton) {
        //: guard let urlString = button.userInfo?["url"] as? String else {
        guard let urlString = button.userInfo?[(String(show_refValue))] as? String else {
            //: return
            return
        }

        //: self.urlStr = urlString
        self.urlStr = urlString
        //: func__startRequest()
        shOn()
    }

    /// 退出登录
    //: private func needLogin() {
    private func second() {
        //: guard Int(MarginExamineer.share.loginUid) ?? 0 > 0 else {
        guard Int(MarginExamineer.share.loginUid) ?? 0 > 0 else {
            //: self.webView.reload()
            self.webView.reload()
            //: return
            return
        }

        //: NotificationCenter.default.post(name: DID_LOGIN_OUT_SUCCESS_NOTIFICATION,
        NotificationCenter.default.post(name: mainOperationPadString,
                                        //: object: nil,
                                        object: nil,
                                        //: userInfo: nil)
                                        userInfo: nil)
    }

    /// 苹果 支付/订阅 成功验证通过
    /// - Parameters:
    ///   - price: 充值金额
    ///   - payType: 支付类型
    //: func appleIAPVeritySucceed(price: Double, payType: ApplePayType) {
    func poloAlways(price: Double, payType: AssociateImplementation) {
        //: self.webView.evaluateJavaScript("payResultCallback();") { data, error in
        self.webView.evaluateJavaScript((String(constScatterBelongTitle.suffix(7)) + String(mainYesterdayKey) + String(user_sKey))) { data, error in
        }
        //: let scene = (payType == .Subscribe) ? "1":"2"
        let scene = (payType == .Subscribe) ? "1" : "2"
        //: refreshCoin(price: price, payMode: "Apple", scene: scene)
        screening(price: price, payMode: (String(dataPageUndertakeName.suffix(5))), scene: scene)
    }

    /// 支付/订阅 成功刷新个人信息【Apple/Other】
    /// - Parameters:
    ///   - price: 充值金额
    ///   - payMode: 类型
    ///   - scene: 1：订阅，2：内购
    //: private func refreshCoin(price: Double, payMode: String, scene: String) {
    private func screening(price: Double, payMode: String, scene: String) {
        //: NotificationCenter.default.post(name: UPDATE_USER_GETMYINFO_NOTIFICATION, object: nil)
        NotificationCenter.default.post(name: appShowSineStr, object: nil)
        //: NotificationCenter.default.post(name: UPDATE_USER_GETINFOCOLUMN_NOTIFICATION, object: nil)
        NotificationCenter.default.post(name: main_attachMessage, object: nil)
        //: NotificationCenter.default.post(name: REFRESH_INDEX_GETCONFIG_NOTIFICATION, object: nil)
        NotificationCenter.default.post(name: appCurveTitle, object: nil)
        //: NotificationCenter.default.post(name: USER_UPDATE_LOUNGEPLUSINFO_NOTIFICATION, object: nil)
        NotificationCenter.default.post(name: main_colorKey, object: nil)
        // 成功埋点
        //: if scene == "1" {
        if scene == "1" {
            //: TalkingAdjustManager.share.addSubscribeEvent(price: price, currency: "USD")
            SignInputAdjust.share.tooFollow(price: price, currency: (String(data_tempId)))
            //: } else {
        } else {
            //: TalkingAdjustManager.share.addPurchasedEvent(price: price, currency: "USD")
            SignInputAdjust.share.formLevel(price: price, currency: (String(data_tempId)))
        }
        //: FirebaseAnalyticsManager.share.trackPurchase(payType: payMode, price: price, currency: "USD")
        ProcessorReceive.share.mediaAround(payType: payMode, price: price, currency: (String(data_tempId)))

        // 半屏充值成功埋点
        //: if RECHARGE_CLICK_EVENT.count > 0 {
        if noti_significantTitle.count > 0 {
            //: uploadRecord.uploadRecordEvent(eventID: RECHARGE_CLICK_EVENT, parameterStr: ["amount": price])
            noti_loadResolveMsg.groupSplit(eventID: noti_significantTitle, parameterStr: [String(bytes: dataLastId.map{offerProtection(success: $0)}, encoding: .utf8)!: price])
        }

        // 支付成功发送通知，调用三方js事件
        //: NotificationCenter.default.post(name: WEBVIEW_THIRD_REFRESHCOIN_NOTIFICATION, object: nil)
        NotificationCenter.default.post(name: dataEquipmentFormat, object: nil)
    }

    /// 广播设置
    /// - Parameter show: 是否上广播
    //: private func appearsOnGiftBroadcast(show: Bool) {
    private func totalerval(show: Bool) {}

    /// 底部tabbar切换
    //: private func tabberSelete(type: Int) {
    private func sir(type: Int) {
        //: guard type < self.tabBarController?.viewControllers?.count ?? 0 else { return }
        guard type < self.tabBarController?.viewControllers?.count ?? 0 else { return }
        //: let vc = self.tabBarController?.viewControllers?[type]
        let vc = self.tabBarController?.viewControllers?[type]
        //: self.tabBarController?.tabBar.isHidden = false
        self.tabBarController?.tabBar.isHidden = false
        //: self.tabBarController?.selectedIndex = type
        self.tabBarController?.selectedIndex = type
        //: self.tabBarController?.selectedViewController = vc
        self.tabBarController?.selectedViewController = vc
        //: self.navigationController?.popViewController(animated: true)
        self.navigationController?.popViewController(animated: true)
    }

    /// 获取系统通知状态
    //: private func changeNotifaStatus(type: Int) {
    private func pendingReflect(type: Int) {
        //: if type == 0 {
        if type == 0 {
            //: if let url = URL(string: UIApplication.openSettingsURLString), UIApplication.shared.canOpenURL(url) {
            if let url = URL(string: UIApplication.openSettingsURLString), UIApplication.shared.canOpenURL(url) {
                //: if #available(iOS 10.0, *) {
                if #available(iOS 10.0, *) {
                    //: UIApplication.shared.open(url, options: [:], completionHandler: nil)
                    UIApplication.shared.open(url, options: [:], completionHandler: nil)
                    //: } else {
                } else {
                    //: if UIApplication.shared.canOpenURL(url) {
                    if UIApplication.shared.canOpenURL(url) {
                        //: UIApplication.shared.openURL(url)
                        UIApplication.shared.openURL(url)
                    }
                }
            }
            //: } else if type == 1 {
        } else if type == 1 {
            //: AppManagerRequest.func__requestUserConfig { succeed, result, errorModel in }
            ProjectBlack.extra { succeed, result, errorModel in }
            //: AppManagerRequest.func__requestUserInfo { succeed, result, errorModel in }
            ProjectBlack.wait { succeed, result, errorModel in }
            //: TalkingPermissionTool.checkPushNotification { isShow in
            MultiplePoint.switchicial { isShow in
                //: DispatchQueue.main.sync {
                DispatchQueue.main.sync {
                    //: let str = isShow ? "getSystemNotificationtStatus(true)" : "getSystemNotificationtStatus(false)"
                    let str = isShow ? String(bytes: mainNeedKey.map{collectStat(commit: $0)}, encoding: .utf8)! : String(bytes: dataTheMixTitle.map{subtleRecentInjury(replacement: $0)}, encoding: .utf8)!
                    //: self.webView.evaluateJavaScript(str) { data, error in
                    self.webView.evaluateJavaScript(str) { data, error in
                    }
                }
            }
        }
    }

    /// 刷新金币
    /// - Parameter coin: 传' ', 刷新接口；传其他，进行相加（含负数））
    //: private func req_refreshCoinAll(coin: String) {
    private func outsideCapture(coin: String) {
        //: let value: Double = Double(coin) ?? 0
        let value = Double(coin) ?? 0
        //: if value == 0 {
        if value == 0 { // 刷新接口
            //: AppManagerRequest.func__getInfoColumn { succeed, result, errorModel in
            ProjectBlack.enter { succeed, result, errorModel in
            }
            //: } else {
        } else { // 加减
            //: let newCoin = Double(MarginExamineer.share.loginUserMode.mf_coin)! + value
            let newCoin = Double(MarginExamineer.share.loginUserMode.mf_coin)! + value
            //: MarginExamineer.share.loginUserMode.mf_coin = String(format: "%.2f", newCoin)
            MarginExamineer.share.loginUserMode.mf_coin = String(format: "%.2f", newCoin)
        }
    }

    /// 刷新礼物背包
    //: private func req_refreshPackage() {
    private func dataObtain() {
        //: NotificationCenter.default.post(name: GIFT_REFRESHPACKAGE_NOTIFICATION, object: nil)
        NotificationCenter.default.post(name: mainLockString, object: nil)
    }

    /// 事件上报
    /// - Parameters:
    ///   - eventName: 事件名称
    ///   - jsonStr: 事件参数
    //: private func reportEvent(eventName: String, jsonStr: String) {
    private func instruction(eventName: String, jsonStr: String) {
        //: uploadRecord.uploadRecordEvent(eventID: eventName, toUid: "", jsonStr: jsonStr)
        noti_loadResolveMsg.priorityFun(eventID: eventName, toUid: "", jsonStr: jsonStr)
    }

    /// 手机震动
    //: private func phoneVibrate() {
    private func exhaust() {
        //: AudioServicesPlaySystemSound(kSystemSoundID_Vibrate)
        AudioServicesPlaySystemSound(kSystemSoundID_Vibrate)
    }
}
