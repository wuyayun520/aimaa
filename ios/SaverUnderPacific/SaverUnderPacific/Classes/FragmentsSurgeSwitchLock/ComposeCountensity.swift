
//: Declare String Begin

/*: "init(coder:) has not been implemented" :*/
fileprivate let main_piTalkString:[UInt8] = [0x64,0x65,0x74,0x6e,0x65,0x6d,0x65,0x6c,0x70,0x6d,0x69,0x20,0x6e,0x65,0x65,0x62,0x20,0x74,0x6f,0x6e,0x20,0x73,0x61,0x68,0x20,0x29,0x3a,0x72,0x65,0x64,0x6f,0x63,0x28,0x74,0x69,0x6e,0x69]

/*: "Load..." :*/
fileprivate let user_tensionMsg:String = "Load.pack eager"
fileprivate let k_userMsg:[Character] = [".","."]

/*: "Skip" :*/
fileprivate let user_packCornerTitle:String = "peer supposedSkip"

/*: "HttpTool.NativeToJs('recharge')" :*/
fileprivate let const_itsData:[UInt8] = [0x19,0x25,0x25,0x21,0x5,0x3e,0x3e,0x3d,0x7f,0x1f,0x30,0x25,0x38,0x27,0x34,0x5,0x3e,0x1b,0x22,0x79,0x76,0x23,0x34,0x32,0x39,0x30,0x23,0x36,0x34,0x76,0x78]

private func priceEstimated(clean num: UInt8) -> UInt8 {
    return num ^ 81
}

/*: "window.onPageShow&&onPageShow();" :*/
fileprivate let notiPermissionData:[UInt8] = [0x5a,0x44,0x43,0x49,0x42,0x5a,0x3,0x42,0x43,0x7d,0x4c,0x4a,0x48,0x7e,0x45,0x42,0x5a,0xb,0xb,0x42,0x43,0x7d,0x4c,0x4a,0x48,0x7e,0x45,0x42,0x5a,0x5,0x4,0x16]

/*: "jsEvent(onPageShow):  :*/
fileprivate let showSpecifyUrl:String = "my block await procedurejsEv"
fileprivate let dataCompositionString:String = "onPaopen relation"
fileprivate let appCommitFormat:String = "ow): remind comprehensive signature"

/*: "window.onPageHide&&onPageHide();" :*/
fileprivate let notiMedicPath:[UInt8] = [0xaf,0xb1,0xb6,0xbc,0xb7,0xaf,0xf6,0xb7,0xb6,0x88,0xb9,0xbf,0xbd,0x90,0xb1,0xbc,0xbd,0xfe,0xfe,0xb7,0xb6,0x88,0xb9,0xbf,0xbd,0x90,0xb1,0xbc,0xbd,0xf0,0xf1,0xe3]

private func floorReplace(camera num: UInt8) -> UInt8 {
    return num ^ 216
}

/*: "jsEvent(onPageHide):  :*/
fileprivate let app_resData:[Character] = ["j","s","E","v","e","n","t","(","o","n","P","a","g","e","H"]
fileprivate let data_rebuildMsg:[Character] = ["i","d","e",")",":"," "]

/*: "title" :*/
fileprivate let dataPowderData:[Character] = ["t","i","t","l","e"]

/*: "canGoBack" :*/
fileprivate let app_helpAnonymousId:String = "ya position increase warningcanGo"

/*: "PHPSESSID= :*/
fileprivate let dataRootAccessibleId:String = "obtainP"
fileprivate let show_decisionUrl:String = "SESSID=confirm brush interest three boyfriend"

/*: ";uid= :*/
fileprivate let const_blocMsg:[Character] = [";","u","i","d","="]

/*: "Cookie" :*/
fileprivate let data_cancelValue:String = "tolerance pull flexible weeklyCookie"

/*: "nav_back_black_nor" :*/
fileprivate let userSlideBeyondPath:[Character] = ["n","a","v","_","b","a","c","k","_","b","l"]
fileprivate let kRoundValue:String = "head restoreack_nor"

/*: "ico_intimate_list_sm" :*/
fileprivate let showThirdAriaPath:[Character] = ["i","c","o","_","i","n","t","i","m","a"]
fileprivate let kSpecTitle:String = "schedule chapter a request methodte_li"

/*: "document.getElementsByTagName('body')[0].style.background='rgba(0,0,0,0)'" :*/
fileprivate let show_recoverValue:[UInt8] = [0x7b,0x86,0x7a,0x8c,0x84,0x7c,0x85,0x8b,0x45,0x7e,0x7c,0x8b,0x5c,0x83,0x7c,0x84,0x7c,0x85,0x8b,0x8a,0x59,0x90,0x6b,0x78,0x7e,0x65,0x78,0x84,0x7c,0x3f,0x3e,0x79,0x86,0x7b,0x90,0x3e,0x40,0x72,0x47,0x74,0x45,0x8a,0x8b,0x90,0x83,0x7c,0x45,0x79,0x78,0x7a,0x82,0x7e,0x89,0x86,0x8c,0x85,0x7b,0x54,0x3e,0x89,0x7e,0x79,0x78,0x3f,0x47,0x43,0x47,0x43,0x47,0x43,0x47,0x40,0x3e]

fileprivate func sumeractionAt(ask num: UInt8) -> UInt8 {
    let value = Int(num) - 23
    if value < 0 {
        return UInt8(value + 256)
    } else {
        return UInt8(value)
    }
}

/*: "Alert" :*/
fileprivate let notiWeekProId:[Character] = ["A","l","e","r","t"]

/*: "Cancle" :*/
fileprivate let const_orangeKey:[Character] = ["C","a","n","c","l","e"]

/*: "Done" :*/
fileprivate let main_formalListMsg:[Character] = ["D","o","n","e"]

/*: "js call method name =  :*/
fileprivate let app_othersTitle:String = "js cpass anonymous mm word mode"
fileprivate let userSpeedString:String = "metmineod"
fileprivate let user_viaLogDeliveryKey:[Character] = [" "]

/*: , message =  :*/
fileprivate let show_piCanTitle:String = ", mestrength factor parent"
fileprivate let kEvolutionId:String = "e = leader game segment concrete post"

/*: "http://" :*/
fileprivate let dataOfKey:[Character] = ["h","t","t","p",":","/","/"]

/*: "https://" :*/
fileprivate let showSendKey:String = "https://dark skip ad written"

/*: "var cookieNames = document.cookie.split('; ').map(function(cookie) { return cookie.split('=')[0] } );\n" :*/
fileprivate let constWithoutBigFormat:[UInt8] = [0x6,0xf1,0x2,0xb0,0xf3,0xff,0xff,0xfb,0xf9,0xf5,0xde,0xf1,0xfd,0xf5,0x3,0xb0,0xcd,0xb0,0xf4,0xff,0xf3,0x5,0xfd,0xf5,0xfe,0x4,0xbe,0xf3,0xff,0xff,0xfb,0xf9,0xf5,0xbe,0x3,0x0,0xfc,0xf9,0x4,0xb8,0xb7,0xcb,0xb0,0xb7,0xb9,0xbe,0xfd,0xf1,0x0,0xb8,0xf6,0x5,0xfe,0xf3,0x4,0xf9,0xff,0xfe,0xb8,0xf3,0xff,0xff,0xfb,0xf9,0xf5,0xb9,0xb0,0xb,0xb0,0x2,0xf5,0x4,0x5,0x2,0xfe,0xb0,0xf3,0xff,0xff,0xfb,0xf9,0xf5,0xbe,0x3,0x0,0xfc,0xf9,0x4,0xb8,0xb7,0xcd,0xb7,0xb9,0xeb,0xc0,0xed,0xb0,0xd,0xb0,0xb9,0xcb,0x9a]

fileprivate func hereYes(age num: UInt8) -> UInt8 {
    let value = Int(num) - 144
    if value < 0 {
        return UInt8(value + 256)
    } else {
        return UInt8(value)
    }
}

/*: "uid= :*/
fileprivate let userItActionData:String = "uid=back model unless attach symbol"

/*: ;domain= :*/
fileprivate let noti_poGreetKey:[Character] = [";","d"]
fileprivate let k_maleKey:String = "omain=smart radio"

/*: ;path=/" :*/
fileprivate let show_appropriateBetterPath:[Character] = [";","p","a","t","h","=","/"]

/*: "document.cookie=' :*/
fileprivate let constAssociateData:String = "veryocu"
fileprivate let show_dealKey:String = "construct locate if professional bigookie="
fileprivate let notiPeopleOccurPath:[Character] = ["\'"]

/*: ;domin= :*/
fileprivate let notiReceiverId:[Character] = [";"]
fileprivate let main_transactionKey:String = "snap elevator available other modeldomin="

/*: "if (cookieNames.indexOf('PHPSESSID') == -1) { document.cookie=' :*/
fileprivate let show_beId:[UInt8] = [0xee,0xe1,0xa7,0xaf,0xe4,0xe8,0xe8,0xec,0xee,0xe2,0xc9,0xe6,0xea,0xe2,0xf4,0xa9,0xee,0xe9,0xe3,0xe2,0xff,0xc8,0xe1,0xaf,0xa0,0xd7,0xcf,0xd7,0xd4,0xc2,0xd4,0xd4,0xce,0xc3,0xa0,0xae,0xa7,0xba,0xba,0xa7,0xaa,0xb6,0xae,0xa7,0xfc,0xa7,0xe3,0xe8,0xe4,0xf2,0xea,0xe2,0xe9,0xf3,0xa9,0xe4,0xe8,0xe8,0xec,0xee,0xe2,0xba,0xa0]

private func greetParty(tab num: UInt8) -> UInt8 {
    return num ^ 135
}

/*: "h5Domain =  :*/
fileprivate let mainIndependentId:[Character] = ["h","5","D"]
fileprivate let dataPresentationFormat:String = "recommendation away opportunity sparkomain = "

/*: "getAllCookies =  :*/
fileprivate let data_wantTitleMakeData:String = "getAlllocate infra directly ceiling she"
fileprivate let showMicFastPath:[Character] = ["C","o","o","k","i","e","s"," ","="," "]

/*: "PHPSESSID" :*/
fileprivate let user_presentationMessage:[Character] = ["P","H","P","S","E","S","S","I","D"]

/*: "uid" :*/
fileprivate let showStorageTitle:String = "uarchitecture"

/*: "appCookies =  :*/
fileprivate let noti_ariaKey:[Character] = ["a","p","p","C","o"]
fileprivate let userCordId:[Character] = ["o","k","i","e","s"," ","="," "]

/*: "UID" :*/
fileprivate let noti_closedValue:String = "uid"

/*: "navigator.userAgent" :*/
fileprivate let main_everythingDepthName:String = "navvolumea"
fileprivate let mainYesterdayTempKey:String = "serAgentpublisher weight"

/*: "xunwu_iphone" :*/
fileprivate let showCountoId:[Character] = ["x","u","n","w","u","_","i","p","h","o"]
fileprivate let showJustCapableId:String = "nlight"

/*: " xunwu_device_id/ :*/
fileprivate let kThumbString:String = "speed us xunw"
fileprivate let const_choiceData:[Character] = ["u","_","d","e","v","i","c","e","_","i","d","/"]

/*: "xunwu_iphone xunwu_version/ :*/
fileprivate let constPublisherPath:String = "xgemwu"
fileprivate let noti_blockData:String = "ne xurunning appear standard enable stop"
fileprivate let main_installFullMsg:String = "day track range today mageon/"

/*:  xunwu_package/ :*/
fileprivate let k_minuteMessage:[Character] = [" ","x","u","n","w","u","_"]
fileprivate let user_cutMenuId:String = "addition staff daily resource wedpackage/"

/*:  xunwu_bundleId/ :*/
fileprivate let mainAboutMessage:[Character] = [" ","x","u","n","w","u","_","b","u","n","d"]
fileprivate let app_specifyPath:String = "leId/delicate receiver action lock judge"

/*:   webkit/1" :*/
fileprivate let mainIncomeTitleFormat:[Character] = [" "," ","w","e","b"]
fileprivate let dataMistData:String = "kit/1coordinate constraint role texture"

//: Declare String End

// __DEBUG__
// __CLOSE_PRINT__
//
//  ComposeCountensity.swift
//  AbroadTalking
//
//  Created by Hemming on 2022/9/2.
//

//: import UIKit
import UIKit
//: import WebKit
import WebKit

//: class TalkingWebViewController: TalkingBasePresentViewController {
class ComposeCountensity: ResponseTopic {
    //: typealias ExitWebAction = ( ) -> Void
    typealias ExitWebAction = () -> Void
    //: typealias OpenWebSuccess = (_ openSucc: Bool) -> Void
    typealias OpenWebSuccess = (_ openSucc: Bool) -> Void
    //: typealias HandleJSAction = (_ actionName: String) -> Void
    typealias HandleJSAction = (_ actionName: String) -> Void

    //: private let disposeBag = DisposeBag()
    private let disposeBag = DisposeBag()
    //: private var requestReady = false
    private var requestReady = false

    //: private var config: TalkingWebConfig!
    private var config: ProvisionObserve!
    //: var isModal = false
    var isModal = false // 是否是模态弹出
    //: var urlStr: String!
    var urlStr: String!
    //: private var openBlock: OpenWebSuccess!
    private var openBlock: OpenWebSuccess!
    //: private var handleJSBlock: HandleJSAction!
    private var handleJSBlock: HandleJSAction!
    //: private var exitBlock: ExitWebAction!
    private var exitBlock: ExitWebAction!
    //: private var webUrlType: WebUrlType?
    private var webUrlType: MessageJob?
    //: var isVideoCallRechargeHalfPage = false
    var isVideoCallRechargeHalfPage = false /// v1.5.8是否视频弹窗唤醒的半屏充值或订阅

    //: init(urlString: String, webViewType: WebUrlType?, webConfig: TalkingWebConfig) {
    init(urlString: String, webViewType: MessageJob?, webConfig: ProvisionObserve) {
        //: super.init()
        super.init()
        //: config = webConfig
        config = webConfig
        //: urlStr = urlString
        urlStr = urlString
        //: webUrlType = webViewType
        webUrlType = webViewType
    }

    //: required init?(coder: NSCoder) {
    required init?(coder: NSCoder) {
        //: fatalError("init(coder:) has not been implemented")
        fatalError(String(bytes: main_piTalkString.reversed(), encoding: .utf8)!)
    }

    //: deinit {
    deinit {
        //: NSObject.cancelPreviousPerformRequests(withTarget: self)
        NSObject.cancelPreviousPerformRequests(withTarget: self)
        //: webView.stopLoading()
        webView.stopLoading()
        //: webView.uiDelegate = nil
        webView.uiDelegate = nil
        //: webView.navigationDelegate = nil
        webView.navigationDelegate = nil
        //: webView.removeFromSuperview()
        webView.removeFromSuperview()

        // 清缓存
        //: let websiteDataTypes: Set<String> = [WKWebsiteDataTypeDiskCache]
        let websiteDataTypes: Set<String> = [WKWebsiteDataTypeDiskCache]
        //: let dateForm = Date.init(timeIntervalSince1970: 0)
        let dateForm = Date(timeIntervalSince1970: 0)
        //: WKWebsiteDataStore.default().removeData(ofTypes: websiteDataTypes, modifiedSince: dateForm) {
        WKWebsiteDataStore.default().removeData(ofTypes: websiteDataTypes, modifiedSince: dateForm) {}
    }

    //: override func viewWillAppear(_ animated: Bool) {
    override func viewWillAppear(_ animated: Bool) {
        //: super.viewWillAppear(animated)
        super.viewWillAppear(animated)
        //: jsEvent_onPageShow()
        styleSimultaneously()
    }

    //: override func viewWillDisappear(_ animated: Bool) {
    override func viewWillDisappear(_ animated: Bool) {
        //: super.viewWillDisappear(animated)
        super.viewWillDisappear(animated)
        //: jsEvent_onPageHide()
        examineerComplete()
        /// 移除通知
        //: NotificationCenter.default.removeObserver(self)
        NotificationCenter.default.removeObserver(self)
    }

    //: override func viewDidLoad() {
    override func viewDidLoad() {
        //: super.viewDidLoad()
        super.viewDidLoad()
        //: view.clipsToBounds = true
        view.clipsToBounds = true
        //: self.title = "Load..."
        self.title = (String(user_tensionMsg.prefix(5)) + String(k_userMsg))
        //: if config.isHideBackBtn {
        if config.isHideBackBtn {
            //: hideNavi = true
            hideNavi = true
        }
        //: addNotification()
        agentArray()
        //: func__checkConfig()
        clearOver()
        //: func__setUpWebView()
        lessInsideTypical()
        //: func__registerObserver()
        circleSly()
        //: func__beginStartRequest()
        fireReply()
        //: func_checkAppleIAP()
        memberAlong()

        //: self.tranConfig.exitPrentView = { [weak self] in
        self.tranConfig.exitPrentView = { [weak self] in
            //: guard let self = self else {
            guard let self = self else {
                //: return
                return
            }
            //: if self.config.isHalfView && self.webUrlType == .RechargeHalfPage {
            if self.config.isHalfView && self.webUrlType == .RechargeHalfPage {
                //: OriginMediumWill.share.func__pushToWebVC(webViewType: .DiscountPopup)
                OriginMediumWill.share.coalition(webViewType: .DiscountPopup)
                //: NotificationCenter.default.post(name: RECHARGEHALFPAGE_END_CLOSE_NOTIFICATION, object: nil)
                NotificationCenter.default.post(name: user_sampleValue, object: nil)
            }
        }
    }

    // MARK: - Lazy load

    //: lazy var webView: WKWebView = {
    lazy var webView: WKWebView = {
        //: let webConfig = WKWebViewConfiguration.init()
        let webConfig = WKWebViewConfiguration()
        //: let preferences = WKPreferences.init()
        let preferences = WKPreferences()
        //: preferences.javaScriptEnabled = true
        preferences.javaScriptEnabled = true
        //: webConfig.preferences = preferences
        webConfig.preferences = preferences
        //: webConfig.allowsInlineMediaPlayback = true
        webConfig.allowsInlineMediaPlayback = true
        //: webConfig.mediaTypesRequiringUserActionForPlayback = []
        webConfig.mediaTypesRequiringUserActionForPlayback = []

        //: let userControl = WKUserContentController.init()
        let userControl = WKUserContentController()
        //: webConfig.userContentController = userControl
        webConfig.userContentController = userControl
        //: if #available(iOS 11.0, *) {
        if #available(iOS 11.0, *) {
            //: func__checkWebCookiesBeforeRequest(webConfig: webConfig)
            betweenMagnitude(webConfig: webConfig)
        }
        //: let w = WKWebView.init(frame: .zero, configuration: webConfig)
        let w = WKWebView(frame: .zero, configuration: webConfig)
        //: w.uiDelegate = self
        w.uiDelegate = self
        //: w.navigationDelegate = self
        w.navigationDelegate = self
        //: w.allowsLinkPreview = false
        w.allowsLinkPreview = false
        //: w.allowsBackForwardNavigationGestures = true
        w.allowsBackForwardNavigationGestures = true
        //: if #available(iOS 11.0, *) {
        if #available(iOS 11.0, *) {
            //: w.scrollView.contentInsetAdjustmentBehavior = .never
            w.scrollView.contentInsetAdjustmentBehavior = .never
        }
        //: return w
        return w
        //: }()
    }()

    //: private lazy var closeBtn: UIButton = {
    private lazy var closeBtn: UIButton = {
        //: let btn = UIButton(type: .custom)
        let btn = UIButton(type: .custom)
        //: let attributes = [.foregroundColor: UIColor.appThemeColor(), .font: UIFont.pingfangFont(type: .Medium, fontSize: 22), .underlineStyle: 1] as [NSAttributedString.Key: Any]
        let attributes = [.foregroundColor: UIColor.systemRequest(), .font: UIFont.yearNearby(type: .Medium, fontSize: 22), .underlineStyle: 1] as [NSAttributedString.Key: Any]
        //: let attributeString = NSAttributedString(string: "Skip".localized, attributes: attributes)
        let attributeString = NSAttributedString(string: (String(user_packCornerTitle.suffix(4))).localized, attributes: attributes)
        //: btn.setAttributedTitle(attributeString, for: .normal)
        btn.setAttributedTitle(attributeString, for: .normal)
        //: return btn
        return btn
        //: }()
    }()
}

// MARK: - 【通知、js事件】

//: extension TalkingWebViewController {
extension ComposeCountensity {
    /// 添加通知
    //: private func addNotification() {
    private func agentArray() {
        //: NotificationCenter.default.addObserver(self,
        NotificationCenter.default.addObserver(self,
                                               //: selector: #selector(third_jsEvent_refreshCoin),
                                               selector: #selector(ridCircle),
                                               //: name: WEBVIEW_THIRD_REFRESHCOIN_NOTIFICATION,
                                               name: dataEquipmentFormat,
                                               //: object: nil)
                                               object: nil)
        // 应用从后台切换到前台
        //: NotificationCenter.default.addObserver(self,
        NotificationCenter.default.addObserver(self,
                                               //: selector: #selector(jsEvent_onPageShow),
                                               selector: #selector(styleSimultaneously),
                                               //: name: UIApplication.willEnterForegroundNotification,
                                               name: UIApplication.willEnterForegroundNotification,
                                               //: object: nil)
                                               object: nil)
    }

    /// 通知三方H5刷新金币
    //: @objc private func third_jsEvent_refreshCoin() {
    @objc private func ridCircle() {
        //: self.webView.evaluateJavaScript("HttpTool.NativeToJs('recharge')") { data, error in
        self.webView.evaluateJavaScript(String(bytes: const_itsData.map{priceEstimated(clean: $0)}, encoding: .utf8)!) { data, error in
        }
    }

    /// js事件：网页展示
    //: @objc private func jsEvent_onPageShow() {
    @objc private func styleSimultaneously() {
        //: self.webView.evaluateJavaScript("window.onPageShow&&onPageShow();") { data, error in
        self.webView.evaluateJavaScript(String(bytes: notiPermissionData.map{$0^45}, encoding: .utf8)!) { data, error in
            //: printLog(message: "jsEvent(onPageShow): \(String(describing: data))---\(String(describing: error))")
            printLog(message: (String(showSpecifyUrl.suffix(4)) + "ent(" + String(dataCompositionString.prefix(4)) + "geSh" + String(appCommitFormat.prefix(5))) + "\(String(describing: data))---\(String(describing: error))")
        }
    }

    /// js事件：网页消失
    //: private func jsEvent_onPageHide() {
    private func examineerComplete() {
        //: self.webView.evaluateJavaScript("window.onPageHide&&onPageHide();") { data, error in
        self.webView.evaluateJavaScript(String(bytes: notiMedicPath.map{floorReplace(camera: $0)}, encoding: .utf8)!) { data, error in
            //: printLog(message: "jsEvent(onPageHide): \(String(describing: data))---\(String(describing: error))")
            printLog(message: (String(app_resData) + String(data_rebuildMsg)) + "\(String(describing: data))---\(String(describing: error))")
        }
    }
}

//: extension TalkingWebViewController {
extension ComposeCountensity {
    //: func func__registerObserver() {
    func circleSly() {
        //: webView.rx.observeWeakly(String.self, "title")
        webView.rx.observeWeakly(String.self, (String(dataPowderData)))
            //: .subscribe(onNext: { [weak self] (value) in
            .subscribe(onNext: { [weak self] value in
                //: guard let self = self else { return }
                guard let self = self else { return }
                //: self.title = value
                self.title = value
                //: })
            })
            //: .disposed(by: disposeBag)
            .disposed(by: disposeBag)

        //: webView.rx.observeWeakly(String.self, "canGoBack")
        webView.rx.observeWeakly(String.self, (String(app_helpAnonymousId.suffix(5)) + "Back"))
            //: .subscribe(onNext: { _ in
            .subscribe(onNext: { _ in
                //: })
            })
            //: .disposed(by: disposeBag)
            .disposed(by: disposeBag)

        //: NotificationCenter.default.addObserver(self, selector: #selector(closeDiscountsView), name: VIDEOCALL_END_CLOSEDISCOUNTS_NOTIFICATION, object: nil)
        NotificationCenter.default.addObserver(self, selector: #selector(unctionRemote), name: const_uniqueString, object: nil)
    }

    //: func func__setUpWebView() {
    func lessInsideTypical() {
        //: view.addSubview(webView)
        view.addSubview(webView)
        //: webView.snp.makeConstraints { make in
        webView.snp.makeConstraints { make in
            //: make.edges.equalToSuperview()
            make.edges.equalToSuperview()
        }
        //: func__addcustomUserAgent()
        eventOutputComposeAddMemory()
        //: func__addBridgeMethod()
        characteristicReading()

        //: if config.clearBgColor {
        if config.clearBgColor {
            //: view.backgroundColor = .clear
            view.backgroundColor = .clear
            //: } else if config.isAlertType || config.showLoadingView {
        } else if config.isAlertType || config.showLoadingView {
            //: view.backgroundColor = .black.withAlphaComponent(0.4)
            view.backgroundColor = .black.withAlphaComponent(0.4)
            //: webView.backgroundColor = UIColor.clear
            webView.backgroundColor = UIColor.clear
            //: } else {
        } else {
            //: view.backgroundColor = UIColor.appBgColor()
            view.backgroundColor = UIColor.alongSupposed()
        }
        //: if config.isHalfView {
        if config.isHalfView {
            //: webView.scrollView.bounces = false
            webView.scrollView.bounces = false
        }

        //: if config.showLoadingView {
        if config.showLoadingView {
            //: ProgressHUD.show(superView: webView)
            BeforeImagePhase.priorNearbyPermanent(superView: webView)
        }

        //: if config.showCloseBtn {
        if config.showCloseBtn {
            //: view.addSubview(closeBtn)
            view.addSubview(closeBtn)
            //: view.bringSubviewToFront(closeBtn)
            view.bringSubviewToFront(closeBtn)
            //: closeBtn.isHidden = false
            closeBtn.isHidden = false
            //: closeBtn.snp.makeConstraints { make in
            closeBtn.snp.makeConstraints { make in
                //: make.height.width.equalTo(50)
                make.height.width.equalTo(50)
                //: make.trailing.equalTo(-10)
                make.trailing.equalTo(-10)
                //: make.height.equalToSuperview().offset(StatusBarHeight)
                make.height.equalToSuperview().offset(const_musicMessage)
            }
        }
    }

    //: func func__beginStartRequest() {
    func fireReply() {
        //: if !requestReady {
        if !requestReady {
            //: return
            return
        }
        //: if config.isAlertType {
        if config.isAlertType {
            //: DispatchQueue.main.asyncAfter(deadline: .now()+1) { [self] in
            DispatchQueue.main.asyncAfter(deadline: .now() + 1) { [self] in
                //: self.func__startRequest()
                self.shOn()
            }
            //: } else {
        } else {
            //: func__startRequest()
            shOn()
        }
    }

    //: func func__startRequest() {
    func shOn() {
        //: var urlRequest = URLRequest.init(url: URL.init(string: urlStr)!)
        var urlRequest = URLRequest(url: URL(string: urlStr)!)
        //: let uid = MarginExamineer.share.loginUid
        let uid = MarginExamineer.share.loginUid
        //: let ssid = MarginExamineer.share.loginSessionId
        let ssid = MarginExamineer.share.loginSessionId
        //: var cookieStr = "PHPSESSID=\(ssid)"
        var cookieStr = (dataRootAccessibleId.replacingOccurrences(of: "obtain", with: "PH") + String(show_decisionUrl.prefix(7))) + "\(ssid)"
        //: if uid.count > 0 {
        if uid.count > 0 {
            //: cookieStr.append(";uid=\(uid)")
            cookieStr.append((String(const_blocMsg)) + "\(uid)")
        }
        //: urlRequest.addValue(cookieStr, forHTTPHeaderField: "Cookie")
        urlRequest.addValue(cookieStr, forHTTPHeaderField: (String(data_cancelValue.suffix(6))))
        //: urlRequest.httpShouldHandleCookies = false
        urlRequest.httpShouldHandleCookies = false
        //: webView.load(urlRequest)
        webView.load(urlRequest)
        //: func__clearJSBgColor()
        recoverForVia()
        //: func__initRightButton()
        domainOf()
    }

    /// apple支付重试末完成的订单
    //: func func_checkAppleIAP() {
    func memberAlong() {
        //: if webUrlType == .RechargeFullPage || webUrlType == .RechargeHalfPage {
        if webUrlType == .RechargeFullPage || webUrlType == .RechargeHalfPage {
            //: AppleIAPManager.shared.completionHandle = { [weak self] status, reportMoney, payType in
            ProvisionCell.shared.completionHandle = { [weak self] status, reportMoney, payType in
                //: guard let self = self else {
                guard let self = self else {
                    //: return
                    return
                }
                //: if status == .veritySucceed {
                if status == .veritySucceed {
                    //: self.appleIAPVeritySucceed(price: reportMoney, payType: payType)
                    self.poloAlways(price: reportMoney, payType: payType)
                }
            }
            //: AppleIAPManager.shared.iap_checkUnfinishedTransactions()
            ProvisionCell.shared.recordBy()
        }
    }

    //: func func__initLeftButton() {
    func nearWindow() {
        //: if self.presentingViewController != nil {
        if self.presentingViewController != nil {
            //: return
            return
        }
        //: let item = UIBarButtonItem.init(image: UIImage.BundleImageNamed(name: "nav_back_black_nor"), style: .plain, target: self, action: #selector(func__closeAction(sender:)))
        let item = UIBarButtonItem(image: UIImage.managerToSecond(name: (String(userSlideBeyondPath) + String(kRoundValue.suffix(7)))), style: .plain, target: self, action: #selector(bindOf(sender:)))
        //: item.imageInsets = UIEdgeInsets(top: 0, left: -6, bottom: 0, right: 0)
        item.imageInsets = UIEdgeInsets(top: 0, left: -6, bottom: 0, right: 0)
        //: item.width = 34
        item.width = 34
        //: self.navigationItem.leftBarButtonItem = item
        self.navigationItem.leftBarButtonItem = item
    }

    //: func func__initRightButton() {
    func domainOf() {
        //: self.navigationItem.rightBarButtonItem = nil
        self.navigationItem.rightBarButtonItem = nil
        //: if urlStr.contains(WebUrlType.Intimate.rawValue) {
        if urlStr.contains(MessageJob.Intimate.rawValue) {
            //: let item = UIBarButtonItem.init(image: UIImage.BundleImageNamed(name: "ico_intimate_list_sm"), style: .plain, target: self, action: #selector(func__intimateAction))
            let item = UIBarButtonItem(image: UIImage.managerToSecond(name: (String(showThirdAriaPath) + String(kSpecTitle.suffix(5)) + "st_sm")), style: .plain, target: self, action: #selector(statDirect))
            //: self.navigationItem.rightBarButtonItem = item
            self.navigationItem.rightBarButtonItem = item
        }
    }

    //: @objc func func__closeAction(sender: UIBarButtonItem?) {
    @objc func bindOf(sender: UIBarButtonItem?) {
        //: if webView.canGoBack && sender != nil {
        if webView.canGoBack, sender != nil {
            //: webView.goBack()
            webView.goBack()
            //: return
            return
        }
        //: if  exitBlock != nil {
        if exitBlock != nil {
            //: exitBlock()
            exitBlock()
        }
        //: self.tranConfig = nil
        self.tranConfig = nil
        //: func__removeBridgeMethod()
        visitWritten()
        //: if self.presentingViewController != nil {
        if self.presentingViewController != nil {
            // 当前页面dismiss后，下面还是网页时，手动调用viewDidAppear
            //: dismiss(animated: true) {
            dismiss(animated: true) {
                //: if let currentVC = self.currentViewController() {
                if let currentVC = self.myAcross() {
                    //: if currentVC.isKind(of: TalkingWebViewController.self) &&
                    if currentVC.isKind(of: ComposeCountensity.self),
                       //: (currentVC as! TalkingWebViewController).isModal == true {
                       (currentVC as! ComposeCountensity).isModal == true
                    {
                        //: (currentVC as! TalkingWebViewController).jsEvent_onPageShow()
                        (currentVC as! ComposeCountensity).styleSimultaneously()
                    }
                }
            }
            //: } else {
        } else {
            //: navigationController?.popViewController(animated: true)
            navigationController?.popViewController(animated: true)
        }
    }

    //: func func__checkConfig() {
    func clearOver() {
        //: if config.isAlertType {
        if config.isAlertType {
            //: config.showLoadingView = true
            config.showLoadingView = true
            //: config.showCloseBtn = true
            config.showCloseBtn = true
        }
        //: if #available(iOS 11.0, *) {
        if #available(iOS 11.0, *) {
            //: } else {
        } else {
            //: requestReady = true
            requestReady = true
        }
    }

    //: func func__hideLoadingBgViewAndCloseBtn(sucess: Bool) {
    func zoneOf(sucess: Bool) {
        //: ProgressHUD.dismiss()
        BeforeImagePhase.totalGap()
        //: UIView.animate(withDuration: 0.1) {
        UIView.animate(withDuration: 0.1) {
            //: self.view.backgroundColor = .clear
            self.view.backgroundColor = .clear
            //: if sucess {
            if sucess {
                //: self.closeBtn.isHidden = true
                self.closeBtn.isHidden = true
            }
        }
    }

    //: func func__clearJSBgColor() {
    func recoverForVia() {
        //: if config.clearBgColor {
        if config.clearBgColor {
            //: self.webView.evaluateJavaScript("document.getElementsByTagName('body')[0].style.background='rgba(0,0,0,0)'") { _, _  in
            self.webView.evaluateJavaScript(String(bytes: show_recoverValue.map{sumeractionAt(ask: $0)}, encoding: .utf8)!) { _, _ in
            }

            //: webView.backgroundColor = .clear
            webView.backgroundColor = .clear
            //: webView.scrollView.backgroundColor = .clear
            webView.scrollView.backgroundColor = .clear
            //: webView.scrollView.bounces = false
            webView.scrollView.bounces = false
            //: webView.isOpaque = false
            webView.isOpaque = false
        }
    }

    //: func openTourl(url: String) {
    func normDeploy(url: String) {
        //: if url.count > 0 {
        if url.count > 0 {
            //: OriginMediumWill.share.func__pushToWebVC(urlStr: url)
            OriginMediumWill.share.anyCalled(urlStr: url)
        }
    }

    //: func openToOutUrl(url: String) {
    func exhaustDown(url: String) {
        //: UIApplication.shared.open(URL(string: url)!, options: [:], completionHandler: nil)
        UIApplication.shared.open(URL(string: url)!, options: [:], completionHandler: nil)
    }

    //: @objc private func closeDiscountsView() {
    @objc private func unctionRemote() {
        //: if config.isDiscountsView {
        if config.isDiscountsView {
            //: var root = self.presentingViewController
            var root = self.presentingViewController
            //: while root != nil  {
            while root != nil {
                //: root?.dismiss(animated: true)
                root?.dismiss(animated: true)
                //: root = root?.presentingViewController
                root = root?.presentingViewController
            }
            //: NotificationCenter.default.removeObserver(self, name: VIDEOCALL_END_CLOSEDISCOUNTS_NOTIFICATION, object: nil)
            NotificationCenter.default.removeObserver(self, name: const_uniqueString, object: nil)
        }
    }

    /// 亲密度说明
    //: @objc private func func__intimateAction() {
    @objc private func statDirect() {
        //: let view = TalkingIntimateInstructionsPopUpView.init(frame: CGRect(x: 0, y: 0, width: ScreenWidth, height: ScreenHeight))
        let view = OriginalTranslate(frame: CGRect(x: 0, y: 0, width: show_errorChangeFormat, height: appLoadMessage))
        //: view.show()
        view.the()
    }
}

//: extension TalkingWebViewController: WKNavigationDelegate, WKUIDelegate {
extension ComposeCountensity: WKNavigationDelegate, WKUIDelegate {
    //: func webView(_ webView: WKWebView, decidePolicyFor navigationAction: WKNavigationAction, decisionHandler: @escaping (WKNavigationActionPolicy) -> Void) {
    func webView(_ webView: WKWebView, decidePolicyFor navigationAction: WKNavigationAction, decisionHandler: @escaping (WKNavigationActionPolicy) -> Void) {
        //: decisionHandler(.allow)
        decisionHandler(.allow)
    }

    //: func webView(_ webView: WKWebView, didStartProvisionalNavigation navigation: WKNavigation!) {
    func webView(_ webView: WKWebView, didStartProvisionalNavigation navigation: WKNavigation!) {
        //: UIApplication.shared.isNetworkActivityIndicatorVisible = true
        UIApplication.shared.isNetworkActivityIndicatorVisible = true
        //: if config.showLoadingView {
        if config.showLoadingView {
            //: ProgressHUD.show(superView: view)
            BeforeImagePhase.priorNearbyPermanent(superView: view)
        }
        //: if config.showCloseBtn {
        if config.showCloseBtn {
            //: view.bringSubviewToFront(closeBtn)
            view.bringSubviewToFront(closeBtn)
        }
    }

    //: func webView(_ webView: WKWebView, didFinish navigation: WKNavigation!) {
    func webView(_ webView: WKWebView, didFinish navigation: WKNavigation!) {
        //: UIApplication.shared.isNetworkActivityIndicatorVisible = false
        UIApplication.shared.isNetworkActivityIndicatorVisible = false
        //: if config.showCloseBtn && config.showLoadingView {
        if config.showCloseBtn && config.showLoadingView {
            //: func__hideLoadingBgViewAndCloseBtn(sucess: true)
            zoneOf(sucess: true)
        }
        //: if openBlock != nil {
        if openBlock != nil {
            //: openBlock(true)
            openBlock(true)
        }
        //: func__initLeftButton()
        nearWindow()
        //: func__clearJSBgColor()
        recoverForVia()
    }

    //: func webView(_ webView: WKWebView, didFailProvisionalNavigation navigation: WKNavigation!, withError error: Error) {
    func webView(_ webView: WKWebView, didFailProvisionalNavigation navigation: WKNavigation!, withError error: Error) {
        //: UIApplication.shared.isNetworkActivityIndicatorVisible = false
        UIApplication.shared.isNetworkActivityIndicatorVisible = false
        //: if config.showCloseBtn && config.showLoadingView {
        if config.showCloseBtn && config.showLoadingView {
            //: func__hideLoadingBgViewAndCloseBtn(sucess: false)
            zoneOf(sucess: false)
        }

        //: if openBlock != nil {
        if openBlock != nil {
            //: openBlock(false)
            openBlock(false)
        }
    }

    //: func webView(_ webView: WKWebView, didFail navigation: WKNavigation!, withError error: Error) {
    func webView(_ webView: WKWebView, didFail navigation: WKNavigation!, withError error: Error) {
        //: if config.showCloseBtn && config.showLoadingView {
        if config.showCloseBtn && config.showLoadingView {
            //: func__hideLoadingBgViewAndCloseBtn(sucess: false)
            zoneOf(sucess: false)
        }
        //: if openBlock != nil {
        if openBlock != nil {
            //: openBlock(false)
            openBlock(false)
        }
    }

    //: func webView(_ webView: WKWebView, didReceive challenge: URLAuthenticationChallenge, completionHandler: @escaping (URLSession.AuthChallengeDisposition, URLCredential?) -> Void) {
    func webView(_ webView: WKWebView, didReceive challenge: URLAuthenticationChallenge, completionHandler: @escaping (URLSession.AuthChallengeDisposition, URLCredential?) -> Void) {
        //: DispatchQueue.global().async {
        DispatchQueue.global().async {
            //: if challenge.protectionSpace.authenticationMethod == NSURLAuthenticationMethodServerTrust {
            if challenge.protectionSpace.authenticationMethod == NSURLAuthenticationMethodServerTrust {
                //: if challenge.previousFailureCount == 0 {
                if challenge.previousFailureCount == 0 {
                    //: let credential = URLCredential.init(trust: challenge.protectionSpace.serverTrust!)
                    let credential = URLCredential(trust: challenge.protectionSpace.serverTrust!)
                    //: completionHandler(.useCredential, credential)
                    completionHandler(.useCredential, credential)
                    //: } else {
                } else {
                    //: completionHandler(.cancelAuthenticationChallenge, nil)
                    completionHandler(.cancelAuthenticationChallenge, nil)
                }
                //: } else {
            } else {
                //: completionHandler(.cancelAuthenticationChallenge, nil)
                completionHandler(.cancelAuthenticationChallenge, nil)
            }
        }
    }

//    func webView(_ webView: WKWebView, runJavaScriptAlertPanelWithMessage message: String, initiatedByFrame frame: WKFrameInfo, completionHandler: @escaping () -> Void) {
//        let alertController = UIAlertController.init(title: "Alert", message: message, preferredStyle: .alert)
//        let action =  UIAlertAction.init(title: "OK".localized, style: .default) { _ in
//            completionHandler()
//        }
//        alertController.addAction(action)
//        present(alertController, animated: true)
//    }

    //: func webView(_ webView: WKWebView, runJavaScriptConfirmPanelWithMessage message: String, initiatedByFrame frame: WKFrameInfo, completionHandler: @escaping (Bool) -> Void) {
    func webView(_ webView: WKWebView, runJavaScriptConfirmPanelWithMessage message: String, initiatedByFrame frame: WKFrameInfo, completionHandler: @escaping (Bool) -> Void) {
        //: let alertController = UIAlertController.init(title: "Alert", message: message, preferredStyle: .alert)
        let alertController = UIAlertController(title: (String(notiWeekProId)), message: message, preferredStyle: .alert)
        //: let action =  UIAlertAction.init(title: "OK".localized, style: .default) { _ in
        let action = UIAlertAction(title: "OK".localized, style: .default) { _ in
            //: completionHandler(true)
            completionHandler(true)
        }
        //: let cancleAction =  UIAlertAction.init(title: "Cancle", style: .cancel) { _ in
        let cancleAction = UIAlertAction(title: (String(const_orangeKey)), style: .cancel) { _ in
            //: completionHandler(false)
            completionHandler(false)
        }
        //: alertController.addAction(cancleAction)
        alertController.addAction(cancleAction)
        //: alertController.addAction(action)
        alertController.addAction(action)

        //: present(alertController, animated: true)
        present(alertController, animated: true)
    }

    //: func webView(_ webView: WKWebView, runJavaScriptTextInputPanelWithPrompt prompt: String, defaultText: String?, initiatedByFrame frame: WKFrameInfo, completionHandler: @escaping (String?) -> Void) {
    func webView(_ webView: WKWebView, runJavaScriptTextInputPanelWithPrompt prompt: String, defaultText: String?, initiatedByFrame frame: WKFrameInfo, completionHandler: @escaping (String?) -> Void) {
        //: let alertController = UIAlertController.init(title: prompt, message: "", preferredStyle: .alert)
        let alertController = UIAlertController(title: prompt, message: "", preferredStyle: .alert)
        //: alertController.addTextField { textField in
        alertController.addTextField { textField in
            //: textField.text = defaultText
            textField.text = defaultText
        }
        //: let action =  UIAlertAction.init(title: "Done".localized, style: .default) { _ in
        let action = UIAlertAction(title: (String(main_formalListMsg)).localized, style: .default) { _ in
            //: completionHandler(alertController.textFields![0].text)
            completionHandler(alertController.textFields![0].text)
        }
        //: alertController.addAction(action)
        alertController.addAction(action)

        //: present(alertController, animated: true)
        present(alertController, animated: true)
    }
}

//: extension TalkingWebViewController: WKScriptMessageHandler {
extension ComposeCountensity: WKScriptMessageHandler {
    //: func userContentController(_ userContentController: WKUserContentController, didReceive message: WKScriptMessage) {
    func userContentController(_ userContentController: WKUserContentController, didReceive message: WKScriptMessage) {
        //: printLog(message: "js call method name = \(message.name), message = \(message.body)")
        printLog(message: (String(app_othersTitle.prefix(4)) + "all " + userSpeedString.replacingOccurrences(of: "mine", with: "h") + " name =" + String(user_viaLogDeliveryKey)) + "\(message.name)" + (String(show_piCanTitle.prefix(4)) + "ssag" + String(kEvolutionId.prefix(4))) + "\(message.body)")
        //: DispatchQueue.main.async {
        DispatchQueue.main.async {
            //: self.HandleScriptMessage(message)
            self.practicalMaterial(message)
            //: if self.handleJSBlock != nil {
            if self.handleJSBlock != nil {
                //: self.handleJSBlock(message.name)
                self.handleJSBlock(message.name)
            }
        }
    }

    //: func func__addBridgeMethod() {
    func characteristicReading() {
        //: let ucController = webView.configuration.userContentController
        let ucController = webView.configuration.userContentController
        //: for method in WebMethodType.allCases {
        for method in HideInstead.allCases {
            //: ucController.add(self, name: method.rawValue)
            ucController.add(self, name: method.rawValue)
        }
    }

    //: func func__removeBridgeMethod() {
    func visitWritten() {
        //: let ucController = webView.configuration.userContentController
        let ucController = webView.configuration.userContentController
        //: for method in WebMethodType.allCases {
        for method in HideInstead.allCases {
            //: ucController.removeScriptMessageHandler(forName: method.rawValue)
            ucController.removeScriptMessageHandler(forName: method.rawValue)
        }
    }
}

//: extension TalkingWebViewController {
extension ComposeCountensity {
    //: func func__addUserScript() {
    func tweenVertical() {
        //: var htmlDomain: String = MarginExamineer.share.appConfigMode.urlH5Domain
        var htmlDomain: String = MarginExamineer.share.appConfigMode.urlH5Domain

        //: if htmlDomain.hasPrefix("http://") {
        if htmlDomain.hasPrefix((String(dataOfKey))) {
            //: htmlDomain = htmlDomain.replacingOccurrences(of: "http://", with: "")
            htmlDomain = htmlDomain.replacingOccurrences(of: (String(dataOfKey)), with: "")
        }
        //: if htmlDomain.hasPrefix("https://") {
        if htmlDomain.hasPrefix((String(showSendKey.prefix(8)))) {
            //: htmlDomain = htmlDomain.replacingOccurrences(of: "https://", with: "")
            htmlDomain = htmlDomain.replacingOccurrences(of: (String(showSendKey.prefix(8))), with: "")
        }
        //: var cookiesStr = ""
        var cookiesStr = ""
        //: let tempUid: String = MarginExamineer.share.loginUserMode.userID
        let tempUid: String = MarginExamineer.share.loginUserMode.userID
        //: let tempSessionID: String = MarginExamineer.share.loginSessionId
        let tempSessionID: String = MarginExamineer.share.loginSessionId
        //: if #available(iOS 11.0, *) {
        if #available(iOS 11.0, *) {
            //: cookiesStr.append("var cookieNames = document.cookie.split('; ').map(function(cookie) { return cookie.split('=')[0] } );\n")
            cookiesStr.append(String(bytes: constWithoutBigFormat.map{hereYes(age: $0)}, encoding: .utf8)!)
            //: var isfirst = true
            var isfirst = true
            //: if tempUid.count > 0 {
            if tempUid.count > 0 {
                //: isfirst = false
                isfirst = false
                //: let tempStr = "uid=\(tempUid);domain=\(htmlDomain);path=/"
                let tempStr = (String(userItActionData.prefix(4))) + "\(tempUid)" + (String(noti_poGreetKey) + String(k_maleKey.prefix(6))) + "\(htmlDomain)" + (String(show_appropriateBetterPath))
                //: cookiesStr.append("document.cookie='\(tempStr)';\n")
                cookiesStr.append((constAssociateData.replacingOccurrences(of: "very", with: "d") + "ment.c" + String(show_dealKey.suffix(6)) + String(notiPeopleOccurPath)) + "\(tempStr)';\n")
            }
            //: if tempSessionID.count > 0 {
            if tempSessionID.count > 0 {
                //: let tempStr = "PHPSESSID=\(tempSessionID);domin=\(htmlDomain);path=/"
                let tempStr = (dataRootAccessibleId.replacingOccurrences(of: "obtain", with: "PH") + String(show_decisionUrl.prefix(7))) + "\(tempSessionID)" + (String(notiReceiverId) + String(main_transactionKey.suffix(6))) + "\(htmlDomain)" + (String(show_appropriateBetterPath))
                //: if isfirst {
                if isfirst {
                    //: cookiesStr.append("document.cookie='\(tempStr)';\n")
                    cookiesStr.append((constAssociateData.replacingOccurrences(of: "very", with: "d") + "ment.c" + String(show_dealKey.suffix(6)) + String(notiPeopleOccurPath)) + "\(tempStr)';\n")
                    //: } else {
                } else {
                    //: cookiesStr.append("if (cookieNames.indexOf('PHPSESSID') == -1) { document.cookie='\(tempStr)'; };\n")
                    cookiesStr.append(String(bytes: show_beId.map{greetParty(tab: $0)}, encoding: .utf8)! + "\(tempStr)'; };\n")
                }
            }
            //: } else {
        } else {
            //: if tempUid.count > 0 {
            if tempUid.count > 0 {
                //: let tempStr = "uid=\(tempUid);domain=\(htmlDomain);path=/"
                let tempStr = (String(userItActionData.prefix(4))) + "\(tempUid)" + (String(noti_poGreetKey) + String(k_maleKey.prefix(6))) + "\(htmlDomain)" + (String(show_appropriateBetterPath))
                //: cookiesStr.append("document.cookie='\(tempStr)'")
                cookiesStr.append((constAssociateData.replacingOccurrences(of: "very", with: "d") + "ment.c" + String(show_dealKey.suffix(6)) + String(notiPeopleOccurPath)) + "\(tempStr)'")
            }
            //: if tempSessionID.count > 0 {
            if tempSessionID.count > 0 {
                //: let tempStr = "uid=\(tempSessionID);domain=\(htmlDomain);path=/"
                let tempStr = (String(userItActionData.prefix(4))) + "\(tempSessionID)" + (String(noti_poGreetKey) + String(k_maleKey.prefix(6))) + "\(htmlDomain)" + (String(show_appropriateBetterPath))
                //: cookiesStr.append("document.cookie='\(tempStr)'")
                cookiesStr.append((constAssociateData.replacingOccurrences(of: "very", with: "d") + "ment.c" + String(show_dealKey.suffix(6)) + String(notiPeopleOccurPath)) + "\(tempStr)'")
            }
        }

        //: if cookiesStr.count > 0 {
        if cookiesStr.count > 0 {
            //: let userScript = WKUserScript.init(source: cookiesStr, injectionTime: .atDocumentStart, forMainFrameOnly: false)
            let userScript = WKUserScript(source: cookiesStr, injectionTime: .atDocumentStart, forMainFrameOnly: false)
            //: webView.configuration.userContentController.addUserScript(userScript)
            webView.configuration.userContentController.addUserScript(userScript)
        }
    }

    //: func func__checkWebCookiesBeforeRequest(webConfig: WKWebViewConfiguration) {
    func betweenMagnitude(webConfig: WKWebViewConfiguration) {
        // iOS11+ 判断当前的WKWebViewCookie
        //: if #available(iOS 11.0, *) {
        if #available(iOS 11.0, *) {
            //: let cookieStore = webConfig.websiteDataStore.httpCookieStore
            let cookieStore = webConfig.websiteDataStore.httpCookieStore
            //: let htmlDomain: String = MarginExamineer.share.appConfigMode.urlH5Domain
            let htmlDomain: String = MarginExamineer.share.appConfigMode.urlH5Domain
            //: let domain: String = URL.init(string: htmlDomain)?.host ?? ""
            let domain: String = URL(string: htmlDomain)?.host ?? ""
            //: printLog(message: "h5Domain = \(htmlDomain)")
            printLog(message: (String(mainIndependentId) + String(dataPresentationFormat.suffix(8))) + "\(htmlDomain)")
            //: let userID: String = MarginExamineer.share.loginUserMode.userID
            let userID: String = MarginExamineer.share.loginUserMode.userID
            //: cookieStore.getAllCookies { [weak self] cookies in
            cookieStore.getAllCookies { [weak self] cookies in
                //: guard let self = self else { return }
                guard let self = self else { return }
                //: printLog(message: "getAllCookies = \(cookies)")
                printLog(message: (String(data_wantTitleMakeData.prefix(6)) + String(showMicFastPath)) + "\(cookies)")
                //: var hasUidCookie = false
                var hasUidCookie = false
                //: var hasSessionCookie = false
                var hasSessionCookie = false
                //: for cookie in cookies {
                for cookie in cookies {
                    //: if domain.contains(cookie.domain) {
                    if domain.contains(cookie.domain) {
                        //: if cookie.name == "PHPSESSID" {
                        if cookie.name == (String(user_presentationMessage)) {
                            //: hasSessionCookie = true
                            hasSessionCookie = true
                            //: continue
                            continue
                        }
                        //: if cookie.name == "uid" && cookie.value == userID {
                        if cookie.name == (showStorageTitle.replacingOccurrences(of: "architecture", with: "id")), cookie.value == userID {
                            //: hasUidCookie = true
                            hasUidCookie = true
                            //: continue
                            continue
                        }
                        //: if hasSessionCookie && hasUidCookie {
                        if hasSessionCookie, hasUidCookie {
                            //: break
                            break
                        }
                    }
                }
                /// 服务器有返回
                //: if hasUidCookie && hasSessionCookie {
                if hasUidCookie, hasSessionCookie {
                    //: self.requestReady = true
                    self.requestReady = true
                    //: self.func__beginStartRequest()
                    self.fireReply()
                    //: } else {
                } else {
                    //: self.func__insertWebCookies(webConfig: webConfig, hasUid: hasUidCookie, hasSession: hasSessionCookie)
                    self.bathSign(webConfig: webConfig, hasUid: hasUidCookie, hasSession: hasSessionCookie)
                }
            }
        }
    }

    //: func func__insertWebCookies(webConfig: WKWebViewConfiguration, hasUid: Bool, hasSession: Bool ) {
    func bathSign(webConfig: WKWebViewConfiguration, hasUid: Bool, hasSession: Bool) {
        //: if #available(iOS 11.0, *) {
        if #available(iOS 11.0, *) {
            //: DispatchQueue.main.async {
            DispatchQueue.main.async {
                //: let cookieStore = webConfig.websiteDataStore.httpCookieStore
                let cookieStore = webConfig.websiteDataStore.httpCookieStore
                //: let htmlDomain: String = MarginExamineer.share.appConfigMode.urlH5Domain
                let htmlDomain: String = MarginExamineer.share.appConfigMode.urlH5Domain
                //: let h5Domain: String = URL.init(string: htmlDomain)?.host ?? ""
                let h5Domain: String = URL(string: htmlDomain)?.host ?? ""
                //: printLog(message: "h5Domain = \(htmlDomain)")
                printLog(message: (String(mainIndependentId) + String(dataPresentationFormat.suffix(8))) + "\(htmlDomain)")
                //: let appCookies: [HTTPCookie] = HTTPCookieStorage.shared.cookies(for: URL.init(string: TalkingRequestAddrTool.share.serverUrlStr)!)!
                let appCookies: [HTTPCookie] = HTTPCookieStorage.shared.cookies(for: URL(string: PresentLocal.share.serverUrlStr)!)!
                //: var addCookies: [HTTPCookie] = []
                var addCookies: [HTTPCookie] = []

                //: printLog(message: "appCookies = \(appCookies)")
                printLog(message: (String(noti_ariaKey) + String(userCordId)) + "\(appCookies)")

                //: for cookies in appCookies {
                for cookies in appCookies {
                    //: if cookies.name.uppercased() == "UID" && !hasUid {
                    if cookies.name.uppercased() == (noti_closedValue.uppercased()), !hasUid {
                        //: var temPro: Dictionary<HTTPCookiePropertyKey, Any> = cookies.properties!
                        var temPro: [HTTPCookiePropertyKey: Any] = cookies.properties!
                        //: temPro[HTTPCookiePropertyKey.name] = "uid"
                        temPro[HTTPCookiePropertyKey.name] = (showStorageTitle.replacingOccurrences(of: "architecture", with: "id"))
                        //: temPro[HTTPCookiePropertyKey.domain] = h5Domain
                        temPro[HTTPCookiePropertyKey.domain] = h5Domain
                        //: let temCookie: HTTPCookie = HTTPCookie.init(properties: temPro)!
                        let temCookie: HTTPCookie = .init(properties: temPro)!
                        //: addCookies.append(temCookie)
                        addCookies.append(temCookie)
                    }
                    //: if cookies.name.uppercased() == "PHPSESSID" && !hasSession {
                    if cookies.name.uppercased() == (String(user_presentationMessage)), !hasSession {
                        //: var temPro: Dictionary<HTTPCookiePropertyKey, Any> = cookies.properties!
                        var temPro: [HTTPCookiePropertyKey: Any] = cookies.properties!
                        //: temPro[HTTPCookiePropertyKey.name] = "PHPSESSID"
                        temPro[HTTPCookiePropertyKey.name] = (String(user_presentationMessage))
                        //: temPro[HTTPCookiePropertyKey.domain] = h5Domain
                        temPro[HTTPCookiePropertyKey.domain] = h5Domain
                        //: let temCookie: HTTPCookie = HTTPCookie.init(properties: temPro)!
                        let temCookie: HTTPCookie = .init(properties: temPro)!
                        //: addCookies.append(temCookie)
                        addCookies.append(temCookie)
                    }
                }
                //: for cookies in addCookies {
                for cookies in addCookies {
                    //: cookieStore.setCookie(cookies)
                    cookieStore.setCookie(cookies)
                }
                //: self.requestReady = true
                self.requestReady = true
                //: self.func__beginStartRequest()
                self.fireReply()
            }
        }
    }

    //: func func__addcustomUserAgent() {
    func eventOutputComposeAddMemory() {
        //: self.webView.evaluateJavaScript("navigator.userAgent") { [weak self] (result, err) in
        self.webView.evaluateJavaScript((main_everythingDepthName.replacingOccurrences(of: "volume", with: "ig") + "tor.u" + String(mainYesterdayTempKey.prefix(8)))) { [weak self] result, err in
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: printLog(message: "navigator.userAgent")
            printLog(message: (main_everythingDepthName.replacingOccurrences(of: "volume", with: "ig") + "tor.u" + String(mainYesterdayTempKey.prefix(8))))
            //: var userAgent = result as? String ?? ""
            var userAgent = result as? String ?? ""
            //: if !userAgent.contains("xunwu_iphone") {
            if !userAgent.contains((String(showCountoId) + showJustCapableId.replacingOccurrences(of: "light", with: "e"))) {
                //: let str = getUserAgentString()
                let str = greenPolo()
                //: userAgent.append(" \(str)")
                userAgent.append(" \(str)")
                //: DispatchQueue.main.async {
                DispatchQueue.main.async {
                    //: self.webView.customUserAgent = userAgent
                    self.webView.customUserAgent = userAgent
                }
            }
            //: UIDevice.getDeviceDeviceIdentifier { deviceId in
            UIDevice.pastAspect { deviceId in
                //: guard deviceId.count > 0 else { return }
                guard deviceId.count > 0 else { return }
                //: let str = " xunwu_device_id/\(deviceId)"
                let str = (String(kThumbString.suffix(5)) + String(const_choiceData)) + "\(deviceId)"
                //: if !userAgent.contains(str) {
                if !userAgent.contains(str) {
                    //: userAgent.append(" \(str)")
                    userAgent.append(" \(str)")
                    //: DispatchQueue.main.async {
                    DispatchQueue.main.async {
                        //: self.webView.customUserAgent = userAgent
                        self.webView.customUserAgent = userAgent
                    }
                }
            }
        }
    }

    //: func getUserAgentString() -> String {
    func greenPolo() -> String {
        //: return "xunwu_iphone xunwu_version/\(AppNetVersion) xunwu_package/\(PackageID) xunwu_bundleId/\(AppBundle)  webkit/1"
        return (constPublisherPath.replacingOccurrences(of: "gem", with: "un") + "_ipho" + String(noti_blockData.prefix(5)) + "nwu_versi" + String(main_installFullMsg.suffix(3))) + "\(notiOriginalKey)" + (String(k_minuteMessage) + String(user_cutMenuId.suffix(8))) + "\(k_perBounceString)" + (String(mainAboutMessage) + String(app_specifyPath.prefix(5))) + "\(showPartyUntilId)" + (String(mainIncomeTitleFormat) + String(dataMistData.prefix(5)))
    }
}
