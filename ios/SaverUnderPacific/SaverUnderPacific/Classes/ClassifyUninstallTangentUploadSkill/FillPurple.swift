
//: Declare String Begin

/*: "init(coder:) has not been implemented" :*/
fileprivate let constLeaderId:[UInt8] = [0x26,0x21,0x26,0x3b,0x67,0x2c,0x20,0x2b,0x2a,0x3d,0x75,0x66,0x6f,0x27,0x2e,0x3c,0x6f,0x21,0x20,0x3b,0x6f,0x2d,0x2a,0x2a,0x21,0x6f,0x26,0x22,0x3f,0x23,0x2a,0x22,0x2a,0x21,0x3b,0x2a,0x2b]

private func questionTrue(nobody num: UInt8) -> UInt8 {
    return num ^ 79
}

/*: "uid" :*/
fileprivate let constSignId:[UInt8] = [0x44,0x38,0x33]

fileprivate func delayStage(tell num: UInt8) -> UInt8 {
    let value = Int(num) - 207
    if value < 0 {
        return UInt8(value + 256)
    } else {
        return UInt8(value)
    }
}

/*: "headPic" :*/
fileprivate let user_scopeMergeTitle:[Character] = ["h","e","a","d","P","i","c"]

/*: "nickname" :*/
fileprivate let show_clubFormat:[Character] = ["n","i"]
fileprivate let main_burnAcrossRemainTitle:String = "CKNAME"

/*: "birthday" :*/
fileprivate let appHourApplicationMessage:[Character] = ["b","i","r","t","h"]
fileprivate let main_welcomePaintKey:String = "dframe"

/*: "videoUrl" :*/
fileprivate let app_executeHideKey:[Character] = ["v","i","d","e","o","U","r","l"]

/*: "cover" :*/
fileprivate let constLocateMsg:String = "covevid"

/*: "location" :*/
fileprivate let noti_cameraShrimpScanFragmentId:[Character] = ["l","o","c","a","t","i"]
fileprivate let dataFinanceData:String = "oinfo"

/*: "cmd" :*/
fileprivate let kInsideString:[UInt8] = [0x64,0x6d,0x63]

/*: "refuseCall" :*/
fileprivate let app_eagerMessage:[Character] = ["r","e","f","u","s","e","C"]
fileprivate let dataUnknownAttributeName:[Character] = ["a","l","l"]

/*: "data" :*/
fileprivate let notiExamineMicString:[UInt8] = [0x61,0x74,0x61,0x64]

/*: "Call interruption" :*/
fileprivate let k_impactNobodyStr:String = "current crucial bold slowCall "
fileprivate let notiSendData:String = "PTION"

/*: "stopCall" :*/
fileprivate let app_buildTabString:String = "rover bassstopCall"

/*: "logId" :*/
fileprivate let mainHundredMsg:[UInt8] = [0x64,0x49,0x67,0x6f,0x6c]

/*: "cancelCall" :*/
fileprivate let constIgnoreStr:[Character] = ["c","a","n","c","e","l"]
fileprivate let noti_tillHimAccessKey:String = "accept hour take weekly sophisticatedCall"

/*: "No answer" :*/
fileprivate let userByPiName:[Character] = ["N","o"," ","a","n","s"]
fileprivate let userDonName:String = "wecomplete"

/*: "duration" :*/
fileprivate let showBroadName:[Character] = ["d","u","r","a","t","i","o","n"]

/*: "She has hung up" :*/
fileprivate let constOwnerMechanismUrl:String = "She hcomponent success message"
fileprivate let constPushName:String = "load medium law healthy insertung up"

/*: "cancelSex" :*/
fileprivate let data_crucialMsg:String = "CANCE"

/*: "He has hung up" :*/
fileprivate let main_operationName:String = "migration dense gift slideHe ha"
fileprivate let kHistoryId:String = "s hunsort coffee law white"

/*: "endType" :*/
fileprivate let k_denseMsg:[Character] = ["e","n","d","T","y","p","e"]

/*: "stopSex" :*/
fileprivate let appFractionTitle:String = "stopSexnone direction sly feed"

/*: "Insufficient gold coins, call has ended" :*/
fileprivate let k_graphicPath:[UInt8] = [0x9b,0xbc,0xa1,0xa7,0xb4,0xb4,0xbb,0xb1,0xbb,0xb7,0xbc,0xa6,0xf2,0xb5,0xbd,0xbe,0xb6,0xf2,0xb1,0xbd,0xbb,0xbc,0xa1,0xfe,0xf2,0xb1,0xb3,0xbe,0xbe,0xf2,0xba,0xb3,0xa1,0xf2,0xb7,0xbc,0xb6,0xb7,0xb6]

private func fullShAccuracy(container num: UInt8) -> UInt8 {
    return num ^ 210
}

/*: "The other hung up the phone" :*/
fileprivate let appTurnFormat:String = "The otsignature bot aspect"
fileprivate let noti_listenerUrl:String = "hung transfer yield"
fileprivate let user_reflectKey:String = "e phonrender upon spine interrupt"
fileprivate let dataQueenTempString:String = "program"

/*: "type" :*/
fileprivate let constHarassmentPromptUrl:[Character] = ["t","y","p","e"]

/*: "jumpUrl" :*/
fileprivate let show_sceneName:String = "she commit nearby assist listenjumpUrl"

/*: "Cancel" :*/
fileprivate let const_wordCollectMsg:String = "Cancelmm priority way"

/*: "Get gold coins" :*/
fileprivate let dataGapTitle:String = "custom trigger decide true springGet gol"
fileprivate let app_redGraphEquallyFormat:[Character] = ["d"," ","c","o","i","n","s"]

/*: "refuseSex" :*/
fileprivate let const_exactlyKey:[Character] = ["r","e","f","u","s","e","S","e","x"]

/*: "onAcceptCall" :*/
fileprivate let mainThinSpellData:String = "drawing weight educateonAcc"

/*: "acceptCall" :*/
fileprivate let constRedKey:[Character] = ["a","c","c","e","p","t","C","a","l"]
fileprivate let noti_methodId:[Character] = ["l"]

/*: "Calls are cancelled" :*/
fileprivate let k_crucialKey:String = "Callshero density package m"
fileprivate let mainExceptTitle:String = " cancepriority export"
fileprivate let mainDynamicsId:String = "ouroured"

/*: "Continue matching with other users？" :*/
fileprivate let app_detailedValue:[UInt8] = [0xf9,0x25,0x24,0x2a,0x1f,0x24,0x2b,0x1b,0xd6,0x23,0x17,0x2a,0x19,0x1e,0x1f,0x24,0x1d,0xd6,0x2d,0x1f,0x2a,0x1e,0xd6,0x25,0x2a,0x1e,0x1b,0x28,0xd6,0x2b,0x29,0x1b,0x28,0x29,0xa5,0x72,0x55]

fileprivate func ratingBetween(global num: UInt8) -> UInt8 {
    let value = Int(num) - 182
    if value < 0 {
        return UInt8(value + 256)
    } else {
        return UInt8(value)
    }
}

/*: "Yes" :*/
fileprivate let const_portValue:[Character] = ["Y","e","s"]

/*: "The call has ended" :*/
fileprivate let data_joinFormat:[Character] = ["T","h","e"," ","c","a","l","l"," ","h","a","s"," ","e","n","d"]
fileprivate let app_lightTitle:[Character] = ["e","d"]

//: Declare String End

// __DEBUG__
// __CLOSE_PRINT__
//
//  FillPurple.swift
//  AbroadTalking
//
//  Created by young on 2022/11/8.
//

//: import CallKit.CXCallObserver
import CallKit.CXCallObserver
//: import UIKit
import UIKit

// 通话监听(必须为全局属性，否则无法接听)
//: let videoCallHelper = CXCallObserver()
let noti_saltBehaviorBuildMessage = CXCallObserver()
//: class TalkingVideoChatViewController: TalkingBaseViewController {
class FillPurple: EasyChainSensor {
    //: var chatModel: TalkingVideoChatModel?
    var chatModel: IconModel? // 视频通话模型
    //: private var player: TalkingCallBGMPlayer?
    private var player: VerticalName? // 拨打中背景音乐
    //: private var timeOutTimer: Timer?
    private var timeOutTimer: Timer? // 未接听超时定时器
    //: private var expiredDate = Date()
    private var expiredDate = Date() // 失效时间
    //: private var miniView: TalkingVideoMiniView?
    private var miniView: AwayBuilder? // 视频通话小窗口
    //: private var isFromMiniView = false
    private var isFromMiniView = false // 是否点击小窗口进入
    //: private var reportView: TalkingVideoFloatScreenView?
    private var reportView: FlushView? // 视频举报弹窗

    //: override init(nibName nibNameOrNil: String?, bundle nibBundleOrNil: Bundle?) {
    override init(nibName nibNameOrNil: String?, bundle nibBundleOrNil: Bundle?) {
        //: super.init(nibName: nil, bundle: nil)
        super.init(nibName: nil, bundle: nil)
        // 提前绑定delegate
        //: TalkingSocketManager.shared.callerDelegate = self
        MaterialCurrency.shared.callerDelegate = self
        //: TalkingSocketManager.shared.calledPartyDelegate = self
        MaterialCurrency.shared.calledPartyDelegate = self
        //: TalkingSocketManager.shared.errorDelegate = self
        MaterialCurrency.shared.errorDelegate = self
    }

    //: required init?(coder: NSCoder) {
    required init?(coder: NSCoder) {
        //: fatalError("init(coder:) has not been implemented")
        fatalError(String(bytes: constLeaderId.map{questionTrue(nobody: $0)}, encoding: .utf8)!)
    }

    //: override func viewDidAppear(_ animated: Bool) {
    override func viewDidAppear(_ animated: Bool) {
        //: super.viewDidAppear(animated)
        super.viewDidAppear(animated)
        //: let nav = self.navigationController as! TalkingNavigationController
        let nav = self.navigationController as! FloorEvolution
        //: nav.protectedPushVc = nil
        nav.protectedPushVc = nil

        //: UIApplication.shared.isIdleTimerDisabled = true
        UIApplication.shared.isIdleTimerDisabled = true // 设备不会进入休眠状态
        //: self.startBGM()
        self.pressAir()
        //: if self.chatModel?.videoStage == .Recieved {
        if self.chatModel?.videoStage == .Recieved { // 接收方验证权限
            //: checkVideoAuth()
            agreePolicy()
            //: } else if self.chatModel?.videoStage == .Calling {
        } else if self.chatModel?.videoStage == .Calling { // 直接创建通话中的界面（包括重连）
            // 通话小窗口进入无需重复创建
            //: guard isFromMiniView == false else { return }
            guard isFromMiniView == false else { return }
            //: createCallingRoom(logId: self.chatModel?.logId)
            contentCircle(logId: self.chatModel?.logId)
        }
    }

    //: override func viewWillAppear(_ animated: Bool) {
    override func viewWillAppear(_ animated: Bool) {
        //: super.viewWillAppear(animated)
        super.viewWillAppear(animated)
        //: if self.miniView != nil {
        if self.miniView != nil { // 移除小窗口
            //: self.miniView?.removeFromSuperview()
            self.miniView?.removeFromSuperview()
            //: self.miniView = nil
            self.miniView = nil
            //: TalkingSocketManager.shared.pacificVC = nil
            MaterialCurrency.shared.pacificVC = nil
        }
        //: interactionView.initCachEffectData()
        interactionView.playerInstall()
    }

    //: override func viewDidLoad() {
    override func viewDidLoad() {
        //: super.viewDidLoad()
        super.viewDidLoad()
        //: self.hideNavi = true
        self.hideNavi = true
        //: setupSubviews()
        splitOn()
        //: setupSubViewsConstraint()
        spineConcrete()
        //: initNotificationObserver()
        lastRow()
        //: req_callGetUserInfo()
        someRender()
        //: timeoutRules()
        specifyIn()
        //: setUpBGMPlayer()
        limitedFrom()
        //: if self.chatModel?.videoStage != .Recieved {
        if self.chatModel?.videoStage != .Recieved {
            //: self.startPreview()
            self.translateFuture()
        }
    }

    //: override func loadView() {
    override func loadView() {
        //: if #available(iOS 13.2, *) {
        if #available(iOS 13.2, *) {
            //: if MarginExamineer.share.appConfigMode.disableShootScreen {
            if MarginExamineer.share.appConfigMode.disableShootScreen {
                //: super.loadView()
                super.loadView()
                //: } else {
            } else {
                //: let bgView = MAScreenShieldView(frame: CGRect(x: 0, y: 0, width: ScreenWidth, height: ScreenHeight))
                let bgView = OccasionAll(frame: CGRect(x: 0, y: 0, width: show_errorChangeFormat, height: appLoadMessage))
                //: self.view = bgView
                self.view = bgView
            }
            //: } else {
        } else {
            //: super.loadView()
            super.loadView()
        }
    }

    //: deinit {
    deinit {
        //: UIApplication.shared.isIdleTimerDisabled = false
        UIApplication.shared.isIdleTimerDisabled = false
        //: NotificationCenter.default.removeObserver(self)
        NotificationCenter.default.removeObserver(self)
        //: TalkingSocketManager.shared.pacific = false
        MaterialCurrency.shared.pacific = false
        //: TalkingSocketManager.shared.isCalling = false
        MaterialCurrency.shared.isCalling = false
        //: self.stopVCAllActivties()
        self.totalerest()
    }

    // MARK: - Lazy Load

    /// 视频通话背景
    //: private lazy var bgVideoView: TalkingBGVideoView = {
    private lazy var bgVideoView: ExtendHead = {
        //: let v = TalkingBGVideoView()
        let v = ExtendHead()
        //: v.showCoverView(show: self.chatModel?.videoStage != .Calling)
        v.agileOf(show: self.chatModel?.videoStage != .Calling)
        //: return v
        return v
        //: }()
    }()

    /// 视频通话操作按钮
    //: private lazy var interactionView: Talking1v1VideoInteractionView = {
    private lazy var interactionView: TaskAsset = {
        //: let v = Talking1v1VideoInteractionView(frame: .zero, chatModel: self.chatModel)
        let v = TaskAsset(frame: .zero, chatModel: self.chatModel)
        //: v.delegate = self
        v.delegate = self
        //: return v
        return v
        //: }()
    }()

    // 视频通话小窗口
    //: private lazy var windowView: TalkingVideoWindowView = {
    private lazy var windowView: SymbolView = {
        //: let v = TalkingVideoWindowView()
        let v = SymbolView()
        //: var x = ScreenWidth-15-actualWidth(w: 125)
        var x = show_errorChangeFormat - 15 - actualWidth(w: 125)
        //: if LanguageManager.shared.direction == .rightToLeft {
        if InputCloseInfo.shared.direction == .rightToLeft {
            //: x = 15
            x = 15
        }
        //: v.frame = CGRect(x: x, y: StatusBarHeight+actualHeight(h: 25)+26+35, width: actualWidth(w: 125), height: actualHeight(h: 170))
        v.frame = CGRect(x: x, y: const_musicMessage + actualHeight(h: 25) + 26 + 35, width: actualWidth(w: 125), height: actualHeight(h: 170))
        //: v.isHidden = self.chatModel?.videoStage != .Calling
        v.isHidden = self.chatModel?.videoStage != .Calling
        //: v.block = { [weak self] in
        v.block = { [weak self] in
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: self.changeRenderView()
            self.decisionBy()
        }
        //: return v
        return v
        //: }()
    }()

    //: private lazy var videoManager: TalkingVideoChatManager = {
    private lazy var videoManager: ImplementSpecify = {
        //: let m = TalkingVideoChatManager()
        let m = ImplementSpecify()
        //: m.infoModel = self.chatModel
        m.infoModel = self.chatModel
        // 视图传递给manager
        //: m.bgVideoView = self.bgVideoView.renderView
        m.bgVideoView = self.bgVideoView.renderView
        //: m.bgView = self.bgVideoView.bgView
        m.bgView = self.bgVideoView.bgView
        //: m.windowVideoView = self.windowView.renderView
        m.windowVideoView = self.windowView.renderView
        //: return m
        return m
        //: }()
    }()
}

// MARK: - Request && 刷新UI

//: extension TalkingVideoChatViewController {
extension FillPurple {
    /// 查询接收者信息
    //: func req_callGetUserInfo() {
    func someRender() {
        //: guard let pairUid = chatModel?.pairUid else { return }
        guard let pairUid = chatModel?.pairUid else { return }
        //: TalkingVideoChatManager.req_callGetUserInfo(uid: pairUid) { [weak self] succeed, result, errorModel in
        ImplementSpecify.analyze(uid: pairUid) { [weak self] succeed, result, errorModel in
            //: guard succeed == true else { return }
            guard succeed == true else { return }
            //: guard let self = self else { return }
            guard let self = self else { return }

            //: let json = JSON(result ?? [String: Any]())
            let json = JSON(result ?? [String: Any]())

            //: self.chatModel?.pairUid = json["uid"].stringValue
            self.chatModel?.pairUid = json[String(bytes: constSignId.map{delayStage(tell: $0)}, encoding: .utf8)!].stringValue
            //: self.chatModel?.pairHeadPic = json["headPic"].stringValue
            self.chatModel?.pairHeadPic = json[(String(user_scopeMergeTitle))].stringValue
            //: self.chatModel?.pairNickName = json["nickname"].stringValue
            self.chatModel?.pairNickName = json[(String(show_clubFormat) + main_burnAcrossRemainTitle.lowercased())].stringValue
            //: self.chatModel?.pairBirthday = json["birthday"].stringValue
            self.chatModel?.pairBirthday = json[(String(appHourApplicationMessage) + main_welcomePaintKey.replacingOccurrences(of: "frame", with: "ay"))].stringValue
            //: self.chatModel?.videoUrl = json["videoUrl"].stringValue
            self.chatModel?.videoUrl = json[(String(app_executeHideKey))].stringValue
            //: self.chatModel?.cover = json["cover"].stringValue
            self.chatModel?.cover = json[(constLocateMsg.replacingOccurrences(of: "vid", with: "r"))].stringValue
            //: self.chatModel?.location = json["location"].stringValue
            self.chatModel?.location = json[(String(noti_cameraShrimpScanFragmentId) + dataFinanceData.replacingOccurrences(of: "info", with: "n"))].stringValue

            // 刷新通话视图
            //: self.interactionView.afterGetInfo()
            self.interactionView.numberensity()
            //: self.videoBG_refreshViewWithCameraStatus()
            self.modelSound()
        }
    }

    /// 根据摄像头是否开启状态，刷新视频通话背景
    //: private func videoBG_refreshViewWithCameraStatus() {
    private func modelSound() {
        //: var bgHeadPic: String?
        var bgHeadPic: String?
        //: var bgShowName: String?
        var bgShowName: String?
        //: var bgOpenCamera = VideoCameraStatus.Open
        var bgOpenCamera = LibrarySmartTemp.Open
        //: var windowHeadPic: String?
        var windowHeadPic: String?
        //: var windowOpenCamera = VideoCameraStatus.Open
        var windowOpenCamera = LibrarySmartTemp.Open

        //: if self.chatModel?.renderPostion == .SelfOnWindow {
        if self.chatModel?.renderPostion == .SelfOnWindow { // 自己在窗口
            //: bgHeadPic = self.chatModel?.pairHeadPic
            bgHeadPic = self.chatModel?.pairHeadPic
            //: bgShowName = self.chatModel?.pairNickName
            bgShowName = self.chatModel?.pairNickName
            //: bgOpenCamera = self.videoManager.config.remoteCameraStatus
            bgOpenCamera = self.videoManager.config.remoteCameraStatus
            //: windowHeadPic = self.chatModel?.iHeadPic
            windowHeadPic = self.chatModel?.iHeadPic
            //: windowOpenCamera = self.videoManager.config.openCamera ? .Open:.Closed
            windowOpenCamera = self.videoManager.config.openCamera ? .Open : .Closed

            //: } else {
        } else { // 自己在大背景
            //: bgHeadPic = self.chatModel?.iHeadPic
            bgHeadPic = self.chatModel?.iHeadPic
            //: bgShowName = self.chatModel?.iNickName
            bgShowName = self.chatModel?.iNickName
            //: bgOpenCamera = self.videoManager.config.openCamera ? .Open:.Closed
            bgOpenCamera = self.videoManager.config.openCamera ? .Open : .Closed
            //: windowHeadPic = self.chatModel?.pairHeadPic
            windowHeadPic = self.chatModel?.pairHeadPic
            //: windowOpenCamera = self.videoManager.config.remoteCameraStatus
            windowOpenCamera = self.videoManager.config.remoteCameraStatus
        }

        //: self.bgVideoView.refreshBgView(urlStr: bgHeadPic, showName: bgShowName, isOpenCamera: bgOpenCamera)
        self.bgVideoView.erase(urlStr: bgHeadPic, showName: bgShowName, isOpenCamera: bgOpenCamera)
        //: self.windowView.refreshBgView(urlStr: windowHeadPic, isOpenCamera: windowOpenCamera)
        self.windowView.tagSchedule(urlStr: windowHeadPic, isOpenCamera: windowOpenCamera)
    }
}

// MARK: - Event

//: extension TalkingVideoChatViewController {
extension FillPurple {
    /// 权限校验，播放BGM
    //: private func checkVideoAuth() {
    private func agreePolicy() {
        //: TalkingPermissionTool.checkCameraAndMicrophone { isOpen in
        MultiplePoint.someone { isOpen in
            //: guard isOpen == true else {
            guard isOpen == true else {
                //: self.func__showStatusBarErrorMsg(showMsg: kMessage_permissions_error)
                self.exitMarker(showMsg: k_eventControlUrl)
                //: TalkingSocketManager.shared.sendMessage(info: ["cmd": "refuseCall", "data": ["uid": self.chatModel?.pairUid]])
                MaterialCurrency.shared.warningVersion(info: [String(bytes: kInsideString.reversed(), encoding: .utf8)!: (String(app_eagerMessage) + String(dataUnknownAttributeName)), String(bytes: notiExamineMicString.reversed(), encoding: .utf8)!: [String(bytes: constSignId.map{delayStage(tell: $0)}, encoding: .utf8)!: self.chatModel?.pairUid]])
                //: self.popCurrentViewController()
                self.spacePop()
                //: return
                return
            }

            //: self.startPreview()
            self.translateFuture()
            //: self.stopBGM()
            self.agentFresh()
            //: self.player = TalkingCallBGMPlayer.callBGMPlayer(type: .Response)
            self.player = VerticalName.arrayEach(type: .Response)
            //: self.startBGM()
            self.pressAir()
        }
    }

    /// 预览视频画面
    //: private func startPreview() {
    private func translateFuture() {
        //: self.videoManager.startPreview()
        self.videoManager.fitByCoordinator()
    }

    /// 释放当前页面所有资源
    //: private func stopVCAllActivties() {
    private func totalerest() {
        //: if self.timeOutTimer != nil {
        if self.timeOutTimer != nil {
            //: self.timeOutTimer?.invalidate()
            self.timeOutTimer?.invalidate()
            //: self.timeOutTimer = nil
            self.timeOutTimer = nil
        }

        //: self.stopBGM()
        self.agentFresh()
        //: TalkingVideoDanmuManager.danmu_releaseAllResource()
        GraphKeep.want()
        //: TalkingSocketManager.shared.callerDelegate = nil
        MaterialCurrency.shared.callerDelegate = nil
        //: TalkingSocketManager.shared.calledPartyDelegate = nil
        MaterialCurrency.shared.calledPartyDelegate = nil
        //: TalkingSocketManager.shared.errorDelegate = nil
        MaterialCurrency.shared.errorDelegate = nil
    }

    // 将当前视图从栈中移除
    //: private func popCurrentViewController(animated: Bool = true) {
    private func spacePop(animated: Bool = true) {
        //: self.stopBGM()
        self.agentFresh()

        //: if self.navigationController?.topViewController == self {
        if self.navigationController?.topViewController == self {
            //: self.navigationController?.popViewController(animated: animated)
            self.navigationController?.popViewController(animated: animated)
            //: } else {
        } else {
            //: if var vcArr = self.navigationController?.viewControllers {
            if var vcArr = self.navigationController?.viewControllers {
                //: if let index = vcArr.firstIndex(of: self) {
                if let index = vcArr.firstIndex(of: self) {
                    //: vcArr.remove(at: index)
                    vcArr.remove(at: index)
                    //: self.navigationController?.setViewControllers(vcArr, animated: false)
                    self.navigationController?.setViewControllers(vcArr, animated: false)
                }
            }
        }
    }

    /// 视频通话双方窗口转换
    //: private func changeRenderView() {
    private func decisionBy() {
        //: if self.chatModel?.renderPostion == .selfOnBg {
        if self.chatModel?.renderPostion == .selfOnBg {
            //: self.chatModel?.renderPostion = .SelfOnWindow
            self.chatModel?.renderPostion = .SelfOnWindow
            //: } else if self.chatModel?.renderPostion == .SelfOnWindow {
        } else if self.chatModel?.renderPostion == .SelfOnWindow {
            //: self.chatModel?.renderPostion = .selfOnBg
            self.chatModel?.renderPostion = .selfOnBg
        }

        //: self.videoManager.changeRenderView()
        self.videoManager.eachBy()
        //: self.videoBG_refreshViewWithCameraStatus()
        self.modelSound()
    }

    /// 创建视频通话房间
    //: private func createTalkRoom() {
    private func afterHome() {
        //: self.windowView.isHidden = self.chatModel?.videoStage != .Calling
        self.windowView.isHidden = self.chatModel?.videoStage != .Calling
        //: self.videoManager.createRoomAndStartPublish()
        self.videoManager.receiveCollection()
        //: self.videoBG_refreshViewWithCameraStatus()
        self.modelSound()
        // 展示投诉说明飘屏（男性）
        //: self.show_videoFloatScreenAnimation()
        self.install()
    }

    /// 展示投诉说明飘屏（男性）
    //: private func show_videoFloatScreenAnimation() {
    private func install() {
        //: guard MarginExamineer.share.loginUserMode.sex == Gender.male.rawValue else { return }
        guard MarginExamineer.share.loginUserMode.sex == EnvironmentEmpty.male.rawValue else { return }
        //: guard reportView == nil else { return }
        guard reportView == nil else { return }

        //: reportView = TalkingVideoFloatScreenView()
        reportView = FlushView()
        //: view.addSubview(reportView!)
        view.addSubview(reportView!)
        //: reportView?.show_animation()
        reportView?.tellUniversalUsed()
    }
}

// MARK: - 通知

//: extension TalkingVideoChatViewController {
extension FillPurple {
    /// 初始化通知
    //: private func initNotificationObserver() {
    private func lastRow() {
        // 监听 App 从后台切前台
        //: NotificationCenter.default.addObserver(self,
        NotificationCenter.default.addObserver(self,
                                               //: selector: #selector(appWillEnterForeground(notif:)),
                                               selector: #selector(skirtGold(notif:)),
                                               //: name: UIApplication.willEnterForegroundNotification,
                                               name: UIApplication.willEnterForegroundNotification,
                                               //: object: nil)
                                               object: nil)

        // 监听 对方摄像头是否可用
        //: NotificationCenter.default.addObserver(self,
        NotificationCenter.default.addObserver(self,
                                               //: selector: #selector(updateCameraStatus(notification:)),
                                               selector: #selector(recover(notification:)),
                                               //: name: VIDEO_CHAT_OTHER_CAMERA_AVAILABLE_NOTIFICATION,
                                               name: app_markDirectlyPath,
                                               //: object: nil)
                                               object: nil)

        // socket断网重连监听
        //: NotificationCenter.default.addObserver(self,
        NotificationCenter.default.addObserver(self,
                                               //: selector: #selector(reachabilityChanged(note:)),
                                               selector: #selector(remain(note:)),
                                               //: name: SOCKET_IS_OPEN_NOTIFICATION,
                                               name: userVisitMessage,
                                               //: object: nil)
                                               object: nil)

        //: videoCallHelper.setDelegate(self, queue: DispatchQueue.main)
        noti_saltBehaviorBuildMessage.setDelegate(self, queue: DispatchQueue.main)
    }

    /// app进入前台
    //: @objc private func appWillEnterForeground(notif: Notification) {
    @objc private func skirtGold(notif: Notification) {
        //: guard self.chatModel?.videoStage != .Calling else { return }
        guard self.chatModel?.videoStage != .Calling else { return }
        //: let date = Date()
        let date = Date()
        //: let interval = date.timeIntervalSince(self.expiredDate)
        let interval = date.timeIntervalSince(self.expiredDate)
        //: if interval >= 0 {
        if interval >= 0 {
            //: popCurrentViewController()
            spacePop()
        }
    }

    /// 断网重连socket
    //: @objc private func reachabilityChanged(note: Notification) {
    @objc private func remain(note: Notification) {
        //: guard TalkingSocketManager.shared.needReconnect == true else { return }
        guard MaterialCurrency.shared.needReconnect == true else { return }
        //: TalkingSocketManager.shared.callerDelegate = self
        MaterialCurrency.shared.callerDelegate = self
        //: TalkingSocketManager.shared.calledPartyDelegate = self
        MaterialCurrency.shared.calledPartyDelegate = self
        //: TalkingSocketManager.shared.errorDelegate = self
        MaterialCurrency.shared.errorDelegate = self
        //: self.videoManager.restartTalkWhenDisconnectedAndNetAvailable()
        self.videoManager.bassAppropriate()
    }

    /// 根据对方摄像头状态更新UI
    /// - Parameter notification: 通知
    //: @objc private func updateCameraStatus(notification: NSNotification) {
    @objc private func recover(notification: NSNotification) {
        //: self.videoBG_refreshViewWithCameraStatus()
        self.modelSound()
    }
}

// MARK: - CXCallObserverDelegate【通话监听】

//: extension TalkingVideoChatViewController: CXCallObserverDelegate {
extension FillPurple: CXCallObserverDelegate {
    //: func callObserver(_ callObserver: CXCallObserver, callChanged call: CXCall) {
    func callObserver(_ callObserver: CXCallObserver, callChanged call: CXCall) {
        //: if call.hasConnected == true {
        if call.hasConnected == true { // 通话中
            //: self.func__showStatusBarErrorMsg(showMsg: "Call interruption")
            self.exitMarker(showMsg: (String(k_impactNobodyStr.suffix(5)) + "interru" + notiSendData.lowercased()))
            //: switch(self.chatModel?.videoStage) {
            switch self.chatModel?.videoStage {
            //: case .Calling:
            case .Calling:
                //: TalkingSocketManager.shared.sendMessage(info: ["cmd": "stopCall", "data": ["logId": self.chatModel?.logId]])
                MaterialCurrency.shared.warningVersion(info: [String(bytes: kInsideString.reversed(), encoding: .utf8)!: (String(app_buildTabString.suffix(8))), String(bytes: notiExamineMicString.reversed(), encoding: .utf8)!: [String(bytes: mainHundredMsg.reversed(), encoding: .utf8)!: self.chatModel?.logId]])
            //: case .Waiting:
            case .Waiting:
                //: TalkingSocketManager.shared.sendMessage(info: ["cmd": "cancelCall", "data": ["logId": self.chatModel?.logId]])
                MaterialCurrency.shared.warningVersion(info: [String(bytes: kInsideString.reversed(), encoding: .utf8)!: (String(constIgnoreStr) + String(noti_tillHimAccessKey.suffix(4))), String(bytes: notiExamineMicString.reversed(), encoding: .utf8)!: [String(bytes: mainHundredMsg.reversed(), encoding: .utf8)!: self.chatModel?.logId]])
            //: case .Recieved:
            case .Recieved:
                //: TalkingSocketManager.shared.sendMessage(info: ["cmd": "refuseCall", "data": ["uid": self.chatModel?.pairUid]])
                MaterialCurrency.shared.warningVersion(info: [String(bytes: kInsideString.reversed(), encoding: .utf8)!: (String(app_eagerMessage) + String(dataUnknownAttributeName)), String(bytes: notiExamineMicString.reversed(), encoding: .utf8)!: [String(bytes: constSignId.map{delayStage(tell: $0)}, encoding: .utf8)!: self.chatModel?.pairUid]])
            //: case .none:
            case .none:
                //: break
                break
            }

            //: if miniView != nil {
            if miniView != nil {
                //: self.miniView?.dismissWhenCallEnd()
                self.miniView?.pause()
                //: TalkingSocketManager.shared.pacificVC = nil
                MaterialCurrency.shared.pacificVC = nil
            }
            //: popCurrentViewController()
            spacePop()
        }
    }
}

// MARK: - 通话音乐

//: extension TalkingVideoChatViewController {
extension FillPurple {
    /// 初始化视频通话背景音乐
    //: private func setUpBGMPlayer() {
    private func limitedFrom() {
        //: guard self.chatModel?.videoStage != .Calling else { return }
        guard self.chatModel?.videoStage != .Calling else { return }

        //: let type = self.chatModel!.isCaller == true ? CallBGMPlayerType.Call : CallBGMPlayerType.Response
        let type = self.chatModel!.isCaller == true ? RoundingDepth.Call : RoundingDepth.Response
        //: self.player = TalkingCallBGMPlayer.callBGMPlayer(type: type)
        self.player = VerticalName.arrayEach(type: type)
    }

    /// 开始播放背景音乐
    //: private func startBGM() {
    private func pressAir() {
        //: guard self.chatModel?.videoStage != .Calling else { return }
        guard self.chatModel?.videoStage != .Calling else { return }
        //: guard self.chatModel?.resumeTalk != true else { return }
        guard self.chatModel?.resumeTalk != true else { return }

        //: self.player?.play()
        self.player?.same()
    }

    /// 停止播放背景音乐
    //: private func stopBGM() {
    private func agentFresh() {
        //: guard self.player != nil else { return }
        guard self.player != nil else { return }
        //: self.player?.stop()
        self.player?.bean()
        //: self.player = nil
        self.player = nil
    }
}

// MARK: - 定时器

//: extension TalkingVideoChatViewController {
extension FillPurple {
    /// 超时定时器
    //: private func timeoutRules() {
    private func specifyIn() {
        //: guard self.chatModel?.videoStage != .Calling else { return }
        guard self.chatModel?.videoStage != .Calling else { return }

        //: let timeout = self.chatModel?.isCaller == true ? VIDEO_1v1_CALL_TIMEOUT : self.chatModel?.remainAnswerCallTime()
        let timeout = self.chatModel?.isCaller == true ? showDelayFlexibleTitle : self.chatModel?.mp()
        //: if timeout! <= 0 {
        if timeout! <= 0 {
            //: buildTalkTimeOutAction()
            nearMinimum()
        }
        //: self.timeOutTimer = Timer.scheduledTimer(withTimeInterval: timeout!, block: { [weak self] timer in
        self.timeOutTimer = Timer.scheduledTimer(withTimeInterval: timeout!, block: { [weak self] timer in
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: self.buildTalkTimeOutAction()
            self.nearMinimum()
            //: }, repeats: false)
        }, repeats: false)

        //: self.expiredDate = Date().addingTimeInterval(60)
        self.expiredDate = Date().addingTimeInterval(60)
    }

    /// 倒计时超时逻辑处理
    //: private func buildTalkTimeOutAction() {
    private func nearMinimum() {
        //: guard !TalkingSocketManager.shared.pacific else { return }
        guard !MaterialCurrency.shared.pacific else { return }

        //: var socketDict: [String: Any]
        var socketDict: [String: Any]
        //: if self.chatModel?.isCaller == true {
        if self.chatModel?.isCaller == true { // 拨打方
            //: self.func__showStatusBarErrorMsg(showMsg: "No answer".localized)
            self.exitMarker(showMsg: (String(userByPiName) + userDonName.replacingOccurrences(of: "complete", with: "r")).localized)
            //: socketDict = ["cmd": "cancelCall", "data": ["logId": self.chatModel?.logId]]
            socketDict = [String(bytes: kInsideString.reversed(), encoding: .utf8)!: (String(constIgnoreStr) + String(noti_tillHimAccessKey.suffix(4))), String(bytes: notiExamineMicString.reversed(), encoding: .utf8)!: [String(bytes: mainHundredMsg.reversed(), encoding: .utf8)!: self.chatModel?.logId]]
            //: } else {
        } else { // 接收方
            //: socketDict = ["cmd": "refuseCall", "data": ["uid": self.chatModel?.pairUid]]
            socketDict = [String(bytes: kInsideString.reversed(), encoding: .utf8)!: (String(app_eagerMessage) + String(dataUnknownAttributeName)), String(bytes: notiExamineMicString.reversed(), encoding: .utf8)!: [String(bytes: constSignId.map{delayStage(tell: $0)}, encoding: .utf8)!: self.chatModel?.pairUid]]
        }
        //: TalkingSocketManager.shared.sendMessage(info: socketDict)
        MaterialCurrency.shared.warningVersion(info: socketDict)
        //: popCurrentViewController()
        spacePop()
    }
}

// MARK: - resume talk【恢复通话】

//: extension TalkingVideoChatViewController {
extension FillPurple {
    /// 恢复视频通话，更新chatModel
    /// - Parameter resumeData: 数据
    //: func updateInfoViaResumeTalkData(resumeData: [String: Any]) {
    func reOfDistance(resumeData: [String: Any]) {
        //: let json = JSON(resumeData)
        let json = JSON(resumeData)
        //: let logId = json["logId"].intValue
        let logId = json[String(bytes: mainHundredMsg.reversed(), encoding: .utf8)!].intValue
        //: let duration = json["duration"].intValue
        let duration = json[(String(showBroadName))].intValue
        //: let interval = Date().timeIntervalSince1970
        let interval = Date().timeIntervalSince1970
        //: let startTalkTime = floor(interval) - Double(duration)
        let startTalkTime = floor(interval) - Double(duration)
        //: self.chatModel?.startTime = startTalkTime
        self.chatModel?.startTime = startTalkTime
        //: self.interactionView.updateStartTime(startTime: startTalkTime)
        self.interactionView.someCrush(startTime: startTalkTime)

        //: if self.chatModel?.videoStage != .Calling {
        if self.chatModel?.videoStage != .Calling {
            //: self.chatModel?.logId = logId
            self.chatModel?.logId = logId
            //: self.chatModel?.videoStage = .Calling
            self.chatModel?.videoStage = .Calling
            //: self.interactionView.showViewsViaStage()
            self.interactionView.obtain()
            //: createTalkRoom()
            afterHome()
            //: self.stopBGM()
            self.agentFresh()
        }
    }

    /// 创建通话中房间
    /// - Parameter logId: 通话记录Id
    //: private func createCallingRoom(logId: Int?) {
    private func contentCircle(logId: Int?) {
        //: guard let logId = logId else { return }
        guard let logId = logId else { return }

        //: self.chatModel?.logId = logId
        self.chatModel?.logId = logId
        //: self.chatModel?.videoStage = .Calling
        self.chatModel?.videoStage = .Calling
        //: self.chatModel?.renderPostion = .SelfOnWindow
        self.chatModel?.renderPostion = .SelfOnWindow

        //: self.videoManager.changeRenderView()
        self.videoManager.eachBy()
        //: self.bgVideoView.showCoverView(show: false)
        self.bgVideoView.agileOf(show: false)
        //: self.stopBGM()
        self.agentFresh()
        //: self.interactionView.showViewsViaStage()
        self.interactionView.obtain()
        //: self.createTalkRoom()
        self.afterHome()
    }
}

// MARK: - ThemeMenu【拨打方】

//: extension TalkingVideoChatViewController: SocketManagerVideoChatCallerDelegate {
extension FillPurple: ThemeMenu {
    /// 拨打方拒绝了通话，接收方收到该指令（未通话）
    //: func socket_videoChat_cancelCall(data: [String: Any]) {
    func verticalAgile(data: [String: Any]) {
        //: if self.miniView == nil {
        if self.miniView == nil {
            //: let json = JSON(data)
            let json = JSON(data)
            //: var typeStr = "She has hung up".localized
            var typeStr = (String(constOwnerMechanismUrl.prefix(5)) + "as h" + String(constPushName.suffix(6))).localized
            //: if json["cancelSex"].stringValue == Gender.male.rawValue {
            if json[(data_crucialMsg.lowercased() + "lSex")].stringValue == EnvironmentEmpty.male.rawValue {
                //: typeStr = "He has hung up".localized
                typeStr = (String(main_operationName.suffix(5)) + String(kHistoryId.prefix(5)) + "g up").localized
            }
            //: self.func__showStatusBarErrorMsg(showMsg: typeStr)
            self.exitMarker(showMsg: typeStr)
            //: } else {
        } else {
            //: self.miniView?.dismissWhenCallEnd()
            self.miniView?.pause()
            //: TalkingSocketManager.shared.pacificVC = nil
            MaterialCurrency.shared.pacificVC = nil
        }

        //: popCurrentViewController()
        spacePop()
    }

    /// 对方接受了来电，建立视频通话
    //: func socket_videoChat_acceptCall(data: [String: Any]) {
    func exLayer(data: [String: Any]) {
        //: let json = JSON(data)
        let json = JSON(data)
        //: createCallingRoom(logId: json["logId"].intValue)
        contentCircle(logId: json[String(bytes: mainHundredMsg.reversed(), encoding: .utf8)!].intValue)
    }

    /// 对方结束了通话（通话中）
    //: func socket_videoChat_stopCall(data: [String: Any]) {
    func beforeReliability(data: [String: Any]) {
        //: TalkingAlertShow.hideAlert()
        InputMight.runningProjection()
        //: let json = JSON(data)
        let json = JSON(data)

        //: let endType = json["endType"].intValue
        let endType = json[(String(k_denseMsg))].intValue
        //: var typeStr = "She has hung up".localized
        var typeStr = (String(constOwnerMechanismUrl.prefix(5)) + "as h" + String(constPushName.suffix(6))).localized
        //: if json["stopSex"].stringValue == Gender.male.rawValue {
        if json[(String(appFractionTitle.prefix(7)))].stringValue == EnvironmentEmpty.male.rawValue {
            //: typeStr = "He has hung up".localized
            typeStr = (String(main_operationName.suffix(5)) + String(kHistoryId.prefix(5)) + "g up").localized
        }

        //: if endType == 1 {
        if endType == 1 { // 付费方余额不足
            //: if MarginExamineer.share.appStatus == AppSkinStatus.normal.rawValue {
            if MarginExamineer.share.appStatus == AwayPush.normal.rawValue {
                //: typeStr = "Insufficient gold coins, call has ended".localized
                typeStr = String(bytes: k_graphicPath.map{fullShAccuracy(container: $0)}, encoding: .utf8)!.localized
                //: } else {
            } else {
                //: typeStr = "The other hung up the phone".localized
                typeStr = (String(appTurnFormat.prefix(6)) + "her " + String(noti_listenerUrl.prefix(5)) + "up th" + String(user_reflectKey.prefix(6)) + dataQueenTempString.replacingOccurrences(of: "program", with: "e")).localized
            }
        }
        //: if self.miniView == nil {
        if self.miniView == nil {
            //: self.func__showStatusBarErrorMsg(showMsg: typeStr)
            self.exitMarker(showMsg: typeStr)
            //: } else {
        } else {
            //: self.miniView?.dismissWhenCallEnd()
            self.miniView?.pause()
            //: TalkingSocketManager.shared.pacificVC = nil
            MaterialCurrency.shared.pacificVC = nil
        }

        //: self.interactionView.func_giftViewDismiss()
        self.interactionView.aboveActionTransform()
        //: popCurrentViewController()
        spacePop()
    }

    /// 余额不足提醒
    //: func socket_videoChat_onMissCoin(msg: String, data: [String: Any]) {
    func distinctionIn(msg: String, data: [String: Any]) {
        //: guard MarginExamineer.share.appStatus == AppSkinStatus.normal.rawValue else { return }
        guard MarginExamineer.share.appStatus == AwayPush.normal.rawValue else { return }

        //: let index = data["type"] as? String ?? ""
        let index = data[(String(constHarassmentPromptUrl))] as? String ?? ""
        //: if index == "2" {
        if index == "2" {
//            if self.interactionView.isShowRecharge {
//                return
//            }
            //: guard let jumpUrl = data["jumpUrl"] as? String  else { return }
            guard let jumpUrl = data[(String(show_sceneName.suffix(7)))] as? String else { return }
            //: let httpsUrl = MarginExamineer.share.appConfigMode.urlH5Domain+jumpUrl
            let httpsUrl = MarginExamineer.share.appConfigMode.urlH5Domain + jumpUrl
            //: var config = TalkingWebConfig.init()
            var config = ProvisionObserve()
            //: config.isDiscountsView = true
            config.isDiscountsView = true
            //: OriginMediumWill.share.func__pushToWebVC(urlStr: httpsUrl, webConfig: config)
            OriginMediumWill.share.anyCalled(urlStr: httpsUrl, webConfig: config)
            //: } else {
        } else {
            //: TalkingAlertShow.alert(title: nil,
            InputMight.untilPhone(title: nil,
                                        //: message: msg,
                                        message: msg,
                                        //: leftBtnTitle: "Cancel".localized,
                                        leftBtnTitle: (String(const_wordCollectMsg.prefix(6))).localized,
                                        //: rightBtnTitle: "Get gold coins".localized) {
                                        rightBtnTitle: (String(dataGapTitle.suffix(7)) + String(app_redGraphEquallyFormat)).localized)
            {
                //: TalkingAlertShow.hideAlert()
                InputMight.runningProjection()
                //: } rightBlock: {
            } rightBlock: {
                //: OriginMediumWill.share.func__jumpToWebRecharge(clickEvent: clickLess1minButton, sufficient: false)
                OriginMediumWill.share.resumeUniversal(clickEvent: data_consumptionStr, sufficient: false)
            }
        }
    }

    /// 收到礼物消息展示
    //: @objc func receiveGiftPlay(giftArr: Array<Any>) {
    @objc func obtainAgainst(giftArr: [Any]) {
        //: interactionView.addGiftEffectModelArr(giftArr: giftArr)
        interactionView.need(giftArr: giftArr)
    }
}

// MARK: - MultipleMarkAmong【接收方】

//: extension TalkingVideoChatViewController: SocketManagerVideoChatCalledPartyDelegate {
extension FillPurple: MultipleMarkAmong {
    /// 接收方拒绝了通话，拨打方收到该指令（未通话）
    /// - Parameter data: 数据
    //: func socket_videoChat_refuseCall(data: [String: Any]) {
    func buttTar(data: [String: Any]) {
        //: if self.miniView == nil {
        if self.miniView == nil {
            //: let json = JSON(data)
            let json = JSON(data)
            //: var typeStr = "She has hung up".localized
            var typeStr = (String(constOwnerMechanismUrl.prefix(5)) + "as h" + String(constPushName.suffix(6))).localized
            //: if json["refuseSex"].stringValue == Gender.male.rawValue {
            if json[(String(const_exactlyKey))].stringValue == EnvironmentEmpty.male.rawValue {
                //: typeStr = "He has hung up".localized
                typeStr = (String(main_operationName.suffix(5)) + String(kHistoryId.prefix(5)) + "g up").localized
            }
            //: self.func__showStatusBarErrorMsg(showMsg: typeStr)
            self.exitMarker(showMsg: typeStr)
            //: } else {
        } else {
            //: self.miniView?.dismissWhenCallEnd()
            self.miniView?.pause()
            //: TalkingSocketManager.shared.pacificVC = nil
            MaterialCurrency.shared.pacificVC = nil
        }

        //: popCurrentViewController()
        spacePop()
    }

    /// 接收方接受了通话，改变状态为通话中，acceptCall回执
    //: func socket_videoChat_onAcceptCall(data: [String: Any]) {
    func fragmentWithinCurrency(data: [String: Any]) {
        //: let json = JSON(data)
        let json = JSON(data)
        //: createCallingRoom(logId: json["logId"].intValue)
        contentCircle(logId: json[String(bytes: mainHundredMsg.reversed(), encoding: .utf8)!].intValue)
    }
}

// MARK: - GlobalSustainable【异常处理】

//: extension TalkingVideoChatViewController: TalkingSocketManagerErrorDelegate {
extension FillPurple: GlobalSustainable {
    // 处理error情况
    //: func socket_didRecieveError(errorNo: Int, cmd: String, msg: String, data: [String: Any]) {
    func connectTo(errorNo: Int, cmd: String, msg: String, data: [String: Any]) {
        //: if cmd == "refuseCall" { // 对方拒绝了通话
        if cmd == (String(app_eagerMessage) + String(dataUnknownAttributeName)) { // 对方拒绝了通话
            //: if self.miniView == nil {
            if self.miniView == nil {
                //: self.func__showStatusBarErrorMsg(showMsg: "No answer".localized)
                self.exitMarker(showMsg: (String(userByPiName) + userDonName.replacingOccurrences(of: "complete", with: "r")).localized)
                //: } else {
            } else {
                //: self.miniView?.dismissWhenCallEnd()
                self.miniView?.pause()
                //: TalkingSocketManager.shared.pacificVC = nil
                MaterialCurrency.shared.pacificVC = nil
            }
            //: popCurrentViewController()
            spacePop()

            //: } else if cmd == "onAcceptCall" && errorNo == 101 { // 接听通话时，余额不足
        } else if cmd == (String(mainThinSpellData.suffix(5)) + "eptCall") && errorNo == 101 { // 接听通话时，余额不足
            //: guard MarginExamineer.share.appStatus == AppSkinStatus.normal.rawValue else { return }
            guard MarginExamineer.share.appStatus == AwayPush.normal.rawValue else { return }
            //: OriginMediumWill.share.func__jumpToWebRecharge(sufficient: false)
            OriginMediumWill.share.resumeUniversal(sufficient: false)

            //: } else if cmd == "onAcceptCall" && errorNo == 102 { // 对方已取消通话
        } else if cmd == (String(mainThinSpellData.suffix(5)) + "eptCall") && errorNo == 102 { // 对方已取消通话
            //: self.func__showStatusBarErrorMsg(showMsg: msg)
            self.exitMarker(showMsg: msg)
            //: popCurrentViewController()
            spacePop()
        }
    }
}

// MARK: - ExtendAround【通话按钮操作事件】

//: extension TalkingVideoChatViewController: Talking1v1VideoInteractionViewDelegate {
extension FillPurple: ExtendAround {
    /// 接听视频通话
    //: func interactionView_shouldAcceptCall() {
    func field() {
        //: TalkingSocketManager.shared.sendMessage(info: ["cmd": "acceptCall", "data": ["uid": self.chatModel?.pairUid]])
        MaterialCurrency.shared.warningVersion(info: [String(bytes: kInsideString.reversed(), encoding: .utf8)!: (String(constRedKey) + String(noti_methodId)), String(bytes: notiExamineMicString.reversed(), encoding: .utf8)!: [String(bytes: constSignId.map{delayStage(tell: $0)}, encoding: .utf8)!: self.chatModel?.pairUid]])
    }

    /// 拨打方结束通话
    //: func interactionView_shouldEndCall() {
    func doingInner() {
        //: if self.chatModel?.videoStage == .Waiting {
        if self.chatModel?.videoStage == .Waiting { // 拨打方取消【未通话】
            //: self.func__showStatusBarErrorMsg(showMsg: "Calls are cancelled".localized)
            self.exitMarker(showMsg: (String(k_crucialKey.prefix(5)) + " are" + String(mainExceptTitle.prefix(6)) + mainDynamicsId.replacingOccurrences(of: "our", with: "l")).localized)
            //: TalkingSocketManager.shared.sendMessage(info: ["cmd": "cancelCall", "data": ["logId": self.chatModel?.logId]])
            MaterialCurrency.shared.warningVersion(info: [String(bytes: kInsideString.reversed(), encoding: .utf8)!: (String(constIgnoreStr) + String(noti_tillHimAccessKey.suffix(4))), String(bytes: notiExamineMicString.reversed(), encoding: .utf8)!: [String(bytes: mainHundredMsg.reversed(), encoding: .utf8)!: self.chatModel?.logId]])
            //: } else if self.chatModel?.videoStage == .Recieved {
        } else if self.chatModel?.videoStage == .Recieved { // 接收方取消【未通话】
            //: self.func__showStatusBarErrorMsg(showMsg: "Calls are cancelled".localized)
            self.exitMarker(showMsg: (String(k_crucialKey.prefix(5)) + " are" + String(mainExceptTitle.prefix(6)) + mainDynamicsId.replacingOccurrences(of: "our", with: "l")).localized)
            //: TalkingSocketManager.shared.sendMessage(info: ["cmd": "refuseCall", "data": ["uid": self.chatModel?.pairUid]])
            MaterialCurrency.shared.warningVersion(info: [String(bytes: kInsideString.reversed(), encoding: .utf8)!: (String(app_eagerMessage) + String(dataUnknownAttributeName)), String(bytes: notiExamineMicString.reversed(), encoding: .utf8)!: [String(bytes: constSignId.map{delayStage(tell: $0)}, encoding: .utf8)!: self.chatModel?.pairUid]])
            //: } else {
        } else { // 【通话中】
            //: if chatModel?.isRandomVCPush ?? false {
            if chatModel?.isRandomVCPush ?? false {
                //: TalkingAlertShow.alert(title: nil, message: "Continue matching with other users？".localized, leftBtnTitle: "No".localized, rightBtnTitle: "Yes".localized, leftBlock: {
                InputMight.untilPhone(title: nil, message: String(bytes: app_detailedValue.map{ratingBetween(global: $0)}, encoding: .utf8)!.localized, leftBtnTitle: "No".localized, rightBtnTitle: (String(const_portValue)).localized, leftBlock: {
                    //: self.randowCallIsEndTool(isNewRandow: false)
                    self.countensity(isNewRandow: false)
                    //: }, rightBlock: {
                }, rightBlock: {
                    //: self.randowCallIsEndTool(isNewRandow: true)
                    self.countensity(isNewRandow: true)
                    //: })
                })
                //: return
                return
                    //: } else {
            } else {
                //: self.func__showStatusBarErrorMsg(showMsg: "The call has ended".localized)
                self.exitMarker(showMsg: (String(data_joinFormat) + String(app_lightTitle)).localized)
                //: TalkingSocketManager.shared.sendMessage(info: ["cmd": "stopCall", "data": ["logId": self.chatModel?.logId]])
                MaterialCurrency.shared.warningVersion(info: [String(bytes: kInsideString.reversed(), encoding: .utf8)!: (String(app_buildTabString.suffix(8))), String(bytes: notiExamineMicString.reversed(), encoding: .utf8)!: [String(bytes: mainHundredMsg.reversed(), encoding: .utf8)!: self.chatModel?.logId]])
            }
        }

        //: popCurrentViewController()
        spacePop()
    }

    /// 随机匹配通话挂断时
    //: func randowCallIsEndTool(isNewRandow: Bool) {
    func countensity(isNewRandow: Bool) {
        //: self.func__showStatusBarErrorMsg(showMsg: "The call has ended".localized)
        self.exitMarker(showMsg: (String(data_joinFormat) + String(app_lightTitle)).localized)
        //: TalkingSocketManager.shared.sendMessage(info: ["cmd": "stopCall", "data": ["logId": self.chatModel?.logId]])
        MaterialCurrency.shared.warningVersion(info: [String(bytes: kInsideString.reversed(), encoding: .utf8)!: (String(app_buildTabString.suffix(8))), String(bytes: notiExamineMicString.reversed(), encoding: .utf8)!: [String(bytes: mainHundredMsg.reversed(), encoding: .utf8)!: self.chatModel?.logId]])

        //: if !isNewRandow{
        if !isNewRandow {
            //: if let targetVC = navigationController?.viewControllers.first(where: { $0 is RandomMatchVC }) {
            if let targetVC = navigationController?.viewControllers.first(where: { $0 is PutPromiseViewController }) {
                //: navigationController?.viewControllers = [targetVC]
                navigationController?.viewControllers = [targetVC]
                //: navigationController?.popToViewController(targetVC, animated: false)
                navigationController?.popToViewController(targetVC, animated: false)
            }
            //: }else {
        } else {
            //: popCurrentViewController()
            spacePop()
            //: NotificationCenter.default.post(name: MATCHVIDEOCALL_END_NOTIFICATION, object: nil)
            NotificationCenter.default.post(name: const_barKey, object: nil)
        }
    }

    /// 旋转摄像头
    //: func interactionView_shouldChangeCamera(isFront: Bool) {
    func premiumErr(isFront: Bool) {
        //: self.videoManager.useFrontCamera(isFront)
        self.videoManager.environment(isFront)
    }

    /// 开启/关闭摄像头
    //: func interactionView_shouldForbiddenCamera(forbidden: Bool) {
    func insideBy(forbidden: Bool) {
        //: if self.chatModel?.videoStage == .Recieved {
        if self.chatModel?.videoStage == .Recieved {
            //: self.bgVideoView.showCoverView(show: !forbidden)
            self.bgVideoView.agileOf(show: !forbidden)
        }
        //: self.videoManager.useCamera(!forbidden)
        self.videoManager.typicalIn(!forbidden)
        //: self.videoBG_refreshViewWithCameraStatus()
        self.modelSound()
    }

    /// 展示视频通话小窗口
    //: func interactionView_shouldDisplayMiniWindow() {
    func note() {
        //: TalkingSocketManager.shared.pacificVC = self
        MaterialCurrency.shared.pacificVC = self
        //: self.miniView = TalkingVideoMiniView.buildVideoMiniView()
        self.miniView = AwayBuilder.critical()
        //: self.videoManager.smallRenderView = self.miniView?.renderView
        self.videoManager.smallRenderView = self.miniView?.renderView
        //: self.videoManager.enterSmallRenderMode()
        self.videoManager.belowDraft()
        //: self.miniView?.tapGestureBlock = { [weak self] in
        self.miniView?.tapGestureBlock = { [weak self] in // 还原回通话界面
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: self.videoManager.exitSmallRenderMode()
            self.videoManager.progressCorner()
            //: self.isFromMiniView = true
            self.isFromMiniView = true
            //: self.currentViewController()?.navigationController?.pushViewController(self, animated: true)
            self.myAcross()?.navigationController?.pushViewController(self, animated: true)
        }
        //: popCurrentViewController()
        spacePop()
    }

    /// 投诉成功
    //: func interactionView_reportSucceed() {
    func bounceDown() {
        //: if reportView?.superview != nil {
        if reportView?.superview != nil {
            //: reportView?.removeFromSuperview()
            reportView?.removeFromSuperview()
        }
    }
}

// MARK: - Layout

//: extension TalkingVideoChatViewController {
extension FillPurple {
    /// 添加视图
    //: private func setupSubviews() {
    private func splitOn() {
        //: self.view.layer.masksToBounds = true
        self.view.layer.masksToBounds = true
        //: self.view.addSubview(bgVideoView)
        self.view.addSubview(bgVideoView)
        //: self.view.addSubview(interactionView)
        self.view.addSubview(interactionView)
        //: self.interactionView.addWindowView(view: self.windowView)
        self.interactionView.coverWithPan(view: self.windowView)
    }

    /// 添加约束
    //: private func setupSubViewsConstraint() {
    private func spineConcrete() {
        //: bgVideoView.snp.makeConstraints { make in
        bgVideoView.snp.makeConstraints { make in
            //: make.edges.equalToSuperview()
            make.edges.equalToSuperview()
        }
        //: interactionView.snp.makeConstraints { make in
        interactionView.snp.makeConstraints { make in
            //: make.edges.equalToSuperview()
            make.edges.equalToSuperview()
        }
    }
}
