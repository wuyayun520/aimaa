
//: Declare String Begin

/*: "init(coder:) has not been implemented" :*/
fileprivate let constListPoId:[UInt8] = [0x90,0x95,0x90,0x9b,0x4f,0x8a,0x96,0x8b,0x8c,0x99,0x61,0x50,0x47,0x8f,0x88,0x9a,0x47,0x95,0x96,0x9b,0x47,0x89,0x8c,0x8c,0x95,0x47,0x90,0x94,0x97,0x93,0x8c,0x94,0x8c,0x95,0x9b,0x8c,0x8b]

fileprivate func taThan(year num: UInt8) -> UInt8 {
    let value = Int(num) + 217
    if value > 255 {
        return UInt8(value - 256)
    } else {
        return UInt8(value)
    }
}

/*: "#128CFF" :*/
fileprivate let kArkId:[Character] = ["#","1","2","8","C","F"]
fileprivate let mainGivingData:String = "f"

/*: "Feedback" :*/
fileprivate let k_makerTitle:String = "Feedbathree efficiency"
fileprivate let main_tapMsg:String = "CK"

/*: "#8370FF" :*/
fileprivate let const_outsideValue:String = "explain educate char box standard#8370FF"

/*: "Quick Greeting" :*/
fileprivate let k_extentDivideId:String = "literal while out final totalQuick"
fileprivate let main_generallyString:String = "partyng"

/*: "Beauty Photos" :*/
fileprivate let notiBlockString:[Character] = ["B","e","a","u"]
fileprivate let user_childWithName:String = "ty Photoswarning small local whole"

/*: "url" :*/
fileprivate let kReadFitTitle:[Character] = ["u","r","l"]

/*: "unlockGift" :*/
fileprivate let k_unlessMessage:[Character] = ["u","n","l","o","c","k","G","i","f","t"]

/*: "isBurn" :*/
fileprivate let main_maybeSlidePath:[UInt8] = [0x6e,0x72,0x75,0x42,0x73,0x69]

/*: "giftId" :*/
fileprivate let dataBuildValue:String = "giftIdcurrent at constraint"

/*: "rYMsgType" :*/
fileprivate let notiPartyValue:[Character] = ["r"]
fileprivate let mainRelativePlatValue:[Character] = ["Y","M","s","g","T","y","p","e"]

/*: "GJ:CallCustom" :*/
fileprivate let notiTranslateMessage:String = "GJ:Callsearcher oval command"
fileprivate let user_mendName:[Character] = ["C","u","s","t","o","m"]

/*: "changeDirection" :*/
fileprivate let data_subtleId:String = "chchestge"
fileprivate let mainGemPath:String = "ON"

/*: "extra" :*/
fileprivate let appWenRegionData:String = "extconstraint"

/*: "msgInfo" :*/
fileprivate let show_transactionCountoStr:String = "fatal"
fileprivate let appRetMessage:String = "hunk accessiblesgInfo"

/*: "toUid" :*/
fileprivate let dataUndertakeRecoverBeforePath:String = "toUidmerge fee context"

/*: "wantGift" :*/
fileprivate let notiCordAfterData:String = "wdirectn"
fileprivate let appResourceString:String = "collection give beneath maintaintGift"

/*: "jumpKey" :*/
fileprivate let showFinanceUrl:String = "jumpKetry universal"
fileprivate let noti_thatTitle:String = "submit"

/*: "logType" :*/
fileprivate let kPainterFragmentFormat:[Character] = ["l","o","g","T","y"]
fileprivate let mainCertainMsg:String = "PE"

/*: "content" :*/
fileprivate let k_tipData:[UInt8] = [0x74,0x6e,0x65,0x74,0x6e,0x6f,0x63]

/*: "msgType" :*/
fileprivate let app_plainLeadingCorrectString:[UInt8] = [0x79,0x7f,0x73,0x60,0x85,0x7c,0x71]

fileprivate func towardInvite(ease num: UInt8) -> UInt8 {
    let value = Int(num) - 12
    if value < 0 {
        return UInt8(value + 256)
    } else {
        return UInt8(value)
    }
}

/*: "txt" :*/
fileprivate let const_bindMessage:String = "TXT"

/*: "message" :*/
fileprivate let user_gravityMsg:[UInt8] = [0xc0,0xc8,0xde,0xde,0xcc,0xca,0xc8]

private func hungReceiver(dismiss num: UInt8) -> UInt8 {
    return num ^ 173
}

/*: "replayId" :*/
fileprivate let notiAnnouncementMessage:[UInt8] = [0x9e,0x89,0x9c,0x80,0x8d,0x95,0xa5,0x88]

private func thenObject(wen num: UInt8) -> UInt8 {
    return num ^ 236
}

/*: "type" :*/
fileprivate let constYourselfData:[Character] = ["t","y","p","e"]

/*: "TxtMsg" :*/
fileprivate let user_needKey:String = "similar"
fileprivate let mainProvideData:[Character] = ["x","t","M","s","g"]

/*: "params" :*/
fileprivate let user_keyWantGreenFormat:[UInt8] = [0xaf,0xa0,0xb1,0xa0,0xac,0xb2]

fileprivate func cookieResolve(week num: UInt8) -> UInt8 {
    let value = Int(num) - 63
    if value < 0 {
        return UInt8(value + 256)
    } else {
        return UInt8(value)
    }
}

/*: "MultiQuickMsg" :*/
fileprivate let app_careStr:String = "channel safe vid norm recordMultiQu"
fileprivate let userPrivacyKey:String = "accuracy would denseickMsg"

/*: "QuickMsg" :*/
fileprivate let app_relateTitle:String = "line void access am fastQuickMs"
fileprivate let main_againstValue:String = "big"

/*: "txtId" :*/
fileprivate let notiNeverJudgeStandardValue:String = "where indicatortxtId"

/*: "Voice hello download failed. Procedure" :*/
fileprivate let notiJobStr:[UInt8] = [0x41,0x78,0x7e,0x74,0x72,0x37,0x7f,0x72,0x7b,0x7b,0x78,0x37,0x73,0x78,0x60,0x79,0x7b,0x78,0x76,0x73,0x37,0x71,0x76,0x7e,0x7b,0x72,0x73,0x39,0x37,0x47,0x65,0x78,0x74,0x72,0x73,0x62,0x65,0x72]

/*: "length" :*/
fileprivate let const_appealData:[Character] = ["l","e","n","g","t","h"]

/*: "audio" :*/
fileprivate let k_automaticallyTerrificData:String = "AUDIO"

/*: "audioId" :*/
fileprivate let k_selectedString:String = "monster product automobile carefulaudioId"

/*: "audioLength" :*/
fileprivate let show_obtainString:[UInt8] = [0xd9,0xed,0xdc,0xe1,0xe7,0xc4,0xdd,0xe6,0xdf,0xec,0xe0]

fileprivate func atKing(operation num: UInt8) -> UInt8 {
    let value = Int(num) - 120
    if value < 0 {
        return UInt8(value + 256)
    } else {
        return UInt8(value)
    }
}

/*: "audioSandbox" :*/
fileprivate let main_othersGalleryValue:[Character] = ["a","u","d"]
fileprivate let showMatchValue:[Character] = ["i","o","S","a","n","d","b","o","x"]

/*: "senderId" :*/
fileprivate let noti_triggerTitle:[UInt8] = [0x64,0x49,0x72,0x65,0x64,0x6e,0x65,0x73]

/*: "AudioMsg" :*/
fileprivate let showLastSpellFormat:String = "Audiodatabase hunk stretch effect"
fileprivate let notiCrushId:[Character] = ["M","s","g"]

/*: "Can't send blank message" :*/
fileprivate let user_albumMsg:[Character] = ["C","a","n","\'","t"," ","s","e","n","d"," "]
fileprivate let notiTimingMultipleString:[Character] = ["b","l","a","n","k"," "]
fileprivate let user_eagerString:[Character] = ["m","e","s","s","a","g","e"]

/*: "quoteMsgId" :*/
fileprivate let constUnlessKingKey:String = "universal while highlightquoteMs"
fileprivate let data_pointThreeKey:String = "gIdmobile angle instance unknown"

/*: "quoteMsgInfo" :*/
fileprivate let notiDescriptionPolicyTitle:String = "quojust"

/*: "contentType" :*/
fileprivate let show_warningPath:[UInt8] = [0x46,0x4a,0x4b,0x51,0x40,0x4b,0x51,0x71,0x5c,0x55,0x40]

private func mobileAdmin(tell num: UInt8) -> UInt8 {
    return num ^ 37
}

/*: "ImgMsg" :*/
fileprivate let userUpTitle:String = "ImgMsguniform evolution similar drag resume"

/*: "img" :*/
fileprivate let kPublisherMsg:String = "createg"

/*: "imageUri" :*/
fileprivate let constMpValue:[UInt8] = [0x8b,0x8f,0x83,0x85,0x87,0xb7,0x90,0x8b]

private func chartAdminAway(clean num: UInt8) -> UInt8 {
    return num ^ 226
}

/*: "customSend" :*/
fileprivate let user_specifyRemindData:[UInt8] = [0xdd,0xef,0xed,0xee,0xe9,0xe7,0xcd,0xdf,0xe8,0xde]

fileprivate func programSame(wed num: UInt8) -> UInt8 {
    let value = Int(num) - 122
    if value < 0 {
        return UInt8(value + 256)
    } else {
        return UInt8(value)
    }
}

/*: "pic" :*/
fileprivate let app_commonUrl:String = "pressureic"

/*: "VideoMsg" :*/
fileprivate let app_uponId:String = "origin mobile typical usedVideoMsg"

/*: "video" :*/
fileprivate let showProperlyPath:String = "vidsampleo"

/*: "coverImg" :*/
fileprivate let dataBoardKey:[UInt8] = [0xfe,0xf2,0xeb,0xf8,0xef,0xd4,0xf0,0xfa]

/*: "videoUrl" :*/
fileprivate let main_memoryBeanUrl:[UInt8] = [0x6c,0x72,0x55,0x6f,0x65,0x64,0x69,0x76]

/*: "duration" :*/
fileprivate let user_dismissUrl:[UInt8] = [0x9e,0xaf,0xac,0x9b,0xae,0xa3,0xa9,0xa8]

fileprivate func weightAcross(running num: UInt8) -> UInt8 {
    let value = Int(num) + 198
    if value > 255 {
        return UInt8(value - 256)
    } else {
        return UInt8(value)
    }
}

/*: "len" :*/
fileprivate let const_spreadMutualMessage:[UInt8] = [0xc4,0xcd,0xc6]

/*: "Documents/User/voice/" :*/
fileprivate let showBackName:String = "Documelab special shot"
fileprivate let kMigrationLogicId:String = "pact awake dader/"
fileprivate let app_coordinatorSubjectMountMessage:String = "pose decide child providevoice/"

/*: "Documents/User/record/" :*/
fileprivate let notiStrokeId:[Character] = ["D","o","c","u","m","e","n","t","s","/","U","s","e","r"]
fileprivate let show_portDelicateMessage:String = "eager"
fileprivate let data_rapidBeforeId:[Character] = ["r","e","c","o","r","d","/"]

/*: "msgIncome" :*/
fileprivate let appTermsOvalUrl:[Character] = ["m","s","g","I","n","c","o","m","e"]

/*: "messageType" :*/
fileprivate let constUniformAssociatedPath:String = "quality around beyond schedule customermessageT"
fileprivate let k_insteadPath:String = "ypaccept"

/*: "WantGift" :*/
fileprivate let dataShadowMessage:String = "WantGiftcon characteristic invitation cloud"

/*: _PrivateChatTipsTitle_ :*/
fileprivate let constCompleteBurnMsg:[Character] = ["_","P","r","i","v","a","t","e","C","h","a","t","T","i","p","s","T","i"]
fileprivate let k_errorData:[Character] = ["t","l","e","_"]

/*: "msgId" :*/
fileprivate let const_successKey:String = "msgIdfemale shared present"

/*: "totalMfCoin" :*/
fileprivate let mainMagnitudeervalValue:String = "totalMfCoautomatic progress medal"
fileprivate let appEraseRapidMessage:[Character] = ["i","n"]

/*: "attachSysMsg" :*/
fileprivate let appSustainableString:String = "proceduretach"
fileprivate let constFixedKey:String = "SysMsgease another project or accessible"

/*: "intimatePhoto" :*/
fileprivate let notiProcessTitle:[Character] = ["i","n","t","i","m","a"]
fileprivate let dataCalculateString:String = "pull processor capacity draw distributiontePhoto"

/*: "totalIntimate" :*/
fileprivate let user_disappearTitle:String = "totalInappropriate tension"
fileprivate let userFilterStr:String = "tgroupate"

/*: "uid" :*/
fileprivate let noti_attachStr:[UInt8] = [0xcb,0xbf,0xba]

fileprivate func foundationEven(support num: UInt8) -> UInt8 {
    let value = Int(num) - 86
    if value < 0 {
        return UInt8(value + 256)
    } else {
        return UInt8(value)
    }
}

/*: "intimate" :*/
fileprivate let showBitTurnColorPath:[UInt8] = [0xea,0xed,0xf7,0xea,0xee,0xe2,0xf7,0xe6]

private func pictureConversion(since num: UInt8) -> UInt8 {
    return num ^ 131
}

/*: "Can't use the function due to your lack of intimacy level." :*/
fileprivate let userRejectDisappearMessage:[UInt8] = [0xa4,0x86,0x89,0xc0,0x93,0xc7,0x92,0x94,0x82,0xc7,0x93,0x8f,0x82,0xc7,0x81,0x92,0x89,0x84,0x93,0x8e,0x88,0x89,0xc7,0x83,0x92,0x82,0xc7,0x93,0x88,0xc7,0x9e,0x88,0x92,0x95,0xc7,0x8b,0x86,0x84,0x8c,0xc7,0x88,0x81,0xc7,0x8e,0x89,0x93,0x8e,0x8a,0x86,0x84,0x9e,0xc7,0x8b,0x82,0x91,0x82,0x8b,0xc9]

/*: "giftNum" :*/
fileprivate let main_nervousUrl:String = "present in decreasegiftNum"

/*: "pkgItemsetId" :*/
fileprivate let data_obtainId:String = "album abs people vertical powderpkgIt"
fileprivate let showSucceedMeasureUrl:[Character] = ["e","m"]
fileprivate let mainExperiencedEntityId:[Character] = ["s","e","t","I","d"]

/*: "from" :*/
fileprivate let data_communicateData:String = "makerrom"

/*: "wantGiftId" :*/
fileprivate let data_removeBlackStr:String = "wacontrol"
fileprivate let dataStreakAmongUrl:String = "asset"

/*: "WantGiftMsg" :*/
fileprivate let dataFeeMsg:String = "Wantexercise sort dire expression"
fileprivate let noti_packData:String = "me girl do assertMsg"

/*: "gift" :*/
fileprivate let noti_happyString:String = "gateift"

/*: "View info" :*/
fileprivate let user_talkId:String = "tap eh address contactView "

/*: "Notes" :*/
fileprivate let k_restaurantStr:[Character] = ["N","o","t","e","s"]

/*: "Report" :*/
fileprivate let kDiscloseData:String = "any operation normReport"

/*: "View read receipts?" :*/
fileprivate let dataSectionStr:[Character] = ["V","i","e","w"," ","r","e","a","d"," "]
fileprivate let appEnabledAirUrl:String = "owner common sir exclusive tareceipts?"

/*: "She" :*/
fileprivate let dataImplementProblemId:[Character] = ["S","h","e"]

/*: "%@ is using too low a version" :*/
fileprivate let k_respectiveName:String = "application column maybe para graph%@ i"
fileprivate let mainDrawerRelateValue:String = "outer lawg too"
fileprivate let noti_perceptionTitle:String = " versiorole remaining gender"
fileprivate let data_movieEvolutionPath:String = "effect"

/*: "Shoot video at least 5 seconds" :*/
fileprivate let notiAKey:String = "Shootfixed hick ping"
fileprivate let data_factExclusiveFormat:String = "o at provider in guide"
fileprivate let kAriaMessage:[Character] = ["t"," ","5"," ","s","e","c","o","n","d","s"]

/*: "Upload failed" :*/
fileprivate let noti_retainDeemData:String = "attach auto collection country sheUploa"
fileprivate let const_afterId:String = "ilebox"

/*: "播放状态: :*/
fileprivate let dataInfoData:String = "播\u{653e}\u{72b6}态:"

/*: "c2c_" :*/
fileprivate let app_stageDatabaseId:String = "c2c_weak tar associate level"

/*: "msg" :*/
fileprivate let const_clearStatePath:[Character] = ["m","s","g"]

/*: "tips" :*/
fileprivate let const_serverStr:[Character] = ["t","i","p","s"]

/*: "You can use the video chat feature to give %@ a call" :*/
fileprivate let userCapacityMsg:[UInt8] = [0x2e,0x18,0x2,0x57,0x14,0x16,0x19,0x57,0x2,0x4,0x12,0x57,0x3,0x1f,0x12,0x57,0x1,0x1e,0x13,0x12,0x18,0x57,0x14,0x1f,0x16,0x3,0x57,0x11,0x12,0x16,0x3,0x2,0x5,0x12,0x57,0x3,0x18,0x57,0x10,0x1e,0x1,0x12,0x57,0x52,0x37,0x57,0x16,0x57,0x14,0x16,0x1b,0x1b]

private func zzAnima(fixed num: UInt8) -> UInt8 {
    return num ^ 119
}

/*: "cellData" :*/
fileprivate let show_controlKey:String = "CELL"

/*: "F5F5F5" :*/
fileprivate let userMageUrl:[Character] = ["F","5","F","5","F","5"]

/*: "System notification" :*/
fileprivate let show_changeData:[Character] = ["S","y","s","t","e","m"," ","n","o","t","i","f","i","c","a"]
fileprivate let app_colorAgileStr:String = "tonlyon"

//: Declare String End

// __DEBUG__
// __CLOSE_PRINT__
//
//  ViaNative.swift
//  AbroadTalking
//
//  Created by Hemming on 2022/9/9.
//

//: import AVFAudio
import AVFAudio
//: import Photos
import Photos
//: import UIKit
import UIKit

/// 私聊入口枚举
//: enum PrivateChatIsFromEnum {
enum ListenProcessing {
    //: case Normal
    case Normal // 默认入口
    //: case LiveRoom
    case LiveRoom // 来自直播间（半屏展示）
}

//: class TalkingPrivateChatController: TalkingBaseViewController {
class ViaNative: EasyChainSensor {
    //: static var replyLatestTimes: Date = Date(timeIntervalSince1970: 0)
    static var replyLatestTimes: Date = .init(timeIntervalSince1970: 0) // 最新回复时间
    //: var needShowRating = false
    var needShowRating = false // 是否展示app store评分
    //: var quickGreetingTextDict: [String: Any]?
    var quickGreetingTextDict: [String: Any]? // 一键打招呼文本消息
    //: var quickGreetingAudioDict: [String: Any]?
    var quickGreetingAudioDict: [String: Any]? // 一键打招呼语音消息
    //: private let disposeBag = DisposeBag()
    private let disposeBag = DisposeBag()
    //: private var conversationType: PacificUserType = .private
    private var conversationType: PacificUserType = .private
    //: private var userWrap = DocumentLevelPurple()
    private var userWrap = DocumentLevelPurple() // chatInfo模型
    //: private var isFrom = PrivateChatIsFromEnum.Normal
    private var isFrom = ListenProcessing.Normal // 私聊入口

    //: private var isShowIntimateView = false
    private var isShowIntimateView = false // 顶部是否已经展示了亲密度头像
    //: var QuickEndBlock: ((_ isSuccess: Bool, _ errCode: Int) -> Void)?
    var QuickEndBlock: ((_ isSuccess: Bool, _ errCode: Int) -> Void)? /// 1.7.8快速打招呼完成后

    //: init(chatID: String, isFrom: PrivateChatIsFromEnum = .Normal, userWrap: DocumentLevelPurple = DocumentLevelPurple()) {
    init(chatID: String, isFrom: ListenProcessing = .Normal, userWrap: DocumentLevelPurple = DocumentLevelPurple()) {
        //: super.init(nibName: nil, bundle: nil)
        super.init(nibName: nil, bundle: nil)
        //: self.msgView.targetID = chatID
        self.msgView.targetID = chatID
        //: self.userWrap = userWrap
        self.userWrap = userWrap
        //: self.msgView.isRecharge = userWrap.isRecharge
        self.msgView.isRecharge = userWrap.isRecharge

        //: TalkingPrivateChatController.replyLatestTimes = Date(timeIntervalSince1970: 0)
        ViaNative.replyLatestTimes = Date(timeIntervalSince1970: 0)
        //: TalkingPrivateChatManager.share.readReceiptDict[msgView.targetID] = self.userWrap.readReceiptTime
        InvisibleJob.share.readReceiptDict[msgView.targetID] = self.userWrap.readReceiptTime
        //: self.isFrom = isFrom
        self.isFrom = isFrom
        //: if msgView.targetID == MovementStructure.getXiaoMiID() {
        if msgView.targetID == MovementStructure.uniqueAbove() { // 系统消息
            //: conversationType = .system
            conversationType = .system
            //: hideNavi = false
            hideNavi = false
            //: } else if msgView.targetID == MovementStructure.getCustomerServiceID() {
        } else if msgView.targetID == MovementStructure.cartoonConnection() { // 官方客服
            //: conversationType = .service
            conversationType = .service
            //: hideNavi = true
            hideNavi = true

            //: } else {
        } else {
            //: hideNavi = true
            hideNavi = true
        }
    }

    //: required init?(coder: NSCoder) {
    required init?(coder: NSCoder) {
        //: fatalError("init(coder:) has not been implemented")
        fatalError(String(bytes: constListPoId.map{taThan(year: $0)}, encoding: .utf8)!)
    }

    //: override func loadView() {
    override func loadView() {
        //: if #available(iOS 13.2, *) {
        if #available(iOS 13.2, *) {
            //: if MarginExamineer.share.appConfigMode.disableShootScreen {
            if MarginExamineer.share.appConfigMode.disableShootScreen {
                //: super.loadView()
                super.loadView()
                //: } else {
            } else {
                //: let bgView = MAScreenShieldView(frame: CGRect(x: 0, y: 0, width: ScreenWidth, height: ScreenHeight))
                let bgView = OccasionAll(frame: CGRect(x: 0, y: 0, width: show_errorChangeFormat, height: appLoadMessage))
                //: self.view = bgView
                self.view = bgView
            }
            //: } else {
        } else {
            //: super.loadView()
            super.loadView()
        }
    }

    //: override func viewDidLoad() {
    override func viewDidLoad() {
        //: super.viewDidLoad()
        super.viewDidLoad()
        //: half_showViewAnimation()
        restriction()
        //: createUI()
        unlessSquare()
        //: createUIConstraints()
        except()
        //: bindInteraction()
        sumegration()
        //: addTapGestureRecognizer()
        reservePanel()
        //: func__requestChatInfo()
        whenTurn()
        //: func__getFillDraftText()
        doingPlayer()
        //: TalkingVoiceMsgPlayManager.shared.addDaskManagerDelegate()
        TemporaryOperation.shared.sourceDownTarget()
        //: func__addVoiceNotification()
        foundSet()
        //: addReadPeceiptNotification()
        onicial()

        //: if conversationType == .private {
        if conversationType == .private {
            //: initCachEffectData()
            segmentBy()
            //: func__TipsNotification()
            doingMention()
            //: if MarginExamineer.share.loginUserMode.sex == Gender.male.rawValue {
            if MarginExamineer.share.loginUserMode.sex == EnvironmentEmpty.male.rawValue {
                // 处理男性侧快捷回复逻辑
                //: func__setReplyTipsView()
                despiteLight()
                // 判断问题回答消息遮罩是否展示
                //: refreshRechargeRequiredView()
                painterPurple()
            }
        }
    }

    //: override func viewDidAppear(_ animated: Bool) {
    override func viewDidAppear(_ animated: Bool) {
        //: super.viewDidAppear(animated)
        super.viewDidAppear(animated)
        //: func__addOrRrmoveObserverForMsgPlayer(isAdd: true)
        position(isAdd: true)
    }

    //: override func viewWillDisappear(_ animated: Bool) {
    override func viewWillDisappear(_ animated: Bool) {
        //: super.viewWillDisappear(animated)
        super.viewWillDisappear(animated)
        //: view.endEditing(true)
        view.endEditing(true)
        //: func__addOrRrmoveObserverForMsgPlayer(isAdd: false)
        position(isAdd: false)
        //: if conversationType == .private {
        if conversationType == .private {
            //: func__saveFillDraftText()
            matchOpen()
        }
    }

    //: override func viewDidDisappear(_ animated: Bool) {
    override func viewDidDisappear(_ animated: Bool) {
        //: super.viewDidDisappear(animated)
        super.viewDidDisappear(animated)
        // 展示App Store评分
        //: if needShowRating {
        if needShowRating {
            //: AppStoreRatingManager.shared.requestQuickRating()
            SystemDensity.shared.stepChallenge()
        }
    }

    //: deinit {
    deinit {
        //: NotificationCenter.default.removeObserver(self)
        NotificationCenter.default.removeObserver(self)
    }

    // MARK: - Lazy Load

    //: private lazy var contentView: UIView = {
    private lazy var contentView: UIView = {
        //: let v = UIView()
        let v = UIView()
        //: v.backgroundColor = .white
        v.backgroundColor = .white
        //: return v
        return v
        //: }()
    }()

    //: lazy var msgView: TUIMessageController = {
    lazy var msgView: DeployViewController = {
        //: let msgVC = TUIMessageController.init()
        let msgVC = DeployViewController()
        //: msgVC.delegate = self
        msgVC.delegate = self
        //: msgVC.tableView.register(TalkingChatTextMsgCell.self, forCellReuseIdentifier: TalkingChatTextMsgCell.className())
        msgVC.tableView.register(StartEquipment.self, forCellReuseIdentifier: StartEquipment.className())
        //: msgVC.tableView.register(TalkingChatImageMsgCell.self, forCellReuseIdentifier: TalkingChatImageMsgCell.className())
        msgVC.tableView.register(PageFactor.self, forCellReuseIdentifier: PageFactor.className())
        //: msgVC.tableView.register(TalkingChatVideoMsgCell.self, forCellReuseIdentifier: TalkingChatVideoMsgCell.className())
        msgVC.tableView.register(InfrastructureReflect.self, forCellReuseIdentifier: InfrastructureReflect.className())
        //: msgVC.tableView.register(TalkingChatGiftMsgCell.self, forCellReuseIdentifier: TalkingChatGiftMsgCell.className())
        msgVC.tableView.register(PainterHandle.self, forCellReuseIdentifier: PainterHandle.className())
        //: msgVC.tableView.register(TalkingChatAskForGiftMsgCell.self, forCellReuseIdentifier: TalkingChatAskForGiftMsgCell.className())
        msgVC.tableView.register(SoundBrownFirst.self, forCellReuseIdentifier: SoundBrownFirst.className())
        //: msgVC.tableView.register(TalkingChatTipsMsgCell.self, forCellReuseIdentifier: TalkingChatTipsMsgCell.className())
        msgVC.tableView.register(EagerSpeed.self, forCellReuseIdentifier: EagerSpeed.className())
        //: msgVC.tableView.register(TalkingChatMatchMsgCell.self, forCellReuseIdentifier: TalkingChatMatchMsgCell.className())
        msgVC.tableView.register(DisplayOwner.self, forCellReuseIdentifier: DisplayOwner.className())
        //: msgVC.tableView.register(TalkingChatAudioMsgCell.self, forCellReuseIdentifier: TalkingChatAudioMsgCell.className())
        msgVC.tableView.register(WhiteProcessor.self, forCellReuseIdentifier: WhiteProcessor.className())
        //: msgVC.tableView.register(TalkingChatSystemNotifJumpMsgCell.self, forCellReuseIdentifier: TalkingChatSystemNotifJumpMsgCell.className())
        msgVC.tableView.register(SessionSave.self, forCellReuseIdentifier: SessionSave.className())
        //: msgVC.tableView.register(TUISystemMessageCell.self, forCellReuseIdentifier: BlackBy.TSystemMessageCell_ReuseId)
        msgVC.tableView.register(TensionIndependent.self, forCellReuseIdentifier: BlackBy.TSystemMessageCell_ReuseId)
        //: msgVC.currentChatType = conversationType
        msgVC.currentChatType = conversationType

        //: return msgVC
        return msgVC
        //: }()
    }()

    //: lazy var bgImgeView: UIImageView = {
    lazy var bgImgeView: UIImageView = {
        //: let imgView = UIImageView.init()
        let imgView = UIImageView()
        //: imgView.backgroundColor = .clear
        imgView.backgroundColor = .clear
        //: imgView.contentMode = .scaleAspectFill
        imgView.contentMode = .scaleAspectFill
        //: imgView.clipsToBounds = true
        imgView.clipsToBounds = true
        //: return imgView
        return imgView
        //: }()
    }()

    //: lazy var coverView: UIVisualEffectView = {
    lazy var coverView: UIVisualEffectView = {
        //: let effect = UIBlurEffect.init(style: .dark)
        let effect = UIBlurEffect(style: .dark)
        //: let effectView = UIVisualEffectView.init(effect: effect)
        let effectView = UIVisualEffectView(effect: effect)
        //: effectView.alpha = 0.45
        effectView.alpha = 0.45
        //: return effectView
        return effectView
        //: }()
    }()

    //: lazy var bgView: UIView = {
    lazy var bgView: UIView = {
        //: let view = UIImageView.init()
        let view = UIImageView()
        //: view.backgroundColor = UIColor.black.withAlphaComponent(0.75)
        view.backgroundColor = UIColor.black.withAlphaComponent(0.75)
        //: return view
        return view
        //: }()
    }()

    //: private lazy var videoCallManager: TalkingCallMenuManager = {
    private lazy var videoCallManager: ClipPost = {
        //: let m = TalkingCallMenuManager()
        let m = ClipPost()
        //: m.uid = msgView.targetID
        m.uid = msgView.targetID
        //: m.onReqestCallErrorBlock = { [weak self] errNo in
        m.onReqestCallErrorBlock = { [weak self] errNo in
            //: guard let self = self,errNo == 101 else { return }
            guard let self = self, errNo == 101 else { return }
            //: self.msgInputView.setHidenFreeMsgTimesView()
            self.msgInputView.enableMaterial()
        }
        //: return m
        return m
        //: }()
    }()

    //: lazy var msgInputView: TalkingChatInputView = {
    lazy var msgInputView: WorkSend = {
        //: var type: InputViewType = .PrivateChat
        var type: ScaleTemporary = .PrivateChat
        //: if conversationType == .service {
        if conversationType == .service {
            //: type = .CustomerChat
            type = .CustomerChat
            //: } else if self.isFrom == .LiveRoom {
        } else if self.isFrom == .LiveRoom {
            //: type = .LiveRoom
            type = .LiveRoom
        }

        //: let view = TalkingChatInputView(type: type)
        let view = WorkSend(type: type)
        //: view.delegate = self
        view.delegate = self
        //: return view
        return view
        //: }()
    }()

    //: lazy var feedView: UIView = {
    lazy var feedView: UIView = {
        //: let view = UIView.init()
        let view = UIView()
        //: view.backgroundColor = .white
        view.backgroundColor = .white
        //: return view
        return view
        //: }()
    }()

    //: lazy var feedbackBtn: UIButton = {
    lazy var feedbackBtn: UIButton = {
        //: let btn = UIButton.init(type: UIButton.ButtonType.custom)
        let btn = UIButton(type: UIButton.ButtonType.custom)
        //: btn.backgroundColor = .white
        btn.backgroundColor = .white
        //: let attributes = [.foregroundColor: UIColor(hex: "#128CFF")!, .font: UIFont.pingfangFont(type: .Medium, fontSize: 16), .underlineStyle: 1] as [NSAttributedString.Key: Any]
        let attributes = [.foregroundColor: UIColor(hex: (String(kArkId) + mainGivingData.uppercased()))!, .font: UIFont.yearNearby(type: .Medium, fontSize: 16), .underlineStyle: 1] as [NSAttributedString.Key: Any]
        //: let attributeString = NSAttributedString(string: "Feedback".localized, attributes: attributes)
        let attributeString = NSAttributedString(string: (String(k_makerTitle.prefix(6)) + main_tapMsg.lowercased()).localized, attributes: attributes)
        //: btn.setAttributedTitle(attributeString, for: .normal)
        btn.setAttributedTitle(attributeString, for: .normal)
        //: btn.addTarget(self, action: #selector(feedbackBtnClick), for: .touchUpInside)
        btn.addTarget(self, action: #selector(listen), for: .touchUpInside)
        //: return btn
        return btn
        //: }()
    }()

    //: lazy var giftEffectView: TalkingPrivateChatAnimatView = {
    lazy var giftEffectView: ThresholdView = {
        //: let effectView = TalkingPrivateChatAnimatView.init()
        let effectView = ThresholdView()
        //: effectView.contentMode = .scaleAspectFill
        effectView.contentMode = .scaleAspectFill
        //: return effectView
        return effectView
        //: }()
    }()

    //: lazy var navView: TalkingChatNavTitleView = {
    lazy var navView: RouteConsumer = {
        //: let navView = TalkingChatNavTitleView(type: conversationType)
        let navView = RouteConsumer(type: conversationType)
        //: return navView
        return navView
        //: }()
    }()

    //: lazy var sendGreetBtn: UIButton = {
    lazy var sendGreetBtn: UIButton = {
        //: let btn = UIButton.init(type: UIButton.ButtonType.custom)
        let btn = UIButton(type: UIButton.ButtonType.custom)
        //: btn.backgroundColor = .white
        btn.backgroundColor = .white
        //: btn.titleLabel?.font = UIFont.pingfangFont(type: .Medium, fontSize: 15)
        btn.titleLabel?.font = UIFont.yearNearby(type: .Medium, fontSize: 15)
        //: btn.setTitleColor(UIColor(hex: "#8370FF"), for: .normal)
        btn.setTitleColor(UIColor(hex: (String(const_outsideValue.suffix(7)))), for: .normal)
        //: btn.setTitle("Quick Greeting".localized, for: .normal)
        btn.setTitle((String(k_extentDivideId.suffix(5)) + " Gree" + main_generallyString.replacingOccurrences(of: "party", with: "ti")).localized, for: .normal)
        //: btn.layer.cornerRadius = 15
        btn.layer.cornerRadius = 15
        //: btn.layer.borderColor = UIColor.appThemeColor().cgColor
        btn.layer.borderColor = UIColor.systemRequest().cgColor
        //: btn.layer.borderWidth = 1
        btn.layer.borderWidth = 1
        //: btn.layer.masksToBounds = true
        btn.layer.masksToBounds = true
        //: btn.addTarget(self, action: #selector(sendGreetBtnClick), for: .touchUpInside)
        btn.addTarget(self, action: #selector(recommend), for: .touchUpInside)
        //: btn.isHidden = !MarginExamineer.share.appUserConfigMode.showDirectGreet || MarginExamineer.share.loginUserMode.sex == Gender.male.rawValue || MarginExamineer.share.appStatus == AppSkinStatus.special.rawValue
        btn.isHidden = !MarginExamineer.share.appUserConfigMode.showDirectGreet || MarginExamineer.share.loginUserMode.sex == EnvironmentEmpty.male.rawValue || MarginExamineer.share.appStatus == AwayPush.special.rawValue
        //: return btn
        return btn
        //: }()
    }()

    //: private lazy var replyTipsView: TalkingPrivateChatReplyTipsView? = {
    private lazy var replyTipsView: SharedView? = {
        //: let view = TalkingPrivateChatReplyTipsView.init()
        let view = SharedView()
        //: view.delegate = self
        view.delegate = self
        //: return view
        return view
        //: }()
    }()

    //: private lazy var beautyPhotosBtn: UIButton = {
    private lazy var beautyPhotosBtn: UIButton = {
        //: let btn = UIButton.init(type: UIButton.ButtonType.custom)
        let btn = UIButton(type: UIButton.ButtonType.custom)
        //: btn.backgroundColor = .white
        btn.backgroundColor = .white
        //: btn.titleLabel?.font = UIFont.pingfangFont(type: .Medium, fontSize: 15)
        btn.titleLabel?.font = UIFont.yearNearby(type: .Medium, fontSize: 15)
        //: btn.setTitleColor(UIColor(hex: "#8370FF"), for: .normal)
        btn.setTitleColor(UIColor(hex: (String(const_outsideValue.suffix(7)))), for: .normal)
        //: btn.setTitle("Beauty Photos".localized, for: .normal)
        btn.setTitle((String(notiBlockString) + String(user_childWithName.prefix(9))).localized, for: .normal)
        //: btn.layer.cornerRadius = 15
        btn.layer.cornerRadius = 15
        //: btn.layer.borderColor = UIColor.appThemeColor().cgColor
        btn.layer.borderColor = UIColor.systemRequest().cgColor
        //: btn.layer.borderWidth = 1
        btn.layer.borderWidth = 1
        //: btn.layer.masksToBounds = true
        btn.layer.masksToBounds = true
        //: btn.addTarget(self, action: #selector(beautyPhotosBtnClick), for: .touchUpInside)
        btn.addTarget(self, action: #selector(choice), for: .touchUpInside)
        //: btn.isHidden = MarginExamineer.share.loginUserMode.sex == Gender.male.rawValue || MarginExamineer.share.appStatus == AppSkinStatus.special.rawValue
        btn.isHidden = MarginExamineer.share.loginUserMode.sex == EnvironmentEmpty.male.rawValue || MarginExamineer.share.appStatus == AwayPush.special.rawValue
        //: return btn
        return btn
        //: }()
    }()

    //: private lazy var gamesView: TalkingLiveRoomGamesView = {
    private lazy var gamesView: AllView = {
        //: let v = TalkingLiveRoomGamesView()
        let v = AllView()
        //: return v
        return v
        //: }()
    }()

    //: private lazy var bannerView: TalkingPrivateBannerView = {
    private lazy var bannerView: PageButtonView = {
        //: let view = TalkingPrivateBannerView.init()
        let view = PageButtonView()
        //: view.delegate = self
        view.delegate = self
        //: return view
        return view
        //: }()
    }()

    //: private lazy var floatScreenView: TalkingGameFloatingScreenView = {
    private lazy var floatScreenView: NeedEdge = { // 飘屏
        //: let v = TalkingGameFloatingScreenView()
        let v = NeedEdge()
        //: v.isHidden = (MarginExamineer.share.appStatus == AppSkinStatus.special.rawValue)
        v.isHidden = (MarginExamineer.share.appStatus == AwayPush.special.rawValue)
        //: v.isUserInteractionEnabled = false
        v.isUserInteractionEnabled = false
        //: v.endPlayBlock = { [weak self] in
        v.endPlayBlock = { [weak self] in
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: v.isUserInteractionEnabled = false
            v.isUserInteractionEnabled = false
        }
        //: return v
        return v
        //: }()
    }()

    //: private lazy var notesTopView: PrivateChatNotesTopView = {
    private lazy var notesTopView: JobView = { // 备注置顶
        //: let v = PrivateChatNotesTopView()
        let v = JobView()
        //: v.tapBlock = { [weak self] in
        v.tapBlock = { [weak self] in
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: self.jumpToNotesView()
            self.sincePause()
        }
        //: v.isHidden = true
        v.isHidden = true
        //: return v
        return v
        //: }()
    }()

    //: private lazy var rechargeView: RechargeRequiredView = {
    private lazy var rechargeView: CancelView = { // 充值限制视图
        //: let v = RechargeRequiredView()
        let v = CancelView()
        //: v.tapBlock = { [weak self] in
        v.tapBlock = { [weak self] in
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: if self.userWrap.isRecharge == -1 {
            if self.userWrap.isRecharge == -1 { // 首充优惠
                //: OriginMediumWill.share.func_pushToDiscountRechargeView()
                OriginMediumWill.share.belong()
                //: } else if self.userWrap.isRecharge == 0 {
            } else if self.userWrap.isRecharge == 0 { // 半屏充值
                //: OriginMediumWill.share.func__pushToHalfWebVC(webViewType: .RechargeHalfPage)
                OriginMediumWill.share.untilAmongForce(webViewType: .RechargeHalfPage)
            }
        }
        //: v.isHidden = true
        v.isHidden = true
        //: return v
        return v
        //: }()
    }()

    //: private lazy var quickReplayView: QuestionQuickReplayView = {
    private lazy var quickReplayView: EmptyEqualPut = { // 问题快捷回复视图
        //: let v = QuestionQuickReplayView()
        let v = EmptyEqualPut()
        //: v.tapBlock = { [weak self] model in
        v.tapBlock = { [weak self] model in
            //: guard let self = self else { return }
            guard let self = self else { return }
            // 调用发送消息接口
            //: self.func_sendQuestionQuickReplayMsg(model)
            self.satisfy(model)
        }
        //: v.isHidden = true
        v.isHidden = true
        //: return v
        return v
        //: }()
    }()
}

// MARK: - banner

//: extension TalkingPrivateChatController: PrivateBannerDelegate {
extension ViaNative: IsolateCrucialSmall {
    //: func changeBannerViewFream(isMiniView: Bool) {
    func third(isMiniView: Bool) {
        //: guard self.isFrom == .Normal else { return }
        guard self.isFrom == .Normal else { return }
        //: UIView.animate(withDuration: 0.3) {
        UIView.animate(withDuration: 0.3) {
            //: if isMiniView {
            if isMiniView {
                //: self.bannerView.snp.remakeConstraints { make in
                self.bannerView.snp.remakeConstraints { make in
                    //: if self.replyTipsView != nil {
                    if self.replyTipsView != nil {
                        //: make.bottom.equalTo(self.replyTipsView!.snp.top).offset(-8)
                        make.bottom.equalTo(self.replyTipsView!.snp.top).offset(-8)
                        //: } else {
                    } else {
                        //: make.bottom.equalTo(self.msgInputView.snp.top).offset(-8)
                        make.bottom.equalTo(self.msgInputView.snp.top).offset(-8)
                    }
                    //: make.trailing.equalTo(-4)
                    make.trailing.equalTo(-4)
                    //: make.height.equalTo(36)
                    make.height.equalTo(36)
                    //: make.width.equalTo(89)
                    make.width.equalTo(89)
                }
                //: } else {
            } else {
                //: self.bannerView.snp.remakeConstraints { make in
                self.bannerView.snp.remakeConstraints { make in
                    //: make.trailing.equalToSuperview()
                    make.trailing.equalToSuperview()
                    //: if self.replyTipsView != nil {
                    if self.replyTipsView != nil {
                        //: make.bottom.equalTo(self.replyTipsView!.snp.top).offset(-8)
                        make.bottom.equalTo(self.replyTipsView!.snp.top).offset(-8)
                        //: } else {
                    } else {
                        //: make.bottom.equalTo(self.msgInputView.snp.top).offset(-8)
                        make.bottom.equalTo(self.msgInputView.snp.top).offset(-8)
                    }
                    //: make.height.equalTo(106)
                    make.height.equalTo(106)
                    //: make.width.equalTo(93)
                    make.width.equalTo(93)
                }
            }
            //: self.bannerView.layoutIfNeeded()
            self.bannerView.layoutIfNeeded()
            //: self.bannerView.resetView(isMiniView: isMiniView)
            self.bannerView.anyTargetBeauty(isMiniView: isMiniView)
        }
    }
}

// MARK: - 聊天相关

//: extension TalkingPrivateChatController: TMessageControllerDelegate {
extension ViaNative: AdjustLength {
    /// 一键打招呼
    //: @objc func sendGreetBtnClick() {
    @objc func recommend() {
        //: TalkingPrivateChatManager.chat_sendQuickGreeting(toUserId: self.msgView.targetID) { errorCode, errorMsg, textDict, audioDict in
        InvisibleJob.directionTick(toUserId: self.msgView.targetID) { errorCode, errorMsg, textDict, audioDict in
            //: guard (textDict?.count ?? 0 > 0) || (audioDict?.count ?? 0 > 0) else {
            guard (textDict?.count ?? 0 > 0) || (audioDict?.count ?? 0 > 0) else {
                //: if errorMsg?.count ?? 0 > 0 {
                if errorMsg?.count ?? 0 > 0 {
                    //: self.func__showStatusBarErrorMsg(showMsg: errorMsg!)
                    self.exitMarker(showMsg: errorMsg!)
                }
                //: return
                return
            }

            // 发送一键打招呼消息
            //: self.func_sendQuickGreetingMsg(textDict: textDict, audioDict: audioDict)
            self.goldKey(textDict: textDict, audioDict: audioDict)
        }
    }

    /// 图片打招呼
    //: @objc func beautyPhotosBtnClick() {
    @objc func choice() {
        //: TalkingPrivateChatManager.chat_sendBeautyPhotos(toUserId: self.msgView.targetID) { isPushSet, dict in
        InvisibleJob.object(toUserId: self.msgView.targetID) { isPushSet, dict in
            //: if isPushSet {
            if isPushSet {
                //: return
                return
            }
            //: if dict != nil {
            if dict != nil {
                //: UIImageView().setUrlImage(urlStr: dict?["url"] as? String ?? "") {[weak self] _, image in
                UIImageView().chest(urlStr: dict?[(String(kReadFitTitle))] as? String ?? "") { [weak self] _, image in
                    //: guard let self = self else { return }
                    guard let self = self else { return }
                    //: if image != nil {
                    if image != nil {
                        //: let unlockGift = dict?["unlockGift"] as? Dictionary<String, Any>
                        let unlockGift = dict?[(String(k_unlessMessage))] as? [String: Any]
                        //: let sendModel = AbTalkingChatMsgCustomSendModel()
                        let sendModel = UserStageModel()
                        //: sendModel.isBurn = String.init(format: "%d", dict?["isBurn"] as? Int ?? 0)
                        sendModel.isBurn = String(format: "%d", dict?[String(bytes: main_maybeSlidePath.reversed(), encoding: .utf8)!] as? Int ?? 0)
                        //: sendModel.giftId = unlockGift?["giftId"] as? String ?? "0"
                        sendModel.giftId = unlockGift?[(String(dataBuildValue.prefix(6)))] as? String ?? "0"
                        //: self.func__sendPictureMsg(img: image!, sendModel: sendModel)
                        self.dealCamera(img: image!, sendModel: sendModel)
                        //: return
                        return
                    }
                }
            }
        }
    }

    //: func messageController(_ controller: TUIMessageController!, onNewMessage data: V2TIMMessage!) -> AlongCrucial! {
    func flexible(_ controller: DeployViewController!, heap data: V2TIMMessage!) -> AlongCrucial! {
        //: let dic = NeedSpecify.parseTXMessageData(data: data.customElem.data)
        let dic = NeedSpecify.onStrategy(data: data.customElem.data)
        //: var cellData = TUISocialChatManager.handleTXReceiveMessage(withExtral: dic, cusData: data.localCustomData, tempModel: data)
        var cellData = IndependentObserve.moreBloc(withExtral: dic, cusData: data.localCustomData, tempModel: data)
        //: if data.isKind(of: TalkingChatMatchMsgCellData.self), self.userWrap.uid.count > 0 {
        if data.isKind(of: EfficiencyDraw.self), self.userWrap.uid.count > 0 {
            //: cellData?.msgModel.userInfoModel = self.userWrap
            cellData?.msgModel.userInfoModel = self.userWrap
        }

        //: let jsonDic = JSON(dic)
        let jsonDic = JSON(dic)
        //: if jsonDic["rYMsgType"].stringValue == "GJ:CallCustom" { // 音视频通话消息
        if jsonDic[(String(notiPartyValue) + String(mainRelativePlatValue))].stringValue == (String(notiTranslateMessage.prefix(7)) + String(user_mendName)) { // 音视频通话消息
            //: let isSelf = jsonDic["changeDirection"].boolValue
            let isSelf = jsonDic[(data_subtleId.replacingOccurrences(of: "chest", with: "an") + "Directi" + mainGemPath.lowercased())].boolValue
            //: if isSelf == true {
            if isSelf == true { // 消息需要翻转方向
                // 更正当前用户uid
                //: cellData?.msgModel.user.uid = jsonDic["extra"]["msgInfo"]["toUid"].intValue
                cellData?.msgModel.user.uid = jsonDic[(appWenRegionData.replacingOccurrences(of: "constraint", with: "ra"))][(show_transactionCountoStr.replacingOccurrences(of: "fatal", with: "m") + String(appRetMessage.suffix(6)))][(String(dataUndertakeRecoverBeforePath.prefix(5)))].intValue
                // 更正消息展示方向
                //: let msgModel = cellData!.msgModel
                let msgModel = cellData!.msgModel
                //: cellData = ExceptionKindLocationCreate(direction: isSelf ? .MsgDirectionOutgoing:.MsgDirectionIncoming)
                cellData = ExceptionKindLocationCreate(direction: isSelf ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
                //: cellData?.msgModel = msgModel
                cellData?.msgModel = msgModel
            }
        }
        //: if cellData?.isKind(of: BoxStack.self) == true ||
        if cellData?.isKind(of: BoxStack.self) == true ||
            //: cellData?.isKind(of: TalkingChatImageMsgCellData.self) == true ||
            cellData?.isKind(of: DelayComposition.self) == true ||
            //: cellData?.isKind(of: ExceptionKindLocationCreate.self) == true ||
            cellData?.isKind(of: ExceptionKindLocationCreate.self) == true ||
            //: cellData?.isKind(of: TalkingChatVideoMsgCellData.self) == true {
            cellData?.isKind(of: MeasurePaint.self) == true
        {
            //: self.userWrap.existSess = true
            self.userWrap.existSess = true
        }

        //: if let cellData = cellData {
        if let cellData = cellData {
            // 接收解锁 私密消息（照片、视频）礼物，刷新本地消息状态
            //: TalkingPrivateChatManager.chat_onNewMessage_refreshLocalIntimateMsgStatus(cellData: cellData, msgView: self.msgView)
            InvisibleJob.effect(cellData: cellData, msgView: self.msgView)
            // 男性是否展示快捷回复消息
            //: checkQuestionQuickReplayView()
            finishShared()
        }

        //: return cellData
        return cellData
    }

    //: func messageController(_ controller: TUIMessageController!, onShowMessageData data: AlongCrucial!) -> BlackBy! {
    func nearCompose(_ controller: DeployViewController!, shadow data: AlongCrucial!) -> BlackBy! {
        // 更新聊天头像，不会更新腾讯数据库
        //: let data = changeChatHeadPic(data: data)
        let data = observer(data: data)

        //: if data.isKind(of: ExceptionKindLocationCreate.self) {
        if data.isKind(of: ExceptionKindLocationCreate.self) {
            //: let cell = TalkingChatTextMsgCell.init(style: .default, reuseIdentifier: TalkingChatTextMsgCell.className())
            let cell = StartEquipment(style: .default, reuseIdentifier: StartEquipment.className())
            //: let cellData = data as! ExceptionKindLocationCreate
            let cellData = data as! ExceptionKindLocationCreate

            //: if cellData.msgModel.quoteMsgInfo.isQuoteMsg() {
            if cellData.msgModel.quoteMsgInfo.blue() {
                //: if cellData.msgModel.quoteMsgInfo.uid == MarginExamineer.share.loginUserMode.userID {
                if cellData.msgModel.quoteMsgInfo.uid == MarginExamineer.share.loginUserMode.userID {
                    //: cellData.msgModel.quoteMsgInfo.sendName = MarginExamineer.share.loginUserMode.nickname ?? ""
                    cellData.msgModel.quoteMsgInfo.sendName = MarginExamineer.share.loginUserMode.nickname ?? ""
                    //: }else{
                } else {
                    //: cellData.msgModel.quoteMsgInfo.sendName = self.userWrap.nickname
                    cellData.msgModel.quoteMsgInfo.sendName = self.userWrap.nickname
                }
            }
            //: cell.fill(with: cellData)
            cell.satisfyAssociated(with: cellData)
//            cell.transBlock = { [weak self] in
//                guard let self = self else { return }
//                self.msgView.translateMsg(with: cellData)
//            }
            //: return cell
            return cell
        }

        //: if data.isKind(of: TalkingChatImageMsgCellData.self) {
        if data.isKind(of: DelayComposition.self) {
            //: let cell = TalkingChatImageMsgCell.init(style: .default, reuseIdentifier: TalkingChatImageMsgCell.className())
            let cell = PageFactor(style: .default, reuseIdentifier: PageFactor.className())
            //: let cellData = data as! TalkingChatImageMsgCellData
            let cellData = data as! DelayComposition
            //: cell.fill(with: cellData)
            cell.satisfyAssociated(with: cellData)
            //: return cell
            return cell
        }

        //: if data.isKind(of: TalkingChatVideoMsgCellData.self) {
        if data.isKind(of: MeasurePaint.self) {
            //: let cell = TalkingChatVideoMsgCell.init(style: .default, reuseIdentifier: TalkingChatVideoMsgCell.className())
            let cell = InfrastructureReflect(style: .default, reuseIdentifier: InfrastructureReflect.className())
            //: let cellData = data as! TalkingChatVideoMsgCellData
            let cellData = data as! MeasurePaint
            //: cell.fill(with: cellData)
            cell.satisfyAssociated(with: cellData)
            //: return cell
            return cell
        }

        //: if data.isKind(of: TalkingChatGiftMsgCellData.self) {
        if data.isKind(of: ContextLength.self) {
            //: let cell = TalkingChatGiftMsgCell.init(style: .default, reuseIdentifier: TalkingChatGiftMsgCell.className())
            let cell = PainterHandle(style: .default, reuseIdentifier: PainterHandle.className())
            //: let cellData = data as! TalkingChatGiftMsgCellData
            let cellData = data as! ContextLength
            //: cell.fill(with: cellData)
            cell.satisfyAssociated(with: cellData)
            //: return cell
            return cell
        }
        //: if data.isKind(of: LightSaveLabel.self) {
        if data.isKind(of: LightSaveLabel.self) {
            //: let cell = TalkingChatAskForGiftMsgCell.init(style: .default, reuseIdentifier: TalkingChatAskForGiftMsgCell.className())
            let cell = SoundBrownFirst(style: .default, reuseIdentifier: SoundBrownFirst.className())
            //: let cellData = data as! LightSaveLabel
            let cellData = data as! LightSaveLabel
            //: cell.fill(with: cellData)
            cell.satisfyAssociated(with: cellData)
            //: cell.sendBlock = { [weak self] in
            cell.sendBlock = { [weak self] in
                //: guard let self = self else {
                guard let self = self else {
                    //: return
                    return
                }
                //: var model = TalkingRoomGiftModel.init()
                var model = EvaluationMenuRate()
                //: model.gid = cellData.msgModel.msgInfo.wantGift.giftId
                model.gid = cellData.msgModel.msgInfo.wantGift.giftId
                //: self.func__sendGiftMsg(giftModel: model, giftNum: "1", cellData: cellData, fromStr: "wantGift")
                self.notMini(giftModel: model, giftNum: "1", cellData: cellData, fromStr: (notiCordAfterData.replacingOccurrences(of: "direct", with: "a") + String(appResourceString.suffix(5))))
            }
            //: return cell
            return cell
        }
        //: if data.isKind(of: BoxStack.self) {
        if data.isKind(of: BoxStack.self) {
            //: let cell = TalkingChatAudioMsgCell.init(style: .default, reuseIdentifier: TalkingChatAudioMsgCell.className())
            let cell = WhiteProcessor(style: .default, reuseIdentifier: WhiteProcessor.className())
            //: let cellData = data as! BoxStack
            let cellData = data as! BoxStack
            //: cell.fill(with: cellData)
            cell.satisfyAssociated(with: cellData)
            //: return cell
            return cell
        }

        //: if data.isKind(of: TalkingChatMatchMsgCellData.self) {
        if data.isKind(of: EfficiencyDraw.self) {
            //: let cell = TalkingChatMatchMsgCell.init(style: .default, reuseIdentifier: TalkingChatMatchMsgCell.className())
            let cell = DisplayOwner(style: .default, reuseIdentifier: DisplayOwner.className())
            //: let cellData = data as! TalkingChatMatchMsgCellData
            let cellData = data as! EfficiencyDraw
            //: if self.userWrap.uid.count > 0 {
            if self.userWrap.uid.count > 0 {
                //: cellData.msgModel.userInfoModel = self.userWrap
                cellData.msgModel.userInfoModel = self.userWrap
            }
            //: cell.fill(with: cellData)
            cell.satisfyAssociated(with: cellData)
            //: return cell
            return cell
        }

        //: if data.isKind(of: TalkingChatTipsMsgCellData.self) {
        if data.isKind(of: ToAdjust.self) {
            //: let cell = TalkingChatTipsMsgCell.init(style: .default, reuseIdentifier: TalkingChatTipsMsgCell.className())
            let cell = EagerSpeed(style: .default, reuseIdentifier: EagerSpeed.className())
            //: let cellData = data as! TalkingChatTipsMsgCellData
            let cellData = data as! ToAdjust
            //: cell.fill(with: cellData)
            cell.satisfyAssociated(with: cellData)
            //: return cell
            return cell
        }

        //: if data.isKind(of: TalkingChatSystemNotifJumpMsgCellData.self) {
        if data.isKind(of: SearchPublish.self) {
            //: let cell = TalkingChatSystemNotifJumpMsgCell.init(style: .default, reuseIdentifier: TalkingChatSystemNotifJumpMsgCell.className())
            let cell = SessionSave(style: .default, reuseIdentifier: SessionSave.className())
            //: let cellData = data as! TalkingChatSystemNotifJumpMsgCellData
            let cellData = data as! SearchPublish
            //: cell.fill(with: cellData)
            cell.satisfyAssociated(with: cellData)
            //: return cell
            return cell
        }

        //: return nil
        return nil
    }

    //: func messageController(_ controller: TUIMessageController!, onSelectMessageAvatar cell: BlackBy!) {
    func isolate(forDelay controller: DeployViewController!, user cell: BlackBy!) {
        //: guard self.isFrom != .LiveRoom,
        guard self.isFrom != .LiveRoom,
              //: conversationType != .system,
              conversationType != .system,
              //: String(cell.messageData?.msgModel.user.uid ?? 0) != MovementStructure.getCustomerServiceID() else { return }
              String(cell.messageData?.msgModel.user.uid ?? 0) != MovementStructure.cartoonConnection() else { return }

        //: if let uid = cell.messageData?.msgModel.user.uid {
        if let uid = cell.messageData?.msgModel.user.uid {
            //: OriginMediumWill.share.func__pushToUserDetailVC(uid: String(uid))
            OriginMediumWill.share.petAcrossAm(uid: String(uid))
        }
    }

    //: func messageController(_ controller: TUIMessageController!, onSelectMessageContent cell: BlackBy!) {
    func evaluation(_ controller: DeployViewController!, name cell: BlackBy!) {
        //: if cell .isKind(of: TalkingChatImageMsgCell.self) {
        if cell.isKind(of: PageFactor.self) { // 图片
            //: let imageCellData = cell.messageData as! TalkingChatImageMsgCellData
            let imageCellData = cell.messageData as! DelayComposition
            //: if imageCellData.msgModel.msgInfo.intimatePhoto.previewUrl.count > 0 {
            if imageCellData.msgModel.msgInfo.intimatePhoto.previewUrl.count > 0 { // 是私密照片
                // 已销毁的图片不能点击
                //: guard imageCellData.msgModel.msgInfo.intimatePhoto.burnStatus != 1 else { return }
                guard imageCellData.msgModel.msgInfo.intimatePhoto.burnStatus != 1 else { return }
                // 展示私密照片视图
                //: let vc = TalkingIntimatePhotoPreviewVC(imageCellData: imageCellData, toUid: self.msgView.targetID)
                let vc = NameBarProcessor(imageCellData: imageCellData, toUid: self.msgView.targetID)
                // 更改本地私密照片状态
                //: vc.statusBlock = { (model) in
                vc.statusBlock = { model in
                    //: self.msgView.changeIntimatePhotoMsg(imageCellData, photoModel: model)
                    self.msgView.response(imageCellData, hero: model)
                }
                // 送礼效果在当前页面处理
                //: vc.sendGiftResultBlock = { succeed, result, errorModel in
                vc.sendGiftResultBlock = { succeed, result, errorModel in
                    //: guard succeed else {
                    guard succeed else {
                        //: self.dealSendMsgError(errorCode: errorModel!.errorCode, errorStr: errorModel!.errorMsg, isGift: true, isResend: false, clickEvent: clickUnlockButton)
                        self.detailOff(errorCode: errorModel!.errorCode, errorStr: errorModel!.errorMsg, isGift: true, isResend: false, clickEvent: notiCapLocalMountStr)
                        //: return
                        return
                    }

                    //: self.func__insertGiftMessageWithExtral(extral: result as! Dictionary<String, Any?>, isFromInputView: false)
                    self.denseInmate(extral: result as! [String: Any?], isFromInputView: false)
                    //: let dataDict = result as! Dictionary<String, Any>
                    let dataDict = result as! [String: Any]
                    //: self.unlockPhotoOrVideoTool(dataDict: dataDict)
                    self.pin(dataDict: dataDict)
                }
                //: getNavigationController()?.pushViewController(vc, animated: true)
                aboveShadow()?.pushViewController(vc, animated: true)

                //: } else {
            } else { // 普通图片
                //: let libraryPath = NSSearchPathForDirectoriesInDomains(.cachesDirectory, .userDomainMask, true).first!
                let libraryPath = NSSearchPathForDirectoriesInDomains(.cachesDirectory, .userDomainMask, true).first!

                //: var filePath = (libraryPath as NSString).appendingPathComponent(imageFileName)
                var filePath = (libraryPath as NSString).appendingPathComponent(show_viewBottomName)
                //: filePath = (filePath as NSString).appendingPathComponent((imageCellData.imageUrl! as NSString).lastPathComponent)
                filePath = (filePath as NSString).appendingPathComponent((imageCellData.imageUrl! as NSString).lastPathComponent)

                //: var model = TalkingMomentPhotoModel.init()
                var model = AreaModel()
                //: if FileManager.default.fileExists(atPath: filePath) {
                if FileManager.default.fileExists(atPath: filePath) {
                    //: let data = NSData.init(contentsOfFile: filePath)
                    let data = NSData(contentsOfFile: filePath)
                    //: model.imagePic = UIImage(data: data! as Data)
                    model.imagePic = UIImage(data: data! as Data)
                    //: } else {
                } else {
                    //: if imageCellData.imageData != nil {
                    if imageCellData.imageData != nil {
                        //: model.imagePic = imageCellData.imageData
                        model.imagePic = imageCellData.imageData
                        //: } else {
                    } else {
                        //: model.imgUrl =  imageCellData.imageUrl
                        model.imgUrl = imageCellData.imageUrl
                    }
                }
                //: var currenModel = TalkingMomentModel.init()
                var currenModel = StatusModel()
                //: var tempArr: [TalkingMomentPhotoModel] = []
                var tempArr: [AreaModel] = []
                //: tempArr.append(model)
                tempArr.append(model)
                //: currenModel.pic = tempArr
                currenModel.pic = tempArr
                //: let vc = TalkingMomentPhotosVC.init(momentModel: currenModel, index: 0, type: .normal)
                let vc = QuantityegrityViewController(momentModel: currenModel, index: 0, type: .normal)
                //: getNavigationController()?.pushViewController(vc, animated: true)
                aboveShadow()?.pushViewController(vc, animated: true)
            }

            //: } else if cell.isKind(of: TalkingChatVideoMsgCell.self) {
        } else if cell.isKind(of: InfrastructureReflect.self) { // 视频
            //: let videoCellData = cell.messageData as! TalkingChatVideoMsgCellData
            let videoCellData = cell.messageData as! MeasurePaint
            //: let videoModel = videoCellData.msgModel.msgInfo.video
            let videoModel = videoCellData.msgModel.msgInfo.video
            //: if (videoModel.burnStatus == -1 && videoModel.lockStatus == -1 && videoModel.giftId == "0") { // 普通视频
            if videoModel.burnStatus == -1 && videoModel.lockStatus == -1 && videoModel.giftId == "0" { // 普通视频
                // 优先使用本地视频，没有再使用网络
                //: var videoPath = videoCellData.msgModel.msgInfo.customSend.localVideoUrl
                var videoPath = videoCellData.msgModel.msgInfo.customSend.localVideoUrl
                //: if videoPath.count == 0 || !FileManager.default.fileExists(atPath: videoPath) {
                if videoPath.count == 0 || !FileManager.default.fileExists(atPath: videoPath) {
                    //: videoPath = videoModel.videoUrl
                    videoPath = videoModel.videoUrl
                }

                //: let vc = TalkingMomentVideoVC.init(videoPath: videoPath)
                let vc = EqualGlobal(videoPath: videoPath)
                //: getNavigationController()?.pushViewController(vc, animated: true)
                aboveShadow()?.pushViewController(vc, animated: true)

                //: } else {
            } else { // 私密视频
                // 已销毁的视频不能点击
                //: guard videoModel.burnStatus != 1 else { return }
                guard videoModel.burnStatus != 1 else { return }
                // 预览私密视频
                //: let vc = TalkingIntimateVideoPreviewVC(videoCellData: videoCellData, toUid: self.msgView.targetID)
                let vc = SystemMomentum(videoCellData: videoCellData, toUid: self.msgView.targetID)
                // 更改本地私密视频状态
                //: vc.statusBlock = { (model) in
                vc.statusBlock = { model in
                    //: self.msgView.changeIntimateVideoMsg(videoCellData, videoModel: model)
                    self.msgView.undertake(videoCellData, label: model)
                }
                // 送礼效果在当前页面处理
                //: vc.sendGiftResultBlock = { succeed, result, errorModel in
                vc.sendGiftResultBlock = { succeed, result, errorModel in
                    //: guard succeed else {
                    guard succeed else {
                        //: self.dealSendMsgError(errorCode: errorModel!.errorCode, errorStr: errorModel!.errorMsg, isGift: true, isResend: false, clickEvent: clickUnlockButton)
                        self.detailOff(errorCode: errorModel!.errorCode, errorStr: errorModel!.errorMsg, isGift: true, isResend: false, clickEvent: notiCapLocalMountStr)
                        //: return
                        return
                    }

                    //: self.func__insertGiftMessageWithExtral(extral: result as! Dictionary<String, Any?>, isFromInputView: false)
                    self.denseInmate(extral: result as! [String: Any?], isFromInputView: false)
                    //: let dataDict = result as! Dictionary<String, Any>
                    let dataDict = result as! [String: Any]
                    //: self.unlockPhotoOrVideoTool(dataDict: dataDict)
                    self.pin(dataDict: dataDict)
                }
                //: getNavigationController()?.pushViewController(vc, animated: true)
                aboveShadow()?.pushViewController(vc, animated: true)
            }

            //: } else if cell.isKind(of: TalkingChatAudioMsgCell.self) {
        } else if cell.isKind(of: WhiteProcessor.self) {
            //: let audioCell = cell as! TalkingChatAudioMsgCell
            let audioCell = cell as! WhiteProcessor
            //: func__DidTouchVoiceCellNeedPlayWithCurrentModel(cellData: audioCell.cellData as! BoxStack, cellView: audioCell)
            element(cellData: audioCell.cellData as! BoxStack, cellView: audioCell)

            //: } else if cell.isKind(of: TalkingChatTextMsgCell.self) {
        } else if cell.isKind(of: StartEquipment.self) {
            //: let textCell = cell as! TalkingChatTextMsgCell
            let textCell = cell as! StartEquipment
            //: let textData = textCell.messageData
            let textData = textCell.messageData
            //: let dic = NeedSpecify.parseTXMessageData(data: (textData?.innerMessage.customElem.data)!)
            let dic = NeedSpecify.onStrategy(data: (textData?.innerMessage.customElem.data)!)
            //: guard let extraDic = dic["extra"] as? Dictionary<String, Any> else {
            guard let extraDic = dic[(appWenRegionData.replacingOccurrences(of: "constraint", with: "ra"))] as? [String: Any] else {
                //: return
                return
            }
            //: if extraDic.keys.contains("jumpKey") {
            if extraDic.keys.contains((String(showFinanceUrl.prefix(6)) + noti_thatTitle.replacingOccurrences(of: "submit", with: "y"))) {
                //: let truePersonAuth = extraDic["jumpKey"] as? String ?? ""
                let truePersonAuth = extraDic[(String(showFinanceUrl.prefix(6)) + noti_thatTitle.replacingOccurrences(of: "submit", with: "y"))] as? String ?? ""

                //: if let jumpModel = JSONDeserializer<TalkingMessageJumpModel>.deserializeFrom(dict: extraDic, designatedPath: nil) {
                if let jumpModel = JSONDeserializer<PopModel>.deserializeFrom(dict: extraDic, designatedPath: nil) {
                    //: OriginMediumWill.share.func__actionPushForMessage(jumpModel: jumpModel)
                    OriginMediumWill.share.brokenDisabled(jumpModel: jumpModel)
                }
            }

            //: let extraJson = JSON(extraDic)
            let extraJson = JSON(extraDic)
            //: if  dic["rYMsgType"] as? String == "GJ:CallCustom" {  // 音视频通话消息
            if dic[(String(notiPartyValue) + String(mainRelativePlatValue))] as? String == (String(notiTranslateMessage.prefix(7)) + String(user_mendName)) { // 音视频通话消息
                //: if extraJson["msgInfo"]["logType"].intValue == 1 {
                if extraJson[(show_transactionCountoStr.replacingOccurrences(of: "fatal", with: "m") + String(appRetMessage.suffix(6)))][(String(kPainterFragmentFormat) + mainCertainMsg.lowercased())].intValue == 1 {
                    //: checkAndCallVoice()
                    advertisingOfTap()
                    //: } else {
                } else {
                    //: checkAndCallVideo()
                    fill()
                }
            }

            //: } else if cell.isKind(of: TalkingChatTipsMsgCell.self) {
        } else if cell.isKind(of: EagerSpeed.self) {
            //: let tipCell = cell as! TalkingChatTipsMsgCell
            let tipCell = cell as! EagerSpeed
            //: let tipData = tipCell.messageData
            let tipData = tipCell.messageData
            //: let dic = NeedSpecify.parseTXMessageData(data: (tipData?.innerMessage.customElem.data)!)
            let dic = NeedSpecify.onStrategy(data: (tipData?.innerMessage.customElem.data)!)
            //: guard let extraDic = dic["extra"] as? Dictionary<String, Any> else {
            guard let extraDic = dic[(appWenRegionData.replacingOccurrences(of: "constraint", with: "ra"))] as? [String: Any] else {
                //: return
                return
            }
            //: if extraDic.keys.contains("jumpKey") {
            if extraDic.keys.contains((String(showFinanceUrl.prefix(6)) + noti_thatTitle.replacingOccurrences(of: "submit", with: "y"))) {
                //: if let jumpModel = JSONDeserializer<TalkingMessageJumpModel>.deserializeFrom(dict: extraDic, designatedPath: nil) {
                if let jumpModel = JSONDeserializer<PopModel>.deserializeFrom(dict: extraDic, designatedPath: nil) {
                    //: OriginMediumWill.share.func__actionPushForMessage(jumpModel: jumpModel)
                    OriginMediumWill.share.brokenDisabled(jumpModel: jumpModel)
                }
            }
            //: } else if cell.isKind(of: TalkingChatMatchMsgCell.self) {
        } else if cell.isKind(of: DisplayOwner.self) {
            //: OriginMediumWill.share.func__pushToUserDetailVC(uid: msgView.targetID)
            OriginMediumWill.share.petAcrossAm(uid: msgView.targetID)
        }
    }

    //: func messageController(_ controller: TUIMessageController!, onRetryMessageContent cell: BlackBy!) {
    func stackHalf(_ controller: DeployViewController!, thanMobile cell: BlackBy!) {
        //: var cellData: TalkingChatMsgBaseCellData?
        var cellData: DisplayOrigin?
        //: if cell .isKind(of: TalkingChatImageMsgCell.self) {
        if cell.isKind(of: PageFactor.self) {
            //: let imageCell = cell as! TalkingChatImageMsgCell
            let imageCell = cell as! PageFactor
            //: cellData = imageCell.cellData as! TalkingChatImageMsgCellData
            cellData = imageCell.cellData as! DelayComposition
            //: } else if cell.isKind(of: TalkingChatVideoMsgCell.self) {
        } else if cell.isKind(of: InfrastructureReflect.self) {
            //: let videoCell = cell as! TalkingChatVideoMsgCell
            let videoCell = cell as! InfrastructureReflect
            //: cellData = videoCell.cellData as! TalkingChatVideoMsgCellData
            cellData = videoCell.cellData as! MeasurePaint
            //: } else if cell.isKind(of: TalkingChatAudioMsgCell.self) {
        } else if cell.isKind(of: WhiteProcessor.self) {
            //: let audioCell = cell as! TalkingChatAudioMsgCell
            let audioCell = cell as! WhiteProcessor
            //: cellData = audioCell.cellData as! BoxStack
            cellData = audioCell.cellData as! BoxStack
            //: } else if cell.isKind(of: TalkingChatTextMsgCell.self) {
        } else if cell.isKind(of: StartEquipment.self) {
            //: let textCell = cell as! TalkingChatTextMsgCell
            let textCell = cell as! StartEquipment
            //: cellData = textCell.cellData as! ExceptionKindLocationCreate
            cellData = textCell.cellData as! ExceptionKindLocationCreate
        }
        //: if cellData != nil {
        if cellData != nil {
            //: let reCellData: AlongCrucial? = TUISocialChatManager.handleTXMessageReSend(withData: cellData!)
            let reCellData: AlongCrucial? = IndependentObserve.despiteCur(withData: cellData!)
            //: if reCellData != nil {
            if reCellData != nil {
                //: cell.messageData?.msgModel = reCellData!.msgModel
                cell.messageData?.msgModel = reCellData!.msgModel
                //: cell.messageData?.status = .Msg_Status_Fail
                cell.messageData?.status = .Msg_Status_Fail
                //: if reCellData!.isKind(of: TalkingChatVideoMsgCellData.self) {
                if reCellData!.isKind(of: MeasurePaint.self) { // 视频重发
                    //: func__sendVideoMsg(msgData: cell.messageData, sendModel: cell.messageData!.msgModel.msgInfo.customSend)
                    frameValid(msgData: cell.messageData, sendModel: cell.messageData!.msgModel.msgInfo.customSend)
                    //: } else {
                } else {
                    //: func__sendPriveteMsg(msgData: cell.messageData!, reSend: true)
                    join(msgData: cell.messageData!, reSend: true)
                }
            }
        }
    }

    /// 回复消息
    //: func messageController(_ controller: TUIMessageController!, onReplyMessage cellData: AlongCrucial!) {
    func sound(_ controller: DeployViewController!, aboveBlackSmall cellData: AlongCrucial!) {
        //: msgInputView.quoteModel = TalkingPrivateChatManager.handleReplyMsg(cellData: cellData, targetId: userWrap.uid)
        msgInputView.quoteModel = InvisibleJob.should(cellData: cellData, targetId: userWrap.uid)
    }

    /// 点击“引用”消息
    //: func messageController(_ controller: TUIMessageController!, onClickReplyMessage cellData: AlongCrucial!) {
    func activity(_ controller: DeployViewController!, might cellData: AlongCrucial!) {
        //: TalkingPrivateChatManager.handleClickReplyMsg(cellData: cellData, targetId: userWrap.uid)
        InvisibleJob.moreHost(cellData: cellData, targetId: userWrap.uid)
    }

    /// 实时更新聊天双方头像
    /// - Parameter data: 腾讯数据
    /// - Returns: 腾讯数据
    //: private func changeChatHeadPic(data: AlongCrucial) -> AlongCrucial {
    private func observer(data: AlongCrucial) -> AlongCrucial {
        //: let isSelf = data.msgModel.user.uid == Int(MarginExamineer.share.loginUserMode.userID)
        let isSelf = data.msgModel.user.uid == Int(MarginExamineer.share.loginUserMode.userID)
        //: if isSelf {
        if isSelf {
            //: if MarginExamineer.share.loginUserMode.headPic?.count ?? 0 > 0 {
            if MarginExamineer.share.loginUserMode.headPic?.count ?? 0 > 0 {
                //: data.msgModel.user.headPic = MarginExamineer.share.loginUserMode.headPic!
                data.msgModel.user.headPic = MarginExamineer.share.loginUserMode.headPic!
                //: data.msgModel.user.headPicFrame = MarginExamineer.share.loginUserMode.headPicFrame
                data.msgModel.user.headPicFrame = MarginExamineer.share.loginUserMode.headPicFrame
            }
            //: } else {
        } else {
            //: if self.userWrap.headPic.count > 0 {
            if self.userWrap.headPic.count > 0 {
                //: data.msgModel.user.headPic = self.userWrap.headPic
                data.msgModel.user.headPic = self.userWrap.headPic
                //: data.msgModel.user.headPicFrame = self.userWrap.headPicFrame
                data.msgModel.user.headPicFrame = self.userWrap.headPicFrame
            }
        }
        //: return data
        return data
    }
}

// MARK: - 发送消息

//: extension TalkingPrivateChatController {
extension ViaNative {
    /// 发送问题回复消息 -- 回答
    /// - Parameter model: 回答问题模型
    //: func func_sendQuestionQuickReplayMsg(_ model: QuestionQuickReplayModel) {
    func satisfy(_ model: StructureModel) {
        //: let msgInfo = ["content": model.content, "msgType": "txt"]
        let msgInfo = [String(bytes: k_tipData.reversed(), encoding: .utf8)!: model.content, String(bytes: app_plainLeadingCorrectString.map{towardInvite(ease: $0)}, encoding: .utf8)!: (const_bindMessage.lowercased())]
        //: let paramsDic = ["message": model.content, "replayId": model.replayId, "type": "TxtMsg"] as [String : Any]
        let paramsDic = [String(bytes: user_gravityMsg.map{hungReceiver(dismiss: $0)}, encoding: .utf8)!: model.content, String(bytes: notiAnnouncementMessage.map{thenObject(wen: $0)}, encoding: .utf8)!: model.replayId, (String(constYourselfData)): (user_needKey.replacingOccurrences(of: "similar", with: "T") + String(mainProvideData))] as [String: Any]
        //: let dic = ["msgInfo": msgInfo, "params": paramsDic]
        let dic = [(show_transactionCountoStr.replacingOccurrences(of: "fatal", with: "m") + String(appRetMessage.suffix(6))): msgInfo, String(bytes: user_keyWantGreenFormat.map{cookieResolve(week: $0)}, encoding: .utf8)!: paramsDic]
        //: if let cellData = TUISocialChatManager.handleTXMessageSend(withExtral: dic, isSender: true, isTip: false) {
        if let cellData = IndependentObserve.on(withExtral: dic, isSender: true, isTip: false) {
            //: self.func__sendPriveteMsg(msgData: cellData, reSend: false)
            self.join(msgData: cellData, reSend: false)
        }
    }

    /// 发送一键打招呼消息
    //: func func_sendSayHiMsg() {
    func pending() {
        //: let msgInfo = ["msgType": "txt"]
        let msgInfo = [String(bytes: app_plainLeadingCorrectString.map{towardInvite(ease: $0)}, encoding: .utf8)!: (const_bindMessage.lowercased())]
        //: let paramsDic = ["type": "MultiQuickMsg"]
        let paramsDic = [(String(constYourselfData)): (String(app_careStr.suffix(7)) + String(userPrivacyKey.suffix(6)))]
        //: let dic = ["msgInfo": msgInfo, "params": paramsDic]
        let dic = [(show_transactionCountoStr.replacingOccurrences(of: "fatal", with: "m") + String(appRetMessage.suffix(6))): msgInfo, String(bytes: user_keyWantGreenFormat.map{cookieResolve(week: $0)}, encoding: .utf8)!: paramsDic]
        //: if let cellData = TUISocialChatManager.handleTXMessageSend(withExtral: dic, isSender: true, isTip: false) {
        if let cellData = IndependentObserve.on(withExtral: dic, isSender: true, isTip: false) {
            //: self.func__sendPriveteMsg(msgData: cellData, reSend: false)
            self.join(msgData: cellData, reSend: false)
        }
    }

    /// 发送快速打招呼消息
    //: func func_sendQuickGreetingMsg() {
    func noDisable() {
        //: let msgInfo = ["msgType": "txt"]
        let msgInfo = [String(bytes: app_plainLeadingCorrectString.map{towardInvite(ease: $0)}, encoding: .utf8)!: (const_bindMessage.lowercased())]
        //: let paramsDic = ["type": "QuickMsg"]
        let paramsDic = [(String(constYourselfData)): (String(app_relateTitle.suffix(7)) + main_againstValue.replacingOccurrences(of: "big", with: "g"))]
        //: let dic = ["msgInfo": msgInfo, "params": paramsDic]
        let dic = [(show_transactionCountoStr.replacingOccurrences(of: "fatal", with: "m") + String(appRetMessage.suffix(6))): msgInfo, String(bytes: user_keyWantGreenFormat.map{cookieResolve(week: $0)}, encoding: .utf8)!: paramsDic]
        //: if let cellData = TUISocialChatManager.handleTXMessageSend(withExtral: dic, isSender: true, isTip: false) {
        if let cellData = IndependentObserve.on(withExtral: dic, isSender: true, isTip: false) {
            //: self.func__sendPriveteMsg(msgData: cellData, reSend: false)
            self.join(msgData: cellData, reSend: false)
        }
    }

    /// 发送一键打招呼消息
    /// - Parameters:
    ///   - textDict: 文本消息
    ///   - audioDict: 语音消息
    //: private func func_sendQuickGreetingMsg(textDict: [String: Any]?, audioDict: [String: Any]?) {
    private func goldKey(textDict: [String: Any]?, audioDict: [String: Any]?) {
        //: if textDict?.count ?? 0 > 0 {
        if textDict?.count ?? 0 > 0 { // 发送文本消息
            //: let content = textDict?["content"]
            let content = textDict?[String(bytes: k_tipData.reversed(), encoding: .utf8)!]
            //: let greetId = textDict?["id"]
            let greetId = textDict?["id"]
            //: let msgInfo = ["content": content, "txtId": greetId, "msgType": "txt"]
            let msgInfo = [String(bytes: k_tipData.reversed(), encoding: .utf8)!: content, (String(notiNeverJudgeStandardValue.suffix(5))): greetId, String(bytes: app_plainLeadingCorrectString.map{towardInvite(ease: $0)}, encoding: .utf8)!: (const_bindMessage.lowercased())]
            //: let paramsDic = ["message": content, "txtId": greetId, "type": "TxtMsg"]
            let paramsDic = [String(bytes: user_gravityMsg.map{hungReceiver(dismiss: $0)}, encoding: .utf8)!: content, (String(notiNeverJudgeStandardValue.suffix(5))): greetId, (String(constYourselfData)): (user_needKey.replacingOccurrences(of: "similar", with: "T") + String(mainProvideData))]
            //: let dic = ["msgInfo": msgInfo, "params": paramsDic]
            let dic = [(show_transactionCountoStr.replacingOccurrences(of: "fatal", with: "m") + String(appRetMessage.suffix(6))): msgInfo, String(bytes: user_keyWantGreenFormat.map{cookieResolve(week: $0)}, encoding: .utf8)!: paramsDic]
            //: if let cellData = TUISocialChatManager.handleTXMessageSend(withExtral: dic, isSender: true, isTip: false) {
            if let cellData = IndependentObserve.on(withExtral: dic, isSender: true, isTip: false) {
                //: self.func__sendPriveteMsg(msgData: cellData, reSend: false)
                self.join(msgData: cellData, reSend: false)
            }
        }

        //: if audioDict?.count ?? 0 > 0 {
        if audioDict?.count ?? 0 > 0 { // 发送语音消息
            //: VoiceDownloadTaskManager.shared.addAudioGreetDownLoadTask(audioDict!) { model in
            UserFractionRead.shared.set(audioDict!) { model in
                //: if model.taskTypeId != .oneKeyGreet {
                if model.taskTypeId != .oneKeyGreet {
                    //: return
                    return
                }
                //: if model.taskStatus != .finish {
                if model.taskStatus != .finish {
                    //: DispatchQueue.main.async {
                    DispatchQueue.main.async {
                        //: self.func__showStatusBarErrorMsg(showMsg: "Voice hello download failed. Procedure")
                        self.exitMarker(showMsg: String(bytes: notiJobStr.map{$0^23}, encoding: .utf8)!)
                        //: TalkingPrivateChatManager.chat_changeSaveGreetData(toUserId: self.msgView.targetID, isHave: false)
                        InvisibleJob.control(toUserId: self.msgView.targetID, isHave: false)
                    }
                    //: return
                    return
                }
                //: let greetId = audioDict?["id"]
                let greetId = audioDict?["id"]
                //: let length = audioDict?["length"]
                let length = audioDict?[(String(const_appealData))]
                //: let msgInfo = ["msgType": "audio",
                let msgInfo = [String(bytes: app_plainLeadingCorrectString.map{towardInvite(ease: $0)}, encoding: .utf8)!: (k_automaticallyTerrificData.lowercased()),
                               //: "audioId": greetId,
                               (String(k_selectedString.suffix(7))): greetId,
                               //: "audioLength": length,
                               String(bytes: show_obtainString.map{atKing(operation: $0)}, encoding: .utf8)!: length,
                               //: "audioSandbox": model.unZipFilePath,
                               (String(main_othersGalleryValue) + String(showMatchValue)): model.unZipFilePath,
                               //: "senderId": MarginExamineer.share.loginUserMode.userID]
                               String(bytes: noti_triggerTitle.reversed(), encoding: .utf8)!: MarginExamineer.share.loginUserMode.userID]

                //: let paramsDic = ["audioId": greetId,
                let paramsDic = [(String(k_selectedString.suffix(7))): greetId,
                                 //: "audioLength": length,
                                 String(bytes: show_obtainString.map{atKing(operation: $0)}, encoding: .utf8)!: length,
                                 //: "audioSandbox": model.unZipFilePath,
                                 (String(main_othersGalleryValue) + String(showMatchValue)): model.unZipFilePath,
                                 //: "type": "AudioMsg"]
                                 (String(constYourselfData)): (String(showLastSpellFormat.prefix(5)) + String(notiCrushId))]
                //: let dic = ["msgInfo": msgInfo, "params": paramsDic]
                let dic = [(show_transactionCountoStr.replacingOccurrences(of: "fatal", with: "m") + String(appRetMessage.suffix(6))): msgInfo, String(bytes: user_keyWantGreenFormat.map{cookieResolve(week: $0)}, encoding: .utf8)!: paramsDic]
                //: if let cellData = TUISocialChatManager.handleTXMessageSend(withExtral: dic, isSender: true, isTip: false) {
                if let cellData = IndependentObserve.on(withExtral: dic, isSender: true, isTip: false) {
                    //: self.func__sendPriveteMsg(msgData: cellData, reSend: false)
                    self.join(msgData: cellData, reSend: false)
                }
            }
        }
    }

    /// 发送文本消息
    /// - Parameter msgStr: 文本
    //: internal func func__sendTextMsg(msgStr: String, quoteModel: AbTalkingChatMsgQuoteModel?) {
    func now(msgStr: String, quoteModel: LocationModel?) {
        //: let temStr = msgStr.trimmingCharacters(in: CharacterSet.whitespacesAndNewlines)
        let temStr = msgStr.trimmingCharacters(in: CharacterSet.whitespacesAndNewlines)
        //: if temStr.count == 0 {
        if temStr.count == 0 {
            //: func__showStatusBarErrorMsg(showMsg: "Can't send blank message".localized)
            exitMarker(showMsg: (String(user_albumMsg) + String(notiTimingMultipleString) + String(user_eagerString)).localized)
            //: return
            return
        }

        //: let msgInfo = ["content": temStr, "msgType": "txt"]
        let msgInfo = [String(bytes: k_tipData.reversed(), encoding: .utf8)!: temStr, String(bytes: app_plainLeadingCorrectString.map{towardInvite(ease: $0)}, encoding: .utf8)!: (const_bindMessage.lowercased())]
        //: var paramsDic = ["message": temStr, "type": "TxtMsg"]
        var paramsDic = [String(bytes: user_gravityMsg.map{hungReceiver(dismiss: $0)}, encoding: .utf8)!: temStr, (String(constYourselfData)): (user_needKey.replacingOccurrences(of: "similar", with: "T") + String(mainProvideData))]
        //: var dic:[String: Any] = ["msgInfo": msgInfo]
        var dic: [String: Any] = [(show_transactionCountoStr.replacingOccurrences(of: "fatal", with: "m") + String(appRetMessage.suffix(6))): msgInfo]

        //: if let tempQuoteModel = quoteModel{
        if let tempQuoteModel = quoteModel {
            //: paramsDic["quoteMsgId"] = tempQuoteModel.msgId
            paramsDic[(String(constUnlessKingKey.suffix(7)) + String(data_pointThreeKey.prefix(3)))] = tempQuoteModel.msgId
            //: dic["quoteMsgInfo"] = tempQuoteModel.toDictionary()
            dic[(notiDescriptionPolicyTitle.replacingOccurrences(of: "just", with: "t") + "eMsgInfo")] = tempQuoteModel.toDictionary()
        }
        //: dic["params"] = paramsDic
        dic[String(bytes: user_keyWantGreenFormat.map{cookieResolve(week: $0)}, encoding: .utf8)!] = paramsDic

        //: if let cellData = TUISocialChatManager.handleTXMessageSend(withExtral: dic, isSender: true, isTip: false) {
        if let cellData = IndependentObserve.on(withExtral: dic, isSender: true, isTip: false) {
            //: cellData.msgModel.quoteMsgInfo = quoteModel ?? AbTalkingChatMsgQuoteModel()
            cellData.msgModel.quoteMsgInfo = quoteModel ?? LocationModel()
            //: func__sendPriveteMsg(msgData: cellData, reSend: false)
            join(msgData: cellData, reSend: false)
        }
    }

    /// 发送图片消息
    /// - Parameters:
    ///   - img: 图片
    ///   - sendModel: 本地数据模型
    //: private func func__sendPictureMsg(img: UIImage, sendModel: AbTalkingChatMsgCustomSendModel) {
    private func dealCamera(img: UIImage, sendModel: UserStageModel) {
        //: let imagePath = TalkingChatImageMsgCellData.getChatImagePath(image: img)
        let imagePath = DelayComposition.product(image: img)
        // 缓存本地的消息
        //: let sendDict = sendModel.toDictionary() ?? [:]
        let sendDict = sendModel.toDictionary() ?? [:]
        //: let msgInfo: [String: Any] = ["contentType": "ImgMsg", "msgType": "img", "imageUri": imagePath, "customSend": sendDict]
        let msgInfo: [String: Any] = [String(bytes: show_warningPath.map{mobileAdmin(tell: $0)}, encoding: .utf8)!: (String(userUpTitle.prefix(6))), String(bytes: app_plainLeadingCorrectString.map{towardInvite(ease: $0)}, encoding: .utf8)!: (kPublisherMsg.replacingOccurrences(of: "create", with: "im")), String(bytes: constMpValue.map{chartAdminAway(clean: $0)}, encoding: .utf8)!: imagePath, String(bytes: user_specifyRemindData.map{programSame(wed: $0)}, encoding: .utf8)!: sendDict]
        // 接口参数
        //: let paramsDic: [String: Any] = ["pic": imagePath,
        let paramsDic: [String: Any] = [(app_commonUrl.replacingOccurrences(of: "pressure", with: "p")): imagePath,
                                        //: "type": "ImgMsg",
                                        (String(constYourselfData)): (String(userUpTitle.prefix(6))),
                                        //: "isBurn": sendModel.isBurn,
                                        String(bytes: main_maybeSlidePath.reversed(), encoding: .utf8)!: sendModel.isBurn,
                                        //: "giftId": sendModel.giftId]
                                        (String(dataBuildValue.prefix(6))): sendModel.giftId]
        //: let dic = ["msgInfo": msgInfo, "params": paramsDic]
        let dic = [(show_transactionCountoStr.replacingOccurrences(of: "fatal", with: "m") + String(appRetMessage.suffix(6))): msgInfo, String(bytes: user_keyWantGreenFormat.map{cookieResolve(week: $0)}, encoding: .utf8)!: paramsDic]
        //: if let cellData = TUISocialChatManager.handleTXMessageSend(withExtral: dic, isSender: true, isTip: false) {
        if let cellData = IndependentObserve.on(withExtral: dic, isSender: true, isTip: false) {
            //: func__sendPriveteMsg(msgData: cellData, reSend: false)
            join(msgData: cellData, reSend: false)
        }
    }

    /// 发送视频消息
    /// - Parameters:
    ///   - msgData: 发送视频cellData；nil：上传流程，有值 ：发送流程
    ///   - sendModel: 视频消息自定义本地模型
    //: private func func__sendVideoMsg(msgData: AlongCrucial?, sendModel: AbTalkingChatMsgCustomSendModel) {
    private func frameValid(msgData: AlongCrucial?, sendModel: UserStageModel) {
        // 缓存本地的消息
        //: let videoDict = sendModel.toDictionary() ?? [:]
        let videoDict = sendModel.toDictionary() ?? [:]
        //: let msgInfo: [String: Any] = ["contentType": "VideoMsg", "msgType": "video", "customSend": videoDict]
        let msgInfo: [String: Any] = [String(bytes: show_warningPath.map{mobileAdmin(tell: $0)}, encoding: .utf8)!: (String(app_uponId.suffix(8))), String(bytes: app_plainLeadingCorrectString.map{towardInvite(ease: $0)}, encoding: .utf8)!: (showProperlyPath.replacingOccurrences(of: "sample", with: "e")), String(bytes: user_specifyRemindData.map{programSame(wed: $0)}, encoding: .utf8)!: videoDict]
        // 接口参数
        //: let paramsDic: [String: Any] = ["type": "VideoMsg",
        let paramsDic: [String: Any] = [(String(constYourselfData)): (String(app_uponId.suffix(8))),
                                        //: "coverImg": sendModel.coverImg,
                                        String(bytes: dataBoardKey.map{$0^157}, encoding: .utf8)!: sendModel.coverImg,
                                        //: "videoUrl": sendModel.videoUrl,
                                        String(bytes: main_memoryBeanUrl.reversed(), encoding: .utf8)!: sendModel.videoUrl,
                                        //: "duration": sendModel.duration,
                                        String(bytes: user_dismissUrl.map{weightAcross(running: $0)}, encoding: .utf8)!: sendModel.duration,
                                        //: "isBurn": sendModel.isBurn,
                                        String(bytes: main_maybeSlidePath.reversed(), encoding: .utf8)!: sendModel.isBurn,
                                        //: "giftId": sendModel.giftId]
                                        (String(dataBuildValue.prefix(6))): sendModel.giftId]
        //: let dict = ["msgInfo": msgInfo, "params": paramsDic]
        let dict = [(show_transactionCountoStr.replacingOccurrences(of: "fatal", with: "m") + String(appRetMessage.suffix(6))): msgInfo, String(bytes: user_keyWantGreenFormat.map{cookieResolve(week: $0)}, encoding: .utf8)!: paramsDic]

        // 压缩成功 ---> 创建cellData ---> 上传视频
        //: guard let cellData = msgData else {
        guard let cellData = msgData else {
            //: if let uploadCellData = TUISocialChatManager.handleTXMessageSend(withExtral: dict, isSender: true, isTip: false) {
            if let uploadCellData = IndependentObserve.on(withExtral: dict, isSender: true, isTip: false) {
                //: self.msgView.sendCustomMessage(uploadCellData)
                self.msgView.normSave(uploadCellData)
                //: self.cos_UploadVideo(msgData: uploadCellData, sendModel: sendModel)
                self.undertakeIn(msgData: uploadCellData, sendModel: sendModel)
            }
            //: return
            return
        }

        // 失败重试，展示加载动画
        //: if cellData.status != .Msg_Status_Sending_2 {
        if cellData.status != .Msg_Status_Sending_2 {
            //: self.msgView.changeCustomMsg(cellData, status: .Msg_Status_Sending_2)
            self.msgView.estimated(to: cellData, enhance: .Msg_Status_Sending_2)
        }

        // 失败重试上传视频流程
        //: guard sendModel.videoUrl.count > 0 else {
        guard sendModel.videoUrl.count > 0 else {
            //: self.cos_UploadVideo(msgData: cellData, sendModel: sendModel)
            self.undertakeIn(msgData: cellData, sendModel: sendModel)
            //: return
            return
        }

        // 修改本地消息数据，调用发送视频接口
        //: self.msgView.modifyCustomMessageData(withExtral: dict, cellData: cellData)
        self.msgView.regulate(of: dict, join: cellData)
        //: func__sendPriveteMsg(msgData: cellData, reSend: false)
        join(msgData: cellData, reSend: false)
    }

    /// 发送语音
    /// - Parameters:
    ///   - voicePath: 语音路径
    ///   - voiceTime: 时间
    //: func func__sendVoiceMsg(voicePath: String, voiceTime: Int) {
    func displayGate(voicePath: String, voiceTime: Int) {
        //: let msgInfo = ["msgType": "audio",
        let msgInfo = [String(bytes: app_plainLeadingCorrectString.map{towardInvite(ease: $0)}, encoding: .utf8)!: (k_automaticallyTerrificData.lowercased()),
                       //: "audioLength": "\(voiceTime)",
                       String(bytes: show_obtainString.map{atKing(operation: $0)}, encoding: .utf8)!: "\(voiceTime)",
                       //: "audioSandbox": voicePath,
                       (String(main_othersGalleryValue) + String(showMatchValue)): voicePath,
                       //: "senderId": MarginExamineer.share.loginUserMode.userID]
                       String(bytes: noti_triggerTitle.reversed(), encoding: .utf8)!: MarginExamineer.share.loginUserMode.userID]

        //: let paramsDic = ["audioSandbox": voicePath,
        let paramsDic = [(String(main_othersGalleryValue) + String(showMatchValue)): voicePath,
                         //: "type": "AudioMsg",
                         (String(constYourselfData)): (String(showLastSpellFormat.prefix(5)) + String(notiCrushId)),
                         //: "len": "\(voiceTime)"]
                         String(bytes: const_spreadMutualMessage.map{$0^168}, encoding: .utf8)!: "\(voiceTime)"]
        //: let dic = ["msgInfo": msgInfo, "params": paramsDic]
        let dic = [(show_transactionCountoStr.replacingOccurrences(of: "fatal", with: "m") + String(appRetMessage.suffix(6))): msgInfo, String(bytes: user_keyWantGreenFormat.map{cookieResolve(week: $0)}, encoding: .utf8)!: paramsDic]
        //: if let cellData = TUISocialChatManager.handleTXMessageSend(withExtral: dic, isSender: true, isTip: false) {
        if let cellData = IndependentObserve.on(withExtral: dic, isSender: true, isTip: false) {
            //: func__sendPriveteMsg(msgData: cellData, reSend: false)
            join(msgData: cellData, reSend: false)
        }
    }

    /// 发送消息
    /// - Parameters:
    ///   - msgData: 消息数据结构
    ///   - reSend: 是否重发【目前未使用该字段】
    //: private func func__sendPriveteMsg(msgData: AlongCrucial, reSend: Bool) {
    private func join(msgData: AlongCrucial, reSend: Bool) {
        //: let reachability = try? Reachability()
        let reachability = try? Reachability()
        //: if reachability?.connection == .unavailable {
        if reachability?.connection == .unavailable {
            //: func__showStatusBarErrorMsg(showMsg: kNetErrorMsg)
            exitMarker(showMsg: kRequestPath)
            //: return
            return
        }

        //: var params: Dictionary<String, Any> = msgData.msgModel.params as! Dictionary<String, Any>
        var params: [String: Any] = msgData.msgModel.params as! [String: Any]
        //: params["toUid"] = self.msgView.targetID
        params[(String(dataUndertakeRecoverBeforePath.prefix(5)))] = self.msgView.targetID

        // 发送视频消息，索要礼物消息，快速打招呼消息，不在此处插入
        //: if msgData.isKind(of: TalkingChatVideoMsgCellData.self) == false && msgData.isKind(of: LightSaveLabel.self) == false && params["type"] as? String != "QuickMsg" && params["type"] as? String != "MultiQuickMsg" {
        if msgData.isKind(of: MeasurePaint.self) == false, msgData.isKind(of: LightSaveLabel.self) == false, params[(String(constYourselfData))] as? String != (String(app_relateTitle.suffix(7)) + main_againstValue.replacingOccurrences(of: "big", with: "g")), params[(String(constYourselfData))] as? String != (String(app_careStr.suffix(7)) + String(userPrivacyKey.suffix(6))) {
            //: self.msgView.sendCustomMessage(msgData)
            self.msgView.normSave(msgData)
        }
//        let recordTime = NeedSpecify.getMessageInsertTime()

        //: if params.keys .contains("pic") { // 图片
        if params.keys.contains((app_commonUrl.replacingOccurrences(of: "pressure", with: "p"))) { // 图片
            //: let imageStr: String = params["pic"] as! String
            let imageStr: String = params[(app_commonUrl.replacingOccurrences(of: "pressure", with: "p"))] as! String
            //: do {
            do {
                //: let data: Data = try Data.init(contentsOf: URL.init(fileURLWithPath: imageStr))
                let data: Data = try Data(contentsOf: URL(fileURLWithPath: imageStr))
                //: params["pic"] = data
                params[(app_commonUrl.replacingOccurrences(of: "pressure", with: "p"))] = data
                //: } catch {
            } catch {
                //: printLog(message: error)
                printLog(message: error)
            }
        }
        //: let type  = params["type"] as? String
        let type = params[(String(constYourselfData))] as? String
        //: if type == "AudioMsg" { // 语音
        if type == (String(showLastSpellFormat.prefix(5)) + String(notiCrushId)) { // 语音
            //: if params.keys.contains("audioSandbox") {
            if params.keys.contains((String(main_othersGalleryValue) + String(showMatchValue))) {
                //: var path =  params["audioSandbox"] as! String
                var path = params[(String(main_othersGalleryValue) + String(showMatchValue))] as! String
                //: params.removeValue(forKey: "audioSandbox")
                params.removeValue(forKey: (String(main_othersGalleryValue) + String(showMatchValue)))

                //: let theFileName = (path as NSString).lastPathComponent
                let theFileName = (path as NSString).lastPathComponent

                //: if path.contains("Documents/User/voice/") {
                if path.contains((String(showBackName.prefix(6)) + "nts/Us" + String(kMigrationLogicId.suffix(3)) + String(app_coordinatorSubjectMountMessage.suffix(6)))) {
                    //: path = Talking_Chat_voice_SandboxPath + theFileName
                    path = appRestrictionSquareString + theFileName
                    //: } else if(path.contains("Documents/User/record/")) {
                } else if path.contains((String(notiStrokeId) + show_portDelicateMessage.replacingOccurrences(of: "eager", with: "/") + String(data_rapidBeforeId))) {
                    //: path = Talking_Chat_record_SandboxPath + theFileName
                    path = kNameureAcceptData + theFileName
                }

                //: let audioId = params["audioId"] ?? "0"
                let audioId = params[(String(k_selectedString.suffix(7)))] ?? "0"
                //: if  (audioId as! NSString).integerValue == 0 {
                if (audioId as! NSString).integerValue == 0 {
                    //: do {
                    do {
                        //: let voiceData = try Data.init(contentsOf: URL.init(fileURLWithPath: path))
                        let voiceData = try Data(contentsOf: URL(fileURLWithPath: path))
                        //: params["audio"] = voiceData
                        params[(k_automaticallyTerrificData.lowercased())] = voiceData
                        //: } catch {
                    } catch {}
                }
            }
        }

        // 发送消息接口
        //: TalkingChatRequestTool.req_SendPrivete(param: params, completion: { succeed, result, errorModel in
        PriorMedia.remind(param: params, completion: { succeed, result, errorModel in
            //: if !succeed {
            if !succeed {
                //: if type == "QuickMsg" || type == "MultiQuickMsg" {
                if type == (String(app_relateTitle.suffix(7)) + main_againstValue.replacingOccurrences(of: "big", with: "g")) || type == (String(app_careStr.suffix(7)) + String(userPrivacyKey.suffix(6))) {
                    //: self.QuickEndBlock?(false, errorModel?.errorCode ?? 0)
                    self.QuickEndBlock?(false, errorModel?.errorCode ?? 0)
                }
                //: self.msgView.changeCustomMsg(msgData, msgId: nil, msgIncome: NSNumber(value: 0), status: .Msg_Status_Fail)
                self.msgView.functional(msgData, tillEffectDensity: nil, flushBorder: NSNumber(value: 0), overShadow: .Msg_Status_Fail)
                
                //: self.dealSendMsgError(errorCode: errorModel!.errorCode, errorStr: errorModel!.errorMsg, isGift: true, isResend: false, clickEvent: clickMessagesendButton)
                self.detailOff(errorCode: errorModel!.errorCode, errorStr: errorModel!.errorMsg, isGift: true, isResend: false, clickEvent: app_iconPerValue)
                //: if params.keys.contains("txtId") {
                if params.keys.contains((String(notiNeverJudgeStandardValue.suffix(5)))) {
                    //: TalkingPrivateChatManager.chat_changeSaveGreetData(toUserId: self.msgView.targetID, isHave: false)
                    InvisibleJob.control(toUserId: self.msgView.targetID, isHave: false)
                }
                /// 打招呼达到设定次数失败时
                //: if errorModel!.errorCode == 10015 {
                if errorModel!.errorCode == 10015 {
                    //: self.msgView.deleteCustomMessage(msgData)
                    self.msgView.video(msgData)
                }
                //: return
                return
            }
            //: var updateMsgData = msgData
            var updateMsgData = msgData
            //: let dataDict = result as! Dictionary<String, Any>
            let dataDict = result as! [String: Any]
            //: var msgInfo = dataDict["msgInfo"] as! Dictionary<String, Any>
            var msgInfo = dataDict[(show_transactionCountoStr.replacingOccurrences(of: "fatal", with: "m") + String(appRetMessage.suffix(6)))] as! [String: Any]

            //: var msgIncome: NSNumber = NSNumber.init(value: 0)
            var msgIncome: NSNumber = .init(value: 0)
            //: if msgInfo.keys.contains("msgIncome") {
            if msgInfo.keys.contains((String(appTermsOvalUrl))) {
                //: msgIncome = msgInfo["msgIncome"] as! NSNumber
                msgIncome = msgInfo[(String(appTermsOvalUrl))] as! NSNumber
            }

            /// 亲密度
            //: self.sendMsgIntimateTool(msgInfo: msgInfo)
            self.failure(msgInfo: msgInfo)

            /// 索要礼物，快速打招呼
            //: let messageType = msgInfo["messageType"] as? Int
            let messageType = msgInfo[(String(constUniformAssociatedPath.suffix(8)) + k_insteadPath.replacingOccurrences(of: "accept", with: "e"))] as? Int
            //: if messageType == 25 || type == "QuickMsg" || type == "MultiQuickMsg" {
            if messageType == 25 || type == (String(app_relateTitle.suffix(7)) + main_againstValue.replacingOccurrences(of: "big", with: "g")) || type == (String(app_careStr.suffix(7)) + String(userPrivacyKey.suffix(6))) {
                //: if messageType == 25 {
                if messageType == 25 {
                    //: msgInfo["msgType"] = "WantGift"
                    msgInfo[String(bytes: app_plainLeadingCorrectString.map{towardInvite(ease: $0)}, encoding: .utf8)!] = (String(dataShadowMessage.prefix(8)))
                    //: uploadRecord.uploadRecordEvent(eventID: clickMessageDetailGiftPopAskBtnSusType)
                    noti_loadResolveMsg.priorityFun(eventID: show_pressureFormat)
                    //: } else if type == "QuickMsg" || type == "MultiQuickMsg" {
                } else if type == (String(app_relateTitle.suffix(7)) + main_againstValue.replacingOccurrences(of: "big", with: "g")) || type == (String(app_careStr.suffix(7)) + String(userPrivacyKey.suffix(6))) {
                    //: msgInfo["msgType"] = "txt"
                    msgInfo[String(bytes: app_plainLeadingCorrectString.map{towardInvite(ease: $0)}, encoding: .utf8)!] = (const_bindMessage.lowercased())
                    //: self.QuickEndBlock?(true, errorModel?.errorCode ?? 0)
                    self.QuickEndBlock?(true, errorModel?.errorCode ?? 0)
                }
                //: let dic = ["msgInfo": msgInfo]
                let dic = [(show_transactionCountoStr.replacingOccurrences(of: "fatal", with: "m") + String(appRetMessage.suffix(6))): msgInfo]
                //: if let cellData = TUISocialChatManager.handleTXMessageSend(withExtral: dic, isSender: true, isTip: false) {
                if let cellData = IndependentObserve.on(withExtral: dic, isSender: true, isTip: false) {
                    //: updateMsgData = cellData
                    updateMsgData = cellData
                    //: self.msgView.sendCustomMessage(updateMsgData)
                    self.msgView.normSave(updateMsgData)
                }
            }

            // 更新提示文案
            //: self.userWrap.existSess = true
            self.userWrap.existSess = true
            //: if self.rechargeView.isHidden {
            if self.rechargeView.isHidden {
                //: if MarginExamineer.share.loginUserMode.sex == Gender.male.rawValue &&
                if MarginExamineer.share.loginUserMode.sex == EnvironmentEmpty.male.rawValue,
                   //: type != "QuickMsg" &&
                   type != (String(app_relateTitle.suffix(7)) + main_againstValue.replacingOccurrences(of: "big", with: "g")),
                   //: type != "MultiQuickMsg" {
                   type != (String(app_careStr.suffix(7)) + String(userPrivacyKey.suffix(6)))
                {
                    //: let key =  "\(MarginExamineer.share.loginUserMode.userID)_PrivateChatTipsTitle_\(String(self.msgView.targetID))"
                    let key = "\(MarginExamineer.share.loginUserMode.userID)" + (String(constCompleteBurnMsg) + String(k_errorData)) + "\(String(self.msgView.targetID))"
                    //: UserDefaults.standard.set(true, forKey: key)
                    UserDefaults.standard.set(true, forKey: key)
                    //: self.removeReplyTipsView()
                    self.marginHome()
                }
            }
            // 更新本地消息信息
            //: self.msgView.changeCustomMsg(updateMsgData, msgId: msgInfo["msgId"] as? String, msgIncome: msgIncome, status: .Msg_Status_Succ)
            self.msgView.functional(updateMsgData, tillEffectDensity: msgInfo[(String(const_successKey.prefix(5)))] as? String, flushBorder: msgIncome, overShadow: .Msg_Status_Succ)
            //: if dataDict.keys.contains("totalMfCoin") {
            if dataDict.keys.contains((String(mainMagnitudeervalValue.prefix(9)) + String(appEraseRapidMessage))) {
                //: let totalMfCoin: NSNumber = dataDict["totalMfCoin"] as! NSNumber
                let totalMfCoin: NSNumber = dataDict[(String(mainMagnitudeervalValue.prefix(9)) + String(appEraseRapidMessage))] as! NSNumber
                //: MarginExamineer.share.loginUserMode.mf_coin = totalMfCoin.stringValue
                MarginExamineer.share.loginUserMode.mf_coin = totalMfCoin.stringValue
            }

            /// 免费消息提示
            //: if dataDict.keys.contains("attachSysMsg") {
            if dataDict.keys.contains((appSustainableString.replacingOccurrences(of: "procedure", with: "at") + String(constFixedKey.prefix(6)))) {
                //: let attachSysMsg: String = dataDict["attachSysMsg"] as? String ?? ""
                let attachSysMsg: String = dataDict[(appSustainableString.replacingOccurrences(of: "procedure", with: "at") + String(constFixedKey.prefix(6)))] as? String ?? ""
                //: if attachSysMsg.count>0 {
                if attachSysMsg.count > 0 {
                    //: self.func__privateAttachSys_insertTipMsg(str: attachSysMsg)
                    self.group(str: attachSysMsg)
                }
            }

            //: if msgInfo.keys.contains("intimatePhoto") { // 发送成功私密照片 => model，更新本地消息
            if msgInfo.keys.contains((String(notiProcessTitle) + String(dataCalculateString.suffix(7)))) { // 发送成功私密照片 => model，更新本地消息
                //: let intimatePhotoDict = msgInfo["intimatePhoto"] as! [String: Any]
                let intimatePhotoDict = msgInfo[(String(notiProcessTitle) + String(dataCalculateString.suffix(7)))] as! [String: Any]
                //: if let model = try? AbTalkingChatMsgIntimatePhotoModel(dictionary: intimatePhotoDict) {
                if let model = try? EaseModel(dictionary: intimatePhotoDict) {
                    //: updateMsgData.msgModel.msgInfo.intimatePhoto = model
                    updateMsgData.msgModel.msgInfo.intimatePhoto = model
                    //: self.msgView.changeIntimatePhotoMsg(updateMsgData, photoModel: model)
                    self.msgView.response(updateMsgData, hero: model)
                }

                //: } else if msgInfo.keys.contains("video") { // 发送成功私密视频 => model，更新本地消息
            } else if msgInfo.keys.contains((showProperlyPath.replacingOccurrences(of: "sample", with: "e"))) { // 发送成功私密视频 => model，更新本地消息
                //: let intimateVideoDict = msgInfo["video"] as! [String: Any]
                let intimateVideoDict = msgInfo[(showProperlyPath.replacingOccurrences(of: "sample", with: "e"))] as! [String: Any]
                //: if let model = try? AbTalkingChatMsgVideoModel(dictionary: intimateVideoDict) {
                if let model = try? PermanentModel(dictionary: intimateVideoDict) {
                    //: updateMsgData.msgModel.msgInfo.video = model
                    updateMsgData.msgModel.msgInfo.video = model
                    //: self.msgView.changeIntimateVideoMsg(updateMsgData, videoModel: model)
                    self.msgView.undertake(updateMsgData, label: model)
                }
            }
            //: })
        })

        // 判断是否需要插入音视频提示消息
        //: func__privateChatVideo_insertTipMsg()
        userDirect()

        //: updateSession()
        flexibleSubsequent()
    }

    /// 建立会话标识，本地更新数据和会话列表
    //: func updateSession() {
    func flexibleSubsequent() {
        //: DBUserInfoManager.cache_updateSessionAndPostNotif(with: msgView.targetID)
        ProgressAssociated.edge(with: msgView.targetID)
    }

    /// 处理发送失败情况
    //: private func dealSendMsgError(errorCode: Int, errorStr: String, isGift: Bool, isResend: Bool, clickEvent: String) {
    private func detailOff(errorCode: Int, errorStr: String, isGift: Bool, isResend: Bool, clickEvent: String) {
        //: if errorCode == 50203 {
        if errorCode == 50203 { // 余额不足
            //: guard MarginExamineer.share.loginUserMode.status != 1 else {
            guard MarginExamineer.share.loginUserMode.status != 1 else {
                //: if !errorStr.isEmptyString {
                if !errorStr.isEmptyString {
                    //: func__showStatusBarErrorMsg(showMsg: errorStr)
                    exitMarker(showMsg: errorStr)
                }
                //: return
                return
            }

            //: OriginMediumWill.share.func__jumpToWebRecharge(clickEvent: clickEvent, sufficient: false)
            OriginMediumWill.share.resumeUniversal(clickEvent: clickEvent, sufficient: false)
            //: msgInputView.giftView.dismissView()
            msgInputView.giftView.fadePhone()
            //: MarginExamineer.share.loginUserMode.freeMsgTimes = ""
            MarginExamineer.share.loginUserMode.freeMsgTimes = ""
            //: msgInputView.setHidenFreeMsgTimesView()
            msgInputView.enableMaterial()
//            insertTipMsg_Nobalance(tipStr: errorStr)
            //: } else if errorCode == 10888 {
        } else if errorCode == 10888 { // vip订阅弹窗
            //: let view = TalkingLiveVipSubscribePopView.init(frame: CGRect(x: 0, y: 0, width: ScreenWidth, height: ScreenHeight))
            let view = ProjectionView(frame: CGRect(x: 0, y: 0, width: show_errorChangeFormat, height: appLoadMessage))
            //: view.show()
            view.someModify()
            //: } else {
        } else {
            //: if !errorStr.isEmptyString {
            if !errorStr.isEmptyString {
                //: func__showStatusBarErrorMsg(showMsg: errorStr)
                exitMarker(showMsg: errorStr)
            }
        }
    }
}

// MARK: - 亲密度

//: extension TalkingPrivateChatController {
extension ViaNative {
    /// 解锁图片视频送礼亲密度
    //: private func unlockPhotoOrVideoTool(dataDict: Dictionary<String, Any>) {
    private func pin(dataDict: [String: Any]) {
        //: let msgInfo = dataDict["msgInfo"] as! Dictionary<String, Any>
        let msgInfo = dataDict[(show_transactionCountoStr.replacingOccurrences(of: "fatal", with: "m") + String(appRetMessage.suffix(6)))] as! [String: Any]
        /// 亲密度
        //: self.sendMsgIntimateTool(msgInfo: msgInfo)
        self.failure(msgInfo: msgInfo)
    }

    /// 初次展示导航栏的亲密度头像
    //: private func showFirstIntimateView() {
    private func yellowExtra() {
        //: if self.userWrap.intimate >= MarginExamineer.share.appUserConfigMode.intimateLimit.chatTitleEffect && conversationType == .private  {
        if self.userWrap.intimate >= MarginExamineer.share.appUserConfigMode.intimateLimit.chatTitleEffect, conversationType == .private {
            //: self.isShowIntimateView = true
            self.isShowIntimateView = true
            //: navView.private_updateIntimateNavView(userModel: userWrap)
            navView.quantity(userModel: userWrap)
            /// 初次展示动效
            //: let key = "\(MarginExamineer.share.loginUserMode.userID)_\(String(self.msgView.targetID))"
            let key = "\(MarginExamineer.share.loginUserMode.userID)_\(String(self.msgView.targetID))"
            //: var intimateCacheData = (Defaults.array(forKey: TalkingFirstShowIntimateGuide)) as? [String] ?? []
            var intimateCacheData = (user_findBoundMessage.array(forKey: show_overColorOccasionString)) as? [String] ?? []
            //: if !intimateCacheData.contains(key) {
            if !intimateCacheData.contains(key) {
                //: navView.playAnimation()
                navView.minAlongside()
                //: intimateCacheData.append(key)
                intimateCacheData.append(key)
                //: Defaults.set(intimateCacheData, forKey: TalkingFirstShowIntimateGuide)
                user_findBoundMessage.set(intimateCacheData, forKey: show_overColorOccasionString)
            }
        }
    }

    /// 收到亲密度消息
    //: @objc func newMsgIntimate(notification: NSNotification) {
    @objc func signatureBy(notification: NSNotification) {
        //: let data = notification.object as! V2TIMMessage
        let data = notification.object as! V2TIMMessage
        //: let dic = NeedSpecify.parseTXMessageData(data: data.customElem.data)
        let dic = NeedSpecify.onStrategy(data: data.customElem.data)
        //: let jsonDic = JSON(dic)
        let jsonDic = JSON(dic)
        //: let extra = jsonDic["extra"].dictionary
        let extra = jsonDic[(appWenRegionData.replacingOccurrences(of: "constraint", with: "ra"))].dictionary
        //: let msgInfo = extra?["msgInfo"]?.dictionary
        let msgInfo = extra?[(show_transactionCountoStr.replacingOccurrences(of: "fatal", with: "m") + String(appRetMessage.suffix(6)))]?.dictionary
        //: if data.sender == self.msgView.targetID {
        if data.sender == self.msgView.targetID {
            //: if let totalIntimate = msgInfo?["totalIntimate"]?.int {
            if let totalIntimate = msgInfo?[(String(user_disappearTitle.prefix(7)) + userFilterStr.replacingOccurrences(of: "group", with: "im"))]?.int {
                //: if self.conversationType == .private {
                if self.conversationType == .private {
                    //: let time = data.timestamp.timeIntervalSince1970
                    let time = data.timestamp.timeIntervalSince1970
                    //: self.intimateTool(currenIntimate: totalIntimate, time: Int(time))
                    self.sustainable(currenIntimate: totalIntimate, time: Int(time))
                }
            }
        }
    }

    /// 发送消息时
    //: private func sendMsgIntimateTool(msgInfo: Dictionary<String, Any>) {
    private func failure(msgInfo: [String: Any]) {
        //: if msgInfo.keys.contains("totalIntimate") && self.conversationType == .private {
        if msgInfo.keys.contains((String(user_disappearTitle.prefix(7)) + userFilterStr.replacingOccurrences(of: "group", with: "im"))), self.conversationType == .private {
            //: let totalIntimacy = msgInfo["totalIntimate"] as? Int ?? 0
            let totalIntimacy = msgInfo[(String(user_disappearTitle.prefix(7)) + userFilterStr.replacingOccurrences(of: "group", with: "im"))] as? Int ?? 0
            //: self.intimateTool(currenIntimate: totalIntimacy)
            self.sustainable(currenIntimate: totalIntimacy)
        }
    }

    /// 亲密度操作
    //: func intimateTool(currenIntimate: Int, time: Int = 0) {
    func sustainable(currenIntimate: Int, time: Int = 0) {
        //: guard currenIntimate > 0 else { return }
        guard currenIntimate > 0 else { return }
        //: let model = DBUserInfoManager.cache_getCachedUserInfo(targetId: self.msgView.targetID)
        let model = ProgressAssociated.betweenPolicy(targetId: self.msgView.targetID)
        //: self.userWrap.intimate = currenIntimate
        self.userWrap.intimate = currenIntimate
        /// 满足最低条件，展示页面
        //: if currenIntimate >= MarginExamineer.share.appUserConfigMode.intimateLimit.chatTitleEffect {
        if currenIntimate >= MarginExamineer.share.appUserConfigMode.intimateLimit.chatTitleEffect {
            /// 亲密度增加
            //: if !self.isShowIntimateView {
            if !self.isShowIntimateView {
                //: self.isShowIntimateView = true
                self.isShowIntimateView = true
                //: navView.private_updateIntimateNavView(userModel: self.userWrap)
                navView.quantity(userModel: self.userWrap)
            }
            //: if currenIntimate - (model?.intimate ?? 0) > 0 {
            if currenIntimate - (model?.intimate ?? 0) > 0 {
                //: self.navView.playIntimateAnim(intimate: currenIntimate - (model?.intimate ?? 0))
                self.navView.cornerOrBegin(intimate: currenIntimate - (model?.intimate ?? 0))
                /// 播放完毕
                //: self.navView.animEndBlock = { [weak self] in
                self.navView.animEndBlock = { [weak self] in
                    //: guard let self = self else { return }
                    guard let self = self else { return }
                    //: self.navView.setAllData(intimate: currenIntimate)
                    self.navView.enableicialInMargin(intimate: currenIntimate)
                }
                //: } else {
            } else {
                //: self.navView.setAllData(intimate: currenIntimate)
                self.navView.enableicialInMargin(intimate: currenIntimate)
            }
        }

        //: let currTime = time > 0 ? time: Int(Date().timeIntervalSince1970)
        let currTime = time > 0 ? time : Int(Date().timeIntervalSince1970)
        //: DBUserInfoManager.cache_updateIntimate(with: msgView.targetID, intimate: currenIntimate, currTime: currTime)
        ProgressAssociated.frame(with: msgView.targetID, intimate: currenIntimate, currTime: currTime)
        //: let userInfo = ["uid": self.msgView.targetID ?? "",
        let userInfo = [String(bytes: noti_attachStr.map{foundationEven(support: $0)}, encoding: .utf8)!: self.msgView.targetID ?? "",
                        //: "intimate": currenIntimate] as [String : Any]
                        String(bytes: showBitTurnColorPath.map{pictureConversion(since: $0)}, encoding: .utf8)!: currenIntimate] as [String: Any]
        //: NotificationCenter.default.post(name: MSGLIST_UPDATE_INTIMATE_NOTIFICATION,
        NotificationCenter.default.post(name: mainEquallyName,
                                        //: object: nil,
                                        object: nil,
                                        //: userInfo: userInfo)
                                        userInfo: userInfo)
    }
}

// MARK: - 输入框

//: extension TalkingPrivateChatController: ChatInputViewDelegate {
extension ViaNative: PerformSum {
    //: func func_inputContentIntervalOneSecond() {
    func beauty() {}

    //: func func__selectMentionUser(range: NSRange) {}
    func yield(range: NSRange) {}

    //: func func__selectMoreFunAction(actionType: MoreActionType) {
    func over(actionType: MobileExercise) {
        //: switch actionType {
        switch actionType {
        //: case .Photo:
        case .Photo:
            //: if self.conversationType == .private {
            if self.conversationType == .private {
                //: if self.userWrap.intimate >= MarginExamineer.share.appUserConfigMode.intimateLimit.sendImg {
                if self.userWrap.intimate >= MarginExamineer.share.appUserConfigMode.intimateLimit.sendImg {
                    //: func__choicePhotos()
                    counteraction()
                    //: } else {
                } else {
                    //: showIntimateErrorStr()
                    packageLikeOutside()
                }
                //: } else {
            } else {
                //: func__choicePhotos()
                counteraction()
            }
        //: case .VideoCall:
        case .VideoCall: // 音/视频通话
            //: var videoStr: String?
            var videoStr: String?
            //: var voiceStr: String?
            var voiceStr: String?
            //: if MarginExamineer.share.loginUserMode.loungePlus {
            if MarginExamineer.share.loginUserMode.loungePlus { // 男性会员
                //: videoStr = self.userWrap.videoVIPPrice
                videoStr = self.userWrap.videoVIPPrice
                //: voiceStr = self.userWrap.voiceVIPPrice
                voiceStr = self.userWrap.voiceVIPPrice
                //: } else {
            } else {
                //: videoStr = self.userWrap.videoPrice
                videoStr = self.userWrap.videoPrice
                //: voiceStr = self.userWrap.voicePrice
                voiceStr = self.userWrap.voicePrice
            }
            //: if self.userWrap.intimate >= MarginExamineer.share.appUserConfigMode.intimateLimit.requestCall {
            if self.userWrap.intimate >= MarginExamineer.share.appUserConfigMode.intimateLimit.requestCall {
                //: videoCallManager.showVideoCallMenu(videoPrice: videoStr, voicePrice: voiceStr)
                videoCallManager.adjustExcept(videoPrice: videoStr, voicePrice: voiceStr)
                //: } else {
            } else {
                //: showIntimateErrorStr()
                packageLikeOutside()
            }

        //: case .ShortVideo:
        case .ShortVideo: // 短视频
            //: if self.conversationType == .private {
            if self.conversationType == .private {
                //: if self.userWrap.intimate >= MarginExamineer.share.appUserConfigMode.intimateLimit.sendVideo {
                if self.userWrap.intimate >= MarginExamineer.share.appUserConfigMode.intimateLimit.sendVideo {
                    //: func__sendShortVideo()
                    adminHide()
                    //: } else {
                } else {
                    //: showIntimateErrorStr()
                    packageLikeOutside()
                }
                //: } else {
            } else {
                //: func__sendShortVideo()
                adminHide()
            }

        //: case .Gift: break
        case .Gift: break

        //: case .Game:
        case .Game: // 游戏
            //: gamesView.showView(from: .PrivateChat)
            gamesView.doingtoDestination(from: .PrivateChat)
        }
    }

    //: private func showIntimateErrorStr() {
    private func packageLikeOutside() {
        //: self.func__showStatusBarErrorMsg(showMsg: "Can't use the function due to your lack of intimacy level.".localized)
        self.exitMarker(showMsg: String(bytes: userRejectDisappearMessage.map{$0^231}, encoding: .utf8)!.localized)
    }

    //: func func__sendChatRoomGiftMsg(giftModel: TalkingRoomGiftModel, giftNum: String, model: TalkingChatRoomMemberModel) {}
    func stepEager(giftModel: EvaluationMenuRate, giftNum: String, model: ReduceModel) {}

    // 聊天面板送礼
    /// - Parameters:
    ///   - giftModel: 礼物模型
    ///   - giftNum: 礼物数量
    //: func func__sendGiftMsg(giftModel: TalkingRoomGiftModel, giftNum: String) {
    func selecter(giftModel: EvaluationMenuRate, giftNum: String) {
        //: func__sendGiftMsg(giftModel: giftModel, giftNum: giftNum, cellData: nil, fromStr: nil)
        notMini(giftModel: giftModel, giftNum: giftNum, cellData: nil, fromStr: nil)
    }

    //: func func__sendGiftMsg(giftModel: TalkingRoomGiftModel, giftNum: String, cellData: LightSaveLabel?, fromStr: String?) {
    func notMini(giftModel: EvaluationMenuRate, giftNum: String, cellData: LightSaveLabel?, fromStr: String?) {
        //: let reachability = try? Reachability()
        let reachability = try? Reachability()
        //: if reachability?.connection == .unavailable {
        if reachability?.connection == .unavailable {
            //: func__showStatusBarErrorMsg(showMsg: kNetErrorMsg)
            exitMarker(showMsg: kRequestPath)
            //: return
            return
        }
        //: var params: Dictionary<String, Any> = [:]
        var params: [String: Any] = [:]
        //: params["toUid"] = self.msgView.targetID
        params[(String(dataUndertakeRecoverBeforePath.prefix(5)))] = self.msgView.targetID
        //: params["giftId"] = giftModel.gid
        params[(String(dataBuildValue.prefix(6)))] = giftModel.gid
        //: params["giftNum"] = giftNum
        params[(String(main_nervousUrl.suffix(7)))] = giftNum
        //: if giftModel.pkgItemsetId != nil {
        if giftModel.pkgItemsetId != nil {
            //: params["pkgItemsetId"] = giftModel.pkgItemsetId
            params[(String(data_obtainId.suffix(5)) + String(showSucceedMeasureUrl) + String(mainExperiencedEntityId))] = giftModel.pkgItemsetId
        }
        //: if fromStr != nil {
        if fromStr != nil {
            //: params["from"] = fromStr
            params[(data_communicateData.replacingOccurrences(of: "maker", with: "f"))] = fromStr
        }
        // 送礼接口
        //: TalkingChatRequestTool.req_SendGiftPrivete(param: params, completion: { succeed, result, errorModel in
        PriorMedia.along(param: params, completion: { succeed, result, errorModel in
            //: guard succeed else {
            guard succeed else {
                //: self.dealSendMsgError(errorCode: errorModel!.errorCode, errorStr: errorModel!.errorMsg, isGift: true, isResend: false, clickEvent: clickSendingiftsButton)
                self.detailOff(errorCode: errorModel!.errorCode, errorStr: errorModel!.errorMsg, isGift: true, isResend: false, clickEvent: notiProgressId)
                //: return
                return
            }

            // 更新本地礼物背包
            //: if giftModel.pkgItemsetId != nil {
            if giftModel.pkgItemsetId != nil {
                //: self.msgInputView.giftView.bags_removeGiftPackage(pkgItemsetId: giftModel.pkgItemsetId!, num: Int(giftNum) ?? 0)
                self.msgInputView.giftView.strategyBy(pkgItemsetId: giftModel.pkgItemsetId!, num: Int(giftNum) ?? 0)
            }
            //: self.func__insertGiftMessageWithExtral(extral: result as! Dictionary<String, Any?>, isFromInputView: true)
            self.denseInmate(extral: result as! [String: Any?], isFromInputView: true)
            //: self.updateSession()
            self.flexibleSubsequent()
            /// 亲密度
            //: let extralInfo = result as! Dictionary<String, Any?>
            let extralInfo = result as! [String: Any?]
            //: let msgInfoDic: Dictionary<String, Any> = extralInfo["msgInfo"] as! Dictionary<String, Any>
            let msgInfoDic: [String: Any] = extralInfo[(show_transactionCountoStr.replacingOccurrences(of: "fatal", with: "m") + String(appRetMessage.suffix(6)))] as! [String: Any]
            //: if msgInfoDic.keys.contains("totalIntimate") && self.conversationType == .private {
            if msgInfoDic.keys.contains((String(user_disappearTitle.prefix(7)) + userFilterStr.replacingOccurrences(of: "group", with: "im"))), self.conversationType == .private {
                //: let totalIntimacy = msgInfoDic["totalIntimate"] as? Int ?? 0
                let totalIntimacy = msgInfoDic[(String(user_disappearTitle.prefix(7)) + userFilterStr.replacingOccurrences(of: "group", with: "im"))] as? Int ?? 0
                //: self.intimateTool(currenIntimate: totalIntimacy)
                self.sustainable(currenIntimate: totalIntimacy)
            }
            /// 索要礼物送礼
            //: if cellData != nil {
            if cellData != nil {
                // 修改状态
                //: self.msgView.changeIntimateGiftMsg(cellData)
                self.msgView.coverExcept(cellData)
                //: uploadRecord.uploadRecordEvent(eventID: ClickmessageDetailaskMsgsendBtn)
                noti_loadResolveMsg.priorityFun(eventID: notiVersusMessage)
            }
            //: })
        })
    }

    /// 索要礼物
    //: func func__askforGiftMsg(giftModel: TalkingRoomGiftModel, giftNum: String) {
    func medium(giftModel: EvaluationMenuRate, giftNum: String) {
        //: uploadRecord.uploadRecordEvent(eventID: ClickMessageDetailGiftPopAskBtnAllType)
        noti_loadResolveMsg.priorityFun(eventID: k_equalKeyConsumerKey)
        //: let reachability = try? Reachability()
        let reachability = try? Reachability()
        //: if reachability?.connection == .unavailable {
        if reachability?.connection == .unavailable {
            //: func__showStatusBarErrorMsg(showMsg: kNetErrorMsg)
            exitMarker(showMsg: kRequestPath)
            //: return
            return
        }
        //: guard self.userWrap.intimate >= MarginExamineer.share.appUserConfigMode.intimateLimit.wantGift else {
        guard self.userWrap.intimate >= MarginExamineer.share.appUserConfigMode.intimateLimit.wantGift else {
            //: showIntimateErrorStr()
            packageLikeOutside()
            //: return
            return
        }
        //: var params: Dictionary<String, Any> = [:]
        var params: [String: Any] = [:]
        //: params["wantGiftId"] = giftModel.gid
        params[(data_removeBlackStr.replacingOccurrences(of: "control", with: "nt") + "GiftI" + dataStreakAmongUrl.replacingOccurrences(of: "asset", with: "d"))] = giftModel.gid
        //: params["type"] = "WantGiftMsg"
        params[(String(constYourselfData))] = (String(dataFeeMsg.prefix(4)) + "Gift" + String(noti_packData.suffix(3)))

        //: let msgInfo = ["msgType": "WantGift"]
        let msgInfo = [String(bytes: app_plainLeadingCorrectString.map{towardInvite(ease: $0)}, encoding: .utf8)!: (String(dataShadowMessage.prefix(8)))]
        //: let dic = ["msgInfo": msgInfo, "params": params]
        let dic = [(show_transactionCountoStr.replacingOccurrences(of: "fatal", with: "m") + String(appRetMessage.suffix(6))): msgInfo, String(bytes: user_keyWantGreenFormat.map{cookieResolve(week: $0)}, encoding: .utf8)!: params]
        //: if let cellData = TUISocialChatManager.handleTXMessageSend(withExtral: dic, isSender: true, isTip: false) {
        if let cellData = IndependentObserve.on(withExtral: dic, isSender: true, isTip: false) {
            //: func__sendPriveteMsg(msgData: cellData, reSend: false)
            join(msgData: cellData, reSend: false)
        }
    }

    //: func heightToBottomChanged(heightToBottom: CGFloat) {
    func primary(heightToBottom: CGFloat) {
        //: if self.isFrom == .LiveRoom {
        if self.isFrom == .LiveRoom {
            //: contentView.snp.updateConstraints { make in
            contentView.snp.updateConstraints { make in
                //: make.top.equalTo(HalfViewTopMargin-heightToBottom)
                make.top.equalTo(main_saltStr - heightToBottom)
            }
        }
        //: msgInputView.snp.updateConstraints { make in
        msgInputView.snp.updateConstraints { make in
            //: make.bottom.equalTo(self.view).offset(-heightToBottom)
            make.bottom.equalTo(self.view).offset(-heightToBottom)
        }
        //: DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 0.3) {
        DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 0.3) {
            //: self.msgView.paintAcross(true)
            self.msgView.paintAcross(true)
        }
    }

    //: func inputViewHeightChanged(height: CGFloat) {
    func phoneStructure(height: CGFloat) {
        //: msgInputView.snp.updateConstraints { make in
        msgInputView.snp.updateConstraints { make in
            //: make.height.equalTo(height)
            make.height.equalTo(height)
        }
        //: DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 0.3) {
        DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 0.3) {
            //: self.msgView.paintAcross(true)
            self.msgView.paintAcross(true)
        }
    }

    //: @objc override func func__TapGestureRecognizer(_ tap: UITapGestureRecognizer) {
    @objc override func purchase(_ tap: UITapGestureRecognizer) {
        //: super.func__TapGestureRecognizer(tap)
        super.purchase(tap)
        //: if self.isFrom == .LiveRoom {
        if self.isFrom == .LiveRoom {
            //: let location = tap.location(ofTouch: 0, in: self.contentView)
            let location = tap.location(ofTouch: 0, in: self.contentView)
            //: if !contentView.point(inside: location, with: nil) {
            if !contentView.point(inside: location, with: nil) {
                // 点击列表以外区域，移除视图（包括聊天列表）
                //: half_dismissViewAnimation(true)
                read(true)
            }
        }
    }

    /// 开始录音操作
    //: func func__starRecordBtnDown() {
    func occasionBan() {
        //: if TalkingVoiceMsgPlayManager.shared.playStatus == .Playing {
        if TemporaryOperation.shared.playStatus == .Playing {
            //: TalkingVoiceMsgPlayManager.shared.stopAudioPlayer()
            TemporaryOperation.shared.report()
        }
    }
}

// MARK: - 动效

//: extension TalkingPrivateChatController {
extension ViaNative {
    /// 插入礼物消息，播放动效
    /// - Parameters:
    ///   - extral: 送礼接口返回结果
    ///   - isFromInputView: 是否来自输入框送礼入口
    //: func func__insertGiftMessageWithExtral(extral: Dictionary<String, Any?>, isFromInputView: Bool) {
    func denseInmate(extral: [String: Any?], isFromInputView: Bool) {
        //: var extralInfo = extral
        var extralInfo = extral
        //: var msgInfoDic: Dictionary<String, Any> = extralInfo["msgInfo"] as! Dictionary<String, Any>
        var msgInfoDic: [String: Any] = extralInfo[(show_transactionCountoStr.replacingOccurrences(of: "fatal", with: "m") + String(appRetMessage.suffix(6)))] as! [String: Any]
        //: msgInfoDic["msgType"] = "gift"
        msgInfoDic[String(bytes: app_plainLeadingCorrectString.map{towardInvite(ease: $0)}, encoding: .utf8)!] = (noti_happyString.replacingOccurrences(of: "gate", with: "g"))
        //: extralInfo["msgInfo"] = msgInfoDic
        extralInfo[(show_transactionCountoStr.replacingOccurrences(of: "fatal", with: "m") + String(appRetMessage.suffix(6)))] = msgInfoDic

        //: let cellData = TUISocialChatManager.handleTXMessageSend(withExtral: extralInfo as [String: Any], isSender: true, isTip: false)
        let cellData = IndependentObserve.on(withExtral: extralInfo as [String: Any], isSender: true, isTip: false)
        //: self.msgView.sendCustomMessage(cellData)
        self.msgView.normSave(cellData)
        //: var msgIncome: NSNumber = NSNumber.init(floatLiteral: 0)
        var msgIncome = NSNumber(floatLiteral: 0)
        //: if msgInfoDic.keys.contains("msgIncome") {
        if msgInfoDic.keys.contains((String(appTermsOvalUrl))) {
            //: msgIncome = msgInfoDic["msgIncome"] as! NSNumber
            msgIncome = msgInfoDic[(String(appTermsOvalUrl))] as! NSNumber
        }
        //: self.msgView.changeCustomMsg(cellData, msgId: msgInfoDic["msgId"] as? String, msgIncome: msgIncome, status: .Msg_Status_Succ)
        self.msgView.functional(cellData, tillEffectDensity: msgInfoDic[(String(const_successKey.prefix(5)))] as? String, flushBorder: msgIncome, overShadow: .Msg_Status_Succ)

        //: sendGiftByMeToEfffecct(giftDic: extral as [String: Any])
        sample(giftDic: extral as [String: Any])
        //: if extralInfo.keys.contains("totalMfCoin") {
        if extralInfo.keys.contains((String(mainMagnitudeervalValue.prefix(9)) + String(appEraseRapidMessage))) {
            //: let totalMfCoin: NSNumber = extralInfo["totalMfCoin"] as! NSNumber
            let totalMfCoin: NSNumber = extralInfo[(String(mainMagnitudeervalValue.prefix(9)) + String(appEraseRapidMessage))] as! NSNumber
            //: MarginExamineer.share.loginUserMode.mf_coin = totalMfCoin.stringValue
            MarginExamineer.share.loginUserMode.mf_coin = totalMfCoin.stringValue
        }
        //: if isFromInputView {
        if isFromInputView {
            //: msgInputView.giftView.updateGiftInfo(needReload: false, mf_coin: MarginExamineer.share.loginUserMode.mf_coin)
            msgInputView.giftView.color(needReload: false, mf_coin: MarginExamineer.share.loginUserMode.mf_coin)
        }
    }

    //: func initCachEffectData() {
    func segmentBy() {
        //: giftEffectView.initCachAnimatData(tagetID: msgView.targetID)
        giftEffectView.titleure(tagetID: msgView.targetID)
    }

    //: func sendGiftByMeToEfffecct(giftDic: Dictionary<String, Any>) {
    func sample(giftDic: [String: Any]) {
        //: giftEffectView.dealGiftAnimatData(dict: giftDic)
        giftEffectView.onIndex(dict: giftDic)
    }

    //: @objc func addGiftEffectModelArr(giftArr: Array<Any>) {
    @objc func oval(giftArr: [Any]) {
        //: giftEffectView.addGiftAnimatModelArr(modelArr: giftArr)
        giftEffectView.who(modelArr: giftArr)
    }

    //: @objc func getChatVCTargetId() ->String {
    @objc func threeManager() -> String {
        //: return msgView.targetID
        return msgView.targetID
    }
}

// MARK: - 音视频通话Event

//: extension TalkingPrivateChatController {
extension ViaNative {
    /// 检查权限后发起音频通话
    //: func checkAndCallVoice() {
    func advertisingOfTap() {
        //: videoCallManager.checkAndCallVoice {[weak self] succeed in
        videoCallManager.pastHardFuture { [weak self] succeed in
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: guard succeed else { return }
            guard succeed else { return }
            //: self.updateSession()
            self.flexibleSubsequent()
        }
    }

    /// 检测权限后发起视频通话
    //: func checkAndCallVideo() {
    func fill() {
        //: videoCallManager.checkAndCallVideo {[weak self] succeed in
        videoCallManager.afterQuality { [weak self] succeed in
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: guard succeed else { return }
            guard succeed else { return }
            //: self.updateSession()
            self.flexibleSubsequent()
        }
    }
}

// MARK: - Event

//: extension TalkingPrivateChatController {
extension ViaNative {
    //: @objc private func feedbackBtnClick() {
    @objc private func listen() {
        //: let vc = TalkingFeedbackVC.init()
        let vc = AddSpellKey()
        //: getNavigationController()?.pushViewController(vc, animated: true)
        aboveShadow()?.pushViewController(vc, animated: true)
    }

    /// 更多按钮事件
    //: private func moreBtnClick() {
    private func privacy() {
        //: let vc = TalkingMunuPopView.init(frame: self.view.frame)
        let vc = PinSecond(frame: self.view.frame)
        //: vc.initwithList(cellTitleList: ["View info".localized, "Notes".localized, "Report".localized])
        vc.techniqueCapacity(cellTitleList: [(String(user_talkId.suffix(5)) + "info").localized, (String(k_restaurantStr)).localized, (String(kDiscloseData.suffix(6))).localized])
        //: vc.cancelColor = .appTitleColor()
        vc.cancelColor = .untilExpected()
        //: vc.munuBlock = { [weak self] index, str in
        vc.munuBlock = { [weak self] index, str in
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: if index == 0 {
            if index == 0 {
                //: OriginMediumWill.share.func__pushToUserDetailVC(uid: self.msgView.targetID)
                OriginMediumWill.share.petAcrossAm(uid: self.msgView.targetID)

                //: } else if index == 1 {
            } else if index == 1 { // 备注
                //: jumpToNotesView()
                sincePause()

                //: } else if index == 2 {
            } else if index == 2 { // 举报
                //: self.msgInputView.showOtherInputView = true
                self.msgInputView.showOtherInputView = true
                //: let reportView = TalkingReportAlertView.init(frame: UIScreen.main.bounds, type: .reportUserType, rId: self.msgView.targetID)
                let reportView = ReadExclusiveRateAppearanceView(frame: UIScreen.main.bounds, type: .reportUserType, rId: self.msgView.targetID)
                //: reportView.showReportViewIn(view: nil)
                reportView.medal(view: nil)
                //: reportView.reportHideBlock = { _ in
                reportView.reportHideBlock = { _ in
                    //: self.msgInputView.showOtherInputView = false
                    self.msgInputView.showOtherInputView = false
                }
            }
        }
    }

    /// 跳转到备注页面
    //: private func jumpToNotesView() {
    private func sincePause() {
        //: self.msgInputView.showOtherInputView = true
        self.msgInputView.showOtherInputView = true
        //: let view = PrivateChatNotesView()
        let view = GreatView()
        //: view.show(self.userWrap.content, isTop: self.userWrap.top, toUid: self.msgView.targetID)
        view.behindScreen(self.userWrap.content, isTop: self.userWrap.top, toUid: self.msgView.targetID)
        //: view.dismissBlock = { content, isTop in
        view.dismissBlock = { content, isTop in
            //: self.msgInputView.showOtherInputView = false
            self.msgInputView.showOtherInputView = false
            //: self.userWrap.content = content
            self.userWrap.content = content
            //: self.userWrap.top = isTop
            self.userWrap.top = isTop
            //: self.notesTopView.refresh(self.userWrap)
            self.notesTopView.volumeDown(self.userWrap)
        }
    }

    /// 返回按钮点击事件
    //: private func backButtonClick() {
    private func challenge() {
        //: if self.isFrom == .LiveRoom {
        if self.isFrom == .LiveRoom {
            //: half_dismissViewAnimation(false)
            read(false)
            //: } else {
        } else {
            //: getNavigationController()?.popViewController(animated: true)
            aboveShadow()?.popViewController(animated: true)
        }
    }

    /// 展示飘屏内容
    //: @objc private func showFloatScreen(notification: NSNotification) {
    @objc private func bigAway(notification: NSNotification) {
        //: if conversationType == .private {
        if conversationType == .private {
            //: let dataInfo = notification.userInfo as! [String: AnyObject]
            let dataInfo = notification.userInfo as! [String: AnyObject]
            //: if let model = TalkingGameFloatingScreenModel.deserialize(from: dataInfo as? Dictionary) {
            if let model = ChapterEquallyPush.deserialize(from: dataInfo as? Dictionary) {
                //: self.floatScreenView.isUserInteractionEnabled = true
                self.floatScreenView.isUserInteractionEnabled = true
                //: self.floatScreenView.didReceive(model)
                self.floatScreenView.listener(model)
            }
        }
    }
}

// MARK: - VIP消息已读回执

//: extension TalkingPrivateChatController {
extension ViaNative {
    /// 添加通知
    //: private func addReadPeceiptNotification() {
    private func onicial() {
        //: NotificationCenter.default.addObserver(self,
        NotificationCenter.default.addObserver(self,
                                               //: selector: #selector(func__msgReadReceipt(notification:)),
                                               selector: #selector(output(notification:)),
                                               //: name: CHAT_MSG_READ_RECEIPT_NOTIFICATION,
                                               name: main_subtleTakeStr,
                                               //: object: nil)
                                               object: nil)

        //: NotificationCenter.default.addObserver(self,
        NotificationCenter.default.addObserver(self,
                                               //: selector: #selector(func__refreshMsgList),
                                               selector: #selector(about),
                                               //: name: USER_UPDATE_LOUNGEPLUSINFO_NOTIFICATION,
                                               name: main_colorKey,
                                               //: object: nil)
                                               object: nil)
    }

    /// 收到对端已读消息回执通知 -> 刷新列表
    //: @objc func func__msgReadReceipt(notification: NSNotification) {
    @objc func output(notification: NSNotification) {
        //: let obj = notification.object as! V2TIMMessageReceipt
        let obj = notification.object as! V2TIMMessageReceipt
        //: guard obj.userID == msgView.targetID else { return }
        guard obj.userID == msgView.targetID else { return }
        //: guard MarginExamineer.share.loginUserMode.loungePlus == true else { return }
        guard MarginExamineer.share.loginUserMode.loungePlus == true else { return }
        //: DispatchQueue.main.async {
        DispatchQueue.main.async {
            //: self.msgView.tableView.reloadData()
            self.msgView.tableView.reloadData()
        }
    }

    /// 订阅成功 -> 更改订阅状态 -> 刷新列表
    //: @objc private func func__refreshMsgList() {
    @objc private func about() {
        //: MarginExamineer.share.loginUserMode.loungePlus = true
        MarginExamineer.share.loginUserMode.loungePlus = true
        //: userWrap.isRecharge = 1
        userWrap.isRecharge = 1
        //: msgView.isRecharge = userWrap.isRecharge
        msgView.isRecharge = userWrap.isRecharge
        //: refreshRechargeRequiredView()
        painterPurple()
        //: DispatchQueue.main.async {
        DispatchQueue.main.async {
            //: self.msgView.tableView.reloadData()
            self.msgView.tableView.reloadData()
        }
    }

    /// 判断是否插入已读回执引导充值VIP提示消息
    //: func func__privateVIPReadReceipt_insertTipsMsg() {
    func policyInvisible() {
        //: DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 0.4) {
        DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 0.4) {
            //: guard let lastCellData = self.msgView.uiMsgs.lastObject as? AlongCrucial else { return }
            guard let lastCellData = self.msgView.uiMsgs.lastObject as? AlongCrucial else { return }
            //: if TalkingPrivateChatManager.needInsertVIPReadReceiptMsg(cellData: lastCellData) {
            if InvisibleJob.writtenPrepare(cellData: lastCellData) {
                // 只做内存插入展示
                //: self.insertTipMsg_Nobalance(tipStr: "View read receipts?".localized, needInsertLocal: false)
                self.boxAdjust(tipStr: (String(dataSectionStr) + String(appEnabledAirUrl.suffix(9))).localized, needInsertLocal: false)
            }
        }
    }
}

// MARK: - 选择图片

//: extension TalkingPrivateChatController: UIImagePickerControllerDelegate, UINavigationControllerDelegate {
extension ViaNative: UIImagePickerControllerDelegate, UINavigationControllerDelegate {
    //: private func func__choicePhotos() {
    private func counteraction() {
        //: TalkingPermissionTool.func__openAlbumServiceWithBlock(true) { (isOpen: Bool) in
        MultiplePoint.outLimited(true) { (isOpen: Bool) in
            //: if isOpen {
            if isOpen {
                //: let vc = TalkingImagePickTool.initImagePickerVc(maxCount: 1, allowPhoto: true, allowVideo: false)
                let vc = ZonePosition.exAccelerateReliability(maxCount: 1, allowPhoto: true, allowVideo: false)
                //: vc.modalPresentationStyle = .fullScreen
                vc.modalPresentationStyle = .fullScreen
                //: self.present(vc, animated: true)
                self.present(vc, animated: true)
                //: vc.didFinishPickingPhotosHandle = { [weak self] ( photos: Array<UIImage>?,
                vc.didFinishPickingPhotosHandle = { [weak self] (photos: [UIImage]?,
                                                                 //: assets: Array<Any>?,
                                                                 assets: [Any]?,
                                                                 //: isSelectOriginalPhoto: Bool?)  in
                                                                 isSelectOriginalPhoto: Bool?) in
                        //: guard let self = self else {return}
                        guard let self = self else { return }
                        //: if photos!.count > 0 {
                        if photos!.count > 0 {
                            //: self.showUsePhoto(img: photos![0])
                            self.speed(img: photos![0])
                        }
                }
            }
        }
    }

    //: private func func__camaraPhotos() {
    private func ask() {
        //: TalkingPermissionTool.func__openCaptureDeviceServiceWithBlock(true) { (isOpen: Bool) in
        MultiplePoint.closeicialLine(true) { (isOpen: Bool) in
            //: if isOpen {
            if isOpen {
                //: let imagePicker = UIImagePickerController()
                let imagePicker = UIImagePickerController()
                //: imagePicker.sourceType = .camera
                imagePicker.sourceType = .camera
                //: imagePicker.delegate = self
                imagePicker.delegate = self
                //: self.present(imagePicker, animated: true, completion: {})
                self.present(imagePicker, animated: true, completion: {})
            }
        }
    }

    //: func imagePickerController(_ picker: UIImagePickerController, didFinishPickingMediaWithInfo info: [UIImagePickerController.InfoKey: Any]) {
    func imagePickerController(_ picker: UIImagePickerController, didFinishPickingMediaWithInfo info: [UIImagePickerController.InfoKey: Any]) {
        //: self.dismiss(animated: true) {
        self.dismiss(animated: true) {
            //: if let photo = info[UIImagePickerController.InfoKey.originalImage] as! UIImage? {
            if let photo = info[UIImagePickerController.InfoKey.originalImage] as! UIImage? {
                //: self.showUsePhoto(img: photo)
                self.speed(img: photo)
            }
        }
    }

    //: func imagePickerControllerDidCancel(_ picker: UIImagePickerController) {
    func imagePickerControllerDidCancel(_ picker: UIImagePickerController) {
        //: self.dismiss(animated: true, completion: nil)
        self.dismiss(animated: true, completion: nil)
    }

    /// 展示阅后即焚+私密照片选项
    /// - Parameter img: 图片资源
    //: private func showUsePhoto(img: UIImage) {
    private func speed(img: UIImage) {
        //: guard conversationType == .private &&
        guard conversationType == .private &&
            //: MarginExamineer.share.appStatus == AppSkinStatus.normal.rawValue else {
            MarginExamineer.share.appStatus == AwayPush.normal.rawValue
        else { // 非私聊 && 非审核模式
            //: let sendModel = AbTalkingChatMsgCustomSendModel()
            let sendModel = UserStageModel()
            //: sendModel.isBurn = "0"
            sendModel.isBurn = "0"
            //: sendModel.giftId = "0"
            sendModel.giftId = "0"
            //: self.func__sendPictureMsg(img: img, sendModel: sendModel)
            self.dealCamera(img: img, sendModel: sendModel)
            //: return
            return
        }

        //: let vc = TalkingMomentPhotosVC(images: [img], index: 0, type: .sendIntimatePhoto)
        let vc = QuantityegrityViewController(images: [img], index: 0, type: .sendIntimatePhoto)
        //: vc.sendIntimatePhotoBlock = {[weak self] isBurn, giftId in
        vc.sendIntimatePhotoBlock = { [weak self] isBurn, giftId in
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: let sendModel = AbTalkingChatMsgCustomSendModel()
            let sendModel = UserStageModel()
            //: sendModel.isBurn = isBurn
            sendModel.isBurn = isBurn
            //: sendModel.giftId = giftId
            sendModel.giftId = giftId
            //: self.func__sendPictureMsg(img: img, sendModel: sendModel)
            self.dealCamera(img: img, sendModel: sendModel)
        }
        //: getNavigationController()?.pushViewController(vc, animated: true)
        aboveShadow()?.pushViewController(vc, animated: true)
    }
}

// MARK: - 选择视频

//: extension TalkingPrivateChatController {
extension ViaNative {
    /// 发送短视频
    //: private func func__sendShortVideo() {
    private func adminHide() {
        // 检测用户短视频版本是否可用
        //: guard TalkingPrivateChatManager.checkShortVideo(userVersion: self.userWrap.version) else {
        guard InvisibleJob.mount(userVersion: self.userWrap.version) else {
            //: let gender = self.userWrap.sex == Gender.male.rawValue ? "He".localized:"She".localized
            let gender = self.userWrap.sex == EnvironmentEmpty.male.rawValue ? "He".localized : (String(dataImplementProblemId)).localized
            //: self.func__showStatusBarErrorMsg(showMsg: "%@ is using too low a version".localizedArguments(gender))
            self.exitMarker(showMsg: (String(k_respectiveName.suffix(4)) + "s usin" + String(mainDrawerRelateValue.suffix(5)) + " low a" + String(noti_perceptionTitle.prefix(7)) + data_movieEvolutionPath.replacingOccurrences(of: "effect", with: "n")).theAppear(gender))
            //: return
            return
        }

        //: self.openVideoPicker()
        self.turnSend()
    }

    /// 打开视频相册
    //: private func openVideoPicker() {
    private func turnSend() {
        //: let vc = TalkingImagePickTool.initImagePickerVc(maxCount: 1, allowPhoto: false, allowVideo: true)
        let vc = ZonePosition.exAccelerateReliability(maxCount: 1, allowPhoto: false, allowVideo: true)
        //: vc.modalPresentationStyle = .fullScreen
        vc.modalPresentationStyle = .fullScreen
        //: self.present(vc, animated: true)
        self.present(vc, animated: true)
        //: vc.didFinishPickingVideoHandle = { [weak self] ( coverImage: UIImage?, assets: PHAsset?)  in
        vc.didFinishPickingVideoHandle = { [weak self] (coverImage: UIImage?, assets: PHAsset?) in
            //: guard let self = self else {return}
            guard let self = self else { return }
            //: TalkingImagePickTool.getVideoPath(asset: assets) { filePath in
            ZonePosition.duringSly(asset: assets) { filePath in
                //: guard filePath != nil else { return }
                guard filePath != nil else { return }
                //: let asset = AVURLAsset.init(url: filePath!)
                let asset = AVURLAsset(url: filePath!)
                //: let time = asset.duration
                let time = asset.duration
                //: let seconds = Int(roundf(Float(CMTimeGetSeconds(time))))
                let seconds = Int(roundf(Float(CMTimeGetSeconds(time))))
                //: if seconds >= 5 {
                if seconds >= 5 { // 压缩视频
                    //: self.CompressedVideo(url: filePath!)
                    self.location(url: filePath!)
                    //: } else {
                } else {
                    //: DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 0.5) {
                    DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 0.5) { // 视频压缩取消
                        //: self.func__showStatusBarErrorMsg(showMsg: "Shoot video at least 5 seconds".localized)
                        self.exitMarker(showMsg: (String(notiAKey.prefix(5)) + " vide" + String(data_factExclusiveFormat.prefix(5)) + "leas" + String(kAriaMessage)).localized)
                    }
                }
            }
        }
    }

    /// 视频压缩
    /// - Parameter url: 本地路径
    //: private func CompressedVideo(url: URL) {
    private func location(url: URL) {
        //: let style: TalkingVideoEditorStyle = (conversationType == .private) ? .intimateVideo : .normal
        let style: ReceiveJoin = (conversationType == .private) ? .intimateVideo : .normal
        //: let videoVC = TalkingVideoEditorController(style: style, maxDuration: 60.0, videoURL: url) { videoInfo in
        let videoVC = ResultViewController(style: style, maxDuration: 60.0, videoURL: url) { videoInfo in
            //: guard let videoInfo = videoInfo else { return }
            guard let videoInfo = videoInfo else { return }
            // 压缩成功，实例化模型
            //: let sendModel = AbTalkingChatMsgCustomSendModel()
            let sendModel = UserStageModel()
            //: sendModel.giftId = videoInfo.giftId
            sendModel.giftId = videoInfo.giftId
            //: sendModel.isBurn = videoInfo.isBurn
            sendModel.isBurn = videoInfo.isBurn
            //: sendModel.localCoverImg = videoInfo.coverPath
            sendModel.localCoverImg = videoInfo.coverPath
            //: sendModel.localVideoUrl = videoInfo.videoPath
            sendModel.localVideoUrl = videoInfo.videoPath
            //: sendModel.duration = Int(roundf(Float(videoInfo.duration)))
            sendModel.duration = Int(roundf(Float(videoInfo.duration)))
            // 发送视频消息
            //: self.func__sendVideoMsg(msgData: nil, sendModel: sendModel)
            self.frameValid(msgData: nil, sendModel: sendModel)
        }

        //: ProgressHUD.dismiss()
        BeforeImagePhase.totalGap()
        //: getNavigationController()?.pushViewController(videoVC, animated: true)
        aboveShadow()?.pushViewController(videoVC, animated: true)
    }

    /// 上传视频
    /// - Parameters:
    ///   - msgData: cellData，此处只数据传递
    ///   - sendModel: 消息自定义本地模型
    //: private func cos_UploadVideo(msgData: AlongCrucial, sendModel: AbTalkingChatMsgCustomSendModel) {
    private func undertakeIn(msgData: AlongCrucial, sendModel: UserStageModel) {
        //: TalkingMomentVideoManager.shared.cos_uploadVideo(type: 3, coverPath: sendModel.localCoverImg, videoPath: sendModel.localVideoUrl) { coverUrl, videoUrl, succeed in
        MethodBold.shared.state(type: 3, coverPath: sendModel.localCoverImg, videoPath: sendModel.localVideoUrl) { coverUrl, videoUrl, succeed in
            //: guard succeed else {
            guard succeed else {
                //: self.func__showStatusBarErrorMsg(showMsg: "Upload failed".localized)
                self.exitMarker(showMsg: (String(noti_retainDeemData.suffix(5)) + "d fa" + const_afterId.replacingOccurrences(of: "box", with: "d")).localized)
                // 更改消息状态为失败
                //: self.msgView.changeCustomMsg(msgData, msgId: nil, msgIncome: NSNumber(value: 0), status: .Msg_Status_Fail)
                self.msgView.functional(msgData, tillEffectDensity: nil, flushBorder: NSNumber(value: 0), overShadow: .Msg_Status_Fail)
                
                //: return
                return
            }

            /// 上传成功，刷新本地视频消息，调用发消息接口
            //: sendModel.coverImg = coverUrl
            sendModel.coverImg = coverUrl
            //: sendModel.videoUrl = videoUrl
            sendModel.videoUrl = videoUrl
            //: self.func__sendVideoMsg(msgData: msgData, sendModel: sendModel)
            self.frameValid(msgData: msgData, sendModel: sendModel)
        }
    }
}

// MARK: - 语音消息

//: extension TalkingPrivateChatController {
extension ViaNative {
    //: func func__addVoiceNotification() {
    func foundSet() {
        //: NotificationCenter.default.addObserver(self, selector: #selector(appDidBecomeActive),
        NotificationCenter.default.addObserver(self, selector: #selector(normMayMechanism),
                                               //: name: UIApplication.willEnterForegroundNotification,
                                               name: UIApplication.willEnterForegroundNotification,
                                               //: object: nil)
                                               object: nil)

        //: NotificationCenter.default.addObserver(self, selector: #selector(appDidEnterBack),
        NotificationCenter.default.addObserver(self, selector: #selector(publish),
                                               //: name: UIApplication.didEnterBackgroundNotification,
                                               name: UIApplication.didEnterBackgroundNotification,
                                               //: object: nil)
                                               object: nil)

        //: NotificationCenter.default.addObserver(self,
        NotificationCenter.default.addObserver(self,
                                               //: selector: #selector(audioSessionInterrupted(notification:)),
                                               selector: #selector(maximumBy(notification:)),
                                               //: name: AVAudioSession.interruptionNotification,
                                               name: AVAudioSession.interruptionNotification,
                                               //: object: nil)
                                               object: nil)

        //: NotificationCenter.default.addObserver(self, selector: #selector(insertGiftMsgNotif(notification:)),
        NotificationCenter.default.addObserver(self, selector: #selector(operateClose(notification:)),
                                               //: name: CHAT_ADD_CALL_RECORD_MSG_NOTIFICATION,
                                               name: app_evenStorageString,
                                               //: object: nil)
                                               object: nil)

        //: NotificationCenter.default.addObserver(self,
        NotificationCenter.default.addObserver(self,
                                               //: selector: #selector(newMsgIntimate(notification:)),
                                               selector: #selector(signatureBy(notification:)),
                                               //: name: CHAT_NEW_MSG_NOTIFICATION,
                                               name: mainGuidePath,
                                               //: object: nil)
                                               object: nil)
    }

    //: func func__addOrRrmoveObserverForMsgPlayer(isAdd: Bool) {
    func position(isAdd: Bool) {
        //: if isAdd {
        if isAdd {
            //: TalkingVoiceMsgPlayManager.shared.addDaskManagerDelegate()
            TemporaryOperation.shared.sourceDownTarget()
            //: } else {
        } else {
            //: TalkingVoiceMsgPlayManager.shared.removeDaskManagerDelegate()
            TemporaryOperation.shared.sinceSubstantial()
        }
    }

    //: @objc func appDidBecomeActive() {
    @objc func normMayMechanism() {
        //: func__addOrRrmoveObserverForMsgPlayer(isAdd: true)
        position(isAdd: true)
    }

    //: @objc func appDidEnterBack() {
    @objc func publish() {
        //: func__addOrRrmoveObserverForMsgPlayer(isAdd: false)
        position(isAdd: false)
    }

    //: @objc func audioSessionInterrupted(notification: NSNotification) {
    @objc func maximumBy(notification: NSNotification) {
        //: guard let userInfo = notification.userInfo, let reasonValue = userInfo[AVAudioSessionInterruptionTypeKey] as? UInt else { return }
        guard let userInfo = notification.userInfo, let reasonValue = userInfo[AVAudioSessionInterruptionTypeKey] as? UInt else { return }
        //: switch reasonValue {
        switch reasonValue {
        //: case AVAudioSession.InterruptionType.began.rawValue:
        case AVAudioSession.InterruptionType.began.rawValue: // Began
            //: func__addOrRrmoveObserverForMsgPlayer(isAdd: false)
            position(isAdd: false)
        //: break
        //: case AVAudioSession.InterruptionType.ended.rawValue:
        case AVAudioSession.InterruptionType.ended.rawValue: // End
            //: func__addOrRrmoveObserverForMsgPlayer(isAdd: true)
            position(isAdd: true)
        //: break
        //: default:
        default:
            //: break
            break
        }
    }

    //: func func__DidTouchVoiceCellNeedPlayWithCurrentModel(cellData: BoxStack, cellView: TalkingChatAudioMsgCell) {
    func element(cellData: BoxStack, cellView: WhiteProcessor) {
        //: guard TalkingSocketManager.shared.pacific == false else {
        guard MaterialCurrency.shared.pacific == false else {
            //: self.func__showStatusBarErrorMsg(showMsg: kMessage_videoTalking_limit)
            self.exitMarker(showMsg: constFormalValue)
            //: return
            return
        }

        //: let message = cellData.innerMessage
        let message = cellData.innerMessage

        //: for indexModel in self.msgView.uiMsgs {
        for indexModel in self.msgView.uiMsgs {
            //: if (indexModel as? AlongCrucial)!.isKind(of: BoxStack.self) == false {
            if (indexModel as? AlongCrucial)!.isKind(of: BoxStack.self) == false {
                //: continue
                continue
            }
            //: let audioModel = indexModel as! BoxStack
            let audioModel = indexModel as! BoxStack
            //: audioModel.voiceModel.isPlayingStatus = 2
            audioModel.voiceModel.isPlayingStatus = 2
            //: audioModel.voiceModel.activityShowStatus = 2
            audioModel.voiceModel.activityShowStatus = 2
            // 红点提示隐藏
            //: if (audioModel.isEqual(cellData)) {
            if audioModel.isEqual(cellData) {
                //: cellView.voiceTrackPoint.isHidden = true
                cellView.voiceTrackPoint.isHidden = true
            }
        }
        //: if message != nil {
        if message != nil {
            //: cellData.voiceModel.db_voiceCacheWrap = DBUserInfoManager.cache_getAudioDB(message: message!)
            cellData.voiceModel.db_voiceCacheWrap = ProgressAssociated.triggerReady(message: message!)
        }
        //: TalkingVoiceMsgPlayManager.shared.stopAudioPlayer()
        TemporaryOperation.shared.report()

        //: if TalkingVoiceMsgPlayManager.shared.clickCellId == cellData.voiceModel.db_voiceCacheWrap.msgId {
        if TemporaryOperation.shared.clickCellId == cellData.voiceModel.db_voiceCacheWrap.msgId {
            //: printLog(message: "播放状态:\(TalkingVoiceMsgPlayManager.shared.playStatus)")
            printLog(message: (dataInfoData) + "\(TemporaryOperation.shared.playStatus)")
            //: if TalkingVoiceMsgPlayManager.shared.playStatus != .Unknown {
            if TemporaryOperation.shared.playStatus != .Unknown {
                //: TalkingVoiceMsgPlayManager.shared.playStatus = .Unknown
                TemporaryOperation.shared.playStatus = .Unknown
                //: return
                return
            }
        }
        // 指定播放的cell
        //: TalkingVoiceMsgPlayManager.shared.clickCellId = cellData.voiceModel.db_voiceCacheWrap.msgId
        TemporaryOperation.shared.clickCellId = cellData.voiceModel.db_voiceCacheWrap.msgId
        //: TalkingVoiceMsgPlayManager.shared.playStatus = .Waiting
        TemporaryOperation.shared.playStatus = .Waiting
        //: var waitArray = [cellData.voiceModel]
        var waitArray = [cellData.voiceModel]
        //: if cellData.voiceModel.db_voiceCacheWrap.db_isRead == "0" {
        if cellData.voiceModel.db_voiceCacheWrap.db_isRead == "0" {
            //: let currentIndex = self.msgView.uiMsgs.index(of: cellData)
            let currentIndex = self.msgView.uiMsgs.index(of: cellData)
            //: for index in currentIndex+1 ..< self.msgView.uiMsgs.count {
            for index in currentIndex + 1 ..< self.msgView.uiMsgs.count {
                //: let subModel: AlongCrucial = self.msgView.uiMsgs.object(at: index) as! AlongCrucial
                let subModel: AlongCrucial = self.msgView.uiMsgs.object(at: index) as! AlongCrucial
                //: let subMessage = subModel.innerMessage
                let subMessage = subModel.innerMessage
                //: if subModel.isKind(of: BoxStack.self) {
                if subModel.isKind(of: BoxStack.self) {
                    //: let audioModel = subModel as! BoxStack
                    let audioModel = subModel as! BoxStack
                    //: let voiceModel = audioModel.voiceModel
                    let voiceModel = audioModel.voiceModel
                    //: if subMessage != nil {
                    if subMessage != nil {
                        //: voiceModel.db_voiceCacheWrap = DBUserInfoManager.cache_getAudioDB(message: subMessage!)
                        voiceModel.db_voiceCacheWrap = ProgressAssociated.triggerReady(message: subMessage!)
                    }
                    //: if (voiceModel.db_voiceCacheWrap.db_isRead == "0") {
                    if voiceModel.db_voiceCacheWrap.db_isRead == "0" {
                        //: waitArray.append(voiceModel)
                        waitArray.append(voiceModel)
                    }
                    //: cellData.voiceModel.db_voiceCacheWrap.db_isRead = "1"
                    cellData.voiceModel.db_voiceCacheWrap.db_isRead = "1"
                    //: cellView.voiceTrackPoint.isHidden = true
                    cellView.voiceTrackPoint.isHidden = true
                    //: WCDBVoiceMsgTable.db_updateVoiceMsg(cellData.voiceModel.db_voiceCacheWrap)
                    ComponentMultiple.provideDown(cellData.voiceModel.db_voiceCacheWrap)
                }
            }
        }
        //: TalkingVoiceMsgPlayManager.shared.starPlayMsg(msgArr: waitArray)
        TemporaryOperation.shared.tillReliability(msgArr: waitArray)
    }
}

// MARK: - 用户相关

//: extension TalkingPrivateChatController {
extension ViaNative {
    /// 请求聊天用户信息 V1.8.0 提前到进入页面之前，先判断是否封号
    //: func func__requestChatInfo() {
    func whenTurn() {
        //: guard conversationType == .private || conversationType == .service else { return }
        guard conversationType == .private || conversationType == .service else { return }

        //: self.func__updateUIWithUserWrap()
        self.outVersus()
        //: self.msgView.tableView.reloadData()
        self.msgView.tableView.reloadData()
        // 判断是否需要插入一键打招呼消息
        //: if (self.quickGreetingTextDict?.count ?? 0 > 0) || (self.quickGreetingAudioDict?.count ?? 0 > 0) {
        if (self.quickGreetingTextDict?.count ?? 0 > 0) || (self.quickGreetingAudioDict?.count ?? 0 > 0) {
            //: self.func_sendQuickGreetingMsg(textDict: self.quickGreetingTextDict, audioDict: self.quickGreetingAudioDict)
            self.goldKey(textDict: self.quickGreetingTextDict, audioDict: self.quickGreetingAudioDict)
        }
        //: if self.msgView.uiMsgs.count > 0 && self.userWrap.existSess == false {
        if self.msgView.uiMsgs.count > 0, self.userWrap.existSess == false {
            //: for model in self.msgView.uiMsgs {
            for model in self.msgView.uiMsgs {
                //: let indexModel = model as? AlongCrucial
                let indexModel = model as? AlongCrucial
                //: if indexModel?.isKind(of: BoxStack.self) == true || indexModel?.isKind(of: TalkingChatImageMsgCellData.self) == true || indexModel?.isKind(of: ExceptionKindLocationCreate.self) == true || indexModel?.isKind(of: TalkingChatVideoMsgCellData.self) == true {
                if indexModel?.isKind(of: BoxStack.self) == true || indexModel?.isKind(of: DelayComposition.self) == true || indexModel?.isKind(of: ExceptionKindLocationCreate.self) == true || indexModel?.isKind(of: MeasurePaint.self) == true {
                    //: self.userWrap.existSess = true
                    self.userWrap.existSess = true
                    //: break
                    break
                }
            }
        }
        // 判断是否需要插入音视频提示消息
        //: self.func__privateChatVideo_insertTipMsg()
        self.userDirect()
        // 判断是否需要插入官方客服提示消息
        //: self.func__customerServiceCenter_insertTipMsg()
        self.be()
        /// 判断是否插入已读回执引导充值VIP提示消息
        //: self.func__privateVIPReadReceipt_insertTipsMsg()
        self.policyInvisible()
    }

    /// 刷新UI
    //: func func__updateUIWithUserWrap() {
    func outVersus() {
        //: guard userWrap.uid.count > 0 else { return }
        guard userWrap.uid.count > 0 else { return }
        //: bgImgeView.setUrlImage(urlStr: userWrap.headPic)
        bgImgeView.poolFee(urlStr: userWrap.headPic)
        //: navView.private_updateNavView(userModel: userWrap, isFrom: self.isFrom)
        navView.closedDelicate(userModel: userWrap, isFrom: self.isFrom)
        /// 亲密度头像
        //: self.showFirstIntimateView()
        self.yellowExtra()

        //: if sendGreetBtn.isHidden == false {
        if sendGreetBtn.isHidden == false {
            //: sendGreetBtn.isHidden = userWrap.sex == MarginExamineer.share.loginUserMode.sex
            sendGreetBtn.isHidden = userWrap.sex == MarginExamineer.share.loginUserMode.sex
        }
        //: if beautyPhotosBtn.isHidden == false {
        if beautyPhotosBtn.isHidden == false {
            //: beautyPhotosBtn.isHidden = userWrap.sex == MarginExamineer.share.loginUserMode.sex
            beautyPhotosBtn.isHidden = userWrap.sex == MarginExamineer.share.loginUserMode.sex
        }
        // 备注置顶
        //: notesTopView.refresh(userWrap)
        notesTopView.volumeDown(userWrap)
    }

    //: func func__getFillDraftText() {
    func doingPlayer() {
        //: let userID = "c2c_"+msgView.targetID
        let userID = (String(app_stageDatabaseId.prefix(4))) + msgView.targetID
        //: V2TIMManager.sharedInstance().getConversation(userID) { [weak self] conv in
        V2TIMManager.sharedInstance().getConversation(userID) { [weak self] conv in
            //: guard let self = self else {return}
            guard let self = self else { return }
            //: DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 0.5) {
            DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 0.5) {
                //: let draftText = conv?.draftText ?? ""
                let draftText = conv?.draftText ?? ""
                //: if !draftText.isEmptyString {
                if !draftText.isEmptyString {
                    //: self.msgInputView.inputTextView.text = conv?.draftText
                    self.msgInputView.inputTextView.text = conv?.draftText
                    //: self.msgInputView.inputTextView.becomeFirstResponder()
                    self.msgInputView.inputTextView.becomeFirstResponder()
                }
            }
            //: } fail: { code, desc in
        } fail: { code, desc in
        }
    }

    //: func func__saveFillDraftText() {
    func matchOpen() {
        //: let userID = "c2c_"+msgView.targetID
        let userID = (String(app_stageDatabaseId.prefix(4))) + msgView.targetID
        //: V2TIMManager.sharedInstance().setConversationDraft(userID, draftText: msgInputView.inputTextView.text!) {
        V2TIMManager.sharedInstance().setConversationDraft(userID, draftText: msgInputView.inputTextView.text!) {
            //: } fail: { code, desc in
        } fail: { code, desc in
        }
    }
}

// MARK: - 提示消息

//: extension TalkingPrivateChatController {
extension ViaNative {
    //: func func__TipsNotification() {
    func doingMention() {
        //: NotificationCenter.default.addObserver(self, selector: #selector(insertTipsNotif(notification:)),
        NotificationCenter.default.addObserver(self, selector: #selector(evaluation(notification:)),
                                               //: name: CHAT_TIPS_TEXT_NOTIFICATION,
                                               name: notiSquareFeedbackSourceString,
                                               //: object: nil)
                                               object: nil)
    }

    //: @objc func insertTipsNotif(notification: NSNotification) -> Void {
    @objc func evaluation(notification: NSNotification) {
        //: let userinfo = notification.userInfo as! [String: AnyObject]
        let userinfo = notification.userInfo as! [String: AnyObject]
        //: let celldata: TalkingChatTipsMsgCellData  = userinfo["msg"] as! TalkingChatTipsMsgCellData
        let celldata: ToAdjust = userinfo[(String(const_clearStatePath))] as! ToAdjust

        //: self.msgView.uiMsgs.add(celldata)
        self.msgView.uiMsgs.add(celldata)
        //: self.msgView.tableView.reloadData()
        self.msgView.tableView.reloadData()
        //: self.msgView.paintAcross(true)
        self.msgView.paintAcross(true)
    }

    /// 插入提示消息
    /// - Parameters:
    ///   - tipStr: 提示消息文本
    ///   - needInsertLocal: 是否需要插入本地数据库（默认：是）
    //: func insertTipMsg_Nobalance(tipStr: String, needInsertLocal: Bool = true) {
    func boxAdjust(tipStr: String, needInsertLocal: Bool = true) {
        //: let msgInfo: [String: Any] = ["content": tipStr,
        let msgInfo: [String: Any] = [String(bytes: k_tipData.reversed(), encoding: .utf8)!: tipStr,
                                      //: "msgType": "tips"]
                                      String(bytes: app_plainLeadingCorrectString.map{towardInvite(ease: $0)}, encoding: .utf8)!: (String(const_serverStr))]
        //: var message: AlongCrucial?
        var message: AlongCrucial?
        //: if needInsertLocal == true {
        if needInsertLocal == true { // 需要插入本地
            //: message = TUISocialChatManager.insertTXMessage(withExtral: msgInfo, toUid: msgView.targetID, isSender: true, isTip: true)
            message = IndependentObserve.belowAlongsideLayer(withExtral: msgInfo, toUid: msgView.targetID, isSender: true, isTip: true)
            //: } else {
        } else {
            //: message = TUISocialChatManager.handleTXMessageSend(withExtral: msgInfo, isSender: true, isTip: true)
            message = IndependentObserve.on(withExtral: msgInfo, isSender: true, isTip: true)
        }

        //: guard let message = message else { return }
        guard let message = message else { return }
        //: self.msgView.uiMsgs.add(message)
        self.msgView.uiMsgs.add(message)
        //: self.msgView.tableView.reloadData()
        self.msgView.tableView.reloadData()
        //: self.msgView.paintAcross(true)
        self.msgView.paintAcross(true)
    }

    /// 官方客服插入系统提示消息
    //: func func__customerServiceCenter_insertTipMsg() {
    func be() {
        //: guard conversationType == .service else { return }
        guard conversationType == .service else { return }
        //: TalkingPrivateChatManager.chat_customerServiceCenter_insertTipMsg { tipsStr in
        InvisibleJob.maleOf { tipsStr in
            //: guard tipsStr != nil else { return }
            guard tipsStr != nil else { return }
            // 客服提示消息，只做内存插入展示
            //: self.insertTipMsg_Nobalance(tipStr: tipsStr!, needInsertLocal: false)
            self.boxAdjust(tipStr: tipsStr!, needInsertLocal: false)
        }
    }

    /// 判断是否需要插入免费提示消息
    //: func func__privateAttachSys_insertTipMsg(str: String) {
    func group(str: String) {
        //: guard conversationType == .private else { return }
        guard conversationType == .private else { return }
        //: self.insertTipMsg_Nobalance(tipStr: str)
        self.boxAdjust(tipStr: str)
    }

    /// 判断是否需要插入音视频通话提示消息
    //: func func__privateChatVideo_insertTipMsg() {
    func userDirect() {
        //: guard conversationType == .private else { return }
        guard conversationType == .private else { return }
        //: TalkingPrivateChatManager.chat_privateChatVideo_insertTipMsg(toUid: self.userWrap.uid, msgView: self.msgView) {
        InvisibleJob.acceptVersus(toUid: self.userWrap.uid, msgView: self.msgView) {
            // 插入音视频提示消息
            //: let str = self.userWrap.nickname
            let str = self.userWrap.nickname
            //: self.insertTipMsg_Nobalance(tipStr: "You can use the video chat feature to give %@ a call".localizedArguments(str))
            self.boxAdjust(tipStr: String(bytes: userCapacityMsg.map{zzAnima(fixed: $0)}, encoding: .utf8)!.theAppear(str))
        }
    }

    /// 通话礼物消息
    //: @objc func insertGiftMsgNotif(notification: NSNotification) {
    @objc func operateClose(notification: NSNotification) {
        //: let dict = notification.userInfo
        let dict = notification.userInfo
        //: let cellData: AlongCrucial = dict?["cellData"] as! AlongCrucial
        let cellData: AlongCrucial = dict?[(show_controlKey.lowercased() + "Data")] as! AlongCrucial
        //: self.msgView.uiMsgs.add(cellData)
        self.msgView.uiMsgs.add(cellData)
        //: self.msgView.tableView.reloadData()
        self.msgView.tableView.reloadData()
        //: self.msgView.paintAcross(true)
        self.msgView.paintAcross(true)
    }
}

// MARK: - 回复文案

//: extension TalkingPrivateChatController: PrivateChatReplyTipsProtocol {
extension ViaNative: IncludeBloc {
    //: func func__setReplyTipsView() {
    func despiteLight() {
        //: let key = "\(MarginExamineer.share.loginUserMode.userID)_PrivateChatTipsTitle_\(String(self.msgView.targetID))"
        let key = "\(MarginExamineer.share.loginUserMode.userID)" + (String(constCompleteBurnMsg) + String(k_errorData)) + "\(String(self.msgView.targetID))"
        //: let isShow: Bool = UserDefaults.standard.object(forKey: key) as? Bool ?? false
        let isShow: Bool = UserDefaults.standard.object(forKey: key) as? Bool ?? false
        //: if !isShow && MarginExamineer.share.appStatus == AppSkinStatus.normal.rawValue {
        if !isShow && MarginExamineer.share.appStatus == AwayPush.normal.rawValue {
            //: contentView.addSubview(replyTipsView!)
            contentView.addSubview(replyTipsView!)
            //: replyTipsView?.snp.makeConstraints { make in
            replyTipsView?.snp.makeConstraints { make in
                //: make.bottom.equalTo(msgInputView.snp.top)
                make.bottom.equalTo(msgInputView.snp.top)
                //: make.leading.trailing.equalToSuperview()
                make.leading.trailing.equalToSuperview()
                //: make.height.equalTo(50)
                make.height.equalTo(50)
            }
            //: msgView.view.snp.remakeConstraints { make in
            msgView.view.snp.remakeConstraints { make in
                //: make.leading.trailing.equalToSuperview()
                make.leading.trailing.equalToSuperview()
                //: make.top.equalTo(navView.snp.bottom)
                make.top.equalTo(navView.snp.bottom)
                //: make.bottom.equalTo(replyTipsView!.snp.top)
                make.bottom.equalTo(replyTipsView!.snp.top)
            }
            //: self.replyTipsView?.setTipsStrView()
            self.replyTipsView?.quit()
            // 修改banner位置
            //: changeBannerViewFream(isMiniView: self.bannerView.isMiniView)
            third(isMiniView: self.bannerView.isMiniView)
            //: } else {
        } else {
            //: removeReplyTipsView()
            marginHome()
        }
    }

    //: func seleteReplyTipsMessage(str: String) {
    func embraceBalance(str: String) {
        //: self.userWrap.existSess = true
        self.userWrap.existSess = true
        //: func__sendTextMsg(msgStr: str, quoteModel: nil)
        now(msgStr: str, quoteModel: nil)
        //: removeReplyTipsView()
        marginHome()
    }

    //: func removeReplyTipsView() {
    func marginHome() {
        //: if conversationType == .private {
        if conversationType == .private {
            //: if self.replyTipsView != nil {
            if self.replyTipsView != nil {
                //: self.replyTipsView?.removeFromSuperview()
                self.replyTipsView?.removeFromSuperview()
                //: self.replyTipsView = nil
                self.replyTipsView = nil
            }
            //: msgView.view.snp.remakeConstraints { make in
            msgView.view.snp.remakeConstraints { make in
                //: make.leading.trailing.equalToSuperview()
                make.leading.trailing.equalToSuperview()
                //: make.top.equalTo(navView.snp.bottom)
                make.top.equalTo(navView.snp.bottom)
                //: make.bottom.equalTo(msgInputView.snp.top)
                make.bottom.equalTo(msgInputView.snp.top)
            }
            //: changeBannerViewFream(isMiniView: self.bannerView.isMiniView)
            third(isMiniView: self.bannerView.isMiniView)
        }
    }
}

// MARK: - 问题回答消息

//: extension TalkingPrivateChatController {
extension ViaNative {
    /// 判断问题回答消息遮罩是否展示
    //: private func refreshRechargeRequiredView() {
    private func painterPurple() {
        //: guard MarginExamineer.share.loginUserMode.sex == Gender.male.rawValue else { return }
        guard MarginExamineer.share.loginUserMode.sex == EnvironmentEmpty.male.rawValue else { return }
        //: if userWrap.isRecharge == 1 || userWrap.isOfficial {
        if userWrap.isRecharge == 1 || userWrap.isOfficial { // 已充值过
            //: quickReplayView.isHidden = true
            quickReplayView.isHidden = true
            //: rechargeView.isHidden = true
            rechargeView.isHidden = true

            //: } else {
        } else { // 未充值
            //: rechargeView.isHidden = false
            rechargeView.isHidden = false
            //: self.checkQuestionQuickReplayView()
            self.finishShared()
        }
    }

    /// 检查快捷回复视图是否展示
    //: private func checkQuestionQuickReplayView() {
    private func finishShared() {
        //: guard userWrap.isOfficial == false else { return }
        guard userWrap.isOfficial == false else { return }
        // 检查男性侧快捷回复是否展示（延时为了拿到uiMsgs数据）
        //: DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 1) {
        DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 1) {
            //: TalkingPrivateChatManager.chat_onNewMessage_checkQuestionQuickReplayMsg(isRecharge: self.userWrap.isRecharge == 1, msgView: self.msgView) { msgInfo in
            InvisibleJob.writeOrComputeSubsequent(isRecharge: self.userWrap.isRecharge == 1, msgView: self.msgView) { msgInfo in
                //: guard let msgInfo = msgInfo else { return }
                guard let msgInfo = msgInfo else { return }
                //: DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 1) {
                DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 1) {
                    //: self.quickReplayView.isHidden = false
                    self.quickReplayView.isHidden = false
                    //: self.quickReplayView.refreshQuickReplayView(msgInfo: msgInfo)
                    self.quickReplayView.numberegrity(msgInfo: msgInfo)
                }
            }
        }
    }
}

// MARK: - 半屏页面展示/隐藏动画

//: extension TalkingPrivateChatController {
extension ViaNative {
    /// 半屏展示动画
    //: private func half_showViewAnimation() {
    private func restriction() {
        //: guard self.isFrom == .LiveRoom else { return }
        guard self.isFrom == .LiveRoom else { return }
        //: NotificationCenter.default.post(name: LIVE_HALF_VIEW_SHOW, object: nil, userInfo: nil)
        NotificationCenter.default.post(name: data_eagerMsg, object: nil, userInfo: nil)
        //: self.view.frame = CGRect(x: 0, y: ScreenHeight-HalfViewTopMargin, width: ScreenWidth, height: ScreenHeight)
        self.view.frame = CGRect(x: 0, y: appLoadMessage - main_saltStr, width: show_errorChangeFormat, height: appLoadMessage)
        //: UIView.animate(withDuration: 0.25) {
        UIView.animate(withDuration: 0.25) {
            //: self.view.frame = CGRect(x: 0,
            self.view.frame = CGRect(x: 0,
                                     //: y: 0,
                                     y: 0,
                                     //: width: ScreenWidth,
                                     width: show_errorChangeFormat,
                                     //: height: ScreenHeight)
                                     height: appLoadMessage)
        }
    }

    /// 半屏页消失动画
    /// - Parameter removeChatList: 是否需要移除聊天列表
    //: private func half_dismissViewAnimation(_ removeChatList: Bool) {
    private func read(_ removeChatList: Bool) {
        //: guard self.isFrom == .LiveRoom else { return }
        guard self.isFrom == .LiveRoom else { return }

        //: self.view.endEditing(true)
        self.view.endEditing(true)
        // 移除聊天列表
        //: if removeChatList == true, let currentVC = self.currentViewController() {
        if removeChatList == true, let currentVC = self.myAcross() {
            //: for vc in currentVC.children {
            for vc in currentVC.children {
                //: if vc.isKind(of: TalkingChatListViewController.self) {
                if vc.isKind(of: ElementPrepareViewController.self) {
                    //: vc.removeFromParent()
                    vc.removeFromParent()
                    //: vc.view.removeFromSuperview()
                    vc.view.removeFromSuperview()
                    //: break
                    break
                }
            }
        }

        // 移除私聊页
        //: UIView.animate(withDuration: 0.25) {
        UIView.animate(withDuration: 0.25) {
            //: self.view.frame = CGRect(x: 0,
            self.view.frame = CGRect(x: 0,
                                     //: y: ScreenHeight-HalfViewTopMargin,
                                     y: appLoadMessage - main_saltStr,
                                     //: width: ScreenWidth,
                                     width: show_errorChangeFormat,
                                     //: height: ScreenHeight)
                                     height: appLoadMessage)
            //: } completion: { finish in
        } completion: { finish in
            //: self.removeFromParent()
            self.removeFromParent()
            //: self.view.removeFromSuperview()
            self.view.removeFromSuperview()
        }
    }
}

// MARK: - Layout

//: extension TalkingPrivateChatController {
extension ViaNative {
    /// 创建UI
    //: private func createUI() {
    private func unlessSquare() {
        //: if conversationType == .private {
        if conversationType == .private {
            //: msgView.view.backgroundColor = UIColor.clear
            msgView.view.backgroundColor = UIColor.clear
            //: msgView.tableView.backgroundColor = UIColor.clear
            msgView.tableView.backgroundColor = UIColor.clear
            //: } else {
        } else {
            //: msgView.view.backgroundColor = UIColor.clear
            msgView.view.backgroundColor = UIColor.clear
            //: msgView.tableView.backgroundColor = UIColor(hex: "F5F5F5")
            msgView.tableView.backgroundColor = UIColor(hex: (String(userMageUrl)))
        }
        //: view.backgroundColor = (self.isFrom == .LiveRoom) ? UIColor.clear:UIColor.white
        view.backgroundColor = (self.isFrom == .LiveRoom) ? UIColor.clear : UIColor.white
        //: view.addSubview(contentView)
        view.addSubview(contentView)

        //: if conversationType == .private {
        if conversationType == .private {
            //: contentView.addSubview(bgImgeView)
            contentView.addSubview(bgImgeView)
            //: contentView.addSubview(bgView)
            contentView.addSubview(bgView)
            //: contentView.addSubview(coverView)
            contentView.addSubview(coverView)
            //: self.addChild(msgView)
            self.addChild(msgView)
            //: contentView.addSubview(msgView.view)
            contentView.addSubview(msgView.view)
            //: contentView.addSubview(beautyPhotosBtn)
            contentView.addSubview(beautyPhotosBtn)
            //: contentView.addSubview(sendGreetBtn)
            contentView.addSubview(sendGreetBtn)
            //: if self.isFrom == .Normal {
            if self.isFrom == .Normal {
                //: contentView.addSubview(bannerView)
                contentView.addSubview(bannerView)
            }
            //: contentView.addSubview(floatScreenView)
            contentView.addSubview(floatScreenView)
            //: contentView.addSubview(giftEffectView)
            contentView.addSubview(giftEffectView)
            //: contentView.addSubview(navView)
            contentView.addSubview(navView)
            //: contentView.addSubview(notesTopView)
            contentView.addSubview(notesTopView)
            //: view.addSubview(msgInputView)
            view.addSubview(msgInputView)
            //: view.addSubview(rechargeView)
            view.addSubview(rechargeView)
            //: view.addSubview(quickReplayView)
            view.addSubview(quickReplayView)

            //: } else if conversationType == .service {
        } else if conversationType == .service {
            //: self.addChild(msgView)
            self.addChild(msgView)
            //: contentView.addSubview(msgView.view)
            contentView.addSubview(msgView.view)
            //: contentView.addSubview(navView)
            contentView.addSubview(navView)
            //: view.addSubview(msgInputView)
            view.addSubview(msgInputView)

            //: } else {
        } else {
            //: title = "System notification".localized
            title = (String(show_changeData) + app_colorAgileStr.replacingOccurrences(of: "only", with: "i")).localized
            //: contentView.addSubview(feedView)
            contentView.addSubview(feedView)
            //: feedView.addSubview(feedbackBtn)
            feedView.addSubview(feedbackBtn)
            //: self.addChild(msgView)
            self.addChild(msgView)
            //: contentView.addSubview(msgView.view)
            contentView.addSubview(msgView.view)
        }
    }

    /// 约束
    //: private func createUIConstraints() {
    private func except() {
        //: contentView.snp.makeConstraints { make in
        contentView.snp.makeConstraints { make in
            //: make.bottom.leading.trailing.equalToSuperview()
            make.bottom.leading.trailing.equalToSuperview()
            //: make.top.equalTo((self.isFrom == .LiveRoom) ? HalfViewTopMargin:0)
            make.top.equalTo((self.isFrom == .LiveRoom) ? main_saltStr : 0)
        }

        //: if self.isFrom == .LiveRoom {
        if self.isFrom == .LiveRoom {
            //: contentView.layoutIfNeeded()
            contentView.layoutIfNeeded()
            //: contentView.Corner(width: contentView.width,
            contentView.locationBody(width: contentView.width,
                                     //: height: contentView.height,
                                     height: contentView.height,
                                     //: corners: [.topLeft, .topRight],
                                     corners: [.topLeft, .topRight],
                                     //: cornerRadii: .init(width: 8, height: 8))
                                     cornerRadii: .init(width: 8, height: 8))
        }

        //: if conversationType == .private {
        if conversationType == .private {
            //: bgView.snp.makeConstraints { make in
            bgView.snp.makeConstraints { make in
                //: make.edges.equalToSuperview()
                make.edges.equalToSuperview()
            }
            //: bgImgeView.snp.makeConstraints { make in
            bgImgeView.snp.makeConstraints { make in
                //: make.edges.equalToSuperview()
                make.edges.equalToSuperview()
            }
            //: coverView.snp.makeConstraints { make in
            coverView.snp.makeConstraints { make in
                //: make.edges.equalToSuperview()
                make.edges.equalToSuperview()
            }
            //: navView.snp.makeConstraints { make in
            navView.snp.makeConstraints { make in
                //: make.leading.trailing.top.equalToSuperview()
                make.leading.trailing.top.equalToSuperview()
                //: make.height.equalTo((self.isFrom == .LiveRoom) ? NavigationBarHeight:StatusBarNavigationBarHeight)
                make.height.equalTo((self.isFrom == .LiveRoom) ? constPainterPath : k_underSubtleTitle)
            }
            //: notesTopView.snp.makeConstraints { make in
            notesTopView.snp.makeConstraints { make in
                //: make.top.equalTo(navView.snp.bottom)
                make.top.equalTo(navView.snp.bottom)
                //: make.leading.equalTo(10)
                make.leading.equalTo(10)
                //: make.trailing.equalTo(-10)
                make.trailing.equalTo(-10)
                //: make.height.equalTo(36)
                make.height.equalTo(36)
            }
            //: msgInputView.snp.makeConstraints { make in
            msgInputView.snp.makeConstraints { make in
                //: make.leading.trailing.bottom.equalTo(view)
                make.leading.trailing.bottom.equalTo(view)
                //: make.height.equalTo(msgInputView.BackViewHeight)
                make.height.equalTo(msgInputView.BackViewHeight)
            }
            //: msgView.view.snp.makeConstraints { make in
            msgView.view.snp.makeConstraints { make in
                //: make.leading.trailing.equalToSuperview()
                make.leading.trailing.equalToSuperview()
                //: make.top.equalTo(navView.snp.bottom)
                make.top.equalTo(navView.snp.bottom)
                //: if beautyPhotosBtn.isHidden {
                if beautyPhotosBtn.isHidden {
                    //: make.bottom.equalTo(msgInputView.snp.top)
                    make.bottom.equalTo(msgInputView.snp.top)
                    //: } else {
                } else {
                    //: make.bottom.equalTo(beautyPhotosBtn.snp.top).offset(-5)
                    make.bottom.equalTo(beautyPhotosBtn.snp.top).offset(-5)
                }
            }
            //: giftEffectView.snp.makeConstraints { make in
            giftEffectView.snp.makeConstraints { make in
                //: make.edges.equalToSuperview()
                make.edges.equalToSuperview()
            }
            //: sendGreetBtn.snp.makeConstraints { make in
            sendGreetBtn.snp.makeConstraints { make in
                //: make.bottom.equalTo(beautyPhotosBtn)
                make.bottom.equalTo(beautyPhotosBtn)
                //: make.leading.equalTo(10)
                make.leading.equalTo(10)
                //: make.size.equalTo(beautyPhotosBtn)
                make.size.equalTo(beautyPhotosBtn)
            }
            //: beautyPhotosBtn.snp.makeConstraints { make in
            beautyPhotosBtn.snp.makeConstraints { make in
                //: make.bottom.equalTo(msgInputView.snp.top).offset(-8)
                make.bottom.equalTo(msgInputView.snp.top).offset(-8)
                //: make.leading.equalTo(sendGreetBtn.snp.trailing).offset(9)
                make.leading.equalTo(sendGreetBtn.snp.trailing).offset(9)
                //: make.width.equalTo(134)
                make.width.equalTo(134)
                //: make.height.equalTo(30)
                make.height.equalTo(30)
            }
            //: floatScreenView.snp.makeConstraints { make in
            floatScreenView.snp.makeConstraints { make in
                //: make.top.equalTo(navView.snp.bottom)
                make.top.equalTo(navView.snp.bottom)
                //: make.leading.trailing.equalToSuperview()
                make.leading.trailing.equalToSuperview()
                //: make.height.equalTo(40)
                make.height.equalTo(40)
            }
            //: rechargeView.snp.makeConstraints { make in
            rechargeView.snp.makeConstraints { make in
                //: make.leading.trailing.bottom.equalToSuperview()
                make.leading.trailing.bottom.equalToSuperview()
                //: make.height.equalTo(rechargeView.ContentView_H)
                make.height.equalTo(rechargeView.ContentView_H)
            }
            //: quickReplayView.snp.makeConstraints { make in
            quickReplayView.snp.makeConstraints { make in
                //: make.leading.equalTo(24)
                make.leading.equalTo(24)
                //: make.width.equalTo(270)
                make.width.equalTo(270)
                //: make.bottom.equalTo(rechargeView.snp.top).offset(-13)
                make.bottom.equalTo(rechargeView.snp.top).offset(-13)
            }
            //: if self.isFrom == .Normal {
            if self.isFrom == .Normal {
                //: bannerView.snp.makeConstraints { make in
                bannerView.snp.makeConstraints { make in
                    //: make.trailing.equalToSuperview()
                    make.trailing.equalToSuperview()
                    //: make.bottom.equalTo(beautyPhotosBtn.snp.top)
                    make.bottom.equalTo(beautyPhotosBtn.snp.top)
                    //: make.height.equalTo(106)
                    make.height.equalTo(106)
                    //: make.width.equalTo(93)
                    make.width.equalTo(93)
                }
            }

            //: } else if conversationType == .service {
        } else if conversationType == .service {
            //: navView.snp.makeConstraints { make in
            navView.snp.makeConstraints { make in
                //: make.leading.trailing.top.equalToSuperview()
                make.leading.trailing.top.equalToSuperview()
                //: make.height.equalTo((self.isFrom == .LiveRoom) ? NavigationBarHeight:StatusBarNavigationBarHeight)
                make.height.equalTo((self.isFrom == .LiveRoom) ? constPainterPath : k_underSubtleTitle)
            }
            //: msgInputView.snp.makeConstraints { make in
            msgInputView.snp.makeConstraints { make in
                //: make.leading.trailing.bottom.equalTo(view)
                make.leading.trailing.bottom.equalTo(view)
                //: make.height.equalTo(msgInputView.BackViewHeight)
                make.height.equalTo(msgInputView.BackViewHeight)
            }
            //: msgView.view.snp.makeConstraints { make in
            msgView.view.snp.makeConstraints { make in
                //: make.leading.trailing.equalToSuperview()
                make.leading.trailing.equalToSuperview()
                //: make.top.equalTo(navView.snp.bottom)
                make.top.equalTo(navView.snp.bottom)
                //: make.bottom.equalTo(msgInputView.snp.top)
                make.bottom.equalTo(msgInputView.snp.top)
            }

            //: } else {
        } else {
            //: feedView.snp.makeConstraints { make in
            feedView.snp.makeConstraints { make in
                //: make.leading.trailing.bottom.equalTo((view))
                make.leading.trailing.bottom.equalTo(view)
                //: make.height.equalTo(56+kDeviceSafeBottomHeight)
                make.height.equalTo(56 + const_closedAddSegmentPath)
            }
            //: feedbackBtn.snp.makeConstraints { make in
            feedbackBtn.snp.makeConstraints { make in
                //: make.leading.trailing.top.equalTo(feedView)
                make.leading.trailing.top.equalTo(feedView)
                //: make.height.equalTo(56)
                make.height.equalTo(56)
            }
            //: msgView.view.snp.makeConstraints { make in
            msgView.view.snp.makeConstraints { make in
                //: make.leading.trailing.top.equalTo((view))
                make.leading.trailing.top.equalTo(view)
                //: make.bottom.equalTo(feedView.snp.top)
                make.bottom.equalTo(feedView.snp.top)
            }
        }
    }

    /// 事件绑定
    //: func bindInteraction() {
    func sumegration() {
        //: navView.backButton.rx.tap
        navView.backButton.rx.tap
            //: .subscribe(onNext: { [weak self] in
            .subscribe(onNext: { [weak self] in
                //: guard let self = self else {return}
                guard let self = self else { return }
                //: self.backButtonClick()
                self.challenge()
                //: })
            })
            //: .disposed(by: disposeBag)
            .disposed(by: disposeBag)
        //: navView.moreButton.rx.controlEvent(.touchUpInside)
        navView.moreButton.rx.controlEvent(.touchUpInside)
            //: .subscribe(onNext: { [weak self] in
            .subscribe(onNext: { [weak self] in
                //: guard let self = self else {return}
                guard let self = self else { return }
                //: self.view.endEditing(true)
                self.view.endEditing(true)
                //: self.moreBtnClick()
                self.privacy()
                //: })
            })
            //: .disposed(by: disposeBag)
            .disposed(by: disposeBag)
        //: navView.videoCallBtn.rx.controlEvent(.touchUpInside)
        navView.videoCallBtn.rx.controlEvent(.touchUpInside)
            //: .subscribe(onNext: { [weak self] in
            .subscribe(onNext: { [weak self] in
                //: guard let self = self else {return}
                guard let self = self else { return }
                //: self.view.endEditing(true)
                self.view.endEditing(true)
                //: self.func__selectMoreFunAction(actionType: .VideoCall)
                self.over(actionType: .VideoCall)
                //: uploadRecord.uploadRecordEvent(eventID: ClickChatVideoButton, toUid: self.userWrap.uid )
                noti_loadResolveMsg.priorityFun(eventID: user_grayLargePath, toUid: self.userWrap.uid)
                //: })
            })
            //: .disposed(by: disposeBag)
            .disposed(by: disposeBag)
    }
}
