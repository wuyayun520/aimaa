
//: Declare String Begin

/*: "SOCKET_ADDR_KEY" :*/
fileprivate let kCameraKey:String = "SOquantity"
fileprivate let show_romanMsg:String = "margin magnitudeKET_"

/*: "SOCKET_VIDEOCHAT_REQUESTCALL_NOTIFICATION" :*/
fileprivate let user_saveMistUrl:[UInt8] = [0xa0,0xbc,0xb0,0xb8,0xb6,0xa7,0xac,0xa5,0xba,0xb7,0xb6,0xbc,0xb0,0xbb,0xb2,0xa7,0xac,0xa1,0xb6,0xa2,0xa6,0xb6,0xa0,0xa7,0xb0,0xb2,0xbf,0xbf,0xac,0xbd,0xbc,0xa7,0xba,0xb5,0xba,0xb0,0xb2,0xa7,0xba,0xbc,0xbd]

private func ageDoingdHandle(about num: UInt8) -> UInt8 {
    return num ^ 243
}

/*: "RECIVE_FLOATSCREEN_NOTIFICATION" :*/
fileprivate let kProPath:[UInt8] = [0xe4,0xf3,0xf5,0xff,0xe0,0xf3,0xe9,0xf0,0xfa,0xf9,0xf7,0xe2,0xe5,0xf5,0xe4,0xf3,0xf3,0xf8,0xe9,0xf8,0xf9,0xe2,0xff,0xf0,0xff,0xf5,0xf7,0xe2,0xff,0xf9,0xf8]

private func micWanderSecurity(discount num: UInt8) -> UInt8 {
    return num ^ 182
}

/*: "SOCKET 连接成功." :*/
fileprivate let user_eventCarrierData:[Character] = ["S","O","C","K","E","T"," ","连","接","成","功","."]

/*: "SOCKET 连接失败, error: :*/
fileprivate let appPanNameTitle:[Character] = ["S","O","C","K","E","T"," ","连"]
fileprivate let showSayPath:String = "接\u{5931}败,"
fileprivate let data_associatedId:String = " error:since drag"

/*: "SOCKET 失败 :*/
fileprivate let showPleaseNumberLogFormat:String = "clearly"
fileprivate let k_hungStr:[Character] = ["O","C","K","E","T"," ","失","败"]

/*: 次, 切换过Socket域名重新尝试，地址： :*/
fileprivate let show_lightKey:String = "次, 切output"
fileprivate let noti_formMessage:String = "et域名重entity ease hang word warn"
fileprivate let user_stopKey:[Character] = ["新","尝","试","\u{ff0c}","地","址","："]

/*: "SOCKET 已关闭, code: :*/
fileprivate let notiUnknownTitle:String = "SObackground"
fileprivate let constShallExpectedData:String = "KET feature column delivery sustainable tick"
fileprivate let userKitBlueByString:String = " code:distribution he generate could regular"

/*: , reason: :*/
fileprivate let const_secureData:String = "active tun appeal farmer, reason"
fileprivate let constReserveMsg:String = ":"

/*: /?sid= :*/
fileprivate let const_cancelFileKey:String = "/"
fileprivate let const_graphicPath:String = "heavy customer invite turn media?sid="

/*: &uid= :*/
fileprivate let showExhaustKey:String = "educator media&uid="

/*: &platform=iphone&version= :*/
fileprivate let userFoundAdKey:String = "&platdetail unable resource person"
fileprivate let data_dueCommunicateValue:String = "bodypho"
fileprivate let appKeyLessWhipKey:String = "rsion=template spread hour nearby"

/*: &packageId= :*/
fileprivate let noti_sayVoiceMessage:String = "&pacfavorite m route future"
fileprivate let user_contactWithId:[Character] = ["k","a","g","e","I","d","="]

/*: "1v1server. :*/
fileprivate let app_writtenUrl:String = "crush task venue stage1v1serv"
fileprivate let kLocateDenseName:String = "er.am standard leading"

/*: .com:7766" :*/
fileprivate let app_followRecordPath:String = ".com:cap fun contrast"

/*: "43.129.185.194:7767" :*/
fileprivate let data_regularMsg:String = "personal3.12"
fileprivate let kSkipId:String = "194:"

/*: "SOCKET 网络reachabilityChanged监听，断网关闭." :*/
fileprivate let k_transitionUrl:[UInt8] = [0x56,0x4a,0x46,0x4e,0x40,0x51,0x25,0xe2,0xb8,0x94,0xe2,0xbe,0x99,0x77,0x60,0x64,0x66,0x6d,0x64,0x67,0x6c,0x69,0x6c,0x71,0x7c,0x46,0x6d,0x64,0x6b,0x62,0x60,0x61,0xe2,0x9e,0x94,0xe0,0x95,0xa9,0xea,0xb9,0x89,0xe3,0x93,0xa8,0xe2,0xb8,0x94,0xe0,0x80,0xb6,0xec,0x92,0xa8,0x2b]

private func persistLayer(assistant num: UInt8) -> UInt8 {
    return num ^ 5
}

/*: "cmd" :*/
fileprivate let userReserveKey:[Character] = ["c","m","d"]

/*: "ping" :*/
fileprivate let showToolPath:String = "piforeg"

/*: "callHeartbeat" :*/
fileprivate let showHeadFormat:[Character] = ["c","a","l","l","H","e","a","r","t","b","e","a"]
fileprivate let dataConversionUrl:String = "used"

/*: "recievedDate" :*/
fileprivate let const_sourceStr:[Character] = ["r","e","c","i","e","v"]
fileprivate let const_ageValue:[Character] = ["e","d","D","a","t","e"]

/*: "liveHeartBeat" :*/
fileprivate let k_rootAcheString:[Character] = ["l","i","v","e","H","e","a","r"]
fileprivate let k_conSequenceId:[Character] = ["t","B","e","a","t"]

/*: "🔗 关闭 socket" :*/
fileprivate let userReloadlyData:[Character] = ["🔗"," ","关","闭"," ","s","o","c","k","e","t"]

/*: "The user logs out" :*/
fileprivate let kMomentMessage:String = "each vertical insertThe u"
fileprivate let mainBeanAllowUrl:String = "logs outdad must status"

/*: "SOCKET  :*/
fileprivate let main_natureGiftData:[Character] = ["S","O","C","K","E","T"," "]

/*: 重连. 重试次数： :*/
fileprivate let dataShrimpStr:[Character] = ["重","连","."," "]
fileprivate let const_thinPath:String = "重\u{8bd5}次数："

/*: "SOCKET 连接正常 不需要重新连接." :*/
fileprivate let app_constructMessage:[Character] = ["S","O","C","K","E","T"," ","连","接","正","常"," ","不","需","要","\u{91cd}","新"]
fileprivate let showHereId:[Character] = ["连","接","."]

/*: "SOCKET 正在连接 不需要重新连接." :*/
fileprivate let kRandomKey:[Character] = ["S","O","C","K","E","T"," ","正"]
fileprivate let data_actualNetKey:[Character] = ["\u{5728}","连","接"," ","不","需","要","重","\u{65b0}","\u{8fde}","接","."]

/*: "&resume=1" :*/
fileprivate let noti_tarKey:[Character] = ["&","r","e","s","u"]
fileprivate let mainVeryMsg:String = "me=1canvas today edit box"

/*: "&lang= :*/
fileprivate let user_fullPath:String = "&lang=brown salt read"

/*: "SOCKET 开始连接, 地址： :*/
fileprivate let constHungString:String = "SOheadK"
fileprivate let appCleanMsg:[Character] = ["连","接"]
fileprivate let show_successfulString:String = ", 地址："

/*: "onNetDisabled" :*/
fileprivate let user_cookieString:String = "onNetmeet line split processing write"

/*: "errno" :*/
fileprivate let notiQualityId:String = "errnclean"

/*: "data" :*/
fileprivate let kRecentId:String = "dfarmertfarmer"

/*: "SOCKET 连接保活cmd: ping." :*/
fileprivate let userFutureData:String = "tempCK"
fileprivate let show_distributionFormat:String = "接保活cmdcustomer pose woman render"

/*: "SOCKET trtc保活cmd: callHeartbeat." :*/
fileprivate let showGameString:[UInt8] = [0x60,0x7c,0x70,0x78,0x76,0x67,0x13,0x47,0x41,0x47,0x50,0xd7,0x8c,0xae,0xd5,0x87,0x88,0x50,0x5e,0x57,0x9,0x13,0x50,0x52,0x5f,0x5f,0x7b,0x56,0x52,0x41,0x47,0x51,0x56,0x52,0x47,0x1d]

private func couldCountro(schedule num: UInt8) -> UInt8 {
    return num ^ 51
}

/*: "SOCKET live保活cmd: liveHeartBeat." :*/
fileprivate let data_withinName:[UInt8] = [0xd3,0xcf,0xc3,0xcb,0xc5,0xd4,0xa0,0xec,0xe9,0xf6,0xe5,0x64,0x3f,0x1d,0x66,0x34,0x3b,0xe3,0xed,0xe4,0xba,0xa0,0xec,0xe9,0xf6,0xe5,0xc8,0xe5,0xe1,0xf2,0xf4,0xc2,0xe5,0xe1,0xf4,0xae]

/*: "onP" :*/
fileprivate let k_automobileString:[Character] = ["o","n","P"]

/*: "msg" :*/
fileprivate let kReliabilityDrawerData:[Character] = ["m","s","g"]

/*: "🔗 处理消息：cmdName: :*/
fileprivate let dataFollowStr:String = "🔗 处\u{7406}\u{6d88}"
fileprivate let kTargetName:String = "dNameagain just successful"
fileprivate let notiBarData:String = "hero"

/*: , errNo: :*/
fileprivate let mainDirectInsertName:String = ", erredge spend root"
fileprivate let dataPartySegmentString:[Character] = ["N","o",":"]

/*: , msg: :*/
fileprivate let show_evaluatePath:String = "queen aching queen hang, msg:"

/*: , data: :*/
fileprivate let app_endStr:String = ", data:feed note limit"

/*: "SOCKET 接收异常消息：cmdName: :*/
fileprivate let data_distributionKey:[Character] = ["S","O","C","K","E","T"," ","接","收","异","\u{5e38}","\u{6d88}","息","：","c","m","d","N","a"]
fileprivate let appCurrentlyName:[Character] = ["m","e",":"]

/*: "onReceivedGift" :*/
fileprivate let const_bulletKey:String = "shall sh snap club extraonRece"
fileprivate let mainSlyMsg:String = "Giftevaluate distinction bag collect"

/*: "onVideoMatch" :*/
fileprivate let const_disclosePrimaryMsg:String = "onVidmoment box"

/*: "onRequestCall" :*/
fileprivate let showGraphicMsg:String = "highlight uniform besideonReque"
fileprivate let k_pressureStr:String = "agile send zone gravity popstCall"

/*: "onCancelCall" :*/
fileprivate let kPanStr:[Character] = ["o","n","C","a","n","c","e","l","C"]
fileprivate let noti_sinceValue:String = "capacityl"

/*: "onRefuseCall" :*/
fileprivate let k_romanUrl:[Character] = ["o","n","R","e","f","u"]
fileprivate let app_everyRocketString:[Character] = ["s","e","C","a","l","l"]

/*: "onAcceptCall" :*/
fileprivate let appDefineMessage:[Character] = ["o","n"]
fileprivate let main_thirdFormat:String = "him white mechanism titi hickAcce"

/*: "onStopCall" :*/
fileprivate let data_proPath:[Character] = ["o","n","S","t","o","p","C","a"]
fileprivate let dataOperateCancelStr:String = "birthdaybirthday"

/*: "freeCallTimes" :*/
fileprivate let main_shadowUrl:String = "FREE"

/*: "mfCoin" :*/
fileprivate let main_itStreamPath:String = "place version sustainable elementmfCoin"

/*: "onMissCoin" :*/
fileprivate let user_limitKey:String = "unless"
fileprivate let data_placeStr:String = "nMissCoindescription bad my job job"

/*: "acceptCall" :*/
fileprivate let data_pureTarMessage:String = "acceindependent"
fileprivate let kLargeId:String = "basic"

/*: "stopCall" :*/
fileprivate let show_implementFormat:String = "stopCalbasic true hard"
fileprivate let kAdviceId:String = "rating"

/*: "requestCall" :*/
fileprivate let main_rashMsg:[Character] = ["r","e","q","u","e","s","t","C","a","l","l"]

/*: "[Invite you to a video call...]" :*/
fileprivate let data_destroyStreakKey:[UInt8] = [0x3f,0x2d,0x52,0x5a,0x4d,0x58,0x49,0x4,0x5d,0x53,0x59,0x4,0x58,0x53,0x4,0x45,0x4,0x5a,0x4d,0x48,0x49,0x53,0x4,0x47,0x45,0x50,0x50,0x12,0x12,0x12,0x41]

fileprivate func injuryLimited(net num: UInt8) -> UInt8 {
    let value = Int(num) - 228
    if value < 0 {
        return UInt8(value + 256)
    } else {
        return UInt8(value)
    }
}

/*: "type" :*/
fileprivate let mainDesignPureTitle:[Character] = ["t","y","p","e"]

/*: "[Invite you to a voice call...]" :*/
fileprivate let user_enoughString:[UInt8] = [0xeb,0xf9,0xde,0xc6,0xd9,0xc4,0xd5,0x90,0xc9,0xdf,0xc5,0x90,0xc4,0xdf,0x90,0xd1,0x90,0xc6,0xdf,0xd9,0xd3,0xd5,0x90,0xd3,0xd1,0xdc,0xdc,0x9e,0x9e,0x9e,0xed]

/*: "uid" :*/
fileprivate let appGroupConMessage:String = "uiscientific"

/*: "nickname" :*/
fileprivate let kBoldName:String = "nibuttk"

/*: "headPic" :*/
fileprivate let const_fullName:[Character] = ["h"]
fileprivate let appMultipleKeyData:[Character] = ["e","a","d","P","i","c"]

/*: "onCallResume" :*/
fileprivate let constExerciseUrl:String = "sum"
fileprivate let dataPairData:[Character] = ["n","C","a","l","l","R","e","s","u","m","e"]

/*: "cancelCall" :*/
fileprivate let user_difficultTitle:[Character] = ["c","a"]
fileprivate let noti_fieldNumberroStr:String = "againstel"

/*: "refuseCall" :*/
fileprivate let show_writeId:String = "REFUSE"

/*: "onSendProgramGift" :*/
fileprivate let kNaturalWhyStr:String = "onSeaccess discover row dynamics core"
fileprivate let data_yearId:[Character] = ["r","a","m","G","i","f","t"]

/*: "onGameFs" :*/
fileprivate let const_boyStr:String = "advanced tag inner carefulonGameFs"

/*: "broadStatus" :*/
fileprivate let constMoveId:[Character] = ["b","r","o","a","d"]
fileprivate let userEquivalentTitle:String = "Statuslack zone normally identity"

/*: "onCallChatMsg" :*/
fileprivate let kContextValue:String = "template interaction orange row requireonCallC"
fileprivate let appStretchUrl:[Character] = ["h","a","t","M","s","g"]

/*: "onUserBlock" :*/
fileprivate let noti_panelRashString:[Character] = ["o","n","U","s","e","r","B","l","o","c","k"]

/*: "isRecharge" :*/
fileprivate let userTargetString:[Character] = ["i","s"]
fileprivate let mainHoneyMessage:String = "Rechgenerate receive"

/*: "source" :*/
fileprivate let appWithMsg:String = "sourcspell"

/*: "invitePop" :*/
fileprivate let k_indicatorUrl:[Character] = ["i","n","v","i"]
fileprivate let user_justKey:String = "tePopintegration oval fraction option exclusive"

/*: "videoPop" :*/
fileprivate let showIncreaseStarMessage:String = "vidwindow"

/*: "coin" :*/
fileprivate let constAyePath:[Character] = ["c","o","i","n"]

//: Declare String End

// __DEBUG__
// __CLOSE_PRINT__
//
//  MaterialCurrency.swift
//  AbroadTalking
//
//  Created by young on 2022/9/13.
//

//: import SocketRocket
import SocketRocket
//: import UIKit
import UIKit

// 保活心跳包ping的时间间隔
//: let SOCKET_PING_INTERVAL = 60.0
let user_boundData = 60.0
// 视频通话心跳包时间间隔
//: let SOCKET_CALL_INTERVAL = 20.0
let data_editNormUrl = 20.0
// 直播心跳包时间间隔
//: let SOCKET_LIVE_INTERVAL = 20.0
let app_divideListenerKey = 20.0
// 失败重试次数
//: let SOCKET_ADDR_RETRY_COUNT = 3
let notiWouldStr = 3
// socket连接超时时间
//: let SOCKET_MANAGER_CONNECT_TIMEOUT = 30.0
let const_marginPetFormat = 30.0
// socket连接成功缓存key
//: let SOCKET_ADDR_KEY = "SOCKET_ADDR_KEY"
let const_searcherFormQuantityeractionValue = (kCameraKey.replacingOccurrences(of: "quantity", with: "C") + String(show_romanMsg.suffix(4)) + "ADDR_KEY")

// 错误码
//: let SOCKET_MANAGER_UNCONNECTED_CODE = 5001
let show_easeMsg = 5001
//: let SOCKET_MANAGER_SELF_BUSY_CODE   = 5002
let noti_writeMsg = 5002
//: let SOCKET_WITHOUT_NET_CODE         = 5003
let constLocationAmongStr = 5003
//: let SOCKET_MANAGER_USER_LOGOUT_CLOSED_CODE = 1002
let userRankMenuFormat = 1002

// 接收方收到音视频通话通知
//: public let SOCKET_VIDEOCHAT_REQUESTCALL_NOTIFICATION = NSNotification.Name(rawValue: "SOCKET_VIDEOCHAT_REQUESTCALL_NOTIFICATION")
public let const_strategyValue = NSNotification.Name(rawValue: String(bytes: user_saveMistUrl.map{ageDoingdHandle(about: $0)}, encoding: .utf8)!)
// 游戏飘屏通知
//: public let RECIVE_FLOATSCREEN_NOTIFICATION = NSNotification.Name(rawValue: "RECIVE_FLOATSCREEN_NOTIFICATION")
public let constExtraString = NSNotification.Name(rawValue: String(bytes: kProPath.map{micWanderSecurity(discount: $0)}, encoding: .utf8)!)

// MARK: - protocol

// MARK: - socket失败代理

/// socket收到失败消息
//: protocol TalkingSocketManagerErrorDelegate: NSObjectProtocol {
protocol GlobalSustainable: NSObjectProtocol {
    /// socket收到失败消息
    /// - Parameters:
    ///   - errorNo: 失败编码
    ///   - cmd: 命令
    ///   - msg: 失败信息
    ///  - data: 数据
    //: func socket_didRecieveError(errorNo: Int, cmd: String, msg: String, data: [String: Any])
    func connectTo(errorNo: Int, cmd: String, msg: String, data: [String: Any])
}

// MARK: - 视频速配代理

/// 视频速配代理（男）
//: protocol SocketManagerVideoMatchDelegate: NSObjectProtocol {
protocol MessageIconEmbrace: NSObjectProtocol {
    /// 视频速配成功
    /// - Parameter data: 速配数据
    //: func socket_match_onVideoMatch(data: [String: Any])
    func link(data: [String: Any])

    /// 匹配成功，可以拨打通话
    /// - Parameter data: 速配数据
    //: func socket_match_onRequestCall(data: [String: Any])
    func distanceTo(data: [String: Any])
}

// MARK: - 音视频通话代理

/// 视频通话【拨打方】代理
//: protocol SocketManagerVideoChatCallerDelegate: NSObjectProtocol {
protocol ThemeMenu: NSObjectProtocol {
    /// 接收方接受了来电
    /// - Parameter data: 数据
    //: func socket_videoChat_acceptCall(data: [String: Any])
    func exLayer(data: [String: Any])

    /// 接收方挂断了通话（通话中）
    /// - Parameter data: 数据
    //: func socket_videoChat_stopCall(data: [String: Any])
    func beforeReliability(data: [String: Any])

    /// 拨打方拒绝了通话，接收方收到该指令（未通话）
    /// - Parameter data: 数据
    //: func socket_videoChat_cancelCall(data: [String: Any])
    func verticalAgile(data: [String: Any])

    /// 通话余额不足提醒
    /// - Parameters:
    ///   - msg: 提醒信息
    ///   - data: 数据
    //: func socket_videoChat_onMissCoin(msg: String, data: [String: Any])
    func distinctionIn(msg: String, data: [String: Any])
}

/// 视频通话【接收方】代理
//: @objc protocol SocketManagerVideoChatCalledPartyDelegate: NSObjectProtocol {
@objc protocol MultipleMarkAmong: NSObjectProtocol {
    /// 接收方拒绝了通话，拨打方收到该指令（未通话）
    /// - Parameter data: 数据
    //: func socket_videoChat_refuseCall(data: [String: Any])
    func buttTar(data: [String: Any])

    /// 接收方接受了通话，改变状态为通话中，acceptCall回执
    /// - Parameter data: 数据
    //: func socket_videoChat_onAcceptCall(data: [String: Any])
    func fragmentWithinCurrency(data: [String: Any])
}

//: class TalkingSocketManager: NSObject {
class MaterialCurrency: NSObject {
    //: enum SocketRetryType: String {
    enum DenseMaintain: String {
        //: case AppLogin = "用户登录"
        case AppLogin = "用户登录"
        //: case NetWork = "网络重连"
        case NetWork = "网络重连"
        //: case EnterForeground = "回到APP"
        case EnterForeground = "回到APP"
        //: case SocketFail = "连接失败"
        case SocketFail = "连接失败"
        //: case SocketClose = "连接关闭"
        case SocketClose = "连接关闭"
        //: case SocketSendMsg = "发送消息失败"
        case SocketSendMsg = "发送消息失败"
    }

    /// 音视频最小化
    //: var pacificVC: AnyObject?
    var pacificVC: AnyObject? // 当前正在通话的音视频视图
    //: var videoMiniPosition = CGPoint(
    var videoMiniPosition = CGPoint( // 音视频小窗口默认位置
        //: x: LanguageManager.shared.direction == .rightToLeft ? 0:ScreenWidth,
        x: InputCloseInfo.shared.direction == .rightToLeft ? 0 : show_errorChangeFormat,
        //: y: StatusBarHeight+190
        y: const_musicMessage + 190
    )
    //: var liveMiniPosition = CGPoint(
    var liveMiniPosition = CGPoint( // 直播小窗口默认位置
        //: x: LanguageManager.shared.direction == .rightToLeft ? 0:ScreenWidth,
        x: InputCloseInfo.shared.direction == .rightToLeft ? 0 : show_errorChangeFormat,
        //: y: StatusBarHeight+190
        y: const_musicMessage + 190
    )
    //: var containerView: UIView?
    var containerView: UIView? // 小窗口容器

    /// delegate
    //: weak var errorDelegate: TalkingSocketManagerErrorDelegate?
    weak var errorDelegate: GlobalSustainable? // error !=0 的异常处理
    //: weak var videoMatchDelegate: SocketManagerVideoMatchDelegate?
    weak var videoMatchDelegate: MessageIconEmbrace? // 视频通话速配
    //: weak var callerDelegate: SocketManagerVideoChatCallerDelegate?
    weak var callerDelegate: ThemeMenu? // 视频通话【拨打方】
    //: weak var calledPartyDelegate: SocketManagerVideoChatCalledPartyDelegate?
    weak var calledPartyDelegate: MultipleMarkAmong? // 视频通话【接收方】

    //: private var webSocket: SRWebSocket?
    private var webSocket: SRWebSocket?
    //: private var keepAliveTimer: Timer?
    private var keepAliveTimer: Timer? // socket保活心跳包
    //: private var connectUrlStr: String?
    private var connectUrlStr: String? // socket连接地址
    //: private var linkIndex = 0
    private var linkIndex = 0 // 当前连接地址索引
    //: private var linkAddrArr: [String]?
    private var linkAddrArr: [String]? // 连接地址数组
    //: private var failureCount = 0
    private var failureCount = 0 // 连接失败次数
    //: private let disposeBag = DisposeBag()
    private let disposeBag = DisposeBag()

    /// talking
    //: var needReconnect = false
    var needReconnect = false // 断网重连socket
    
    @objc dynamic var pacific = false // 是否视频通话中
    //: @objc dynamic var isCalling = false
    @objc dynamic var isCalling = false // 是否呼叫中
    //: private var talkTimer: Timer?
    private var talkTimer: Timer? // 视频通话心跳包
    //: private var liveTimer: Timer?
    private var liveTimer: Timer? // 直播心跳包
    //: private var unHandleCallData: [String: Any]?
    private var unHandleCallData: [String: Any]? // 在后台收到的接听消息
    //: private var cancelCall = false
    private var cancelCall = false // 拨打方是否停止呼叫（还未打通）
    //: var isFreeCall = false
    var isFreeCall = false // 是否发起免费通话

    // singleton
    //: static let shared = TalkingSocketManager()
    static let shared = MaterialCurrency()
    //: override func copy() -> Any { return self }
    override func copy() -> Any { return self }
    //: override func mutableCopy() -> Any { return self }
    override func mutableCopy() -> Any { return self }
    //: private override init() {
    override private init() {
        //: super.init()
        super.init()
        // 网络状态监听
        //: NotificationCenter.default.addObserver(self,
        NotificationCenter.default.addObserver(self,
                                               //: selector: #selector(reachabilityChanged(note:)),
                                               selector: #selector(barManageCounteraction(note:)),
                                               //: name: .reachabilityChanged,
                                               name: .reachabilityChanged,
                                               //: object: nil)
                                               object: nil)

        //: NotificationCenter.default.addObserver(self,
        NotificationCenter.default.addObserver(self,
                                               //: selector: #selector(checkWebsocket),
                                               selector: #selector(version),
                                               //: name: UIApplication.willEnterForegroundNotification,
                                               name: UIApplication.willEnterForegroundNotification,
                                               //: object: nil)
                                               object: nil)
    }

    //: deinit {
    deinit {
        //: NotificationCenter.default.removeObserver(self)
        NotificationCenter.default.removeObserver(self)
    }
}

// MARK: - SRWebSocketDelegate

//: extension TalkingSocketManager: SRWebSocketDelegate {
extension MaterialCurrency: SRWebSocketDelegate {
    //: func webSocketDidOpen(_ webSocket: SRWebSocket!) {
    func webSocketDidOpen(_ webSocket: SRWebSocket!) {
        //: guard self.webSocket == webSocket else { return }
        guard self.webSocket == webSocket else { return }
        //: UploadLogTool.writeLog(msg: "SOCKET 连接成功.")
        EvolutionDelayDestroy.afterTotalo(msg: (String(user_eventCarrierData)))
        //: sendPing()
        distinction()
        //: NotificationCenter.default.post(name: SOCKET_IS_OPEN_NOTIFICATION,
        NotificationCenter.default.post(name: userVisitMessage,
                                        //: object: nil,
                                        object: nil,
                                        //: userInfo: nil)
                                        userInfo: nil)
        // 还原断网重连状态
        //: self.needReconnect = false
        self.needReconnect = false

        //: self.failureCount = 0
        self.failureCount = 0

        // 保存成功连接的记录
        //: if self.linkIndex < self.linkAddrArr?.count ?? 0 {
        if self.linkIndex < self.linkAddrArr?.count ?? 0 {
            //: UserDefaults.standard.set(self.linkAddrArr?[self.linkIndex], forKey: SOCKET_ADDR_KEY)
            UserDefaults.standard.set(self.linkAddrArr?[self.linkIndex], forKey: const_searcherFormQuantityeractionValue)
        }
        // 创建音视频最小化窗口容器
        //: mini_createContainerView()
        storageCommand()
    }

    //: func webSocket(_ webSocket: SRWebSocket!, didFailWithError error: Error!) {
    func webSocket(_ webSocket: SRWebSocket!, didFailWithError error: Error!) {
        //: UploadLogTool.writeLog(msg: "SOCKET 连接失败, error:\(error.localizedDescription).")
        EvolutionDelayDestroy.afterTotalo(msg: (String(appPanNameTitle) + showSayPath + String(data_associatedId.prefix(7))) + "\(error.localizedDescription).")
        //: guard self.webSocket == webSocket else { return }
        guard self.webSocket == webSocket else { return }

        //: guard let status = MarginExamineer.share.reachability?.connection, status != .unavailable else {
        guard let status = MarginExamineer.share.reachability?.connection, status != .unavailable else {
            //: return
            return
        }
        //: if self.failureCount == SOCKET_ADDR_RETRY_COUNT {
        if self.failureCount == notiWouldStr {
            //: self.failureCount = 0
            self.failureCount = 0
            //: self.linkIndex = (self.linkIndex == 0) ? 1:0
            self.linkIndex = (self.linkIndex == 0) ? 1 : 0
            //: self.connectUrlStr = getWebSocketPath(isRetry: true)
            self.connectUrlStr = bottomThree(isRetry: true)
            //: UploadLogTool.writeLog(msg: "SOCKET 失败\(SOCKET_ADDR_RETRY_COUNT)次, 切换过Socket域名重新尝试，地址：\(connectUrlStr ?? "").")
            EvolutionDelayDestroy.afterTotalo(msg: (showPleaseNumberLogFormat.replacingOccurrences(of: "clearly", with: "S") + String(k_hungStr)) + "\(notiWouldStr)" + (show_lightKey.replacingOccurrences(of: "output", with: "换") + "过Sock" + String(noti_formMessage.prefix(5)) + String(user_stopKey)) + "\(connectUrlStr ?? "").")
            //: } else {
        } else {
            //: self.failureCount += 1
            self.failureCount += 1
        }
        //: self.reconnectWebSocket(timeInterval: 2, type: .SocketFail)
        self.completeDown(timeInterval: 2, type: .SocketFail)
    }

    //: func webSocket(_ webSocket: SRWebSocket!, didReceiveMessage message: Any!) {
    func webSocket(_ webSocket: SRWebSocket!, didReceiveMessage message: Any!) {
        //: guard self.webSocket == webSocket else { return }
        guard self.webSocket == webSocket else { return }
        //: self.handleReceiveMessage(message: message as? String)
        self.thanCamera(message: message as? String)
    }

    //: func webSocket(_ webSocket: SRWebSocket!, didCloseWithCode code: Int, reason: String!, wasClean: Bool) {
    func webSocket(_ webSocket: SRWebSocket!, didCloseWithCode code: Int, reason: String!, wasClean: Bool) {
        //: UploadLogTool.writeLog(msg: "SOCKET 已关闭, code:\(code), reason:\(String(describing: reason)).")
        EvolutionDelayDestroy.afterTotalo(msg: (notiUnknownTitle.replacingOccurrences(of: "background", with: "C") + String(constShallExpectedData.prefix(4)) + "\u{5df2}关闭," + String(userKitBlueByString.prefix(6))) + "\(code)" + (String(const_secureData.suffix(8)) + constReserveMsg.capitalized) + "\(String(describing: reason)).")
        //: guard self.webSocket == webSocket else { return }
        guard self.webSocket == webSocket else { return }

        //: self.closeWebSocket(userLoginOut: false)
        self.processingPresent(userLoginOut: false)
        //: if code == SOCKET_MANAGER_USER_LOGOUT_CLOSED_CODE {
        if code == userRankMenuFormat {
            //: return
            return
        }
        //: if let status = MarginExamineer.share.reachability?.connection, status != .unavailable {
        if let status = MarginExamineer.share.reachability?.connection, status != .unavailable {
            //: self.reconnectWebSocket(timeInterval: 5, type: .SocketClose)
            self.completeDown(timeInterval: 5, type: .SocketClose)
        }
    }

    /// 获取socket地址
    //: func getWebSocketPath(isRetry: Bool) -> String? {
    func bottomThree(isRetry: Bool) -> String? {
        //: var linkAddr = ""
        var linkAddr = ""
        //: self.linkAddrArr = MarginExamineer.share.appConfigMode.ws
        self.linkAddrArr = MarginExamineer.share.appConfigMode.ws
        //: if self.linkAddrArr == nil || self.linkAddrArr?.count == 0 {
        if self.linkAddrArr == nil || self.linkAddrArr?.count == 0 {
            //: self.linkAddrArr = defaultWSArr()
            self.linkAddrArr = panelEnable()
        }
        //: let lastAddr = UserDefaults.standard.string(forKey: SOCKET_ADDR_KEY)
        let lastAddr = UserDefaults.standard.string(forKey: const_searcherFormQuantityeractionValue)
        //: if lastAddr == nil || isRetry {
        if lastAddr == nil || isRetry {
            //: if let temLinkAddr = self.linkAddrArr?[safe: self.linkIndex] {
            if let temLinkAddr = self.linkAddrArr?[safe: self.linkIndex] {
                //: linkAddr = temLinkAddr
                linkAddr = temLinkAddr
                //: } else {
            } else {
                //: self.linkIndex = 0
                self.linkIndex = 0
                //: linkAddr = self.linkAddrArr!.first!
                linkAddr = self.linkAddrArr!.first!
            }
            //: } else {
        } else {
            //: if let index = self.linkAddrArr!.firstIndex(of: lastAddr!) {
            if let index = self.linkAddrArr!.firstIndex(of: lastAddr!) {
                //: self.linkIndex = index
                self.linkIndex = index
                //: linkAddr = lastAddr!
                linkAddr = lastAddr!
                //: }else{
            } else {
                //: self.linkIndex = 0
                self.linkIndex = 0
                //: linkAddr = self.linkAddrArr!.first!
                linkAddr = self.linkAddrArr!.first!
            }
        }

        //: let uid = MarginExamineer.share.loginUid
        let uid = MarginExamineer.share.loginUid
        //: let sid = MarginExamineer.share.loginSessionId
        let sid = MarginExamineer.share.loginSessionId
        //: let address = "ws://\(linkAddr)/?sid=\(sid)&uid=\(uid)&platform=iphone&version=\(AppNetVersion)&packageId=\(PackageID)"
        let address = "ws://\(linkAddr)" + (const_cancelFileKey.capitalized + String(const_graphicPath.suffix(5))) + "\(sid)" + (String(showExhaustKey.suffix(5))) + "\(uid)" + (String(userFoundAdKey.prefix(5)) + "form=" + data_dueCommunicateValue.replacingOccurrences(of: "body", with: "i") + "ne&ve" + String(appKeyLessWhipKey.prefix(6))) + "\(notiOriginalKey)" + (String(noti_sayVoiceMessage.prefix(4)) + String(user_contactWithId)) + "\(k_perBounceString)"
        //: return address
        return address
    }

    /// 默认地址
    //: func defaultWSArr() -> [String] {
    func panelEnable() -> [String] {
        //: return Environment_Formal ? ["1v1server.\(ReplaceUrlDomain).com:7766"] : ["43.129.185.194:7767"]
        return const_tempAttachTitle ? [(String(app_writtenUrl.suffix(7)) + String(kLocateDenseName.prefix(3))) + "\(app_readTitle)" + (String(app_followRecordPath.prefix(5)) + "7766")] : [(data_regularMsg.replacingOccurrences(of: "personal", with: "4") + "9.185." + kSkipId.capitalized + "7767")]
    }
}

// MARK: - NotificationCenter

//: extension TalkingSocketManager {
extension MaterialCurrency {
    /// 网络监听通知
    //: @objc func reachabilityChanged(note: Notification) {
    @objc func barManageCounteraction(note: Notification) {
        //: let reachability = note.object as? Reachability
        let reachability = note.object as? Reachability
        //: if reachability?.connection == .unavailable {
        if reachability?.connection == .unavailable {
            // 断网, 关闭socket
            //: UploadLogTool.writeLog(msg: "SOCKET 网络reachabilityChanged监听，断网关闭.")
            EvolutionDelayDestroy.afterTotalo(msg: String(bytes: k_transitionUrl.map{persistLayer(assistant: $0)}, encoding: .utf8)!)
            //: self.closeWebSocket(userLoginOut: false)
            self.processingPresent(userLoginOut: false)
            //: } else {
        } else {
            // 网络原因重连
            //: self.reconnectWebSocket(timeInterval: 2.0, type: .NetWork)
            self.completeDown(timeInterval: 2.0, type: .NetWork)
        }
    }

    /// 检测socket链接状态
    //: @objc func checkWebsocket() {
    @objc func version() {
        //: self.reconnectWebSocket(timeInterval: 2.0, type: .EnterForeground)
        self.completeDown(timeInterval: 2.0, type: .EnterForeground)
        // 处理通话逻辑
        //: self.handleCallData()
        self.opene()
    }
}

// MARK: - Event

//: extension TalkingSocketManager {
extension MaterialCurrency {
    /// socket保活心跳包
    //: private func sendPing() {
    private func distinction() {
        //: self.keepAliveTimer = Timer.scheduledTimer(withTimeInterval: SOCKET_PING_INTERVAL, block: { timer in
        self.keepAliveTimer = Timer.scheduledTimer(withTimeInterval: user_boundData, block: { timer in
            //: self.sendMessage(info: ["cmd": "ping"])
            self.warningVersion(info: [(String(userReserveKey)): (showToolPath.replacingOccurrences(of: "fore", with: "n"))])
            //: }, repeats: true)
        }, repeats: true)
    }

    /// 开启视频通话心跳包
    //: private func startTalkPing() {
    private func requireInTrain() {
        //: endTalkingPing()
        blackHandle()

        //: self.talkTimer = Timer.scheduledTimer(withTimeInterval: SOCKET_CALL_INTERVAL, block: {  timer in
        self.talkTimer = Timer.scheduledTimer(withTimeInterval: data_editNormUrl, block: { timer in
            /// 不在通话中，不在音视频通话页，没有开启最小化时
            //: if !self.pacific || (self.currentViewController()?.isKind(of: TalkingVideoChatViewController.self) == false  && self.currentViewController()?.isKind(of: TalkingVoiceChatViewController.self) == false ) && (self.pacificVC?.isKind(of: TalkingVideoChatViewController.self) == false  && self.pacificVC?.isKind(of: TalkingVoiceChatViewController.self) == false ) {
            if !self.pacific || (self.myAcross()?.isKind(of: FillPurple.self) == false && self.myAcross()?.isKind(of: InserterStorage.self) == false) && (self.pacificVC?.isKind(of: FillPurple.self) == false && self.pacificVC?.isKind(of: InserterStorage.self) == false) {
                //: return
                return
            }
            //: self.sendMessage(info: ["cmd": "callHeartbeat"])
            self.warningVersion(info: [(String(userReserveKey)): (String(showHeadFormat) + dataConversionUrl.replacingOccurrences(of: "used", with: "t"))])
            //: }, repeats: true)
        }, repeats: true)
    }

    /// 结束视频通话心跳包
    //: func endTalkingPing() {
    func blackHandle() {
        //: if self.talkTimer != nil {
        if self.talkTimer != nil {
            //: self.talkTimer?.invalidate()
            self.talkTimer?.invalidate()
            //: self.talkTimer = nil
            self.talkTimer = nil
        }
    }

    /// 处理通话逻辑
    //: func handleCallData() {
    func opene() {
        //: if self.cancelCall == true {
        if self.cancelCall == true { // 拨打方已停止呼叫
            //: self.cancelCall = false
            self.cancelCall = false
            //: self.unHandleCallData = nil
            self.unHandleCallData = nil
            //: return
            return
        }

        //: if self.unHandleCallData != nil {
        if self.unHandleCallData != nil {
            //: let recievedTime = self.unHandleCallData!["recievedDate"] as! Date
            let recievedTime = self.unHandleCallData![(String(const_sourceStr) + String(const_ageValue))] as! Date
            //: let currentTime = Date()
            let currentTime = Date()
            //: if currentTime.timeIntervalSince(recievedTime) < VIDEO_1v1_RECIEVED_CALL_TIMEOUT {
            if currentTime.timeIntervalSince(recievedTime) < app_buildEnvironmentValue {
                //: MarginExamineer.share.recieve1v1TalkRequest(info: self.unHandleCallData!)
                MarginExamineer.share.bothBean(info: self.unHandleCallData!)
            }
            //: self.unHandleCallData = nil
            self.unHandleCallData = nil
        }
    }
}

// MARK: - 直播心跳包

//: extension TalkingSocketManager {
extension MaterialCurrency {
    /// 开启直播心跳包
    //: func startLivePing() {
    func timingBy() {
        //: endLivePing()
        policyDensity()
        //: self.liveTimer = Timer.scheduledTimer(withTimeInterval: SOCKET_LIVE_INTERVAL, block: { timer in
        self.liveTimer = Timer.scheduledTimer(withTimeInterval: app_divideListenerKey, block: { timer in
            //: self.sendMessage(info: ["cmd": "liveHeartBeat"])
            self.warningVersion(info: [(String(userReserveKey)): (String(k_rootAcheString) + String(k_conSequenceId))])
            //: }, repeats: true)
        }, repeats: true)
    }

    /// 结束直播心跳包
    //: func endLivePing() {
    func policyDensity() {
        //: if self.liveTimer != nil {
        if self.liveTimer != nil {
            //: self.liveTimer?.invalidate()
            self.liveTimer?.invalidate()
            //: self.liveTimer = nil
            self.liveTimer = nil
        }
    }
}

// MARK: - 音视频窗口最小化

//: extension TalkingSocketManager {
extension MaterialCurrency {
    /// 创建窗口最小化容器
    //: private func mini_createContainerView() {
    private func storageCommand() {
        //: guard self.containerView == nil else { return }
        guard self.containerView == nil else { return }

        //: let windowView = TalkingMiniContainerView()
        let windowView = MediaPause()
        //: let keyWindow = MovementStructure.getWindow()
        let keyWindow = MovementStructure.levelScientific()
        //: windowView.frame = keyWindow.bounds
        windowView.frame = keyWindow.bounds
        //: windowView.backgroundColor = .clear
        windowView.backgroundColor = .clear
        //: keyWindow.addSubview(windowView)
        keyWindow.addSubview(windowView)
        //: self.containerView = windowView
        self.containerView = windowView
        //: self.containerView?.isHidden = !self.pacific
        self.containerView?.isHidden = !self.pacific
    }

    /// 移除窗口最小化容器
    //: private func mini_removeContainerView() {
    private func nearBoardRender() {
        //: guard self.containerView != nil else { return }
        guard self.containerView != nil else { return }
        //: self.containerView?.removeAllSubviews()
        self.containerView?.removeAllSubviews()
        //: self.containerView?.removeFromSuperview()
        self.containerView?.removeFromSuperview()
        //: self.containerView = nil
        self.containerView = nil
    }
}

// MARK: - Socket

//: extension TalkingSocketManager {
extension MaterialCurrency {
    /// 关闭socket
    /// - Parameter userLoginOut: 是否是用户退出
    //: func closeWebSocket(userLoginOut: Bool) {
    func processingPresent(userLoginOut: Bool) {
        //: printLog(message: "🔗 关闭 socket")
        printLog(message: (String(userReloadlyData)))
        //: if self.webSocket != nil {
        if self.webSocket != nil {
            //: self.webSocket?.delegate = nil
            self.webSocket?.delegate = nil
            //: if userLoginOut == true {
            if userLoginOut == true {
                //: self.webSocket?.close(withCode: SOCKET_MANAGER_USER_LOGOUT_CLOSED_CODE, reason: "The user logs out")
                self.webSocket?.close(withCode: userRankMenuFormat, reason: (String(kMomentMessage.suffix(5)) + "ser " + String(mainBeanAllowUrl.prefix(8))))
                //: } else {
            } else {
                //: self.webSocket?.close()
                self.webSocket?.close()
            }
            //: self.webSocket = nil
            self.webSocket = nil
        }

        //: if self.keepAliveTimer != nil {
        if self.keepAliveTimer != nil {
            //: self.keepAliveTimer?.invalidate()
            self.keepAliveTimer?.invalidate()
            //: self.keepAliveTimer = nil
            self.keepAliveTimer = nil
        }

        //: endTalkingPing()
        blackHandle()

        //: if userLoginOut == true {
        if userLoginOut == true {
            //: self.connectUrlStr = nil
            self.connectUrlStr = nil
            // 销毁最小化窗口
            //: mini_removeContainerView()
            nearBoardRender()
        }
    }

    /// 更新socket连接
    //: func updateWebSocket() {
    func crush() {
        //: self.connectUrlStr = TalkingSocketManager.shared.getWebSocketPath(isRetry: false)
        self.connectUrlStr = MaterialCurrency.shared.bottomThree(isRetry: false)
        //: self.reconnectWebSocket(timeInterval: 0, type: .AppLogin)
        self.completeDown(timeInterval: 0, type: .AppLogin)
    }

    /// 重连socket
    //: private func reconnectWebSocket(timeInterval: TimeInterval, type: SocketRetryType) {
    private func completeDown(timeInterval: TimeInterval, type: DenseMaintain) {
        //: DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + timeInterval) {
        DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + timeInterval) {
            //: UploadLogTool.writeLog(msg: "SOCKET \(type.rawValue)重连. 重试次数：\(self.failureCount), 地址：\(self.connectUrlStr ?? "").")
            EvolutionDelayDestroy.afterTotalo(msg: (String(main_natureGiftData)) + "\(type.rawValue)" + (String(dataShrimpStr) + const_thinPath) + "\(self.failureCount), 地址：\(self.connectUrlStr ?? "").")
            //: self.connectWebSocket(urlStr: self.connectUrlStr)
            self.gate(urlStr: self.connectUrlStr)
        }
    }

    /// 连接socket
    //: private func connectWebSocket(urlStr: String?) {
    private func gate(urlStr: String?) {
        //: guard urlStr?.isEmpty == false else { return }
        guard urlStr?.isEmpty == false else { return }

        //: if urlStr == self.connectUrlStr && self.webSocket != nil && (self.webSocket?.readyState == .OPEN || self.webSocket?.readyState == .CONNECTING) {
        if urlStr == self.connectUrlStr, self.webSocket != nil, self.webSocket?.readyState == .OPEN || self.webSocket?.readyState == .CONNECTING {
            //: if (self.webSocket?.readyState == .OPEN) {
            if self.webSocket?.readyState == .OPEN {
                //: UploadLogTool.writeLog(msg: "SOCKET 连接正常 不需要重新连接.")
                EvolutionDelayDestroy.afterTotalo(msg: (String(app_constructMessage) + String(showHereId)))
                //: } else {
            } else {
                //: UploadLogTool.writeLog(msg: "SOCKET 正在连接 不需要重新连接.")
                EvolutionDelayDestroy.afterTotalo(msg: (String(kRandomKey) + String(data_actualNetKey)))
            }
            //: return
            return
        }

        //: self.connectUrlStr = urlStr
        self.connectUrlStr = urlStr

        //: var newStr = urlStr!
        var newStr = urlStr!
        //: if self.pacific {
        if self.pacific {
            //: newStr = newStr.appending("&resume=1")
            newStr = newStr.appending((String(noti_tarKey) + String(mainVeryMsg.prefix(4))))
        }
        //: newStr = newStr.appending("&lang=\(TalkingRequestAddrTool.share.interfaceLang)")
        newStr = newStr.appending((String(user_fullPath.prefix(6))) + "\(PresentLocal.share.interfaceLang)")
        //: closeWebSocket(userLoginOut: false)
        processingPresent(userLoginOut: false)

        //: let url = URL(string: newStr)
        let url = URL(string: newStr)
        //: var request = URLRequest(url: url!)
        var request = URLRequest(url: url!)
        //: request.timeoutInterval = SOCKET_MANAGER_CONNECT_TIMEOUT
        request.timeoutInterval = const_marginPetFormat
        //: self.webSocket = SRWebSocket(urlRequest: request)
        self.webSocket = SRWebSocket(urlRequest: request)
        //: self.webSocket?.delegate = self
        self.webSocket?.delegate = self
        //: self.webSocket?.open()
        self.webSocket?.open()
        //: UploadLogTool.writeLog(msg: "SOCKET 开始连接, 地址：\(newStr).")
        EvolutionDelayDestroy.afterTotalo(msg: (constHungString.replacingOccurrences(of: "head", with: "C") + "ET 开始" + String(appCleanMsg) + show_successfulString.capitalized) + "\(newStr).")
    }

    /// 发送消息
    /// - Parameter info: 消息体
    /// - Returns: 发送结果
    //: @discardableResult
    @discardableResult
    //: func sendMessage(info: [String: Any]?) -> Bool {
    func warningVersion(info: [String: Any]?) -> Bool {
        //: guard info != nil else { return false }
        guard info != nil else { return false }

        // 1. 判断是否有网络连接
        //: if MarginExamineer.share.reachability?.connection == nil || MarginExamineer.share.reachability?.connection == .unavailable {
        if MarginExamineer.share.reachability?.connection == nil || MarginExamineer.share.reachability?.connection == .unavailable {
            //: let info: [String: Any] = ["cmd": "onNetDisabled",
            let info: [String: Any] = [(String(userReserveKey)): (String(user_cookieString.prefix(5)) + "Disabled"),
                                       //: "errno": "\(SOCKET_WITHOUT_NET_CODE)",
                                       (notiQualityId.replacingOccurrences(of: "clean", with: "o")): "\(constLocationAmongStr)",
                                       //: "data": [:]]
                                       (kRecentId.replacingOccurrences(of: "farmer", with: "a")): [:]]
            //: let data = try? JSONSerialization.data(withJSONObject: info, options: [])
            let data = try? JSONSerialization.data(withJSONObject: info, options: [])
            //: let message = String(data: data!, encoding: String.Encoding.utf8)
            let message = String(data: data!, encoding: String.Encoding.utf8)
            //: handleReceiveMessage(message: message)
            thanCamera(message: message)
            //: return false
            return false
        }

        // 2. 尝试发送消息
        //: if self.webSocket?.readyState == SRReadyState.OPEN {
        if self.webSocket?.readyState == SRReadyState.OPEN {
            //: let data = try? JSONSerialization.data(withJSONObject: info!, options: [])
            let data = try? JSONSerialization.data(withJSONObject: info!, options: [])
            //: let message = String(data: data!, encoding: String.Encoding.utf8)
            let message = String(data: data!, encoding: String.Encoding.utf8)
            //: if info!["cmd"] as? String == "ping" {
            if info![(String(userReserveKey))] as? String == (showToolPath.replacingOccurrences(of: "fore", with: "n")) {
                //: UploadLogTool.writeLog(msg: "SOCKET 连接保活cmd: ping.")
                EvolutionDelayDestroy.afterTotalo(msg: (userFutureData.replacingOccurrences(of: "temp", with: "SO") + "ET 连" + String(show_distributionFormat.prefix(6)) + ": ping."))
                //: } else if info!["cmd"] as? String == "callHeartbeat" {
            } else if info![(String(userReserveKey))] as? String == (String(showHeadFormat) + dataConversionUrl.replacingOccurrences(of: "used", with: "t")) {
                //: UploadLogTool.writeLog(msg: "SOCKET trtc保活cmd: callHeartbeat.")
                EvolutionDelayDestroy.afterTotalo(msg: String(bytes: showGameString.map{couldCountro(schedule: $0)}, encoding: .utf8)!)
                //: } else if info!["cmd"] as? String == "liveHeartBeat" {
            } else if info![(String(userReserveKey))] as? String == (String(k_rootAcheString) + String(k_conSequenceId)) {
                //: UploadLogTool.writeLog(msg: "SOCKET live保活cmd: liveHeartBeat.")
                EvolutionDelayDestroy.afterTotalo(msg: String(bytes: data_withinName.map{$0^128}, encoding: .utf8)!)
            }
            //: self.webSocket?.send(message)
            self.webSocket?.send(message)
            //: return true
            return true

            //: } else {
        } else {
            //: if info!["cmd"] as? String == "ping" {
            if info![(String(userReserveKey))] as? String == (showToolPath.replacingOccurrences(of: "fore", with: "n")) {
                //: let info: [String: Any] = ["cmd": "onP",
                let info: [String: Any] = [(String(userReserveKey)): (String(k_automobileString)),
                                           //: "errno": "\(SOCKET_MANAGER_UNCONNECTED_CODE)",
                                           (notiQualityId.replacingOccurrences(of: "clean", with: "o")): "\(show_easeMsg)",
                                           //: "data": [:]]
                                           (kRecentId.replacingOccurrences(of: "farmer", with: "a")): [:]]
                //: let data = try? JSONSerialization.data(withJSONObject: info, options: [])
                let data = try? JSONSerialization.data(withJSONObject: info, options: [])
                //: let message = String(data: data!, encoding: String.Encoding.utf8)
                let message = String(data: data!, encoding: String.Encoding.utf8)
                //: handleReceiveMessage(message: message)
                thanCamera(message: message)
            }
            //: self.reconnectWebSocket(timeInterval: 5.0, type: .SocketSendMsg)
            self.completeDown(timeInterval: 5.0, type: .SocketSendMsg)
            //: return false
            return false
        }
    }

    /// 处理接收到的消息
    /// - Parameter message: 消息体
    //: private func handleReceiveMessage(message: String?) {
    private func thanCamera(message: String?) {
        //: guard message != nil else { return }
        guard message != nil else { return }

        //: let json = JSON(parseJSON: message!)
        let json = JSON(parseJSON: message!)
        //: let cmdName = json["cmd"].stringValue
        let cmdName = json[(String(userReserveKey))].stringValue
        //: let errorNo = json["errno"].intValue
        let errorNo = json[(notiQualityId.replacingOccurrences(of: "clean", with: "o"))].intValue
        //: let msg = json["msg"].stringValue
        let msg = json[(String(kReliabilityDrawerData))].stringValue
        //: let data = json["data"].dictionaryObject ?? [:]
        let data = json[(kRecentId.replacingOccurrences(of: "farmer", with: "a"))].dictionaryObject ?? [:]

        //: printLog(message: "🔗 处理消息：cmdName:\(cmdName), errNo:\(errorNo), msg:\(msg), data:\(data)")
        printLog(message: (dataFollowStr + "息：cm" + String(kTargetName.prefix(5)) + notiBarData.replacingOccurrences(of: "hero", with: ":")) + "\(cmdName)" + (String(mainDirectInsertName.prefix(5)) + String(dataPartySegmentString)) + "\(errorNo)" + (String(show_evaluatePath.suffix(6))) + "\(msg)" + (String(app_endStr.prefix(7))) + "\(data)")

        // 处理异常
        //: guard errorNo == 0 else {
        guard errorNo == 0 else {
            //: UploadLogTool.writeLog(msg: "SOCKET 接收异常消息：cmdName:\(cmdName), errNo:\(errorNo), msg:\(msg), data:\(data).")
            EvolutionDelayDestroy.afterTotalo(msg: (String(data_distributionKey) + String(appCurrentlyName)) + "\(cmdName)" + (String(mainDirectInsertName.prefix(5)) + String(dataPartySegmentString)) + "\(errorNo)" + (String(show_evaluatePath.suffix(6))) + "\(msg)" + (String(app_endStr.prefix(7))) + "\(data).")
            //: if errorNo == 444 {
            if errorNo == 444 {
                //: MarginExamineer.share.loginUserMode.freeCallTimes = 0
                MarginExamineer.share.loginUserMode.freeCallTimes = 0
            }
            //: self.errorDelegate?.socket_didRecieveError(errorNo: errorNo, cmd: cmdName, msg: msg, data: data)
            self.errorDelegate?.connectTo(errorNo: errorNo, cmd: cmdName, msg: msg, data: data)
            //: return
            return
        }

        // 处理正常接收的消息
        //: if cmdName == "onReceivedGift" { // 礼物
        if cmdName == (String(const_bulletKey.suffix(6)) + "ived" + String(mainSlyMsg.prefix(4))) { // 礼物
            //: NotificationCenter.default.post(name: USER_RECIEVED_GIFT_NOTIFICATION, object: data)
            NotificationCenter.default.post(name: noti_missionFormat, object: data)

            //: } else if cmdName == "onVideoMatch" { // 视频速配
        } else if cmdName == (String(const_disclosePrimaryMsg.prefix(5)) + "eoMatch") { // 视频速配
            //: self.videoMatchDelegate?.socket_match_onVideoMatch(data: data)
            self.videoMatchDelegate?.link(data: data)

            //: } else if cmdName == "onRequestCall" { // 拨打方发出requestCall回执
        } else if cmdName == (String(showGraphicMsg.suffix(7)) + String(k_pressureStr.suffix(6))) { // 拨打方发出requestCall回执
            //: self.isCalling = true
            self.isCalling = true
            // 视频速配 || 音视频通话
            //: self.videoMatchDelegate?.socket_match_onRequestCall(data: data)
            self.videoMatchDelegate?.distanceTo(data: data)

            //: } else if cmdName == "onCancelCall" { // 拨打方发出cancelCall回执，目前无需处理
        } else if cmdName == (String(kPanStr) + noti_sinceValue.replacingOccurrences(of: "capacity", with: "al")) { // 拨打方发出cancelCall回执，目前无需处理
            //: } else if cmdName == "onRefuseCall" { // 接收方发送refuseCall回执，目前无需处理
        } else if cmdName == (String(k_romanUrl) + String(app_everyRocketString)) { // 接收方发送refuseCall回执，目前无需处理
            //: } else if cmdName == "onAcceptCall" { // 接收方发送acceptCall回执
        } else if cmdName == (String(appDefineMessage) + String(main_thirdFormat.suffix(4)) + "ptCall") { // 接收方发送acceptCall回执
            //: self.pacific = true
            self.pacific = true
            //: self.isCalling = false
            self.isCalling = false
            //: startTalkPing()
            requireInTrain()
            //: self.calledPartyDelegate?.socket_videoChat_onAcceptCall(data: data)
            self.calledPartyDelegate?.fragmentWithinCurrency(data: data)

            //: } else if cmdName == "onStopCall" { // 接收方发送stopCall回执
        } else if cmdName == (String(data_proPath) + dataOperateCancelStr.replacingOccurrences(of: "birthday", with: "l")) { // 接收方发送stopCall回执
            //: self.pacific = false
            self.pacific = false
            //: self.isCalling = false
            self.isCalling = false
            //: endTalkingPing()
            blackHandle()
            //: if let freeCallTimes = data["freeCallTimes"] as? Int {
            if let freeCallTimes = data[(main_shadowUrl.lowercased() + "CallTimes")] as? Int {
                //: MarginExamineer.share.loginUserMode.freeCallTimes = freeCallTimes
                MarginExamineer.share.loginUserMode.freeCallTimes = freeCallTimes
            }
            // 更新余额
            //: MarginExamineer.share.loginUserMode.mf_coin = json["data"]["mfCoin"].stringValue
            MarginExamineer.share.loginUserMode.mf_coin = json[(kRecentId.replacingOccurrences(of: "farmer", with: "a"))][(String(main_itStreamPath.suffix(6)))].stringValue

            //: inviteCallPopViewEndShow(data: data)
            oddCircleBullet(data: data)
            //: NotificationCenter.default.post(name: VIDEOCALL_END_CLOSEDISCOUNTS_NOTIFICATION, object: nil)
            NotificationCenter.default.post(name: const_uniqueString, object: nil)
            //: self.isFreeCall = false
            self.isFreeCall = false
            //: } else if cmdName == "onMissCoin" { // 余额不足提醒
        } else if cmdName == (user_limitKey.replacingOccurrences(of: "unless", with: "o") + String(data_placeStr.prefix(9))) { // 余额不足提醒
            //: self.callerDelegate?.socket_videoChat_onMissCoin(msg: msg, data: data)
            self.callerDelegate?.distinctionIn(msg: msg, data: data)

            //: } else if cmdName == "acceptCall" { // 接收方接受了来电，改变状态为通话中,开始推拉流
        } else if cmdName == (data_pureTarMessage.replacingOccurrences(of: "independent", with: "p") + "tCal" + kLargeId.replacingOccurrences(of: "basic", with: "l")) { // 接收方接受了来电，改变状态为通话中,开始推拉流
            //: self.pacific = true
            self.pacific = true
            //: self.isCalling = false
            self.isCalling = false
            //: startTalkPing()
            requireInTrain()
            //: self.callerDelegate?.socket_videoChat_acceptCall(data: data)
            self.callerDelegate?.exLayer(data: data)

            //: } else if cmdName == "stopCall" { // 接收方/拨打方 挂断了通话
        } else if cmdName == (String(show_implementFormat.prefix(7)) + kAdviceId.replacingOccurrences(of: "rating", with: "l")) { // 接收方/拨打方 挂断了通话
            //: self.pacific = false
            self.pacific = false
            //: self.isCalling = false
            self.isCalling = false
            //: endTalkingPing()
            blackHandle()
            //: self.callerDelegate?.socket_videoChat_stopCall(data: data)
            self.callerDelegate?.beforeReliability(data: data)
            // 更新余额
            //: MarginExamineer.share.loginUserMode.mf_coin = json["data"]["mfCoin"].stringValue
            MarginExamineer.share.loginUserMode.mf_coin = json[(kRecentId.replacingOccurrences(of: "farmer", with: "a"))][(String(main_itStreamPath.suffix(6)))].stringValue
            //: if let freeCallTimes = data["freeCallTimes"] as? Int {
            if let freeCallTimes = data[(main_shadowUrl.lowercased() + "CallTimes")] as? Int {
                //: MarginExamineer.share.loginUserMode.freeCallTimes = freeCallTimes
                MarginExamineer.share.loginUserMode.freeCallTimes = freeCallTimes
            }
            //: inviteCallPopViewEndShow(data: data)
            oddCircleBullet(data: data)
            //: NotificationCenter.default.post(name: VIDEOCALL_END_CLOSEDISCOUNTS_NOTIFICATION, object: nil)
            NotificationCenter.default.post(name: const_uniqueString, object: nil)
            //: self.isFreeCall = false
            self.isFreeCall = false

            //: } else if cmdName == "requestCall" { // 接收方收到了拨打方的申请通话
        } else if cmdName == (String(main_rashMsg)) { // 接收方收到了拨打方的申请通话
            //: self.isCalling = true
            self.isCalling = true
            // 发送通知，告知外界处理逻辑
            //: NotificationCenter.default.post(name: SOCKET_VIDEOCHAT_REQUESTCALL_NOTIFICATION, object: self)
            NotificationCenter.default.post(name: const_strategyValue, object: self)

            //: if UIApplication.shared.applicationState != .background {
            if UIApplication.shared.applicationState != .background { // 应用在前台
                //: MarginExamineer.share.recieve1v1TalkRequest(info: data)
                MarginExamineer.share.bothBean(info: data)
                //: } else {
            } else {
                //: self.cancelCall = false
                self.cancelCall = false
                //: self.unHandleCallData = data
                self.unHandleCallData = data
                //: self.unHandleCallData?["recievedDate"] = Date()
                self.unHandleCallData?[(String(const_sourceStr) + String(const_ageValue))] = Date()

                // 应用在后台，发送本地推送告知用户
                //: var body = "[Invite you to a video call...]".localized
                var body = String(bytes: data_destroyStreakKey.map{injuryLimited(net: $0)}, encoding: .utf8)!.localized
                //: if json["data"]["type"].intValue == 1 { // 音频
                if json[(kRecentId.replacingOccurrences(of: "farmer", with: "a"))][(String(mainDesignPureTitle))].intValue == 1 { // 音频
                    //: body = "[Invite you to a voice call...]".localized
                    body = String(bytes: user_enoughString.map{$0^176}, encoding: .utf8)!.localized
                }
                //: AppDelegateHelper.pushLocalNotification(uid: String(json["data"]["uid"].intValue),
                CoordinatorPlatform.countQuantityo(uid: String(json[(kRecentId.replacingOccurrences(of: "farmer", with: "a"))][(appGroupConMessage.replacingOccurrences(of: "scientific", with: "d"))].intValue),
                                                 //: title: data["nickname"] as? String,
                                                 title: data[(kBoldName.replacingOccurrences(of: "butt", with: "c") + "name")] as? String,
                                                 //: body: body,
                                                 body: body,
                                                 //: imageUrl: data["headPic"] as? String)
                                                 imageUrl: data[(String(const_fullName) + String(appMultipleKeyData))] as? String)
                // 播放来电音乐
                //: AppDelegateHelper.shared.startPlayBGM()
                CoordinatorPlatform.shared.fastBy()
            }

            //: } else if cmdName == "onCallResume" { // 接收方/发送方 恢复通话中的页面，并开始推拉流
        } else if cmdName == (constExerciseUrl.replacingOccurrences(of: "sum", with: "o") + String(dataPairData)) { // 接收方/发送方 恢复通话中的页面，并开始推拉流
            //: self.pacific = true
            self.pacific = true
            //: self.isCalling = false
            self.isCalling = false
            //: startTalkPing()
            requireInTrain()

            //: var resumeData = data
            var resumeData = data
            //: resumeData["onCallResume"] = "1"
            resumeData[(constExerciseUrl.replacingOccurrences(of: "sum", with: "o") + String(dataPairData))] = "1"
            //: MarginExamineer.share.recieve1v1TalkResumeRequest(info: resumeData)
            MarginExamineer.share.anyMight(info: resumeData)

            //: } else if cmdName == "cancelCall" { // 拨打方拒绝了通话，接收方收到该指令
        } else if cmdName == (String(user_difficultTitle) + noti_fieldNumberroStr.replacingOccurrences(of: "against", with: "nc") + "Call") { // 拨打方拒绝了通话，接收方收到该指令
            //: self.pacific = false
            self.pacific = false
            //: self.isCalling = false
            self.isCalling = false
            //: self.cancelCall = true
            self.cancelCall = true
            //: self.isFreeCall = false
            self.isFreeCall = false
            //: endTalkingPing()
            blackHandle()
            //: self.callerDelegate?.socket_videoChat_cancelCall(data: data)
            self.callerDelegate?.verticalAgile(data: data)
            // 应用在后台
            //: if UIApplication.shared.applicationState == .background {
            if UIApplication.shared.applicationState == .background {
                // 移除来电本地推送
                //: AppDelegateHelper.local_removeDeliveredNotification(identifier: String(json["data"]["uid"].intValue))
                CoordinatorPlatform.tillBoard(identifier: String(json[(kRecentId.replacingOccurrences(of: "farmer", with: "a"))][(appGroupConMessage.replacingOccurrences(of: "scientific", with: "d"))].intValue))
                // 停止播放来电音乐
                //: AppDelegateHelper.shared.stopPlayBGM()
                CoordinatorPlatform.shared.gnatColorSecond()
            }

            //: } else if cmdName == "refuseCall" { // 接收方拒绝了通话，拨打方收到该指令
        } else if cmdName == (show_writeId.lowercased() + "Call") { // 接收方拒绝了通话，拨打方收到该指令
            //: self.pacific = false
            self.pacific = false
            //: self.isCalling = false
            self.isCalling = false
            //: self.isFreeCall = false
            self.isFreeCall = false
            //: endTalkingPing()
            blackHandle()
            //: self.calledPartyDelegate?.socket_videoChat_refuseCall(data: data)
            self.calledPartyDelegate?.buttTar(data: data)

            //: } else if cmdName == "onSendProgramGift" { // 视频通话节目送礼成功tost
        } else if cmdName == (String(kNaturalWhyStr.prefix(4)) + "ndProg" + String(data_yearId)) { // 视频通话节目送礼成功tost
            //: self.func__showStatusBarSuccessMsg(showMsg: msg)
            self.nearEasy(showMsg: msg)

            //: } else if cmdName == "onGameFs" { // 游戏中奖飘屏
        } else if cmdName == (String(const_boyStr.suffix(8))) { // 游戏中奖飘屏
            //: let broadStatus = data["broadStatus"] as? Int ?? -1
            let broadStatus = data[(String(constMoveId) + String(userEquivalentTitle.prefix(6)))] as? Int ?? -1
            //: if MarginExamineer.share.appStatus == AppSkinStatus.normal.rawValue {
            if MarginExamineer.share.appStatus == AwayPush.normal.rawValue {
                //: if broadStatus == 1 {
                if broadStatus == 1 {
                    //: NotificationCenter.default.post(name: RECIVE_FLOATSCREEN_NOTIFICATION, object: self, userInfo: data)
                    NotificationCenter.default.post(name: constExtraString, object: self, userInfo: data)
                    //: NotificationCenter.default.post(name: USER_RECIEVED_GIFT_NOTIFICATION, object: data)
                    NotificationCenter.default.post(name: noti_missionFormat, object: data)
                    //: } else if broadStatus == -1 {
                } else if broadStatus == -1 {
                    //: NotificationCenter.default.post(name: RECIVE_FLOATSCREEN_NOTIFICATION, object: self, userInfo: data)
                    NotificationCenter.default.post(name: constExtraString, object: self, userInfo: data)
                }
            }

            //: } else if cmdName == "onCallChatMsg" { // 收到视频通话中的聊天消息
        } else if cmdName == (String(kContextValue.suffix(7)) + String(appStretchUrl)) { // 收到视频通话中的聊天消息
            //: TalkingVideoDanmuManager.shared().onRecvDanmuNewMsg(msg: data)
            GraphKeep.nowHidden().darkWith(msg: data)

            //: } else if cmdName == "onUserBlock" { // 用户被封号
        } else if cmdName == (String(noti_panelRashString)) { // 用户被封号
            //: NotificationCenter.default.post(name: DID_LOGIN_OUT_SUCCESS_NOTIFICATION, object: nil, userInfo: nil)
            NotificationCenter.default.post(name: mainOperationPadString, object: nil, userInfo: nil)
        }
    }
}

// MARK: - 处理业务逻辑

//: extension TalkingSocketManager {
extension MaterialCurrency {
    /// 邀请通话通话结束之后，判断弹窗类型
    //: private func inviteCallPopViewEndShow(data: [String: Any]) {
    private func oddCircleBullet(data: [String: Any]) {
        // 展示评分弹窗
        //: AppStoreRatingManager.shared.requestQuickRating()
        SystemDensity.shared.stepChallenge()
        //: guard MarginExamineer.share.loginUserMode.sex == Gender.male.rawValue else {
        guard MarginExamineer.share.loginUserMode.sex == EnvironmentEmpty.male.rawValue else {
            //: return
            return
        }
        // 弹出优惠充值弹窗
        //: if (data["isRecharge"]) as? Bool == false {
        if data[(String(userTargetString) + String(mainHoneyMessage.prefix(4)) + "arge")] as? Bool == false {
            //: OriginMediumWill.share.func_pushToDiscountRechargeView()
            OriginMediumWill.share.belong()
        }
        // 半屏充值弹窗场景
        //: let source = data["source"] as? String
        let source = data[(appWithMsg.replacingOccurrences(of: "spell", with: "e"))] as? String
        //: guard source == "invitePop" || source == "videoPop" else {
        guard source == (String(k_indicatorUrl) + String(user_justKey.prefix(5))) || source == (showIncreaseStarMessage.replacingOccurrences(of: "window", with: "e") + "oPop") else {
            //: return
            return
        }
        //: if (data["coin"] as? Int) ?? 0 < 60 {
        if (data[(String(constAyePath))] as? Int) ?? 0 < 60 {
            //: OriginMediumWill.share.func__pushToHalfWebVC(webViewType: .RechargeHalfPage)
            OriginMediumWill.share.untilAmongForce(webViewType: .RechargeHalfPage)
        }
    }
}
