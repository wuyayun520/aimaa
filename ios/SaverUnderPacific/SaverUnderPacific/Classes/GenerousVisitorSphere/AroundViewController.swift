
//: Declare String Begin

/*: "bg_message_top" :*/
fileprivate let user_todayName:String = "bg_mforward show history pack salt"
fileprivate let user_parentFormat:String = "depth"
fileprivate let constYaMageMessage:String = "capacity why mic index decisione_top"

/*: "777777" :*/
fileprivate let mainNaturalKey:String = "777777"

/*: "Messages" :*/
fileprivate let notiClearlyValue:String = "Messagelight table honey"
fileprivate let data_postMessage:String = "S"

/*: "Who like me" :*/
fileprivate let notiHeartMsg:[Character] = ["W","h","o"," ","l","i","k","e"," "]
fileprivate let showWanderId:[Character] = ["m","e"]

/*: "Call" :*/
fileprivate let main_padPath:String = "fill titiCall"

/*: "#FF2348" :*/
fileprivate let data_premiumKey:[Character] = ["#","F","F","2","3"]
fileprivate let main_relatedKey:String = "48"

/*: "Current network unavailable" :*/
fileprivate let k_goldMemberString:[Character] = ["C","u","r","r","e","n","t"," ","n","e","t","w","o","r","k"," ","u","n","a","v","a"]
fileprivate let notiKingData:[Character] = ["i"]
fileprivate let app_despiteMessage:String = "unknownabunknowne"

/*: "icon_yidu_pre" :*/
fileprivate let show_soundTitle:String = "toward natureicon_yidu"
fileprivate let show_graphId:String = "_pregift sound assert"

/*: "transform.rotation" :*/
fileprivate let app_plateUrl:[Character] = ["t","r","a","n","s","f","o","r","m",".","r","o","t","a","t"]
fileprivate let main_belongRangeName:String = "ION"

/*: "transform.scale" :*/
fileprivate let kPoVoicePath:[Character] = ["t","r","a","n","s","f","o","r","m",".","s"]
fileprivate let const_privacyTitle:[Character] = ["c","a","l","e"]

/*: "zoom&shake" :*/
fileprivate let notiVersusRejectStr:[Character] = ["z","o","o","m","&","s"]
fileprivate let data_commitMessage:String = "hgnate"

/*: "yyyy-MM-dd" :*/
fileprivate let noti_greenKey:[Character] = ["y","y","y","y","-","M"]
fileprivate let mainBeAlwaysString:[Character] = ["M","-","d","d"]

/*: "Allow %@ to send you notifications?" :*/
fileprivate let main_feePath:[UInt8] = [0xed,0xc0,0xc0,0xc3,0xdb,0x8c,0x89,0xec,0x8c,0xd8,0xc3,0x8c,0xdf,0xc9,0xc2,0xc8,0x8c,0xd5,0xc3,0xd9,0x8c,0xc2,0xc3,0xd8,0xc5,0xca,0xc5,0xcf,0xcd,0xd8,0xc5,0xc3,0xc2,0xdf,0x93]

private func tunnelIndex(reflect num: UInt8) -> UInt8 {
    return num ^ 172
}

/*: "Cancel" :*/
fileprivate let appSparkUrl:String = "Cancelinterval due business square deploy"

/*: "Settings" :*/
fileprivate let show_labMsg:[Character] = ["S","e","t","t","i","n","g"]
fileprivate let mainBodyKey:String = "detail"

/*: "badNumber" :*/
fileprivate let noti_gestureBoxId:String = "coalitiona"
fileprivate let userCapacityTitle:String = "dNumberobserver screening similar"

/*: "isConnection" :*/
fileprivate let k_independentName:String = "foundations"
fileprivate let notiEvenName:String = "Connecaccess choice continuing"

/*: "networkStatus" :*/
fileprivate let dataTermsDefineUrl:String = "span rownetworkSt"
fileprivate let main_divideMusicMsg:String = "atuextra"

/*: "unreadMessageNum" :*/
fileprivate let k_indexMsg:String = "uspecialrea"
fileprivate let dataCosyName:String = "nervous beat mage ifdMess"

/*: "Do you want to mark all messages as read?" :*/
fileprivate let noti_retKey:[UInt8] = [0x11,0x3a,0x75,0x2c,0x3a,0x20,0x75,0x22,0x34,0x3b,0x21,0x75,0x21,0x3a,0x75,0x38,0x34,0x27,0x3e,0x75,0x34,0x39,0x39,0x75,0x38,0x30,0x26,0x26,0x34,0x32,0x30,0x26,0x75,0x34,0x26,0x75,0x27,0x30,0x34,0x31,0x6a]

private func pointWork(tell num: UInt8) -> UInt8 {
    return num ^ 85
}

/*: "消息列表一键已读失败：code: :*/
fileprivate let k_wenMessage:[Character] = ["消","息","列","表","一","\u{952e}","已","读"]
fileprivate let main_popularId:String = "失败"
fileprivate let mainRankBrokenKey:[Character] = ["：","c","o","d","e",":"]

/*: , desc: :*/
fileprivate let kGravityName:[Character] = [","," ","d","e","s"]
fileprivate let const_birthdayBlankFunMessage:[Character] = ["c",":"]

//: Declare String End

// __DEBUG__
// __CLOSE_PRINT__
//
//  AroundViewController.swift
//  AbroadTalking
//
//  Created by Hemming on 2022/1/20.
//

//: import UIKit
import UIKit

//: class TalkingMessageViewController: TalkingBaseViewController {
class AroundViewController: EasyChainSensor {
    //: var isNoChangeTop = false
    var isNoChangeTop = false
    //: private let disposeBag = DisposeBag()
    private let disposeBag = DisposeBag()
    //: private var seleteIndex = 0
    private var seleteIndex = 0

    //: override func viewDidAppear(_ animated: Bool) {
    override func viewDidAppear(_ animated: Bool) {
        //: super.viewDidAppear(animated)
        super.viewDidAppear(animated)
        //: setIsShowCleanAmimat()
        belowFinish()
    }

    //: override func viewWillAppear(_ animated: Bool) {
    override func viewWillAppear(_ animated: Bool) {
        //: super.viewWillAppear(animated)
        super.viewWillAppear(animated)
    }

    //: override func viewDidLoad() {
    override func viewDidLoad() {
        //: super.viewDidLoad()
        super.viewDidLoad()
        //: self.hideNavi = true
        self.hideNavi = true
        //: createUI()
        noSelection()
        //: bindInteraction()
        constructBoard()
        //: func__turnOnSystemNotification()
        anonymousCan()
    }

    // MARK: - Lazy load

    // 背景
    //: private lazy var colorImageView: UIImageView = {
    private lazy var colorImageView: UIImageView = {
        //: let colorV = UIImageView.init(frame: CGRect(x: 0, y: 0, width: ScreenWidth, height: 100+StatusBarNavigationBarHeight))
        let colorV = UIImageView(frame: CGRect(x: 0, y: 0, width: show_errorChangeFormat, height: 100 + k_underSubtleTitle))
        //: colorV.image = UIImage.BundleImageNamed(name: "bg_message_top")
        colorV.image = UIImage.managerToSecond(name: (String(user_todayName.prefix(4)) + "essa" + user_parentFormat.replacingOccurrences(of: "depth", with: "g") + String(constYaMageMessage.suffix(5))))
        //: return colorV
        return colorV
        //: }()
    }()

    //: private lazy var noticeView: TalkingNoticeTipView = {
    private lazy var noticeView: AdjustAction = {
        //: let V = TalkingNoticeTipView()
        let V = AdjustAction()
        //: V.isHidden = true
        V.isHidden = true
        //: return V
        return V
        //: }()
    }()

    //: private lazy var slideVC: QYSlideNavigationViewController = {
    private lazy var slideVC: StepRedSend = {
        //: let vc = QYSlideNavigationViewController()
        let vc = StepRedSend()
        //: vc.topDistance = 0
        vc.topDistance = 0
        //: vc.controllerItems = controllerItems
        vc.controllerItems = controllerItems
        //: vc.titleItems = titleItems
        vc.titleItems = titleItems
        //: vc.rightSpace = 60
        vc.rightSpace = 60
        //: vc.titleViewBackgroundColor = .clear
        vc.titleViewBackgroundColor = .clear
        //: vc.titleFont = .pingfangFont(type: .Medium, fontSize: 16)
        vc.titleFont = .yearNearby(type: .Medium, fontSize: 16)
        //: vc.selectTitleFont = .pingfangFont(type: .Medium, fontSize: 18)
        vc.selectTitleFont = .yearNearby(type: .Medium, fontSize: 18)
        //: vc.normalColor = UIColor(hex: "777777")!
        vc.normalColor = UIColor(hex: (mainNaturalKey.capitalized))!
        //: vc.selectedColor = UIColor.appTitleColor()
        vc.selectedColor = UIColor.untilExpected()
        //: vc.hasBttomTabBar = true
        vc.hasBttomTabBar = true
        //: vc.delegate = self
        vc.delegate = self
        //: vc.scrollEnabled = false
        vc.scrollEnabled = false
        //: return vc
        return vc
        //: }()
    }()

    //: private lazy var controllerItems: [TalkingBaseViewController] = {
    private lazy var controllerItems: [EasyChainSensor] = {
        //: var arr = [self.chatListVC, TalkingWhoLikeViewController()]
        var arr = [self.chatListVC, BrushRegulate()]
        //: if MarginExamineer.share.loginUserMode.callTabSwitch == 1 {
        if MarginExamineer.share.loginUserMode.callTabSwitch == 1 {
            //: arr.insert(TalkingVideoCallVC(), at: arr.count-1)
            arr.insert(UserGlobalProgress(), at: arr.count - 1)
        }
        //: return arr
        return arr

        //: }()
    }()

    //: private lazy var chatListVC: TalkingChatListViewController = {
    private lazy var chatListVC: ElementPrepareViewController = {
        //: let vc = TalkingChatListViewController()
        let vc = ElementPrepareViewController()
        //: return vc
        return vc
        //: }()
    }()

    //: private lazy var titleItems: [String] = {
    private lazy var titleItems: [String] = {
        //: var arr = ["Messages".localized, "Who like me".localized]
        var arr = [(String(notiClearlyValue.prefix(7)) + data_postMessage.lowercased()).localized, (String(notiHeartMsg) + String(showWanderId)).localized]
        //: if MarginExamineer.share.loginUserMode.callTabSwitch == 1 {
        if MarginExamineer.share.loginUserMode.callTabSwitch == 1 {
            //: arr.insert("Call".localized, at: arr.count-1)
            arr.insert((String(main_padPath.suffix(4))).localized, at: arr.count - 1)
        }
        //: return arr
        return arr
        //: }()
    }()

    // 断网提醒
    //: private lazy var netTipLabel: UILabel = {
    private lazy var netTipLabel: UILabel = {
        //: let tipView = UILabel.init(frame: CGRect(x: 0, y: StatusBarNavigationBarHeight, width: ScreenWidth, height: 30))
        let tipView = UILabel(frame: CGRect(x: 0, y: k_underSubtleTitle, width: show_errorChangeFormat, height: 30))
        //: tipView.font = UIFont.pingfangRugularFont(fontSize: 14)
        tipView.font = UIFont.enterCommon(fontSize: 14)
        //: tipView.textColor = .white
        tipView.textColor = .white
        //: tipView.backgroundColor = UIColor.init(hex: "#FF2348")?.withAlphaComponent(0.8)
        tipView.backgroundColor = UIColor(hex: (String(data_premiumKey) + main_relatedKey.capitalized))?.withAlphaComponent(0.8)
        //: tipView.textAlignment = .center
        tipView.textAlignment = .center
        //: tipView.text = "Current network unavailable".localized
        tipView.text = (String(k_goldMemberString) + String(notiKingData) + app_despiteMessage.replacingOccurrences(of: "unknown", with: "l")).localized
        //: tipView.isHidden = true
        tipView.isHidden = true
        //: return tipView
        return tipView
        //: }()
    }()

    // message红点角标
    //: private lazy var msgNumLabel: BadgeLab = {
    private lazy var msgNumLabel: InformationLabel = {
        //: let numbLabel = BadgeLab()
        let numbLabel = InformationLabel()
        //: numbLabel.isHidden = true
        numbLabel.isHidden = true
        //: return numbLabel
        return numbLabel
        //: }()
    }()

    // whoLikeMe红点角标
    //: private lazy var badgLB: BadgeLab = {
    private lazy var badgLB: InformationLabel = {
        //: let numbLabel = BadgeLab()
        let numbLabel = InformationLabel()
        //: numbLabel.isHidden = true
        numbLabel.isHidden = true
        //: return numbLabel
        return numbLabel
        //: }()
    }()

    // 一键清除
    //: private lazy var cleanBtn: UIButton = {
    private lazy var cleanBtn: UIButton = {
        //: let btn = UIButton(type: .custom)
        let btn = UIButton(type: .custom)
        //: btn.backgroundColor = .clear
        btn.backgroundColor = .clear
        //: btn.setImage(UIImage.BundleImageNamed(name: "icon_yidu_pre"), for: .normal)
        btn.setImage(UIImage.managerToSecond(name: (String(show_soundTitle.suffix(9)) + String(show_graphId.prefix(4)))), for: .normal)
        //: btn.setImage(UIImage.BundleImageNamed(name: "icon_yidu_pre"), for: .highlighted)
        btn.setImage(UIImage.managerToSecond(name: (String(show_soundTitle.suffix(9)) + String(show_graphId.prefix(4)))), for: .highlighted)
        //: return btn
        return btn
        //: }()
    }()
}

// MARK: - TapConcrete

//: extension TalkingMessageViewController: QYSlideNavigationDelegate {
extension AroundViewController: TapConcrete {
    //: func selectedViewController(_ viewController: UIViewController, index: Int) {
    func disabledUsed(_ viewController: UIViewController, index: Int) {
        //: self.seleteIndex = Int(index)
        self.seleteIndex = Int(index)
        //: if viewController.isKind(of: TalkingChatListViewController.self) {
        if viewController.isKind(of: ElementPrepareViewController.self) {
            //: self.cleanBtn.isHidden = false
            self.cleanBtn.isHidden = false
            //: setIsShowCleanAmimat()
            belowFinish()

            //: } else if viewController.isKind(of: TalkingWhoLikeViewController.self) {
        } else if viewController.isKind(of: BrushRegulate.self) {
            //: self.cleanBtn.isHidden = true
            self.cleanBtn.isHidden = true
            //: uploadRecord.uploadRecordEvent(eventID: ClickWLikeMeTabNoP)
            noti_loadResolveMsg.priorityFun(eventID: show_rebuildPath)
        }
    }
}

// MARK: - Event

//: extension TalkingMessageViewController {
extension AroundViewController {
    //: func setIsTopAll() {
    func nonePosition() {
        /// 默认选中all
        //: if seleteIndex == 0 {
        if seleteIndex == 0 {
            //: let vc = slideVC.controllerItems.first as? TalkingChatListViewController
            let vc = slideVC.controllerItems.first as? ElementPrepareViewController
            //: vc?.resetToTopItemView()
            vc?.old()
            // 刷新在线状态
            //: vc?.refreshOnlineStatus(isFirst: true)
            vc?.discountNorm(isFirst: true)
        }
    }

    /// 无网络提示
    //: func func__checkNetTipState() {
    func spring() {
        //: if MarginExamineer.share.networkStatus != .Unavailable && EditChannel.shared.isConnection {
        if MarginExamineer.share.networkStatus != .Unavailable, EditChannel.shared.isConnection {
            //: netTipLabel.isHidden = true
            netTipLabel.isHidden = true
            //: } else {
        } else {
            //: netTipLabel.isHidden = false
            netTipLabel.isHidden = false
        }
    }

    /// message红点
    //: func func__messagebadgLBState(num: Int) {
    func availableRank(num: Int) {
        //: if num > 0 {
        if num > 0 {
            //: msgNumLabel.isHidden = false
            msgNumLabel.isHidden = false
            //: if num > 99 {
            if num > 99 {
                //: msgNumLabel.text = "99+"
                msgNumLabel.text = "99+"
                //: } else {
            } else {
                //: msgNumLabel.text = String(num)
                msgNumLabel.text = String(num)
            }
            //: } else {
        } else {
            //: msgNumLabel.isHidden = true
            msgNumLabel.isHidden = true
        }
    }

    /// 一键清除动画
    //: func setIsShowCleanAmimat() {
    func belowFinish() {
        //: let unreadMsgCount = MarginExamineer.share.unreadMessageNum
        let unreadMsgCount = MarginExamineer.share.unreadMessageNum
        //: let isClean: Bool = UserDefaults.standard.object(forKey: isShowCleanAnimatCacheKey) as? Bool ?? false
        let isClean: Bool = UserDefaults.standard.object(forKey: dataExtraMsg) as? Bool ?? false
        //: if unreadMsgCount >= 10 && !isClean {
        if unreadMsgCount >= 10, !isClean {
            //: self.cleanBtnBtnAnimat()
            self.skinFile()
        }
    }

    //: func cleanBtnBtnAnimat() {
    func skinFile() {
        //: let degreesToRadians = 180.0 * .pi
        let degreesToRadians = 180.0 * .pi
        //: let angle1 = 0 / degreesToRadians
        let angle1 = 0 / degreesToRadians
        //: let angle2 = 100 / degreesToRadians
        let angle2 = 100 / degreesToRadians
        //: let angle3 = -100 / degreesToRadians
        let angle3 = -100 / degreesToRadians
        //: let angle4 = 100 / degreesToRadians
        let angle4 = 100 / degreesToRadians
        //: let angle5 = -100 / degreesToRadians
        let angle5 = -100 / degreesToRadians
        //: let angle6 = 0 / degreesToRadians
        let angle6 = 0 / degreesToRadians

        //: let du1: NSNumber = 0
        let du1: NSNumber = 0
        //: let du2: NSNumber = 0.13
        let du2: NSNumber = 0.13
        //: let du3: NSNumber = 0.3
        let du3: NSNumber = 0.3
        //: let du4: NSNumber = 0.47
        let du4: NSNumber = 0.47
        //: let du5: NSNumber = 0.8
        let du5: NSNumber = 0.8
        //: let du6: NSNumber = 1.0
        let du6: NSNumber = 1.0

        // 旋转
        //: let roate = CAKeyframeAnimation.init(keyPath: "transform.rotation")
        let roate = CAKeyframeAnimation(keyPath: (String(app_plateUrl) + main_belongRangeName.lowercased()))
        //: roate.values = [(angle1), (angle2), (angle3), (angle4), (angle5), (angle6)]
        roate.values = [angle1, angle2, angle3, angle4, angle5, angle6]
        //: roate.keyTimes = [(du1), (du2), (du3), (du4), (du5), (du6)]
        roate.keyTimes = [du1, du2, du3, du4, du5, du6]

        // 缩放
        //: let zoom = CAKeyframeAnimation.init(keyPath: "transform.scale")
        let zoom = CAKeyframeAnimation(keyPath: (String(kPoVoicePath) + String(const_privacyTitle)))
        //: zoom.values = [(1.0), (1.2), (1.0)]
        zoom.values = [1.0, 1.2, 1.0]
        //: zoom.keyTimes = [(0), (0.5), (1.0)]
        zoom.keyTimes = [0, 0.5, 1.0]

        //
        //: let groupAnnimation = CAAnimationGroup.init()
        let groupAnnimation = CAAnimationGroup()
        //: groupAnnimation.autoreverses = false
        groupAnnimation.autoreverses = false
        //: groupAnnimation.animations = [roate, zoom]
        groupAnnimation.animations = [roate, zoom]
        //: groupAnnimation.duration = 1.0
        groupAnnimation.duration = 1.0
        //: groupAnnimation.repeatCount = 2
        groupAnnimation.repeatCount = 2
        //: groupAnnimation.isRemovedOnCompletion = false
        groupAnnimation.isRemovedOnCompletion = false
        //: groupAnnimation.fillMode = .forwards
        groupAnnimation.fillMode = .forwards
        //: cleanBtn.layer.add(groupAnnimation, forKey: "zoom&shake")
        cleanBtn.layer.add(groupAnnimation, forKey: (String(notiVersusRejectStr) + data_commitMessage.replacingOccurrences(of: "gnat", with: "ak")))
    }

    /// 检查系统通知是否开启
    //: private func func__turnOnSystemNotification() {
    private func anonymousCan() {
        //: TalkingPermissionTool.checkPushNotification { isEnable in
        MultiplePoint.switchicial { isEnable in

            //: DispatchQueue.main.async {
            DispatchQueue.main.async {
                //: if isEnable {
                if isEnable {
                    //: self.noticeView.isHidden = true
                    self.noticeView.isHidden = true
                    //: self.func__hideNotificationTipView(hide: true)
                    self.optionFor(hide: true)
                    //: }else{
                } else {
                    /// 页面顶部提示
                    //: let today = NSDate.getTimeString(date: Date(), dateFormat: "yyyy-MM-dd")
                    let today = NSDate.particleBelong(date: Date(), dateFormat: (String(noti_greenKey) + String(mainBeAlwaysString)))
                    //: if let closeDateStr = Defaults.string(forKey: TalkingMessageTabCloseNotificationsKey), today == closeDateStr {
                    if let closeDateStr = user_findBoundMessage.string(forKey: k_characterFormat), today == closeDateStr {
                        //: self.func__hideNotificationTipView(hide: true)
                        self.optionFor(hide: true)
                        //: }else{
                    } else {
                        //: self.func__hideNotificationTipView(hide: false)
                        self.optionFor(hide: false)
                    }
                    /// 弹框逻辑
                    //: let isShow = Defaults.bool(forKey: TalkingMessageTabTurnOnNotificationsKey)
                    let isShow = user_findBoundMessage.bool(forKey: notiPerPath)
                    //: if !isShow {
                    if !isShow {
                        //: Defaults.set(true, forKey: TalkingMessageTabTurnOnNotificationsKey)
                        user_findBoundMessage.set(true, forKey: notiPerPath)
                        //: TalkingAlertShow.alert(title: nil,
                        InputMight.untilPhone(title: nil,
                                                    //: message: "Allow %@ to send you notifications?".localizedArguments(AppName),
                                                    message: String(bytes: main_feePath.map{tunnelIndex(reflect: $0)}, encoding: .utf8)!.theAppear(appClosetoTitle),
                                                    //: leftBtnTitle: "Cancel".localized,
                                                    leftBtnTitle: (String(appSparkUrl.prefix(6))).localized,
                                                    //: rightBtnTitle: "Settings".localized) {
                                                    rightBtnTitle: (String(show_labMsg) + mainBodyKey.replacingOccurrences(of: "detail", with: "s")).localized)
                        {
                            //: TalkingAlertShow.hideAlert()
                            InputMight.runningProjection()
                            //: return
                            //: } rightBlock: {
                        } rightBlock: {
                            //: if let settingsUrl = URL(string: UIApplication.openSettingsURLString) {
                            if let settingsUrl = URL(string: UIApplication.openSettingsURLString) {
                                //: UIApplication.shared.open(settingsUrl)
                                UIApplication.shared.open(settingsUrl)
                            }
                        }
                    }
                }
            }
        }
    }

    //: private func func__hideNotificationTipView(hide: Bool) {
    private func optionFor(hide: Bool) {
        //: DispatchQueue.main.async {
        DispatchQueue.main.async {
            //: if hide{
            if hide {
                //: self.noticeView.isHidden = true
                self.noticeView.isHidden = true
                //: self.slideVC.view.snp.updateConstraints { make in
                self.slideVC.view.snp.updateConstraints { make in
                    //: make.top.equalToSuperview().offset(StatusBarHeight)
                    make.top.equalToSuperview().offset(const_musicMessage)
                }
                //: self.slideVC.viewHeight = ScreenHeight - StatusBarHeight - TabBarViewHeight
                self.slideVC.viewHeight = appLoadMessage - const_musicMessage - user_directionAnalyzeId
                //: }else{
            } else {
                //: self.noticeView.isHidden = false
                self.noticeView.isHidden = false
                //: self.slideVC.view.snp.updateConstraints { make in
                self.slideVC.view.snp.updateConstraints { make in
                    //: make.top.equalToSuperview().offset(StatusBarHeight + self.noticeView.viewHeight)
                    make.top.equalToSuperview().offset(const_musicMessage + self.noticeView.viewHeight)
                }
                //: self.slideVC.viewHeight = ScreenHeight - self.noticeView.bottom - TabBarViewHeight
                self.slideVC.viewHeight = appLoadMessage - self.noticeView.bottom - user_directionAnalyzeId
            }
        }
    }
}

// MARK: - 通知

//: extension TalkingMessageViewController {
extension AroundViewController {
    /// who like me 红点通知
    //: @objc func showbadgNumberNotif(notification: NSNotification) {
    @objc func compositionCollection(notification: NSNotification) {
        //: let userinfo = notification.userInfo as![String: AnyObject]
        let userinfo = notification.userInfo as! [String: AnyObject]
        //: let badNumber: Int  = userinfo["badNumber"] as! Int
        let badNumber: Int = userinfo[(noti_gestureBoxId.replacingOccurrences(of: "coalition", with: "b") + String(userCapacityTitle.prefix(7)))] as! Int
        //: badgLB.isHidden = false
        badgLB.isHidden = false
        //: if badNumber > 99 {
        if badNumber > 99 {
            //: badgLB.text = "99+"
            badgLB.text = "99+"
            //: } else if badNumber<=0 {
        } else if badNumber <= 0 {
            //: badgLB.isHidden = true
            badgLB.isHidden = true
            //: badgLB.text = "0"
            badgLB.text = "0"
            //: } else {
        } else {
            //: badgLB.text = String(badNumber)
            badgLB.text = String(badNumber)
        }
    }
}

// MARK: - Layout

//: extension TalkingMessageViewController {
extension AroundViewController {
    /// UI
    //: private func createUI() {
    private func noSelection() {
        //: view.backgroundColor = .white
        view.backgroundColor = .white

        //: view.addSubview(colorImageView)
        view.addSubview(colorImageView)
        //: view.addSubview(noticeView)
        view.addSubview(noticeView)
        //: self.addChild(slideVC)
        self.addChild(slideVC)

        //: self.view.addSubview(slideVC.view)
        self.view.addSubview(slideVC.view)
        //: slideVC.view.backgroundColor = UIColor.clear
        slideVC.view.backgroundColor = UIColor.clear
        //: slideVC.view.snp.makeConstraints { make in
        slideVC.view.snp.makeConstraints { make in
            //: make.top.equalToSuperview().offset(StatusBarHeight)
            make.top.equalToSuperview().offset(const_musicMessage)
            //: make.leading.trailing.equalToSuperview()
            make.leading.trailing.equalToSuperview()
            //: make.bottom.equalToSuperview()
            make.bottom.equalToSuperview()
        }

        //: for btn in slideVC.allBtn {
        for btn in slideVC.allBtn {
            //: let curenbrn: UIButton = btn
            let curenbrn: UIButton = btn
            //: if curenbrn.titleLabel!.text == "Who like me".localized {
            if curenbrn.titleLabel!.text == (String(notiHeartMsg) + String(showWanderId)).localized {
                //: (curenbrn as AnyObject).addSubview(badgLB)
                (curenbrn as AnyObject).addSubview(badgLB)
                //: badgLB.snp.makeConstraints { make in
                badgLB.snp.makeConstraints { make in
                    //: make.leading.equalTo(curenbrn.titleLabel!.snp.trailing).offset(-5)
                    make.leading.equalTo(curenbrn.titleLabel!.snp.trailing).offset(-5)
                    //: make.top.equalTo(curenbrn).offset(7)
                    make.top.equalTo(curenbrn).offset(7)
                    //: make.height.equalTo(16)
                    make.height.equalTo(16)
                    //: make.width.greaterThanOrEqualTo(16)
                    make.width.greaterThanOrEqualTo(16)
                }
                //: badgLB.isHidden = true
                badgLB.isHidden = true
                //: } else if (curenbrn.titleLabel!.text == "Messages".localized) {
            } else if curenbrn.titleLabel!.text == (String(notiClearlyValue.prefix(7)) + data_postMessage.lowercased()).localized {
                //: (curenbrn as AnyObject).addSubview(msgNumLabel)
                (curenbrn as AnyObject).addSubview(msgNumLabel)
                //: msgNumLabel.snp.makeConstraints { make in
                msgNumLabel.snp.makeConstraints { make in
                    //: make.leading.equalTo(curenbrn.titleLabel!.snp.trailing).offset(-5)
                    make.leading.equalTo(curenbrn.titleLabel!.snp.trailing).offset(-5)
                    //: make.top.equalTo(curenbrn).offset(7)
                    make.top.equalTo(curenbrn).offset(7)
                    //: make.height.equalTo(16)
                    make.height.equalTo(16)
                    //: make.width.greaterThanOrEqualTo(16)
                    make.width.greaterThanOrEqualTo(16)
                }
            }
        }

        //: view.addSubview(cleanBtn)
        view.addSubview(cleanBtn)
        //: cleanBtn.snp.makeConstraints { make in
        cleanBtn.snp.makeConstraints { make in
            //: make.top.equalTo(slideVC.view.snp.top)
            make.top.equalTo(slideVC.view.snp.top)
            //: make.trailing.equalTo(-5)
            make.trailing.equalTo(-5)
            //: make.size.equalTo(CGSize(width: 44, height: 44))
            make.size.equalTo(CGSize(width: 44, height: 44))
        }

        //: view.addSubview(netTipLabel)
        view.addSubview(netTipLabel)
        //: netTipLabel.snp.makeConstraints { make in
        netTipLabel.snp.makeConstraints { make in
            //: make.top.equalTo(slideVC.view.snp.top).offset(44)
            make.top.equalTo(slideVC.view.snp.top).offset(44)
            //: make.leading.trailing.equalToSuperview()
            make.leading.trailing.equalToSuperview()
            //: make.height.equalTo(30)
            make.height.equalTo(30)
        }
    }

    /// 绑定事件
    //: private func bindInteraction() {
    private func constructBoard() {
        //: EditChannel.shared.rx
        EditChannel.shared.rx
            //: .observeWeakly(Bool.self, "isConnection")
            .observeWeakly(Bool.self, (k_independentName.replacingOccurrences(of: "foundation", with: "i") + String(notiEvenName.prefix(6)) + "tion"))
            //: .subscribe(onNext: { [weak self] (value) in
            .subscribe(onNext: { [weak self] value in
                //: guard let self = self else { return }
                guard let self = self else { return }
                //: self.func__checkNetTipState()
                self.spring()
                //: })
            })
            //: .disposed(by: disposeBag)
            .disposed(by: disposeBag)

        //: MarginExamineer.share.rx.observeWeakly(Int.self, "networkStatus")
        MarginExamineer.share.rx.observeWeakly(Int.self, (String(dataTermsDefineUrl.suffix(9)) + main_divideMusicMsg.replacingOccurrences(of: "extra", with: "s")))
            //: .subscribe(onNext: { [weak self]  (value) in
            .subscribe(onNext: { [weak self] value in
                //: guard let self = self else { return }
                guard let self = self else { return }
                //: self.func__checkNetTipState()
                self.spring()
                //: })
            })
            //: .disposed(by: disposeBag)
            .disposed(by: disposeBag)

        // 展示全部红点
        //: MarginExamineer.share.rx.observeWeakly(Int.self, "unreadMessageNum")
        MarginExamineer.share.rx.observeWeakly(Int.self, (k_indexMsg.replacingOccurrences(of: "special", with: "n") + String(dataCosyName.suffix(5)) + "ageNum"))
            //: .subscribe(onNext: { [weak self] (value) in
            .subscribe(onNext: { [weak self] value in
                //: guard let self = self else { return }
                guard let self = self else { return }
                //: let valueInt = value ?? 0
                let valueInt = value ?? 0
                //: self.func__messagebadgLBState(num: valueInt)
                self.availableRank(num: valueInt)
                //: })
            })
            //: .disposed(by: disposeBag)
            .disposed(by: disposeBag)

        //: cleanBtn.rx.controlEvent(.touchUpInside)
        cleanBtn.rx.controlEvent(.touchUpInside)
            //: .subscribe(onNext: {
            .subscribe(onNext: {
                //: let config = ShowAlertConfig()
                let config = CustomIcon()
                //: config.alignment = .center
                config.alignment = .center

                //: TalkingAlertShow.customAlert(message: "Do you want to mark all messages as read?".localized, leftBtnTitle: "Cancel".localized, rightBtnTitle: "OK".localized, leftBlock: {
                InputMight.during(message: String(bytes: noti_retKey.map{pointWork(tell: $0)}, encoding: .utf8)!.localized, leftBtnTitle: (String(appSparkUrl.prefix(6))).localized, rightBtnTitle: "OK".localized, leftBlock: {
                    //: TalkingAlertShow.hideAlert()
                    InputMight.runningProjection()

                    //: }, rightBlock: {
                }, rightBlock: {
                    //: V2TIMManager.sharedInstance().markAllMessage {
                    V2TIMManager.sharedInstance().markAllMessage {
                        //: UserDefaults.standard.set(true, forKey: isShowCleanAnimatCacheKey)
                        UserDefaults.standard.set(true, forKey: dataExtraMsg)
                        //: } fail: { code, desc in
                    } fail: { code, desc in
                        //: UploadLogTool.writeLog(msg: "消息列表一键已读失败：code:\(code), desc:\(String(describing: desc))")
//                        EvolutionDelayDestroy.afterTotalo(msg: (String(k_wenMessage) + main_popularId.capitalized + String(mainRankBrokenKey)) + "\(code)" + (String(kGravityName) + String(const_birthdayBlankFunMessage)) + "\(String(describing: desc))")
                    }
                    //: }, config: config)
                }, config: config)

                //: })
            })
            //: .disposed(by: disposeBag)
            .disposed(by: disposeBag)

        // who like me 红点通知
        //: NotificationCenter.default.addObserver(self,
        NotificationCenter.default.addObserver(self,
                                               //: selector: #selector(showbadgNumberNotif(notification:)),
                                               selector: #selector(compositionCollection(notification:)),
                                               //: name: WHOLIKEME_BADGENUMBER_NOTIF,
                                               name: data_zoneName,
                                               //: object: nil)
                                               object: nil)

        //: noticeView.closeBtnBlock = { [weak self] ()  -> Void in
        noticeView.closeBtnBlock = { [weak self] () in
            //: guard let self = self else {return}
            guard let self = self else { return }
            //: let today = NSDate.getTimeString(date: Date(), dateFormat: "yyyy-MM-dd")
            let today = NSDate.particleBelong(date: Date(), dateFormat: (String(noti_greenKey) + String(mainBeAlwaysString)))
            //: Defaults.set(today, forKey: TalkingMessageTabCloseNotificationsKey)
            user_findBoundMessage.set(today, forKey: k_characterFormat)
            //: self.func__hideNotificationTipView(hide: true)
            self.optionFor(hide: true)
        }

        //: NotificationCenter.default.rx.notification( UIApplication.willEnterForegroundNotification)
        NotificationCenter.default.rx.notification(UIApplication.willEnterForegroundNotification)
            //: .subscribe(onNext: { [weak self] (notification) in
            .subscribe(onNext: { [weak self] notification in
                //: guard let self = self else { return }
                guard let self = self else { return }
                //: self.func__turnOnSystemNotification()
                self.anonymousCan()
                //: }).disposed(by: disposeBag)
            }).disposed(by: disposeBag)
    }
}
