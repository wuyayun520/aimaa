
//: Declare String Begin

/*: "^([a-zA-Z0-9_\\.-]+)@([\\da-z\\.-]+)\\.([a-z\\.]{2,6})$" :*/
fileprivate let showThroughAdviceUrl:[UInt8] = [0x8f,0xf9,0x8a,0xb0,0xfc,0xab,0x90,0xfc,0x8b,0xe1,0xfc,0xe8,0x8e,0x8d,0xff,0xfc,0x8c,0xfa,0xf8,0x91,0xf9,0x8a,0x8d,0xb5,0xb0,0xfc,0xab,0x8d,0xff,0xfc,0x8c,0xfa,0xf8,0x8d,0xff,0xf9,0x8a,0xb0,0xfc,0xab,0x8d,0xff,0x8c,0xaa,0xe3,0xfd,0xe7,0xac,0xf8,0xf5]

private func careGate(salt num: UInt8) -> UInt8 {
    return num ^ 209
}

/*: "^((1[34578][0-9]{9})|((0\\d{2}-\\d{8})|(0\\d{3}-\\d{7,8})|(0\\d{10,11}))$" :*/
fileprivate let app_blocValue:[UInt8] = [0x80,0x4a,0x4a,0x53,0x7d,0x55,0x56,0x57,0x59,0x5a,0x7f,0x7d,0x52,0x4f,0x5b,0x7f,0x9d,0x5b,0x9f,0x4b,0x9e,0x4a,0x4a,0x52,0x7e,0x86,0x9d,0x54,0x9f,0x4f,0x7e,0x86,0x9d,0x5a,0x9f,0x4b,0x9e,0x4a,0x52,0x7e,0x86,0x9d,0x55,0x9f,0x4f,0x7e,0x86,0x9d,0x59,0x4e,0x5a,0x9f,0x4b,0x9e,0x4a,0x52,0x7e,0x86,0x9d,0x53,0x52,0x4e,0x53,0x53,0x9f,0x4b,0x4b,0x46]

fileprivate func springArea(factor num: UInt8) -> UInt8 {
    let value = Int(num) + 222
    if value > 255 {
        return UInt8(value - 256)
    } else {
        return UInt8(value)
    }
}

/*: "^([\\u4E00-\\u9FA5A-Za-z0-9_]{2,10}$)" :*/
fileprivate let k_tunMsg:[UInt8] = [0x29,0x24,0x7d,0x30,0x31,0x2c,0x32,0x7b,0x5d,0x5f,0x39,0x2d,0x30,0x7a,0x2d,0x61,0x5a,0x2d,0x41,0x35,0x41,0x46,0x39,0x75,0x5c,0x2d,0x30,0x30,0x45,0x34,0x75,0x5c,0x5b,0x28,0x5e]

/*: "^[0-8]\\d{5}(?!\\d)$" :*/
fileprivate let user_availableSuccessStr:String = "^[0-nothing"
fileprivate let mainFadeTitle:String = "(?!\\d)"
fileprivate let const_exactlyUrl:[Character] = ["$"]

/*: "^[a-zA-Z0-9]{6,20}+$" :*/
fileprivate let const_colorData:String = "this rebuild^[a-zA"
fileprivate let show_okValue:[Character] = ["9","]","{","6",",","2","0","}","+","$"]

/*: "^(https?:\\/\\/)?([\\da-z\\.-]+)\\.([a-z\\.]{2,6})([\\/\\w \\.-]*)*\\/?$" :*/
fileprivate let dataAboutQuitFormat:[UInt8] = [0x24,0x3f,0x2f,0x5c,0x2a,0x29,0x2a,0x5d,0x2d,0x2e,0x5c,0x20,0x77,0x5c,0x2f,0x5c,0x5b,0x28,0x29,0x7d,0x36,0x2c,0x32,0x7b,0x5d,0x2e,0x5c,0x7a,0x2d,0x61,0x5b,0x28,0x2e,0x5c,0x29,0x2b,0x5d,0x2d,0x2e,0x5c,0x7a,0x2d,0x61,0x64,0x5c,0x5b,0x28,0x3f,0x29,0x2f,0x5c,0x2f,0x5c,0x3a,0x3f,0x73,0x70,0x74,0x74,0x68,0x28,0x5e]

/*: "^(?:(?:25[0-5]|2[0-4][0-9]|[01]?[0-9][0-9]?)\\.) {3}(?:25[0-5]|2[0-4][0-9]|[01]?[0-9][0-9]?)$" :*/
fileprivate let user_buttData:[UInt8] = [0x24,0x29,0x3f,0x5d,0x39,0x2d,0x30,0x5b,0x5d,0x39,0x2d,0x30,0x5b,0x3f,0x5d,0x31,0x30,0x5b,0x7c,0x5d,0x39,0x2d,0x30,0x5b,0x5d,0x34,0x2d,0x30,0x5b,0x32,0x7c,0x5d,0x35,0x2d,0x30,0x5b,0x35,0x32,0x3a,0x3f,0x28,0x7d,0x33,0x7b,0x20,0x29,0x2e,0x5c,0x29,0x3f,0x5d,0x39,0x2d,0x30,0x5b,0x5d,0x39,0x2d,0x30,0x5b,0x3f,0x5d,0x31,0x30,0x5b,0x7c,0x5d,0x39,0x2d,0x30,0x5b,0x5d,0x34,0x2d,0x30,0x5b,0x32,0x7c,0x5d,0x35,0x2d,0x30,0x5b,0x35,0x32,0x3a,0x3f,0x28,0x3a,0x3f,0x28,0x5e]

/*: "(^[0-9]{15}$)|([0-9]{17}([0-9]|[xX])$)" :*/
fileprivate let user_migrationKey:[UInt8] = [0x44,0x7a,0x77,0x4c,0x49,0x55,0x79,0x97,0x4d,0x51,0x99,0x40,0x45,0x98,0x44,0x77,0x4c,0x49,0x55,0x79,0x97,0x4d,0x53,0x99,0x44,0x77,0x4c,0x49,0x55,0x79,0x98,0x77,0x94,0x74,0x79,0x45,0x40,0x45]

fileprivate func blackReply(ignore num: UInt8) -> UInt8 {
    let value = Int(num) + 228
    if value > 255 {
        return UInt8(value - 256)
    } else {
        return UInt8(value)
    }
}

/*: "SELF MATCHES %@" :*/
fileprivate let noti_uponJumpUrl:String = "SELF recent begin load super"
fileprivate let kFaceMessage:String = "ES %alive resign collection target pause"
fileprivate let const_reduceKey:String = "@"

/*: "Feedback" :*/
fileprivate let noti_adviceMsg:[Character] = ["F","e","e","d","b","a","c"]
fileprivate let showMomentPath:String = "embrace"

/*: "Enter your feedback…" :*/
fileprivate let mainImplementationId:[Character] = ["E","n","t","e","r"," ","y","o","u","r"," ","f","e","e","d","b","a","c"]
fileprivate let user_areaData:String = "k…precede reveal"

/*: "#999999" :*/
fileprivate let notiVisibleKey:String = "#999999"

/*: "Your email (Required) " :*/
fileprivate let constDeemData:[Character] = ["Y","o","u","r"," ","e","m","a","i","l"," ","(","R","e","q","u","i","r","e","d",")"]
fileprivate let mainFlushBoyfriendValue:[Character] = [" "]

/*: "icon_me_feelback_star" :*/
fileprivate let notiMakeDirectionId:[Character] = ["i","c","o","n","_"]
fileprivate let notiIncomeFormat:[Character] = ["m","e","_","f","e","e","l","b","a","c","k"]
fileprivate let main_processingString:[Character] = ["_","s","t","a","r"]

/*: "#CCCCCC" :*/
fileprivate let main_reductionValue:[Character] = ["#","C","C","C","C","C","C"]

/*: "Send" :*/
fileprivate let mainDynamicsTriggerData:String = "selected successfully ark capableSend"

/*: "#D0D0D0" :*/
fileprivate let constToolTitle:String = "receiver marker main#D0D0D"
fileprivate let k_cycleStr:[Character] = ["0"]

/*: "Please fill in the content" :*/
fileprivate let show_taskFormat:String = "po beat correct sir mentionPlea"
fileprivate let k_cityTitle:String = "function due hil in t"
fileprivate let app_receiverFrequencyMsg:String = "mintemint"

/*: "Please enter the correct email address" :*/
fileprivate let kEducatorStr:[UInt8] = [0xe2,0xfe,0xf7,0xf3,0x5,0xf7,0xb2,0xf7,0x0,0x6,0xf7,0x4,0xb2,0x6,0xfa,0xf7,0xb2,0xf5,0x1,0x4,0x4,0xf7,0xf5,0x6,0xb2,0xf7,0xff,0xf3,0xfb,0xfe,0xb2,0xf3,0xf6,0xf6,0x4,0xf7,0x5,0x5]

fileprivate func arrowList(following num: UInt8) -> UInt8 {
    let value = Int(num) - 146
    if value < 0 {
        return UInt8(value + 256)
    } else {
        return UInt8(value)
    }
}

/*: "content" :*/
fileprivate let constBackgroundStr:[Character] = ["c","o","n","t","e","n","t"]

/*: "contactWay" :*/
fileprivate let k_markReportValue:[Character] = ["c","o","n","t","a","c","t"]
fileprivate let const_topString:String = "Waycombined owner package version"

/*: "platform" :*/
fileprivate let user_contextTitle:String = "speedl"
fileprivate let show_lockMessage:[Character] = ["a","t","f","o","r","m"]

/*: "iphone" :*/
fileprivate let noti_compositionCycleNumberoName:[Character] = ["i","p","h","o","n","e"]

/*: "version" :*/
fileprivate let main_presentName:String = "V"
fileprivate let data_partnerExamineionPath:[Character] = ["e","r","s","i","o","n"]

/*: "type" :*/
fileprivate let dataShrimpMsg:String = "typeveryone"

/*: "Operation succeeded" :*/
fileprivate let data_visitorMsg:[Character] = ["O","p","e","r","a","t","i"]
fileprivate let appCertainElementKey:String = "osplit"
fileprivate let noti_whichString:String = "limited equipment suc"

/*: "Problem statements" :*/
fileprivate let show_ofKey:String = "party file agile meProb"
fileprivate let mainPartScreeningFormat:String = "professional reply sustainable streak evaluatelem st"
fileprivate let main_producePainterValue:String = "greatnts"

/*: "Feature advice" :*/
fileprivate let notiComplyMessage:String = "count first strength only connectFeature a"
fileprivate let showRollBeanKey:String = "DVICE"

/*: "Operation questions" :*/
fileprivate let kOperateTitle:String = "Operatappropriate city chest continue"
fileprivate let data_plusMakeName:String = "resolveons"

/*: "Others" :*/
fileprivate let const_roleName:String = "processing three maxi instruction tailOthers"

/*: "#EFEDFF" :*/
fileprivate let noti_helloUrl:String = "must pet isolate#E"
fileprivate let show_conversationId:[Character] = ["F","E","D","F","F"]

//: Declare String End

// __DEBUG__
// __CLOSE_PRINT__
//
//  AddSpellKey.swift
//  AbroadTalking
//
//  Created by Charlotte on 2022/9/15.
//

//: import UIKit
import UIKit

//: enum Validate {
enum VideoTool {
    //: case email(_: String)
    case email(_: String) //  邮箱
    //: case phoneNumber(_: String)
    case phoneNumber(_: String) //  手机号
    //: case userName(_: String)
    case userName(_: String) //  用户名
    //: case password(_: String)
    case password(_: String) //  密码
    //: case nickName(_: String)
    case nickName(_: String) //  昵称
    //: case postalCode(_: String)
    case postalCode(_: String) //  邮编
    //: case URL(_: String)
    case URL(_: String) //  URL
    //: case IP(_: String)
    case IP(_: String) //  IP
    //: case isChinese(_: String)
    case isChinese(_: String) //  是否全是中文字符
    //: case idNumber(_: String)
    case idNumber(_: String) //  身份证号

    //: var isRight: Bool {
    var isRight: Bool {
        //: var predicateStr: String!
        var predicateStr: String!
        //: var currentObject: String!
        var currentObject: String!
        //: switch self {
        switch self {
        //: case let .email(str):
        case let .email(str):
            //: predicateStr = "^([a-zA-Z0-9_\\.-]+)@([\\da-z\\.-]+)\\.([a-z\\.]{2,6})$"
            predicateStr = String(bytes: showThroughAdviceUrl.map{careGate(salt: $0)}, encoding: .utf8)!
            //: currentObject = str
            currentObject = str
        //: case let .phoneNumber(str):
        case let .phoneNumber(str):
            //: predicateStr = "^((1[34578][0-9]{9})|((0\\d{2}-\\d{8})|(0\\d{3}-\\d{7,8})|(0\\d{10,11}))$"
            predicateStr = String(bytes: app_blocValue.map{springArea(factor: $0)}, encoding: .utf8)!
            //: currentObject = str
            currentObject = str
        //: case let .userName(str):
        case let .userName(str):
            //: predicateStr = "^([\\u4E00-\\u9FA5A-Za-z0-9_]{2,10}$)"
            predicateStr = String(bytes: k_tunMsg.reversed(), encoding: .utf8)!
            //: currentObject = str
            currentObject = str
        //: case let .postalCode(str):
        case let .postalCode(str):
            //: predicateStr = "^[0-8]\\d{5}(?!\\d)$"
            predicateStr = (user_availableSuccessStr.replacingOccurrences(of: "nothing", with: "8") + "]\\d{5}" + mainFadeTitle + String(const_exactlyUrl))
            //: currentObject = str
            currentObject = str
        //: case let .password(str):
        case let .password(str):
            //: predicateStr = "^[a-zA-Z0-9]{6,20}+$"
            predicateStr = (String(const_colorData.suffix(6)) + "-Z0-" + String(show_okValue))
            //: currentObject = str
            currentObject = str
        //: case let .nickName(str):
        case let .nickName(str):
            //: predicateStr = "^([\\u4E00-\\u9FA5A-Za-z0-9_]{2,10}$)"
            predicateStr = String(bytes: k_tunMsg.reversed(), encoding: .utf8)!
            //: currentObject = str
            currentObject = str
        //: case let .URL(str):
        case let .URL(str):
            //: predicateStr = "^(https?:\\/\\/)?([\\da-z\\.-]+)\\.([a-z\\.]{2,6})([\\/\\w \\.-]*)*\\/?$"
            predicateStr = String(bytes: dataAboutQuitFormat.reversed(), encoding: .utf8)!
            //: currentObject = str
            currentObject = str
        //: case let .IP(str):
        case let .IP(str):
            //: predicateStr = "^(?:(?:25[0-5]|2[0-4][0-9]|[01]?[0-9][0-9]?)\\.) {3}(?:25[0-5]|2[0-4][0-9]|[01]?[0-9][0-9]?)$"
            predicateStr = String(bytes: user_buttData.reversed(), encoding: .utf8)!
            //: currentObject = str
            currentObject = str
        //: case let . isChinese(str):
        case let .isChinese(str):
            //: predicateStr = "(^[\u{4e00}-\u{9fa5}]$)"
            predicateStr = "(^[\u{4e00}-\u{9fa5}]$)"
            //: currentObject = str
            currentObject = str
        //: case let . idNumber(str):
        case let .idNumber(str):
            //: predicateStr = "(^[0-9]{15}$)|([0-9]{17}([0-9]|[xX])$)"
            predicateStr = String(bytes: user_migrationKey.map{blackReply(ignore: $0)}, encoding: .utf8)!
            //: currentObject = str
            currentObject = str
        }
        //: let predicate =  NSPredicate(format: "SELF MATCHES %@", predicateStr)
        let predicate = NSPredicate(format: (String(noti_uponJumpUrl.prefix(5)) + "MATCH" + String(kFaceMessage.prefix(4)) + const_reduceKey.capitalized), predicateStr)
        //: return predicate.evaluate(with: currentObject)
        return predicate.evaluate(with: currentObject)
    }
}

//: class TalkingFeedbackVC: TalkingBaseViewController {
class AddSpellKey: EasyChainSensor {
    //: let limitCount = 300
    let limitCount = 300
    //: let EmialCount = 50
    let EmialCount = 50
    //: var seleteBtn: UIButton?
    var seleteBtn: UIButton?

    //: override func viewDidLoad() {
    override func viewDidLoad() {
        //: super.viewDidLoad()
        super.viewDidLoad()

        //: self.title = "Feedback".localized
        self.title = (String(noti_adviceMsg) + showMomentPath.replacingOccurrences(of: "embrace", with: "k")).localized
        //: self.view.backgroundColor = .white
        self.view.backgroundColor = .white
        //: designView()
        wrapFresh()
        //: addTapGestureRecognizer()
        reservePanel()
        //: addKeyboardNotification()
        capture()
    }

    // MARK: - Lazy load

    //: lazy var backView: UIScrollView = {
    lazy var backView: UIScrollView = {
        //: let view = UIScrollView.init()
        let view = UIScrollView()
        //: view.backgroundColor = .white
        view.backgroundColor = .white
        //: view.showsHorizontalScrollIndicator = false
        view.showsHorizontalScrollIndicator = false
        //: view.showsVerticalScrollIndicator = false
        view.showsVerticalScrollIndicator = false
        //: return view
        return view
        //: }()
    }()

    //: lazy var contView: UIView = {
    lazy var contView: UIView = {
        //: let view = UIView.init()
        let view = UIView()
        //: view.backgroundColor = .white
        view.backgroundColor = .white
        //: return view
        return view
        //: }()
    }()

    //: lazy var inputTView: UIView = {
    lazy var inputTView: UIView = {
        //: let view = UIView.init()
        let view = UIView()
        //: view.clipsToBounds = true
        view.clipsToBounds = true
        //: view.layer.cornerRadius = 8
        view.layer.cornerRadius = 8
        //: view.backgroundColor = .appBgColor()
        view.backgroundColor = .alongSupposed()
        //: return view
        return view
        //: }()
    }()

    //: lazy var textView: TalkingTextView = {
    lazy var textView: FieldView = {
        //: let textView = TalkingTextView.init()
        let textView = FieldView()
        //: textView.placeholder = "Enter your feedback…".localized
        textView.placeholder = (String(mainImplementationId) + String(user_areaData.prefix(2))).localized
        //: textView.placeholderColor = UIColor.init(hex: "#999999")!
        textView.placeholderColor = UIColor(hex: (notiVisibleKey.capitalized))!
        //: textView.font = UIFont.pingfangFont(type: .Regular, fontSize: 15)
        textView.font = UIFont.yearNearby(type: .Regular, fontSize: 15)
        //: textView.textColor = UIColor.appTitleColor()
        textView.textColor = UIColor.untilExpected()
        //: textView.PlaceholderInsets = UIEdgeInsets(top: 16, left: 16, bottom: 10, right: 16)
        textView.PlaceholderInsets = UIEdgeInsets(top: 16, left: 16, bottom: 10, right: 16)

        //: textView.textContainer.maximumNumberOfLines = 0
        textView.textContainer.maximumNumberOfLines = 0
        //: textView.textContainer.lineBreakMode = .byTruncatingTail
        textView.textContainer.lineBreakMode = .byTruncatingTail
        //: textView.delegate = self
        textView.delegate = self
        //: textView.returnKeyType = UIReturnKeyType.done
        textView.returnKeyType = UIReturnKeyType.done
        //: textView.bounces = false
        textView.bounces = false
        //: textView.backgroundColor = .appBgColor()
        textView.backgroundColor = .alongSupposed()
        //: numberLB.text =  "0/\(limitCount)"
        numberLB.text = "0/\(limitCount)"
        //: return textView
        return textView
        //: }()
    }()

    //: lazy var numberLB: UILabel = {
    lazy var numberLB: UILabel = {
        //: let LB = UILabel.init()
        let LB = UILabel()
        //: LB.textColor = UIColor.init(hex: "#999999")
        LB.textColor = UIColor(hex: (notiVisibleKey.capitalized))
        //: LB.font = UIFont.pingfangFont(type: .Regular, fontSize: 14)
        LB.font = UIFont.yearNearby(type: .Regular, fontSize: 14)
        //: LB.text = "0/\(limitCount)"
        LB.text = "0/\(limitCount)"
        //: LB.textAlignment = .right
        LB.textAlignment = .right
        //: return LB
        return LB
        //: }()
    }()

    //: lazy var emailTF: UITextField = {
    lazy var emailTF: UITextField = {
        //: let tf = UITextField.init()
        let tf = UITextField()
        //: tf.backgroundColor = .appBgColor()
        tf.backgroundColor = .alongSupposed()
        //: tf.textColor = UIColor.appTitleColor()
        tf.textColor = UIColor.untilExpected()
        //: tf.font = UIFont.pingfangFont(type: .Regular, fontSize: 16)
        tf.font = UIFont.yearNearby(type: .Regular, fontSize: 16)
        //: tf.delegate = self
        tf.delegate = self
        //: tf.returnKeyType = UIReturnKeyType.done
        tf.returnKeyType = UIReturnKeyType.done
        //: tf.attributedPlaceholder = NSAttributedString.init(string: "Your email (Required) ".localized, attributes: [
        tf.attributedPlaceholder = NSAttributedString(string: (String(constDeemData) + String(mainFlushBoyfriendValue)).localized, attributes: [
            //: NSAttributedString.Key.foregroundColor: UIColor.init(hex: "#999999")!])
            NSAttributedString.Key.foregroundColor: UIColor(hex: (notiVisibleKey.capitalized))!])
        //: tf.clipsToBounds = true
        tf.clipsToBounds = true
        //: tf.layer.cornerRadius = 8
        tf.layer.cornerRadius = 8
        //: if LanguageManager.shared.direction == .rightToLeft {
        if InputCloseInfo.shared.direction == .rightToLeft {
            //: tf.textAlignment = .right
            tf.textAlignment = .right
        }
        //: let leftView = UIView()
        let leftView = UIView()
        //: leftView.frame = CGRect(origin: .zero, size: CGSize(width: 30, height: 49))
        leftView.frame = CGRect(origin: .zero, size: CGSize(width: 30, height: 49))

        //: let leftImgView = UIImageView.init()
        let leftImgView = UIImageView()
        //: leftImgView.frame = CGRect(x: 16, y: 20, width: 9, height: 9)
        leftImgView.frame = CGRect(x: 16, y: 20, width: 9, height: 9)
        //: leftImgView.image = UIImage.BundleImageNamed(name: "icon_me_feelback_star")
        leftImgView.image = UIImage.managerToSecond(name: (String(notiMakeDirectionId) + String(notiIncomeFormat) + String(main_processingString)))
        //: leftView.addSubview(leftImgView)
        leftView.addSubview(leftImgView)
        //: tf.leftView = leftView
        tf.leftView = leftView
        //: tf.leftViewMode = .always
        tf.leftViewMode = .always
        //: return tf
        return tf
        //: }()
    }()

    //: lazy var doneBtn: UIButton = {
    lazy var doneBtn: UIButton = {
        //: let btn = UIButton.init()
        let btn = UIButton()
        //: btn.layer.cornerRadius = 25
        btn.layer.cornerRadius = 25
        //: btn.setBackgroundColor(color: UIColor.init(hex: "#CCCCCC")!, forState: .disabled)
        btn.faceCrop(color: UIColor(hex: (String(main_reductionValue)))!, forState: .disabled)
        //: btn.setBackgroundColor(color: UIColor.appThemeColor(), forState: .normal)
        btn.faceCrop(color: UIColor.systemRequest(), forState: .normal)
        //: btn.setTitle("Send".localized, for: .normal)
        btn.setTitle((String(mainDynamicsTriggerData.suffix(4))).localized, for: .normal)
        //: btn.setTitleColor(.white, for: .normal)
        btn.setTitleColor(.white, for: .normal)
        //: btn.titleLabel?.font = UIFont.pingfangFont(type: .Medium, fontSize: 17)
        btn.titleLabel?.font = UIFont.yearNearby(type: .Medium, fontSize: 17)
        //: btn.addTarget(self, action: #selector(doneBtnClick), for: .touchUpInside)
        btn.addTarget(self, action: #selector(birthday), for: .touchUpInside)
        //: return btn
        return btn
        //: }()
    }()
}

// MARK: - Event

//: extension TalkingFeedbackVC {
extension AddSpellKey {
    //: @objc func TagBtnClick(sender: UIButton) {
    @objc func large(sender: UIButton) {
        //: seleteBtn?.isSelected = false
        seleteBtn?.isSelected = false
        //: seleteBtn?.layer.borderColor = UIColor.init(hex: "#D0D0D0")?.cgColor
        seleteBtn?.layer.borderColor = UIColor(hex: (String(constToolTitle.suffix(6)) + String(k_cycleStr)))?.cgColor
        //: sender.isSelected = !sender.isSelected
        sender.isSelected = !sender.isSelected
        //: seleteBtn = sender
        seleteBtn = sender
        //: seleteBtn!.layer.borderColor = UIColor.clear.cgColor
        seleteBtn!.layer.borderColor = UIColor.clear.cgColor
    }

    //: @objc func doneBtnClick() {
    @objc func birthday() {
        //: if seleteBtn == nil || textView.text.count<=0 || emailTF.text!.count<=0 {
        if seleteBtn == nil || textView.text.count <= 0 || emailTF.text!.count <= 0 {
            //: self.func__showStatusBarErrorMsg(showMsg: "Please fill in the content".localized)
            self.exitMarker(showMsg: (String(show_taskFormat.suffix(4)) + "se fil" + String(k_cityTitle.suffix(6)) + "he co" + app_receiverFrequencyMsg.replacingOccurrences(of: "min", with: "n")).localized)
            //: return
            return
        }

        //: if LanguageManager.shared.direction == .leftToRight {
        if InputCloseInfo.shared.direction == .leftToRight {
            //: if !Validate.email(emailTF.text!).isRight {
            if !VideoTool.email(emailTF.text!).isRight {
                //: self.func__showStatusBarErrorMsg(showMsg: "Please enter the correct email address".localized)
                self.exitMarker(showMsg: String(bytes: kEducatorStr.map{arrowList(following: $0)}, encoding: .utf8)!.localized)
                //: return
                return
            }
        }

        //: var dict = Dictionary<String, Any>()
        var dict = [String: Any]()
        //: dict["content"] = textView.text
        dict[(String(constBackgroundStr))] = textView.text
        //: dict["contactWay"] = emailTF.text
        dict[(String(k_markReportValue) + String(const_topString.prefix(3)))] = emailTF.text
        //: dict["platform"] = "iphone"
        dict[(user_contextTitle.replacingOccurrences(of: "speed", with: "p") + String(show_lockMessage))] = (String(noti_compositionCycleNumberoName))
        //: dict["version"] = AppVersion
        dict[(main_presentName.lowercased() + String(data_partnerExamineionPath))] = show_prepareMsg
        //: dict["type"] = String((seleteBtn?.tag ?? 100)-100)
        dict[(dataShrimpMsg.replacingOccurrences(of: "everyone", with: "e"))] = String((seleteBtn?.tag ?? 100) - 100)

        //: ProgressHUD.show()
        BeforeImagePhase.theGemRoll()

        //: TalkingMeRequestTool.req_Feedback(params: dict) { succeed, result, errorModel in
        ConstraintPublisher.turnOf(params: dict) { succeed, result, errorModel in
            //: ProgressHUD.dismiss()
            BeforeImagePhase.totalGap()
            //: if succeed {
            if succeed {
                //: self.func__showStatusBarSuccessMsg(showMsg: "Operation succeeded".localized)
                self.nearEasy(showMsg: (String(data_visitorMsg) + appCertainElementKey.replacingOccurrences(of: "split", with: "n") + String(noti_whichString.suffix(4)) + "ceeded").localized)
                //: self.navigationController?.popViewController(animated: true)
                self.navigationController?.popViewController(animated: true)
            }
        }
    }

    //: func addKeyboardNotification() {
    func capture() {
        //: NotificationCenter.default.addObserver(self,
        NotificationCenter.default.addObserver(self,
                                               //: selector: #selector(keyboardWillShow(notification:)),
                                               selector: #selector(transfer(notification:)),
                                               //: name: UIResponder.keyboardWillShowNotification,
                                               name: UIResponder.keyboardWillShowNotification,
                                               //: object: nil)
                                               object: nil)
        //: NotificationCenter.default.addObserver(self,
        NotificationCenter.default.addObserver(self,
                                               //: selector: #selector(keyboardWillHide(notification:)),
                                               selector: #selector(becomeWith(notification:)),
                                               //: name: UIResponder.keyboardWillHideNotification,
                                               name: UIResponder.keyboardWillHideNotification,
                                               //: object: nil)
                                               object: nil)
    }

    //: @objc func keyboardWillShow(notification: Notification) {
    @objc func transfer(notification: Notification) {
        //: guard emailTF.isFirstResponder else { return }
        guard emailTF.isFirstResponder else { return }
        //: guard let info = notification.userInfo else { return }
        guard let info = notification.userInfo else { return }
        //: let value = info[UIResponder.keyboardFrameEndUserInfoKey] as? NSValue
        let value = info[UIResponder.keyboardFrameEndUserInfoKey] as? NSValue
        //: let frame = value?.cgRectValue ?? .zero
        let frame = value?.cgRectValue ?? .zero
        //: let duration = info[UIResponder.keyboardAnimationDurationUserInfoKey] as? Double ?? 0.25
        let duration = info[UIResponder.keyboardAnimationDurationUserInfoKey] as? Double ?? 0.25
        //: guard (emailTF.bottom + StatusBarNavigationBarHeight) > frame.minY else { return }
        guard (emailTF.bottom + k_underSubtleTitle) > frame.minY else { return }

        //: let y = (emailTF.bottom + StatusBarNavigationBarHeight) - frame.minY
        let y = (emailTF.bottom + k_underSubtleTitle) - frame.minY
        //: UIView.animate(withDuration: duration) {
        UIView.animate(withDuration: duration) {
            //: self.backView.transform = CGAffineTransform(translationX: 0, y: -y)
            self.backView.transform = CGAffineTransform(translationX: 0, y: -y)
        }
    }

    //: @objc func keyboardWillHide(notification: Notification) {
    @objc func becomeWith(notification: Notification) {
        //: guard let info = notification.userInfo else { return }
        guard let info = notification.userInfo else { return }
        //: let duration = info[UIResponder.keyboardAnimationDurationUserInfoKey] as? Double ?? 0.25
        let duration = info[UIResponder.keyboardAnimationDurationUserInfoKey] as? Double ?? 0.25

        //: UIView.animate(withDuration: duration) {
        UIView.animate(withDuration: duration) {
            //: self.backView.transform = .identity
            self.backView.transform = .identity
        }
    }
}

// MARK: - UITextViewDelegate, UITextFieldDelegate

//: extension TalkingFeedbackVC: UITextViewDelegate, UITextFieldDelegate {
extension AddSpellKey: UITextViewDelegate, UITextFieldDelegate {
    //: public func textViewDidChange(_ textView: UITextView) {
    public func textViewDidChange(_ textView: UITextView) {
        //: if textView.text.count > limitCount {
        if textView.text.count > limitCount {
            // 获得已输出字数与正输入字母数
            //: let selectRange = textView.markedTextRange
            let selectRange = textView.markedTextRange

            // 获取高亮部分 － 如果有联想词则解包成功
            //: if let selectRange = selectRange {
            if let selectRange = selectRange {
                //: let position =  textView.position(from: (selectRange.start), offset: 0)
                let position = textView.position(from: selectRange.start, offset: 0)
                //: if (position != nil) {
                if position != nil {
                    //: return
                    return
                }
            }

            //: let textContent = textView.text ?? ""
            let textContent = textView.text ?? ""
            //: let textNum = textContent.count
            let textNum = textContent.count

            // 截取
            //: if textNum > limitCount && limitCount > 0 {
            if textNum > limitCount && limitCount > 0 {
                //: textView.text = string_prefix(index: limitCount, text: textContent)
                textView.text = stop(index: limitCount, text: textContent)
            }
        }
        //: numberLB.text =  "\(textView.text.count)/\(limitCount)"
        numberLB.text = "\(textView.text.count)/\(limitCount)"
    }

    //: public func textViewShouldBeginEditing(_ textView: UITextView) -> Bool {
    public func textViewShouldBeginEditing(_ textView: UITextView) -> Bool {
        //: numberLB.isHidden = false
        numberLB.isHidden = false
        //: return true
        return true
    }

    //: public func textViewDidEndEditing(_ textView: UITextView) {
    public func textViewDidEndEditing(_ textView: UITextView) {
        //: if textView.text.isEmpty {
        if textView.text.isEmpty {
            //: numberLB.isHidden = false
            numberLB.isHidden = false
            //: } else {
        } else {
            //: numberLB.isHidden = true
            numberLB.isHidden = true
        }
    }

    //: public func textView(_ textView: UITextView, shouldChangeTextIn range: NSRange, replacementText text: String) -> Bool {
    public func textView(_ textView: UITextView, shouldChangeTextIn range: NSRange, replacementText text: String) -> Bool {
        //: if textView.returnKeyType == .done {
        if textView.returnKeyType == .done {
            //: if text == "\n" {
            if text == "\n" {
                //: textView.resignFirstResponder()
                textView.resignFirstResponder()
            }
            //: return true
            return true
        }
        //: return true
        return true
    }

    // 字符串的截取 从头截取到指定index
    //: private func string_prefix(index: Int,text:String) -> String {
    private func stop(index: Int, text: String) -> String {
        //: if text.count <= index {
        if text.count <= index {
            //: return text
            return text
            //: } else {
        } else {
            //: let index = text.index(text.startIndex, offsetBy: index)
            let index = text.index(text.startIndex, offsetBy: index)
            //: let str = text.prefix(upTo: index)
            let str = text.prefix(upTo: index)
            //: return String(str)
            return String(str)
        }
    }

    //: func textFieldShouldReturn(_ textField: UITextField) -> Bool {
    func textFieldShouldReturn(_ textField: UITextField) -> Bool {
        //: textField.resignFirstResponder()
        textField.resignFirstResponder()

        //: return true
        return true
    }

    //: func textField(_ textField: UITextField, shouldChangeCharactersIn range: NSRange, replacementString string: String) -> Bool {
    func textField(_ textField: UITextField, shouldChangeCharactersIn range: NSRange, replacementString string: String) -> Bool {
        //: if textField.text!.count+string.count>EmialCount || textField.text!.count+string.count <= 0 {
        if textField.text!.count + string.count > EmialCount || textField.text!.count + string.count <= 0 {
            //: return false
            return false
        }
        //: return true
        return true
    }
}

// MARK: - UI

//: extension TalkingFeedbackVC {
extension AddSpellKey {
    //: func designView() {
    func wrapFresh() {
        //: self.view.addSubview(backView)
        self.view.addSubview(backView)
        //: backView.snp.makeConstraints { make in
        backView.snp.makeConstraints { make in
            //: make.edges.equalTo(self.view)
            make.edges.equalTo(self.view)
        }
        //: backView.addSubview(contView)
        backView.addSubview(contView)
        //: contView.snp.makeConstraints { make in
        contView.snp.makeConstraints { make in
            //: make.leading.trailing.top.bottom.equalTo(backView)
            make.leading.trailing.top.bottom.equalTo(backView)
        }

        //: contView.addSubview(inputTView)
        contView.addSubview(inputTView)
        //: inputTView.snp.makeConstraints { make in
        inputTView.snp.makeConstraints { make in
            //: make.leading.equalTo(contView).offset(15)
            make.leading.equalTo(contView).offset(15)
            //: make.trailing.equalTo(contView.snp.trailing).offset(-15)
            make.trailing.equalTo(contView.snp.trailing).offset(-15)
            //: make.top.equalTo(contView).offset(128)
            make.top.equalTo(contView).offset(128)
            //: make.height.equalTo(210)
            make.height.equalTo(210)
        }

        //: inputTView.addSubview(textView)
        inputTView.addSubview(textView)
        //: inputTView.addSubview(numberLB)
        inputTView.addSubview(numberLB)
        //: textView.snp.makeConstraints { make in
        textView.snp.makeConstraints { make in
            //: make.leading.trailing.top.equalTo(inputTView)
            make.leading.trailing.top.equalTo(inputTView)
            //: make.bottom.equalTo(inputTView.snp.bottom).offset(-40)
            make.bottom.equalTo(inputTView.snp.bottom).offset(-40)
        }
        //: numberLB.snp.makeConstraints { make in
        numberLB.snp.makeConstraints { make in
            //: make.trailing.equalTo(inputTView.snp.trailing).offset(-16)
            make.trailing.equalTo(inputTView.snp.trailing).offset(-16)
            //: make.bottom.equalTo(inputTView.snp.bottom).offset(-12)
            make.bottom.equalTo(inputTView.snp.bottom).offset(-12)
            //: make.height.equalTo(20)
            make.height.equalTo(20)
        }
        //: contView.addSubview(emailTF)
        contView.addSubview(emailTF)
        //: emailTF.snp.makeConstraints { make in
        emailTF.snp.makeConstraints { make in
            //: make.leading.equalTo(contView).offset(15)
            make.leading.equalTo(contView).offset(15)
            //: make.trailing.equalTo(contView.snp.trailing).offset(-15)
            make.trailing.equalTo(contView.snp.trailing).offset(-15)
            //: make.top.equalTo(inputTView.snp.bottom).offset(16)
            make.top.equalTo(inputTView.snp.bottom).offset(16)
            //: make.height.equalTo(49)
            make.height.equalTo(49)
        }

        //: contView.addSubview(doneBtn)
        contView.addSubview(doneBtn)
        //: doneBtn.snp.makeConstraints { make in
        doneBtn.snp.makeConstraints { make in
            //: make.leading.equalTo(contView).offset(54)
            make.leading.equalTo(contView).offset(54)
            //: make.trailing.equalTo(contView.snp.trailing).offset(-54)
            make.trailing.equalTo(contView.snp.trailing).offset(-54)
            //: make.top.equalTo(emailTF.snp.bottom).offset(111)
            make.top.equalTo(emailTF.snp.bottom).offset(111)
            //: make.height.equalTo(50)
            make.height.equalTo(50)
        }

        //: let arr = ["Problem statements".localized,
        let arr = [(String(show_ofKey.suffix(4)) + String(mainPartScreeningFormat.suffix(6)) + "atem" + main_producePainterValue.replacingOccurrences(of: "great", with: "e")).localized,
                   //: "Feature advice".localized,
                   (String(notiComplyMessage.suffix(9)) + showRollBeanKey.lowercased()).localized,
                   //: "Operation questions".localized,
                   (String(kOperateTitle.prefix(6)) + "ion ques" + data_plusMakeName.replacingOccurrences(of: "resolve", with: "ti")).localized,
                   //: "Others".localized]
                   (String(const_roleName.suffix(6))).localized]

        //: for i in 0..<arr.count {
        for i in 0 ..< arr.count {
            //: let xz = 15
            let xz = 15
            //: let yz = 16
            let yz = 16
            //: let Width = (Int(ScreenWidth)-xz*3)/2
            let Width = (Int(show_errorChangeFormat) - xz * 3) / 2
            //: let height = 38
            let height = 38
            //: let X = xz+(xz + Width)*(i%2)
            let X = xz + (xz + Width) * (i % 2)
            //: let Y = yz+(yz+height)*(i/2)
            let Y = yz + (yz + height) * (i / 2)

            //: let btn = UIButton.init()
            let btn = UIButton()
            //: btn.frame =  CGRect.init(x: X, y: Y , width: Width, height: height)
            btn.frame = CGRect(x: X, y: Y, width: Width, height: height)
            //: btn.setTitle(arr[i], for: .normal)
            btn.setTitle(arr[i], for: .normal)
            //: btn.setTitleColor(.black, for: .normal)
            btn.setTitleColor(.black, for: .normal)
            //: btn.setTitleColor(.appThemeColor(), for: .selected)
            btn.setTitleColor(.systemRequest(), for: .selected)

            //: btn.titleLabel?.font = UIFont.pingfangFont(type: .Regular, fontSize: 15)
            btn.titleLabel?.font = UIFont.yearNearby(type: .Regular, fontSize: 15)
            //: if TalkingRequestAddrTool.share.interfaceLang == LangType.es.rawValue || TalkingRequestAddrTool.share.interfaceLang == LangType.pt.rawValue {
            if PresentLocal.share.interfaceLang == StrokeHideBind.es.rawValue || PresentLocal.share.interfaceLang == StrokeHideBind.pt.rawValue {
                //: btn.titleLabel?.font = UIFont.pingfangFont(type: .Regular, fontSize: 13)
                btn.titleLabel?.font = UIFont.yearNearby(type: .Regular, fontSize: 13)
            }
            //: btn.layer.cornerRadius = 19
            btn.layer.cornerRadius = 19
            //: btn.layer.borderWidth = 1
            btn.layer.borderWidth = 1
            //: btn.layer.borderColor = UIColor.init(hex: "#D0D0D0")?.cgColor
            btn.layer.borderColor = UIColor(hex: (String(constToolTitle.suffix(6)) + String(k_cycleStr)))?.cgColor
            //: btn.setBackgroundColor(color: UIColor.init(hex: "#EFEDFF")!, forState: .selected)
            btn.faceCrop(color: UIColor(hex: (String(noti_helloUrl.suffix(2)) + String(show_conversationId)))!, forState: .selected)
            //: btn.setBackgroundColor(color: .white, forState: .normal)
            btn.faceCrop(color: .white, forState: .normal)
            //: btn.tag = 100+i
            btn.tag = 100 + i
            //: btn.addTarget( self, action: #selector(TagBtnClick(sender:)), for: .touchUpInside)
            btn.addTarget(self, action: #selector(large(sender:)), for: .touchUpInside)
            //: contView.addSubview(btn)
            contView.addSubview(btn)
        }

        //: contView.snp.remakeConstraints { make in
        contView.snp.remakeConstraints { make in
            //: make.bottom.equalTo(doneBtn.snp.bottom).offset(50)
            make.bottom.equalTo(doneBtn.snp.bottom).offset(50)
            //: make.width.equalTo(backView)
            make.width.equalTo(backView)
        }
    }
}
