
//: Declare String Begin

/*: "fee" :*/
fileprivate let userMissingString:String = "fbuttonbutton"

/*: "VIPFee" :*/
fileprivate let dataStandardCriticalName:[Character] = ["V","I","P","F","e","e"]

/*: "chattingNum" :*/
fileprivate let k_sIllegalString:[Character] = ["c","h","a","t","t","i"]
fileprivate let dataSpeakerFormat:[Character] = ["n","g","N","u","m"]

/*: "You're already in her live room" :*/
fileprivate let dataCityHardTitle:[UInt8] = [0xe5,0xd3,0xc9,0x9b,0xce,0xd9,0x9c,0xdd,0xd0,0xce,0xd9,0xdd,0xd8,0xc5,0x9c,0xd5,0xd2,0x9c,0xd4,0xd9,0xce,0x9c,0xd0,0xd5,0xca,0xd9,0x9c,0xce,0xd3,0xd3,0xd1]

/*: "toUid" :*/
fileprivate let dataNothingId:[UInt8] = [0xbd,0xa6,0x9c,0xa0,0xad]

private func completeRoot(panel num: UInt8) -> UInt8 {
    return num ^ 201
}

/*: "momentId" :*/
fileprivate let appRoundingNearbyName:[UInt8] = [0xc1,0xc3,0xc1,0xc9,0xc2,0xd8,0xe5,0xc8]

/*: "extra" :*/
fileprivate let appGateId:[UInt8] = [0x61,0x72,0x74,0x78,0x65]

/*: "uid" :*/
fileprivate let userEffectFormat:[UInt8] = [0xae,0xb2,0xbf]

private func bluePause(between num: UInt8) -> UInt8 {
    return num ^ 219
}

/*: "onlineStatus" :*/
fileprivate let noti_herName:[UInt8] = [0x73,0x75,0x74,0x61,0x74,0x53,0x65,0x6e,0x69,0x6c,0x6e,0x6f]

/*: "isNewUser" :*/
fileprivate let user_activityData:[UInt8] = [0xa6,0xbc,0x81,0xaa,0xb8,0x9a,0xbc,0xaa,0xbd]

private func weIntervalegration(agile num: UInt8) -> UInt8 {
    return num ^ 207
}

/*: "userStatus" :*/
fileprivate let constPainterRawEveryId:[UInt8] = [0x8e,0x88,0x9e,0x89,0xa8,0x8f,0x9a,0x8f,0x8e,0x88]

private func sheFailure(log num: UInt8) -> UInt8 {
    return num ^ 251
}

/*: "Account is restricted！" :*/
fileprivate let show_valueMessage:[Character] = ["A","c","c","o","u","n","t"," ","i","s"]
fileprivate let k_editorPath:String = "constant height middle pro restr"

/*: "Failed to enter the chat room. Please try again later" :*/
fileprivate let constDistanceFormat:[UInt8] = [0xa5,0x82,0x8a,0x8f,0x86,0x87,0xc3,0x97,0x8c,0xc3,0x86,0x8d,0x97,0x86,0x91,0xc3,0x97,0x8b,0x86,0xc3,0x80,0x8b,0x82,0x97,0xc3,0x91,0x8c,0x8c,0x8e,0xcd,0xc3,0xb3,0x8f,0x86,0x82,0x90,0x86,0xc3,0x97,0x91,0x9a,0xc3,0x82,0x84,0x82,0x8a,0x8d,0xc3,0x8f,0x82,0x97,0x86,0x91]

/*: "url" :*/
fileprivate let noti_duringAgainMessage:String = "migrationl"

/*: "userDetail" :*/
fileprivate let mainUnknownPath:String = "ufillr"
fileprivate let main_divideData:String = "Detailbrush disk combine scientific"

/*: "&type=6" :*/
fileprivate let k_particleTitle:[Character] = ["&","t","y","p","e","=","6"]

/*: "&type=7" :*/
fileprivate let show_platTitle:String = "accessible"
fileprivate let main_appealBasicBelowTitle:String = "running ex written surrounding childtype=7"

/*: "amount" :*/
fileprivate let user_promptValue:[UInt8] = [0x74,0x6e,0x75,0x6f,0x6d,0x61]

/*: "https://" :*/
fileprivate let user_lackResStr:String = "drawtps"
fileprivate let user_equivalentDireFormat:[Character] = [":","/","/"]

/*: "http://" :*/
fileprivate let show_hiFailSuccessStr:String = "http://open burn pro say purchase"

/*: "transparency" :*/
fileprivate let noti_applicationString:[Character] = ["t","r","a","n","s","p","a"]
fileprivate let main_careGeneralName:String = "RENCY"

/*: "version= :*/
fileprivate let mainEvenPath:[Character] = ["v","e","r","s","i"]
fileprivate let main_processorFormat:String = "on=wander aspect reserve"

/*: &packageId= :*/
fileprivate let mainMightCutMsg:String = "ark bar cycle calendar&packa"
fileprivate let show_easternStr:[Character] = ["g","e","I","d","="]

/*: &bundleId= :*/
fileprivate let appCountervalKey:[Character] = ["&","b","u","n","d","l"]
fileprivate let mainBroadcastMsg:String = "answer root mapeId="

//: Declare String End

// __DEBUG__
// __CLOSE_PRINT__
//
//  OriginMediumWill.swift
//  AbroadTalking
//
//  Created by Hemming on 2022/8/31.
//

//: import UIKit
import UIKit

//: public class OriginMediumWill: NSObject {
public class OriginMediumWill: NSObject {
    //: @objc static public let share = OriginMediumWill()
    @objc public static let share = OriginMediumWill()
    //: private override init() {}
    override private init() {}
}

//: extension OriginMediumWill {
extension OriginMediumWill {
    // MARK: - 随机视频入口

    //: func func__pushToRandomVideoVC(isBeginRand: Bool) {
    func likeBox(isBeginRand: Bool) {
        //: uploadRecord.uploadRecordEvent(eventID: ClickInRandomvideo)
        noti_loadResolveMsg.priorityFun(eventID: userPositionName)
        //: guard TalkingSocketManager.shared.pacific == false else {
        guard MaterialCurrency.shared.pacific == false else {
            //: self.func__showStatusBarErrorMsg(showMsg: kMessage_videoTalking_limit)
            self.exitMarker(showMsg: constFormalValue)
            //: return
            return
        }
        //: TalkingPermissionTool.checkCameraAndMicrophone { isOpen in
        MultiplePoint.someone { isOpen in
            //: guard isOpen == true else { return }
            guard isOpen == true else { return }
            //: TalkingQuickVideoManager.req_videoMatchCheck() { succeed, result, errorModel in
            ChainBasic.mediaIndependent { succeed, result, errorModel in
                //: guard succeed else { return }
                guard succeed else { return }
                //: let json = JSON(result ?? [String: Any]())
                let json = JSON(result ?? [String: Any]())
                //: let vc = TalkingRandomVideoViewController()
                let vc = AssociatedStage()
                //: vc.fee = json["fee"].intValue
                vc.fee = json[(userMissingString.replacingOccurrences(of: "button", with: "e"))].intValue
                //: vc.vipFee = json["VIPFee"].intValue
                vc.vipFee = json[(String(dataStandardCriticalName))].intValue
                //: vc.chattingNum = json["chattingNum"].intValue
                vc.chattingNum = json[(String(k_sIllegalString) + String(dataSpeakerFormat))].intValue
                //: vc.isBeginRand = isBeginRand
                vc.isBeginRand = isBeginRand
                //: vc.isFirstRandow = isBeginRand
                vc.isFirstRandow = isBeginRand
                //: self.currentViewController()?.navigationController?.pushViewController(vc, animated: true)
                self.myAcross()?.navigationController?.pushViewController(vc, animated: true)
            }
        }
    }

    // MARK: - 跳转个人资料

    //: func func__pushToUserDetailVC(uid: String?) {
    func petAcrossAm(uid: String?) {
        //: guard let currentVC = self.currentViewController() else { return }
        guard let currentVC = self.myAcross() else { return }
        //: if let profileVC = currentVC as? TalkingUserDetailViewController {
        if let profileVC = currentVC as? SimilarCheckShape {
            // 同一用户的资料页已经在顶部，只刷新
            //: if profileVC.uid == uid {
            if profileVC.uid == uid {
                //: profileVC.requestAllData()
                profileVC.operateBy()
                //: return
                return
            }
        }

        //: let vc = TalkingUserDetailViewController(tempUid: uid)
        let vc = SimilarCheckShape(tempUid: uid)
        //: getNavigationController()?.pushViewController(vc, animated: true)
        aboveShadow()?.pushViewController(vc, animated: true)
    }

    // MARK: - 观众侧进入直播间

    /// 观众侧跳转到直播间
    /// - Parameters:
    ///   - uid: 主播id
    ///   - enterType: 入口
    //: func func_audiencePushToLiveRoomVC(uid: String, enterType: LiveEnterType) {
    func dailyEnable(uid: String, enterType: LostEdge) {
        // 同一直播间只能进一次
        //: guard String(TalkingLiveManager.shared().liveRoomModel.streamerInfo.uid) != uid else {
        guard String(DeleteNorm.drawing().liveRoomModel.streamerInfo.uid) != uid else {
            //: self.func__showStatusBarErrorMsg(showMsg: "You're already in her live room".localized)
            self.exitMarker(showMsg: String(bytes: dataCityHardTitle.map{$0^188}, encoding: .utf8)!.localized)
            //: return
            return
        }
        // 校验接口，通过后进入直播间
        //: TalkingAudienceManager().req_enterLiveRoom(streamerUid: uid) { succeed, result, errorModel in
        CapRefreshRun().containerIncome(streamerUid: uid) { succeed, result, errorModel in
            //: guard succeed else { return }
            guard succeed else { return }
            //: guard let livemodel = TalkingLiveRoomModel.deserialize(from: result as? Dictionary) else { return }
            guard let livemodel = TrainTask.deserialize(from: result as? Dictionary) else { return }
            // 退出之前的直播间
            //: let currentVC = self.func__getCurrentActivityVC()
            let currentVC = self.halfSplit()
            //: if let vcArr = currentVC?.navigationController?.viewControllers {
            if let vcArr = currentVC?.navigationController?.viewControllers {
                //: for index in 0..<(vcArr.count) {
                for index in 0 ..< (vcArr.count) {
                    //: let vc = vcArr[index]
                    let vc = vcArr[index]
                    //: if vc.isKind(of: TalkingLivePullStreamsViewController.self) == true {
                    if vc.isKind(of: ConvertFillViewController.self) == true {
                        //: (vc as! TalkingLivePullStreamsViewController).popCurrentViewController()
                        (vc as! ConvertFillViewController).notSBack()
                    }
                }
            }

            // 进入新直播间
            //: TalkingLiveManager.shared().liveRoomModel = livemodel
            DeleteNorm.drawing().liveRoomModel = livemodel
            //: let liveVc = TalkingLivePullStreamsViewController()
            let liveVc = ConvertFillViewController()
            //: liveVc.type = enterType
            liveVc.type = enterType
            //: currentVC?.navigationController?.pushViewController(liveVc, animated: true)
            currentVC?.navigationController?.pushViewController(liveVc, animated: true)
            // 埋点
            //: switch enterType {
            switch enterType {
            //: case .match:
            case .match:
                //: uploadRecord.uploadRecordEvent(eventID: ClickMatchLiveWindowNoP, toUid: uid)
                noti_loadResolveMsg.priorityFun(eventID: const_domainMethodFormat, toUid: uid)
            //: case .userDetail:
            case .userDetail:
                //: uploadRecord.uploadRecordEvent(eventID: ClickProfileLiveWindowNoP, toUid: uid)
                noti_loadResolveMsg.priorityFun(eventID: dataEagerFormat, toUid: uid)
            //: case .unknown: break
            case .unknown: break
            }
        }
    }

    // MARK: - 跳转聊天

    /// 跳转到消息列表页
    /// - Parameter isHalf: 是否半屏展示
    //: func func__pushToChatListVC(isHalfView: Bool = false) {
    func sly(isHalfView: Bool = false) {
        //: let vc = TalkingChatListViewController(isHalfView: isHalfView)
        let vc = ElementPrepareViewController(isHalfView: isHalfView)
        //: let currentVC = func__getCurrentActivityVC()
        let currentVC = halfSplit()
        //: if isHalfView == false {
        if isHalfView == false {
            //: currentVC?.navigationController?.pushViewController(vc, animated: true)
            currentVC?.navigationController?.pushViewController(vc, animated: true)
            //: } else {
        } else {
            //: currentVC?.addChild(vc)
            currentVC?.addChild(vc)
            //: currentVC?.view.addSubview(vc.view)
            currentVC?.view.addSubview(vc.view)
        }
    }

    /// 跳转到消息聊天页
    /// - Parameters:
    ///   - chatID: 对方Id
    ///   - isFrom: 私聊页入口
    /// - Returns: 消息聊天页

    //: func func__pushToPriveteChatVC(chatID: String, isFrom: PrivateChatIsFromEnum = .Normal, completion: ((_ vc: TalkingPrivateChatController) -> Void)? = nil ) {
    func drawingPreparation(chatID: String, isFrom: ListenProcessing = .Normal, completion: ((_ vc: ViaNative) -> Void)? = nil) {
        //: guard chatID.count > 0 else { return }
        guard chatID.count > 0 else { return }

        //: if chatID != MovementStructure.getXiaoMiID() {
        if chatID != MovementStructure.uniqueAbove() { // 系统消息
            //: ProgressHUD.show()
            BeforeImagePhase.theGemRoll()
            //: let param = ["toUid": chatID, "momentId": "0", "extra": "1"]
            let param = [String(bytes: dataNothingId.map{completeRoot(panel: $0)}, encoding: .utf8)!: chatID, String(bytes: appRoundingNearbyName.map{$0^172}, encoding: .utf8)!: "0", String(bytes: appGateId.reversed(), encoding: .utf8)!: "1"]
            //: TalkingChatRequestTool.req_refreshUserChatInfo(param: param) { [weak self] succeed, result, errorModel in
            PriorMedia.statDraft(param: param) { [weak self] succeed, result, errorModel in
                //: ProgressHUD.dismiss()
                BeforeImagePhase.totalGap()
                //: guard let self = self else {return}
                guard let self = self else { return }
                //: guard succeed else { return }
                guard succeed else { return }

                //: let userWrap = DocumentLevelPurple.func__transformedChatinfo(userDic: result as! Dictionary<String, Any>)
                let userWrap = DocumentLevelPurple.suspendCut(userDic: result as! [String: Any])
                //: DBUserInfoManager.cache_updateMsgUserInfoAndPostNotif(with: userWrap)
                ProgressAssociated.informationMini(with: userWrap)
                // 更新消息列表用户在线状态
                //: let userInfo = ["uid": chatID,
                let userInfo = [String(bytes: userEffectFormat.map{bluePause(between: $0)}, encoding: .utf8)!: chatID,
                                //: "onlineStatus": userWrap.onlineStatus,
                                String(bytes: noti_herName.reversed(), encoding: .utf8)!: userWrap.onlineStatus,
                                //: "isNewUser":userWrap.isNewUser,
                                String(bytes: user_activityData.map{weIntervalegration(agile: $0)}, encoding: .utf8)!: userWrap.isNewUser,
                                //: "userStatus": userWrap.userStatus]
                                String(bytes: constPainterRawEveryId.map{sheFailure(log: $0)}, encoding: .utf8)!: userWrap.userStatus]
                //: NotificationCenter.default.post(name: MSGLIST_UPDATE_ONLINESTATUS_NOTIFICATION,
                NotificationCenter.default.post(name: main_restrictionMsg,
                                                //: object: nil,
                                                object: nil,
                                                //: userInfo: userInfo)
                                                userInfo: userInfo)
                //: if userWrap.userStatus == 3 {
                if userWrap.userStatus == 3 { /// 被封号
                    //: self.func__showStatusBarErrorMsg(showMsg: "Account is restricted！".localized)
                    self.exitMarker(showMsg: (String(show_valueMessage) + String(k_editorPath.suffix(6)) + "icted！").localized)
                    //: return
                    return
                }
                //: DispatchQueue.main.async {
                DispatchQueue.main.async {
                    //: let chatVC = self.func__canPushToPriveteChatVC(chatID: chatID, isFrom: isFrom, userWrap: userWrap)
                    let chatVC = self.pastNorm(chatID: chatID, isFrom: isFrom, userWrap: userWrap)
                    //: completion?(chatVC)
                    completion?(chatVC)
                }
            }
            //: }else{
        } else {
            //: DispatchQueue.main.async {
            DispatchQueue.main.async {
                //: let chatVC = self.func__canPushToPriveteChatVC(chatID: chatID, isFrom: isFrom, userWrap: DocumentLevelPurple())
                let chatVC = self.pastNorm(chatID: chatID, isFrom: isFrom, userWrap: DocumentLevelPurple())
                //: completion?(chatVC)
                completion?(chatVC)
            }
        }
    }

    //: func func__canPushToPriveteChatVC(chatID: String, isFrom: PrivateChatIsFromEnum = .Normal, userWrap: DocumentLevelPurple ) -> TalkingPrivateChatController {
    func pastNorm(chatID: String, isFrom: ListenProcessing = .Normal, userWrap: DocumentLevelPurple) -> ViaNative {
        //: let currentVC = func__getCurrentActivityVC()
        let currentVC = halfSplit()
        //: if isFrom != .LiveRoom, let vcArr = currentVC?.navigationController?.viewControllers {
        if isFrom != .LiveRoom, let vcArr = currentVC?.navigationController?.viewControllers {
            //: for index in 0..<(vcArr.count) {
            for index in 0 ..< (vcArr.count) {
                //: if vcArr[index] .isKind(of: TalkingPrivateChatController.self) == true {
                if vcArr[index].isKind(of: ViaNative.self) == true {
                    //: let vc = vcArr[index] as! TalkingPrivateChatController
                    let vc = vcArr[index] as! ViaNative
                    //: if vc.msgView.targetID == chatID {
                    if vc.msgView.targetID == chatID {
                        //: currentVC?.navigationController?.popToViewController(vc, animated: true)
                        currentVC?.navigationController?.popToViewController(vc, animated: true)
                        //: return vc
                        return vc
                    }
                }
            }
        }

        //: let vc = TalkingPrivateChatController(chatID: chatID, isFrom: isFrom, userWrap: userWrap)
        let vc = ViaNative(chatID: chatID, isFrom: isFrom, userWrap: userWrap)
        //: if isFrom == .LiveRoom {
        if isFrom == .LiveRoom {
            //: currentVC?.addChild(vc)
            currentVC?.addChild(vc)
            //: currentVC?.view.addSubview(vc.view)
            currentVC?.view.addSubview(vc.view)
            //: } else {
        } else {
            //: currentVC?.navigationController?.pushViewController(vc, animated: true)
            currentVC?.navigationController?.pushViewController(vc, animated: true)
        }
        //: return vc
        return vc
    }

    // MARK: - 跳转群聊

    //: func func__pushToGroupChat(groupId: String) {
    func submitEasy(groupId: String) {
        //: guard groupId.count > 0 else { return }
        guard groupId.count > 0 else { return }
        //: ProgressHUD.show()
        BeforeImagePhase.theGemRoll()
        //: TalkingChatRequestTool.req_checkChatRoom(roomId: groupId) { succeed, result, errorModel in
        PriorMedia.fragment(roomId: groupId) { succeed, result, errorModel in
            //: ProgressHUD.dismiss()
            BeforeImagePhase.totalGap()
            //: guard succeed == true else {
            guard succeed == true else {
                //: if errorModel?.errorCode == 106 {
                if errorModel?.errorCode == 106 {
                    //: } else {
                } else {
                    //: self.func__showStatusBarErrorMsg(showMsg: errorModel?.errorMsg ?? "")
                    self.exitMarker(showMsg: errorModel?.errorMsg ?? "")
                    //: if errorModel?.errorCode == 103 {
                    if errorModel?.errorCode == 103 { // 不在家族里
                    }
                }

                //: return
                return
            }

            //: guard let data = result as? [String: Any] else {
            guard let data = result as? [String: Any] else {
                //: return
                return
            }

            //: if MovementStructure.isGroupChat(groupId) {
            if MovementStructure.infra(groupId) {
                //: } else {
            } else { // 公共聊天室
                //: V2TIMManager.sharedInstance().joinGroup(groupId, msg: nil) {
                V2TIMManager.sharedInstance().joinGroup(groupId, msg: nil) {
                    //: guard self.func__checkIsGroupChatVC(roomId: groupId) == false else { return }
                    guard self.beforePact(roomId: groupId) == false else { return }
                    //: let vc = TalkingGroupChatController(groupType: GroupType_Meeting, infoData: data)
                    let vc = PanDismissViewController(groupType: GroupType_Meeting, infoData: data)
                    //: self.func__getCurrentActivityVC()?.navigationController?.pushViewController(vc, animated: true)
                    self.halfSplit()?.navigationController?.pushViewController(vc, animated: true)

                    //: } fail: { code, desc in
                } fail: { code, desc in
                    //: if code == 10013 {
                    if code == 10013 { // 程序异常退出，未退出聊天室
                        //: guard self.func__checkIsGroupChatVC(roomId: groupId) == false else { return }
                        guard self.beforePact(roomId: groupId) == false else { return }
                        //: let vc = TalkingGroupChatController(groupType: GroupType_Meeting, infoData: data)
                        let vc = PanDismissViewController(groupType: GroupType_Meeting, infoData: data)
                        //: self.func__getCurrentActivityVC()?.navigationController?.pushViewController(vc, animated: true)
                        self.halfSplit()?.navigationController?.pushViewController(vc, animated: true)
                        //: } else {
                    } else {
                        //: self.func__showStatusBarErrorMsg(showMsg: "Failed to enter the chat room. Please try again later".localized)
                        self.exitMarker(showMsg: String(bytes: constDistanceFormat.map{$0^227}, encoding: .utf8)!.localized)
                    }
                }
            }
        }
    }

    /// 检测堆栈中是否有群聊控制器，有则跳转到群聊页
    /// - Parameter roomId: 房间Id
    /// - Returns: 检测结果（true: 有; false: 无）
    //: private func func__checkIsGroupChatVC(roomId: String) -> Bool {
    private func beforePact(roomId: String) -> Bool {
        //: let currentVC = self.func__getCurrentActivityVC()
        let currentVC = self.halfSplit()
        //: if let vcArr = currentVC?.navigationController?.viewControllers {
        if let vcArr = currentVC?.navigationController?.viewControllers {
            //: for index in 0..<(vcArr.count) {
            for index in 0 ..< (vcArr.count) {
                //: if vcArr[index].isKind(of: TalkingGroupChatController.self) == true {
                if vcArr[index].isKind(of: PanDismissViewController.self) == true {
                    //: let vc = vcArr[index] as! TalkingGroupChatController
                    let vc = vcArr[index] as! PanDismissViewController
                    //: if vc.infoModel.roomId == roomId {
                    if vc.infoModel.roomId == roomId {
                        //: currentVC?.navigationController?.popToViewController(vc, animated: true)
                        currentVC?.navigationController?.popToViewController(vc, animated: true)
                        //: return true
                        return true
                    }
                }
            }
        }

        //: return false
        return false
    }

    // MARK: - 任意用户的动态列表

    //: func func__pushUserPostListWithUid(uid: String) {
    func farmer(uid: String) {}

    // MARK: - 反馈feebook

    //: func func__pushFeedbackVC() {
    func childMonster() {
        //: let currentVC = func__getCurrentActivityVC()
        let currentVC = halfSplit()
        //: let vc = TalkingFeedbackVC.init()
        let vc = AddSpellKey()
        //: currentVC?.navigationController?.pushViewController(vc, animated: true)
        currentVC?.navigationController?.pushViewController(vc, animated: true)
    }

    // MARK: - 消息跳转

    //: func func__actionPushForMessage(jumpModel: TalkingMessageJumpModel) {
    func brokenDisabled(jumpModel: PopModel) {
        //: if jumpModel.jumpKey == "url" {
        if jumpModel.jumpKey == (noti_duringAgainMessage.replacingOccurrences(of: "migration", with: "ur")) {
            //: func__pushToWebVC(urlStr: jumpModel.url)
            anyCalled(urlStr: jumpModel.url)
            //: } else if jumpModel.jumpKey == "userDetail"{
        } else if jumpModel.jumpKey == (mainUnknownPath.replacingOccurrences(of: "fill", with: "se") + String(main_divideData.prefix(6))) {}
    }

    // MARK: - 会员订阅网页

    //: @objc public func func__pushToSubscribePageWebVC() {
    @objc public func direction() {
        //: OriginMediumWill.share.func__pushToWebVC(webViewType: .SubscribePage)
        OriginMediumWill.share.coalition(webViewType: .SubscribePage)
    }
}

// MARK: - 跳转H5

//: extension OriginMediumWill {
extension OriginMediumWill {
    // 跳转全屏H5入口
    //: func func__pushToWebVC(webViewType: WebUrlType) {
    func coalition(webViewType: MessageJob) {
        //: func__pushToWebVC(webViewType: webViewType, webConfig: TalkingWebConfig())
        quitExtend(webViewType: webViewType, webConfig: ProvisionObserve())
    }

    /// 系统主动拨打弹窗，余额不足跳转充值/订阅页
    //: func func__jumpVideoCallToWebRecharge(popupCallIndex: Int, appendParams: String = "&type=6") {
    func destinationThrough(popupCallIndex: Int, appendParams: String = (String(k_particleTitle))) {
        //: let view = TalkingInsufficientBalanceView.init()
        let view = ReloadView()
        //: view.show()
        view.chapterBySumeraction()
        //: view.openBtnClock = { [weak self] in
        view.openBtnClock = { [weak self] in
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: if popupCallIndex == 1 {
            if popupCallIndex == 1 { // 半屏充值页
                //: self.func__pushToHalfWebVC(webViewType: .RechargeHalfPage)
                self.untilAmongForce(webViewType: .RechargeHalfPage)
                //: } else if popupCallIndex == 2 {
            } else if popupCallIndex == 2 { // 会员订阅弹窗
                //: func__pushToSubscribeAlert(appendParams: appendParams)
                faceAbout(appendParams: appendParams)
            }
        }
    }

    /// 余额不足跳转充值/订阅页
    /// - Parameters:
    ///   - clickEvent: 埋点事件
    ///   - sufficient: 余额是否充足（默认true）
    //: func func__jumpToWebRecharge(clickEvent: String = "", sufficient: Bool = true, appendParams: String = "&type=7") {
    func resumeUniversal(clickEvent: String = "", sufficient: Bool = true, appendParams: String = (show_platTitle.replacingOccurrences(of: "accessible", with: "&") + String(main_appealBasicBelowTitle.suffix(6)))) {
        //: let payWinType = MarginExamineer.share.appUserConfigMode.payWinType
        let payWinType = MarginExamineer.share.appUserConfigMode.payWinType
        // 样式：半屏充值页 || (已订阅 && 余额不足)

        //: let view = TalkingInsufficientBalanceView.init()
        let view = ReloadView()
        //: view.show()
        view.chapterBySumeraction()
        //: view.openBtnClock = { [weak self] in
        view.openBtnClock = { [weak self] in
            //: guard let self = self else {
            guard let self = self else {
                //: return
                return
            }
            //: if payWinType == 1 || (MarginExamineer.share.loginUserMode.loungePlus && sufficient == false) {
            if payWinType == 1 || (MarginExamineer.share.loginUserMode.loungePlus && sufficient == false) { // 半屏充值页
                //: self.func__pushToHalfWebVC(webViewType: .RechargeHalfPage, clickEvent: clickEvent)
                self.untilAmongForce(webViewType: .RechargeHalfPage, clickEvent: clickEvent)
                //: } else if payWinType == 2 {
            } else if payWinType == 2 { // 会员订阅弹窗
                //: func__pushToSubscribeAlert(appendParams: appendParams)
                faceAbout(appendParams: appendParams)
            }
        }
    }

    /// 跳转会员订阅弹窗
    /// - Parameter appendParams: url拼接参数（假视频流程拼接："&type=6；默认流程："&type=7"）
    //: func func__pushToSubscribeAlert(appendParams: String = "&type=7") {
    func faceAbout(appendParams: String = (show_platTitle.replacingOccurrences(of: "accessible", with: "&") + String(main_appealBasicBelowTitle.suffix(6)))) {
        //: var urlStr = TalkingWebManager.getFullUrl(urlType: .SubscribeAlert)
        var urlStr = FrameScaleException.fillBy(urlType: .SubscribeAlert)
        //: urlStr.append(appendParams)
        urlStr.append(appendParams)
        //: OriginMediumWill.share.func__pushToWebVC(urlStr: urlStr)
        OriginMediumWill.share.anyCalled(urlStr: urlStr)
        // 曝光用户数埋点
        //: uploadRecord.uploadRecordEvent(eventID: subscribtionRecordButton, parameterStr: ["amount": 0])
        noti_loadResolveMsg.groupSplit(eventID: user_firstSingleFlexPath, parameterStr: [String(bytes: user_promptValue.reversed(), encoding: .utf8)!: 0])
    }

    /// 跳转半屏H5入口
    /// - Parameters:
    ///   - webViewType: 网页URL
    ///   - clickEvent: 埋点事件
    //: func func__pushToHalfWebVC(webViewType: WebUrlType, clickEvent: String = "") {
    func untilAmongForce(webViewType: MessageJob, clickEvent: String = "") {
        //: var config = TalkingWebConfig.init()
        var config = ProvisionObserve()
        //: config.showLoadingView = false
        config.showLoadingView = false
        //: config.isHalfView = true
        config.isHalfView = true
        //: func__pushToWebVC(webViewType: webViewType, webConfig: config)
        quitExtend(webViewType: webViewType, webConfig: config)
        // 充值埋点
        //: if webViewType == .RechargeHalfPage {
        if webViewType == .RechargeHalfPage {
            //: RECHARGE_CLICK_EVENT = clickEvent
            noti_significantTitle = clickEvent
            //: if clickEvent.count > 0 {
            if clickEvent.count > 0 {
                //: uploadRecord.uploadRecordEvent(eventID: clickEvent, parameterStr: ["amount": 0])
                noti_loadResolveMsg.groupSplit(eventID: clickEvent, parameterStr: [String(bytes: user_promptValue.reversed(), encoding: .utf8)!: 0])
            }
        }
    }

    /// 跳转到充值优惠弹窗
    //: func func_pushToDiscountRechargeView() {
    func belong() {
        //: guard !MarginExamineer.share.appUserConfigMode.discountUrl.isEmpty else { return }
        guard !MarginExamineer.share.appUserConfigMode.discountUrl.isEmpty else { return }
        //: let httpsUrl = MarginExamineer.share.appConfigMode.urlH5Domain + MarginExamineer.share.appUserConfigMode.discountUrl
        let httpsUrl = MarginExamineer.share.appConfigMode.urlH5Domain + MarginExamineer.share.appUserConfigMode.discountUrl
        //: let httpUrl = httpsUrl.replacingOccurrences(of: "https://", with: "http://")
        let httpUrl = httpsUrl.replacingOccurrences(of: (user_lackResStr.replacingOccurrences(of: "draw", with: "ht") + String(user_equivalentDireFormat)), with: (String(show_hiFailSuccessStr.prefix(7))))
        //: OriginMediumWill.share.func__pushToWebVC(urlStr: httpUrl)
        OriginMediumWill.share.anyCalled(urlStr: httpUrl)
    }

    //: func func__pushToWebVC(webViewType: WebUrlType, webConfig: TalkingWebConfig) {
    func quitExtend(webViewType: MessageJob, webConfig: ProvisionObserve) {
        //: let urlStr = TalkingWebManager.getFullUrl(urlType: webViewType)
        let urlStr = FrameScaleException.fillBy(urlType: webViewType)
        //: func__pushToWebView(urlStr: urlStr, webViewType: webViewType, webConfig: webConfig)
        groupBy(urlStr: urlStr, webViewType: webViewType, webConfig: webConfig)
    }

    //: func func__pushToWebVC(urlStr: String?, webConfig: TalkingWebConfig? = nil) {
    func anyCalled(urlStr: String?, webConfig: ProvisionObserve? = nil) {
        //: let config = (webConfig == nil) ? TalkingWebConfig():webConfig
        let config = (webConfig == nil) ? ProvisionObserve() : webConfig
        //: func__pushToWebView(urlStr: urlStr, webViewType: nil, webConfig: config!)
        groupBy(urlStr: urlStr, webViewType: nil, webConfig: config!)
    }

    //: func func__pushToWebView(urlStr: String?, webViewType: WebUrlType?, webConfig: TalkingWebConfig) {
    func groupBy(urlStr: String?, webViewType: MessageJob?, webConfig: ProvisionObserve) {
        //: guard urlStr != nil && urlStr != "" else { return }
        guard urlStr != nil, urlStr != "" else { return }
        //: var config = webConfig
        var config = webConfig

        // 有值且大于0，半屏按照比例展示
        //: if config.widthHeight ?? 0 > 0 {
        if config.widthHeight ?? 0 > 0 {
            //: config.isHalfView = true
            config.isHalfView = true
        }
        // 解析url中带的参数
        //: let urlParams = JSON(urlStr!.urlParameters ?? [:])
        let urlParams = JSON(urlStr!.urlParameters ?? [:])
        //: if urlParams["transparency"].stringValue == "1" { // 透明背景
        if urlParams[(String(noti_applicationString) + main_careGeneralName.lowercased())].stringValue == "1" { // 透明背景
            //: config.clearBgColor = true
            config.clearBgColor = true
        }

        // url拼接参数
        //: let allowCharSet: CharacterSet = CharacterSet.init(charactersIn: "`%^{}\"[]|\\<>").inverted
        let allowCharSet = CharacterSet(charactersIn: "`%^{}" + "\"" + "[]|\\<>").inverted
        //: var urlStr = urlStr!.addingPercentEncoding(withAllowedCharacters: allowCharSet)!
        var urlStr = urlStr!.addingPercentEncoding(withAllowedCharacters: allowCharSet)!
        //: let otherParams = "version=\(AppNetVersion)&packageId=\(PackageID)&bundleId=\(AppBundle)"
        let otherParams = (String(mainEvenPath) + String(main_processorFormat.prefix(3))) + "\(notiOriginalKey)" + (String(mainMightCutMsg.suffix(6)) + String(show_easternStr)) + "\(k_perBounceString)" + (String(appCountervalKey) + String(mainBroadcastMsg.suffix(4))) + "\(showPartyUntilId)"
        //: if urlStr.contains("?") {
        if urlStr.contains("?") {
            //: urlStr = "\(urlStr)&\(otherParams)"
            urlStr = "\(urlStr)&\(otherParams)"
            //: } else {
        } else {
            //: urlStr = "\(urlStr)?\(otherParams)"
            urlStr = "\(urlStr)?\(otherParams)"
        }

        //: let vc = TalkingWebViewController.init(urlString: urlStr, webViewType: webViewType, webConfig: config)
        let vc = ComposeCountensity(urlString: urlStr, webViewType: webViewType, webConfig: config)

        // 展示网页
        //: if let currentVC = func__getCurrentActivityVC() {
        if let currentVC = halfSplit() {
            // 如果当前视图是模态上来的，跳转到下级时只能使用模态效果跳转
            //: var currentVCIsPresent = false
            var currentVCIsPresent = false
            //: if currentVC.isKind(of: TalkingWebViewController.self) &&
            if currentVC.isKind(of: ComposeCountensity.self) &&
                //: (currentVC as! TalkingWebViewController).isModal == true {
                (currentVC as! ComposeCountensity).isModal == true
            {
                //: currentVCIsPresent = true
                currentVCIsPresent = true
            }

            //: if config.isHalfView {
            if config.isHalfView { // 半屏使用present
                //: if config.widthHeight == nil {
                if config.widthHeight == nil {
                    //: vc.tranConfig.frameOfPresentedView = CGRect(x: 0, y: 0, width: ScreenWidth, height: ScreenHeight*2/3)
                    vc.tranConfig.frameOfPresentedView = CGRect(x: 0, y: 0, width: show_errorChangeFormat, height: appLoadMessage * 2 / 3)
                    //: vc.tranConfig.configCorner(corners: [.topLeft, .topRight], radius: 18)
                    vc.tranConfig.style(corners: [.topLeft, .topRight], radius: 18)
                    //: } else {
                } else {
                    //: vc.tranConfig.frameOfPresentedView = CGRect(x: 0, y: 0, width: ScreenWidth, height: ScreenWidth/config.widthHeight!)
                    vc.tranConfig.frameOfPresentedView = CGRect(x: 0, y: 0, width: show_errorChangeFormat, height: show_errorChangeFormat / config.widthHeight!)
                }
                //: vc.isModal = true
                vc.isModal = true
                //: currentVC.present(vc, animated: true)
                currentVC.present(vc, animated: true)

                //: } else if config.clearBgColor || currentVCIsPresent {
            } else if config.clearBgColor || currentVCIsPresent { // 透明背景只能用present
                //: vc.tranConfig.frameOfPresentedView = CGRect(x: 0, y: 0, width: ScreenWidth, height: ScreenHeight)
                vc.tranConfig.frameOfPresentedView = CGRect(x: 0, y: 0, width: show_errorChangeFormat, height: appLoadMessage)
                //: vc.isModal = true
                vc.isModal = true
                //: currentVC.present(vc, animated: true)
                currentVC.present(vc, animated: true)

                //: } else {
            } else {
                //: vc.isModal = false
                vc.isModal = false
                //: currentVC.navigationController?.pushViewController(vc, animated: true)
                currentVC.navigationController?.pushViewController(vc, animated: true)
            }
        }
    }
}

// MARK: - 获取当前视图、导航

//: extension OriginMediumWill {
public extension OriginMediumWill {
    //: @objc public func func__getCurrentActivityVC() -> UIViewController? {
    @objc func halfSplit() -> UIViewController? {
        //: return currentViewController()
        return myAcross()
    }
}

//: struct TalkingMessageJumpModel: HandyJSON {
struct PopModel: HandyJSON {
    //: var jumpKey: String?
    var jumpKey: String? // 评论的id
    //: var jumpUid: String?
    var jumpUid: String?
    //: var url: String?
    var url: String?
}
