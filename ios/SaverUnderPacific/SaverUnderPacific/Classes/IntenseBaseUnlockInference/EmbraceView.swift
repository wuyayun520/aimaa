
//: Declare String Begin

/*: "formatType" :*/
fileprivate let data_operationId:String = "formisolate"

/*: "plain" :*/
fileprivate let dataCosyId:String = "plaibring"

/*: "init(coder:) has not been implemented" :*/
fileprivate let dataGraphPath:[UInt8] = [0x70,0x77,0x70,0x6d,0x31,0x7a,0x76,0x7d,0x7c,0x6b,0x23,0x30,0x39,0x71,0x78,0x6a,0x39,0x77,0x76,0x6d,0x39,0x7b,0x7c,0x7c,0x77,0x39,0x70,0x74,0x69,0x75,0x7c,0x74,0x7c,0x77,0x6d,0x7c,0x7d]

private func infraPackage(accelerate num: UInt8) -> UInt8 {
    return num ^ 25
}

/*: "#FF2348" :*/
fileprivate let mainHunkChainMessage:[Character] = ["#","F","F","2","3","4","8"]

/*: "icon_lounge" :*/
fileprivate let kParticipationMsg:String = "professional lock underly exceptionicon_l"

/*: "icon_home_v" :*/
fileprivate let user_boyMsg:[Character] = ["i","c","o","n","_","h","o","m"]
fileprivate let k_independentTitle:[Character] = ["e","_","v"]

/*: "icon_chat_list_new" :*/
fileprivate let mainCareKey:String = "topic combine status receive waiticon_c"
fileprivate let main_unknownMarkActionMsg:[Character] = ["i","s"]
fileprivate let userMUrl:String = "resume complaint cosy errt_new"

/*: "Official" :*/
fileprivate let app_extraString:String = "level evaluation color raw childOffi"

/*: "#30D00B" :*/
fileprivate let main_viewName:[Character] = ["#","3","0","D","0","0","B"]

/*: "iv_message_honey" :*/
fileprivate let notiLatName:[Character] = ["i","v","_","m","e","s","s","a","g"]
fileprivate let app_rollFeatureMsg:[Character] = ["e","_","h","o","n","e","y"]

/*: "#FF60B9" :*/
fileprivate let userFarPath:[Character] = ["#","F","F","6","0","B","9"]

/*: "#EEEEEE" :*/
fileprivate let userProvisionConversionName:String = "#EEEEEEmusic magnitude red guard along"

/*: "opacity" :*/
fileprivate let k_throughRevenueMessage:String = "opfireci"
fileprivate let notiLiteralName:String = "tradio"

/*: "path" :*/
fileprivate let app_placeHardPath:[Character] = ["p","a","t","h"]

/*: "extra" :*/
fileprivate let data_orientationUrl:String = "labtra"

/*: "msgInfo" :*/
fileprivate let main_objectEducatorValue:[Character] = ["m","s","g","I","n","f","o"]

/*: "content" :*/
fileprivate let noti_peopleFormat:String = "contotalentotal"

/*: "messageType" :*/
fileprivate let k_accessStr:[Character] = ["m","e","s","s","a"]
fileprivate let show_exclusiveResignName:[Character] = ["g"]
fileprivate let user_matchData:String = "eTypemultiple production"

/*: "[Reply messages to get points]" :*/
fileprivate let constSpeakerDeleteTitle:String = "reason rating[Reply"
fileprivate let const_stateTitle:String = "s tostop quality"
fileprivate let appHeapTweenName:[Character] = ["i","n","t","s","]"]

/*: "title" :*/
fileprivate let app_closedMsg:String = "TITLE"

/*: "img" :*/
fileprivate let data_takeTagStr:String = "middlemg"

/*: "[Photo]" :*/
fileprivate let noti_quitValue:String = "during dynamics manual compare[Phot"
fileprivate let mainItemMessage:String = "coalition somethingo]"

/*: "[Audio]" :*/
fileprivate let constBirthdayMsg:[Character] = ["[","A","u","d","i","o","]"]

/*: "rYMsgType" :*/
fileprivate let main_dragStr:String = "equally hi mount orientation sinkrYMsgType"

/*: "GJ:CallCustom" :*/
fileprivate let noti_expressionPlaceUrl:[Character] = ["G","J",":","C","a","l","l","C","u","s","t","o"]
fileprivate let notiInviteString:String = "picture"

/*: "callCmd" :*/
fileprivate let app_wedParticipationBeyondValue:String = "callCmdequivalent actual attention"

/*: "missCall" :*/
fileprivate let user_ionOpentoName:String = "mwrite"
fileprivate let main_devicePanPath:[Character] = ["s","s","C","a","l","l"]

/*: "[Missed Call]" :*/
fileprivate let const_enhanceValue:[Character] = ["[","M","i","s","s","e","d"," ","C","a","l","l","]"]

/*: "logType" :*/
fileprivate let show_exactlyContinuingUrl:[Character] = ["l","o","g","T","y","p"]
fileprivate let notiLeadingString:[Character] = ["e"]

/*: "[Voice Call]" :*/
fileprivate let k_crucialTitle:String = "notice visit[Voice"
fileprivate let const_progressPushMessage:String = " Call]remark level cap take"

/*: "[Video Call]" :*/
fileprivate let app_wireId:String = "[Videolibrary limit challenge easy ting"
fileprivate let main_mageId:String = "recommend hello middle theme tempo Call]"

/*: "video" :*/
fileprivate let mainReplySkinPath:[Character] = ["v","i","d","e","o"]

/*: "contentType" :*/
fileprivate let mainEnterMsg:String = "covenueen"
fileprivate let data_ayeData:String = "tTypedatabase tolerance nobody union"

/*: "VideoMsg" :*/
fileprivate let show_adminData:String = "Videenvironment experienced business"

/*: "[Short Video]" :*/
fileprivate let appLibraryString:String = "[Shorlimited mp"
fileprivate let userProfessionalWhyAirFormat:String = "deo]appropriate specify button disable pad"

/*: "match" :*/
fileprivate let const_inputMsg:String = "matceveryone"

/*: "[Cupid messaging]" :*/
fileprivate let notiIsolateMsg:[Character] = ["[","C","u","p","i","d"," ","m","e","s","s","a"]
fileprivate let noti_aboveId:String = "blind normal safe customerging]"

/*: "GJ:Sess:TxtCustom" :*/
fileprivate let userDrawerVolumeFormat:[Character] = ["G","J",":","S","e","s","s",":","T","x","t"]
fileprivate let appContrastData:String = "Customwithout upper chain read off"

/*: "isSend" :*/
fileprivate let kSuccessfullyId:String = "isSendmomentum lack"

/*: "Sent Gift" :*/
fileprivate let dataStartString:String = "Sentgirl hard via sh disabled"

/*: "Receive Gift" :*/
fileprivate let k_drawBehaviorValue:String = "video construct possibleRecei"

/*: "uid" :*/
fileprivate let kCitySquareString:[Character] = ["u","i","d"]

/*: "gift" :*/
fileprivate let data_accessFirstValue:String = "GIFT"

/*: "name" :*/
fileprivate let kSubsequentTitle:String = "nrowme"

/*: "num" :*/
fileprivate let data_pointTitle:[Character] = ["n","u","m"]

/*: "imageUri" :*/
fileprivate let noti_premiumIndicatorOneName:[Character] = ["i","m"]
fileprivate let show_rocketPath:String = "ageUriwrap send terms prompt"

/*: "imageData" :*/
fileprivate let appMmFormat:[Character] = ["i","m","a","g","e","D","a","t","a"]

/*: "<img>" :*/
fileprivate let constSupportId:String = "statement mirror detect<img>"

/*: "</img>" :*/
fileprivate let dataKnockMsg:[Character] = ["<","/","i","m","g",">"]

/*: "wantGift" :*/
fileprivate let noti_scriptMessage:String = "wantGiftsale too canvas him weight"

/*: "roundNum" :*/
fileprivate let k_lessWritePath:[Character] = ["r","o","u","n","d","N","u","m"]

/*: "[Question]" :*/
fileprivate let dataBorderRequestUrl:[Character] = ["[","Q","u","e","s","t","i","o","n","]"]

/*: "intimatePhoto" :*/
fileprivate let userProcessorName:String = "INTIM"
fileprivate let kBurnUrl:String = "mount"

/*: "audioData" :*/
fileprivate let mainSendKitFormat:String = "aresolve"
fileprivate let noti_dayEarnFormat:[Character] = ["d","i","o","D","a","t","a"]

/*: "audioUri" :*/
fileprivate let main_likeName:String = "makerudi"

/*: "draftContent" :*/
fileprivate let notiAcceptData:[Character] = ["d","r","a","f","t","C"]
fileprivate let kNowWireTitle:String = "oinvisibleteinvisiblet"

/*: "[draft]%@" :*/
fileprivate let k_acheFarmerGenerallyUrl:String = "[dinterested supposed want appear gesture"
fileprivate let mainFaceMsg:String = "raft]%@skin frequency out pass"

/*: "99999+" :*/
fileprivate let showCaptureUrl:[UInt8] = [0x2b,0x39,0x39,0x39,0x39,0x39]

/*: "New" :*/
fileprivate let user_remainAcrossName:String = "Newthen path occasion rate"

/*: "Account is restricted！" :*/
fileprivate let const_strengthData:String = "big readAccoun"
fileprivate let app_angleDisplayKey:String = "restriparty"
fileprivate let show_audiencePath:String = "ted！inner before"

/*: "%@ Online" :*/
fileprivate let dataSustainableSpringValue:[Character] = ["%","@"," ","O","n","l","i","n","e"]

/*: "Messages not been replied here" :*/
fileprivate let app_gemUrl:[Character] = ["M","e","s","s","a","g","e","s"," ","n"]
fileprivate let dataBeforeMessage:[Character] = ["o","t"," ","b","e","e","n"," ","r","e","p","l","i","e","d"," ","h","e","r","e"]

//: Declare String End

// __DEBUG__
// __CLOSE_PRINT__
//
//  EmbraceView.swift
//  AbroadTalking
//
//  Created by Hemming on 2022/9/12.
//

//: import UIKit
import UIKit

//: let RCSystemFormatType                 = "formatType"
let dataRoundEdgeDrawerPath = (data_operationId.replacingOccurrences(of: "isolate", with: "a") + "tType")
//: let RCSystemFormatTypePlain            = "plain"
let userSendKey = (dataCosyId.replacingOccurrences(of: "bring", with: "n"))

//: class TalkingChatListTableCell: UITableViewCell {
class EmbraceView: UITableViewCell {
    //: required init?(coder: NSCoder) {
    required init?(coder: NSCoder) {
        //: super.init(coder: coder)
        super.init(coder: coder)
        //: fatalError("init(coder:) has not been implemented")
        fatalError(String(bytes: dataGraphPath.map{infraPackage(accelerate: $0)}, encoding: .utf8)!)
    }

    //: override init(style: UITableViewCell.CellStyle, reuseIdentifier: String?) {
    override init(style: UITableViewCell.CellStyle, reuseIdentifier: String?) {
        //: super.init(style: style, reuseIdentifier: reuseIdentifier)
        super.init(style: style, reuseIdentifier: reuseIdentifier)
        //: createCellUI()
        enablely()
        //: createCellUIConstraints()
        fileAd()
    }

    // MARK: - Lazy load

    //: private lazy var headImgView: UIImageView = {
    private lazy var headImgView: UIImageView = {
        //: let imageView = UIImageView.init()
        let imageView = UIImageView()
        //: imageView.layer.cornerRadius = 30
        imageView.layer.cornerRadius = 30
        //: imageView.clipsToBounds = true
        imageView.clipsToBounds = true
        //: imageView.backgroundColor = UIColor.white
        imageView.backgroundColor = UIColor.white
        //: imageView.contentMode = .scaleAspectFill
        imageView.contentMode = .scaleAspectFill
        //: return imageView
        return imageView
        //: }()
    }()

    //: private lazy var iconBorder: UIButton = {
    private lazy var iconBorder: UIButton = {
        //: let btn = UIButton.init()
        let btn = UIButton()
        //: btn.imageView?.contentMode = .scaleAspectFill
        btn.imageView?.contentMode = .scaleAspectFill
        //: btn.isUserInteractionEnabled = false
        btn.isUserInteractionEnabled = false
        //: self.addSubview(btn)
        self.addSubview(btn)
        //: return btn
        return btn
        //: }()
    }()

    //: private lazy var badgeLab: UILabel = {
    private lazy var badgeLab: UILabel = {
        //: let label = UILabel.init()
        let label = UILabel()
        //: label.font = .pingfangFont(type: .Medium, fontSize: 13)
        label.font = .yearNearby(type: .Medium, fontSize: 13)
        //: label.textAlignment = .center
        label.textAlignment = .center
        //: label.textColor = .white
        label.textColor = .white
        //: label.backgroundColor = UIColor(hex: "#FF2348")
        label.backgroundColor = UIColor(hex: (String(mainHunkChainMessage)))
        //: label.layer.cornerRadius = 10
        label.layer.cornerRadius = 10
        //: label.layer.masksToBounds = true
        label.layer.masksToBounds = true
        //: label.layer.borderColor = UIColor.white.cgColor
        label.layer.borderColor = UIColor.white.cgColor
        //: label.layer.borderWidth = 1
        label.layer.borderWidth = 1
        //: label.isHidden = true
        label.isHidden = true
        //: return label
        return label
        //: }()
    }()

    //: private lazy var nickNameLabel: UILabel = {
    private lazy var nickNameLabel: UILabel = {
        //: let label = UILabel.init()
        let label = UILabel()
        //: label.font = .pingfangFont(type: .Medium, fontSize: 18)
        label.font = .yearNearby(type: .Medium, fontSize: 18)
        //: label.textColor = .appTitleColor()
        label.textColor = .untilExpected()
        //: return label
        return label
        //: }()
    }()

    //: private lazy var msgShowLabel: UILabel = {
    private lazy var msgShowLabel: UILabel = {
        //: let label = UILabel.init()
        let label = UILabel()
        //: label.font = .pingfangFont(type: .Regular, fontSize: 15)
        label.font = .yearNearby(type: .Regular, fontSize: 15)
        //: label.textColor = .appValueDetailColor()
        label.textColor = .theProvision()
        //: return label
        return label
        //: }()
    }()

    //: private lazy var timeLab: UILabel = {
    private lazy var timeLab: UILabel = {
        //: let label = UILabel.init()
        let label = UILabel()
        //: label.font = .pingfangFont(type: .Medium, fontSize: 12)
        label.font = .yearNearby(type: .Medium, fontSize: 12)
        //: label.textColor = .appValueDetailColor()
        label.textColor = .theProvision()
        //: return label
        return label
        //: }()
    }()

    //: lazy var loungeImgV: UIImageView = {
    lazy var loungeImgV: UIImageView = {
        //: let img = UIImageView()
        let img = UIImageView()
        //: img.image = UIImage.BundleImageNamed(name: "icon_lounge")
        img.image = UIImage.managerToSecond(name: (String(kParticipationMsg.suffix(6)) + "ounge"))
        //: return img
        return img
        //: }()
    }()

    //: private lazy var authImgV: UIImageView = {
    private lazy var authImgV: UIImageView = {
        //: let imgV = UIImageView()
        let imgV = UIImageView()
        //: imgV.image = UIImage.BundleImageNamed(name: "icon_home_v")
        imgV.image = UIImage.managerToSecond(name: (String(user_boyMsg) + String(k_independentTitle)))
        //: return imgV
        return imgV
        //: }()
    }()

    //: private lazy var newImgV: TalkingButton = {
    private lazy var newImgV: DistinctionButton = {
        //: let imgV = TalkingButton()
        let imgV = DistinctionButton()
        //: imgV.setBackgroundImage(UIImage.BundleImageNamed(name: "icon_chat_list_new"), for: .normal)
        imgV.setBackgroundImage(UIImage.managerToSecond(name: (String(mainCareKey.suffix(6)) + "hat_l" + String(main_unknownMarkActionMsg) + String(userMUrl.suffix(5)))), for: .normal)
        //: return imgV
        return imgV
        //: }()
    }()

    //: private lazy var officialImgV: UIButton = {
    private lazy var officialImgV: UIButton = {
        //: let img = UIButton()
        let img = UIButton()
        //: img.setBackgroundColor(color: UIColor.msgTipsColor(), forState: .normal)
        img.faceCrop(color: UIColor.atMount(), forState: .normal)
        //: img.setTitle("Official".localized, for: .normal)
        img.setTitle((String(app_extraString.suffix(4)) + "cial").localized, for: .normal)
        //: img.setTitleColor(.white, for: .normal)
        img.setTitleColor(.white, for: .normal)
        //: img.titleLabel?.font = UIFont.pingfangRugularFont(fontSize: 11)
        img.titleLabel?.font = UIFont.enterCommon(fontSize: 11)
        //: img.layer.cornerRadius = 8
        img.layer.cornerRadius = 8
        //: img.layer.masksToBounds = true
        img.layer.masksToBounds = true
        //: return img
        return img
        //: }()
    }()

    //: private lazy var onlineStatus: UIView = {
    private lazy var onlineStatus: UIView = {
        //: let v = UIView()
        let v = UIView()
        //: v.backgroundColor = UIColor(hex: "#30D00B")
        v.backgroundColor = UIColor(hex: (String(main_viewName)))
        //: v.layer.cornerRadius = 5
        v.layer.cornerRadius = 5
        //: v.layer.borderColor = UIColor.white.cgColor
        v.layer.borderColor = UIColor.white.cgColor
        //: v.layer.borderWidth = 1
        v.layer.borderWidth = 1
        //: v.isHidden = true
        v.isHidden = true
        //: return v
        return v
        //: }()
    }()

    //: private lazy var imtimateBtn: TalkingButton = {
    private lazy var imtimateBtn: DistinctionButton = {
        //: let btn = TalkingButton.init()
        let btn = DistinctionButton()
        //: btn.setImage(UIImage.BundleImageNamed(name: "iv_message_honey"), for: .normal)
        btn.setImage(UIImage.managerToSecond(name: (String(notiLatName) + String(app_rollFeatureMsg))), for: .normal)
        //: btn.spaceBetweenTitleAndImage = 2
        btn.spaceBetweenTitleAndImage = 2
        //: btn.titleLabel?.font = .pingfangFont(type: .Medium, fontSize: 12)
        btn.titleLabel?.font = .yearNearby(type: .Medium, fontSize: 12)
        //: btn.setTitleColor(UIColor.init(hex: "#FF60B9"), for: .normal)
        btn.setTitleColor(UIColor(hex: (String(userFarPath))), for: .normal)
        //: btn.contentHorizontalAlignment = .right
        btn.contentHorizontalAlignment = .right
        //: if LanguageManager.shared.direction == .rightToLeft {
        if InputCloseInfo.shared.direction == .rightToLeft {
            //: btn.contentHorizontalAlignment = .left
            btn.contentHorizontalAlignment = .left
        }
        //: return btn
        return btn
        //: }()
    }()
}

//: extension TalkingChatListTableCell {
extension EmbraceView {
    //: override func touchesBegan(_ touches: Set<UITouch>, with event: UIEvent?) {
    override func touchesBegan(_ touches: Set<UITouch>, with event: UIEvent?) {
        //: let animationColor = UIColor.init(hex: "#EEEEEE")
        let animationColor = UIColor(hex: (String(userProvisionConversionName.prefix(7))))
        //: let animationDuration: Double = 0.4
        let animationDuration = 0.4

        //: super.touchesBegan(touches, with: event)
        super.touchesBegan(touches, with: event)
        //: let coverView = UIView(frame: bounds)
        let coverView = UIView(frame: bounds)
        //: coverView.autoresizingMask = [.flexibleWidth, .flexibleHeight]
        coverView.autoresizingMask = [.flexibleWidth, .flexibleHeight]
        //: coverView.backgroundColor = UIColor.clear
        coverView.backgroundColor = UIColor.clear
        //: self.addSubview(coverView)
        self.addSubview(coverView)

        //: let touch = touches.first!
        let touch = touches.first!
        //: let point = touch.location(in: self)
        let point = touch.location(in: self)

        //: let ourTouchView = UIView(frame: CGRect(x: point.x - 5, y: point.y - 5, width: 10, height: 10))
        let ourTouchView = UIView(frame: CGRect(x: point.x - 5, y: point.y - 5, width: 10, height: 10))

        //: let circleMaskPathInitial = UIBezierPath(ovalIn: ourTouchView.frame)
        let circleMaskPathInitial = UIBezierPath(ovalIn: ourTouchView.frame)
        //: let pathRect = self.bounds.inset(by: UIEdgeInsets(top: 1, left: 1, bottom: 1, right: 1))
        let pathRect = self.bounds.inset(by: UIEdgeInsets(top: 1, left: 1, bottom: 1, right: 1))
        //: let circleMaskPathFinal = UIBezierPath(roundedRect: pathRect, cornerRadius: 5)
        let circleMaskPathFinal = UIBezierPath(roundedRect: pathRect, cornerRadius: 5)

        //: let rippleLayer = CAShapeLayer()
        let rippleLayer = CAShapeLayer()
        //: rippleLayer.opacity = 1.0
        rippleLayer.opacity = 1.0
        //: rippleLayer.fillColor = animationColor?.cgColor
        rippleLayer.fillColor = animationColor?.cgColor
        //: rippleLayer.path = circleMaskPathFinal.cgPath
        rippleLayer.path = circleMaskPathFinal.cgPath
        //: coverView.layer.addSublayer(rippleLayer)
        coverView.layer.addSublayer(rippleLayer)

        // fade up
        //: let fadeUp = CABasicAnimation(keyPath: "opacity")
        let fadeUp = CABasicAnimation(keyPath: (k_throughRevenueMessage.replacingOccurrences(of: "fire", with: "a") + notiLiteralName.replacingOccurrences(of: "radio", with: "y")))
        //: fadeUp.beginTime = CACurrentMediaTime()
        fadeUp.beginTime = CACurrentMediaTime()
        //: fadeUp.duration = 0.1
        fadeUp.duration = 0.1
        //: fadeUp.toValue = 0.8
        fadeUp.toValue = 0.8
        //: fadeUp.timingFunction = CAMediaTimingFunction(name: CAMediaTimingFunctionName.easeOut)
        fadeUp.timingFunction = CAMediaTimingFunction(name: CAMediaTimingFunctionName.easeOut)
        //: fadeUp.fillMode = CAMediaTimingFillMode.forwards
        fadeUp.fillMode = CAMediaTimingFillMode.forwards
        //: fadeUp.isRemovedOnCompletion = false
        fadeUp.isRemovedOnCompletion = false
        //: rippleLayer.add(fadeUp, forKey: nil)
        rippleLayer.add(fadeUp, forKey: nil)

        // fade down
        //: let fade = CABasicAnimation(keyPath: "opacity")
        let fade = CABasicAnimation(keyPath: (k_throughRevenueMessage.replacingOccurrences(of: "fire", with: "a") + notiLiteralName.replacingOccurrences(of: "radio", with: "y")))
        //: fade.beginTime = CACurrentMediaTime() + animationDuration * 0.60
        fade.beginTime = CACurrentMediaTime() + animationDuration * 0.60
        //: fade.duration = 0.1
        fade.duration = 0.1
        //: fade.toValue = 0
        fade.toValue = 0
        //: fade.timingFunction = CAMediaTimingFunction(name: CAMediaTimingFunctionName.easeOut)
        fade.timingFunction = CAMediaTimingFunction(name: CAMediaTimingFunctionName.easeOut)
        //: fade.fillMode = CAMediaTimingFillMode.forwards
        fade.fillMode = CAMediaTimingFillMode.forwards
        //: fade.isRemovedOnCompletion = false
        fade.isRemovedOnCompletion = false
        //: rippleLayer.add(fade, forKey: nil)
        rippleLayer.add(fade, forKey: nil)

        // change path
        //: CATransaction.begin()
        CATransaction.begin()
        //: let maskLayerAnimation = CABasicAnimation(keyPath: "path")
        let maskLayerAnimation = CABasicAnimation(keyPath: (String(app_placeHardPath)))
        //: maskLayerAnimation.fromValue = circleMaskPathInitial.cgPath
        maskLayerAnimation.fromValue = circleMaskPathInitial.cgPath
        //: maskLayerAnimation.toValue = circleMaskPathFinal.cgPath
        maskLayerAnimation.toValue = circleMaskPathFinal.cgPath
        //: maskLayerAnimation.beginTime = CACurrentMediaTime()
        maskLayerAnimation.beginTime = CACurrentMediaTime()
        //: maskLayerAnimation.duration = 0.1
        maskLayerAnimation.duration = 0.1
        //: maskLayerAnimation.timingFunction = CAMediaTimingFunction(name: CAMediaTimingFunctionName.easeOut)
        maskLayerAnimation.timingFunction = CAMediaTimingFunction(name: CAMediaTimingFunctionName.easeOut)
        //: CATransaction.setCompletionBlock({
        CATransaction.setCompletionBlock {
            //: coverView.removeFromSuperview()
            coverView.removeFromSuperview()
            //: })
        }
        //: rippleLayer.add(maskLayerAnimation, forKey: "path")
        rippleLayer.add(maskLayerAnimation, forKey: (String(app_placeHardPath)))
        //: CATransaction.commit()
        CATransaction.commit()
    }
}

// MARK: - Public Event

//: extension TalkingChatListTableCell {
extension EmbraceView {
    /// 刷新cell
    /// - Parameter model: 模型
    //: func refreshCell(model: TalkingConversationModel?) {
    func scriptM(model: FormSubtleModel?) {
        //: guard let model = model else { return }
        guard let model = model else { return }
        //: let isOther = (model.chatType == .system || model.chatType == .service ||
        let isOther = (model.chatType == .system || model.chatType == .service ||
            //: model.chatType == .group || model.chatType == .moreMsg)
            model.chatType == .group || model.chatType == .moreMsg)
        // 更新昵称、头像、vip标识
        //: updateUserInfo(userInfo: model.gj_userInfo, model: model, isOther: isOther)
        singleComposition(userInfo: model.gj_userInfo, model: model, isOther: isOther)
        // 未读消息数
        //: updateUnreadNumber(unreadCount: Int(model.unreadCount))
        streamConfirm(unreadCount: Int(model.unreadCount))
        // 详情消息
        //: handleC2CMessage(model: model)
        keepBloc(model: model)

        //: if model.chatType == .group || model.chatType == .moreMsg {
        if model.chatType == .group || model.chatType == .moreMsg {
            //: self.accessoryType = .disclosureIndicator
            self.accessoryType = .disclosureIndicator // 添加箭头
            //: } else {
        } else {
            //: self.accessoryType = .none
            self.accessoryType = .none
        }
        // 时间
        //: timeLab.text = nil
        timeLab.text = nil
        //: if isOther == false && model.hasTimestamp {
        if isOther == false, model.hasTimestamp {
            //: let timestamp: Date? = model.timestamp as Date?
            let timestamp: Date? = model.timestamp as Date?
            //: if timestamp != nil && timestamp!.timeIntervalSince1970 > NSTimeIntervalSince1970 {
            if timestamp != nil, timestamp!.timeIntervalSince1970 > NSTimeIntervalSince1970 {
                //: timeLab.text = NSDate.tx_messageTimeString(showDetail: false, date: timestamp!)
                timeLab.text = NSDate.outsideDelivery(showDetail: false, date: timestamp!)
            }
        }
        // 置顶背景色
        //: if model.isPinned == true {
        if model.isPinned == true {
            //: contentView.backgroundColor = UIColor(hex: "#EEEEEE")
            contentView.backgroundColor = UIColor(hex: (String(userProvisionConversionName.prefix(7))))
            //: } else {
        } else {
            //: contentView.backgroundColor = (MarginExamineer.share.appStatus == AppSkinStatus.special.rawValue) ? .clear:.white
            contentView.backgroundColor = (MarginExamineer.share.appStatus == AwayPush.special.rawValue) ? .clear : .white
        }
        //: self.backgroundColor = contentView.backgroundColor
        self.backgroundColor = contentView.backgroundColor
    }

    /// 处理最新消息
    //: static func handleShowC2CDetailTextOfMessage(model: TalkingConversationModel) -> NSMutableAttributedString? {
    static func dealCell(model: FormSubtleModel) -> NSMutableAttributedString? {
//        messageType 消息类型：1系统消息 2官方小秘 3自动打招呼 4主动打招呼 5付费消息 6普通消息 7主动打招呼未回复 8.主打招呼回复 9，自动打招呼未回复 10, 自动打招呼回复11.发送礼物消息 12互相关注消息 13 取消关注 22家族邀请跳转消息 25索要礼物 29问题回答消息
        //: var detailAttrString: NSMutableAttributedString?
        var detailAttrString: NSMutableAttributedString?
        //: let change = false
        let change = false
        //: var speColor = [NSValue: UIColor]()
        var speColor = [NSValue: UIColor]()
        //: let showUnread = model.isShowMsgUnread && model.unreadCount > 0
        let showUnread = model.isShowMsgUnread && model.unreadCount > 0
        //: let direction: TMsgDirection = (model.listShowMessage?.isSelf ?? false) ? .MsgDirectionOutgoing:.MsgDirectionIncoming
        let direction: CommandNorm = (model.listShowMessage?.isSelf ?? false) ? .MsgDirectionOutgoing : .MsgDirectionIncoming
        //: var voice_msgInfo: JSON?
        var voice_msgInfo: JSON?

        //: guard model.draftText.isEmptyString else {
        guard model.draftText.isEmptyString else { // 草稿
            //: detailAttrString = self.getDraftString(draft: &model.draftText)
            detailAttrString = self.multiFirst(draft: &model.draftText)
            //: detailAttrString?.addAttribute(.font, value: UIFont.pingfangFont(type: .Regular, fontSize: 15), range: NSRange(location: 0, length: detailAttrString?.length ?? 0))
            detailAttrString?.addAttribute(.font, value: UIFont.yearNearby(type: .Regular, fontSize: 15), range: NSRange(location: 0, length: detailAttrString?.length ?? 0))
            //: return detailAttrString
            return detailAttrString
        }

        //: let extra = String(data: (model.listShowMessage?.customElem.data)!, encoding: .utf8)
        let extra = String(data: (model.listShowMessage?.customElem.data)!, encoding: .utf8)
        //: let json = JSON(parseJSON: extra!)
        let json = JSON(parseJSON: extra!)
        //: let extraDic = json["extra"]
        let extraDic = json[(data_orientationUrl.replacingOccurrences(of: "lab", with: "ex"))]
        //: let msgInfo = extraDic["msgInfo"]
        let msgInfo = extraDic[(String(main_objectEducatorValue))]
        //: var content = msgInfo["content"].stringValue.deleteHtmlTips()
        var content = msgInfo[(noti_peopleFormat.replacingOccurrences(of: "total", with: "t"))].stringValue.untilFee()
        //: let messageType = msgInfo["messageType"].intValue
        let messageType = msgInfo[(String(k_accessStr) + String(show_exclusiveResignName) + String(user_matchData.prefix(5)))].intValue
        //: let formatTypeStr = extraDic["formatType"].stringValue
        let formatTypeStr = extraDic[(data_operationId.replacingOccurrences(of: "isolate", with: "a") + "tType")].stringValue

        //: if TalkingPrivateChatManager.isReplyToGetPointsMsg(direction, messageType, msgTime: model.listShowMessage?.timestamp) {
        if InvisibleJob.analyzeClip(direction, messageType, msgTime: model.listShowMessage?.timestamp) { // 回复获取积分消息
            //: let content = "[Reply messages to get points]".localized
            let content = (String(constSpeakerDeleteTitle.suffix(6)) + " message" + String(const_stateTitle.prefix(4)) + " get po" + String(appHeapTweenName)).localized
            //: let color  = UIColor.appValueColor()
            let color = UIColor.obtainOf()
            //: detailAttrString = NSMutableAttributedString(string: content)
            detailAttrString = NSMutableAttributedString(string: content)
            //: speColor = [NSValue(range: NSRange(location: 0, length: detailAttrString?.length ?? 0)): color]
            speColor = [NSValue(range: NSRange(location: 0, length: detailAttrString?.length ?? 0)): color]

            //: } else if model.chatType == .system {
        } else if model.chatType == .system { // 官方消息，只展示文本
            //: var content = extraDic["title"].stringValue
            var content = extraDic[(app_closedMsg.lowercased())].stringValue
            //: let sysContent = extraDic["content"].stringValue
            let sysContent = extraDic[(noti_peopleFormat.replacingOccurrences(of: "total", with: "t"))].stringValue
            //: let sysImg = extraDic["img"].stringValue
            let sysImg = extraDic[(data_takeTagStr.replacingOccurrences(of: "middle", with: "i"))].stringValue
            //: if content.count > 0 || sysContent.count > 0 || sysImg.count > 0 {
            if content.count > 0 || sysContent.count > 0 || sysImg.count > 0 { // 跳转系统消息
                //: if content.count == 0 {
                if content.count == 0 {
                    //: content.append(sysContent)
                    content.append(sysContent)
                }
                //: if sysImg.count > 0 {
                if sysImg.count > 0 {
                    //: content.append("[Photo]".localized)
                    content.append((String(noti_quitValue.suffix(5)) + String(mainItemMessage.suffix(2))).localized)
                }

                //: } else {
            } else { // 默认系统消息
                //: content = msgInfo["content"].stringValue
                content = msgInfo[(noti_peopleFormat.replacingOccurrences(of: "total", with: "t"))].stringValue
                //: if content.isEmptyString {
                if content.isEmptyString {
                    //: content = json["content"].stringValue
                    content = json[(noti_peopleFormat.replacingOccurrences(of: "total", with: "t"))].stringValue
                }
                //: content = content.deleteHtmlTips()
                content = content.untilFee()
            }
            //: detailAttrString = NSMutableAttributedString(string: content)
            detailAttrString = NSMutableAttributedString(string: content)

            //: } else if NeedSpecify.checkVoiceJsonMsg(msginfo: msgInfo.dictionaryValue) {
        } else if NeedSpecify.accelerateWritten(msginfo: msgInfo.dictionaryValue) { // 语音
            //: voice_msgInfo = msgInfo
            voice_msgInfo = msgInfo
            //: content = "[Audio]".localized
            content = (String(constBirthdayMsg)).localized
            //: detailAttrString = NSMutableAttributedString(string: content)
            detailAttrString = NSMutableAttributedString(string: content)

            //: } else if json["rYMsgType"].stringValue == "GJ:CallCustom" { // 音视频通话
        } else if json[(String(main_dragStr.suffix(9)))].stringValue == (String(noti_expressionPlaceUrl) + notiInviteString.replacingOccurrences(of: "picture", with: "m")) { // 音视频通话
            //: if msgInfo["callCmd"].stringValue == "missCall" { // 未接来电
            if msgInfo[(String(app_wedParticipationBeyondValue.prefix(7)))].stringValue == (user_ionOpentoName.replacingOccurrences(of: "write", with: "i") + String(main_devicePanPath)) { // 未接来电
                //: content = "[Missed Call]".localized
                content = (String(const_enhanceValue)).localized
                //: } else {
            } else {
                //: if msgInfo["logType"].intValue == 1 { // 音频
                if msgInfo[(String(show_exactlyContinuingUrl) + String(notiLeadingString))].intValue == 1 { // 音频
                    //: content = "[Voice Call]".localized
                    content = (String(k_crucialTitle.suffix(6)) + String(const_progressPushMessage.prefix(6))).localized
                    //: } else {
                } else {
                    //: content = "[Video Call]".localized
                    content = (String(app_wireId.prefix(6)) + String(main_mageId.suffix(6))).localized
                }
            }

            //: detailAttrString = NSMutableAttributedString(string: content)
            detailAttrString = NSMutableAttributedString(string: content)

            //: } else if msgInfo["video"].exists() || msgInfo["contentType"].stringValue == "VideoMsg" { // 短视频
        } else if msgInfo[(String(mainReplySkinPath))].exists() || msgInfo[(mainEnterMsg.replacingOccurrences(of: "venue", with: "nt") + String(data_ayeData.prefix(5)))].stringValue == (String(show_adminData.prefix(4)) + "oMsg") { // 短视频
            //: content = "[Short Video]".localized
            content = (String(appLibraryString.prefix(5)) + "t Vi" + String(userProfessionalWhyAirFormat.prefix(4))).localized
            //: detailAttrString = NSMutableAttributedString(string: content)
            detailAttrString = NSMutableAttributedString(string: content)

            //: } else if (extraDic["match"].stringValue == "1" || msgInfo["match"].stringValue == "1") { // 匹配消息
        } else if extraDic[(const_inputMsg.replacingOccurrences(of: "everyone", with: "h"))].stringValue == "1" || msgInfo[(const_inputMsg.replacingOccurrences(of: "everyone", with: "h"))].stringValue == "1" { // 匹配消息
            //: content = "[Cupid messaging]".localized
            content = (String(notiIsolateMsg) + String(noti_aboveId.suffix(5))).localized
            //: detailAttrString = NSMutableAttributedString(string: content)
            detailAttrString = NSMutableAttributedString(string: content)

            //: } else if json["rYMsgType"].stringValue == "GJ:Sess:TxtCustom" { // 提示消息
        } else if json[(String(main_dragStr.suffix(9)))].stringValue == (String(userDrawerVolumeFormat) + String(appContrastData.prefix(6))) { // 提示消息
            //: content = json["content"].stringValue
            content = json[(noti_peopleFormat.replacingOccurrences(of: "total", with: "t"))].stringValue
            //: detailAttrString = NSMutableAttributedString(string: content)
            detailAttrString = NSMutableAttributedString(string: content)

            //: } else if messageType == 11 {
        } else if messageType == 11 { // 送礼
            //: let isSend = msgInfo["isSend"].string
            let isSend = msgInfo[(String(kSuccessfullyId.prefix(6)))].string
            //: var string1: String?
            var string1: String?
            //: if isSend != nil {
            if isSend != nil {
                //: string1 = isSend!.boolValue ? "Sent Gift".localized:"Receive Gift".localized
                string1 = isSend!.boolValue ? (String(dataStartString.prefix(4)) + " Gift").localized : (String(k_drawBehaviorValue.suffix(5)) + "ve Gift").localized
                //: } else {
            } else {
                //: let uid = msgInfo["uid"].stringValue
                let uid = msgInfo[(String(kCitySquareString))].stringValue
                //: string1 = (uid == MarginExamineer.share.loginUserMode.userID) ? "Sent Gift".localized:"Receive Gift".localized
                string1 = (uid == MarginExamineer.share.loginUserMode.userID) ? (String(dataStartString.prefix(4)) + " Gift").localized : (String(k_drawBehaviorValue.suffix(5)) + "ve Gift").localized
            }
            //: let name = extraDic["gift"]["name"].stringValue
            let name = extraDic[(data_accessFirstValue.lowercased())][(kSubsequentTitle.replacingOccurrences(of: "row", with: "a"))].stringValue
            //: let num = extraDic["gift"]["num"].stringValue
            let num = extraDic[(data_accessFirstValue.lowercased())][(String(data_pointTitle))].stringValue

            //: content = "\(string1!) \(name) x\(num)"
            content = "\(string1!) \(name) x\(num)"
            //: detailAttrString = NSMutableAttributedString(string: content)
            detailAttrString = NSMutableAttributedString(string: content)

            //: } else if messageType == 4 {
        } else if messageType == 4 { // 主动打招呼
//            let uid = msgInfo["uid"].stringValue
//            let toUid = msgInfo["toUid"].stringValue
            //: detailAttrString = NSMutableAttributedString(string: content)
            detailAttrString = NSMutableAttributedString(string: content)

            //: } else if messageType == 3 {
        } else if messageType == 3 { // 自动打招呼
            //: let imageUri = msgInfo["imageUri"].stringValue
            let imageUri = msgInfo[(String(noti_premiumIndicatorOneName) + String(show_rocketPath.prefix(6)))].stringValue
            //: let imageData = msgInfo["imageData"].stringValue
            let imageData = msgInfo[(String(appMmFormat))].stringValue

            //: if NeedSpecify.checkVoiceJsonMsg(msginfo: msgInfo.dictionaryValue) {
            if NeedSpecify.accelerateWritten(msginfo: msgInfo.dictionaryValue) {
                //: content = "[Audio]".localized
                content = (String(constBirthdayMsg)).localized
                //: } else if (imageUri.count > 0 || imageData.count > 0) {
            } else if imageUri.count > 0 || imageData.count > 0 {
                //: content = "[Photo]".localized
                content = (String(noti_quitValue.suffix(5)) + String(mainItemMessage.suffix(2))).localized
            }
            //: detailAttrString = NSMutableAttributedString(string: content)
            detailAttrString = NSMutableAttributedString(string: content)

            //: } else if formatTypeStr == RCSystemFormatTypePlain {
        } else if formatTypeStr == userSendKey {
            //: var content  = msgInfo["content"].stringValue
            var content = msgInfo[(noti_peopleFormat.replacingOccurrences(of: "total", with: "t"))].stringValue
            //: var imageUrl = extraDic["imageUri"].stringValue
            var imageUrl = extraDic[(String(noti_premiumIndicatorOneName) + String(show_rocketPath.prefix(6)))].stringValue
            //: let imageData = extraDic["imageData"].stringValue
            let imageData = extraDic[(String(appMmFormat))].stringValue
            //: if imageUrl.count == 0 || imageData.count > 0 {
            if imageUrl.count == 0 || imageData.count > 0 {
                //: if content.hasPrefix("<img>") && content.hasSuffix("</img>") {
                if content.hasPrefix((String(constSupportId.suffix(5)))), content.hasSuffix((String(dataKnockMsg))) {
                    //: var str = NSString(string: content)
                    var str = NSString(string: content)
                    //: str = str.replacingOccurrences(of: "<img>", with: "") as NSString
                    str = str.replacingOccurrences(of: (String(constSupportId.suffix(5))), with: "") as NSString
                    //: str = str.replacingOccurrences(of: "</img>", with: "") as NSString
                    str = str.replacingOccurrences(of: (String(dataKnockMsg)), with: "") as NSString
                    //: imageUrl = str as String
                    imageUrl = str as String
                }
            }

            //: if (imageUrl.count > 0 || imageData.count > 0) {
            if imageUrl.count > 0 || imageData.count > 0 {
                //: content = "[Photo]".localized
                content = (String(noti_quitValue.suffix(5)) + String(mainItemMessage.suffix(2))).localized
                //: } else {
            } else {
                //: content = content.deleteHtmlTips()
                content = content.untilFee()
            }

            //: detailAttrString = NSMutableAttributedString(string: content)
            detailAttrString = NSMutableAttributedString(string: content)

            //: } else if messageType == 25 {
        } else if messageType == 25 { // 索要礼物
            //: if let wantGift  = msgInfo["wantGift"].dictionary {
            if let wantGift = msgInfo[(String(noti_scriptMessage.prefix(8)))].dictionary {
                //: let content = wantGift["content"]?.string ?? ""
                let content = wantGift[(noti_peopleFormat.replacingOccurrences(of: "total", with: "t"))]?.string ?? ""
                //: detailAttrString = NSMutableAttributedString(string: content)
                detailAttrString = NSMutableAttributedString(string: content)
            }

            //: } else if messageType == 29, direction == .MsgDirectionIncoming, msgInfo["roundNum"].intValue == 1 { // 问题回答消息
        } else if messageType == 29, direction == .MsgDirectionIncoming, msgInfo[(String(k_lessWritePath))].intValue == 1 { // 问题回答消息
            //: content = "[Question]".localized
            content = (String(dataBorderRequestUrl)).localized
            //: detailAttrString = NSMutableAttributedString(string: content)
            detailAttrString = NSMutableAttributedString(string: content)

            //: } else {
        } else {
            // 图片
            //: let imageUri             = msgInfo["imageUri"].stringValue
            let imageUri = msgInfo[(String(noti_premiumIndicatorOneName) + String(show_rocketPath.prefix(6)))].stringValue
            //: let imageData            = msgInfo["imageData"].stringValue
            let imageData = msgInfo[(String(appMmFormat))].stringValue
            //: let intimatePhotoDict    = msgInfo["intimatePhoto"].dictionaryValue // 私密照片
            let intimatePhotoDict = msgInfo[(userProcessorName.lowercased() + "atePhot" + kBurnUrl.replacingOccurrences(of: "mount", with: "o"))].dictionaryValue // 私密照片
            //: if (imageUri.count > 0 || imageData.count > 0 || intimatePhotoDict.keys.count > 0) {
            if imageUri.count > 0 || imageData.count > 0 || intimatePhotoDict.keys.count > 0 {
                //: content = "[Photo]".localized
                content = (String(noti_quitValue.suffix(5)) + String(mainItemMessage.suffix(2))).localized
            }
            // 语音消息
            //: if NeedSpecify.checkVoiceJsonMsg(msginfo: msgInfo.dictionaryValue) {
            if NeedSpecify.accelerateWritten(msginfo: msgInfo.dictionaryValue) {
                //: content = "[Audio]".localized
                content = (String(constBirthdayMsg)).localized
            }

            //: detailAttrString = NSMutableAttributedString(string: content)
            detailAttrString = NSMutableAttributedString(string: content)
        }

        //: if (!change) {
        if !change {
            //: if detailAttrString?.string == "[Audio]".localized {
            if detailAttrString?.string == (String(constBirthdayMsg)).localized {
                //: let sendUid = model.sender
                let sendUid = model.sender
                //: let mid = MarginExamineer.share.loginUserMode.userID
                let mid = MarginExamineer.share.loginUserMode.userID
                //: if sendUid == mid {
                if sendUid == mid { // 我发送的
                    //: } else {
                } else { // 别人发的
                    //: var downurl = ""
                    var downurl = ""
                    //: if (voice_msgInfo?["audioData"].stringValue.isEmptyString) != nil {
                    if (voice_msgInfo?[(mainSendKitFormat.replacingOccurrences(of: "resolve", with: "u") + String(noti_dayEarnFormat))].stringValue.isEmptyString) != nil {
                        //: downurl = voice_msgInfo!["audioData"].stringValue
                        downurl = voice_msgInfo![(mainSendKitFormat.replacingOccurrences(of: "resolve", with: "u") + String(noti_dayEarnFormat))].stringValue
                        //: } else if (voice_msgInfo?["audioUri"].stringValue.isEmptyString) != nil {
                    } else if (voice_msgInfo?[(main_likeName.replacingOccurrences(of: "maker", with: "a") + "oUri")].stringValue.isEmptyString) != nil {
                        //: downurl = voice_msgInfo!["audioUri"].stringValue
                        downurl = voice_msgInfo![(main_likeName.replacingOccurrences(of: "maker", with: "a") + "oUri")].stringValue
                    }
                }
                //: let unreadColor = UIColor.appValueDetailColor()
                let unreadColor = UIColor.theProvision()
                //: detailAttrString?.addAttribute(.foregroundColor, value: unreadColor, range: NSRange(location: 0, length: detailAttrString?.length ?? 0))
                detailAttrString?.addAttribute(.foregroundColor, value: unreadColor, range: NSRange(location: 0, length: detailAttrString?.length ?? 0))

                //: } else {
            } else {
                //: let unreadColor = UIColor.appValueDetailColor()
                let unreadColor = UIColor.theProvision()
                //: detailAttrString?.addAttribute(.foregroundColor, value: unreadColor, range: NSRange(location: 0, length: detailAttrString?.length ?? 0))
                detailAttrString?.addAttribute(.foregroundColor, value: unreadColor, range: NSRange(location: 0, length: detailAttrString?.length ?? 0))
            }
        }
        //: if speColor.keys.count > 0 {
        if speColor.keys.count > 0 {
            //: for (key, value) in speColor {
            for (key, value) in speColor {
                //: detailAttrString?.addAttribute(.foregroundColor, value: value, range: key.rangeValue)
                detailAttrString?.addAttribute(.foregroundColor, value: value, range: key.rangeValue)
            }
        }
        //: detailAttrString?.addAttribute(.font, value: UIFont.pingfangFont(type: .Regular, fontSize: 15), range: NSRange(location: 0, length: detailAttrString?.length ?? 0))
        detailAttrString?.addAttribute(.font, value: UIFont.yearNearby(type: .Regular, fontSize: 15), range: NSRange(location: 0, length: detailAttrString?.length ?? 0))

        //: return detailAttrString
        return detailAttrString
    }

    /// 处理草稿消息
    //: static func getDraftString(draft: inout String) -> NSMutableAttributedString {
    static func multiFirst(draft: inout String) -> NSMutableAttributedString {
        //: if draft.contains("draftContent") {
        if draft.contains((String(notiAcceptData) + kNowWireTitle.replacingOccurrences(of: "invisible", with: "n"))) {
            //: let dict = NSDictionary.dictionary(withJsonString: draft)
            let dict = NSDictionary.surroundingDown(withJsonString: draft)
            //: draft = "\(String(describing: dict?["draftContent"]))"
            draft = "\(String(describing: dict?[(String(notiAcceptData) + kNowWireTitle.replacingOccurrences(of: "invisible", with: "n"))]))"
        }
        //: let draftString = NSMutableAttributedString.init(string: "[draft]%@".localizedArguments(draft))
        let draftString = NSMutableAttributedString(string: (String(k_acheFarmerGenerallyUrl.prefix(2)) + String(mainFaceMsg.prefix(7))).theAppear(draft))
        //: draftString.addAttribute(.foregroundColor, value: UIColor.msgTipsColor(), range: NSRange(location: 0, length: 7))
        draftString.addAttribute(.foregroundColor, value: UIColor.atMount(), range: NSRange(location: 0, length: 7))
        //: return draftString
        return draftString
    }
}

// MARK: - Private Event

//: extension TalkingChatListTableCell {
extension EmbraceView {
    /// 更新昵称、头像
    /// - Parameters:
    ///   - userInfo: 数据库信息
    ///   - model: tx数据
    ///   - isOther: 是否其他消息
    //: private func updateUserInfo(userInfo: DocumentLevelPurple?, model: TalkingConversationModel, isOther: Bool) {
    private func singleComposition(userInfo: DocumentLevelPurple?, model: FormSubtleModel, isOther: Bool) {
        // 在线状态
        //: onlineStatus.isHidden = (model.listOnlineStatus == 0)
        onlineStatus.isHidden = (model.listOnlineStatus == 0)
        //: newImgV.isHidden = !model.isNewUser
        newImgV.isHidden = !model.isNewUser
        // 是否存在用户信息
        //: if userInfo == nil || userInfo?.uid == MovementStructure.getXiaoMiID() || userInfo?.uid == MovementStructure.getCustomerServiceID() {
        if userInfo == nil || userInfo?.uid == MovementStructure.uniqueAbove() || userInfo?.uid == MovementStructure.cartoonConnection() {
            //: nickNameLabel.textColor = .appTitleColor()
            nickNameLabel.textColor = .untilExpected()
            //: nickNameLabel.text = model.showName
            nickNameLabel.text = model.showName
            //: nickNameLabel.snp.updateConstraints { make in
            nickNameLabel.snp.updateConstraints { make in
                //: make.trailing.lessThanOrEqualToSuperview().offset(-40)
                make.trailing.lessThanOrEqualToSuperview().offset(-40)
            }
            //: if model.chatType == .group || model.chatType == .moreMsg {
            if model.chatType == .group || model.chatType == .moreMsg {
                //: headImgView.image = UIImage.BundleImageNamed(name: model.faceUrl)
                headImgView.image = UIImage.managerToSecond(name: model.faceUrl)
                //: } else {
            } else {
                //: headImgView.setUrlImage(urlStr: model.faceUrl, placeImg: UIImage.placeSquareBigImgBanner())
                headImgView.chest(urlStr: model.faceUrl, placeImg: UIImage.enhanceBy())
            }
            //: iconBorder.isHidden = true
            iconBorder.isHidden = true
            //: loungeImgV.isHidden = true
            loungeImgV.isHidden = true
            //: authImgV.isHidden = true
            authImgV.isHidden = true
            //: officialImgV.isHidden = true
            officialImgV.isHidden = true
            //: imtimateBtn.isHidden = true
            imtimateBtn.isHidden = true
            //: return
            return
        }
        //: guard let userInfo = userInfo else {
        guard let userInfo = userInfo else {
            //: return
            return
        }
        // 昵称
        //: nickNameLabel.textColor = userInfo.loungePlus ? .userVipColor():.appTitleColor()
        nickNameLabel.textColor = userInfo.loungePlus ? .boyfriend() : .untilExpected()
        //: nickNameLabel.text = userInfo.nickname.count > 0 ? userInfo.nickname:model.userID
        nickNameLabel.text = userInfo.nickname.count > 0 ? userInfo.nickname : model.userID
        // 头像
        //: headImgView.setUrlImage(urlStr: userInfo.headPic, placeImg: UIImage.placeHolderImage(sex: userInfo.sex))
        headImgView.chest(urlStr: userInfo.headPic, placeImg: UIImage.queryedChange(sex: userInfo.sex))
        // VIP头像框
        //: if isOther == false && !userInfo.headPicFrame.isEmptyString {
        if isOther == false, !userInfo.headPicFrame.isEmptyString {
            //: iconBorder.isHidden = false
            iconBorder.isHidden = false
            //: iconBorder.setHeadFrameUrlImage(urlStr: userInfo.headPicFrame)
            iconBorder.quantityoIt(urlStr: userInfo.headPicFrame)
            //: } else {
        } else {
            //: iconBorder.isHidden = true
            iconBorder.isHidden = true
        }
        //: imtimateBtn.setTitle(model.totalIntimate > 99999 ? "99999+": "\(model.totalIntimate)", for: .normal)
        imtimateBtn.setTitle(model.totalIntimate > 99999 ? String(bytes: showCaptureUrl.reversed(), encoding: .utf8)! : "\(model.totalIntimate)", for: .normal)
        //: imtimateBtn.isHidden = model.totalIntimate <= 0
        imtimateBtn.isHidden = model.totalIntimate <= 0
        //: if model.totalIntimate > 0 {
        if model.totalIntimate > 0 {
            //: let size = imtimateBtn.titleLabel?.text?.boundingRect(with: CGSize(width: CGFloat(MAXFLOAT), height: 0.0), options: .usesLineFragmentOrigin, attributes: [NSAttributedString.Key.font: imtimateBtn.titleLabel?.font ?? .pingfangFont(type: .Medium, fontSize: 12)], context: nil)
            let size = imtimateBtn.titleLabel?.text?.boundingRect(with: CGSize(width: CGFloat(MAXFLOAT), height: 0.0), options: .usesLineFragmentOrigin, attributes: [NSAttributedString.Key.font: imtimateBtn.titleLabel?.font ?? .yearNearby(type: .Medium, fontSize: 12)], context: nil)
            //: imtimateBtn.snp.updateConstraints { make in
            imtimateBtn.snp.updateConstraints { make in
                //: make.width.equalTo((size?.width ?? 50) + 20)
                make.width.equalTo((size?.width ?? 50) + 20)
            }
        }
        // 是否其他消息
        //: if isOther == false {
        if isOther == false {
            //: loungeImgV.isHidden = !userInfo.loungePlus
            loungeImgV.isHidden = !userInfo.loungePlus
            //: authImgV.isHidden = !userInfo.tpAuth
            authImgV.isHidden = !userInfo.tpAuth
            //: officialImgV.isHidden = !userInfo.isOfficial
            officialImgV.isHidden = !userInfo.isOfficial
            //: } else {
        } else {
            //: loungeImgV.isHidden = true
            loungeImgV.isHidden = true
            //: authImgV.isHidden = true
            authImgV.isHidden = true
            //: officialImgV.isHidden = true
            officialImgV.isHidden = true
        }

        //: nickNameLabel.snp.updateConstraints { make in
        nickNameLabel.snp.updateConstraints { make in
            //: make.trailing.lessThanOrEqualToSuperview().offset( isOther == true ? -40 : -130)
            make.trailing.lessThanOrEqualToSuperview().offset(isOther == true ? -40 : -130)
        }
        //: loungeImgV.snp.updateConstraints { make in
        loungeImgV.snp.updateConstraints { make in
            //: make.leading.equalTo(nickNameLabel.snp.trailing).offset(loungeImgV.isHidden == true ? 0:4)
            make.leading.equalTo(nickNameLabel.snp.trailing).offset(loungeImgV.isHidden == true ? 0 : 4)
            //: make.width.equalTo(loungeImgV.isHidden == true ? 0:20)
            make.width.equalTo(loungeImgV.isHidden == true ? 0 : 20)
        }
        //: authImgV.snp.updateConstraints { make in
        authImgV.snp.updateConstraints { make in
            //: make.leading.equalTo(loungeImgV.snp.trailing).offset(authImgV.isHidden == true ? 0:4)
            make.leading.equalTo(loungeImgV.snp.trailing).offset(authImgV.isHidden == true ? 0 : 4)
            //: make.width.equalTo(authImgV.isHidden == true ? 0:16)
            make.width.equalTo(authImgV.isHidden == true ? 0 : 16)
        }
        //: let text = "New".localized
        let text = (String(user_remainAcrossName.prefix(3))).localized
        //: let rect = text.boundingRect(with: .zero, options: .usesLineFragmentOrigin, attributes: [.font: UIFont.pingfangMediumFont(fontSize: 11)], context: nil)
        let rect = text.boundingRect(with: .zero, options: .usesLineFragmentOrigin, attributes: [.font: UIFont.hangProgram(fontSize: 11)], context: nil)
        //: newImgV.snp.updateConstraints { make in
        newImgV.snp.updateConstraints { make in
            //: make.leading.equalTo(authImgV.snp.trailing).offset(newImgV.isHidden == true ? 0:4)
            make.leading.equalTo(authImgV.snp.trailing).offset(newImgV.isHidden == true ? 0 : 4)
            //: make.width.equalTo(newImgV.isHidden == true ? 0:rect.width+10)
            make.width.equalTo(newImgV.isHidden == true ? 0 : rect.width + 10)
        }
        //: officialImgV.snp.remakeConstraints { make in
        officialImgV.snp.remakeConstraints { make in
            //: make.centerY.equalTo(nickNameLabel)
            make.centerY.equalTo(nickNameLabel)
            //: make.height.equalTo(15)
            make.height.equalTo(15)
            //: make.leading.equalTo(newImgV.snp.trailing).offset(officialImgV.isHidden == true ? 0:4)
            make.leading.equalTo(newImgV.snp.trailing).offset(officialImgV.isHidden == true ? 0 : 4)
            //: if officialImgV.isHidden {
            if officialImgV.isHidden {
                //: make.width.equalTo(0)
                make.width.equalTo(0)
                //: } else {
            } else {
                //: make.width.greaterThanOrEqualTo(50)
                make.width.greaterThanOrEqualTo(50)
            }
        }
    }

    /// 更新未读数
    /// - Parameter unreadCount: 未读消息数
    //: private func updateUnreadNumber(unreadCount: Int) {
    private func streamConfirm(unreadCount: Int) {
        //: guard unreadCount > 0 else {
        guard unreadCount > 0 else {
            //: self.badgeLab.isHidden = true
            self.badgeLab.isHidden = true
            //: return
            return
        }

        //: self.badgeLab.isHidden = false
        self.badgeLab.isHidden = false
        //: var unreadCountStr = String(unreadCount)
        var unreadCountStr = String(unreadCount)
        //: if unreadCount > 99 {
        if unreadCount > 99 {
            //: unreadCountStr = String(unreadCount) + "+"
            unreadCountStr = String(unreadCount) + "+"
            //: badgeLab.text = "99+"
            badgeLab.text = "99+"
            //: } else {
        } else {
            //: badgeLab.text = unreadCountStr
            badgeLab.text = unreadCountStr
        }
        //: var width = badgeLab.text!.size(withAttributes: [.font: self.badgeLab.font!]).width + 12
        var width = badgeLab.text!.size(withAttributes: [.font: self.badgeLab.font!]).width + 12
        //: if width < 20 { width = 20 }
        if width < 20 { width = 20 }
        //: badgeLab.snp.updateConstraints { make in
        badgeLab.snp.updateConstraints { make in
            //: make.width.greaterThanOrEqualTo(width)
            make.width.greaterThanOrEqualTo(width)
        }
    }

    /// 处理详情消息
    //: private func handleC2CMessage(model: TalkingConversationModel) {
    private func keepBloc(model: FormSubtleModel) {
        //: if model.userStatus == 5 {
        if model.userStatus == 5 {
            //: let str = "Account is restricted！".localized
            let str = (String(const_strengthData.suffix(6)) + "t is " + app_angleDisplayKey.replacingOccurrences(of: "party", with: "c") + String(show_audiencePath.prefix(4))).localized
            //: let attributes: [NSAttributedString.Key: Any] = [
            let attributes: [NSAttributedString.Key: Any] = [
                //: NSAttributedString.Key.font: UIFont.pingfangFont(type: .Regular, fontSize: 15),
                NSAttributedString.Key.font: UIFont.yearNearby(type: .Regular, fontSize: 15),
                //: NSAttributedString.Key.foregroundColor: UIColor.appValueDetailColor()
                NSAttributedString.Key.foregroundColor: UIColor.theProvision(),
            ]
            //: msgShowLabel.attributedText = NSAttributedString(string: str, attributes: attributes)
            msgShowLabel.attributedText = NSAttributedString(string: str, attributes: attributes)
            //: return
            return
        }
        //: if model.listShowMessage != nil {
        if model.listShowMessage != nil {
            //: msgShowLabel.attributedText = TalkingChatListTableCell.handleShowC2CDetailTextOfMessage(model: model)
            msgShowLabel.attributedText = EmbraceView.dealCell(model: model)
            //: } else if model.chatType == .group {
        } else if model.chatType == .group { // 公共聊天室入口
            //: let str = "%@ Online".localizedArguments(model.num)
            let str = (String(dataSustainableSpringValue)).theAppear(model.num)
            //: let attributes: [NSAttributedString.Key: Any] = [
            let attributes: [NSAttributedString.Key: Any] = [
                //: NSAttributedString.Key.font: UIFont.pingfangFont(type: .Regular, fontSize: 15),
                NSAttributedString.Key.font: UIFont.yearNearby(type: .Regular, fontSize: 15),
                //: NSAttributedString.Key.foregroundColor: UIColor.appValueDetailColor()
                NSAttributedString.Key.foregroundColor: UIColor.theProvision(),
            ]
            //: msgShowLabel.attributedText = NSAttributedString(string: str, attributes: attributes)
            msgShowLabel.attributedText = NSAttributedString(string: str, attributes: attributes)

            //: } else if model.chatType == .moreMsg {
        } else if model.chatType == .moreMsg { // 更多消息入口，无数据时，占位文案
            //: let str = "Messages not been replied here".localized
            let str = (String(app_gemUrl) + String(dataBeforeMessage)).localized
            //: let attributes: [NSAttributedString.Key: Any] = [
            let attributes: [NSAttributedString.Key: Any] = [
                //: NSAttributedString.Key.font: UIFont.pingfangFont(type: .Regular, fontSize: 15),
                NSAttributedString.Key.font: UIFont.yearNearby(type: .Regular, fontSize: 15),
                //: NSAttributedString.Key.foregroundColor: UIColor.appValueDetailColor()
                NSAttributedString.Key.foregroundColor: UIColor.theProvision(),
            ]
            //: msgShowLabel.attributedText = NSAttributedString(string: str, attributes: attributes)
            msgShowLabel.attributedText = NSAttributedString(string: str, attributes: attributes)

            //: } else {
        } else { // 草稿箱
            //: var attrStr: NSMutableAttributedString?
            var attrStr: NSMutableAttributedString?
            //: if !model.draftText.isEmptyString {
            if !model.draftText.isEmptyString {
                //: attrStr = TalkingChatListTableCell.getDraftString(draft: &model.draftText)
                attrStr = EmbraceView.multiFirst(draft: &model.draftText)
            }
            //: attrStr?.addAttribute(.font, value: UIFont.pingfangFont(type: .Regular, fontSize: 15), range: NSRange(location: 0, length: attrStr?.length ?? 0))
            attrStr?.addAttribute(.font, value: UIFont.yearNearby(type: .Regular, fontSize: 15), range: NSRange(location: 0, length: attrStr?.length ?? 0))
            //: msgShowLabel.attributedText = attrStr
            msgShowLabel.attributedText = attrStr
        }
    }
}

// MARK: - UI

//: extension TalkingChatListTableCell {
extension EmbraceView {
    //: private func createCellUI() {
    private func enablely() {
        //: contentView.addSubview(headImgView)
        contentView.addSubview(headImgView)
        //: contentView.addSubview(iconBorder)
        contentView.addSubview(iconBorder)
        //: contentView.addSubview(onlineStatus)
        contentView.addSubview(onlineStatus)
        //: contentView.addSubview(badgeLab)
        contentView.addSubview(badgeLab)
        //: contentView.addSubview(nickNameLabel)
        contentView.addSubview(nickNameLabel)
        //: contentView.addSubview(loungeImgV)
        contentView.addSubview(loungeImgV)
        //: contentView.addSubview(authImgV)
        contentView.addSubview(authImgV)
        //: contentView.addSubview(newImgV)
        contentView.addSubview(newImgV)
        //: contentView.addSubview(officialImgV)
        contentView.addSubview(officialImgV)
        //: contentView.addSubview(msgShowLabel)
        contentView.addSubview(msgShowLabel)
        //: contentView.addSubview(timeLab)
        contentView.addSubview(timeLab)
        //: contentView.addSubview(imtimateBtn)
        contentView.addSubview(imtimateBtn)
    }

    //: private func createCellUIConstraints() {
    private func fileAd() {
        //: headImgView.snp.makeConstraints { make in
        headImgView.snp.makeConstraints { make in
            //: make.centerY.equalToSuperview()
            make.centerY.equalToSuperview()
            //: make.leading.equalTo(15)
            make.leading.equalTo(15)
            //: make.width.height.equalTo(60)
            make.width.height.equalTo(60)
        }
        //: iconBorder.snp.makeConstraints { make in
        iconBorder.snp.makeConstraints { make in
            //: make.centerY.equalToSuperview()
            make.centerY.equalToSuperview()
            //: make.leading.equalTo(10)
            make.leading.equalTo(10)
            //: make.width.height.equalTo(69.5)
            make.width.height.equalTo(69.5)
        }
        //: onlineStatus.snp.makeConstraints { make in
        onlineStatus.snp.makeConstraints { make in
            //: make.trailing.equalTo(headImgView.snp.trailing).offset(-3)
            make.trailing.equalTo(headImgView.snp.trailing).offset(-3)
            //: make.bottom.equalTo(headImgView.snp.bottom).offset(-3)
            make.bottom.equalTo(headImgView.snp.bottom).offset(-3)
            //: make.width.height.equalTo(10)
            make.width.height.equalTo(10)
        }
        //: badgeLab.snp.makeConstraints { make in
        badgeLab.snp.makeConstraints { make in
            //: make.top.equalTo(self.headImgView.snp.top).offset(-5)
            make.top.equalTo(self.headImgView.snp.top).offset(-5)
            //: make.trailing.equalTo(self.headImgView.snp.trailing).offset(6)
            make.trailing.equalTo(self.headImgView.snp.trailing).offset(6)
            //: make.height.equalTo(20)
            make.height.equalTo(20)
            //: make.width.greaterThanOrEqualTo(20)
            make.width.greaterThanOrEqualTo(20)
        }
        //: nickNameLabel.snp.makeConstraints { make in
        nickNameLabel.snp.makeConstraints { make in
            //: make.top.equalTo(headImgView).offset(6)
            make.top.equalTo(headImgView).offset(6)
            //: make.leading.equalTo(headImgView.snp.trailing).offset(10)
            make.leading.equalTo(headImgView.snp.trailing).offset(10)
            //: make.trailing.lessThanOrEqualToSuperview().offset(-40)
            make.trailing.lessThanOrEqualToSuperview().offset(-40)
            //: make.height.equalTo(21)
            make.height.equalTo(21)
        }
        //: loungeImgV.snp.makeConstraints { make in
        loungeImgV.snp.makeConstraints { make in
            //: make.centerY.equalTo(nickNameLabel)
            make.centerY.equalTo(nickNameLabel)
            //: make.leading.equalTo(nickNameLabel.snp.trailing).offset(4)
            make.leading.equalTo(nickNameLabel.snp.trailing).offset(4)
            //: make.height.width.equalTo(20)
            make.height.width.equalTo(20)
        }
        //: authImgV.snp.makeConstraints { make in
        authImgV.snp.makeConstraints { make in
            //: make.centerY.equalTo(nickNameLabel)
            make.centerY.equalTo(nickNameLabel)
            //: make.leading.equalTo(loungeImgV.snp.trailing).offset(4)
            make.leading.equalTo(loungeImgV.snp.trailing).offset(4)
            //: make.width.equalTo(16)
            make.width.equalTo(16)
            //: make.height.equalTo(16)
            make.height.equalTo(16)
        }
        //: newImgV.snp.makeConstraints { make in
        newImgV.snp.makeConstraints { make in
            //: make.centerY.equalTo(nickNameLabel)
            make.centerY.equalTo(nickNameLabel)
            //: make.leading.equalTo(authImgV.snp.trailing).offset(4)
            make.leading.equalTo(authImgV.snp.trailing).offset(4)
            //: make.width.equalTo(26)
            make.width.equalTo(26)
            //: if LanguageManager.shared.direction == .rightToLeft {
            if InputCloseInfo.shared.direction == .rightToLeft {
                //: make.height.equalTo(16)
                make.height.equalTo(16)
                //: } else {
            } else {
                //: make.height.equalTo(14)
                make.height.equalTo(14)
            }
        }
        //: officialImgV.snp.makeConstraints { make in
        officialImgV.snp.makeConstraints { make in
            //: make.centerY.equalTo(nickNameLabel)
            make.centerY.equalTo(nickNameLabel)
            //: make.leading.equalTo(newImgV.snp.trailing).offset(4)
            make.leading.equalTo(newImgV.snp.trailing).offset(4)
            //: make.width.greaterThanOrEqualTo(50)
            make.width.greaterThanOrEqualTo(50)
            //: make.height.equalTo(16)
            make.height.equalTo(16)
        }
        //: timeLab.snp.makeConstraints { make in
        timeLab.snp.makeConstraints { make in
            //: make.centerY.equalTo(nickNameLabel)
            make.centerY.equalTo(nickNameLabel)
            //: make.trailing.equalToSuperview().offset(-15)
            make.trailing.equalToSuperview().offset(-15)
        }
        //: imtimateBtn.snp.makeConstraints { make in
        imtimateBtn.snp.makeConstraints { make in
            //: make.centerY.equalTo(msgShowLabel)
            make.centerY.equalTo(msgShowLabel)
            //: make.width .equalTo(50)
            make.width.equalTo(50)
            //: make.trailing.equalToSuperview().offset(-15)
            make.trailing.equalToSuperview().offset(-15)
        }
        //: msgShowLabel.snp.makeConstraints { make in
        msgShowLabel.snp.makeConstraints { make in
            //: make.bottom.equalTo(headImgView.snp.bottom).offset(-6)
            make.bottom.equalTo(headImgView.snp.bottom).offset(-6)
            //: make.leading.equalTo(nickNameLabel)
            make.leading.equalTo(nickNameLabel)
            //: make.trailing.equalTo(imtimateBtn.snp.leading).offset(-10)
            make.trailing.equalTo(imtimateBtn.snp.leading).offset(-10)
        }
    }
}
