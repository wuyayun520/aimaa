
//: Declare String Begin

/*: "init(coder:) has not been implemented" :*/
fileprivate let mainLawEqualValue:[UInt8] = [0x62,0x65,0x62,0x7f,0x23,0x68,0x64,0x6f,0x6e,0x79,0x31,0x22,0x2b,0x63,0x6a,0x78,0x2b,0x65,0x64,0x7f,0x2b,0x69,0x6e,0x6e,0x65,0x2b,0x62,0x66,0x7b,0x67,0x6e,0x66,0x6e,0x65,0x7f,0x6e,0x6f]

private func supposedInjury(abs num: UInt8) -> UInt8 {
    return num ^ 11
}

/*: "male_invite_call_mask" :*/
fileprivate let noti_incomeCountroFeeUrl:String = "sound stroke favoritemale_i"
fileprivate let app_dropFormat:String = "l_maskfront over mode open"

/*: "get json error" :*/
fileprivate let data_musicData:[Character] = ["g","e","t"," ","j","s","o","n"," ","e","r","r","o","r"]

/*: "#FF2348" :*/
fileprivate let show_quoteCompareFormat:String = "#FF23point prepare collection tolerance identity"
fileprivate let user_curMsg:String = "4production"

/*: "She invites you to call" :*/
fileprivate let noti_shouldAyeKey:String = "obtain instruction weak me sessionShe in"
fileprivate let show_modelName:String = " you to creturn else attribute command"
fileprivate let app_localConstantTitle:[Character] = ["a","l","l"]

/*: "btn_video_drop_nor" :*/
fileprivate let noti_sinceString:[Character] = ["b","t","n","_","v","i","d","e","o","_","d","r","o","p","_","n","o","r"]

/*: "male_invite_call_nor" :*/
fileprivate let main_mendMsg:String = "activele"
fileprivate let kMagnitudePath:String = "e_callrank box"

/*: "transform.scale" :*/
fileprivate let k_rateLineId:[Character] = ["t","r","a","n","s","f"]
fileprivate let show_resignUrl:[Character] = ["o","r","m",".","s","c","a","l","e"]

/*: "animation" :*/
fileprivate let constProKeepMessage:String = "anprocessorati"
fileprivate let k_hisString:String = "collection"

/*: "icon_Chat_freetimes" :*/
fileprivate let app_effString:String = "iplayeron"
fileprivate let mainMaximumTitle:String = "t_frcrush mp"
fileprivate let data_dropKey:[Character] = ["e","e","t","i","m","e","s"]

/*: "limit" :*/
fileprivate let showAlbumMsg:[UInt8] = [0x79,0x7c,0x78,0x7c,0x61]

private func complyJudge(strip num: UInt8) -> UInt8 {
    return num ^ 21
}

/*: "uid" :*/
fileprivate let dataFoundationMsg:String = "uiallow"

/*: "invitePop" :*/
fileprivate let user_minimizeData:String = "incrushte"
fileprivate let userBridgeWeekId:[Character] = ["P","o","p"]

/*: "headPic" :*/
fileprivate let main_businessTitle:String = "extra"
fileprivate let mainSheCrucialData:[Character] = ["e","a","d","P","i","c"]

/*: "videoUrl" :*/
fileprivate let userPlatformStr:String = "videoUrlbetween provide fraction greet present"

/*: "videoPop" :*/
fileprivate let kBeautyPath:[Character] = ["v","i","d","e","o","P","o"]
fileprivate let noti_discountStr:[Character] = ["p"]

/*: "yyyy-MM-dd" :*/
fileprivate let app_easeTitle:[Character] = ["y","y","y","y"]
fileprivate let constBeginPath:String = "red graphic post-MM-dd"

/*: "today" :*/
fileprivate let k_literalMsg:[Character] = ["t","o","d","a","y"]

/*: "times" :*/
fileprivate let mainWouldPath:[Character] = ["t","i","m","e","s"]

/*: "ShowMaleInviteCallViewDateKey_ :*/
fileprivate let k_femalePath:String = "ShowMaleIanswer part mode"
fileprivate let user_pullHundredKey:String = "union time other filternviteC"
fileprivate let showCleanStr:String = "wDateinfo sir generally priority concrete"

//: Declare String End

// __DEBUG__
// __CLOSE_PRINT__
//
//  PainterUnder.swift
//  SaverUnderPacific
//
//  Created by DouXiu on 2025/8/21.
//

//: import UIKit
import UIKit

//: class MaleInviteCallView: UIView {
class PainterUnder: UIView {
    //: var dismissBlock: (() -> Void)?
    var dismissBlock: (() -> Void)?
    //: private var popView: TalkingPopView?
    private var popView: ImmediateSuccess?
    /// 计时器
    //: private var timeoutTimer: Timer?
    private var timeoutTimer: Timer?
    /// 拨打中背景音乐
    //: private var bgmPlayer: TalkingCallBGMPlayer?
    private var bgmPlayer: VerticalName?
    /// 视频封面
    //: private var myVideoUrl = ""
    private var myVideoUrl = ""
    /// 通话头像
    //: private var callHeadPic = ""
    private var callHeadPic = ""
    /// 用户不在线时，是否换个用户拨打
    //: private var needCallAgain = false
    private var needCallAgain = false

    //: private override init(frame: CGRect) {
    override private init(frame: CGRect) {
        //: super.init(frame: frame)
        super.init(frame: frame)
        //: self.frame = UIScreen.main.bounds
        self.frame = UIScreen.main.bounds
    }

    //: required init?(coder: NSCoder) {
    required init?(coder: NSCoder) {
        //: fatalError("init(coder:) has not been implemented")
        fatalError(String(bytes: mainLawEqualValue.map{supposedInjury(abs: $0)}, encoding: .utf8)!)
    }

    // MARK: - Lazy Load

    //: private lazy var bgImgView: UIImageView = {
    private lazy var bgImgView: UIImageView = {
        //: let v = UIImageView()
        let v = UIImageView()
        //: v.image = UIImage.placeHolderImage(sex: Gender.female.rawValue)
        v.image = UIImage.queryedChange(sex: EnvironmentEmpty.female.rawValue)
        //: v.contentMode = .scaleAspectFill
        v.contentMode = .scaleAspectFill
        //: v.isUserInteractionEnabled = true
        v.isUserInteractionEnabled = true
        //: v.layer.cornerRadius = 16
        v.layer.cornerRadius = 16
        //: v.layer.masksToBounds = true
        v.layer.masksToBounds = true
        //: self.addSubview(v)
        self.addSubview(v)
        //: return v
        return v
        //: }()
    }()

    // 视频播放器
    //: private lazy var videoPlayer: TalkingVideoPlayerManager = {
    private lazy var videoPlayer: EqualPublisher = {
        //: let player = TalkingVideoPlayerManager()
        let player = EqualPublisher()
        //: player.setMute(bEnable: true)
        player.area(bEnable: true)
        //: return player
        return player
        //: }()
    }()

    //: private lazy var maskTopImgV: UIImageView = {
    private lazy var maskTopImgV: UIImageView = {
        //: let v = UIImageView()
        let v = UIImageView()
        //: let img = UIImage.BundleImageNamed(name: "male_invite_call_mask")
        let img = UIImage.managerToSecond(name: (String(noti_incomeCountroFeeUrl.suffix(6)) + "nvite_cal" + String(app_dropFormat.prefix(6))))
        //: if let cgimage = img.cgImage {
        if let cgimage = img.cgImage {
            //: v.image = UIImage(cgImage: cgimage, scale: img.scale, orientation: .down)
            v.image = UIImage(cgImage: cgimage, scale: img.scale, orientation: .down)
        }
        //: v.isUserInteractionEnabled = true
        v.isUserInteractionEnabled = true
        //: bgImgView.addSubview(v)
        bgImgView.addSubview(v)
        //: return v
        return v
        //: }()
    }()

    //: private lazy var svgaPlayer: SVGAPlayer = {
    private lazy var svgaPlayer: SVGAPlayer = {
        //: let player = SVGAPlayer()
        let player = SVGAPlayer()
        //: player.loops = 0
        player.loops = 0
        //: player.clearsAfterStop = false
        player.clearsAfterStop = false
        //: player.isUserInteractionEnabled = false
        player.isUserInteractionEnabled = false
        //: player.contentMode = .scaleAspectFill
        player.contentMode = .scaleAspectFill
        //: bgImgView.addSubview(player)
        bgImgView.addSubview(player)
        //: let url = SVGAEffectTool.default.getZipEffectPath(type: .Live_countdown_bg)
        let url = UniqueProcessing.default.scenarioProject(type: .Live_countdown_bg)
        //: do {
        do {
            //: let data = try Data(contentsOf: url)
            let data = try Data(contentsOf: url)
            //: let parser = SVGAParser()
            let parser = SVGAParser()
            //: parser.parse(with: data, cacheKey: "") { [weak self] videoItem in
            parser.parse(with: data, cacheKey: "") { [weak self] videoItem in
                //: guard let self = self else { return }
                guard let self = self else { return }
                //: player.videoItem = videoItem
                player.videoItem = videoItem
                //: player.startAnimation()
                player.startAnimation()
            }
            //: } catch {
        } catch {
            //: printLog(message: "get json error")
            printLog(message: (String(data_musicData)))
        }
        //: return player
        return player
        //: }()
    }()

    //: private lazy var svgaNumLab: UILabel = {
    private lazy var svgaNumLab: UILabel = {
        //: let lab = UILabel()
        let lab = UILabel()
        //: lab.font = UIFont.pingfangMediumFont(fontSize: 20)
        lab.font = UIFont.hangProgram(fontSize: 20)
        //: lab.textAlignment = .center
        lab.textAlignment = .center
        //: lab.textColor = UIColor(hex: "#FF2348")
        lab.textColor = UIColor(hex: (String(show_quoteCompareFormat.prefix(5)) + user_curMsg.replacingOccurrences(of: "production", with: "8")))
        //: bgImgView.addSubview(lab)
        bgImgView.addSubview(lab)
        //: return lab
        return lab
        //: }()
    }()

    //: private lazy var titleLab: UILabel = {
    private lazy var titleLab: UILabel = {
        //: let label = UILabel()
        let label = UILabel()
        //: label.text = "She invites you to call".localized
        label.text = (String(noti_shouldAyeKey.suffix(6)) + "vites" + String(show_modelName.prefix(9)) + String(app_localConstantTitle)).localized
        //: label.font = UIFont.pingfangMediumFont(fontSize: 18)
        label.font = UIFont.hangProgram(fontSize: 18)
        //: label.textColor = .white
        label.textColor = .white
        //: label.textAlignment = .center
        label.textAlignment = .center
        //: label.numberOfLines = 0
        label.numberOfLines = 0
        //: bgImgView.addSubview(label)
        bgImgView.addSubview(label)
        //: return label
        return label
        //: }()
    }()

    //: private lazy var maskBottomImgV: UIImageView = {
    private lazy var maskBottomImgV: UIImageView = {
        //: let v = UIImageView()
        let v = UIImageView()
        //: v.image = UIImage.BundleImageNamed(name: "male_invite_call_mask")
        v.image = UIImage.managerToSecond(name: (String(noti_incomeCountroFeeUrl.suffix(6)) + "nvite_cal" + String(app_dropFormat.prefix(6))))
        //: v.isUserInteractionEnabled = true
        v.isUserInteractionEnabled = true
        //: bgImgView.addSubview(v)
        bgImgView.addSubview(v)
        //: return v
        return v
        //: }()
    }()

    // 挂断/取消 按钮
    //: private lazy var hangupBtn: UIButton = {
    private lazy var hangupBtn: UIButton = {
        //: let btn = UIButton()
        let btn = UIButton()
        //: btn.setImage(UIImage.BundleImageNamed(name: "btn_video_drop_nor"), for: .normal)
        btn.setImage(UIImage.managerToSecond(name: (String(noti_sinceString))), for: .normal)
        //: btn.addTarget(self, action: #selector(hangupButtonClick), for: .touchUpInside)
        btn.addTarget(self, action: #selector(sinceService), for: .touchUpInside)
        //: btn.isHidden = true
        btn.isHidden = true
        //: bgImgView.addSubview(btn)
        bgImgView.addSubview(btn)
        //: return btn
        return btn
        //: }()
    }()

    // 接听按钮
    //: private lazy var acceptBtn: UIButton = {
    private lazy var acceptBtn: UIButton = {
        //: let btn = UIButton()
        let btn = UIButton()
        //: btn.setImage(UIImage.BundleImageNamed(name: "male_invite_call_nor"), for: .normal)
        btn.setImage(UIImage.managerToSecond(name: (main_mendMsg.replacingOccurrences(of: "active", with: "ma") + "_invit" + String(kMagnitudePath.prefix(6)) + "_nor")), for: .normal)
        //: btn.addTarget(self, action: #selector(acceptButtonClick), for: .touchUpInside)
        btn.addTarget(self, action: #selector(duringHave), for: .touchUpInside)
        //: btn.isHidden = true
        btn.isHidden = true
        // 添加关键帧动画
        //: let keyAnimation = CAKeyframeAnimation()
        let keyAnimation = CAKeyframeAnimation()
        //: keyAnimation.keyPath = "transform.scale"
        keyAnimation.keyPath = (String(k_rateLineId) + String(show_resignUrl))
        //: keyAnimation.values = [NSValue(cgAffineTransform: CGAffineTransform(scaleX: 1, y: 1)),
        keyAnimation.values = [NSValue(cgAffineTransform: CGAffineTransform(scaleX: 1, y: 1)),
                               //: NSValue(cgAffineTransform: CGAffineTransform(scaleX: actualWidth(w: 84/70), y: actualWidth(w: 84/70))),
                               NSValue(cgAffineTransform: CGAffineTransform(scaleX: actualWidth(w: 84 / 70), y: actualWidth(w: 84 / 70))),
                               //: NSValue(cgAffineTransform: CGAffineTransform(scaleX: 1, y: 1))]
                               NSValue(cgAffineTransform: CGAffineTransform(scaleX: 1, y: 1))]
        //: keyAnimation.duration = 1.2
        keyAnimation.duration = 1.2
        //: keyAnimation.repeatCount = MAXFLOAT
        keyAnimation.repeatCount = MAXFLOAT
        //: keyAnimation.isRemovedOnCompletion = false
        keyAnimation.isRemovedOnCompletion = false
        //: keyAnimation.fillMode = .both
        keyAnimation.fillMode = .both
        //: keyAnimation.calculationMode = .cubicPaced
        keyAnimation.calculationMode = .cubicPaced
        //: btn.layer.add(keyAnimation, forKey: "animation")
        btn.layer.add(keyAnimation, forKey: (constProKeepMessage.replacingOccurrences(of: "processor", with: "im") + k_hisString.replacingOccurrences(of: "collection", with: "on")))
        //: bgImgView.addSubview(btn)
        bgImgView.addSubview(btn)
        //: return btn
        return btn
        //: }()
    }()

    // 免费通话图标
    //: private lazy var freeImg: UIImageView = {
    private lazy var freeImg: UIImageView = {
        //: let img = UIImageView()
        let img = UIImageView()
        //: img.image = UIImage.BundleImageNamed(name: "icon_Chat_freetimes")
        img.image = UIImage.managerToSecond(name: (app_effString.replacingOccurrences(of: "player", with: "c") + "_Cha" + String(mainMaximumTitle.prefix(4)) + String(data_dropKey)))
        //: img.isHidden = true
        img.isHidden = true
        //: bgImgView.addSubview(img)
        bgImgView.addSubview(img)
        //: return img
        return img
        //: }()
    }()

    // 通话控件
    //: private lazy var videoCallManager: TalkingCallMenuManager = {
    private lazy var videoCallManager: ClipPost = {
        //: let m = TalkingCallMenuManager()
        let m = ClipPost()
        //: return m
        return m
        //: }()
    }()
}

// MARK: - Load Data

//: extension MaleInviteCallView {
extension PainterUnder {
    /// 加载视频匹配用户数据
    //: private func loadMatchData(needRefreshUI: Bool) {
    private func sizeBrown(needRefreshUI: Bool) {
        //: TalkingQuickVideoManager.req_getMatchV4List(params: ["limit":20]) { succeed, result, errorModel in
        ChainBasic.mediaWithDrawer(params: [String(bytes: showAlbumMsg.map{complyJudge(strip: $0)}, encoding: .utf8)!: 20]) { succeed, result, errorModel in
            //: guard succeed == true, let result = result as? [[String: Any]], !result.isEmpty else {
            guard succeed == true, let result = result as? [[String: Any]], !result.isEmpty else {
                //: self.dismiss()
                self.smartPublisher()
                //: return
                return
            }
            // 随机选择一个字典
            //: guard let randomDict = result.randomElement() else {
            guard let randomDict = result.randomElement() else {
                //: self.dismiss()
                self.smartPublisher()
                //: return
                return
            }
            //: self.videoCallManager.uid = randomDict["uid"] as? String ?? ""
            self.videoCallManager.uid = randomDict[(dataFoundationMsg.replacingOccurrences(of: "allow", with: "d"))] as? String ?? ""
            //: self.videoCallManager.source = "invitePop"
            self.videoCallManager.source = (user_minimizeData.replacingOccurrences(of: "crush", with: "vi") + String(userBridgeWeekId))

            //: if needRefreshUI == true {
            if needRefreshUI == true { // 更新当前用户信息，刷新UI
                //: self.callHeadPic = randomDict["headPic"] as? String ?? ""
                self.callHeadPic = randomDict[(main_businessTitle.replacingOccurrences(of: "extra", with: "h") + String(mainSheCrucialData))] as? String ?? ""
                //: self.myVideoUrl = randomDict["videoUrl"] as? String ?? ""
                self.myVideoUrl = randomDict[(String(userPlatformStr.prefix(8)))] as? String ?? ""
                //: self.refreshUI()
                self.modelBy()

                //: } else {
            } else { // 直接拨打
                //: self.callVideo()
                self.selectedThree()
            }
        }
    }
}

// MARK: - Event

//: extension MaleInviteCallView {
extension PainterUnder {
    /// 展示弹窗
    /// - Parameters:
    ///   - uid: 用户ID
    ///   - videoUrl: 视频封面
    ///   - headPic: 头像
    //: func show(uid: String, videoUrl: String, headPic: String) {
    func movement(uid: String, videoUrl: String, headPic: String) {
        // 记录一次展示次数
        //: MaleInviteCallView.writeMaleInviteCallViewTimes()
        PainterUnder.highlight()

        //: if !uid.isEmpty {
        if !uid.isEmpty { // 直接展示，没有再取匹配池
            //: videoCallManager.uid = uid
            videoCallManager.uid = uid
            //: videoCallManager.source = "videoPop"
            videoCallManager.source = (String(kBeautyPath) + String(noti_discountStr))
            //: callHeadPic = headPic
            callHeadPic = headPic
            //: myVideoUrl = videoUrl
            myVideoUrl = videoUrl
            //: needCallAgain = true
            needCallAgain = true
            //: refreshUI()
            modelBy()

            //: } else {
        } else {
            // 直接取匹配池
            //: loadMatchData(needRefreshUI: true)
            sizeBrown(needRefreshUI: true)
        }
    }

    /// 拨打视频通话
    //: private func callVideo() {
    private func selectedThree() {
        //: videoCallManager.checkAndCallVideo()
        videoCallManager.afterQuality()
        //: videoCallManager.onReqestCallErrorBlock = { [weak self] errNo in
        videoCallManager.onReqestCallErrorBlock = { [weak self] errNo in
            //: guard let self = self, needCallAgain == true, errNo == 250822 else {
            guard let self = self, needCallAgain == true, errNo == 250_822 else {
                //: self?.dismiss()
                self?.smartPublisher()
                //: return
                return
            }
            //: self.needCallAgain = false
            self.needCallAgain = false
            //: self.loadMatchData(needRefreshUI: false)
            self.sizeBrown(needRefreshUI: false)
        }
    }

    /// 取消通话按钮点击事件
    //: @objc private func hangupButtonClick() {
    @objc private func sinceService() {
        //: dismiss()
        smartPublisher()
    }

    /// 接听按钮点击事件
    //: @objc private func acceptButtonClick() {
    @objc private func duringHave() {
        //: self.callVideo()
        self.selectedThree()
    }

    //: @objc private func dismiss() {
    @objc private func smartPublisher() {
        //: dismissBlock?()
        dismissBlock?()
        //: popView?.dismissView()
        popView?.command()
        //: popView = nil
        popView = nil
        //: timeoutTimer?.invalidate()
        timeoutTimer?.invalidate()
        //: timeoutTimer = nil
        timeoutTimer = nil
        //: stopBGM()
        city()
    }

    //: func closeView() {
    func betweenDirectTill() {
        //: dismiss()
        smartPublisher()
    }
}

// MARK: - 控制每日展示的Key

//: extension MaleInviteCallView {
extension PainterUnder {
    /// 检测是否可以展示邀请视频
    /// - Returns: 检测结果
    //: class func checkShowMaleInviteCallView() -> Bool {
    class func a() -> Bool {
        //: let today = NSDate.getTimeString(date: Date(), dateFormat: "yyyy-MM-dd")
        let today = NSDate.particleBelong(date: Date(), dateFormat: (String(app_easeTitle) + String(constBeginPath.suffix(6))))
        //: let dict = Defaults.dictionary(forKey: MaleInviteCallView.getMaleInviteCallViewKey())
        let dict = user_findBoundMessage.dictionary(forKey: PainterUnder.launchOn())
        //: let storedDate = dict?["today"] as? String
        let storedDate = dict?[(String(k_literalMsg))] as? String
        //: var times = dict?["times"] as? Int ?? 0
        var times = dict?[(String(mainWouldPath))] as? Int ?? 0
        //: if dict == nil || storedDate != today {
        if dict == nil || storedDate != today {
            //: times = 0
            times = 0
        }
        //: if times >= MarginExamineer.share.appUserConfigMode.inviteTimes {
        if times >= MarginExamineer.share.appUserConfigMode.inviteTimes {
            //: return false
            return false
        }
        //: return true
        return true
    }

    /// 消耗当日一次弹窗额度
    //: class private func writeMaleInviteCallViewTimes() {
    private class func highlight() {
        //: let key = MaleInviteCallView.getMaleInviteCallViewKey()
        let key = PainterUnder.launchOn()
        //: let today = NSDate.getTimeString(date: Date(), dateFormat: "yyyy-MM-dd")
        let today = NSDate.particleBelong(date: Date(), dateFormat: (String(app_easeTitle) + String(constBeginPath.suffix(6))))
        //: var dict = Defaults.dictionary(forKey: key) ?? [:]
        var dict = user_findBoundMessage.dictionary(forKey: key) ?? [:]
        //: let storedDate = dict["today"] as? String
        let storedDate = dict[(String(k_literalMsg))] as? String
        //: var times = dict["times"] as? Int ?? 0
        var times = dict[(String(mainWouldPath))] as? Int ?? 0
        //: if storedDate != today {
        if storedDate != today {
            //: times = 0
            times = 0
        }
        //: dict["today"] = today
        dict[(String(k_literalMsg))] = today
        //: dict["times"] = times + 1
        dict[(String(mainWouldPath))] = times + 1
        //: Defaults.set(dict, forKey: key)
        user_findBoundMessage.set(dict, forKey: key)
    }

    /// 展示邀请视频弹窗的Key
    //: class private func getMaleInviteCallViewKey() -> String {
    private class func launchOn() -> String {
        //: return "ShowMaleInviteCallViewDateKey_\(MarginExamineer.share.loginUserMode.userID)"
        return (String(k_femalePath.prefix(9)) + String(user_pullHundredKey.suffix(6)) + "allVie" + String(showCleanStr.prefix(5)) + "Key_") + "\(MarginExamineer.share.loginUserMode.userID)"
    }
}

// MARK: - 通话音乐

//: extension MaleInviteCallView {
extension PainterUnder {
    /// 初始化视频通话背景音乐
    //: private func setUpBGMAndStartPlayer() {
    private func beforeFieldTotal() {
        //: self.bgmPlayer = TalkingCallBGMPlayer.callBGMPlayer(type: .invitePop)
        self.bgmPlayer = VerticalName.arrayEach(type: .invitePop)
        //: self.bgmPlayer?.play()
        self.bgmPlayer?.same()
    }

    /// 停止播放背景音乐
    //: private func stopBGM() {
    private func city() {
        //: guard self.bgmPlayer != nil else { return }
        guard self.bgmPlayer != nil else { return }
        //: self.bgmPlayer?.stop()
        self.bgmPlayer?.bean()
        //: self.bgmPlayer = nil
        self.bgmPlayer = nil
    }
}

// MARK: - HideWrite【视频播放器】

//: extension MaleInviteCallView: TalkingVideoPlayerDelegate {
extension PainterUnder: HideWrite {
    //: func func__playerStatus(player: TalkingVideoPlayerManager, status: VideoPlayerStatus) {
    func manage(player: EqualPublisher, status: EventMap) {
        //: if status == .Playing {
        if status == .Playing {
            //: self.videoPlayer.setRenderMode(renderMode: .FILL_SCREEN)
            self.videoPlayer.against(renderMode: .FILL_SCREEN)
        }
    }

    //: func func__playerDuration(player: TalkingVideoPlayerManager, duration: Int, currentTime: Int) {}
    func manualBy(player: EqualPublisher, duration: Int, currentTime: Int) {}

    //: func func__playerProgress(player: TalkingVideoPlayerManager, progress: CGFloat) {}
    func agentMust(player: EqualPublisher, progress: CGFloat) {}
}

// MARK: - Layout

//: extension MaleInviteCallView {
extension PainterUnder {
    /// 刷新UI
    //: private func refreshUI() {
    private func modelBy() {
        //: if self.popView == nil {
        if self.popView == nil {
            //: self.popView = TalkingPopView.init(frame: UIScreen.main.bounds)
            self.popView = ImmediateSuccess(frame: UIScreen.main.bounds)
            //: self.popView?.initWithView(view: self)
            self.popView?.re(view: self)
            //: self.popView?.showInView(view: MovementStructure.getWindow())
            self.popView?.extraBag(view: MovementStructure.levelScientific())
            //: self.popView?.isRemoveTapGes = true
            self.popView?.isRemoveTapGes = true
        }
        //: setupSubViewsConstraint()
        graphicRecommend()
        //: if timeoutTimer == nil {
        if timeoutTimer == nil {
            //: var countdown = 30
            var countdown = 30
            //: timeoutTimer = Timer.scheduledTimer(withTimeInterval: 1, repeats: true, block: { [weak self] _ in
            timeoutTimer = Timer.scheduledTimer(withTimeInterval: 1, repeats: true, block: { [weak self] _ in
                //: guard let self = self else { return }
                guard let self = self else { return }
                //: self.svgaNumLab.text = "\(countdown)"
                self.svgaNumLab.text = "\(countdown)"
                //: countdown -= 1
                countdown -= 1
                //: if countdown <= 0 {
                if countdown <= 0 {
                    //: self.dismiss()
                    self.smartPublisher()
                }
                //: })
            })
            //: timeoutTimer?.fire()
            timeoutTimer?.fire()
        }
        //: setUpBGMAndStartPlayer()
        beforeFieldTotal()
        //: bgImgView.setUrlImage(urlStr: callHeadPic)
        bgImgView.poolFee(urlStr: callHeadPic)

        //: if !myVideoUrl.isEmpty {
        if !myVideoUrl.isEmpty {
            //: videoPlayer.delegate = self
            videoPlayer.delegate = self
            //: videoPlayer.playerWithUrlAndVideoView(url: self.myVideoUrl, view: self.bgImgView)
            videoPlayer.roundHost(url: self.myVideoUrl, view: self.bgImgView)
            //: } else {
        } else {
            //: videoPlayer.stopPlay()
            videoPlayer.circleDecision()
            //: videoPlayer.removeVideoWidget()
            videoPlayer.holder()
        }

        //: hangupBtn.isHidden = false
        hangupBtn.isHidden = false
        //: acceptBtn.isHidden = false
        acceptBtn.isHidden = false
        //: freeImg.isHidden = !(MarginExamineer.share.loginUserMode.freeCallTimes > 0)
        freeImg.isHidden = !(MarginExamineer.share.loginUserMode.freeCallTimes > 0)
    }

    /// 添加约束
    //: private func setupSubViewsConstraint() {
    private func graphicRecommend() {
        //: bgImgView.snp.remakeConstraints { make in
        bgImgView.snp.remakeConstraints { make in
            //: make.center.equalToSuperview()
            make.center.equalToSuperview()
            //: make.size.equalTo(CGSize(width: actualWidth(w: 279), height: actualWidth(w: 346)))
            make.size.equalTo(CGSize(width: actualWidth(w: 279), height: actualWidth(w: 346)))
        }

        //: maskTopImgV.snp.remakeConstraints { make in
        maskTopImgV.snp.remakeConstraints { make in
            //: make.leading.trailing.top.equalToSuperview()
            make.leading.trailing.top.equalToSuperview()
            //: make.height.equalTo(actualWidth(w: 70))
            make.height.equalTo(actualWidth(w: 70))
        }

        //: svgaPlayer.snp.makeConstraints { make in
        svgaPlayer.snp.makeConstraints { make in
            //: make.leading.top.equalTo(actualWidth(w: 20))
            make.leading.top.equalTo(actualWidth(w: 20))
            //: make.width.height.equalTo(40)
            make.width.height.equalTo(40)
        }

        //: svgaNumLab.snp.makeConstraints { make in
        svgaNumLab.snp.makeConstraints { make in
            //: make.center.width.height.equalTo(svgaPlayer)
            make.center.width.height.equalTo(svgaPlayer)
        }

        //: titleLab.snp.remakeConstraints { make in
        titleLab.snp.remakeConstraints { make in
            //: make.centerY.equalTo(svgaPlayer)
            make.centerY.equalTo(svgaPlayer)
            //: make.leading.equalTo(svgaPlayer.snp.trailing).offset(actualWidth(w: 10))
            make.leading.equalTo(svgaPlayer.snp.trailing).offset(actualWidth(w: 10))
            //: make.trailing.equalTo(-actualWidth(w: 20))
            make.trailing.equalTo(-actualWidth(w: 20))
        }

        //: maskBottomImgV.snp.remakeConstraints { make in
        maskBottomImgV.snp.remakeConstraints { make in
            //: make.leading.trailing.bottom.equalToSuperview()
            make.leading.trailing.bottom.equalToSuperview()
            //: make.height.equalTo(maskTopImgV)
            make.height.equalTo(maskTopImgV)
        }

        //: hangupBtn.snp.remakeConstraints { make in
        hangupBtn.snp.remakeConstraints { make in
            //: make.leading.equalTo(actualWidth(w: 20))
            make.leading.equalTo(actualWidth(w: 20))
            //: make.bottom.equalTo(-actualWidth(w: 40))
            make.bottom.equalTo(-actualWidth(w: 40))
            //: make.size.equalTo(actualWidth(w: 56))
            make.size.equalTo(actualWidth(w: 56))
        }

        //: acceptBtn.snp.remakeConstraints { make in
        acceptBtn.snp.remakeConstraints { make in
            //: make.trailing.equalTo(-actualWidth(w: 20))
            make.trailing.equalTo(-actualWidth(w: 20))
            //: make.centerY.equalTo(hangupBtn)
            make.centerY.equalTo(hangupBtn)
            //: make.size.equalTo(actualWidth(w: 70))
            make.size.equalTo(actualWidth(w: 70))
        }

        //: freeImg.snp.makeConstraints { make in
        freeImg.snp.makeConstraints { make in
            //: make.trailing.equalTo(acceptBtn).offset(actualWidth(w: 2))
            make.trailing.equalTo(acceptBtn).offset(actualWidth(w: 2))
            //: make.top.equalTo(acceptBtn).offset(actualWidth(w: -8))
            make.top.equalTo(acceptBtn).offset(actualWidth(w: -8))
            //: make.size.equalTo(CGSize(width: actualWidth(w: 41), height: actualWidth(w: 28)))
            make.size.equalTo(CGSize(width: actualWidth(w: 41), height: actualWidth(w: 28)))
        }
    }
}
