
//: Declare String Begin

/*: "init(coder:) has not been implemented" :*/
fileprivate let show_calledRecentTitle:[UInt8] = [0x66,0x61,0x66,0x7b,0x27,0x6c,0x60,0x6b,0x6a,0x7d,0x35,0x26,0x2f,0x67,0x6e,0x7c,0x2f,0x61,0x60,0x7b,0x2f,0x6d,0x6a,0x6a,0x61,0x2f,0x66,0x62,0x7f,0x63,0x6a,0x62,0x6a,0x61,0x7b,0x6a,0x6b]

/*: "uid" :*/
fileprivate let user_randomString:[UInt8] = [0xdd,0xc1,0xcc]

private func businessPartner(manage num: UInt8) -> UInt8 {
    return num ^ 168
}

/*: "headPic" :*/
fileprivate let main_identifyGnatStr:[Character] = ["h","e","a","d","P","i","c"]

/*: "nickname" :*/
fileprivate let constListenKey:[Character] = ["n","i","c","k","n","a","m","e"]

/*: "birthday" :*/
fileprivate let dataBothMsg:String = "requestir"
fileprivate let userHourShrimpStr:[Character] = ["t","h","d","a","y"]

/*: "videoUrl" :*/
fileprivate let mainAroundKey:[Character] = ["v","i","d","e","o","U","r","l"]

/*: "location" :*/
fileprivate let appBoyTitle:[Character] = ["l","o","c","a","t","i"]
fileprivate let main_anotherStr:[Character] = ["o","n"]

/*: "cmd" :*/
fileprivate let user_shouldId:[UInt8] = [0x58,0x56,0x5f]

/*: "refuseCall" :*/
fileprivate let constQueenName:[Character] = ["r","e","f","u","s","e","C","a"]
fileprivate let appMultipleUrl:String = "thinthin"

/*: "data" :*/
fileprivate let k_boardName:[UInt8] = [0x61,0x74,0x61,0x64]

/*: "Call interruption" :*/
fileprivate let app_poloMedalFormat:String = "adjustment awake never distributionCall inte"
fileprivate let const_anonymousPath:String = "legu"
fileprivate let show_deviceTitle:[Character] = ["p","t","i","o","n"]

/*: "stopCall" :*/
fileprivate let const_enjoyPassName:[Character] = ["s","t","o","p","C","a","l","l"]

/*: "logId" :*/
fileprivate let data_draftComplaintMsg:[UInt8] = [0xc8,0xcb,0xc3,0xed,0xc0]

/*: "cancelCall" :*/
fileprivate let dataMinimizeName:[Character] = ["c","a","n"]
fileprivate let appShadeName:[Character] = ["c","e","l","C","a","l","l"]

/*: "No answer" :*/
fileprivate let const_pickName:String = "element tip baseNo an"

/*: "duration" :*/
fileprivate let showCollectionLackFirstStr:String = "duratdraftn"

/*: "She has hung up" :*/
fileprivate let constDiscloseCleanMessage:[Character] = ["S","h","e"," ","h","a","s"]
fileprivate let const_divideMixKey:[Character] = [" ","h","u","n","g"," ","u","p"]

/*: "cancelSex" :*/
fileprivate let main_beyondStr:[Character] = ["c","a","n","c","e","l","S","e","x"]

/*: "He has hung up" :*/
fileprivate let k_harassmentProjectionName:String = "need dimension sub translationHe has "
fileprivate let app_requireValue:[Character] = ["h","u","n","g"," ","u","p"]

/*: "endType" :*/
fileprivate let kOwnerBlocDisappearStr:[Character] = ["e","n","d","T","y","p","e"]

/*: "stopSex" :*/
fileprivate let showThinString:[Character] = ["s","t","o","p","S","e","x"]

/*: "Insufficient gold coins, call has ended" :*/
fileprivate let app_alreadyData:[UInt8] = [0x7f,0x58,0x45,0x43,0x50,0x50,0x5f,0x55,0x5f,0x53,0x58,0x42,0x16,0x51,0x59,0x5a,0x52,0x16,0x55,0x59,0x5f,0x58,0x45,0x1a,0x16,0x55,0x57,0x5a,0x5a,0x16,0x5e,0x57,0x45,0x16,0x53,0x58,0x52,0x53,0x52]

private func giftRemove(vertical num: UInt8) -> UInt8 {
    return num ^ 54
}

/*: "The other hung up the phone" :*/
fileprivate let user_saleFormat:[Character] = ["T","h","e"," ","o","t","h","e","r"," ","h"]
fileprivate let kAchingAyeMessage:String = "ucircle"
fileprivate let appProduceEmbraceName:String = "lack compareg up"
fileprivate let const_forwardMixRemoteValue:String = "hosun"

/*: "type" :*/
fileprivate let app_heapValue:[Character] = ["t","y","p","e"]

/*: "jumpUrl" :*/
fileprivate let k_assistantUrl:[Character] = ["j","u","m","p","U","r"]
fileprivate let noti_topicUndertakeFormat:[Character] = ["l"]

/*: "Cancel" :*/
fileprivate let user_signatureClipTitle:String = "Cancenearby scene"
fileprivate let noti_masterMsg:[Character] = ["l"]

/*: "Get gold coins" :*/
fileprivate let showConnectionId:[Character] = ["G","e","t"," ","g","o"]
fileprivate let kAccessString:String = "boyfriend key start cloudld c"

/*: "refuseSex" :*/
fileprivate let userAyeMsg:String = "quick mobilerefuseSe"
fileprivate let user_piValue:String = "compare"

/*: "onAcceptCall" :*/
fileprivate let noti_restaurantMsg:[Character] = ["o","n"]
fileprivate let appDirectionTitle:[Character] = ["A","c","c","e","p","t","C","a","l","l"]

/*: "acceptCall" :*/
fileprivate let userMobileKey:String = "acovercovere"
fileprivate let showHungUnderlyUrl:String = "ptCallclass hang output"

/*: "Calls are cancelled" :*/
fileprivate let noti_middleMsg:[Character] = ["C","a","l","l","s"," "]
fileprivate let const_editKey:String = "are camic currently"

/*: "The call has ended" :*/
fileprivate let main_processorKey:String = "The ting volume origin writing"
fileprivate let user_naturalString:String = "s endedconvert agile"

//: Declare String End

// __DEBUG__
// __CLOSE_PRINT__
//
//  InserterStorage.swift
//  AbroadTalking
//
//  Created by young on 2022/11/11.
//

//: import CallKit.CXCallObserver
import CallKit.CXCallObserver
//: import UIKit
import UIKit

// 通话监听(必须为全局属性，否则无法接听)
//: let voiceCallHelper = CXCallObserver()
let notiSegmentStr = CXCallObserver()
//: class TalkingVoiceChatViewController: TalkingBaseViewController {
class InserterStorage: EasyChainSensor {
    //: var chatModel: TalkingVideoChatModel?
    var chatModel: IconModel? // 语音通话模型
    //: private var player: TalkingCallBGMPlayer?
    private var player: VerticalName? // 拨打中背景音乐
    //: private var timeOutTimer: Timer?
    private var timeOutTimer: Timer? // 未接听超时定时器
    //: private var expiredDate = Date()
    private var expiredDate = Date() // 失效时间
    //: private var miniView: TalkingVoiceMiniView?
    private var miniView: AttachHalfSegment? // 音频通话小窗口

    //: override init(nibName nibNameOrNil: String?, bundle nibBundleOrNil: Bundle?) {
    override init(nibName nibNameOrNil: String?, bundle nibBundleOrNil: Bundle?) {
        //: super.init(nibName: nil, bundle: nil)
        super.init(nibName: nil, bundle: nil)
        // 提前绑定delegate
        //: TalkingSocketManager.shared.callerDelegate = self
        MaterialCurrency.shared.callerDelegate = self
        //: TalkingSocketManager.shared.calledPartyDelegate = self
        MaterialCurrency.shared.calledPartyDelegate = self
        //: TalkingSocketManager.shared.errorDelegate = self
        MaterialCurrency.shared.errorDelegate = self
    }

    //: required init?(coder: NSCoder) {
    required init?(coder: NSCoder) {
        //: fatalError("init(coder:) has not been implemented")
        fatalError(String(bytes: show_calledRecentTitle.map{$0^15}, encoding: .utf8)!)
    }

    //: override func viewDidAppear(_ animated: Bool) {
    override func viewDidAppear(_ animated: Bool) {
        //: super.viewDidAppear(animated)
        super.viewDidAppear(animated)
        //: let nav = self.navigationController as! TalkingNavigationController
        let nav = self.navigationController as! FloorEvolution
        //: nav.protectedPushVc = nil
        nav.protectedPushVc = nil

        //: UIApplication.shared.isIdleTimerDisabled = true
        UIApplication.shared.isIdleTimerDisabled = true // 设备不会进入休眠状态
        //: self.startBGM()
        self.basicSubmit()
        //: if self.chatModel?.videoStage == .Recieved {
        if self.chatModel?.videoStage == .Recieved { // 接收方验证权限
            //: checkVoiceAuth()
            memory()
        }
    }

    //: override func viewWillAppear(_ animated: Bool) {
    override func viewWillAppear(_ animated: Bool) {
        //: super.viewWillAppear(animated)
        super.viewWillAppear(animated)
        //: if self.miniView != nil {
        if self.miniView != nil { // 移除小窗口
            //: self.miniView?.removeFromSuperview()
            self.miniView?.removeFromSuperview()
            //: self.miniView = nil
            self.miniView = nil
            //: TalkingSocketManager.shared.pacificVC = nil
            MaterialCurrency.shared.pacificVC = nil
        }
        //: interactionView.initCachEffectData()
        interactionView.stopCurrency()
    }

    //: override func viewDidLoad() {
    override func viewDidLoad() {
        //: super.viewDidLoad()
        super.viewDidLoad()
        //: self.hideNavi = true
        self.hideNavi = true
        //: setupSubviews()
        equally()
        //: setupSubViewsConstraint()
        guideWith()
        //: initNotificationObserver()
        everySlow()
        //: req_callGetUserInfo()
        clipAcross()
        //: timeoutRules()
        noticeThroughTotal()
        //: resumeTalk()
        betweenYellow()
        //: setUpBGMPlayer()
        tagFor()
    }

    //: override func loadView() {
    override func loadView() {
        //: if #available(iOS 13.2, *) {
        if #available(iOS 13.2, *) {
            //: if MarginExamineer.share.appConfigMode.disableShootScreen {
            if MarginExamineer.share.appConfigMode.disableShootScreen {
                //: super.loadView()
                super.loadView()
                //: } else {
            } else {
                //: let bgView = MAScreenShieldView(frame: CGRect(x: 0, y: 0, width: ScreenWidth, height: ScreenHeight))
                let bgView = OccasionAll(frame: CGRect(x: 0, y: 0, width: show_errorChangeFormat, height: appLoadMessage))
                //: self.view = bgView
                self.view = bgView
            }
            //: } else {
        } else {
            //: super.loadView()
            super.loadView()
        }
    }

    //: deinit {
    deinit {
        //: UIApplication.shared.isIdleTimerDisabled = false
        UIApplication.shared.isIdleTimerDisabled = false
        //: NotificationCenter.default.removeObserver(self)
        NotificationCenter.default.removeObserver(self)
        //: TalkingSocketManager.shared.pacific = false
        MaterialCurrency.shared.pacific = false
        //: TalkingSocketManager.shared.isCalling = false
        MaterialCurrency.shared.isCalling = false
        //: self.stopVCAllActivties()
        self.lineRoot()
    }

    // MARK: - Lazy Load

    /// 音频通话操作按钮
    //: private lazy var interactionView: Talking1v1VoiceInteractionView = {
    private lazy var interactionView: LocationView = {
        //: let v = Talking1v1VoiceInteractionView(frame: .zero, chatModel: self.chatModel)
        let v = LocationView(frame: .zero, chatModel: self.chatModel)
        //: v.delegate = self
        v.delegate = self
        //: return v
        return v
        //: }()
    }()

    //: private lazy var voiceManager: TalkingVoiceChatManager = {
    private lazy var voiceManager: UserPainter = {
        //: let m = TalkingVoiceChatManager()
        let m = UserPainter()
        //: m.handsfreeBtn = self.interactionView.handsfreeBtn
        m.handsfreeBtn = self.interactionView.handsfreeBtn
        //: return m
        return m
        //: }()
    }()
}

// MARK: - Request & 数据处理

//: extension TalkingVoiceChatViewController {
extension InserterStorage {
    /// 查询接收者信息
    //: func req_callGetUserInfo() {
    func clipAcross() {
        //: guard let pairUid = chatModel?.pairUid else { return }
        guard let pairUid = chatModel?.pairUid else { return }
        //: TalkingVideoChatManager.req_callGetUserInfo(uid: pairUid) { [weak self] succeed, result, errorModel in
        ImplementSpecify.analyze(uid: pairUid) { [weak self] succeed, result, errorModel in
            //: guard succeed == true else { return }
            guard succeed == true else { return }
            //: guard let self = self else { return }
            guard let self = self else { return }

            //: let json = JSON(result ?? [String: Any]())
            let json = JSON(result ?? [String: Any]())

            //: self.chatModel?.pairUid = json["uid"].stringValue
            self.chatModel?.pairUid = json[String(bytes: user_randomString.map{businessPartner(manage: $0)}, encoding: .utf8)!].stringValue
            //: self.chatModel?.pairHeadPic = json["headPic"].stringValue
            self.chatModel?.pairHeadPic = json[(String(main_identifyGnatStr))].stringValue
            //: self.chatModel?.pairNickName = json["nickname"].stringValue
            self.chatModel?.pairNickName = json[(String(constListenKey))].stringValue
            //: self.chatModel?.pairBirthday = json["birthday"].stringValue
            self.chatModel?.pairBirthday = json[(dataBothMsg.replacingOccurrences(of: "request", with: "b") + String(userHourShrimpStr))].stringValue
            //: self.chatModel?.videoUrl = json["videoUrl"].stringValue
            self.chatModel?.videoUrl = json[(String(mainAroundKey))].stringValue
            //: self.chatModel?.location = json["location"].stringValue
            self.chatModel?.location = json[(String(appBoyTitle) + String(main_anotherStr))].stringValue

            // 刷新通话视图
            //: self.interactionView.afterGetInfo()
            self.interactionView.destroyView()
        }
    }
}

// MARK: - Event

//: extension TalkingVoiceChatViewController {
extension InserterStorage {
    /// 权限校验，播放BGM
    //: private func checkVoiceAuth() {
    private func memory() {
        //: TalkingPermissionTool.func__openRecordServiceWithBlock(true) { isOpen in
        MultiplePoint.access(true) { isOpen in
            //: guard isOpen == true else {
            guard isOpen == true else {
                //: self.func__showStatusBarErrorMsg(showMsg: kMessage_permissions_error)
                self.exitMarker(showMsg: k_eventControlUrl)
                //: TalkingSocketManager.shared.sendMessage(info: ["cmd": "refuseCall", "data": ["uid": self.chatModel?.pairUid]])
                MaterialCurrency.shared.warningVersion(info: [String(bytes: user_shouldId.map{$0^59}, encoding: .utf8)!: (String(constQueenName) + appMultipleUrl.replacingOccurrences(of: "thin", with: "l")), String(bytes: k_boardName.reversed(), encoding: .utf8)!: [String(bytes: user_randomString.map{businessPartner(manage: $0)}, encoding: .utf8)!: self.chatModel?.pairUid]])
                //: self.popCurrentViewController()
                self.globalToLimit()
                //: return
                return
            }

            //: self.stopBGM()
            self.equipment()
            //: self.player = TalkingCallBGMPlayer.callBGMPlayer(type: .Response)
            self.player = VerticalName.arrayEach(type: .Response)
            //: self.startBGM()
            self.basicSubmit()
        }
    }

    /// 释放当前页面所有资源
    //: private func stopVCAllActivties() {
    private func lineRoot() {
        //: if self.timeOutTimer != nil {
        if self.timeOutTimer != nil {
            //: self.timeOutTimer?.invalidate()
            self.timeOutTimer?.invalidate()
            //: self.timeOutTimer = nil
            self.timeOutTimer = nil
        }

        //: self.stopBGM()
        self.equipment()
        //: TalkingSocketManager.shared.callerDelegate = nil
        MaterialCurrency.shared.callerDelegate = nil
        //: TalkingSocketManager.shared.calledPartyDelegate = nil
        MaterialCurrency.shared.calledPartyDelegate = nil
        //: TalkingSocketManager.shared.errorDelegate = nil
        MaterialCurrency.shared.errorDelegate = nil
    }

    // 将当前视图从栈中移除
    //: private func popCurrentViewController(animated: Bool = true) {
    private func globalToLimit(animated: Bool = true) {
        //: self.stopBGM()
        self.equipment()

        //: if self.navigationController?.topViewController == self {
        if self.navigationController?.topViewController == self {
            //: self.navigationController?.popViewController(animated: animated)
            self.navigationController?.popViewController(animated: animated)
            //: } else {
        } else {
            //: if var vcArr = self.navigationController?.viewControllers {
            if var vcArr = self.navigationController?.viewControllers {
                //: if let index = vcArr.firstIndex(of: self) {
                if let index = vcArr.firstIndex(of: self) {
                    //: vcArr.remove(at: index)
                    vcArr.remove(at: index)
                    //: self.navigationController?.setViewControllers(vcArr, animated: false)
                    self.navigationController?.setViewControllers(vcArr, animated: false)
                }
            }
        }
    }

    /// 创建音频通话房间
    //: private func createTalkRoom() {
    private func implementation() {
        //: self.voiceManager.createRoomAndStartPublish(roomId: self.chatModel!.logId)
        self.voiceManager.refuseAdvertising(roomId: self.chatModel!.logId)
    }

    /// 通话重连
    //: private func resumeTalk() {
    private func betweenYellow() {
        //: if self.chatModel?.resumeTalk == true {
        if self.chatModel?.resumeTalk == true {
            //: createTalkRoom()
            implementation()
        }
    }
}

// MARK: - 通知

//: extension TalkingVoiceChatViewController {
extension InserterStorage {
    /// 初始化通知
    //: private func initNotificationObserver() {
    private func everySlow() {
        // 监听 App 从后台切前台
        //: NotificationCenter.default.addObserver(self,
        NotificationCenter.default.addObserver(self,
                                               //: selector: #selector(appWillEnterForeground(notif:)),
                                               selector: #selector(script(notif:)),
                                               //: name: UIApplication.willEnterForegroundNotification,
                                               name: UIApplication.willEnterForegroundNotification,
                                               //: object: nil)
                                               object: nil)

        // socket断网重连监听
        //: NotificationCenter.default.addObserver(self,
        NotificationCenter.default.addObserver(self,
                                               //: selector: #selector(reachabilityChanged(note:)),
                                               selector: #selector(burnErr(note:)),
                                               //: name: SOCKET_IS_OPEN_NOTIFICATION,
                                               name: userVisitMessage,
                                               //: object: nil)
                                               object: nil)

        //: voiceCallHelper.setDelegate(self, queue: DispatchQueue.main)
        notiSegmentStr.setDelegate(self, queue: DispatchQueue.main)
    }

    /// app进入前台
    //: @objc private func appWillEnterForeground(notif: Notification) {
    @objc private func script(notif: Notification) {
        //: guard self.chatModel?.videoStage != .Calling else { return }
        guard self.chatModel?.videoStage != .Calling else { return }
        //: let date = Date()
        let date = Date()
        //: let interval = date.timeIntervalSince(self.expiredDate)
        let interval = date.timeIntervalSince(self.expiredDate)
        //: if interval >= 0 {
        if interval >= 0 {
            //: popCurrentViewController()
            globalToLimit()
        }
    }

    /// 断网重连socket
    //: @objc private func reachabilityChanged(note: Notification) {
    @objc private func burnErr(note: Notification) {
        //: guard TalkingSocketManager.shared.needReconnect == true else { return }
        guard MaterialCurrency.shared.needReconnect == true else { return }
        //: TalkingSocketManager.shared.callerDelegate = self
        MaterialCurrency.shared.callerDelegate = self
        //: TalkingSocketManager.shared.calledPartyDelegate = self
        MaterialCurrency.shared.calledPartyDelegate = self
        //: TalkingSocketManager.shared.errorDelegate = self
        MaterialCurrency.shared.errorDelegate = self
        //: self.voiceManager.restartTalkWhenDisconnectedAndNetAvailable(roomId: self.chatModel?.logId ?? 0)
        self.voiceManager.outWindow(roomId: self.chatModel?.logId ?? 0)
    }
}

// MARK: - CXCallObserverDelegate【通话监听】

//: extension TalkingVoiceChatViewController: CXCallObserverDelegate {
extension InserterStorage: CXCallObserverDelegate {
    //: func callObserver(_ callObserver: CXCallObserver, callChanged call: CXCall) {
    func callObserver(_ callObserver: CXCallObserver, callChanged call: CXCall) {
        //: if call.hasConnected == true {
        if call.hasConnected == true { // 通话中
            //: self.func__showStatusBarErrorMsg(showMsg: "Call interruption".localized)
            self.exitMarker(showMsg: (String(app_poloMedalFormat.suffix(9)) + const_anonymousPath.replacingOccurrences(of: "leg", with: "rr") + String(show_deviceTitle)).localized)
            //: switch(self.chatModel?.videoStage) {
            switch self.chatModel?.videoStage {
            //: case .Calling:
            case .Calling:
                //: TalkingSocketManager.shared.sendMessage(info: ["cmd": "stopCall", "data": ["logId": self.chatModel?.logId]])
                MaterialCurrency.shared.warningVersion(info: [String(bytes: user_shouldId.map{$0^59}, encoding: .utf8)!: (String(const_enjoyPassName)), String(bytes: k_boardName.reversed(), encoding: .utf8)!: [String(bytes: data_draftComplaintMsg.map{$0^164}, encoding: .utf8)!: self.chatModel?.logId]])
            //: case .Waiting:
            case .Waiting:
                //: TalkingSocketManager.shared.sendMessage(info: ["cmd": "cancelCall", "data": ["logId": self.chatModel?.logId]])
                MaterialCurrency.shared.warningVersion(info: [String(bytes: user_shouldId.map{$0^59}, encoding: .utf8)!: (String(dataMinimizeName) + String(appShadeName)), String(bytes: k_boardName.reversed(), encoding: .utf8)!: [String(bytes: data_draftComplaintMsg.map{$0^164}, encoding: .utf8)!: self.chatModel?.logId]])
            //: case .Recieved:
            case .Recieved:
                //: TalkingSocketManager.shared.sendMessage(info: ["cmd": "refuseCall", "data": ["uid": self.chatModel?.pairUid]])
                MaterialCurrency.shared.warningVersion(info: [String(bytes: user_shouldId.map{$0^59}, encoding: .utf8)!: (String(constQueenName) + appMultipleUrl.replacingOccurrences(of: "thin", with: "l")), String(bytes: k_boardName.reversed(), encoding: .utf8)!: [String(bytes: user_randomString.map{businessPartner(manage: $0)}, encoding: .utf8)!: self.chatModel?.pairUid]])
            //: case .none:
            case .none:
                //: break
                break
            }

            //: if miniView != nil {
            if miniView != nil {
                //: self.miniView?.dismissWhenCallEnd()
                self.miniView?.pause()
                //: TalkingSocketManager.shared.pacificVC = nil
                MaterialCurrency.shared.pacificVC = nil
            }
            //: popCurrentViewController()
            globalToLimit()

            //: } else if call.hasEnded == true || call.isOnHold == true {
        } else if call.hasEnded == true || call.isOnHold == true { // 电话挂断 || 无人接听挂断
            //: self.voiceManager.enableAudioModule(enable: true)
            self.voiceManager.exception(enable: true)
        }
    }
}

// MARK: - 通话音乐

//: extension TalkingVoiceChatViewController {
extension InserterStorage {
    /// 初始化音频通话背景音乐
    //: private func setUpBGMPlayer() {
    private func tagFor() {
        //: guard self.chatModel?.videoStage != .Calling else { return }
        guard self.chatModel?.videoStage != .Calling else { return }

        //: let type = self.chatModel!.isCaller == true ? CallBGMPlayerType.Call : CallBGMPlayerType.Response
        let type = self.chatModel!.isCaller == true ? RoundingDepth.Call : RoundingDepth.Response
        //: self.player = TalkingCallBGMPlayer.callBGMPlayer(type: type)
        self.player = VerticalName.arrayEach(type: type)
    }

    /// 开始播放背景音乐
    //: private func startBGM() {
    private func basicSubmit() {
        //: guard self.chatModel?.videoStage != .Calling else { return }
        guard self.chatModel?.videoStage != .Calling else { return }
        //: guard self.chatModel?.resumeTalk != true else { return }
        guard self.chatModel?.resumeTalk != true else { return }

        //: self.player?.play()
        self.player?.same()
    }

    /// 停止播放背景音乐
    //: private func stopBGM() {
    private func equipment() {
        //: guard self.player != nil else { return }
        guard self.player != nil else { return }
        //: self.player?.stop()
        self.player?.bean()
        //: self.player = nil
        self.player = nil
    }
}

// MARK: - 定时器

//: extension TalkingVoiceChatViewController {
extension InserterStorage {
    /// 超时定时器
    //: private func timeoutRules() {
    private func noticeThroughTotal() {
        //: guard self.chatModel?.videoStage != .Calling else { return }
        guard self.chatModel?.videoStage != .Calling else { return }

        //: let timeout = self.chatModel?.isCaller == true ? VIDEO_1v1_CALL_TIMEOUT : self.chatModel?.remainAnswerCallTime()
        let timeout = self.chatModel?.isCaller == true ? showDelayFlexibleTitle : self.chatModel?.mp()
        //: if timeout! <= 0 {
        if timeout! <= 0 {
            //: buildTalkTimeOutAction()
            levelPure()
        }
        //: self.timeOutTimer = Timer.scheduledTimer(withTimeInterval: timeout!, block: { [weak self] timer in
        self.timeOutTimer = Timer.scheduledTimer(withTimeInterval: timeout!, block: { [weak self] timer in
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: self.buildTalkTimeOutAction()
            self.levelPure()
            //: }, repeats: false)
        }, repeats: false)

        //: self.expiredDate = Date().addingTimeInterval(60)
        self.expiredDate = Date().addingTimeInterval(60)
    }

    /// 倒计时超时逻辑处理
    //: private func buildTalkTimeOutAction() {
    private func levelPure() {
        //: guard !TalkingSocketManager.shared.pacific else { return }
        guard !MaterialCurrency.shared.pacific else { return }

        //: var socketDict: [String: Any]
        var socketDict: [String: Any]
        //: if self.chatModel?.isCaller == true {
        if self.chatModel?.isCaller == true { // 拨打方
            //: self.func__showStatusBarErrorMsg(showMsg: "No answer".localized)
            self.exitMarker(showMsg: (String(const_pickName.suffix(5)) + "swer").localized)
            //: socketDict = ["cmd": "cancelCall", "data": ["logId": self.chatModel?.logId]]
            socketDict = [String(bytes: user_shouldId.map{$0^59}, encoding: .utf8)!: (String(dataMinimizeName) + String(appShadeName)), String(bytes: k_boardName.reversed(), encoding: .utf8)!: [String(bytes: data_draftComplaintMsg.map{$0^164}, encoding: .utf8)!: self.chatModel?.logId]]
            //: } else {
        } else { // 接收方
            //: socketDict = ["cmd": "refuseCall", "data": ["uid": self.chatModel?.pairUid]]
            socketDict = [String(bytes: user_shouldId.map{$0^59}, encoding: .utf8)!: (String(constQueenName) + appMultipleUrl.replacingOccurrences(of: "thin", with: "l")), String(bytes: k_boardName.reversed(), encoding: .utf8)!: [String(bytes: user_randomString.map{businessPartner(manage: $0)}, encoding: .utf8)!: self.chatModel?.pairUid]]
        }
        //: TalkingSocketManager.shared.sendMessage(info: socketDict)
        MaterialCurrency.shared.warningVersion(info: socketDict)
        //: popCurrentViewController()
        globalToLimit()
    }
}

// MARK: - resume talk【恢复通话】

//: extension TalkingVoiceChatViewController {
extension InserterStorage {
    /// 恢复音频通话，更新chatModel
    /// - Parameter resumeData: 数据
    //: func updateInfoViaResumeTalkData(resumeData: [String: Any]) {
    func agreeWithoutLine(resumeData: [String: Any]) {
        //: let json = JSON(resumeData)
        let json = JSON(resumeData)
        //: let logId = json["logId"].intValue
        let logId = json[String(bytes: data_draftComplaintMsg.map{$0^164}, encoding: .utf8)!].intValue
        //: let duration = json["duration"].intValue
        let duration = json[(showCollectionLackFirstStr.replacingOccurrences(of: "draft", with: "io"))].intValue
        //: let interval = Date().timeIntervalSince1970
        let interval = Date().timeIntervalSince1970
        //: let startTalkTime = floor(interval) - Double(duration)
        let startTalkTime = floor(interval) - Double(duration)
        //: self.chatModel?.startTime = startTalkTime
        self.chatModel?.startTime = startTalkTime
        //: self.interactionView.updateStartTime(startTime: startTalkTime)
        self.interactionView.borderBit(startTime: startTalkTime)

        //: if self.chatModel?.videoStage != .Calling {
        if self.chatModel?.videoStage != .Calling {
            //: self.chatModel?.logId = logId
            self.chatModel?.logId = logId
            //: self.chatModel?.videoStage = .Calling
            self.chatModel?.videoStage = .Calling
            //: self.interactionView.showViewsViaStage()
            self.interactionView.blockTrigger()
            //: createTalkRoom()
            implementation()
            //: self.stopBGM()
            self.equipment()
        }
    }
}

// MARK: - ThemeMenu【拨打方】

//: extension TalkingVoiceChatViewController: SocketManagerVideoChatCallerDelegate {
extension InserterStorage: ThemeMenu {
    /// 拨打方拒绝了通话，接收方收到该指令（未通话）
    //: func socket_videoChat_cancelCall(data: [String: Any]) {
    func verticalAgile(data: [String: Any]) {
        //: if self.miniView == nil {
        if self.miniView == nil {
            //: let json = JSON(data)
            let json = JSON(data)
            //: var typeStr = "She has hung up".localized
            var typeStr = (String(constDiscloseCleanMessage) + String(const_divideMixKey)).localized
            //: if json["cancelSex"].stringValue == Gender.male.rawValue {
            if json[(String(main_beyondStr))].stringValue == EnvironmentEmpty.male.rawValue {
                //: typeStr = "He has hung up".localized
                typeStr = (String(k_harassmentProjectionName.suffix(7)) + String(app_requireValue)).localized
            }
            //: self.func__showStatusBarErrorMsg(showMsg: typeStr)
            self.exitMarker(showMsg: typeStr)
            //: } else {
        } else {
            //: self.miniView?.dismissWhenCallEnd()
            self.miniView?.pause()
            //: TalkingSocketManager.shared.pacificVC = nil
            MaterialCurrency.shared.pacificVC = nil
        }

        //: popCurrentViewController()
        globalToLimit()
    }

    /// 对方接受了来电，建立音频通话
    //: func socket_videoChat_acceptCall(data: [String: Any]) {
    func exLayer(data: [String: Any]) {
        //: let json = JSON(data)
        let json = JSON(data)

        //: self.chatModel?.logId = json["logId"].intValue
        self.chatModel?.logId = json[String(bytes: data_draftComplaintMsg.map{$0^164}, encoding: .utf8)!].intValue
        //: self.chatModel?.videoStage = .Calling
        self.chatModel?.videoStage = .Calling
        //: self.chatModel?.renderPostion = .SelfOnWindow
        self.chatModel?.renderPostion = .SelfOnWindow

        //: self.stopBGM()
        self.equipment()
        //: self.interactionView.showViewsViaStage()
        self.interactionView.blockTrigger()
        //: self.createTalkRoom()
        self.implementation()
    }

    /// 对方结束了通话（通话中）
    //: func socket_videoChat_stopCall(data: [String: Any]) {
    func beforeReliability(data: [String: Any]) {
        //: let json = JSON(data)
        let json = JSON(data)

        //: let endType = json["endType"].intValue
        let endType = json[(String(kOwnerBlocDisappearStr))].intValue
        //: var typeStr = "She has hung up".localized
        var typeStr = (String(constDiscloseCleanMessage) + String(const_divideMixKey)).localized
        //: if json["stopSex"].stringValue == Gender.male.rawValue {
        if json[(String(showThinString))].stringValue == EnvironmentEmpty.male.rawValue {
            //: typeStr = "He has hung up".localized
            typeStr = (String(k_harassmentProjectionName.suffix(7)) + String(app_requireValue)).localized
        }

        //: if endType == 1 {
        if endType == 1 { // 付费方余额不足
            //: if MarginExamineer.share.appStatus == AppSkinStatus.normal.rawValue {
            if MarginExamineer.share.appStatus == AwayPush.normal.rawValue {
                //: typeStr = "Insufficient gold coins, call has ended".localized
                typeStr = String(bytes: app_alreadyData.map{giftRemove(vertical: $0)}, encoding: .utf8)!.localized
                //: } else {
            } else {
                //: typeStr = "The other hung up the phone".localized
                typeStr = (String(user_saleFormat) + kAchingAyeMessage.replacingOccurrences(of: "circle", with: "n") + String(appProduceEmbraceName.suffix(4)) + " the p" + const_forwardMixRemoteValue.replacingOccurrences(of: "sun", with: "ne")).localized
            }
        }
        //: if self.miniView == nil {
        if self.miniView == nil {
            //: self.func__showStatusBarErrorMsg(showMsg: typeStr)
            self.exitMarker(showMsg: typeStr)
            //: } else {
        } else {
            //: self.miniView?.dismissWhenCallEnd()
            self.miniView?.pause()
            //: TalkingSocketManager.shared.pacificVC = nil
            MaterialCurrency.shared.pacificVC = nil
        }

        //: self.interactionView.func_giftViewDismiss()
        self.interactionView.underLike()
        //: self.popCurrentViewController()
        self.globalToLimit()
        //: NotificationCenter.default.post(name: VIDEOCALL_END_CLOSEDISCOUNTS_NOTIFICATION, object: nil)
        NotificationCenter.default.post(name: const_uniqueString, object: nil)
    }

    /// 余额不足提醒
    //: func socket_videoChat_onMissCoin(msg: String, data: [String: Any]) {
    func distinctionIn(msg: String, data: [String: Any]) {
        //: guard MarginExamineer.share.appStatus == AppSkinStatus.normal.rawValue else { return }
        guard MarginExamineer.share.appStatus == AwayPush.normal.rawValue else { return }

        //: let index = data["type"] as? String ?? ""
        let index = data[(String(app_heapValue))] as? String ?? ""
        //: if index == "2" {
        if index == "2" {
            //: guard let jumpUrl = data["jumpUrl"] as? String  else { return }
            guard let jumpUrl = data[(String(k_assistantUrl) + String(noti_topicUndertakeFormat))] as? String else { return }
            //: let httpsUrl = MarginExamineer.share.appConfigMode.urlH5Domain+jumpUrl
            let httpsUrl = MarginExamineer.share.appConfigMode.urlH5Domain + jumpUrl
            //: var config = TalkingWebConfig.init()
            var config = ProvisionObserve()
            //: config.isDiscountsView = true
            config.isDiscountsView = true
            //: OriginMediumWill.share.func__pushToWebVC(urlStr: httpsUrl, webConfig: config)
            OriginMediumWill.share.anyCalled(urlStr: httpsUrl, webConfig: config)
            //: } else {
        } else {
            //: TalkingAlertShow.alert(title: nil,
            InputMight.untilPhone(title: nil,
                                        //: message: msg,
                                        message: msg,
                                        //: leftBtnTitle: "Cancel".localized,
                                        leftBtnTitle: (String(user_signatureClipTitle.prefix(5)) + String(noti_masterMsg)).localized,
                                        //: rightBtnTitle: "Get gold coins".localized) {
                                        rightBtnTitle: (String(showConnectionId) + String(kAccessString.suffix(4)) + "oins").localized)
            {
                //: TalkingAlertShow.hideAlert()
                InputMight.runningProjection()
                //: } rightBlock: {
            } rightBlock: {
                //: OriginMediumWill.share.func__jumpToWebRecharge(clickEvent: clickLess1minButton, sufficient: false)
                OriginMediumWill.share.resumeUniversal(clickEvent: data_consumptionStr, sufficient: false)
            }
        }
    }
}

// MARK: - MultipleMarkAmong【接收方】

//: extension TalkingVoiceChatViewController: SocketManagerVideoChatCalledPartyDelegate {
extension InserterStorage: MultipleMarkAmong {
    /// 接收方拒绝了通话，拨打方收到该指令（未通话）
    /// - Parameter data: 数据
    //: func socket_videoChat_refuseCall(data: [String: Any]) {
    func buttTar(data: [String: Any]) {
        //: if self.miniView == nil {
        if self.miniView == nil {
            //: let json = JSON(data)
            let json = JSON(data)
            //: var typeStr = "She has hung up".localized
            var typeStr = (String(constDiscloseCleanMessage) + String(const_divideMixKey)).localized
            //: if json["refuseSex"].stringValue == Gender.male.rawValue {
            if json[(String(userAyeMsg.suffix(8)) + user_piValue.replacingOccurrences(of: "compare", with: "x"))].stringValue == EnvironmentEmpty.male.rawValue {
                //: typeStr = "He has hung up".localized
                typeStr = (String(k_harassmentProjectionName.suffix(7)) + String(app_requireValue)).localized
            }
            //: self.func__showStatusBarErrorMsg(showMsg: typeStr)
            self.exitMarker(showMsg: typeStr)
            //: } else {
        } else {
            //: self.miniView?.dismissWhenCallEnd()
            self.miniView?.pause()
            //: TalkingSocketManager.shared.pacificVC = nil
            MaterialCurrency.shared.pacificVC = nil
        }
        //: popCurrentViewController()
        globalToLimit()
    }

    /// 接收方接受了通话，改变状态为通话中，acceptCall回执
    //: func socket_videoChat_onAcceptCall(data: [String: Any]) {
    func fragmentWithinCurrency(data: [String: Any]) {
        //: let json = JSON(data)
        let json = JSON(data)
        //: self.chatModel?.logId = json["logId"].intValue
        self.chatModel?.logId = json[String(bytes: data_draftComplaintMsg.map{$0^164}, encoding: .utf8)!].intValue
        //: self.chatModel?.videoStage = .Calling
        self.chatModel?.videoStage = .Calling
        //: self.chatModel?.renderPostion = .SelfOnWindow
        self.chatModel?.renderPostion = .SelfOnWindow
        //: self.stopBGM()
        self.equipment()
        //: self.interactionView.showViewsViaStage()
        self.interactionView.blockTrigger()
        //: self.createTalkRoom()
        self.implementation()
    }
}

// MARK: - GlobalSustainable【异常处理】

//: extension TalkingVoiceChatViewController: TalkingSocketManagerErrorDelegate {
extension InserterStorage: GlobalSustainable {
    // 处理error情况
    //: func socket_didRecieveError(errorNo: Int, cmd: String, msg: String, data: [String: Any]) {
    func connectTo(errorNo: Int, cmd: String, msg: String, data: [String: Any]) {
        //: if cmd == "refuseCall" { // 对方拒绝了通话
        if cmd == (String(constQueenName) + appMultipleUrl.replacingOccurrences(of: "thin", with: "l")) { // 对方拒绝了通话
            //: if self.miniView == nil {
            if self.miniView == nil {
                //: self.func__showStatusBarErrorMsg(showMsg: "No answer".localized)
                self.exitMarker(showMsg: (String(const_pickName.suffix(5)) + "swer").localized)
                //: } else {
            } else {
                //: self.miniView?.dismissWhenCallEnd()
                self.miniView?.pause()
                //: TalkingSocketManager.shared.pacificVC = nil
                MaterialCurrency.shared.pacificVC = nil
            }
            //: self.popCurrentViewController()
            self.globalToLimit()

            //: } else if cmd == "onAcceptCall" && errorNo == 101 { // 接听通话时，余额不足
        } else if cmd == (String(noti_restaurantMsg) + String(appDirectionTitle)) && errorNo == 101 { // 接听通话时，余额不足
            //: guard MarginExamineer.share.appStatus == AppSkinStatus.normal.rawValue else { return }
            guard MarginExamineer.share.appStatus == AwayPush.normal.rawValue else { return }
            //: OriginMediumWill.share.func__jumpToWebRecharge(sufficient: false)
            OriginMediumWill.share.resumeUniversal(sufficient: false)

            //: } else if cmd == "onAcceptCall" && errorNo == 102 { // 对方已取消通话
        } else if cmd == (String(noti_restaurantMsg) + String(appDirectionTitle)) && errorNo == 102 { // 对方已取消通话
            //: self.func__showStatusBarErrorMsg(showMsg: msg)
            self.exitMarker(showMsg: msg)
            //: self.popCurrentViewController()
            self.globalToLimit()
        }
    }
}

// MARK: - PerformPause【通话按钮操作事件】

//: extension TalkingVoiceChatViewController: Talking1v1VoiceInteractionViewDelegate {
extension InserterStorage: PerformPause {
    /// 接听音频通话
    //: func interactionView_shouldAcceptCall() {
    func refuse() {
        //: TalkingSocketManager.shared.sendMessage(info: ["cmd": "acceptCall", "data": ["uid": self.chatModel?.pairUid]])
        MaterialCurrency.shared.warningVersion(info: [String(bytes: user_shouldId.map{$0^59}, encoding: .utf8)!: (userMobileKey.replacingOccurrences(of: "cover", with: "c") + String(showHungUnderlyUrl.prefix(6))), String(bytes: k_boardName.reversed(), encoding: .utf8)!: [String(bytes: user_randomString.map{businessPartner(manage: $0)}, encoding: .utf8)!: self.chatModel?.pairUid]])
    }

    /// 拨打方结束通话
    //: func interactionView_shouldEndCall() {
    func tolerancePut() {
        //: if self.chatModel?.videoStage == .Waiting {
        if self.chatModel?.videoStage == .Waiting { // 拨打方取消【未通话】
            //: self.func__showStatusBarErrorMsg(showMsg: "Calls are cancelled".localized)
            self.exitMarker(showMsg: (String(noti_middleMsg) + String(const_editKey.prefix(6)) + "ncelled").localized)
            //: TalkingSocketManager.shared.sendMessage(info: ["cmd": "cancelCall", "data": ["logId": self.chatModel?.logId]])
            MaterialCurrency.shared.warningVersion(info: [String(bytes: user_shouldId.map{$0^59}, encoding: .utf8)!: (String(dataMinimizeName) + String(appShadeName)), String(bytes: k_boardName.reversed(), encoding: .utf8)!: [String(bytes: data_draftComplaintMsg.map{$0^164}, encoding: .utf8)!: self.chatModel?.logId]])
            //: } else if self.chatModel?.videoStage == .Recieved {
        } else if self.chatModel?.videoStage == .Recieved { // 接收方取消【未通话】
            //: self.func__showStatusBarErrorMsg(showMsg: "Calls are cancelled".localized)
            self.exitMarker(showMsg: (String(noti_middleMsg) + String(const_editKey.prefix(6)) + "ncelled").localized)
            //: TalkingSocketManager.shared.sendMessage(info: ["cmd": "refuseCall", "data": ["uid": self.chatModel?.pairUid]])
            MaterialCurrency.shared.warningVersion(info: [String(bytes: user_shouldId.map{$0^59}, encoding: .utf8)!: (String(constQueenName) + appMultipleUrl.replacingOccurrences(of: "thin", with: "l")), String(bytes: k_boardName.reversed(), encoding: .utf8)!: [String(bytes: user_randomString.map{businessPartner(manage: $0)}, encoding: .utf8)!: self.chatModel?.pairUid]])
            //: } else {
        } else { // 【通话中】
            //: self.func__showStatusBarErrorMsg(showMsg: "The call has ended".localized)
            self.exitMarker(showMsg: (String(main_processorKey.prefix(4)) + "call ha" + String(user_naturalString.prefix(7))).localized)
            //: TalkingSocketManager.shared.sendMessage(info: ["cmd": "stopCall", "data": ["logId": self.chatModel?.logId]])
            MaterialCurrency.shared.warningVersion(info: [String(bytes: user_shouldId.map{$0^59}, encoding: .utf8)!: (String(const_enjoyPassName)), String(bytes: k_boardName.reversed(), encoding: .utf8)!: [String(bytes: data_draftComplaintMsg.map{$0^164}, encoding: .utf8)!: self.chatModel?.logId]])
        }

        //: self.interactionView.func_giftViewDismiss()
        self.interactionView.underLike()
        //: self.popCurrentViewController()
        self.globalToLimit()
    }

    /// 是否静音
    //: func interactionView_shouldChangeMute(isMute: Bool) {
    func eachInvisible(isMute: Bool) {
        //: self.voiceManager.setMute(isMute: isMute)
        self.voiceManager.full(isMute: isMute)
    }

    /// 是否免提
    //: func interactionView_shouldChangeSpeaker(isHandsfree: Bool) {
    func anPresentation(isHandsfree: Bool) {
        //: self.voiceManager.setHandsfree(isHandsfree: isHandsfree)
        self.voiceManager.mightVid(isHandsfree: isHandsfree)
    }

    /// 展示语音通话小窗口
    //: func interactionView_shouldDisplayMiniWindow() {
    func no() {
        //: TalkingSocketManager.shared.pacificVC = self
        MaterialCurrency.shared.pacificVC = self
        //: self.miniView = TalkingVoiceMiniView.buildVoiceMiniView()
        self.miniView = AttachHalfSegment.decision()
        //: self.miniView?.tapGestureBlock = { [weak self] in
        self.miniView?.tapGestureBlock = { [weak self] in // 还原回通话界面
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: self.currentViewController()?.navigationController?.pushViewController(self, animated: true)
            self.myAcross()?.navigationController?.pushViewController(self, animated: true)
        }
        //: let talkingTime = self.interactionView.getCurrentTalkingTime()
        let talkingTime = self.interactionView.easy()
        //: self.miniView?.updateTalkingTime(talkingTime)
        self.miniView?.chartDoingAttention(talkingTime)
        //: popCurrentViewController()
        globalToLimit()
    }

    /// 更新小窗口通话时间
    //: func interactionView_shouldUpdateTime(time: Double) {
    func frameworkStar(time: Double) {
        //: guard self.miniView != nil else { return }
        guard self.miniView != nil else { return }
        //: self.miniView?.updateTalkingTime(time)
        self.miniView?.chartDoingAttention(time)
    }

    /// 收到礼物消息展示
    //: @objc func receiveGiftPlay(giftArr: Array<Any>) {
    @objc func basicTransform(giftArr: [Any]) {
        //: interactionView.addGiftEffectModelArr(giftArr: giftArr)
        interactionView.offerIn(giftArr: giftArr)
    }
}

// MARK: - Layout

//: extension TalkingVoiceChatViewController {
extension InserterStorage {
    /// 添加视图
    //: private func setupSubviews() {
    private func equally() {
        //: self.view.addSubview(interactionView)
        self.view.addSubview(interactionView)
    }

    /// 添加约束
    //: private func setupSubViewsConstraint() {
    private func guideWith() {
        //: interactionView.snp.makeConstraints { make in
        interactionView.snp.makeConstraints { make in
            //: make.edges.equalToSuperview()
            make.edges.equalToSuperview()
        }
    }
}
