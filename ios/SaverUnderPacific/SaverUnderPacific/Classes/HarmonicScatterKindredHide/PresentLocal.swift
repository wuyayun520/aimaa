
//: Declare String Begin

/*: "http://" :*/
fileprivate let kPoString:String = "distance reserve picturehttp://"

/*: "https://" :*/
fileprivate let user_operatePath:[Character] = ["h","t","t","p","s",":","/","/"]

/*: "http:// :*/
fileprivate let main_bucketRestorelyString:[Character] = ["h","t","t","p",":"]
fileprivate let constZzValue:String = "waitwait"

/*: "serverSelctIndex =  :*/
fileprivate let main_adviceProMsg:[Character] = ["s","e"]
fileprivate let dataWomanUrl:String = "maybe factor no reflect legrverS"
fileprivate let main_modelCaptureMsg:String = "dex = third male"

/*: "serverUrlStr =  :*/
fileprivate let main_nowStr:String = "SERVER"
fileprivate let appRecentId:String = "tr = remote factor translate remove interest"

/*: "app. :*/
fileprivate let showInvitationMsg:[Character] = ["a","p","p","."]

/*: .com" :*/
fileprivate let k_everyMsg:String = "app shade delivery install.com"

/*: "mf/chatRoom/sendGift" :*/
fileprivate let showSophisticatedNextStr:String = "mf/cplay while"
fileprivate let user_premiumTitle:[Character] = ["s","e","n","d","G","i","f","t"]

/*: "recharge/applePayNotify" :*/
fileprivate let notiTeamName:String = "rcontrastha"
fileprivate let const_atString:String = "earn personppleP"

/*: "user/appleLogin" :*/
fileprivate let k_bulletShapeMsg:String = "ualwaysr"
fileprivate let kReaderFormat:String = "leLogrecover busy upper length"
fileprivate let notiIncreaseName:String = "IN"

/*: "IM/getMyBlackList" :*/
fileprivate let userEnabletoTitle:[Character] = ["I","M","/","g","e","t","M","y"]
fileprivate let user_chainTitle:[Character] = ["B","l","a","c"]
fileprivate let userRangeFormat:[Character] = ["k","L","i","s","t"]

/*: "mf/business/sendMsg" :*/
fileprivate let kEnableicialStr:[Character] = ["m","f","/","b","u","s","i","n","e"]
fileprivate let data_collectTitle:String = "ss/senaria session auto operate destination"

/*: "mf/business/sendGift" :*/
fileprivate let k_subBeMsg:String = "mf/businepractical earn literal wire"
fileprivate let kLoopMessage:[Character] = ["s","s","/"]
fileprivate let constScanPath:String = "destroyend"

/*: "mf/chatRoom/sendMsg" :*/
fileprivate let const_audienceHoneyStr:String = "mf/chahello measure appropriate concrete"
fileprivate let k_acheLabTitle:[Character] = ["e","n","d","M","s","g"]

/*: "GJ21FB" :*/
fileprivate let appPrivacyKey:String = "relatedJ21FB"

/*: "完成了检测,请求状态正常" :*/
fileprivate let show_processorWeightMessage:String = "完成了检测,constraint"
fileprivate let constTypicalDownString:[Character] = ["求","状","态","正","常"]

/*: "接口请求失败" :*/
fileprivate let showLengthMessage:String = "接口请求失\u{8d25}"

/*: "完成了检测,需要更换server url" :*/
fileprivate let show_rangePath:String = "完成了检"
fileprivate let show_constructStr:String = "illegal boy更换se"
fileprivate let constGalleryValue:String = "extent general popular between url"

/*: "HTTPProxy" :*/
fileprivate let dataAdministrativeMessage:String = "HTTPProagent attach leg automatically spring"
fileprivate let showFutureVenueFormat:String = "objecty"

/*: "__SCOPED__" :*/
fileprivate let main_workMTitle:String = "__SCOPEcoalition possess maybe decide"
fileprivate let user_policyStr:String = "D__hi filter job painter"

/*: "tap" :*/
fileprivate let dataDiskValue:[Character] = ["t","a","p"]

/*: "tun" :*/
fileprivate let app_spreadString:String = "applicationun"

/*: "ipsec" :*/
fileprivate let notiSpellMediumUrl:[Character] = ["i","p","s","e","c"]

/*: "ppp" :*/
fileprivate let userRebuildFormat:String = "platformp"

/*: "func__updateTalkingCookies =  :*/
fileprivate let const_twoKey:[Character] = ["f","u","n","c","_","_","u","p","d","a","t","e","T","a","l","k","i","n","g","C","o","o","k","i","e","s"," ","="," "]

/*: "https:// :*/
fileprivate let constScreeningPath:[Character] = ["h","t","t","p","s",":","/","/"]

/*: "func__deleteSameCookiesWithUrl =  :*/
fileprivate let user_generateFeedbackData:[UInt8] = [0x20,0x3d,0x20,0x6c,0x72,0x55,0x68,0x74,0x69,0x57,0x73,0x65,0x69,0x6b,0x6f,0x6f,0x43,0x65,0x6d,0x61,0x53,0x65,0x74,0x65,0x6c,0x65,0x64,0x5f,0x5f,0x63,0x6e,0x75,0x66]

/*: "UID" :*/
fileprivate let showSpecialShKey:String = "UIstream"

/*: "PHPSESSID" :*/
fileprivate let show_everyData:String = "block"
fileprivate let appAbsStrokeValue:String = "HverySE"

/*: "func__saveAllCookies =  :*/
fileprivate let noti_segmentLeadingPath:String = "func_format listen reference the"
fileprivate let user_interestedPath:String = "range ease minimize all visitCook"
fileprivate let user_mmAliveUnknownString:String = "substantial pin menu nobody inviteies = "

/*: "func__saveAllCookies  error" :*/
fileprivate let showConstraintName:String = "funplayer"
fileprivate let app_pingCosPath:String = "change half class we generallyveAll"
fileprivate let main_heMsg:String = "fit down local promise  error"

//: Declare String End

// __DEBUG__
// __CLOSE_PRINT__
//
//  PresentLocal.swift
//  AbroadTalking
//
//  Created by Hemming on 2022/8/23.
//

//: import UIKit
import UIKit
//: import WebKit
import WebKit

//: enum LangType: String {
enum StrokeHideBind: String {
    //: case en = "en"
    case en
    //: case ar = "ar"
    case ar
    //: case es = "es"
    case es
    //: case pt = "pt"
    case pt
}

//: public class TalkingRequestAddrTool: NSObject {
public class PresentLocal: NSObject {
    //: @objc static public let share = TalkingRequestAddrTool()
    @objc public static let share = PresentLocal()
    //: private override init() {
    override private init() {
        //: super.init()
        super.init()
        //: setUp()
        creationList()
    }

    //: private var timerDispose: Disposable?
    private var timerDispose: Disposable?

    //: var serverSelctIndex = 0
    var serverSelctIndex = 0
    //: var hasFinishCheck   = false
    var hasFinishCheck = false
    //: var hostCheckNum     = 0
    var hostCheckNum = 0

    //: var domainStr: String?
    var domainStr: String? // 用户登录的信息
    //: var _serverUrlStr: String = ""       //
    var _serverUrlStr: String = "" //

    //: @objc public var serverUrlStr: String {
    @objc public var serverUrlStr: String {
        //: get {
        get {
            //: return _serverUrlStr
            return _serverUrlStr
        }
        //: set {
        set {
            //: var newServerUrl: String = ""
            var newServerUrl: String = ""
            //: if newValue.hasPrefix("http://") || newValue.hasPrefix("https://") {
            if newValue.hasPrefix((String(kPoString.suffix(7)))) || newValue.hasPrefix((String(user_operatePath))) {
                //: newServerUrl = newValue
                newServerUrl = newValue
                //: } else {
            } else {
                //: if Environment_Formal && !Encryption_Request {
                if const_tempAttachTitle && !k_headDrawData {
                    //: newServerUrl = "http://\(newValue )"
                    newServerUrl = (String(main_bucketRestorelyString) + constZzValue.replacingOccurrences(of: "wait", with: "/")) + "\(newValue)"
                    //: } else {
                } else {
                    //: newServerUrl = "http://\(newValue )"
                    newServerUrl = (String(main_bucketRestorelyString) + constZzValue.replacingOccurrences(of: "wait", with: "/")) + "\(newValue)"
                }
            }
            //: if NSURL.init(string: newServerUrl) == nil {
            if NSURL(string: newServerUrl) == nil {
                //: return
                return
            }
            //: let oldServerUrl: String = _serverUrlStr
            let oldServerUrl: String = _serverUrlStr

            //: if oldServerUrl.isEmptyString || oldServerUrl != newServerUrl {
            if oldServerUrl.isEmptyString || oldServerUrl != newServerUrl {
                //: _serverUrlStr = newServerUrl
                _serverUrlStr = newServerUrl
                //: if oldServerUrl.count > 0 {
                if oldServerUrl.count > 0 {
                    //: let oldCookies: [HTTPCookie] = HTTPCookieStorage.shared.cookies(for: URL.init(string: oldServerUrl)!)!
                    let oldCookies: [HTTPCookie] = HTTPCookieStorage.shared.cookies(for: URL(string: oldServerUrl)!)!
                    //: func__updateTalkingCookies(cookies: oldCookies)
                    whipEnd(cookies: oldCookies)
                }
            }
        }
    }

    //: func setUp() {
    func creationList() {
        //: serverSelctIndex = Defaults.integer(forKey: TalkingAddrServerUrlSelectIndexKey)
        serverSelctIndex = user_findBoundMessage.integer(forKey: user_allPath)
        //: printLog(message: "serverSelctIndex = \(serverSelctIndex)")
        printLog(message: (String(main_adviceProMsg) + String(dataWomanUrl.suffix(5)) + "elctIn" + String(main_modelCaptureMsg.prefix(6))) + "\(serverSelctIndex)")
        //: let serverDomain = func__getInitialServerUrl()
        let serverDomain = gallery()
        //: var serverPrefix = "http://"
        var serverPrefix = (String(kPoString.suffix(7)))
        //: if Environment_Formal && !Encryption_Request {
        if const_tempAttachTitle && !k_headDrawData {
            //: serverPrefix = "http://"
            serverPrefix = (String(kPoString.suffix(7)))
        }
        //: serverUrlStr = serverPrefix.appending(serverDomain)
        serverUrlStr = serverPrefix.appending(serverDomain)
        //: printLog(message: "serverUrlStr = \(serverUrlStr)")
        printLog(message: (main_nowStr.lowercased() + "UrlS" + String(appRecentId.prefix(5))) + "\(serverUrlStr)")
    }

    //: func func__getInitialServerUrl() -> String {
    func gallery() -> String {
        //: var appServerUrl = ""
        var appServerUrl = ""
        //: appServerUrl = serverUrlArr[serverSelctIndex]
        appServerUrl = serverUrlArr[serverSelctIndex]
        //: return appServerUrl
        return appServerUrl
    }

    //: func func__saveNewTalkingServerUrl(serverStr: String) {
    func clickOn(serverStr: String) {
        //: serverUrlStr = serverStr
        serverUrlStr = serverStr
    }

    //: private lazy var serverUrlArr: Array = {
    private lazy var serverUrlArr: Array = //: return ["app.\(ReplaceUrlDomain).com", "app.\(ReplaceUrlDomain).com"]
        [(String(showInvitationMsg)) + "\(app_readTitle)" + (String(k_everyMsg.suffix(4))), (String(showInvitationMsg)) + "\(app_readTitle)" + (String(k_everyMsg.suffix(4)))]
    //: }()

    //: lazy var slowPathArr: Array = {
    lazy var slowPathArr: Array = //: return ["mf/chatRoom/sendGift",
        [(String(showSophisticatedNextStr.prefix(4)) + "hatRoom/" + String(user_premiumTitle)),
         //: "recharge/applePayNotify",
         (notiTeamName.replacingOccurrences(of: "contrast", with: "ec") + "rge/a" + String(const_atString.suffix(5)) + "ayNotify"),
         //: "user/appleLogin"] + slowPathArr1
         (k_bulletShapeMsg.replacingOccurrences(of: "always", with: "se") + "/app" + String(kReaderFormat.prefix(5)) + notiIncreaseName.lowercased())] + slowPathArr1
    //: }()

    //: private lazy var slowPathArr1 = {
    private lazy var slowPathArr1 = //: return ["IM/getMyBlackList",
        [(String(userEnabletoTitle) + String(user_chainTitle) + String(userRangeFormat)),
         //: "mf/business/sendMsg",
         (String(kEnableicialStr) + String(data_collectTitle.prefix(6)) + "dMsg"),
         //: "mf/business/sendGift",
         (String(k_subBeMsg.prefix(9)) + String(kLoopMessage) + constScanPath.replacingOccurrences(of: "destroy", with: "s") + "Gift"),
         //: "mf/chatRoom/sendMsg"]
         (String(const_audienceHoneyStr.prefix(6)) + "tRoom/s" + String(k_acheLabTitle))]
    //: }()

    /// 获取接口语言
    //: lazy var interfaceLang: String = {
    lazy var interfaceLang: String = {
        //: let lang = MovementStructure.getSystemLangCode()
        let lang = MovementStructure.readyMerge()
        //: if ["en", "ar", "es", "pt"].contains(lang) {
        if ["en", "ar", "es", "pt"].contains(lang) {
            //: return lang
            return lang
        }
        //: return "en"
        return "en"
        //: }()
    }()

    //: lazy var randomStr: String = {
    lazy var randomStr: String = //: return String.randomString(length: 16)
        .maximumPoolParticle(length: 16)
    //: }()

    //: lazy var encryKeyStr: String = {
    lazy var encryKeyStr: String = {
        //: let sufStr = randomStr.suffix(6)
        let sufStr = randomStr.suffix(6)
        //: let reversedStr: String = String(sufStr.reversed())
        let reversedStr = String(sufStr.reversed())
        //: return "GJ21FB" + reversedStr
        return (appPrivacyKey.replacingOccurrences(of: "related", with: "G")) + reversedStr
        //: }()
    }()

    //: deinit {
    deinit {
        //: NotificationCenter.default.removeObserver(self)
        NotificationCenter.default.removeObserver(self)
    }
}

//: extension TalkingRequestAddrTool {
extension PresentLocal {
    //: func checkServerUrl() {
    func packageInBlind() {
        //: if MarginExamineer.share.networkStatus != .Unavailable {
        if MarginExamineer.share.networkStatus != .Unavailable {
            //: func_requestHost()
            independentBoth()
            //: } else {
        } else {
            //: func_starCodeTime()
            maxi()
        }
    }

    //: func func_starCodeTime() {
    func maxi() {
        //: if self.hasFinishCheck {
        if self.hasFinishCheck {
            //: return
            return
        }
        //: if timerDispose != nil {
        if timerDispose != nil {
            //: timerDispose?.dispose()
            timerDispose?.dispose()
        }
        //: timerDispose = Observable<Int>.interval(.seconds(3), scheduler: MainScheduler.instance)
        timerDispose = Observable<Int>.interval(.seconds(3), scheduler: MainScheduler.instance)
            //: .subscribe(onNext: { [weak self] num in
            .subscribe(onNext: { [weak self] num in
                //: guard let self = self else {return}
                guard let self = self else { return }
                //: self.func_requestHost()
                self.independentBoth()
                //: })
            })
    }

    //: func func_endCodeTime() {
    func vidByRequire() {
        //: timerDispose?.dispose()
        timerDispose?.dispose()
    }

    //: func func_requestHost() {
    func independentBoth() {
        //: if MarginExamineer.share.networkStatus == .Unavailable {
        if MarginExamineer.share.networkStatus == .Unavailable {
            //: return
            return
        }
        //: func_endCodeTime()
        vidByRequire()
        //: AppManagerRequest.func__initRequestHost { [weak self] succeed, result, errorModel in
        ProjectBlack.exercise { [weak self] succeed, result, errorModel in
            //: guard let self = self else {return}
            guard let self = self else { return }
            //: if succeed {
            if succeed {
                //: self.hasFinishCheck = true
                self.hasFinishCheck = true
                //: printLog(message: "完成了检测,请求状态正常")
                printLog(message: (show_processorWeightMessage.replacingOccurrences(of: "constraint", with: "请") + String(constTypicalDownString)))
                //: } else {
            } else {
                //: printLog(message: "接口请求失败")
                printLog(message: (showLengthMessage))
                //: if self.hasFinishCheck {return}
                if self.hasFinishCheck { return }
                //: self.hostCheckNum += 1
                self.hostCheckNum += 1
                //: if self.hostCheckNum < 3 {
                if self.hostCheckNum < 3 {
                    //: self.func_starCodeTime()
                    self.maxi()
                    //: } else {
                } else {
                    //: printLog(message: "完成了检测,需要更换server url")
                    printLog(message: (show_rangePath.capitalized + "测,需\u{8981}" + String(show_constructStr.suffix(4)) + "rver" + String(constGalleryValue.suffix(4))))
                    //: self.func__changeHostUrl()
                    self.streamWithinErr()
                }
            }
        }
    }

    //: func func__changeHostUrl() {
    func streamWithinErr() {
        //: hostCheckNum = 0
        hostCheckNum = 0
        //: hasFinishCheck = true
        hasFinishCheck = true
        //: func_endCodeTime()
        vidByRequire()

        //: serverSelctIndex = (serverSelctIndex == 0) ? 1 : 0
        serverSelctIndex = (serverSelctIndex == 0) ? 1 : 0
        //: Defaults.set(serverSelctIndex, forKey: TalkingAddrServerUrlSelectIndexKey)
        user_findBoundMessage.set(serverSelctIndex, forKey: user_allPath)
        //: let serverDomain = serverUrlArr[serverSelctIndex]
        let serverDomain = serverUrlArr[serverSelctIndex]
        //: var serverPrefix = "http://"
        var serverPrefix = (String(kPoString.suffix(7)))
        //: if Environment_Formal && !Encryption_Request {
        if const_tempAttachTitle, !k_headDrawData {
            //: serverPrefix = "http://"
            serverPrefix = (String(kPoString.suffix(7)))
        }
        //: serverUrlStr = serverPrefix.appending(serverDomain)
        serverUrlStr = serverPrefix.appending(serverDomain)
    }
}

// MARK: - 判断用户设备是否开启VPN

//: extension TalkingRequestAddrTool {
public extension PresentLocal {
    /// 判断用户设备是否开启代理
    /// - Returns: false: 未开启  true: 开启
    //: public class func isUsedProxy() -> Bool {
    class func atQuote() -> Bool {
        //: guard let proxy = CFNetworkCopySystemProxySettings()?.takeUnretainedValue() else { return false }
        guard let proxy = CFNetworkCopySystemProxySettings()?.takeUnretainedValue() else { return false }
        //: guard let dict = proxy as? [String: Any] else { return false }
        guard let dict = proxy as? [String: Any] else { return false }
        //: guard let HTTPProxy = dict["HTTPProxy"] as? String else { return false }
        guard let HTTPProxy = dict[(String(dataAdministrativeMessage.prefix(7)) + showFutureVenueFormat.replacingOccurrences(of: "object", with: "x"))] as? String else { return false }
        //: if (HTTPProxy.count > 0) {
        if HTTPProxy.count > 0 {
            //: return true
            return true
        }
        //: return false
        return false
    }

    /// 判断用户设备是否开启代理VPN
    /// - Returns: false: 未开启  true: 开启
    //: public class func isUsedVPN() -> Bool {
    class func holdMain() -> Bool {
        //: guard let proxy = CFNetworkCopySystemProxySettings()?.takeUnretainedValue() else { return false }
        guard let proxy = CFNetworkCopySystemProxySettings()?.takeUnretainedValue() else { return false }
        //: guard let dict = proxy as? [String: Any] else { return false }
        guard let dict = proxy as? [String: Any] else { return false }
        //: guard let scopedDic = dict["__SCOPED__"] as? [String: Any] else { return false }
        guard let scopedDic = dict[(String(main_workMTitle.prefix(7)) + String(user_policyStr.prefix(3)))] as? [String: Any] else { return false }
        //: for keyStr in scopedDic.keys {
        for keyStr in scopedDic.keys {
            //: if keyStr.contains("tap") || keyStr.contains("tun") || keyStr.contains("ipsec") || keyStr.contains("ppp"){
            if keyStr.contains((String(dataDiskValue))) || keyStr.contains((app_spreadString.replacingOccurrences(of: "application", with: "t"))) || keyStr.contains((String(notiSpellMediumUrl))) || keyStr.contains((userRebuildFormat.replacingOccurrences(of: "platform", with: "pp"))) {
                //: return true
                return true
            }
        }
        //: return false
        return false
    }
}

//: extension TalkingRequestAddrTool {
extension PresentLocal {
    //: func func__updateTalkingCookies(cookies: Array<HTTPCookie>) {
    func whipEnd(cookies: [HTTPCookie]) {
        //: printLog(message: "func__updateTalkingCookies = \(cookies)")
        printLog(message: (String(const_twoKey)) + "\(cookies)")
        //: var tempCookies: Array<HTTPCookie> = cookies
        var tempCookies: [HTTPCookie] = cookies
        //: if cookies.count == 0 {
        if cookies.count == 0 {
            //: tempCookies = func__getCookiesFromOldServerUrl()
            tempCookies = someMap()
        }
        //: if tempCookies.count == 0 {
        if tempCookies.count == 0 {
            //: return
            return
        }
        //: var tempUrlArr: Array<URL> = []
        var tempUrlArr: [URL] = []

        //: if !self.serverUrlStr.isEmptyString {
        if !self.serverUrlStr.isEmptyString {
            //: tempUrlArr.append(URL.init(string: self.serverUrlStr)!)
            tempUrlArr.append(URL(string: self.serverUrlStr)!)
        }

        //: var urlDomain: String = MarginExamineer.share.appConfigMode.urlH5Domain
        var urlDomain: String = MarginExamineer.share.appConfigMode.urlH5Domain
        //: urlDomain = urlDomain.replacingOccurrences(of: "http://", with: "https://")
        urlDomain = urlDomain.replacingOccurrences(of: (String(kPoString.suffix(7))), with: (String(user_operatePath)))
        //: if urlDomain.hasPrefix("https://") {
        if urlDomain.hasPrefix((String(user_operatePath))) {
            //: tempUrlArr.append(URL.init(string: urlDomain)!)
            tempUrlArr.append(URL(string: urlDomain)!)
            //: } else {
        } else {
            //: tempUrlArr.append(URL.init(string: "https://\(urlDomain)")!)
            tempUrlArr.append(URL(string: (String(constScreeningPath)) + "\(urlDomain)")!)
        }

        //: var addCookieArr: Array<HTTPCookie> = []
        var addCookieArr: [HTTPCookie] = []
        //: for cookies in tempCookies {
        for cookies in tempCookies {
            //: var cookieInfo: [HTTPCookiePropertyKey: Any] = cookies.properties!
            var cookieInfo: [HTTPCookiePropertyKey: Any] = cookies.properties!
            //: for url in tempUrlArr {
            for url in tempUrlArr {
                //: cookieInfo[HTTPCookiePropertyKey.domain] = url.host
                cookieInfo[HTTPCookiePropertyKey.domain] = url.host
                //: cookieInfo.removeValue(forKey: HTTPCookiePropertyKey.discard)
                cookieInfo.removeValue(forKey: HTTPCookiePropertyKey.discard)
                //: let newCookie = HTTPCookie.init(properties: cookieInfo)
                let newCookie = HTTPCookie(properties: cookieInfo)
                //: if newCookie != nil {
                if newCookie != nil {
                    //: addCookieArr.append(newCookie!)
                    addCookieArr.append(newCookie!)
                }
            }
        }
        //: for cookies in addCookieArr {
        for cookies in addCookieArr {
            //: HTTPCookieStorage.shared.setCookie(cookies)
            HTTPCookieStorage.shared.setCookie(cookies)
        }

        //: for url in tempUrlArr {
        for url in tempUrlArr {
            //: func__deleteSameCookiesWithUrl(url: url)
            moveDistinction(url: url)
        }
        //: DispatchQueue.main.async {
        DispatchQueue.main.async {
            //: WKWebsiteDataStore.default().fetchDataRecords(ofTypes: [WKWebsiteDataTypeCookies]) { _ in
            WKWebsiteDataStore.default().fetchDataRecords(ofTypes: [WKWebsiteDataTypeCookies]) { _ in
            }
        }

        //: func__saveAllCookies()
        lostAcross()
    }

    //: func func__deleteSameCookiesWithUrl(url: URL) {
    func moveDistinction(url: URL) {
        //: let cookiesArr: Array<HTTPCookie> = HTTPCookieStorage.shared.cookies(for: url)!
        let cookiesArr: [HTTPCookie] = HTTPCookieStorage.shared.cookies(for: url)!
        //: var saveDic: Dictionary<String, String> = [:]
        var saveDic: [String: String] = [:]
        //: for cookies in cookiesArr {
        for cookies in cookiesArr {
            //: if !cookies.name.isEmptyString {
            if !cookies.name.isEmptyString {
                //: let saveKey = cookies.name
                let saveKey = cookies.name
                //: if saveDic.keys.contains(saveKey) {
                if saveDic.keys.contains(saveKey) {
                    //: printLog(message: "func__deleteSameCookiesWithUrl = \(cookies.name)")
                    printLog(message: String(bytes: user_generateFeedbackData.reversed(), encoding: .utf8)! + "\(cookies.name)")
                    //: HTTPCookieStorage.shared.deleteCookie(cookies)
                    HTTPCookieStorage.shared.deleteCookie(cookies)
                    //: } else {
                } else {
                    //: saveDic[saveKey] = "1"
                    saveDic[saveKey] = "1"
                }
            }
        }
    }

    //: func func__getCookiesFromOldServerUrl() -> Array<HTTPCookie> {
    func someMap() -> [HTTPCookie] {
        //: var serverPrefix = "http://"
        var serverPrefix = (String(kPoString.suffix(7)))
        //: if Environment_Formal && !Encryption_Request {
        if const_tempAttachTitle, !k_headDrawData {
            //: serverPrefix = "https://"
            serverPrefix = (String(user_operatePath))
        }
        //: let fullDomain = serverPrefix.appending("app.\(ReplaceUrlDomain).com")
        let fullDomain = serverPrefix.appending((String(showInvitationMsg)) + "\(app_readTitle)" + (String(k_everyMsg.suffix(4))))
        //: let oldCookies = HTTPCookieStorage.shared.cookies(for: URL.init(string: fullDomain)!)
        let oldCookies = HTTPCookieStorage.shared.cookies(for: URL(string: fullDomain)!)
        //: return oldCookies!
        return oldCookies!
    }

    //: @objc func cookieChanged(note: Notification) {
    @objc func dire(note: Notification) {
        //: if MarginExamineer.share.loginUid.isEmptyString {
        if MarginExamineer.share.loginUid.isEmptyString {
            //: return
            return
        }
        //: if #available(iOS 11.0, *) {
        if #available(iOS 11.0, *) {
            //: checkHtmlCookieIfNeedCurrentVersion()
            perUnless()
            //: } else {
        } else {
            //: checkHtmlCookieIfNeedEarlyVersion()
            questionSomeone()
        }
    }

    //: func checkHtmlCookieIfNeedCurrentVersion() {
    func perUnless() {
        //: var htmlDomain: String = MarginExamineer.share.appConfigMode.urlH5Domain
        var htmlDomain: String = MarginExamineer.share.appConfigMode.urlH5Domain

        //: if !htmlDomain.isEmptyString {
        if !htmlDomain.isEmptyString {
            //: let url = URL.init(string: htmlDomain)
            let url = URL(string: htmlDomain)
            //: htmlDomain = url?.host ?? ""
            htmlDomain = url?.host ?? ""
            //: let cookieArr: Array<HTTPCookie> = HTTPCookieStorage.shared.cookies!
            let cookieArr: [HTTPCookie] = HTTPCookieStorage.shared.cookies!
            //: var appCookie: Dictionary<String, HTTPCookie> =  Dictionary()
            var appCookie: [String: HTTPCookie] = Dictionary()
            //: var h5Cookie: Dictionary<String, HTTPCookie> =  Dictionary()
            var h5Cookie: [String: HTTPCookie] = Dictionary()

            //: for cookie in cookieArr {
            for cookie in cookieArr {
                //: let tempServerUrlStr = self.serverUrlStr
                let tempServerUrlStr = self.serverUrlStr
                //: let temphtmlDomainStr = htmlDomain
                let temphtmlDomainStr = htmlDomain
                //: if tempServerUrlStr.contains(cookie.domain) {
                if tempServerUrlStr.contains(cookie.domain) {
                    //: if cookie.name.uppercased() == "UID" {
                    if cookie.name.uppercased() == (showSpecialShKey.replacingOccurrences(of: "stream", with: "D")) {
                        //: appCookie["UID"] = cookie
                        appCookie[(showSpecialShKey.replacingOccurrences(of: "stream", with: "D"))] = cookie
                    }
                    //: if cookie.name.uppercased() == "PHPSESSID" {
                    if cookie.name.uppercased() == (show_everyData.replacingOccurrences(of: "block", with: "P") + appAbsStrokeValue.replacingOccurrences(of: "very", with: "P") + "SSID") {
                        //: appCookie["PHPSESSID"] = cookie
                        appCookie[(show_everyData.replacingOccurrences(of: "block", with: "P") + appAbsStrokeValue.replacingOccurrences(of: "very", with: "P") + "SSID")] = cookie
                    }

                    //: } else if temphtmlDomainStr.contains(cookie.domain) {
                } else if temphtmlDomainStr.contains(cookie.domain) {
                    //: if cookie.name.uppercased() == "UID" {
                    if cookie.name.uppercased() == (showSpecialShKey.replacingOccurrences(of: "stream", with: "D")) {
                        //: h5Cookie["UID"] = cookie
                        h5Cookie[(showSpecialShKey.replacingOccurrences(of: "stream", with: "D"))] = cookie
                    }
                    //: if cookie.name.uppercased() == "PHPSESSID" {
                    if cookie.name.uppercased() == (show_everyData.replacingOccurrences(of: "block", with: "P") + appAbsStrokeValue.replacingOccurrences(of: "very", with: "P") + "SSID") {
                        //: h5Cookie["PHPSESSID"] = cookie
                        h5Cookie[(show_everyData.replacingOccurrences(of: "block", with: "P") + appAbsStrokeValue.replacingOccurrences(of: "very", with: "P") + "SSID")] = cookie
                    }
                }
                //: var cookieEqual = true
                var cookieEqual = true
                //: if appCookie.keys.count != h5Cookie.keys.count {
                if appCookie.keys.count != h5Cookie.keys.count {
                    //: cookieEqual = false
                    cookieEqual = false
                    //: } else {
                } else {
                    //: if appCookie.keys.contains("UID") && h5Cookie.keys.contains("UID") {
                    if appCookie.keys.contains((showSpecialShKey.replacingOccurrences(of: "stream", with: "D"))), h5Cookie.keys.contains((showSpecialShKey.replacingOccurrences(of: "stream", with: "D"))) {
                        //: let appUid: HTTPCookie = appCookie["UID"]!
                        let appUid: HTTPCookie = appCookie[(showSpecialShKey.replacingOccurrences(of: "stream", with: "D"))]!
                        //: let h5Uid: HTTPCookie = h5Cookie["UID"]!
                        let h5Uid: HTTPCookie = h5Cookie[(showSpecialShKey.replacingOccurrences(of: "stream", with: "D"))]!

                        //: if h5Uid.value != appUid.value {
                        if h5Uid.value != appUid.value {
                            //: cookieEqual = false
                            cookieEqual = false
                        }
                    }
                    //: if appCookie.keys.contains("PHPSESSID") && h5Cookie.keys.contains("PHPSESSID") {
                    if appCookie.keys.contains((show_everyData.replacingOccurrences(of: "block", with: "P") + appAbsStrokeValue.replacingOccurrences(of: "very", with: "P") + "SSID")), h5Cookie.keys.contains((show_everyData.replacingOccurrences(of: "block", with: "P") + appAbsStrokeValue.replacingOccurrences(of: "very", with: "P") + "SSID")) {
                        //: let appPHPSESSID: HTTPCookie = appCookie["PHPSESSID"]!
                        let appPHPSESSID: HTTPCookie = appCookie[(show_everyData.replacingOccurrences(of: "block", with: "P") + appAbsStrokeValue.replacingOccurrences(of: "very", with: "P") + "SSID")]!
                        //: let h5PHPSESSID: HTTPCookie = h5Cookie["PHPSESSID"]!
                        let h5PHPSESSID: HTTPCookie = h5Cookie[(show_everyData.replacingOccurrences(of: "block", with: "P") + appAbsStrokeValue.replacingOccurrences(of: "very", with: "P") + "SSID")]!
                        //: if appPHPSESSID.value != h5PHPSESSID.value {
                        if appPHPSESSID.value != h5PHPSESSID.value {
                            //: cookieEqual = false
                            cookieEqual = false
                        }
                    }
                }
                //: if !cookieEqual {
                if !cookieEqual {
                    //: for (key, _) in appCookie {
                    for (key, _) in appCookie {
                        //: let cookie: HTTPCookie = appCookie[key]!
                        let cookie: HTTPCookie = appCookie[key]!
                        //: var cookiePro: Dictionary<HTTPCookiePropertyKey, Any> = cookie.properties!
                        var cookiePro: [HTTPCookiePropertyKey: Any] = cookie.properties!
                        //: cookiePro[HTTPCookiePropertyKey.domain] = htmlDomain
                        cookiePro[HTTPCookiePropertyKey.domain] = htmlDomain
                        //: cookiePro.removeValue(forKey: HTTPCookiePropertyKey.discard)
                        cookiePro.removeValue(forKey: HTTPCookiePropertyKey.discard)
                        //: HTTPCookieStorage.shared.setCookie(HTTPCookie.init(properties: cookiePro)!)
                        HTTPCookieStorage.shared.setCookie(HTTPCookie(properties: cookiePro)!)
                    }
                }
            }
        }
    }

    //: func checkHtmlCookieIfNeedEarlyVersion() {
    func questionSomeone() {
        //: let htmlDomain: String = MarginExamineer.share.appConfigMode.urlH5Domain
        let htmlDomain: String = MarginExamineer.share.appConfigMode.urlH5Domain
        //: let url: URL = URL.init(string: htmlDomain)!
        let url = URL(string: htmlDomain)!
        //: let cookieArr: Array<HTTPCookie> = HTTPCookieStorage.shared.cookies(for: url)!
        let cookieArr: [HTTPCookie] = HTTPCookieStorage.shared.cookies(for: url)!
        //: var hasUID = false
        var hasUID = false
        //: var hasPHPSSID = false
        var hasPHPSSID = false
        //: for cookie in cookieArr {
        for cookie in cookieArr {
            //: if cookie.name == "UID" && cookie.value == MarginExamineer.share.loginUserMode.userID {
            if cookie.name == (showSpecialShKey.replacingOccurrences(of: "stream", with: "D")), cookie.value == MarginExamineer.share.loginUserMode.userID {
                //: hasUID = true
                hasUID = true
            }
            //: if cookie.name == "PHPSESSID" {
            if cookie.name == (show_everyData.replacingOccurrences(of: "block", with: "P") + appAbsStrokeValue.replacingOccurrences(of: "very", with: "P") + "SSID") {
                //: hasPHPSSID = true
                hasPHPSSID = true
            }
        }
        //: if !hasUID || !hasPHPSSID {
        if !hasUID || !hasPHPSSID {
            //: let cookieArr: Array<HTTPCookie> = HTTPCookieStorage.shared.cookies!
            let cookieArr: [HTTPCookie] = HTTPCookieStorage.shared.cookies!
            //: var appCookie: Dictionary<String, Any> =  Dictionary()
            var appCookie: [String: Any] = Dictionary()
            //: for cookie in cookieArr {
            for cookie in cookieArr {
                //: let tempServerUrlStr = self.serverUrlStr
                let tempServerUrlStr = self.serverUrlStr

                //: if tempServerUrlStr.contains(cookie.domain) {
                if tempServerUrlStr.contains(cookie.domain) {
                    //: if cookie.name.uppercased() == "UID" {
                    if cookie.name.uppercased() == (showSpecialShKey.replacingOccurrences(of: "stream", with: "D")) {
                        //: appCookie["UID"] = cookie
                        appCookie[(showSpecialShKey.replacingOccurrences(of: "stream", with: "D"))] = cookie
                    }
                    //: if cookie.name.uppercased() == "PHPSESSID" {
                    if cookie.name.uppercased() == (show_everyData.replacingOccurrences(of: "block", with: "P") + appAbsStrokeValue.replacingOccurrences(of: "very", with: "P") + "SSID") {
                        //: appCookie["PHPSESSID"] = cookie
                        appCookie[(show_everyData.replacingOccurrences(of: "block", with: "P") + appAbsStrokeValue.replacingOccurrences(of: "very", with: "P") + "SSID")] = cookie
                    }
                }
                //: if !hasUID && appCookie.keys.contains("UID") {
                if !hasUID, appCookie.keys.contains((showSpecialShKey.replacingOccurrences(of: "stream", with: "D"))) {
                    //: let uidCookie: HTTPCookie = appCookie["UID"] as! HTTPCookie
                    let uidCookie: HTTPCookie = appCookie[(showSpecialShKey.replacingOccurrences(of: "stream", with: "D"))] as! HTTPCookie
                    //: var uidPro: Dictionary<HTTPCookiePropertyKey, Any> = uidCookie.properties!
                    var uidPro: [HTTPCookiePropertyKey: Any] = uidCookie.properties!
                    //: uidPro[HTTPCookiePropertyKey.domain] = url.host
                    uidPro[HTTPCookiePropertyKey.domain] = url.host
                    //: uidPro.removeValue(forKey: HTTPCookiePropertyKey.discard)
                    uidPro.removeValue(forKey: HTTPCookiePropertyKey.discard)
                    //: HTTPCookieStorage.shared.setCookie(HTTPCookie.init(properties: uidPro)!)
                    HTTPCookieStorage.shared.setCookie(HTTPCookie(properties: uidPro)!)
                }
                //: if !hasPHPSSID && appCookie.keys.contains("PHPSESSID") {
                if !hasPHPSSID, appCookie.keys.contains((show_everyData.replacingOccurrences(of: "block", with: "P") + appAbsStrokeValue.replacingOccurrences(of: "very", with: "P") + "SSID")) {
                    //: let uidCookie: HTTPCookie = appCookie["PHPSESSID"] as! HTTPCookie
                    let uidCookie: HTTPCookie = appCookie[(show_everyData.replacingOccurrences(of: "block", with: "P") + appAbsStrokeValue.replacingOccurrences(of: "very", with: "P") + "SSID")] as! HTTPCookie
                    //: var uidPro: Dictionary<HTTPCookiePropertyKey, Any> = uidCookie.properties!
                    var uidPro: [HTTPCookiePropertyKey: Any] = uidCookie.properties!
                    //: uidPro[HTTPCookiePropertyKey.domain] = url.host
                    uidPro[HTTPCookiePropertyKey.domain] = url.host
                    //: uidPro.removeValue(forKey: HTTPCookiePropertyKey.discard)
                    uidPro.removeValue(forKey: HTTPCookiePropertyKey.discard)
                    //: HTTPCookieStorage.shared.setCookie(HTTPCookie.init(properties: uidPro)!)
                    HTTPCookieStorage.shared.setCookie(HTTPCookie(properties: uidPro)!)
                }
            }
        }
    }

    //: func func__saveAllCookies() {
    func lostAcross() {
        //: do {
        do {
            //: let cookies: Array<HTTPCookie> = HTTPCookieStorage.shared.cookies!
            let cookies: [HTTPCookie] = HTTPCookieStorage.shared.cookies!
            //: printLog(message: "func__saveAllCookies = \(cookies)")
            printLog(message: (String(noti_segmentLeadingPath.prefix(5)) + "_saveAll" + String(user_interestedPath.suffix(4)) + String(user_mmAliveUnknownString.suffix(6))) + "\(cookies)")
            //: let cookieData = try NSKeyedArchiver.archivedData(withRootObject: cookies)
            let cookieData = try NSKeyedArchiver.archivedData(withRootObject: cookies)
            //: Defaults.set(cookieData, forKey: TalkingUserCookiesKey)
            user_findBoundMessage.set(cookieData, forKey: mainTimeMsg)
            //: } catch _ {
        } catch _ {
            //: printLog(message: "func__saveAllCookies  error")
            printLog(message: (showConstraintName.replacingOccurrences(of: "player", with: "c") + "__sa" + String(app_pingCosPath.suffix(5)) + "Cookies" + String(main_heMsg.suffix(7))))
        }
    }

    //: func func__setAppCookies() {
    func fieldFor() {
        //: let data = Defaults.object(forKey: TalkingUserCookiesKey)
        let data = user_findBoundMessage.object(forKey: mainTimeMsg)
        //: if data != nil {
        if data != nil {
            //: let data2: NSArray = NSKeyedUnarchiver.unarchiveObject(with: data as! Data) as! NSArray
            let data2: NSArray = NSKeyedUnarchiver.unarchiveObject(with: data as! Data) as! NSArray
            //: for cookie in data2 {
            for cookie in data2 {
                //: HTTPCookieStorage.shared.setCookie(cookie as! HTTPCookie)
                HTTPCookieStorage.shared.setCookie(cookie as! HTTPCookie)
            }
        }
    }

    //: func func__removeAllCookies() {
    func checkerThroughBasic() {
        //: Defaults.removeObject(forKey: TalkingUserCookiesKey)
        user_findBoundMessage.removeObject(forKey: mainTimeMsg)
        //: let cookiesArr: Array<HTTPCookie> = HTTPCookieStorage.shared.cookies!
        let cookiesArr: [HTTPCookie] = HTTPCookieStorage.shared.cookies!
        //: for cookies in cookiesArr {
        for cookies in cookiesArr {
            //: printLog(message: "🍪 删除\(cookies.name)")
            printLog(message: "🍪 删除\(cookies.name)")
            //: HTTPCookieStorage.shared.deleteCookie(cookies)
            HTTPCookieStorage.shared.deleteCookie(cookies)
        }
    }
}
