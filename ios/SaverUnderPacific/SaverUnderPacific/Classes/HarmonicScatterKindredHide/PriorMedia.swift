
//: Declare String Begin

/*: "mf/business/giftList" :*/
fileprivate let appMinimizeKey:[Character] = ["m","f","/","b","u"]
fileprivate let noti_identifyHeroString:String = "sistaffs"
fileprivate let kSeaString:String = "pending greatList"

/*: "type" :*/
fileprivate let userDetailedWithinResumeId:[UInt8] = [0xf,0x2,0xb,0x1e]

private func minMerge(after num: UInt8) -> UInt8 {
    return num ^ 123
}

/*: "mf/chatRoom/giftList" :*/
fileprivate let show_boldVersionMsg:String = "mf/chautomatically access fast now cosy"
fileprivate let k_assetStr:String = "remind dynamic meetingm/gi"

/*: "live/giftList" :*/
fileprivate let k_associatedMessage:[Character] = ["l","i","v","e","/","g","i"]
fileprivate let showThirdAcceptData:String = "additional feature root contextftList"

/*: "IM/getMessageList" :*/
fileprivate let user_familyMessage:[Character] = ["I","M","/","g","e","t","M","e","s","s","a","g","e","L","i","s","t"]

/*: "mf/business/sendMsg" :*/
fileprivate let notiHolderMessage:[Character] = ["m","f","/","b"]
fileprivate let user_methodLiveFormat:String = "usiresume"
fileprivate let showCountervalMessage:String = "valid provider rate personal flexsendMsg"

/*: "mf/chatRoom/sendMsg" :*/
fileprivate let mainSystemMsg:[Character] = ["m","f","/","c","h","a","t","R","o","o","m","/","s","e","n","d","M","s","g"]

/*: "mf/business/sendGift" :*/
fileprivate let main_continuingSelectValue:String = "equipment system calculate greatmf/busine"
fileprivate let user_acheMsg:[Character] = ["s","s","/","s","e","n","d","G","i","f","t"]

/*: "mf/chatRoom/sendGift" :*/
fileprivate let kSlyPowderName:String = "change global logicmf/chat"
fileprivate let userResourceNumbereractionMessage:[Character] = ["R","o","o","m","/","s","e","n","d","G","i","f","t"]

/*: "live/sendGift" :*/
fileprivate let notiProcedureMsg:String = "maker contact sample thresholdlive/"
fileprivate let notiMidLevelPagePath:String = "sendGiftmove max change pop recommendation"

/*: "mf/user/getChatInfo" :*/
fileprivate let dataTitleValue:[Character] = ["m","f","/","u","s","e","r","/","g","e","t","C","h","a","t","I","n","f","o"]

/*: "IM/getMessageUserInfoList" :*/
fileprivate let notiMomentumName:String = "vertical bind guide logIM/get"
fileprivate let mainOvalData:String = "ageUam scan eh remove"
fileprivate let notiPeerOperationData:String = "template too detailed putserI"

/*: "mf/crush/whoLikeMeList" :*/
fileprivate let show_adId:String = "bath run practical reward coalitionmf/cru"
fileprivate let k_capFormat:String = "with help fire yieldoLik"
fileprivate let dataBagChangeValue:[Character] = ["t"]

/*: "mf/crush/ignore" :*/
fileprivate let noti_consumerTitle:[Character] = ["m","f","/","c","r","u","s","h","/","i","g","n","o","r"]
fileprivate let k_advertisingUrl:String = "vertical"

/*: "mf/crush/like" :*/
fileprivate let const_beanKey:String = "mf/crushtoward cluster manager server while"
fileprivate let k_floorKey:[Character] = ["/","l","i","k","e"]

/*: "mf/user/getRelation" :*/
fileprivate let userWeekContactListenName:String = "mf/usnormally ark"
fileprivate let data_seaId:String = "tRelyour state rocket maxi cos"

/*: "user/attention" :*/
fileprivate let mainFlushId:[UInt8] = [0x62,0x64,0x72,0x65,0x38,0x76,0x63,0x63,0x72,0x79,0x63,0x7e,0x78,0x79]

private func plateExpression(testimony num: UInt8) -> UInt8 {
    return num ^ 23
}

/*: "user/removeAttention" :*/
fileprivate let dataBodyFormat:String = "additional mutualuser/r"
fileprivate let show_menuConstructUrl:String = "Attclass specify"
fileprivate let const_petPath:[Character] = ["e","n","t","i","o","n"]

/*: "follow Success" :*/
fileprivate let constAgentValue:String = "FOLLO"
fileprivate let mainLocateId:String = "sub critical guidew S"
fileprivate let constMaleAutomobileAdvicePath:String = "ugameess"

/*: "unfollow Success" :*/
fileprivate let noti_untilValue:String = "ucorefol"
fileprivate let dataObserveData:String = "ccelast"

/*: "translate/msg" :*/
fileprivate let user_optionStr:String = "TRAN"
fileprivate let userEnableRemarkPath:[Character] = ["e","/","m","s","g"]

/*: "msgId" :*/
fileprivate let main_failureData:[UInt8] = [0x4,0x1a,0xe,0x20,0xd]

private func adviceFunctional(push num: UInt8) -> UInt8 {
    return num ^ 105
}

/*: "targetLng" :*/
fileprivate let const_femaleGraphFormat:[UInt8] = [0xf5,0xe0,0xf3,0xe6,0xe4,0xf5,0xcd,0xef,0xe6]

private func ourContinuing(remark num: UInt8) -> UInt8 {
    return num ^ 129
}

/*: "mf/greet/getAutoGreetContent" :*/
fileprivate let userScanAllowDeemData:[Character] = ["m","f","/","g","r","e","e","t"]
fileprivate let userOriginalPath:String = "edit always odd low/g"
fileprivate let mainPicData:[Character] = ["e","t","A","u","t","o","G","r","e","e","t","C","o","n","t","e","n","t"]

/*: "mf/greet/getIntimatePhoto" :*/
fileprivate let user_weeklyValue:[Character] = ["m","f","/","g","r","e","e","t"]
fileprivate let noti_evenSaveerId:[Character] = ["/","g","e","t","I","n","t","i","m"]
fileprivate let main_welcomeCarrierFormat:[Character] = ["a","t","e","P","h","o","t","o"]

/*: "toUid" :*/
fileprivate let constSucceedAppropriatePath:[UInt8] = [0xdc,0xc7,0xfd,0xc1,0xcc]

/*: "mf/chatRoom/info" :*/
fileprivate let main_listStr:String = "mf/chabody movement app union white"
fileprivate let const_processorDynamicsName:String = "m/infcountry voice recover clearly stream"
fileprivate let app_recoverMsg:String = "beneath"

/*: "mf/chatRoom/list" :*/
fileprivate let user_perHeMessage:String = "mf/chinstance transition"
fileprivate let appFragmentMessage:[Character] = ["a","t","R","o","o","m","/","l","i","s","t"]

/*: "roomId" :*/
fileprivate let data_popMessage:[UInt8] = [0xd,0xa,0xa,0x8,0xe4,0xff]

fileprivate func panEdge(mp num: UInt8) -> UInt8 {
    let value = Int(num) - 155
    if value < 0 {
        return UInt8(value + 256)
    } else {
        return UInt8(value)
    }
}

/*: "needExtraInfo" :*/
fileprivate let userDataId:[UInt8] = [0x59,0x50,0x50,0x4f,0x30,0x63,0x5f,0x5d,0x4c,0x34,0x59,0x51,0x5a]

fileprivate func tempKeyFrequency(you num: UInt8) -> UInt8 {
    let value = Int(num) - 235
    if value < 0 {
        return UInt8(value + 256)
    } else {
        return UInt8(value)
    }
}

/*: "mf/chatRoom/check" :*/
fileprivate let data_maintainCalculateFormat:[Character] = ["m","f","/","c","h","a"]
fileprivate let kHideTitle:String = "tRoomquality belong drawing judge"

/*: "mf/chatRoom/joinOrQuit" :*/
fileprivate let kScopeMutualPath:[Character] = ["m","f","/","c","h","a","t","R","o","o","m","/"]
fileprivate let user_riotRequireString:String = "map pact execute cleanjoinOr"

/*: "mf/chatRoom/members" :*/
fileprivate let appOperateTempoString:String = "recommend shape thumb found communicatemf/c"
fileprivate let user_remoteFormat:String = "maximum route knockoom/m"

/*: "mf/user/queryStatus" :*/
fileprivate let main_freshGivingPath:String = "mf/usecontent mention live"
fileprivate let constVerticalId:String = "parent"
fileprivate let show_tickUrl:[Character] = ["y","S","t","a","t","u","s"]

/*: "toUids" :*/
fileprivate let const_broadStr:[UInt8] = [0x4b,0x46,0x2c,0x40,0x3b,0x4a]

fileprivate func maximumScenarioInstance(swing num: UInt8) -> UInt8 {
    let value = Int(num) - 215
    if value < 0 {
        return UInt8(value + 256)
    } else {
        return UInt8(value)
    }
}

/*: "call/getCallTab" :*/
fileprivate let kMinimizeName:String = "mll"
fileprivate let const_effectHolderValue:[Character] = ["l","l","T","a","b"]

/*: "mf/user/remark" :*/
fileprivate let noti_tunnelString:[Character] = ["m","f","/","u","s","e","r","/","r","e","m","a"]
fileprivate let main_pairName:String = "cancelk"

/*: "content" :*/
fileprivate let appSortName:[UInt8] = [0x35,0x41,0x40,0x46,0x37,0x40,0x46]

fileprivate func taAbove(actual num: UInt8) -> UInt8 {
    let value = Int(num) + 46
    if value > 255 {
        return UInt8(value - 256)
    } else {
        return UInt8(value)
    }
}

/*: "top" :*/
fileprivate let user_lawMsg:[UInt8] = [0x3e,0x25,0x3a]

private func alreadyVisible(revenue num: UInt8) -> UInt8 {
    return num ^ 74
}

//: Declare String End

// __DEBUG__
// __CLOSE_PRINT__
//
//  PriorMedia.swift
//  AbroadTalking
//
//  Created by Charlotte on 2022/9/7.
//

//: import UIKit
import UIKit

//: public class TalkingChatRequestTool: NSObject {
public class PriorMedia: NSObject {
    //: public typealias RequestEndBlock = (_ t: Bool) -> Void
    public typealias RequestEndBlock = (_ t: Bool) -> Void
    //: typealias FinishBlocks = (_ succeed: Bool, _ result: NSDictionary) -> Void
    typealias FinishBlocks = (_ succeed: Bool, _ result: NSDictionary) -> Void
    //: public typealias RequestMsgUserListBlock = (_ succeed: Bool, _ dataArr: [DocumentLevelPurple]) -> Void
    public typealias RequestMsgUserListBlock = (_ succeed: Bool, _ dataArr: [DocumentLevelPurple]) -> Void

    /// 私信礼物面板
    /// - Parameters:
    ///   - type: 0=默认， 1=私密照片，2=私密视频,3=节目单
    ///   - completion: 回调
    //: class func req_ChatGiftList(type: String, completion: @escaping FinishBlock) {
    class func lastExtend(type: String, completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = ObtainMomentum()
        //: reqModel.requestPath = "mf/business/giftList"
        reqModel.requestPath = (String(appMinimizeKey) + noti_identifyHeroString.replacingOccurrences(of: "staff", with: "ne") + "s/gift" + String(kSeaString.suffix(4)))
        //: reqModel.params = ["type": type]
        reqModel.params = [String(bytes: userDetailedWithinResumeId.map{minMerge(after: $0)}, encoding: .utf8)!: type]
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        notiSignatureItemMessage.lightForPath(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 聊天室礼物面板
    /// - Parameters：
    ///   - completion: 回调
    //: class func req_ChatRoomGiftList(type: String, completion: @escaping FinishBlock) {
    class func pointCap(type: String, completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = ObtainMomentum()
        //: reqModel.requestPath = "mf/chatRoom/giftList"
        reqModel.requestPath = (String(show_boldVersionMsg.prefix(5)) + "atRoo" + String(k_assetStr.suffix(4)) + "ftList")
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        notiSignatureItemMessage.lightForPath(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 直播礼物面板
    /// - Parameters：
    ///   - completion: 回调
    //: class func req_LiveGiftList(type: String, completion: @escaping FinishBlock) {
    class func discover(type: String, completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = ObtainMomentum()
        //: reqModel.requestPath = "live/giftList"
        reqModel.requestPath = (String(k_associatedMessage) + String(showThirdAcceptData.suffix(6)))
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        notiSignatureItemMessage.lightForPath(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 请求系统消息
    /// - Parameter completion: 回调
    //: @objc class func req_sysMessageList(completion: @escaping FinishBlocks) {
    @objc class func err(completion: @escaping FinishBlocks) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = ObtainMomentum()
        //: reqModel.requestPath = "IM/getMessageList"
        reqModel.requestPath = (String(user_familyMessage))
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        notiSignatureItemMessage.lightForPath(model: reqModel) { succeed, result, errorModel in
            //: if succeed {
            if succeed {
                //: completion(true, result as! NSDictionary)
                completion(true, result as! NSDictionary)
            }
        }
    }

    /// 私聊发送消息
    /// - Parameters:
    ///   - param: 传参
    ///   - completion: 回调
    //: class func req_SendPrivete(param: Dictionary<String, Any>, completion: @escaping FinishBlock) {
    class func remind(param: [String: Any], completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = ObtainMomentum()
        //: reqModel.requestType = .POST
        reqModel.requestType = .POST
        //: reqModel.requestPath = "mf/business/sendMsg"
        reqModel.requestPath = (String(notiHolderMessage) + user_methodLiveFormat.replacingOccurrences(of: "resume", with: "n") + "ess/" + String(showCountervalMessage.suffix(7)))
        //: reqModel.params = param
        reqModel.params = param
        //: reqModel.showErrorStatusBar = false
        reqModel.showErrorStatusBar = false
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        notiSignatureItemMessage.lightForPath(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 公共聊天室发送消息
    /// - Parameters:
    ///   - param: 传参
    ///   - completion: 回调
    //: class func req_SendRoomMsg(param: Dictionary<String, Any>, completion: @escaping FinishBlock) {
    class func duringNative(param: [String: Any], completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel()
        let reqModel = ObtainMomentum()
        //: reqModel.requestType = .POST
        reqModel.requestType = .POST
        //: reqModel.requestPath = "mf/chatRoom/sendMsg"
        reqModel.requestPath = (String(mainSystemMsg))
        //: reqModel.params = param
        reqModel.params = param
        //: reqModel.showErrorStatusBar = false
        reqModel.showErrorStatusBar = false
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        notiSignatureItemMessage.lightForPath(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 送礼物
    /// - Parameters:
    ///   - param: 参数
    ///   - completion: 回调
    //: class func req_SendGiftPrivete( param: Dictionary<String, Any>, completion: @escaping FinishBlock) {
    class func along(param: [String: Any], completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = ObtainMomentum()
        //: reqModel.requestType = .POST
        reqModel.requestType = .POST
        //: reqModel.requestPath = "mf/business/sendGift"
        reqModel.requestPath = (String(main_continuingSelectValue.suffix(9)) + String(user_acheMsg))
        //: reqModel.params = param
        reqModel.params = param
        //: reqModel.showErrorStatusBar = false
        reqModel.showErrorStatusBar = false
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        notiSignatureItemMessage.lightForPath(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 聊天室送礼物
    /// - Parameters:
    ///   - param: 参数
    ///   - completion: 回调
    //: class func req_SendGiftChatRoom( param: Dictionary<String, Any>, completion: @escaping FinishBlock) {
    class func anonymousDoing(param: [String: Any], completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = ObtainMomentum()
        //: reqModel.requestType = .POST
        reqModel.requestType = .POST
        //: reqModel.requestPath = "mf/chatRoom/sendGift"
        reqModel.requestPath = (String(kSlyPowderName.suffix(7)) + String(userResourceNumbereractionMessage))
        //: reqModel.params = param
        reqModel.params = param
        //: reqModel.showErrorStatusBar = false
        reqModel.showErrorStatusBar = false
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        notiSignatureItemMessage.lightForPath(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 直播送礼物
    /// - Parameters:
    ///   - param: 参数
    ///   - completion: 回调
    //: class func req_SendGiftLive( param: Dictionary<String, Any>, completion: @escaping FinishBlock) {
    class func magnitudeensityMid(param: [String: Any], completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = ObtainMomentum()
        //: reqModel.requestType = .POST
        reqModel.requestType = .POST
        //: reqModel.requestPath = "live/sendGift"
        reqModel.requestPath = (String(notiProcedureMsg.suffix(5)) + String(notiMidLevelPagePath.prefix(8)))
        //: reqModel.params = param
        reqModel.params = param
        //: reqModel.showErrorStatusBar = false
        reqModel.showErrorStatusBar = false
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        notiSignatureItemMessage.lightForPath(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 请求聊天用户信息
    /// - Parameters:
    //: class func req_refreshUserChatInfo( param: Dictionary<String, Any>, completion: @escaping FinishBlock) {
    class func statDraft(param: [String: Any], completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = ObtainMomentum()
        //: reqModel.requestPath = "mf/user/getChatInfo"
        reqModel.requestPath = (String(dataTitleValue))
        //: reqModel.params = param
        reqModel.params = param
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        notiSignatureItemMessage.lightForPath(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 请求用户消息列表
    /// - Parameters:
    //: @objc class public func req_refreshMsgUserListInfo( param: Dictionary<String, Any>, completion: @escaping RequestMsgUserListBlock) {
    @objc public class func launchDownSound(param: [String: Any], completion: @escaping RequestMsgUserListBlock) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = ObtainMomentum()
        //: reqModel.requestPath = "IM/getMessageUserInfoList"
        reqModel.requestPath = (String(notiMomentumName.suffix(6)) + "Mess" + String(mainOvalData.prefix(4)) + String(notiPeerOperationData.suffix(4)) + "nfoList")
        //: reqModel.params = param
        reqModel.params = param
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        notiSignatureItemMessage.lightForPath(model: reqModel) { succeed, result, errorModel in
            //: guard succeed else {
            guard succeed else {
                //: completion(succeed, [])
                completion(succeed, [])
                //: return
                return
            }

            //: if let datas = Array<DocumentLevelPurple>.deserialize(from: result as? Array) {
            if let datas = Array<DocumentLevelPurple>.deserialize(from: result as? Array) {
                //: completion(succeed, datas as! [DocumentLevelPurple])
                completion(succeed, datas as! [DocumentLevelPurple])
            }
        }
    }

    /// 请求用户消息列表
    //: @objc class public func req_giftEffectDataUrl(giftUrl: String, path: String, completion: @escaping RequestEndBlock) {
    @objc public class func monster(giftUrl: String, path: String, completion: @escaping RequestEndBlock) {
        //: GJ.startGiftZipRequest(giftUrl: giftUrl, path: path) { succeed, result, errorModel in
        notiSignatureItemMessage.betweenVariable(giftUrl: giftUrl, path: path) { succeed, result, errorModel in
            //: completion(succeed)
            completion(succeed)
        }
    }

    /// 谁心动我 - 列表
    /// - Parameters:
    ///   - completion: 回调
    //: class func req_whoLikeMeList(params: Dictionary<String, Any>, completion: @escaping FinishBlock) {
    class func warningKey(params: [String: Any], completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = ObtainMomentum()
        //: reqModel.requestPath = "mf/crush/whoLikeMeList"
        reqModel.requestPath = (String(show_adId.suffix(6)) + "sh/wh" + String(k_capFormat.suffix(4)) + "eMeLis" + String(dataBagChangeValue))
        //: reqModel.params = params
        reqModel.params = params
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        notiSignatureItemMessage.lightForPath(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 谁心动我 - nope拒绝操作
    /// - Parameters:
    ///   - completion: 回调
    //: class func req_whoLikeMeIgnore(params: Dictionary<String, Any>, completion: @escaping FinishBlock) {
    class func viewGap(params: [String: Any], completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = ObtainMomentum()
        //: reqModel.requestPath = "mf/crush/ignore"
        reqModel.requestPath = (String(noti_consumerTitle) + k_advertisingUrl.replacingOccurrences(of: "vertical", with: "e"))
        //: reqModel.params = params
        reqModel.params = params
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        notiSignatureItemMessage.lightForPath(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 谁心动我 - like操作
    /// - Parameters:
    ///   - completion: 回调
    //: class func req_whoLikeMelike(params: Dictionary<String, Any>, completion: @escaping FinishBlock) {
    class func anyJump(params: [String: Any], completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = ObtainMomentum()
        //: reqModel.requestPath = "mf/crush/like"
        reqModel.requestPath = (String(const_beanKey.prefix(8)) + String(k_floorKey))
        //: reqModel.params = params
        reqModel.params = params
        //: reqModel.requestType = .POST
        reqModel.requestType = .POST
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        notiSignatureItemMessage.lightForPath(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 关注列表
    /// - Parameters:
    ///   - completion: 回调
    //: class func req_atationList(params: Dictionary<String, Any>, completion: @escaping FinishBlock) {
    class func monthFail(params: [String: Any], completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = ObtainMomentum()
        //: reqModel.requestPath = "mf/user/getRelation"
        reqModel.requestPath = (String(userWeekContactListenName.prefix(5)) + "er/ge" + String(data_seaId.prefix(4)) + "ation")
        //: reqModel.params = params
        reqModel.params = params
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        notiSignatureItemMessage.lightForPath(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 关注操作
    /// - Parameters:
    /// - isAttention: 关注/取消
    ///   - completion: 回调
    //: class func req_atationTool(isAttention: Bool, params: Dictionary<String, Any>, completion: @escaping FinishBlock) {
    class func consumptionOn(isAttention: Bool, params: [String: Any], completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = ObtainMomentum()
        //: let path = isAttention==true ? "user/attention" :"user/removeAttention"
        let path = isAttention == true ? String(bytes: mainFlushId.map{plateExpression(testimony: $0)}, encoding: .utf8)! : (String(dataBodyFormat.suffix(6)) + "emove" + String(show_menuConstructUrl.prefix(3)) + String(const_petPath))
        //: reqModel.requestPath = path
        reqModel.requestPath = path
        //: reqModel.params = params
        reqModel.params = params
        //: reqModel.showErrorStatusBar = false
        reqModel.showErrorStatusBar = false
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        notiSignatureItemMessage.lightForPath(model: reqModel) { succeed, result, errorModel in
            // 统一处理toast
            //: if succeed {
            if succeed {
                //: if isAttention {
                if isAttention {
                    //: object.func__showStatusBarSuccessMsg(showMsg: "follow Success".localized)
                    user_painterPath.nearEasy(showMsg: (constAgentValue.lowercased() + String(mainLocateId.suffix(3)) + constMaleAutomobileAdvicePath.replacingOccurrences(of: "game", with: "cc")).localized)
                    //: } else {
                } else {
                    //: object.func__showStatusBarSuccessMsg(showMsg: "unfollow Success".localized)
                    user_painterPath.nearEasy(showMsg: (noti_untilValue.replacingOccurrences(of: "core", with: "n") + "low Su" + dataObserveData.replacingOccurrences(of: "last", with: "ss")).localized)
                }

                //: } else {
            } else {
                //: object.func__showStatusBarErrorMsg(showMsg: errorModel?.errorMsg ?? "")
                user_painterPath.exitMarker(showMsg: errorModel?.errorMsg ?? "")
            }
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 文本翻译
    /// - Parameters:
    ///   - msgId: 消息ID
    ///   - type=0表示私聊消息，type=1表示聊天室消息
    ///   - completion: 回调
    //: @objc class public func req_translateMsg(msgId: String, type: Int, completion: @escaping (_ succeed: Bool, _ result: NSDictionary, _ errorCode: Int) -> Void) {
    @objc public class func signStyle(msgId: String, type: Int, completion: @escaping (_ succeed: Bool, _ result: NSDictionary, _ errorCode: Int) -> Void) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = ObtainMomentum()
        //: reqModel.requestPath = "translate/msg"
        reqModel.requestPath = (user_optionStr.lowercased() + "slat" + String(userEnableRemarkPath))
        //: reqModel.params = ["msgId": msgId, "targetLng": MovementStructure.getSystemLangCode(), "type": "\(type)"]
        reqModel.params = [String(bytes: main_failureData.map{adviceFunctional(push: $0)}, encoding: .utf8)!: msgId, String(bytes: const_femaleGraphFormat.map{ourContinuing(remark: $0)}, encoding: .utf8)!: MovementStructure.readyMerge(), String(bytes: userDetailedWithinResumeId.map{minMerge(after: $0)}, encoding: .utf8)!: "\(type)"]
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        notiSignatureItemMessage.lightForPath(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result as? NSDictionary ?? [:], errorModel?.errorCode ?? 0)
            completion(succeed, result as? NSDictionary ?? [:], errorModel?.errorCode ?? 0)
        }
    }

    /// 获取一键打招呼内容
    ///   - completion: 回调
    //: class func req_getAutoGreetContent(completion: @escaping FinishBlock) {
    class func mechanism(completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = ObtainMomentum()
        //: reqModel.requestPath = "mf/greet/getAutoGreetContent"
        reqModel.requestPath = (String(userScanAllowDeemData) + String(userOriginalPath.suffix(2)) + String(mainPicData))
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        notiSignatureItemMessage.lightForPath(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 获取随机私密照片
    ///   - completion: 回调
    //: class func req_getIntimatePhoto(toUid: String, completion: @escaping FinishBlock) {
    class func doinger(toUid: String, completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = ObtainMomentum()
        //: reqModel.requestPath = "mf/greet/getIntimatePhoto"
        reqModel.requestPath = (String(user_weeklyValue) + String(noti_evenSaveerId) + String(main_welcomeCarrierFormat))
        //: reqModel.params = ["toUid": toUid]
        reqModel.params = [String(bytes: constSucceedAppropriatePath.map{$0^168}, encoding: .utf8)!: toUid]
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        notiSignatureItemMessage.lightForPath(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 获取聊天广场在线人数和家族信息
    /// - Parameters:
    ///   - completion: 回调
    //: class func req_getChatRoomInfo(completion: @escaping FinishBlock) {
    class func insideThree(completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = ObtainMomentum()
        //: reqModel.requestPath = "mf/chatRoom/info"
        reqModel.requestPath = (String(main_listStr.prefix(6)) + "tRoo" + String(const_processorDynamicsName.prefix(5)) + app_recoverMsg.replacingOccurrences(of: "beneath", with: "o"))
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        notiSignatureItemMessage.lightForPath(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 聊天广场列表
    /// - Parameters:
    ///   - completion: 回调
    //: class func req_getChatRoomList(completion: @escaping FinishBlock) {
    class func atForDay(completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = ObtainMomentum()
        //: reqModel.requestPath = "mf/chatRoom/list"
        reqModel.requestPath = (String(user_perHeMessage.prefix(5)) + String(appFragmentMessage))
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        notiSignatureItemMessage.lightForPath(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 检测聊天室
    /// - Parameters:
    ///   - roomId: 聊天室Id
    ///   - completion: 回调
    //: class func req_checkChatRoom(roomId: String, completion: @escaping FinishBlock) {
    class func fragment(roomId: String, completion: @escaping FinishBlock) {
        //: let params = ["roomId": roomId, "needExtraInfo": "1"]
        let params = [String(bytes: data_popMessage.map{panEdge(mp: $0)}, encoding: .utf8)!: roomId, String(bytes: userDataId.map{tempKeyFrequency(you: $0)}, encoding: .utf8)!: "1"]
        //: let reqModel = TalkingRequestModel()
        let reqModel = ObtainMomentum()
        //: reqModel.requestPath = "mf/chatRoom/check"
        reqModel.requestPath = (String(data_maintainCalculateFormat) + String(kHideTitle.prefix(5)) + "/check")
        //: reqModel.params = params
        reqModel.params = params
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        notiSignatureItemMessage.lightForPath(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 进入或退出聊天室
    /// - Parameters:
    ///   - params: 参数
    ///   - completion: 回调
    //: class func req_chatRoomJoinOrQuit(params: [String: Any], completion: @escaping FinishBlock) {
    class func publishTo(params: [String: Any], completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel()
        let reqModel = ObtainMomentum()
        //: reqModel.requestPath = "mf/chatRoom/joinOrQuit"
        reqModel.requestPath = (String(kScopeMutualPath) + String(user_riotRequireString.suffix(6)) + "Quit")
        //: reqModel.params = params
        reqModel.params = params
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        notiSignatureItemMessage.lightForPath(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 聊天室在线名单
    /// - Parameters:
    ///   - completion: 回调
    //: class func req_getChatRoomMembers(params: Dictionary<String, Any>, completion: @escaping FinishBlock) {
    class func spendThe(params: [String: Any], completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = ObtainMomentum()
        //: reqModel.requestPath = "mf/chatRoom/members"
        reqModel.requestPath = (String(appOperateTempoString.suffix(4)) + "hatR" + String(user_remoteFormat.suffix(5)) + "embers")
        //: reqModel.params = params
        reqModel.params = params
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        notiSignatureItemMessage.lightForPath(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 批量查询消息列表用户在线状态
    /// - Parameters:
    ///   - toUids: 用户ids
    ///   - completion: 回调
    //: @objc class public func req_queryStatus(toUids: String, completion: @escaping (_ succeed: Bool, _ result: [[String: Any]]?) -> Void) {
    @objc public class func lineSquare(toUids: String, completion: @escaping (_ succeed: Bool, _ result: [[String: Any]]?) -> Void) {
        //: let reqModel = TalkingRequestModel()
        let reqModel = ObtainMomentum()
        //: reqModel.requestType = .POST
        reqModel.requestType = .POST
        //: reqModel.requestPath = "mf/user/queryStatus"
        reqModel.requestPath = (String(main_freshGivingPath.prefix(6)) + "r/que" + constVerticalId.replacingOccurrences(of: "parent", with: "r") + String(show_tickUrl))
        //: reqModel.params = ["toUids": toUids, "type": 1]
        reqModel.params = [String(bytes: const_broadStr.map{maximumScenarioInstance(swing: $0)}, encoding: .utf8)!: toUids, String(bytes: userDetailedWithinResumeId.map{minMerge(after: $0)}, encoding: .utf8)!: 1]
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        notiSignatureItemMessage.lightForPath(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result as? [[String: Any]] ?? [])
            completion(succeed, result as? [[String: Any]] ?? [])
        }
    }

    /// 拨打通话记录
    /// - Parameters:
    ///   - completion: 回调
    //: class func req_getCallTab(params: Dictionary<String, Any>, completion: @escaping FinishBlock) {
    class func produceerEnable(params: [String: Any], completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = ObtainMomentum()
        //: reqModel.requestPath = "call/getCallTab"
        reqModel.requestPath = (kMinimizeName.replacingOccurrences(of: "m", with: "ca") + "/getCa" + String(const_effectHolderValue))
        //: reqModel.requestType = .POST
        reqModel.requestType = .POST
        //: reqModel.params = params
        reqModel.params = params
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        notiSignatureItemMessage.lightForPath(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 新增/修改备注
    /// - Parameters:
    ///   - content: 备注内容
    ///   - isTop: 是否置顶，1：是，2：否
    ///   - toUid: 对方uid
    ///   - completion: 回调
    //: class func req_remark(content: String, isTop: Int, toUid: String, completion: @escaping FinishBlock) {
    class func whoPicAgain(content: String, isTop: Int, toUid: String, completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel()
        let reqModel = ObtainMomentum()
        //: reqModel.requestPath = "mf/user/remark"
        reqModel.requestPath = (String(noti_tunnelString) + main_pairName.replacingOccurrences(of: "cancel", with: "r"))
        //: reqModel.requestType = .POST
        reqModel.requestType = .POST
        //: reqModel.params = ["content": content, "top": isTop, "toUid": toUid]
        reqModel.params = [String(bytes: appSortName.map{taAbove(actual: $0)}, encoding: .utf8)!: content, String(bytes: user_lawMsg.map{alreadyVisible(revenue: $0)}, encoding: .utf8)!: isTop, String(bytes: constSucceedAppropriatePath.map{$0^168}, encoding: .utf8)!: toUid]
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        notiSignatureItemMessage.lightForPath(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }
}
