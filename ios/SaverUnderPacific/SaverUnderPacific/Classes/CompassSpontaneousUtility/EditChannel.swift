
//: Declare String Begin

/*: "IM退出登录失败，code: :*/
fileprivate let noti_judgeKey:String = "IM\u{9000}出"
fileprivate let app_automaticTitle:String = "before missing keyode:"

/*: , desc: :*/
fileprivate let appWelcomeHelloUrl:[Character] = [","," ","d","e","s"]
fileprivate let kGenerateName:String = "attach fragment vid ref hec:"

/*: "IM登录失败，code: :*/
fileprivate let main_skinEntityMessage:String = "IM登\u{5f55}\u{5931}"
fileprivate let show_remainingMessage:[Character] = [":"]

/*: "IM获取未读消息总数失败，code: :*/
fileprivate let notiRestrictionPath:[Character] = ["I","M","获","取","未","读","\u{6d88}"]
fileprivate let dataOfferId:String = "息总star"
fileprivate let const_followId:String = "\u{8d25}，code:"

/*: "crushBadge" :*/
fileprivate let appExData:[Character] = ["c"]
fileprivate let noti_curveUrl:[Character] = ["r","u","s","h","B","a","d","g","e"]

/*: "badNumber" :*/
fileprivate let showSingleKey:[UInt8] = [0xac,0xaf,0xaa,0x80,0xbb,0xa3,0xac,0xab,0xbc]

/*: "plaza" :*/
fileprivate let showEarlyName:[Character] = ["p","l","a","z","a"]

/*: "roomNum" :*/
fileprivate let showInformationName:[Character] = ["r","o","o","m","N"]
fileprivate let show_cordMsg:[Character] = ["u","m"]

/*: "num" :*/
fileprivate let data_strengthStr:String = "unctionm"

/*: "onlyOneRoomId" :*/
fileprivate let data_youRecordingUrl:[Character] = ["o","n"]
fileprivate let notiSuccessfullyString:[Character] = ["l","y","O","n","e","R","o","o","m","I","d"]

/*: "LIVE-CHAT" :*/
fileprivate let app_mixAgreeShouldData:String = "gray mistLIVE-"

/*: "extra" :*/
fileprivate let main_existCycleId:String = "exlocationa"

/*: "msgType" :*/
fileprivate let dataSlideUrl:String = "interaction"
fileprivate let noti_occurPath:[Character] = ["s","g","T","y","p","e"]

/*: "newPkgGift" :*/
fileprivate let kTakeJoinStr:[Character] = ["n","e","w","P","k"]
fileprivate let mainSelecterString:String = "gGiftexcept chapter"

/*: "MF:ChatMentionMsg" :*/
fileprivate let mainFindRunningId:String = "MF:Chatrobot alive observe following"
fileprivate let notiQuestionId:[Character] = ["M","e","n","t"]
fileprivate let show_placeStr:[Character] = ["i","o","n","M","s","g"]

/*: "uid" :*/
fileprivate let k_helloPath:[UInt8] = [0x5d,0x41,0x4c]

private func financeRapid(complete num: UInt8) -> UInt8 {
    return num ^ 40
}

/*: "onlineStatus" :*/
fileprivate let main_balanceData:[UInt8] = [0x2,0x3,0x1,0x4,0x3,0x8,0x3e,0x19,0xc,0x19,0x18,0x1e]

private func wrapEducator(fade num: UInt8) -> UInt8 {
    return num ^ 109
}

/*: "isNewUser" :*/
fileprivate let mainPhoneTitle:[UInt8] = [0xd3,0xdd,0xb8,0xcf,0xe1,0xbf,0xdd,0xcf,0xdc]

fileprivate func complaintPut(input num: UInt8) -> UInt8 {
    let value = Int(num) - 106
    if value < 0 {
        return UInt8(value + 256)
    } else {
        return UInt8(value)
    }
}

/*: "c2c_ :*/
fileprivate let mainCareString:String = "c2c_period shade"

/*: "IM获取会话失败，convId: :*/
fileprivate let dataYellowFormat:String = "minimize search tooIM获取会话失"
fileprivate let userGrayStr:[Character] = ["败","\u{ff0c}","c","o","n","v","I","d",":"]

/*: , code: :*/
fileprivate let k_logicWithoutFormat:[Character] = [","," ","c","o","d","e"]
fileprivate let notiBusyUsId:String = "group"

/*: "IM插入消息失败，targetId: :*/
fileprivate let mainArcExamplePath:String = "IM插入\u{6d88}"
fileprivate let show_spotMaximumName:String = "argetIddrag year rover"
fileprivate let const_sinceCountSystemString:[Character] = [":"]

/*: "changeDirection" :*/
fileprivate let data_helloPath:String = "areaan"
fileprivate let dataMaxiInfoId:String = "ctirecent"

/*: "rYMsgType" :*/
fileprivate let userDailyMessage:String = "rYMsgTypsine cookie beneath remark"
fileprivate let data_disableFractionHeadMsg:String = "E"

/*: "GJ:CallCustom" :*/
fileprivate let app_licenseName:[Character] = ["G","J",":","C","a","l","l","C","u","s","t","o","m"]

/*: "IM获取会话列表失败，code: :*/
fileprivate let userSpecifyTitle:String = "weight tap type perIM获取会话列"
fileprivate let dataFutureMemberUrl:String = "表失败\u{ff0c}code:"

/*: "msgInfo" :*/
fileprivate let appMergeMessage:String = "mini cap polomsgInfo"

/*: "needFold" :*/
fileprivate let data_mysteryString:String = "command accuracy media integrityneedFold"

/*: "group_ :*/
fileprivate let showGalleryAchingPath:String = "group_man final"

/*: "IM删除会话失败，convID: :*/
fileprivate let main_pressureValue:String = "IM删除会话hidden modify"
fileprivate let kDeployVeryUrl:String = "vID:leg para standard"

/*: "IM置顶会话失败，convId: :*/
fileprivate let main_problemKey:String = "IM\u{7f6e}顶会话失"
fileprivate let app_barKey:[Character] = ["败","\u{ff0c}","c","o","n","v","I","d",":"]

/*: "SDK 正在连接到服务器" :*/
fileprivate let show_smallManagerData:String = "SDK 正在连first coordinator place protection"
fileprivate let kMemberValue:[Character] = ["接","到","\u{670d}","务","\u{5668}"]

/*: "SDK 已经成功连接到服务器" :*/
fileprivate let constRunningPath:String = "within interested signal value scriptSDK "
fileprivate let const_privacyValue:String = "到\u{670d}\u{52a1}器"

/*: "IM连接服务器失败，code: :*/
fileprivate let showHungStreamString:String = "IM\u{8fde}接"
fileprivate let app_abovePath:String = "adjustment fail logic database，code:"

/*: , error: :*/
fileprivate let constConversionId:String = "report of practical, error"
fileprivate let app_mixValue:[Character] = [":"]

/*: "IM账号被挤下线。" :*/
fileprivate let main_flowUrl:String = "gnat"
fileprivate let dataDocumentValue:[Character] = ["M","\u{8d26}","号","被","\u{6324}","下","\u{7ebf}","。"]

/*: "Your account is logged in on another device and you are forced to go offline! If it is not done by me, the password may have been leaked, and it is recommended to change the password." :*/
fileprivate let main_quoteAtPath:[UInt8] = []

private func restorePlat(alongside num: UInt8) -> UInt8 {
    return num ^ 195
}

/*: "Offline Notification" :*/
fileprivate let user_videoName:[Character] = ["O","f","f","l","i","n","e"," "]
fileprivate let noti_bottomStr:String = "replacement asset purple limitedNoti"
fileprivate let showSizeWanderStr:String = "tiassert"

/*: "Cancel" :*/
fileprivate let k_inputStr:String = "Cancelpolo age maybe deny"

/*: "Re Login" :*/
fileprivate let user_noteKey:[Character] = ["R","e"," ","L","o","g"]
fileprivate let kAutomaticDescriptionMessage:String = "attribute"

//: Declare String End

// __DEBUG__
// __CLOSE_PRINT__
//
//  EditChannel.swift
//  AbroadTalking
//
//  Created by Hemming on 2023/3/7.
//

//: import UIKit
import UIKit

/// 视频通话【拨打方】代理
//: @objc public protocol IMManagerDelegate: NSObjectProtocol {
@objc public protocol RowMargin: NSObjectProtocol {
    /// 会话列表更新
    //: @objc optional func onRefreshConversationList(data: [TalkingConversationModel])
    @objc optional func typical(data: [FormSubtleModel])

    /// 未读数更新
    //: @objc optional func onUnreadMsgCountChanged(count: Int)
    @objc optional func taAcross(count: Int)

    /// 收到新消息
    //: @objc optional func onRecvNewMessage(msg: V2TIMMessage)
    @objc optional func custom(msg: V2TIMMessage)
}

/// 数据列表枚举
//: enum ChatListDataType {
enum ShapePhone {
    //: case normalList
    case normalList // 默认列表
    //: case topList
    case topList // 置顶列表
    //: case moreList
    case moreList // 更多消息（超过5天未回复）
}

//: @objcMembers public class EditChannel: NSObject {
@objcMembers public class EditChannel: NSObject {
    //: public static let shared = EditChannel()
    public static let shared = EditChannel()
    //: public dynamic var isConnection = false
    public dynamic var isConnection = false
    //: var isRefreshSysMsg = false
    var isRefreshSysMsg = false
    //: var lastSysTime: TimeInterval = 0
    var lastSysTime: TimeInterval = 0

    //: var officialList: [TalkingConversationModel] = []
    var officialList: [FormSubtleModel] = [] // 官方列表（like+系统通知+客服）
    //: var topConvList: [TalkingConversationModel] = []
    var topConvList: [FormSubtleModel] = [] // 置顶列表
    //: var norConvList: [TalkingConversationModel] = []
    var norConvList: [FormSubtleModel] = [] // 普通会话列表
    //: var chatRoomList: [TalkingConversationModel] = []
    var chatRoomList: [FormSubtleModel] = [] // 聊天室
    //: var moreMsgList: [TalkingConversationModel] = []
    var moreMsgList: [FormSubtleModel] = [] // 更多消息（超过5天未回复）

    /// 消息列表数据分页
    //: private var nextSeq: UInt64 = 0
    private var nextSeq: UInt64 = 0
    /// 多个代理容器
    //: private let multiDelegate: NSHashTable<AnyObject> = NSHashTable.weakObjects()
    private let multiDelegate: NSHashTable<AnyObject> = NSHashTable.weakObjects()
    /// 会话列表缓存数据（避免重复转换）
    //: private var convListCache = [String: TalkingConversationModel]()
    private var convListCache = [String: FormSubtleModel]()

    //: override private init() {
    override private init() {
        //: super.init()
        super.init()
        //: self.initialization()
        self.permanent()
    }

    // MARK: - SDK初始化

    //: func func__TXSDKInit() {
    func heapMessage() {
        //: let config = V2TIMSDKConfig()
        let config = V2TIMSDKConfig()
        //: config.logLevel = .LOG_DEBUG
        config.logLevel = .LOG_DEBUG
        //: V2TIMManager.sharedInstance().initSDK(TXIMAppID, config: config)
        V2TIMManager.sharedInstance().initSDK(app_volumeData, config: config)
        //: V2TIMManager.sharedInstance().addAdvancedMsgListener(listener: self)
        V2TIMManager.sharedInstance().addAdvancedMsgListener(listener: self)
        //: V2TIMManager.sharedInstance().addConversationListener(listener: self)
        V2TIMManager.sharedInstance().addConversationListener(listener: self)
        //: V2TIMManager.sharedInstance().add(self)
        V2TIMManager.sharedInstance().add(self)
    }

    // MARK: - 清除单例状态

    //: func initialization() {
    func permanent() {
        //: func__removeAllDelegate()
        graphicEnable()
        //: self.isConnection = false
        self.isConnection = false
        //: self.nextSeq = 0
        self.nextSeq = 0
        //: self.lastSysTime = 0
        self.lastSysTime = 0
        //: self.func__adddofficialList()
        self.host()
        //: self.convListCache.removeAll()
        self.convListCache.removeAll()
        //: self.norConvList.removeAll()
        self.norConvList.removeAll()
        //: self.topConvList.removeAll()
        self.topConvList.removeAll()
        //: self.moreMsgList.removeAll()
        self.moreMsgList.removeAll()
    }

    /// 添加官方数据模型
    //: func func__adddofficialList() {
    func host() {
        //: self.officialList.removeAll()
        self.officialList.removeAll()
        // 添加官方占位model
        //: let systemModel = TalkingConversationModel.getPlaceholderModel(chatType: .system)
        let systemModel = FormSubtleModel.option(chatType: .system)
        //: let serviceModel = TalkingConversationModel.getPlaceholderModel(chatType: .service)
        let serviceModel = FormSubtleModel.option(chatType: .service)
        //: self.officialList = [systemModel, serviceModel]
        self.officialList = [systemModel, serviceModel]
    }

    /// 获取官方数据列表（在审核模式下过滤客服消息不展示）
    /// - Returns: 模型
    //: func getOfficialList() -> [TalkingConversationModel] {
    func extend() -> [FormSubtleModel] {
        // 审核模式，在消息列表移除官方客服
        //: if MarginExamineer.share.appStatus == AppSkinStatus.special.rawValue {
        if MarginExamineer.share.appStatus == AwayPush.special.rawValue {
            //: if let index = self.officialList.firstIndex(where: { $0.userID == MovementStructure.getCustomerServiceID() }) {
            if let index = self.officialList.firstIndex(where: { $0.userID == MovementStructure.cartoonConnection() }) {
                //: self.officialList.remove(at: index)
                self.officialList.remove(at: index)
            }
        }
        //: return self.officialList
        return self.officialList
    }
}

// MARK: - 管理多个代理

//: extension EditChannel {
extension EditChannel {
    //: func func__removeAllDelegate() {
    func graphicEnable() {
        //: self.multiDelegate.removeAllObjects()
        self.multiDelegate.removeAllObjects()
    }

    //: func func__addDelegate(_ delegate: IMManagerDelegate) {
    func impressionSquare(_ delegate: RowMargin) {
        //: self.multiDelegate.add(delegate)
        self.multiDelegate.add(delegate)
    }

    //: func func__removeDelegate(_ delegate: IMManagerDelegate) {
    func betweenSomeone(_ delegate: RowMargin) {
        //: self.func__invokeDelegate {
        self.gray {
            //: if $0 === delegate as AnyObject {
            if $0 === delegate as AnyObject {
                //: self.multiDelegate.remove($0)
                self.multiDelegate.remove($0)
            }
        }
    }

    //: private func func__invokeDelegate(_ invocation: (IMManagerDelegate) -> Void) {
    private func gray(_ invocation: (RowMargin) -> Void) {
        //: for delegate in self.multiDelegate.allObjects.reversed() {
        for delegate in self.multiDelegate.allObjects.reversed() {
            //: invocation(delegate as! IMManagerDelegate)
            invocation(delegate as! RowMargin)
        }
    }
}

// MARK: - IM登录态管理

//: extension EditChannel {
extension EditChannel {
    /// 退出登录IM
    //: func func__LogingOut() {
    func logingOut() {
        //: V2TIMManager.sharedInstance().logout {} fail: { code, desc in
        V2TIMManager.sharedInstance().logout {} fail: { code, desc in
            //: UploadLogTool.writeLog(msg: "IM退出登录失败，code:\(code), desc:\(String(describing: desc)).")
            EvolutionDelayDestroy.afterTotalo(msg: (noti_judgeKey + "登录失败，c" + String(app_automaticTitle.suffix(4))) + "\(code)" + (String(appWelcomeHelloUrl) + String(kGenerateName.suffix(2))) + "\(String(describing: desc)).")
        }
        //: self.initialization()
        self.permanent()
    }

    /// 登录IM
    //: func func__LogingIn(completion: ((_ succeed: Bool) -> Void)? = nil) {
    func untilDiscoverQuick(completion: ((_ succeed: Bool) -> Void)? = nil) {
        //: guard self.isConnection == false else { return }
        guard self.isConnection == false else { return }
        //: let uSigStr = MarginExamineer.share.appUserConfigMode.uSig
        let uSigStr = MarginExamineer.share.appUserConfigMode.uSig
        //: let uidStr = MarginExamineer.share.loginUserMode.userID
        let uidStr = MarginExamineer.share.loginUserMode.userID
        //: V2TIMManager.sharedInstance().login(uidStr, userSig: uSigStr) {
        V2TIMManager.sharedInstance().login(uidStr, userSig: uSigStr) {
            //: self.isConnection = true
            self.isConnection = true
            //: DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 1) {
            DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 1) {
                //: self.func__getUnreadMsgCount()
                self.starAcross()
            }
            //: completion?(true)
            completion?(true)
            //: } fail: { code, desc in
        } fail: { code, desc in
            //: UploadLogTool.writeLog(msg: "IM登录失败，code:\(code), desc:\(String(describing: desc)).")
            EvolutionDelayDestroy.afterTotalo(msg: (main_skinEntityMessage + "败，code" + String(show_remainingMessage)) + "\(code)" + (String(appWelcomeHelloUrl) + String(kGenerateName.suffix(2))) + "\(String(describing: desc)).")
            //: self.isConnection = false
            self.isConnection = false
            //: completion?(false)
            completion?(false)
        }
    }

    /// 检测是否登录
    //: func func__checkCanOperateList() -> Bool {
    func aboveIgnore() -> Bool {
        //: let status = V2TIMManager.sharedInstance().getLoginStatus()
        let status = V2TIMManager.sharedInstance().getLoginStatus()
        //: if self.isConnection == false || status != .STATUS_LOGINED || MarginExamineer.share.networkStatus == .Unavailable {
        if self.isConnection == false || status != .STATUS_LOGINED || MarginExamineer.share.networkStatus == .Unavailable {
            //: self.func__showStatusBarErrorMsg(showMsg: kNetErrorMsg)
            self.exitMarker(showMsg: kRequestPath)
            //: return false
            return false
        }
        //: return true
        return true
    }
}

// MARK: - 获取未读消息数、wholikeme消息角标、聊天室信息

//: extension EditChannel {
extension EditChannel {
    /// 获取未读消息数
    //: func func__getUnreadMsgCount() {
    func starAcross() {
        //: V2TIMManager.sharedInstance().getTotalUnreadMessageCount { totalCount in
        V2TIMManager.sharedInstance().getTotalUnreadMessageCount { totalCount in
            //: self.onTotalUnreadMessageCountChanged(totalCount)
            self.onTotalUnreadMessageCountChanged(totalCount)
            //: } fail: { code, desc in
        } fail: { code, desc in
            //: UploadLogTool.writeLog(msg: "IM获取未读消息总数失败，code:\(code), desc:\(String(describing: desc)).")
            EvolutionDelayDestroy.afterTotalo(msg: (String(notiRestrictionPath) + dataOfferId.replacingOccurrences(of: "star", with: "数失") + const_followId) + "\(code)" + (String(appWelcomeHelloUrl) + String(kGenerateName.suffix(2))) + "\(String(describing: desc)).")
        }
    }

    /// 刷新wholikeme消息角标
    //: func func__refreshSysMsg() {
    func add() {
        //: let nowTs = NSDate.timeIntervalSinceReferenceDate
        let nowTs = NSDate.timeIntervalSinceReferenceDate
        //: if nowTs - self.lastSysTime < 10 || self.isRefreshSysMsg {
        if nowTs - self.lastSysTime < 10 || self.isRefreshSysMsg {
            //: return
            return
        }
        //: self.lastSysTime = nowTs
        self.lastSysTime = nowTs
        //: self.isRefreshSysMsg = true
        self.isRefreshSysMsg = true

        //: TalkingChatRequestTool.req_sysMessageList { _, result in
        PriorMedia.err { _, result in
            //: self.isRefreshSysMsg = false
            self.isRefreshSysMsg = false
            //: guard let dict = result as? [String: Any] else {
            guard let dict = result as? [String: Any] else {
                //: return
                return
            }
            //: let number = dict["crushBadge"] as! Int
            let number = dict[(String(appExData) + String(noti_curveUrl))] as! Int
            //: NotificationCenter.default.post(name: WHOLIKEME_BADGENUMBER_NOTIF, object: self, userInfo: ["badNumber": number])
            NotificationCenter.default.post(name: data_zoneName, object: self, userInfo: [String(bytes: showSingleKey.map{$0^206}, encoding: .utf8)!: number])
        }
    }

    /// 聊天室信息
    /// - Parameter completion: 回调
    //: func getChatRoomData(completion: @escaping (_ succeed: Bool) -> Void) {
    func session(completion: @escaping (_ succeed: Bool) -> Void) {
        //: chatRoomList.removeAll()
        chatRoomList.removeAll()
        //: TalkingChatRequestTool.req_getChatRoomInfo { succeed, result, _ in
        PriorMedia.insideThree { succeed, result, _ in
            //: guard succeed,
            guard succeed,
                  //: let data = result as? [String: Any],
                  let data = result as? [String: Any],
                  //: let plaza = data["plaza"] as? [String: Any] else {
                  let plaza = data[(String(showEarlyName))] as? [String: Any]
            else {
                //: completion(false)
                completion(false)
                //: return
                return
            }

            //: if plaza["roomNum"] as? Int ?? 0 > 0 {
            if plaza[(String(showInformationName) + String(show_cordMsg))] as? Int ?? 0 > 0 {
                //: let model = TalkingConversationModel.getPlaceholderModel(chatType: .group)
                let model = FormSubtleModel.option(chatType: .group)
                //: model.num = plaza["num"] as? Int ?? 0
                model.num = plaza[(data_strengthStr.replacingOccurrences(of: "unction", with: "nu"))] as? Int ?? 0
                //: model.onlyOneRoomId = plaza["onlyOneRoomId"] as? String ?? ""
                model.onlyOneRoomId = plaza[(String(data_youRecordingUrl) + String(notiSuccessfullyString))] as? String ?? ""
                //: self.chatRoomList.append(model)
                self.chatRoomList.append(model)
            }
            //: completion(true)
            completion(true)
        }
    }
}

// MARK: - V2TIMAdvancedMsgListener

//: extension EditChannel: V2TIMAdvancedMsgListener {
extension EditChannel: V2TIMAdvancedMsgListener {
    /// 接收到新消息
    /// - Parameter msg: 消息数据
    //: public func onRecvNewMessage(_ msg: V2TIMMessage!) {
    public func onRecvNewMessage(_ msg: V2TIMMessage!) {
        //: if msg.groupID != nil && (msg.groupID.contains("LIVE-CHAT")) { // 开播 弹幕提醒
        if msg.groupID != nil && (msg.groupID.contains((String(app_mixAgreeShouldData.suffix(5)) + "CHAT"))) { // 开播 弹幕提醒
            //: TalkingDanmuManager.shared().onRecvDanmuNewMsg(msg: msg)
            ScientificSimilar.panArc().visitorMedia(msg: msg)
            //: return
            return
        }
        //: if msg.sender == MarginExamineer.share.loginUserMode.userID { return }
        if msg.sender == MarginExamineer.share.loginUserMode.userID { return }

        //: let dic = NeedSpecify.parseTXMessageData(data: msg.customElem.data)
        let dic = NeedSpecify.onStrategy(data: msg.customElem.data)
        //: let extraDic = dic["extra"] as! [AnyHashable: Any]
        let extraDic = dic[(main_existCycleId.replacingOccurrences(of: "location", with: "tr"))] as! [AnyHashable: Any]
        //: let msgModel = try? AbTalkingChatMsgBaseModel(dictionary: extraDic)
        let msgModel = try? EnhanceModel(dictionary: extraDic)

        //: NotificationCenter.default.post(name: CHAT_NEW_MSG_NOTIFICATION, object: msg)
        NotificationCenter.default.post(name: mainGuidePath, object: msg)

        //: if let type = extraDic["msgType"] {
        if let type = extraDic[(dataSlideUrl.replacingOccurrences(of: "interaction", with: "m") + String(noti_occurPath))] {
            //: if type as? String == "newPkgGift" { // 刷新礼物背包
            if type as? String == (String(kTakeJoinStr) + String(mainSelecterString.prefix(5))) { // 刷新礼物背包
                //: NotificationCenter.default.post(name: GIFT_REFRESHPACKAGE_NOTIFICATION, object: nil)
                NotificationCenter.default.post(name: mainLockString, object: nil)
            }
        }

        //: if msg.userID != nil && msg.userID.count > 0 {
        if msg.userID != nil && msg.userID.count > 0 {
            //: if NeedSpecify.checkVoiceMessage(message: msg) {
            if NeedSpecify.reserveHost(message: msg) { // 语音消息
                //: let dic = TUISocialChatManager.packageDict(withTXMessage: msg)
                let dic = IndependentObserve.distanceContext(withTXMessage: msg)
                //: WCDBVoiceMsgTable.db_inserVoiceMsgToDB(dic)
                ComponentMultiple.liteAction(dic)
            }
            //: self.judgeisHaveIntimate(msg: msg, dic: dic)
            self.poolCount(msg: msg, dic: dic)
            // 处理礼物消息
            //: AbTalkingPrivateChatAnimatTool.shared.disposeReceiveGiftMsg(giftMessageDic: dic as NSDictionary)
            KindThrough.shared.multipleCalculate(giftMessageDic: dic as NSDictionary)
        }
        //: if msgModel?.msgInfo.messageType == .MessageTypeGift {
        if msgModel?.msgInfo.messageType == .MessageTypeGift { // 群聊收礼
            //: AbTalkingPrivateChatAnimatTool.shared.chatRoomDisposeReceiveGiftMsg(giftMessageDic: dic as NSDictionary)
            KindThrough.shared.sh(giftMessageDic: dic as NSDictionary)
        }
        //: if msg.customElem.extension == "MF:ChatMentionMsg" { // 群聊@ 消息
        if msg.customElem.extension == (String(mainFindRunningId.prefix(7)) + String(notiQuestionId) + String(show_placeStr)) { // 群聊@ 消息
            //: TalkingGroupChatManager.share.receivedMentionMsg(msg: msg, extraInfo: extraDic)
            HalfLength.share.oneMiss(msg: msg, extraInfo: extraDic)
        }

        //: if msgModel?.gift.unlockMsgId.count ?? 0 > 0 {
        if msgModel?.gift.unlockMsgId.count ?? 0 > 0 { // 记录解锁礼物消息
            //: var array: Array = UserDefaults.standard.object(forKey: TalkingPrivateChatManager.share.UnlockMsgIdArrayKey) as? [String] ?? [String]()
            var array: Array = UserDefaults.standard.object(forKey: InvisibleJob.share.UnlockMsgIdArrayKey) as? [String] ?? [String]()
            //: array.append(msgModel!.gift.unlockMsgId)
            array.append(msgModel!.gift.unlockMsgId)
            //: UserDefaults.standard.set(array, forKey: TalkingPrivateChatManager.share.UnlockMsgIdArrayKey)
            UserDefaults.standard.set(array, forKey: InvisibleJob.share.UnlockMsgIdArrayKey)
        }
        //: if msg.sender != MovementStructure.getXiaoMiID() && (msg.userID != nil && msg.userID.count > 0) {
        if msg.sender != MovementStructure.uniqueAbove() && (msg.userID != nil && msg.userID.count > 0) { // 过滤系统通知、群聊
            // delegate转发
            //: self.func__invokeDelegate { $0.onRecvNewMessage?(msg: msg) }
            self.gray { $0.custom?(msg: msg) }
            // 处理消息通知
            //: TalkingMessageNotificationManager.shared.onRecvNewMessage(msg: msg, dataDict: dic)
            HeadOptionFragment.shared.file(msg: msg, dataDict: dic)
            // 更新消息列表用户为在线状态
            //: DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 1) {
            DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 1) {
                //: let userInfo = ["uid": msg.userID ?? "",
                let userInfo = [String(bytes: k_helloPath.map{financeRapid(complete: $0)}, encoding: .utf8)!: msg.userID ?? "",
                                //: "onlineStatus": 1,
                                String(bytes: main_balanceData.map{wrapEducator(fade: $0)}, encoding: .utf8)!: 1,
                                //: "isNewUser": msgModel?.msgInfo.isNewUser ?? false] as [String: Any]
                                String(bytes: mainPhoneTitle.map{complaintPut(input: $0)}, encoding: .utf8)!: msgModel?.msgInfo.isNewUser ?? false] as [String: Any]
                //: NotificationCenter.default.post(name: MSGLIST_UPDATE_ONLINESTATUS_NOTIFICATION,
                NotificationCenter.default.post(name: main_restrictionMsg,
                                                //: object: nil,
                                                object: nil,
                                                //: userInfo: userInfo)
                                                userInfo: userInfo)
            }
        }
    }

    /// C2C 收到对端用户会话已读通知
    //: public func onRecvC2CReadReceipt(_ receiptList: [V2TIMMessageReceipt]!) {
    public func onRecvC2CReadReceipt(_ receiptList: [V2TIMMessageReceipt]!) {
        //: for receipt in receiptList {
        for receipt in receiptList {
            // 更新数据库和缓存
            //: DBUserInfoManager.cache_updateReadReceiptTime(with: receipt.userID, time: receipt.timestamp)
            ProgressAssociated.vertical(with: receipt.userID, time: receipt.timestamp)
            //: TalkingPrivateChatManager.share.readReceiptDict[receipt.userID] = receipt.timestamp
            InvisibleJob.share.readReceiptDict[receipt.userID] = receipt.timestamp
            // 发送已读回执通知
            //: NotificationCenter.default.post(name: CHAT_MSG_READ_RECEIPT_NOTIFICATION, object: receipt)
            NotificationCenter.default.post(name: main_subtleTakeStr, object: receipt)
        }
    }

    //: @objc public func func__handleMsg(targetId: String, msgCellData: AlongCrucial, isPrivete: Bool) {
    @objc public func all(targetId: String, msgCellData: AlongCrucial, isPrivete: Bool) {
        //: if let imMsg = msgCellData.innerMessage {
        if let imMsg = msgCellData.innerMessage {
            //: self.func__insertMsgToLocalStorage(targetId: targetId, imMsg: imMsg, isPrivete: isPrivete)
            self.scheduleUndertake(targetId: targetId, imMsg: imMsg, isPrivete: isPrivete)
        }
        //: var convId = ""
        var convId = ""
        //: if isPrivete {
        if isPrivete {
            //: convId = "c2c_\(targetId)"
            convId = (String(mainCareString.prefix(4))) + "\(targetId)"
        }
        //: DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 1) {
        DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 1) {
            //: V2TIMManager.sharedInstance().getConversation(convId) { [weak self] conv in
            V2TIMManager.sharedInstance().getConversation(convId) { [weak self] conv in
                //: guard let self = self else { return }
                guard let self = self else { return }
                //: if conv != nil {
                if conv != nil {
                    //: let list = self.func__handleConversationList(convList: [conv!])
                    let list = self.end(convList: [conv!])
                    //: self.func__updateConversationList(conVersationList: list)
                    self.universal(conVersationList: list)
                }
                //: } fail: { code, desc in
            } fail: { code, desc in
                //: UploadLogTool.writeLog(msg: "IM获取会话失败，convId:\(convId), code:\(code), desc:\(String(describing: desc)).")
                EvolutionDelayDestroy.afterTotalo(msg: (String(dataYellowFormat.suffix(7)) + String(userGrayStr)) + "\(convId)" + (String(k_logicWithoutFormat) + notiBusyUsId.replacingOccurrences(of: "group", with: ":")) + "\(code)" + (String(appWelcomeHelloUrl) + String(kGenerateName.suffix(2))) + "\(String(describing: desc)).")
            }
        }
    }

    //: func func__insertMsgToLocalStorage(targetId: String, imMsg: V2TIMMessage, isPrivete: Bool) {
    func scheduleUndertake(targetId: String, imMsg: V2TIMMessage, isPrivete: Bool) {
        //: let senderStr = MarginExamineer.share.loginUserMode.userID
        let senderStr = MarginExamineer.share.loginUserMode.userID
        //: if isPrivete {
        if isPrivete {
            //: V2TIMManager.sharedInstance().insertC2CMessage(toLocalStorage: imMsg, to: targetId, sender: senderStr) {} fail: { code, desc in
            V2TIMManager.sharedInstance().insertC2CMessage(toLocalStorage: imMsg, to: targetId, sender: senderStr) {} fail: { code, desc in
                //: UploadLogTool.writeLog(msg: "IM插入消息失败，targetId:\(targetId), code:\(code), desc:\(String(describing: desc)).")
                EvolutionDelayDestroy.afterTotalo(msg: (mainArcExamplePath + "息失败，t" + String(show_spotMaximumName.prefix(7)) + String(const_sinceCountSystemString)) + "\(targetId)" + (String(k_logicWithoutFormat) + notiBusyUsId.replacingOccurrences(of: "group", with: ":")) + "\(code)" + (String(appWelcomeHelloUrl) + String(kGenerateName.suffix(2))) + "\(String(describing: desc)).")
            }
        }
    }

    //: func judgeisHaveIntimate(msg: V2TIMMessage, dic: [String: Any]) {
    func poolCount(msg: V2TIMMessage, dic: [String: Any]) {
        //: let jsonDic = JSON(dic)
        let jsonDic = JSON(dic)
        //: let isSelf = jsonDic["changeDirection"].boolValue
        let isSelf = jsonDic[(data_helloPath.replacingOccurrences(of: "area", with: "ch") + "geDire" + dataMaxiInfoId.replacingOccurrences(of: "recent", with: "on"))].boolValue
        //: if jsonDic["rYMsgType"] == "GJ:CallCustom" && isSelf && OriginMediumWill.share.func__getCurrentActivityVC()!.isKind(of: TalkingPrivateChatController.self) == false {
        if jsonDic[(String(userDailyMessage.prefix(8)) + data_disableFractionHeadMsg.lowercased())].stringValue == (String(app_licenseName)) && isSelf && OriginMediumWill.share.halfSplit()!.isKind(of: ViaNative.self) == false {
            //: var intimate: [String: Any] = UserDefaults.standard.object(forKey: isConversationIntimateCacheKey) as? [String: Any] ?? Dictionary()
            var intimate: [String: Any] = UserDefaults.standard.object(forKey: userAboveRankStr) as? [String: Any] ?? Dictionary()
            //: intimate.updateValue(msg.userID ?? "", forKey: msg.userID)
            intimate.updateValue(msg.userID ?? "", forKey: msg.userID)
            //: UserDefaults.standard.set(intimate, forKey: isConversationIntimateCacheKey)
            UserDefaults.standard.set(intimate, forKey: userAboveRankStr)
        }
    }
}

// MARK: - V2TIMConversationListener【会话、未读数】

//: extension EditChannel: V2TIMConversationListener {
extension EditChannel: V2TIMConversationListener {
    /// 有新的会话
    //: public func onNewConversation(_ conversationList: [V2TIMConversation]!) {
    public func onNewConversation(_ conversationList: [V2TIMConversation]!) {
        //: let list = self.func__handleConversationList(convList: conversationList)
        let list = self.end(convList: conversationList)
        //: self.func__updateConversationList(conVersationList: list)
        self.universal(conVersationList: list)
    }

    /// 某些会话的关键信息发生变化
    //: public func onConversationChanged(_ conversationList: [V2TIMConversation]!) {
    public func onConversationChanged(_ conversationList: [V2TIMConversation]!) {
        //: let list = self.func__handleConversationList(convList: conversationList)
        let list = self.end(convList: conversationList)
        //: self.func__updateConversationList(conVersationList: list)
        self.universal(conVersationList: list)
    }

    /// 会话未读总数变更通知
    //: public func onTotalUnreadMessageCountChanged(_ totalUnreadCount: UInt64) {
    public func onTotalUnreadMessageCountChanged(_ totalUnreadCount: UInt64) {
        //: MarginExamineer.share.unreadMessageNum = Int(totalUnreadCount)
        MarginExamineer.share.unreadMessageNum = Int(totalUnreadCount)
        //: UIApplication.shared.applicationIconBadgeNumber = Int(totalUnreadCount)
        UIApplication.shared.applicationIconBadgeNumber = Int(totalUnreadCount)
        //: self.func__invokeDelegate {
        self.gray {
            //: $0.onUnreadMsgCountChanged?(count: Int(totalUnreadCount))
            $0.taAcross?(count: Int(totalUnreadCount))
        }
    }

    /// 批量更新会话列表
    //: func func__updateConversationList(conVersationList: [TalkingConversationModel]) {
    func universal(conVersationList: [FormSubtleModel]) {
        //: DispatchQueue.main.async {
        DispatchQueue.main.async {
            // 防止刚启动APP，消息列表还未初始化，先缓存数据，找合适时机批量请求
            //: if DBUserInfoManager.shared().needReqUidsSet != nil {
            if ProgressAssociated.betweenConsumption().needReqUidsSet != nil {
                // 提取非空 userId 并去重
                //: let set = Set(conVersationList.compactMap { $0.userID.isEmpty ? nil : $0.userID })
                let set = Set(conVersationList.compactMap { $0.userID.isEmpty ? nil : $0.userID })
                //: DBUserInfoManager.shared().needReqUidsSet!.formUnion(set)
                ProgressAssociated.betweenConsumption().needReqUidsSet!.formUnion(set)
            }
            //: self.func__invokeDelegate {
            self.gray {
                //: $0.onRefreshConversationList?(data: conVersationList)
                $0.typical?(data: conVersationList)
            }
        }
    }

    //: func func__handleTXConversationList(listArr: [V2TIMConversation], completion: @escaping (_ arr: [Any], _ leftMore: Bool) -> Void) {
    func inmate(listArr: [V2TIMConversation], completion: @escaping (_ arr: [Any], _ leftMore: Bool) -> Void) {
        //: var convList: [TalkingConversationModel] = []
        var convList: [FormSubtleModel] = []

        // V2TIMConversation->FormSubtleModel
        //: for conversation in listArr {
        for conversation in listArr {
            //: let targetId = (conversation.userID != nil && conversation.userID.count > 0) ? conversation.userID : conversation.groupID
            let targetId = (conversation.userID != nil && conversation.userID.count > 0) ? conversation.userID : conversation.groupID
            //: var tempConvModel = self.convListCache[targetId!]
            var tempConvModel = self.convListCache[targetId!]
            //: if tempConvModel == nil {
            if tempConvModel == nil {
                //: tempConvModel = TalkingConversationModel(conv: conversation)
                tempConvModel = FormSubtleModel(conv: conversation)
            }
            //: tempConvModel?.func__updateLastConvModel(conv: conversation)
            tempConvModel?.temporary(conv: conversation)
            //: convList.append(tempConvModel!)
            convList.append(tempConvModel!)
        }

        // 增量
        //: var resultList: [TalkingConversationModel] = []
        var resultList: [FormSubtleModel] = []
        //: let dbModels = WCDBUserInfoTable.db_getUserInfos(with: Array(self.convListCache.keys))
        let dbModels = TakeOval.untilLike(with: Array(self.convListCache.keys))
        //: let dbIdSet = Set(dbModels.map { $0.uid })
        let dbIdSet = Set(dbModels.map { $0.uid })
        //: for conversation in convList {
        for conversation in convList {
            // 添加db中是否有数据判断，防止刚启动APP，Delegate方法还未实现
            //: if self.func__addConvList(convModel: conversation, insertTop: false) ||
            if self.error(convModel: conversation, insertTop: false) ||
                //: (conversation.userID.count > 0 && !dbIdSet.contains(conversation.targetId))
                (conversation.userID.count > 0 && !dbIdSet.contains(conversation.targetId))
            {
                //: resultList.append(conversation)
                resultList.append(conversation)
            }
        }
        //: let leftMore = (resultList.count > 0)
        let leftMore = (resultList.count > 0)
        //: completion(resultList, leftMore)
        completion(resultList, leftMore)
    }
}

// MARK: - 会话排序、删除、置顶

//: extension EditChannel {
extension EditChannel {
    /// 获取消息列表数据
    /// - Parameter completion: 回调
    //: func func__getConversationList(completion: @escaping (_ arr: [Any], _ leftMore: Bool) -> Void) {
    func finish(completion: @escaping (_ arr: [Any], _ leftMore: Bool) -> Void) {
        //: V2TIMManager.sharedInstance().getConversationList(self.nextSeq, count: 40) { list, nextSeq, _ in
        V2TIMManager.sharedInstance().getConversationList(self.nextSeq, count: 40) { list, nextSeq, _ in
            //: self.nextSeq = nextSeq
            self.nextSeq = nextSeq
            //: self.func__handleTXConversationList(listArr: list!) { arr, leftMore in
            self.inmate(listArr: list!) { arr, leftMore in
                //: self.func__updateConversationList(conVersationList: arr as? [TalkingConversationModel] ?? [])
                self.universal(conVersationList: arr as? [FormSubtleModel] ?? [])
                //: completion(arr, leftMore)
                completion(arr, leftMore)
            }
            //: } fail: { code, desc in
        } fail: { code, desc in
            //: UploadLogTool.writeLog(msg: "IM获取会话列表失败，code:\(code), desc:\(String(describing: desc)).")
            EvolutionDelayDestroy.afterTotalo(msg: (String(userSpecifyTitle.suffix(7)) + dataFutureMemberUrl) + "\(code)" + (String(appWelcomeHelloUrl) + String(kGenerateName.suffix(2))) + "\(String(describing: desc)).")
        }
    }

    /// 从缓存会话中获取模型
    //: func func__getCacheConversationModel(targetId: String) -> TalkingConversationModel? {
    func above(targetId: String) -> FormSubtleModel? {
        //: return self.convListCache[targetId]
        return self.convListCache[targetId]
    }

    //: func func__handleConversationList(convList: [V2TIMConversation]) -> [TalkingConversationModel] {
    func end(convList: [V2TIMConversation]) -> [FormSubtleModel] {
        //: var tempArr: [TalkingConversationModel] = []
        var tempArr: [FormSubtleModel] = []
        //: for conv in convList {
        for conv in convList {
            //: let targetId = conv.userID ?? conv.groupID
            let targetId = conv.userID ?? conv.groupID
            //: var convModel = self.convListCache[targetId ?? ""]
            var convModel = self.convListCache[targetId ?? ""]
            //: if convModel == nil {
            if convModel == nil {
                //: convModel = TalkingConversationModel(conv: conv)
                convModel = FormSubtleModel(conv: conv)
            }
            // 更新会话
            //: convModel?.func__updateLastConvModel(conv: conv)
            convModel?.temporary(conv: conv)
            //: if self.func__addConvList(convModel: convModel!, insertTop: true) {
            if self.error(convModel: convModel!, insertTop: true) {
                //: tempArr.append(convModel!)
                tempArr.append(convModel!)
            }
        }
        //: return tempArr
        return tempArr
    }

    /// 会话列表新增会话
    /// - Parameters:
    ///   - convModel: 会话模型
    ///   - insertTop: 是否插入顶部
    /// - Returns: 是否添加成功
    //: func func__addConvList(convModel: TalkingConversationModel, insertTop: Bool) -> Bool {
    func error(convModel: FormSubtleModel, insertTop: Bool) -> Bool {
        //: if convModel.chatType == .unDefined {
        if convModel.chatType == .unDefined {
            //: return false
            return false
        }
        //: if !convModel.groupID.isEmptyString && convModel.chatType == .group { return false }
        if !convModel.groupID.isEmptyString && convModel.chatType == .group { return false }

        //: let targetId = convModel.targetId
        let targetId = convModel.targetId
        // 系统消息，无需重新排序
        //: if convModel.chatType == .system || convModel.chatType == .service {
        if convModel.chatType == .system || convModel.chatType == .service {
            //: self.convListCache[targetId] = convModel
            self.convListCache[targetId] = convModel
            //: if self.officialList.count == 0 {
            if self.officialList.count == 0 {
                //: self.func__adddofficialList()
                self.host()
            }
            //: if let index = officialList.firstIndex(where: { $0.userID == convModel.userID }) {
            if let index = officialList.firstIndex(where: { $0.userID == convModel.userID }) {
                //: let data = self.officialList[index]
                let data = self.officialList[index]
                //: convModel.showName = data.showName
                convModel.showName = data.showName
                //: self.officialList[index] = convModel
                self.officialList[index] = convModel
            }
            //: return true
            return true
        }

        // 普通消息
        //: var dataType: ChatListDataType = .normalList
        var dataType: ShapePhone = .normalList
        //: var listArr = self.norConvList
        var listArr = self.norConvList
        //: let timeInterval = Int(convModel.listShowMessage?.timestamp.timeIntervalSince1970 ?? 0) - MarginExamineer.share.appConfigMode.moreMessageTimestamp
        let timeInterval = Int(convModel.listShowMessage?.timestamp.timeIntervalSince1970 ?? 0) - MarginExamineer.share.appConfigMode.moreMessageTimestamp
        // 是否建立会话
        //: var haveSeSSion = false
        var haveSeSSion = false
        //: if let userModel = DBUserInfoManager.cache_getCachedUserInfo(targetId: convModel.targetId) {
        if let userModel = ProgressAssociated.betweenPolicy(targetId: convModel.targetId) {
            //: haveSeSSion = userModel.isHaveSession
            haveSeSSion = userModel.isHaveSession
        }
        // 是否需要折叠
        //: var needFold = false
        var needFold = false
        //: if let elemData = convModel.listShowMessage?.customElem.data {
        if let elemData = convModel.listShowMessage?.customElem.data {
            //: let dict = NeedSpecify.parseTXMessageData(data: elemData)
            let dict = NeedSpecify.onStrategy(data: elemData)
            //: let json = JSON(dict)
            let json = JSON(dict)
            //: needFold = json["extra"]["msgInfo"]["needFold"].boolValue
            needFold = json[(main_existCycleId.replacingOccurrences(of: "location", with: "tr"))][(String(appMergeMessage.suffix(7)))][(String(data_mysteryString.suffix(8)))].boolValue
        }
        // 非置顶，非自己发送，超出时间 || 未建立会话，需要折叠
        //: if (convModel.isPinned == false && convModel.listShowMessage?.isSelf == false && timeInterval <= 0) ||
        if (convModel.isPinned == false && convModel.listShowMessage?.isSelf == false && timeInterval <= 0) ||
            //: (!haveSeSSion && needFold && convModel.isPinned == false)
            (!haveSeSSion && needFold && convModel.isPinned == false)
        {
            //: dataType = .moreList
            dataType = .moreList
            //: listArr = self.moreMsgList
            listArr = self.moreMsgList
            // 移除重复数据
            //: self.topConvList.removeAll { $0.userID == convModel.userID }
            self.topConvList.removeAll { $0.userID == convModel.userID }
            //: self.norConvList.removeAll { $0.userID == convModel.userID }
            self.norConvList.removeAll { $0.userID == convModel.userID }

            //: } else {
        } else {
            // 移除更多消息
            //: self.moreMsgList.removeAll { $0.userID == convModel.userID }
            self.moreMsgList.removeAll { $0.userID == convModel.userID }
            // 置顶消息
            //: if convModel.isPinned {
            if convModel.isPinned {
                //: dataType = .topList
                dataType = .topList
                //: listArr = self.topConvList
                listArr = self.topConvList
            }
        }

        //: let tempConvModel = self.convListCache[targetId]
        let tempConvModel = self.convListCache[targetId]
        //: if tempConvModel != nil {
        if tempConvModel != nil {
            //: if insertTop && listArr.first != tempConvModel {
            if insertTop && listArr.first != tempConvModel {
                //: if let index = listArr.firstIndex(of: tempConvModel!) {
                if let index = listArr.firstIndex(of: tempConvModel!) {
                    //: listArr.remove(at: index)
                    listArr.remove(at: index)
                }
                //: listArr.append(tempConvModel!)
                listArr.append(tempConvModel!)
                //: self.func__sortConVList(convList: listArr, dataType: dataType)
                self.fireAirSpace(convList: listArr, dataType: dataType)
                //: return true
                return true
            }
            //: return false
            return false
        }

        //: self.convListCache[targetId] = convModel
        self.convListCache[targetId] = convModel
        //: listArr.append(convModel)
        listArr.append(convModel)
        //: self.func__sortConVList(convList: listArr, dataType: dataType)
        self.fireAirSpace(convList: listArr, dataType: dataType)
        //: return true
        return true
    }

    /// 数据排序
    /// - Parameters:
    ///   - convList: 会话列表
    ///   - dataType: 数据类型
    //: func func__sortConVList(convList: [TalkingConversationModel], dataType: ChatListDataType) {
    func fireAirSpace(convList: [FormSubtleModel], dataType: ShapePhone) {
        //: switch dataType {
        switch dataType {
        //: case .topList:
        case .topList:
            //: self.topConvList = convList.sorted { $0.orderKey > $1.orderKey }
            self.topConvList = convList.sorted { $0.orderKey > $1.orderKey }

        //: case .normalList:
        case .normalList:
            //: self.norConvList = convList.sorted { $0.orderKey > $1.orderKey }
            self.norConvList = convList.sorted { $0.orderKey > $1.orderKey }

        //: case .moreList:
        case .moreList:
            //: self.moreMsgList = convList.sorted { $0.orderKey > $1.orderKey }
            self.moreMsgList = convList.sorted { $0.orderKey > $1.orderKey }
        }
    }

    /// 删除会话
    /// - Parameters:
    ///   - targetId: 会话Id
    ///   - dataType: 会话类型
    //: func func__removeConversation(targetId: String, dataType: ChatListDataType) {
    func partner(targetId: String, dataType: ShapePhone) {
        //: guard !targetId.isEmpty else { return }
        guard !targetId.isEmpty else { return }
        //: guard let tempConvModel = self.convListCache[targetId] else { return }
        guard let tempConvModel = self.convListCache[targetId] else { return }
        //: let convID = (tempConvModel.chatType == .private ? "c2c_\(targetId)" : "group_\(targetId)")
        let convID = (tempConvModel.chatType == .private ? (String(mainCareString.prefix(4))) + "\(targetId)" : (String(showGalleryAchingPath.prefix(6))) + "\(targetId)")
        //: V2TIMManager.sharedInstance().deleteConversation(convID) {
        V2TIMManager.sharedInstance().deleteConversation(convID) {
            // 移除db
            //: DBUserInfoManager.db_removeRecord(with: targetId)
            ProgressAssociated.dailyWithinQuit(with: targetId)
            //: WCDBVoiceMsgTable.db_deleteVoiceMsg(MarginExamineer.share.loginUserMode.userID, toUid: targetId)
            ComponentMultiple.beyondEnvironment(MarginExamineer.share.loginUserMode.userID, toUid: targetId)

            //: self.convListCache.removeValue(forKey: targetId)
            self.convListCache.removeValue(forKey: targetId)
            //: switch dataType {
            switch dataType {
            //: case .topList:
            case .topList:
                //: if let index = self.topConvList.firstIndex(of: tempConvModel) {
                if let index = self.topConvList.firstIndex(of: tempConvModel) {
                    //: self.topConvList.remove(at: index)
                    self.topConvList.remove(at: index)
                }
            //: case .normalList:
            case .normalList:
                //: if let index = self.norConvList.firstIndex(of: tempConvModel) {
                if let index = self.norConvList.firstIndex(of: tempConvModel) {
                    //: self.norConvList.remove(at: index)
                    self.norConvList.remove(at: index)
                }
            //: case .moreList:
            case .moreList:
                //: if let index = self.moreMsgList.firstIndex(of: tempConvModel) {
                if let index = self.moreMsgList.firstIndex(of: tempConvModel) {
                    //: self.moreMsgList.remove(at: index)
                    self.moreMsgList.remove(at: index)
                }
            }

            //: let unreadCount = tempConvModel.unreadCount
            let unreadCount = tempConvModel.unreadCount
            //: if unreadCount > 0 {
            if unreadCount > 0 {
                //: tempConvModel.unreadCount = 0
                tempConvModel.unreadCount = 0
                //: tempConvModel.isShowMsgUnread = false
                tempConvModel.isShowMsgUnread = false
            }
            //: self.func__getUnreadMsgCount()
            self.starAcross()

            //: } fail: { code, desc in
        } fail: { code, desc in
            //: UploadLogTool.writeLog(msg: "IM删除会话失败，convID:\(convID), code:\(code), desc:\(String(describing: desc)).")
//            EvolutionDelayDestroy.afterTotalo(msg: (String(main_pressureValue.prefix(6)) + "失败，con" + String(kDeployVeryUrl.prefix(4))) + "\(convID)" + (String(k_logicWithoutFormat) + notiBusyUsId.replacingOccurrences(of: "group", with: ":")) + "\(code)" + (String(appWelcomeHelloUrl) + String(kGenerateName.suffix(2))) + "\(String(describing: desc)).")
        }
    }

    /// 置顶会话
    //: func func__setConversationTop(convModel: TalkingConversationModel, isTop: Bool) {
    func executeIn(convModel: FormSubtleModel, isTop: Bool) {
        //: V2TIMManager.sharedInstance().pinConversation(convModel.conversationID, isPinned: isTop) {} fail: { code, desc in
        V2TIMManager.sharedInstance().pinConversation(convModel.conversationID, isPinned: isTop) {} fail: { code, desc in
            //: UploadLogTool.writeLog(msg: "IM置顶会话失败，convId:\(convModel.conversationID), code:\(code), desc:\(String(describing: desc)).")
            EvolutionDelayDestroy.afterTotalo(msg: (main_problemKey + String(app_barKey)) + "\(convModel.conversationID)" + (String(k_logicWithoutFormat) + notiBusyUsId.replacingOccurrences(of: "group", with: ":")) + "\(code)" + (String(appWelcomeHelloUrl) + String(kGenerateName.suffix(2))) + "\(String(describing: desc)).")
        }
        //: convModel.isPinned = isTop
        convModel.isPinned = isTop
        //: if isTop {
        if isTop {
            //: self.topConvList.insert(convModel, at: 0)
            self.topConvList.insert(convModel, at: 0)
            //: if let index = self.norConvList.firstIndex(of: convModel) {
            if let index = self.norConvList.firstIndex(of: convModel) {
                //: self.norConvList.remove(at: index)
                self.norConvList.remove(at: index)
            }
            //: self.func__sortConVList(convList: self.topConvList, dataType: .topList)
            self.fireAirSpace(convList: self.topConvList, dataType: .topList)
            //: } else {
        } else {
            //: self.norConvList.insert(convModel, at: 0)
            self.norConvList.insert(convModel, at: 0)
            //: if let index = self.topConvList.firstIndex(of: convModel) {
            if let index = self.topConvList.firstIndex(of: convModel) {
                //: self.topConvList.remove(at: index)
                self.topConvList.remove(at: index)
            }
            //: self.func__sortConVList(convList: self.norConvList, dataType: .normalList)
            self.fireAirSpace(convList: self.norConvList, dataType: .normalList)
        }
    }
}

// MARK: - V2TIMSDKListener

//: extension EditChannel: V2TIMSDKListener {
extension EditChannel: V2TIMSDKListener {
    //: public func onConnecting() {
    public func onConnecting() {
        //: printLog(message: "SDK 正在连接到服务器")
        printLog(message: (String(show_smallManagerData.prefix(7)) + String(kMemberValue)))
    }

    //: public func onConnectSuccess() {
    public func onConnectSuccess() {
        //: printLog(message: "SDK 已经成功连接到服务器")
        printLog(message: (String(constRunningPath.suffix(4)) + "已\u{7ecf}\u{6210}功连接" + const_privacyValue))
    }

    //: public func onConnectFailed(_ code: Int32, err: String!) {
    public func onConnectFailed(_ code: Int32, err: String!) {
        //: UploadLogTool.writeLog(msg: "IM连接服务器失败，code:\(code), error:\(err!).")
        EvolutionDelayDestroy.afterTotalo(msg: (showHungStreamString + "服务器失败" + String(app_abovePath.suffix(6))) + "\(code)" + (String(constConversionId.suffix(7)) + String(app_mixValue)) + "\(err!).")
    }

    //: public func onKickedOffline() {
    public func onKickedOffline() {
        //: UploadLogTool.writeLog(msg: "IM账号被挤下线。")
        EvolutionDelayDestroy.afterTotalo(msg: (main_flowUrl.replacingOccurrences(of: "gnat", with: "I") + String(dataDocumentValue)))
        // socket断开连接
        //: TalkingSocketManager.shared.closeWebSocket(userLoginOut: true)
        MaterialCurrency.shared.processingPresent(userLoginOut: true)
        //: EditChannel.shared.isConnection = false
        EditChannel.shared.isConnection = false

        //: let msg = "Your account is logged in on another device and you are forced to go offline! If it is not done by me, the password may have been leaked, and it is recommended to change the password.".localized
        let msg = String(bytes: main_quoteAtPath.map{restorePlat(alongside: $0)}, encoding: .utf8)!.localized
        //: let config = ShowAlertConfig()
        let config = CustomIcon()
        //: if TalkingRequestAddrTool.share.interfaceLang == LangType.ar.rawValue || TalkingRequestAddrTool.share.interfaceLang == LangType.es.rawValue ||
        if PresentLocal.share.interfaceLang == StrokeHideBind.ar.rawValue || PresentLocal.share.interfaceLang == StrokeHideBind.es.rawValue ||
            //: TalkingRequestAddrTool.share.interfaceLang == LangType.pt.rawValue
            PresentLocal.share.interfaceLang == StrokeHideBind.pt.rawValue
        {
            //: config.width = 340
            config.width = 340
            //: config.rightFont = UIFont.pingfangMediumFont(fontSize: 13)
            config.rightFont = UIFont.hangProgram(fontSize: 13)
        }

        //: TalkingAlertShow.alert()
        InputMight.untilPhone()
        //: TalkingAlertShow.alert(title: "Offline Notification".localized, message: msg, leftBtnTitle: "Cancel".localized, rightBtnTitle: "Re Login".localized, leftBlock: {
        InputMight.untilPhone(title: (String(user_videoName) + String(noti_bottomStr.suffix(4)) + "fica" + showSizeWanderStr.replacingOccurrences(of: "assert", with: "on")).localized, message: msg, leftBtnTitle: (String(k_inputStr.prefix(6))).localized, rightBtnTitle: (String(user_noteKey) + kAutomaticDescriptionMessage.replacingOccurrences(of: "attribute", with: "in")).localized, leftBlock: {
            // 调用退出登录接口
            //: TalkingLoginRequestTool.req_loginOut { t in
            ConvertLocal.innerCountensity { t in
                //: ProgressHUD.dismiss()
                BeforeImagePhase.totalGap()
                //: if t {
                if t {
                    //: NotificationCenter.default.post(name: DID_LOGIN_OUT_SUCCESS_NOTIFICATION, object: nil, userInfo: nil)
                    NotificationCenter.default.post(name: mainOperationPadString, object: nil, userInfo: nil)
                }
            }
            //: }, rightBlock: {
        }, rightBlock: {
            //: TalkingLoginRequestTool.func__requestReloginUserInfo { t in
            ConvertLocal.overClear { t in
                //: if t {
                if t {
                    //: EditChannel.shared.func__LogingIn { _ in
                    EditChannel.shared.untilDiscoverQuick { _ in
                        /// 直播弹幕重连
                        //: if TalkingLiveManager.shared().liveRoomModel.chatGroupId.count > 0 {
                        if DeleteNorm.drawing().liveRoomModel.chatGroupId.count > 0 {
                            //: V2TIMManager.sharedInstance().joinGroup(TalkingLiveManager.shared().liveRoomModel.chatGroupId, msg: nil) {} fail: { _, _ in
                            V2TIMManager.sharedInstance().joinGroup(DeleteNorm.drawing().liveRoomModel.chatGroupId, msg: nil) {} fail: { _, _ in
                            }
                        }
                    }
                    // socket重连
                    //: TalkingSocketManager.shared.updateWebSocket()
                    MaterialCurrency.shared.crush()
                }
            }
            //: }, config: config)
        }, config: config)
    }
}
