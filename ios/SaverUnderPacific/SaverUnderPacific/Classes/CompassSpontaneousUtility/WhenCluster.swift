
//: Declare String Begin

/*: "mf/moment/list" :*/
fileprivate let noti_specifyFormat:[Character] = ["m","f","/","m","o","m","e","n","t","/","l","i","s","t"]

/*: "mf/moment/moments" :*/
fileprivate let userTermsStr:[Character] = ["m","f","/","m","o","m","e","n","t","/","m","o"]
fileprivate let k_shapeMsg:String = "madts"

/*: "mf/moment/like" :*/
fileprivate let show_relativeUrl:String = "mf/momready privacy"
fileprivate let user_answerValue:[Character] = ["k","e"]

/*: "mf/crush/send" :*/
fileprivate let noti_appearanceName:String = "option expression hiddenmf/c"
fileprivate let userFamilyActualName:[Character] = ["d"]

/*: "mf/moment/view" :*/
fileprivate let const_tingMessage:[Character] = ["m","f","/","m","o","m"]
fileprivate let app_obtainCanString:String = "privacy halfent/view"

/*: "mf/momentReply/list" :*/
fileprivate let user_runningSomethingStr:String = "member perceptionmf/mom"
fileprivate let mainQuitRelateKey:[Character] = ["l","y","/","l","i","s","t"]

/*: "mf/momentReply/save" :*/
fileprivate let appBoundCorrectTitle:String = "mf/momenmutual dimension collect observe"
fileprivate let mainEnableValue:String = "tRacross find warning"
fileprivate let userComprehensiveMsg:[Character] = ["e","p","l","y","/","s","a","v","e"]

/*: "mf/moment/videoSign" :*/
fileprivate let userPositionStr:[Character] = ["m","f","/","m","o","m","e","n","t","/","v","i","d","e"]
fileprivate let mainExpressionQuickStr:[Character] = ["o","S","i","g","n"]

/*: "dynamicPic.zip" :*/
fileprivate let noti_dealHomeFormat:[Character] = ["d"]
fileprivate let main_installMsg:String = "ynamdensityc"

/*: "pic" :*/
fileprivate let app_tooPath:[UInt8] = [0x1b,0x2,0x8]

/*: "content" :*/
fileprivate let constAutomaticPresentationName:String = "allownten"
fileprivate let data_contentMessage:String = "pop"

/*: "mf/moment/save" :*/
fileprivate let k_accountKey:String = "mf/mohalf thin visual rebuild front"
fileprivate let userFormalData:String = "momentum"

/*: "Publish Successful, Go to My Dynamics to view" :*/
fileprivate let dataTopAnimaMsg:[UInt8] = [0xfe,0x23,0x10,0x1a,0x17,0x21,0x16,0xce,0x1,0x23,0x11,0x11,0x13,0x21,0x21,0x14,0x23,0x1a,0xda,0xce,0xf5,0x1d,0xce,0x22,0x1d,0xce,0xfb,0x27,0xce,0xf2,0x27,0x1c,0xf,0x1b,0x17,0x11,0x21,0xce,0x22,0x1d,0xce,0x24,0x17,0x13,0x25]

fileprivate func poseProperty(create num: UInt8) -> UInt8 {
    let value = Int(num) + 82
    if value > 255 {
        return UInt8(value - 256)
    } else {
        return UInt8(value)
    }
}

/*: "mf/moment/saveVideo" :*/
fileprivate let app_specMessage:[Character] = ["m","f","/","m","o","m","e","n","t","/","s","a","v"]
fileprivate let main_coffeeCanFormat:String = "momentum occureVideo"

/*: .png" :*/
fileprivate let main_videoName:[Character] = [".","p","n","g"]

/*: "dynamicPic" :*/
fileprivate let const_provisionValue:String = "dgenderam"
fileprivate let notiArableValue:String = "icPiccare third bold leading"

/*: "freeImage" :*/
fileprivate let app_foundHisValue:[Character] = ["f","r","e"]
fileprivate let kLeaveRelatedLocateStr:String = "eImageprint disable elevator"

/*: "mf/momentReply/replylist" :*/
fileprivate let const_provideHelpTitle:String = "and running table discovermf/m"
fileprivate let app_nobodyStr:[Character] = ["l","y","/"]
fileprivate let dataBusinessMessage:[Character] = ["r","e","p","l","y","l","i","s","t"]

/*: "mf/moment/delete" :*/
fileprivate let kFunctionalTitle:String = "margin delay triggermf/m"
fileprivate let const_writtenIconValue:[Character] = ["e","l","e","t","e"]

/*: "mf/momentReply/delete" :*/
fileprivate let showYieldMoviePath:[Character] = ["m","f","/","m","o","m","e","n","t","R","e","p","l","y","/","d","e","l","e","t"]
fileprivate let notiSuspendNoStr:String = "explain"

/*: "mf/moment/pin" :*/
fileprivate let noti_composeSunLaunchValue:String = "mf/momgraphic hung occasion"

/*: "mf/moment/remindNum" :*/
fileprivate let const_thirdSpanUrl:String = "charm other scientific aspectmf/mo"
fileprivate let kSumString:String = "administrative down/rem"

/*: "mf/moment/remindList" :*/
fileprivate let dataAgreeStr:String = "mf/momeneasy ya cap"
fileprivate let main_characterUrl:String = "t/reexception her later single inside"

/*: "page" :*/
fileprivate let const_buttonFormat:[UInt8] = [0x24,0x35,0x33,0x31]

private func wireQueryer(frequency num: UInt8) -> UInt8 {
    return num ^ 84
}

/*: "cos/getCredential" :*/
fileprivate let const_distanceName:String = "pin challenge trade coffee pressurecos/ge"
fileprivate let user_factoryTitle:String = "entitransform"

/*: "type" :*/
fileprivate let kItemString:String = "typscript"

/*: "user/reportUserInfo" :*/
fileprivate let dataStartValue:[Character] = ["u","s","e","r","/","r","e","p","o","r","t"]
fileprivate let dataFitId:[Character] = ["U","s","e","r","I","n","f","o"]

/*: "uid" :*/
fileprivate let dataUnderlySkirtKey:String = "methodid"

/*: "deviceId" :*/
fileprivate let kDepressSpineTitle:String = "deviceIdmutual written filter"

/*: "deviceName" :*/
fileprivate let constPersistData:[Character] = ["d","e","v","i","c","e","N","a","m"]
fileprivate let kHidePath:String = "called"

/*: "iphone" :*/
fileprivate let userPromptTitle:[Character] = ["i","p","h","o","n","e"]

/*: "fileUrl" :*/
fileprivate let constResistancePath:[Character] = ["f","i","l","e","U","r","l"]

//: Declare String End

// __DEBUG__
// __CLOSE_PRINT__
//
//  WhenCluster.swift
//  AbroadTalking
//
//  Created by Charlotte on 2022/9/19.
//

//: import Adjust
import Adjust
//: import UIKit
import UIKit

//: class TalkingMomentRequestTool: NSObject {
class WhenCluster: NSObject {
    //: let _fm = FileManager.default
    let _fm = FileManager.default

    /// 动态列表接口
    /// - Parameters:
    ///   - completion: 回调
    //: class func req_MomentList(params: [String: Any], completion: @escaping FinishBlock) {
    class func slow(params: [String: Any], completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel()
        let reqModel = ObtainMomentum()
        //: reqModel.requestPath = "mf/moment/list"
        reqModel.requestPath = (String(noti_specifyFormat))
        //: reqModel.params = params
        reqModel.params = params
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        notiSignatureItemMessage.lightForPath(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 用户动态接口
    /// - Parameters:
    ///   - completion: 回调
    //: class func req_UserMomentList(params: [String: Any], completion: @escaping FinishBlock) {
    class func pastFire(params: [String: Any], completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel()
        let reqModel = ObtainMomentum()
        //: reqModel.requestPath = "mf/moment/moments"
        reqModel.requestPath = (String(userTermsStr) + k_shapeMsg.replacingOccurrences(of: "ad", with: "en"))
        //: reqModel.params = params
        reqModel.params = params
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        notiSignatureItemMessage.lightForPath(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 点赞
    /// - Parameters:
    ///   - completion: 回调
    //: class func req_MomentLike(params: [String: Any], completion: @escaping FinishBlock) {
    class func calendarSubsequent(params: [String: Any], completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel()
        let reqModel = ObtainMomentum()
        //: reqModel.requestPath = "mf/moment/like"
        reqModel.requestPath = (String(show_relativeUrl.prefix(6)) + "ent/li" + String(user_answerValue))
        //: reqModel.params = params
        reqModel.params = params
        //: reqModel.requestType = .POST
        reqModel.requestType = .POST
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        notiSignatureItemMessage.lightForPath(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 谁心动我 - 发送心动
    /// - Parameters:
    ///   - completion: 回调
    //: class func req_CrushSend(params: [String: Any], completion: @escaping FinishBlock) {
    class func column(params: [String: Any], completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel()
        let reqModel = ObtainMomentum()
        //: reqModel.requestPath = "mf/crush/send"
        reqModel.requestPath = (String(noti_appearanceName.suffix(4)) + "rush/sen" + String(userFamilyActualName))
        //: reqModel.params = params
        reqModel.params = params
        //: reqModel.requestType = .POST
        reqModel.requestType = .POST
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        notiSignatureItemMessage.lightForPath(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 动态详情
    /// - Parameters:
    ///   - completion: 回调
    //: class func req_MomentDetail(params: [String: Any], completion: @escaping FinishBlock) {
    class func plus(params: [String: Any], completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel()
        let reqModel = ObtainMomentum()
        //: reqModel.requestPath = "mf/moment/view"
        reqModel.requestPath = (String(const_tingMessage) + String(app_obtainCanString.suffix(8)))
        //: reqModel.params = params
        reqModel.params = params
        //: reqModel.requestType = .POST
        reqModel.requestType = .POST
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        notiSignatureItemMessage.lightForPath(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 动态评论列表
    /// - Parameters:
    ///   - completion: 回调
    //: class func req_CommentList(params: [String: Any], completion: @escaping FinishBlock) {
    class func mission(params: [String: Any], completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel()
        let reqModel = ObtainMomentum()
        //: reqModel.requestPath = "mf/momentReply/list"
        reqModel.requestPath = (String(user_runningSomethingStr.suffix(6)) + "entRep" + String(mainQuitRelateKey))
        //: reqModel.params = params
        reqModel.params = params
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        notiSignatureItemMessage.lightForPath(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 发表评论、回复
    /// - Parameters:
    ///   - completion: 回调
    //: class func req_SendComment(params: [String: Any], completion: @escaping FinishBlock) {
    class func role(params: [String: Any], completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel()
        let reqModel = ObtainMomentum()
        //: reqModel.requestPath = "mf/momentReply/save"
        reqModel.requestPath = (String(appBoundCorrectTitle.prefix(8)) + String(mainEnableValue.prefix(2)) + String(userComprehensiveMsg))
        //: reqModel.params = params
        reqModel.params = params
        //: reqModel.requestType = .POST
        reqModel.requestType = .POST
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        notiSignatureItemMessage.lightForPath(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 获取视频上传sign
    /// - Parameters:
    ///   - completion: 回调
    //: class func req_VideoSign(params: [String: Any], completion: @escaping FinishBlock) {
    class func cityWith(params: [String: Any], completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel()
        let reqModel = ObtainMomentum()
        //: reqModel.requestPath = "mf/moment/videoSign"
        reqModel.requestPath = (String(userPositionStr) + String(mainExpressionQuickStr))
        //: reqModel.requestType = .POST
        reqModel.requestType = .POST
        //: reqModel.params = params
        reqModel.params = params
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        notiSignatureItemMessage.lightForPath(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 动态发布
    /// - Parameters:
    ///   - photos: 图片
    ///   - content: 内容
    ///   - completion: 回调
    //: func releasePhotosMoment(photos: [UIImage], content: String, completion: @escaping FinishBlock) {
    func directly(photos: [UIImage], content: String, completion: @escaping FinishBlock) {
        // 处理图片
        //: deleteFile()
        month()
        //: for (index, photo) in photos.enumerated() {
        for (index, photo) in photos.enumerated() {
            //: getImagePath(index: index, image: photo)
            album(index: index, image: photo)
        }
        //: let documentDictPath = NSSearchPathForDirectoriesInDomains(.documentDirectory, .userDomainMask, true).first ?? ""
        let documentDictPath = NSSearchPathForDirectoriesInDomains(.documentDirectory, .userDomainMask, true).first ?? ""
        //: let doc_path = (documentDictPath as NSString).appendingPathComponent("dynamicPic.zip")
        let doc_path = (documentDictPath as NSString).appendingPathComponent((String(noti_dealHomeFormat) + main_installMsg.replacingOccurrences(of: "density", with: "i") + "Pic.zip"))
        //: SSZipArchive.createZipFile(atPath: doc_path, withContentsOfDirectory: filePath)
        SSZipArchive.createZipFile(atPath: doc_path, withContentsOfDirectory: filePath)
        //: DispatchQueue.global().async {
        DispatchQueue.global().async {
            //: let data = NSData(contentsOfFile: doc_path)
            let data = NSData(contentsOfFile: doc_path)
            //: DispatchQueue.main.async {
            DispatchQueue.main.async {
                // 发布
                //: var params: [String: Any] = ["pic": data as Any]
                var params: [String: Any] = [String(bytes: app_tooPath.map{$0^107}, encoding: .utf8)!: data as Any]
                //: if content.count > 0 {
                if content.count > 0 {
                    //: params["content"] = content
                    params[(constAutomaticPresentationName.replacingOccurrences(of: "allow", with: "co") + data_contentMessage.replacingOccurrences(of: "pop", with: "t"))] = content
                }
                //: let reqModel = TalkingRequestModel()
                let reqModel = ObtainMomentum()
                //: reqModel.requestPath = "mf/moment/save"
                reqModel.requestPath = (String(k_accountKey.prefix(5)) + "ment/sav" + userFormalData.replacingOccurrences(of: "momentum", with: "e"))
                //: reqModel.requestType = .POST
                reqModel.requestType = .POST
                //: reqModel.params = params
                reqModel.params = params
                //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
                notiSignatureItemMessage.lightForPath(model: reqModel) { succeed, result, errorModel in
                    //: if succeed {
                    if succeed {
                        //: self.func__showStatusBarSuccessMsg(showMsg: "Publish Successful, Go to My Dynamics to view".localized)
                        self.nearEasy(showMsg: String(bytes: dataTopAnimaMsg.map{poseProperty(create: $0)}, encoding: .utf8)!.localized)
                        //: self.deleteFile()
                        self.month()
                    }
                    //: completion(succeed, result, errorModel)
                    completion(succeed, result, errorModel)
                }
            }
        }
    }

    /// 动态发布
    /// - Parameters:
    ///   - completion: 回调
    //: class func releaseVideoMoment(params: [String: Any], completion: @escaping FinishBlock) {
    class func menu(params: [String: Any], completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel()
        let reqModel = ObtainMomentum()
        //: reqModel.requestPath = "mf/moment/saveVideo"
        reqModel.requestPath = (String(app_specMessage) + String(main_coffeeCanFormat.suffix(6)))
        //: reqModel.requestType = .POST
        reqModel.requestType = .POST
        //: reqModel.params = params
        reqModel.params = params
        //: reqModel.requestType = .POST
        reqModel.requestType = .POST
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        notiSignatureItemMessage.lightForPath(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    //: private func getImagePath(index: Int, image: UIImage) {
    private func album(index: Int, image: UIImage) {
        //: let data = image.compressedImageData()
        let data = image.prepare()
        //: let nameString = "\(index).png"
        let nameString = "\(index)" + (String(main_videoName))
        //: let picPath = (filePath as NSString).appendingPathComponent("dynamicPic")
        let picPath = (filePath as NSString).appendingPathComponent((const_provisionValue.replacingOccurrences(of: "gender", with: "yn") + String(notiArableValue.prefix(5))))
        //: if !_fm.fileExists(atPath: picPath) {
        if !_fm.fileExists(atPath: picPath) {
            //: try? _fm.createDirectory(atPath: picPath, withIntermediateDirectories: true)
            try? _fm.createDirectory(atPath: picPath, withIntermediateDirectories: true)
        }
        //: let imagePath = (picPath as NSString).appendingPathComponent(nameString)
        let imagePath = (picPath as NSString).appendingPathComponent(nameString)
        //: _fm.createFile(atPath: imagePath, contents: data)
        _fm.createFile(atPath: imagePath, contents: data)
    }

    //: private func deleteFile() {
    private func month() {
        //: if _fm.fileExists(atPath: filePath) {
        if _fm.fileExists(atPath: filePath) {
            //: try? _fm.removeItem(atPath: filePath)
            try? _fm.removeItem(atPath: filePath)
        }
    }

    //: private lazy var filePath: String = {
    private lazy var filePath: String = {
        //: let documentDictPath = NSSearchPathForDirectoriesInDomains(.documentDirectory, .userDomainMask, true).first ?? ""
        let documentDictPath = NSSearchPathForDirectoriesInDomains(.documentDirectory, .userDomainMask, true).first ?? ""
        //: let imagePath = (documentDictPath as NSString).appendingPathComponent("freeImage")
        let imagePath = (documentDictPath as NSString).appendingPathComponent((String(app_foundHisValue) + String(kLeaveRelatedLocateStr.prefix(6))))
        //: return imagePath
        return imagePath
        //: }()
    }()

    /// 子评论列表
    /// - Parameters:
    ///   - completion: 回调
    //: class func req_CommentDetailList(params: [String: Any], completion: @escaping FinishBlock) {
    class func scenario(params: [String: Any], completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel()
        let reqModel = ObtainMomentum()
        //: reqModel.requestPath = "mf/momentReply/replylist"
        reqModel.requestPath = (String(const_provideHelpTitle.suffix(4)) + "omentRep" + String(app_nobodyStr) + String(dataBusinessMessage))
        //: reqModel.params = params
        reqModel.params = params
        //: reqModel.requestType = .POST
        reqModel.requestType = .POST
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        notiSignatureItemMessage.lightForPath(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 删除动态
    /// - Parameters:
    ///   - completion: 回调
    //: class func req_DeleteMoment(params: [String: Any], completion: @escaping FinishBlock) {
    class func flex(params: [String: Any], completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel()
        let reqModel = ObtainMomentum()
        //: reqModel.requestPath = "mf/moment/delete"
        reqModel.requestPath = (String(kFunctionalTitle.suffix(4)) + "oment/d" + String(const_writtenIconValue))
        //: reqModel.params = params
        reqModel.params = params
        //: reqModel.requestType = .POST
        reqModel.requestType = .POST
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        notiSignatureItemMessage.lightForPath(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 删除评论
    /// - Parameters:
    ///   - completion: 回调
    //: class func req_DeleteMomentReply(params: [String: Any], completion: @escaping FinishBlock) {
    class func half(params: [String: Any], completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel()
        let reqModel = ObtainMomentum()
        //: reqModel.requestPath = "mf/momentReply/delete"
        reqModel.requestPath = (String(showYieldMoviePath) + notiSuspendNoStr.replacingOccurrences(of: "explain", with: "e"))
        //: reqModel.params = params
        reqModel.params = params
        //: reqModel.requestType = .POST
        reqModel.requestType = .POST
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        notiSignatureItemMessage.lightForPath(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 置顶操作
    /// - Parameters:
    /// status=1表示置顶，0=取消置顶
    ///   - completion: 回调
    //: class func req_TopMoment(params: [String: Any], completion: @escaping FinishBlock) {
    class func receive(params: [String: Any], completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel()
        let reqModel = ObtainMomentum()
        //: reqModel.requestPath = "mf/moment/pin"
        reqModel.requestPath = (String(noti_composeSunLaunchValue.prefix(6)) + "ent/pin")
        //: reqModel.params = params
        reqModel.params = params
        //: reqModel.requestType = .POST
        reqModel.requestType = .POST
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        notiSignatureItemMessage.lightForPath(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 获取用户未读动态总数
    /// - Parameter completion: 回调
    //: class func req_getRemindNum(completion: @escaping FinishBlock) {
    class func uniformSpot(completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel()
        let reqModel = ObtainMomentum()
        //: reqModel.requestPath = "mf/moment/remindNum"
        reqModel.requestPath = (String(const_thirdSpanUrl.suffix(5)) + "ment" + String(kSumString.suffix(4)) + "indNum")
        //: reqModel.requestType = .GET
        reqModel.requestType = .GET
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        notiSignatureItemMessage.lightForPath(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 获取当前未读动态信息列表
    /// - Parameters:
    ///   - page: 分页
    ///   - completion: 回调
    //: class func req_getRemindList(page: Int, completion: @escaping (_ data: [TalkingRemindListModel]) -> Void) {
    class func collection(page: Int, completion: @escaping (_ data: [AroundDark]) -> Void) {
        //: let reqModel = TalkingRequestModel()
        let reqModel = ObtainMomentum()
        //: reqModel.requestPath = "mf/moment/remindList"
        reqModel.requestPath = (String(dataAgreeStr.prefix(8)) + String(main_characterUrl.prefix(4)) + "mindList")
        //: reqModel.requestType = .GET
        reqModel.requestType = .GET
        //: reqModel.params = ["page": page]
        reqModel.params = [String(bytes: const_buttonFormat.map{wireQueryer(frequency: $0)}, encoding: .utf8)!: page]
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        notiSignatureItemMessage.lightForPath(model: reqModel) { succeed, result, errorModel in
            //: var dataArr: [TalkingRemindListModel] = []
            var dataArr: [AroundDark] = []
            //: guard let arr = result as? [[String: Any]] else {
            guard let arr = result as? [[String: Any]] else {
                //: completion(dataArr)
                completion(dataArr)
                //: return
                return
            }
            //: if let datas = Array<TalkingRemindListModel>.deserialize(from: arr as? Array) as? [TalkingRemindListModel] {
            if let datas = Array<AroundDark>.deserialize(from: arr as? Array) as? [AroundDark] {
                //: dataArr.append(contentsOf: datas)
                dataArr.append(contentsOf: datas)
            }
            //: completion(dataArr)
            completion(dataArr)
        }
    }

    /// COS  视频存储
    /// - Parameters:
    ///   - type: 1=动态，2=视频封面，3=私信视频
    ///   - completion: 回调
    //: class func req_getCredential(type: Int, completion: @escaping FinishBlock) {
    class func everyMonth(type: Int, completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel()
        let reqModel = ObtainMomentum()
        //: reqModel.requestPath = "cos/getCredential"
        reqModel.requestPath = (String(const_distanceName.suffix(6)) + "tCred" + user_factoryTitle.replacingOccurrences(of: "transform", with: "al"))
        //: reqModel.requestType = .GET
        reqModel.requestType = .GET
        //: reqModel.params = ["type": type]
        reqModel.params = [(kItemString.replacingOccurrences(of: "script", with: "e")): type]
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        notiSignatureItemMessage.lightForPath(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 上传cos日志存储路径
    /// - Parameters:
    ///   - fileUrl: 日志路径
    ///   - completion: 回调
    //: class func req_uploadLogPath(fileUrl: String, completion: @escaping FinishBlock) {
    class func arc(fileUrl: String, completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel()
        let reqModel = ObtainMomentum()
        //: reqModel.requestServer = MarginExamineer.share.appConfigMode.reportDomain
        reqModel.requestServer = MarginExamineer.share.appConfigMode.reportDomain
        //: reqModel.requestPath = "user/reportUserInfo"
        reqModel.requestPath = (String(dataStartValue) + String(dataFitId))
        //: reqModel.requestType = .POST
        reqModel.requestType = .POST
        //: var params = [String: Any]()
        var params = [String: Any]()
        //: params["uid"] = MarginExamineer.share.loginUserMode.userID
        params[(dataUnderlySkirtKey.replacingOccurrences(of: "method", with: "u"))] = MarginExamineer.share.loginUserMode.userID
        //: params["deviceId"] = Adjust.adid() ?? ""
        params[(String(kDepressSpineTitle.prefix(8)))] = Adjust.adid() ?? ""
        //: params["deviceName"] = "iphone"
        params[(String(constPersistData) + kHidePath.replacingOccurrences(of: "called", with: "e"))] = (String(userPromptTitle))
        //: params["type"] = 1
        params[(kItemString.replacingOccurrences(of: "script", with: "e"))] = 1
        //: params["fileUrl"] = fileUrl
        params[(String(constResistancePath))] = fileUrl
        //: reqModel.params = params
        reqModel.params = params
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        notiSignatureItemMessage.lightForPath(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }
}
