
//: Declare String Begin

/*: "cmd" :*/
fileprivate let kReportStr:[UInt8] = [0x64,0x6d,0x63]

/*: "cancelCall" :*/
fileprivate let showLargeYesVenueMsg:String = "caachingc"
fileprivate let main_familyTitle:[Character] = ["e","l","C","a","l","l"]

/*: "data" :*/
fileprivate let notiRemMessage:[UInt8] = [0x48,0x45,0x58,0x45]

fileprivate func transformInstead(production num: UInt8) -> UInt8 {
    let value = Int(num) - 228
    if value < 0 {
        return UInt8(value + 256)
    } else {
        return UInt8(value)
    }
}

/*: "logId" :*/
fileprivate let const_listenerKey:String = "logIdeh cycle news future"

/*: "#322870" :*/
fileprivate let mainDrawName:[Character] = ["#","3"]
fileprivate let dataBurnAyeValue:[Character] = ["2","2","8","7","0"]

/*: "Tap to start video chatting" :*/
fileprivate let userLiteBubbleFormat:String = "Tap tmode vertical"
fileprivate let userWithinValue:String = "bridge port calculate assert commont video c"
fileprivate let data_beneathKey:String = "hmedal"
fileprivate let userDeliveryUrl:String = "ttinoperation"

/*: "icon_close_nor" :*/
fileprivate let notiDisableInsideId:[Character] = ["i","c","o","n","_","c","l"]
fileprivate let constCommandFormat:String = "carrier natural depress length ridose_nor"

/*: "icon_views_number" :*/
fileprivate let main_speakerPath:String = "exclusive templateicon_"
fileprivate let k_innerFormat:String = "event local bases_nu"

/*: "%@ chatting" :*/
fileprivate let show_shareExistingString:[Character] = ["%","@"," "]
fileprivate let k_nativeMsg:[Character] = ["c","h","a","t","t","i","n","g"]

/*: "icon_lounge" :*/
fileprivate let show_everyoneKey:[Character] = ["i","c","o","n","_","l"]
fileprivate let showIonAgreeValue:[Character] = ["o","u","n","g","e"]

/*: "icon_rank_coin" :*/
fileprivate let appDistinctionStr:String = "icestimated"
fileprivate let mainRawMsg:String = "k_coinshare spot"

/*: "vip_btn_top" :*/
fileprivate let showNeedKey:String = "publisher yesterday slowvip_b"
fileprivate let showSpendStr:String = "P"

/*: "btn_nearby_settings_nor" :*/
fileprivate let const_privacyName:String = "btn_nentity around service two visible"
fileprivate let appPickJudgeName:String = "settuniform"

/*: "%@ Coins/min" :*/
fileprivate let kReStr:String = "%"
fileprivate let mainModeUrl:String = "strip social@ Coin"

/*: "img_match_connecting" :*/
fileprivate let appBalanceValue:String = "img_madimension role"
fileprivate let k_normallyString:String = "onnecsibling"
fileprivate let main_yourselfMsg:[Character] = ["i","n","g"]

/*: "#9F80FF" :*/
fileprivate let notiInvisibleMessage:String = "#9F80FFsale healthy public people"

/*: "get json error" :*/
fileprivate let mainDisappearPath:String = "ready"
fileprivate let notiManageStr:String = "et jsexample sequence pending natural head"
fileprivate let show_implementationId:String = "introintroointro"

/*: "matchId" :*/
fileprivate let showOutsidePath:String = "minute session orientation titi thirdmatchId"

/*: "errno" :*/
fileprivate let appTunKey:[UInt8] = [0x6e,0x79,0x79,0x65,0x64]

/*: "VIPFee" :*/
fileprivate let main_lastTradeFormat:String = "VIPFeepermanent skirt"

/*: "Matching now" :*/
fileprivate let constCanvasMainFormat:String = "Matchbrown bag vid"
fileprivate let appBadPressTitle:[Character] = ["i","n","g"," ","n","o","w"]

/*: "userTouch" :*/
fileprivate let data_actualVidId:[Character] = ["u"]
fileprivate let notiContainId:[Character] = ["s","e","r","T","o","u","c","h"]

/*: "mf/freeCall/getVideoInfo" :*/
fileprivate let app_withPostString:[Character] = ["m","f","/","f","r","e","e","C","a","l","l","/","g","e","t","V","i","d","e","o","I","n"]
fileprivate let notiBeautyValue:[Character] = ["f","o"]

/*: "fromType" :*/
fileprivate let mainCriticalValue:[UInt8] = [0xae,0xba,0xa7,0xa5,0x9c,0xb1,0xb8,0xad]

private func opTotalerval(status num: UInt8) -> UInt8 {
    return num ^ 200
}

/*: "You've been barred from receiving calls" :*/
fileprivate let k_skinString:[UInt8] = [0xbe,0x88,0x92,0xc0,0x91,0x82,0xc7,0x85,0x82,0x82,0x89,0xc7,0x85,0x86,0x95,0x95,0x82,0x83,0xc7,0x81,0x95,0x88,0x8a,0xc7,0x95,0x82,0x84,0x82,0x8e,0x91,0x8e,0x89,0x80,0xc7,0x84,0x86,0x8b,0x8b,0x94]

private func whipInvite(deadline num: UInt8) -> UInt8 {
    return num ^ 231
}

/*: "source" :*/
fileprivate let const_serviceAMessage:[UInt8] = [0x65,0x63,0x72,0x75,0x6f,0x73]

/*: "type" :*/
fileprivate let userMountUrl:[UInt8] = [0x65,0x70,0x79,0x74]

/*: "uid" :*/
fileprivate let notiPullName:[UInt8] = [0x64,0x69,0x75]

/*: "fromFreeCall" :*/
fileprivate let showProgressReliabilityId:[Character] = ["f","r","o","m","F","r","e","e","C","a","l","l"]

/*: "requestCall" :*/
fileprivate let appConnectSquareName:String = "requestCenter screening core"
fileprivate let showPartnerFormat:String = "aoriginal"

/*: "onVideoMatch" :*/
fileprivate let k_limitedName:[Character] = ["o","n","V","i","d","e","o","M","a"]
fileprivate let k_readyString:String = "tcleading"

/*: "[Automatic video] Receive video call incoming" :*/
fileprivate let notiHomeName:[UInt8] = [0xf,0xf5,0x29,0x28,0x23,0x21,0x15,0x28,0x1d,0x17,0xd4,0x2a,0x1d,0x18,0x19,0x23,0x11,0xd4,0x6,0x19,0x17,0x19,0x1d,0x2a,0x19,0xd4,0x2a,0x1d,0x18,0x19,0x23,0xd4,0x17,0x15,0x20,0x20,0xd4,0x1d,0x22,0x17,0x23,0x21,0x1d,0x22,0x1b]

fileprivate func clickChange(used num: UInt8) -> UInt8 {
    let value = Int(num) + 76
    if value > 255 {
        return UInt8(value - 256)
    } else {
        return UInt8(value)
    }
}

/*: "isRandomVCPush" :*/
fileprivate let notiSubstantialTitle:[Character] = ["i","s","R","a","n","d","o","m","V","C","P","u","s","h"]

/*: "refuseCall" :*/
fileprivate let showOperationMessage:String = "rewords"

/*: "onCancelCall" :*/
fileprivate let main_sourceTitle:[Character] = ["o","n","C","a","n"]
fileprivate let data_administrativeTitle:[Character] = ["c","e","l","C","a","l","l"]

/*: "retryAfter" :*/
fileprivate let kShareAriaId:[Character] = ["r","e","t","r","y","A","f"]
fileprivate let show_customerUrl:[Character] = ["t","e","r"]

/*: "onRequestCall" :*/
fileprivate let appMinimizePath:String = "from pi leave replace prepareonReque"
fileprivate let dataRomanItName:[Character] = ["s","t","C","a","l","l"]

/*: "failType" :*/
fileprivate let notiResWireStr:String = "fsearcherl"

/*: "callWaitingTime" :*/
fileprivate let appCorrectKey:String = "social visitor export coordinatecallW"
fileprivate let dataProviderTitle:String = "gTimeenhance detailed modify"

/*: "toUid" :*/
fileprivate let k_sectionThumbFormat:String = "dynamic spring whiletoUid"

/*: "Free 1 min" :*/
fileprivate let appNeedLessMessage:[Character] = ["F","r","e","e"," ","1"," ","m","i","n"]

/*: "%@/min" :*/
fileprivate let appLogId:[Character] = ["%","@","/","m","i","n"]

//: Declare String End

// __DEBUG__
// __CLOSE_PRINT__
//
//  AssociatedStage.swift
//  AbroadTalking
//
//  Created by young on 2023/8/22.
//

//: import UIKit
import UIKit

//: class TalkingRandomVideoViewController: TalkingBaseViewController {
class AssociatedStage: EasyChainSensor {
    //: var fee = 0
    var fee = 0 // 原价
    //: var vipFee = 0
    var vipFee = 0 // VIP价格
    //: var chattingNum = 0
    var chattingNum = 0 // 当前匹配人数
    //: private var matchId: Int = 0
    private var matchId: Int = 0 // 速配批次Id
    //: private var waitingTimer: Timer?
    private var waitingTimer: Timer? // 呼叫等待定时（等待15s）
    //: private var retryAfterTimer: Timer?
    private var retryAfterTimer: Timer? // 匹配失败重试定时器
    //: private var logId = 0
    private var logId = 0 // 通话id
    //: private var isShowFreeVideo = false
    private var isShowFreeVideo = false // 是否已展示免费视频挽留弹窗
    //: var beginTime: TimeInterval?
    var beginTime: TimeInterval?
    //: private var dispatchWorkItem: DispatchWorkItem?
    private var dispatchWorkItem: DispatchWorkItem?
    //: private var closeBtnWorkItem: DispatchWorkItem?
    private var closeBtnWorkItem: DispatchWorkItem?
    //: var isFirstRandow = true
    var isFirstRandow = true
    //: var isBeginRand = false
    var isBeginRand = false

    //: override func viewWillAppear(_ animated: Bool) {
    override func viewWillAppear(_ animated: Bool) {
        //: super.viewWillAppear(animated)
        super.viewWillAppear(animated)
        //: DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 1.0) {
        DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 1.0) {
            //: if !self.isFirstRandow {
            if !self.isFirstRandow {
                //: self.refreshVideoCallPrice()
                self.cellForReady()
            }
        }
        //: self.resetCamera()
        self.mapChannel()
    }

    //: override func viewDidLoad() {
    override func viewDidLoad() {
        //: super.viewDidLoad()
        super.viewDidLoad()
        //: self.hideNavi = true
        self.hideNavi = true

        //: setupSubviews()
        outTime()
        //: setupSubViewsConstraint()
        addMusic()
        //: bindInteraction()
        momentumSpan()
        //: videoManager.startPreview(isMatch: true)
        videoManager.fitByCoordinator(isMatch: true)
        //: setPlayer(isRandon: false)
        nearRecognize(isRandon: false)
        //: if isBeginRand {
        if isBeginRand {
            //: start_randomVideo()
            windowOf()
        }
    }

    //: deinit {
    deinit {
        //: if waitingTimer != nil {
        if waitingTimer != nil {
            //: TalkingSocketManager.shared.sendMessage(info: ["cmd": "cancelCall", "data": ["logId": logId]])
            MaterialCurrency.shared.warningVersion(info: [String(bytes: kReportStr.reversed(), encoding: .utf8)!: (showLargeYesVenueMsg.replacingOccurrences(of: "aching", with: "n") + String(main_familyTitle)), String(bytes: notiRemMessage.map{transformInstead(production: $0)}, encoding: .utf8)!: [(String(const_listenerKey.prefix(5))): logId]])
        }
        //: NotificationCenter.default.removeObserver(self)
        NotificationCenter.default.removeObserver(self)
        //: destroyWaitingTimer()
        priority()
        //: if TalkingSocketManager.shared.pacific == false {
        if MaterialCurrency.shared.pacific == false {
            //: TalkingSocketManager.shared.videoMatchDelegate = nil
            MaterialCurrency.shared.videoMatchDelegate = nil
            //: TalkingSocketManager.shared.callerDelegate = nil
            MaterialCurrency.shared.callerDelegate = nil
            //: TalkingSocketManager.shared.calledPartyDelegate = nil
            MaterialCurrency.shared.calledPartyDelegate = nil
            //: TalkingSocketManager.shared.errorDelegate = nil
            MaterialCurrency.shared.errorDelegate = nil
        }
    }

    // MARK: - Lazy Load

    //: private lazy var videoManager: TalkingVideoChatManager = {
    private lazy var videoManager: ImplementSpecify = {
        //: let m = TalkingVideoChatManager()
        let m = ImplementSpecify()
        //: let info = TalkingVideoChatModel()
        let info = IconModel()
        //: info.videoStage = .Calling
        info.videoStage = .Calling
        //: info.renderPostion = .selfOnBg
        info.renderPostion = .selfOnBg
        //: m.infoModel = info
        m.infoModel = info
        // 视图传递给manager
        //: m.bgVideoView = self.renderView
        m.bgVideoView = self.renderView
        //: return m
        return m
        //: }()
    }()

    // 视频画面渲染视图
    //: private lazy var renderView: UIView = {
    private lazy var renderView: UIView = {
        //: let v = UIView()
        let v = UIView()
        //: v.backgroundColor = .appTitleColor()
        v.backgroundColor = .untilExpected()
        //: return v
        return v
        //: }()
    }()

    //: private lazy var gradientBtn: UIButton = {
    private lazy var gradientBtn: UIButton = {
        //: let btn = UIButton()
        let btn = UIButton()
        //: btn.addTarget(self, action: #selector(start_randomVideo), for: .touchUpInside)
        btn.addTarget(self, action: #selector(windowOf), for: .touchUpInside)
        //: btn.layer.masksToBounds = true
        btn.layer.masksToBounds = true
        // 渐变
        //: let layer = CAGradientLayer()
        let layer = CAGradientLayer()
        //: layer.frame = CGRect(x: 0, y: 0, width: ScreenWidth+1, height: ScreenHeight+1)
        layer.frame = CGRect(x: 0, y: 0, width: show_errorChangeFormat + 1, height: appLoadMessage + 1)
        //: layer.startPoint = CGPoint(x: 0.5, y: 0)
        layer.startPoint = CGPoint(x: 0.5, y: 0)
        //: layer.endPoint = CGPoint(x: 0.5, y: 1)
        layer.endPoint = CGPoint(x: 0.5, y: 1)
        //: layer.colors = [UIColor(white: 0, alpha: 0.15).cgColor, UIColor(hex: "#322870")!.cgColor]
        layer.colors = [UIColor(white: 0, alpha: 0.15).cgColor, UIColor(hex: (String(mainDrawName) + String(dataBurnAyeValue)))!.cgColor]
        //: layer.locations = [0, 1]
        layer.locations = [0, 1]
        //: btn.layer.addSublayer(layer)
        btn.layer.addSublayer(layer)
        //: return btn
        return btn
        //: }()
    }()

    //: private lazy var svgaPlayer: SVGAPlayer = {
    private lazy var svgaPlayer: SVGAPlayer = {
        //: let player = SVGAPlayer.init()
        let player = SVGAPlayer()
        //: player.loops = 0
        player.loops = 0
        //: player.clearsAfterStop = false
        player.clearsAfterStop = false
        //: player.isUserInteractionEnabled = false
        player.isUserInteractionEnabled = false
        //: player.contentMode = .scaleAspectFill
        player.contentMode = .scaleAspectFill

        //: return player
        return player
        //: }()
    }()

    //: private lazy var tipsLab: UILabel = {
    private lazy var tipsLab: UILabel = {
        //: let lab = UILabel()
        let lab = UILabel()
        //: lab.textColor = .white
        lab.textColor = .white
        //: lab.font = UIFont.boldSystemFont(ofSize: 20)
        lab.font = UIFont.boldSystemFont(ofSize: 20)
        //: lab.textAlignment = .center
        lab.textAlignment = .center
        //: lab.text = "Tap to start video chatting".localized
        lab.text = (String(userLiteBubbleFormat.prefix(5)) + "o star" + String(userWithinValue.suffix(9)) + data_beneathKey.replacingOccurrences(of: "medal", with: "a") + userDeliveryUrl.replacingOccurrences(of: "operation", with: "g")).localized
        //: return lab
        return lab
        //: }()
    }()

    //: private lazy var closeBtn: UIButton = {
    private lazy var closeBtn: UIButton = {
        //: let btn = UIButton()
        let btn = UIButton()
        //: btn.setImage(UIImage.BundleImageNamed(name: "icon_close_nor"), for: .normal)
        btn.setImage(UIImage.managerToSecond(name: (String(notiDisableInsideId) + String(constCommandFormat.suffix(7)))), for: .normal)
        //: btn.addTarget(self, action: #selector(closeButtonClick), for: .touchUpInside)
        btn.addTarget(self, action: #selector(beyondCustomer), for: .touchUpInside)
        //: return btn
        return btn
        //: }()
    }()

    //: private lazy var chattingNumBtn: TalkingButton = {
    private lazy var chattingNumBtn: DistinctionButton = {
        //: let btn = TalkingButton()
        let btn = DistinctionButton()
        //: btn.setImage(UIImage.BundleImageNamed(name: "icon_views_number"), for: .normal)
        btn.setImage(UIImage.managerToSecond(name: (String(main_speakerPath.suffix(5)) + "view" + String(k_innerFormat.suffix(4)) + "mber")), for: .normal)
        //: btn.setImage(UIImage.BundleImageNamed(name: "icon_views_number"), for: .disabled)
        btn.setImage(UIImage.managerToSecond(name: (String(main_speakerPath.suffix(5)) + "view" + String(k_innerFormat.suffix(4)) + "mber")), for: .disabled)
        //: btn.spaceBetweenTitleAndImage = 5
        btn.spaceBetweenTitleAndImage = 5
        //: btn.isEnabled = false
        btn.isEnabled = false
        //: btn.setTitleColor(UIColor.white, for: .normal)
        btn.setTitleColor(UIColor.white, for: .normal)
        //: btn.titleLabel?.font = UIFont.pingfangFont(type: .Regular, fontSize: 15)
        btn.titleLabel?.font = UIFont.yearNearby(type: .Regular, fontSize: 15)
        //: btn.setTitle("%@ chatting".localizedArguments(chattingNum), for: .normal)
        btn.setTitle((String(show_shareExistingString) + String(k_nativeMsg)).theAppear(chattingNum), for: .normal)
        //: return btn
        return btn
        //: }()
    }()

    //: private lazy var priceStackView: UIStackView = {
    private lazy var priceStackView: UIStackView = {
        //: let v = UIStackView()
        let v = UIStackView()
        //: v.axis = .horizontal
        v.axis = .horizontal
        //: v.alignment = .center
        v.alignment = .center
        //: v.spacing = 4
        v.spacing = 4
        //: v.isUserInteractionEnabled = false
        v.isUserInteractionEnabled = false
        //: return v
        return v
        //: }()
    }()

    //: private lazy var vipIcon: UIImageView = {
    private lazy var vipIcon: UIImageView = {
        //: let img = UIImageView()
        let img = UIImageView()
        //: img.image = UIImage.BundleImageNamed(name: "icon_lounge")
        img.image = UIImage.managerToSecond(name: (String(show_everyoneKey) + String(showIonAgreeValue)))
        //: img.contentMode = .scaleAspectFill
        img.contentMode = .scaleAspectFill
        //: return img
        return img
        //: }()
    }()

    //: private lazy var goldCoinIcon: UIImageView = {
    private lazy var goldCoinIcon: UIImageView = {
        //: let img = UIImageView()
        let img = UIImageView()
        //: img.image = UIImage.BundleImageNamed(name: "icon_rank_coin")
        img.image = UIImage.managerToSecond(name: (appDistinctionStr.replacingOccurrences(of: "estimated", with: "on") + "_ran" + String(mainRawMsg.prefix(6))))
        //: return img
        return img
        //: }()
    }()

    //: private lazy var priceLab: UILabel = {
    private lazy var priceLab: UILabel = {
        //: let lab = UILabel()
        let lab = UILabel()
        //: lab.textColor = .white
        lab.textColor = .white
        //: lab.font = UIFont.pingfangFont(type: .Regular, fontSize: 15)
        lab.font = UIFont.yearNearby(type: .Regular, fontSize: 15)
        //: return lab
        return lab
        //: }()
    }()

    //: private lazy var vipTopImgView: UIImageView = {
    private lazy var vipTopImgView: UIImageView = {
        //: let imgV = UIImageView()
        let imgV = UIImageView()
        //: imgV.isHidden = true
        imgV.isHidden = true
        //: imgV.image  = UIImage.BundleImageNamed(name: "vip_btn_top")
        imgV.image = UIImage.managerToSecond(name: (String(showNeedKey.suffix(5)) + "tn_to" + showSpendStr.lowercased()))
        //: return imgV
        return imgV
        //: }()
    }()

    // 会员折扣价
    //: private lazy var vipPriceBtn: TalkingButton = {
    private lazy var vipPriceBtn: DistinctionButton = {
        //: let btn = TalkingButton()
        let btn = DistinctionButton()
        //: btn.backgroundColor = UIColor.white.withAlphaComponent(0.1)
        btn.backgroundColor = UIColor.white.withAlphaComponent(0.1)
        //: btn.layer.cornerRadius = actualWidth(w: 27)
        btn.layer.cornerRadius = actualWidth(w: 27)
        //: btn.clipsToBounds = true
        btn.clipsToBounds = true
        //: btn.addTarget(self, action: #selector(maleVipPriceButtonClick), for: .touchUpInside)
        btn.addTarget(self, action: #selector(duringFactor), for: .touchUpInside)
        //: btn.isHidden = true
        btn.isHidden = true
        //: btn.setBackgroundImage(UIImage.BundleImageNamed(name: "btn_nearby_settings_nor"), for: .normal)
        btn.setBackgroundImage(UIImage.managerToSecond(name: (String(const_privacyName.prefix(5)) + "earby_" + appPickJudgeName.replacingOccurrences(of: "uniform", with: "i") + "ngs_nor")), for: .normal)
        //: return btn
        return btn
        //: }()
    }()

    //: private lazy var vipPriceImg: UIImageView = {
    private lazy var vipPriceImg: UIImageView = {
        //: let imgV = UIImageView()
        let imgV = UIImageView()
        //: imgV.image  = UIImage.BundleImageNamed(name: "icon_rank_coin")
        imgV.image = UIImage.managerToSecond(name: (appDistinctionStr.replacingOccurrences(of: "estimated", with: "on") + "_ran" + String(mainRawMsg.prefix(6))))
        //: return imgV
        return imgV
        //: }()
    }()

    //: private lazy var vipPriceLab: UILabel = {
    private lazy var vipPriceLab: UILabel = {
        //: let lab = UILabel()
        let lab = UILabel()
        //: lab.textColor = .userVipColor()
        lab.textColor = .boyfriend()
        //: lab.text = "%@ Coins/min".localizedArguments(vipFee)
        lab.text = (kReStr.capitalized + String(mainModeUrl.suffix(6)) + "s/min").theAppear(vipFee)
        //: lab.font = UIFont.pingfangFont(type: .Medium, fontSize: 15)
        lab.font = UIFont.yearNearby(type: .Medium, fontSize: 15)
        //: return lab
        return lab
        //: }()
    }()

    //: private lazy var norPriceLab: UILabel = {
    private lazy var norPriceLab: UILabel = {
        //: let lab = UILabel()
        let lab = UILabel()
        //: return lab
        return lab
        //: }()
    }()

    // 免费视频挽留弹窗
    //: private lazy var freeVideoAlert: TalkingLiveAlertView = {
    private lazy var freeVideoAlert: UndertakePublish = //: return UndertakePublish(frame: .zero, type: .freeVideo)
        .init(frame: .zero, type: .freeVideo)
    //: }()

    //: private lazy var connectingImg: UIImageView = {
    private lazy var connectingImg: UIImageView = {
        //: let imgV = UIImageView()
        let imgV = UIImageView()
        //: imgV.image  = UIImage.BundleImageNamed(name: "img_match_connecting")
        imgV.image = UIImage.managerToSecond(name: (String(appBalanceValue.prefix(6)) + "tch_c" + k_normallyString.replacingOccurrences(of: "sibling", with: "t") + String(main_yourselfMsg)))
        //: let view = UIActivityIndicatorView.init()
        let view = UIActivityIndicatorView()
        //: view.color = UIColor(hex: "#9F80FF")
        view.color = UIColor(hex: (String(notiInvisibleMessage.prefix(7))))
        //: imgV.addSubview(view)
        imgV.addSubview(view)
        //: view.snp.makeConstraints { make in
        view.snp.makeConstraints { make in
            //: make.bottom.equalToSuperview().offset(-12)
            make.bottom.equalToSuperview().offset(-12)
            //: make.centerX.equalToSuperview()
            make.centerX.equalToSuperview()
            //: make.size.equalTo(20)
            make.size.equalTo(20)
        }
        //: view.startAnimating()
        view.startAnimating()
        //: imgV.isHidden = true
        imgV.isHidden = true
        //: return imgV
        return imgV
        //: }()
    }()
}

// MARK: - Request & 数据处理

//: extension TalkingRandomVideoViewController {
extension AssociatedStage {
    //: private func setPlayer(isRandon: Bool) {
    private func nearRecognize(isRandon: Bool) {
        //: let url = SVGAEffectTool.default.getZipEffectPath(type: isRandon ? .Random_video_bg : .Random_video_bg_nor )
        let url = UniqueProcessing.default.scenarioProject(type: isRandon ? .Random_video_bg : .Random_video_bg_nor)
        //: do {
        do {
            //: let data = try Data(contentsOf: url)
            let data = try Data(contentsOf: url)
            //: let parser = SVGAParser.init()
            let parser = SVGAParser()
            //: parser.parse(with: data, cacheKey: "") { [weak self] videoItem in
            parser.parse(with: data, cacheKey: "") { [weak self] videoItem in
                //: guard let self = self else {return}
                guard let self = self else { return }
                //: svgaPlayer.videoItem = videoItem
                svgaPlayer.videoItem = videoItem
                //: svgaPlayer.startAnimation()
                svgaPlayer.startAnimation()
            }
            //: } catch {
        } catch {
            //: printLog(message: "get json error")
            printLog(message: (mainDisappearPath.replacingOccurrences(of: "ready", with: "g") + String(notiManageStr.prefix(5)) + "on e" + show_implementationId.replacingOccurrences(of: "intro", with: "r")))
        }
    }

    /// 开始随机视频（男）
    /// - Parameters:
    ///   - cmd: 事件名
    ///   - errno: cmd的错误码
    //: private func req_randomVideoToMatch(cmd: String, errno: String) {
    private func joinMention(cmd: String, errno: String) {
        //: beginTime = Date().timeIntervalSince1970
        beginTime = Date().timeIntervalSince1970
        //: let params = ["matchId": self.matchId,
        let params = [(String(showOutsidePath.suffix(7))): self.matchId,
                      //: "cmd": cmd,
                      String(bytes: kReportStr.reversed(), encoding: .utf8)!: cmd,
                      //: "errno": errno] as [String: Any]
                      String(bytes: appTunKey.map{$0^11}, encoding: .utf8)!: errno] as [String: Any]
        //: TalkingQuickVideoManager.req_randomVideoToMatch(params: params) { succeed, result, errorModel in
        ChainBasic.primaryBy(params: params) { succeed, result, errorModel in
            //: guard succeed == true else {
            guard succeed == true else {
                //: self.stop_randomVideo()
                self.descriptionMutual()
                //: return
                return
            }
            //: let json = JSON(result ?? [String: Any]())
            let json = JSON(result ?? [String: Any]())
            //: self.matchId = json["matchId"].intValue
            self.matchId = json[(String(showOutsidePath.suffix(7)))].intValue
        }
    }

    /// 更新VIP价格
    //: @objc private func req_updateVIPFee() {
    @objc private func between() {
        //: TalkingQuickVideoManager.req_videoMatchCheck() { succeed, result, errorModel in
        ChainBasic.mediaIndependent { succeed, result, errorModel in
            //: guard succeed else { return }
            guard succeed else { return }
            //: let json = JSON(result ?? [String: Any]())
            let json = JSON(result ?? [String: Any]())
            //: self.vipFee = json["VIPFee"].intValue
            self.vipFee = json[(String(main_lastTradeFormat.prefix(6)))].intValue
            //: self.refreshVideoCallPrice()
            self.cellForReady()
        }
    }
}

// MARK: - Event

//: extension TalkingRandomVideoViewController {
extension AssociatedStage {
    /// 男性侧vip价格按钮点击事件
    //: @objc private func maleVipPriceButtonClick() {
    @objc private func duringFactor() {
        //: OriginMediumWill.share.func__pushToSubscribeAlert()
        OriginMediumWill.share.faceAbout()
    }

    /// 关闭按钮点击事件
    //: @objc func closeButtonClick() {
    @objc func beyondCustomer() {
        //: dispatchWorkItem?.cancel()
        dispatchWorkItem?.cancel()
        //: showCloseBtn()
        gemPlate()
        //: uploadRecord.uploadRecordEvent(eventID: ClickRandomvideoCancelNoP)
        noti_loadResolveMsg.priorityFun(eventID: userCanvasKey)

        //: if MarginExamineer.share.appConfigMode.displayMatchV4PopUp, MarginExamineer.share.loginUserMode.freeCallTimes > 0,
        if MarginExamineer.share.appConfigMode.displayMatchV4PopUp, MarginExamineer.share.loginUserMode.freeCallTimes > 0,
           //: isShowFreeVideo == false {
           isShowFreeVideo == false
        {
            //: self.freeVideoAlert.show()
            self.freeVideoAlert.contrastEvaluate()
            //: isShowFreeVideo = true
            isShowFreeVideo = true
            //: return
            return
        }
        //: currentViewController()?.navigationController?.popViewController(animated: true)
        myAcross()?.navigationController?.popViewController(animated: true)
    }

    /// 开始随机视频
    //: @objc private func start_randomVideo() {
    @objc private func windowOf() {
        //: guard TalkingSocketManager.shared.pacific == false else {
        guard MaterialCurrency.shared.pacific == false else {
            //: self.func__showStatusBarErrorMsg(showMsg: kMessage_videoTalking_limit)
            self.exitMarker(showMsg: constFormalValue)
            //: return
            return
        }
        //: guard gradientBtn.isEnabled == true else { return }
        guard gradientBtn.isEnabled == true else { return }
        //: afterShowClose()
        poolMember()
        //: self.matchId = 0
        self.matchId = 0 // 重置匹配池
        //: gradientBtn.isEnabled = false
        gradientBtn.isEnabled = false
        //: tipsLab.text = "Matching now".localized
        tipsLab.text = (String(constCanvasMainFormat.prefix(5)) + String(appBadPressTitle)).localized
        //: setPlayer(isRandon: true)
        nearRecognize(isRandon: true)
        //: priceStackView.isHidden = true
        priceStackView.isHidden = true
        //: vipPriceBtn.isHidden = true
        vipPriceBtn.isHidden = true
        //: vipTopImgView.isHidden = true
        vipTopImgView.isHidden = true
        //: if MarginExamineer.share.loginUserMode.randomVideoType == 2 && MarginExamineer.share.loginUserMode.robotVideoNum > 0 {
        if MarginExamineer.share.loginUserMode.randomVideoType == 2, MarginExamineer.share.loginUserMode.robotVideoNum > 0 {
            //: uploadRecord.uploadRecordEvent(eventID: ClickRobotRandomButton)
            noti_loadResolveMsg.priorityFun(eventID: data_thanLocalCalculateId)
            //: req_getVideoInfo()
            againstQuick()
            //: } else {
        } else {
            //: uploadRecord.uploadRecordEvent(eventID: ClickRandomvideoCallNoP)
            noti_loadResolveMsg.priorityFun(eventID: notiEasyLimitTitle)

            //: TalkingSocketManager.shared.videoMatchDelegate = self
            MaterialCurrency.shared.videoMatchDelegate = self
            //: TalkingSocketManager.shared.callerDelegate = self
            MaterialCurrency.shared.callerDelegate = self
            //: TalkingSocketManager.shared.calledPartyDelegate = self
            MaterialCurrency.shared.calledPartyDelegate = self
            //: TalkingSocketManager.shared.errorDelegate = self
            MaterialCurrency.shared.errorDelegate = self
            //: restart_randomVideo()
            skirt()
        }
    }

    /// 重新开始视频速配
    /// - Parameters:
    ///   - cmd: 事件名（默认用户主动点击）
    ///   - errno: cmd的错误码
    //: private func restart_randomVideo(cmd: String = "userTouch", errno: String = "0") {
    private func skirt(cmd: String = (String(data_actualVidId) + String(notiContainId)), errno: String = "0") {
        // 停止定时器
        //: destroyWaitingTimer()
        priority()
        //: req_randomVideoToMatch(cmd: cmd, errno: errno)
        joinMention(cmd: cmd, errno: errno)
    }

    /// 停止随机视频
    //: private func stop_randomVideo() {
    private func descriptionMutual() {
        // 停止定时器
        //: destroyWaitingTimer()
        priority()
        //: showCloseBtn()
        gemPlate()

        //: gradientBtn.isEnabled = true
        gradientBtn.isEnabled = true
        //: tipsLab.text = "Tap to start video chatting".localized
        tipsLab.text = (String(userLiteBubbleFormat.prefix(5)) + "o star" + String(userWithinValue.suffix(9)) + data_beneathKey.replacingOccurrences(of: "medal", with: "a") + userDeliveryUrl.replacingOccurrences(of: "operation", with: "g")).localized
        //: setPlayer(isRandon: false)
        nearRecognize(isRandon: false)
        //: self.isFirstRandow = false
        self.isFirstRandow = false
        //: if TalkingSocketManager.shared.pacific == false {
        if MaterialCurrency.shared.pacific == false {
            //: TalkingSocketManager.shared.videoMatchDelegate = nil
            MaterialCurrency.shared.videoMatchDelegate = nil
            //: TalkingSocketManager.shared.callerDelegate = nil
            MaterialCurrency.shared.callerDelegate = nil
            //: TalkingSocketManager.shared.calledPartyDelegate = nil
            MaterialCurrency.shared.calledPartyDelegate = nil
            //: TalkingSocketManager.shared.errorDelegate = nil
            MaterialCurrency.shared.errorDelegate = nil
        }
        //: refreshVideoCallPrice()
        cellForReady()
    }

    /// 销毁定时器
    //: private func destroyWaitingTimer() {
    private func priority() {
        //: if self.waitingTimer != nil {
        if self.waitingTimer != nil {
            //: self.waitingTimer?.invalidate()
            self.waitingTimer?.invalidate()
            //: self.waitingTimer = nil
            self.waitingTimer = nil
        }

        //: if self.retryAfterTimer != nil {
        if self.retryAfterTimer != nil {
            //: self.retryAfterTimer?.invalidate()
            self.retryAfterTimer?.invalidate()
            //: self.retryAfterTimer = nil
            self.retryAfterTimer = nil
        }
    }

    /// 获取用户主动拨打视频信息
    //: @objc private func req_getVideoInfo() {
    @objc private func againstQuick() {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = ObtainMomentum()
        //: reqModel.requestPath = "mf/freeCall/getVideoInfo"
        reqModel.requestPath = (String(app_withPostString) + String(notiBeautyValue))
        //: reqModel.requestType = .GET
        reqModel.requestType = .GET
        //: reqModel.params = ["fromType": "1"]
        reqModel.params = [String(bytes: mainCriticalValue.map{opTotalerval(status: $0)}, encoding: .utf8)!: "1"]
        //: reqModel.showErrorStatusBar = false
        reqModel.showErrorStatusBar = false
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        notiSignatureItemMessage.lightForPath(model: reqModel) { succeed, result, errorModel in

            //: guard succeed == true else {
            guard succeed == true else {
                //: self.stop_randomVideo()
                self.descriptionMutual()
                //: if errorModel?.errorCode ?? 1 > 0 {
                if errorModel?.errorCode ?? 1 > 0 {
                    //: MarginExamineer.share.loginUserMode.randomVideoType = 1
                    MarginExamineer.share.loginUserMode.randomVideoType = 1
                    //: self.start_randomVideo()
                    self.windowOf()
                    //: return
                    return
                }
                //: self.func__showStatusBarErrorMsg(showMsg: errorModel?.errorMsg ?? "")
                self.exitMarker(showMsg: errorModel?.errorMsg ?? "")
                //: return
                return
            }
            //: guard let result = result as? Dictionary<String, Any> else {
            guard let result = result as? [String: Any] else {
                //: return
                return
            }
            //: self.connectingImg.isHidden = false
            self.connectingImg.isHidden = false
            //: if let model = TalkingVideoInitivCallModel.deserialize(from: result as? Dictionary) {
            if let model = SessionDate.deserialize(from: result as? Dictionary) {
                /// 预加载
                //: let  player = TalkingVideoPlayerManager.init()
                let player = EqualPublisher()
                //: player.setMute(bEnable: true)
                player.area(bEnable: true)
                //: player.playerWithUrlAndVideoView(url: model.callVideoUrl, view: UIView.init())
                player.roundHost(url: model.callVideoUrl, view: UIView())

                //: TalkingVideoInitivCallManager.shared.videoCallModel = model
                RowThrough.shared.videoCallModel = model

                //: TalkingPermissionTool.checkCameraAndMicrophone { [weak self] isOpen in
                MultiplePoint.someone { [weak self] isOpen in
                    //: guard let self = self else { return }
                    guard let self = self else { return }
                    //: guard isOpen == true else { return }
                    guard isOpen == true else { return }

                    //: self.putThrough()
                    self.photoEntity()
                }
            }
        }
    }

    /// 接通电话
    //: private func putThrough() {
    private func photoEntity() {
        //: uploadRecord.uploadRecordEvent(eventID: clickVideoAcceptButton)
        noti_loadResolveMsg.priorityFun(eventID: showMethodFormat)
        /// 视频通话被禁用
        //: guard TalkingVideoInitivCallManager.shared.videoCallModel.isBan == 0 else {
        guard RowThrough.shared.videoCallModel.isBan == 0 else {
            //: self.func__showStatusBarErrorMsg(showMsg: "You've been barred from receiving calls".localized)
            self.exitMarker(showMsg: String(bytes: k_skinString.map{whipInvite(deadline: $0)}, encoding: .utf8)!.localized)
            //: self.connectingImg.isHidden = true
            self.connectingImg.isHidden = true
            //: return
            return
        }
        //: self.dispatchWorkItem = DispatchWorkItem {
        self.dispatchWorkItem = DispatchWorkItem {
            //: self.connectingImg.isHidden = true
            self.connectingImg.isHidden = true
            //: TalkingVideoInitivCallManager.shared.req_userOperation(type: 1) { succeed, result, errorModel in
            RowThrough.shared.familyWith(type: 1) { succeed, result, errorModel in
                //: guard succeed == true else {
                guard succeed == true else {
                    //: return
                    return
                }
                //: guard succeed == true else {
                guard succeed == true else {
                    //: self.stop_randomVideo()
                    self.descriptionMutual()
                    //: return
                    return
                }
                //: let vc = TalkingVideoInitivCallVC()
                let vc = ObserveVersusRespective()
                //: vc.isRandomVCPush = true
                vc.isRandomVCPush = true
                //: self.currentViewController()?.navigationController?.pushViewController(vc, animated: true)
                self.myAcross()?.navigationController?.pushViewController(vc, animated: true)
                //: self.stop_randomVideo()
                self.descriptionMutual()
            }
        }
        // 添加延时任务
        //: DispatchQueue.main.asyncAfter(deadline: .now() + 3.0, execute: self.dispatchWorkItem!)
        DispatchQueue.main.asyncAfter(deadline: .now() + 3.0, execute: self.dispatchWorkItem!)
    }

    /// 匹配时x秒显示关闭按钮
    //: private func afterShowClose() {
    private func poolMember() {
        //: if MarginExamineer.share.appUserConfigMode.matchCancelBtnDelayTime > 0 {
        if MarginExamineer.share.appUserConfigMode.matchCancelBtnDelayTime > 0 {
            //: self.closeBtn.isHidden = true
            self.closeBtn.isHidden = true
            //: closeBtnWorkItem = DispatchWorkItem { [weak self] in
            closeBtnWorkItem = DispatchWorkItem { [weak self] in
                //: guard let self = self else { return }
                guard let self = self else { return }
                //: self.closeBtn.isHidden = false
                self.closeBtn.isHidden = false
            }
            //: DispatchQueue.main.asyncAfter(deadline: .now() + Double(MarginExamineer.share.appUserConfigMode.matchCancelBtnDelayTime), execute: closeBtnWorkItem!)
            DispatchQueue.main.asyncAfter(deadline: .now() + Double(MarginExamineer.share.appUserConfigMode.matchCancelBtnDelayTime), execute: closeBtnWorkItem!)
        }
    }

    //: private func showCloseBtn() {
    private func gemPlate() {
        //: closeBtnWorkItem?.cancel()
        closeBtnWorkItem?.cancel()
        //: self.closeBtn.isHidden = false
        self.closeBtn.isHidden = false
    }

    //: private func resetCamera() {
    private func mapChannel() {
        //: guard TalkingSocketManager.shared.pacific == false else {
        guard MaterialCurrency.shared.pacific == false else {
            //: return
            return
        }
        //: DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 2.0) {
        DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 2.0) {
            //: self.videoManager.infoModel?.videoStage = .Waiting
            self.videoManager.infoModel?.videoStage = .Waiting
            //: self.videoManager.config.openCamera = true
            self.videoManager.config.openCamera = true
            //: self.videoManager.isOpenTRTCPreview = false
            self.videoManager.isOpenTRTCPreview = false
            //: self.videoManager.startPreview()
            self.videoManager.fitByCoordinator()
        }
    }

    /// 匹配通话结束时
    //: @objc private func closeTool() {
    @objc private func downEase() {
        //: DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 2) {
        DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 2) {
            //: self.start_randomVideo()
            self.windowOf()
        }
    }
}

// MARK: - MessageIconEmbrace

//: extension TalkingRandomVideoViewController: SocketManagerVideoMatchDelegate {
extension AssociatedStage: MessageIconEmbrace {
    /// 视频速配成功
    //: func socket_match_onVideoMatch(data: [String: Any]) {
    func link(data: [String: Any]) {
        // 拨打视频通话
        //: guard let model = TalkingMatchResultModel.deserialize(from: data) else { return }
        guard let model = DifficultRangeSensorSubtleOption.deserialize(from: data) else { return }
        //: self.matchId = model.matchId
        self.matchId = model.matchId
        //: var callData: [String: Any] = ["matchId": model.matchId,
        var callData: [String: Any] = [(String(showOutsidePath.suffix(7))): model.matchId,
                                       //: "source": model.source,
                                       String(bytes: const_serviceAMessage.reversed(), encoding: .utf8)!: model.source,
                                       //: "type": model.type,
                                       String(bytes: userMountUrl.reversed(), encoding: .utf8)!: model.type,
                                       //: "uid": model.uid]
                                       String(bytes: notiPullName.reversed(), encoding: .utf8)!: model.uid]
        //: let index = EnableFreeCallType.randow
        let index = TrainWait.randow
        //: if MarginExamineer.share.loginUserMode.freeCallTimes > 0 && MarginExamineer.share.appUserConfigMode.enableFreeCallType & index.rawValue == index.rawValue {
        if MarginExamineer.share.loginUserMode.freeCallTimes > 0 && MarginExamineer.share.appUserConfigMode.enableFreeCallType & index.rawValue == index.rawValue {
            //: callData.updateValue(1, forKey: "fromFreeCall")
            callData.updateValue(1, forKey: (String(showProgressReliabilityId)))
            //: TalkingSocketManager.shared.isFreeCall = true
            MaterialCurrency.shared.isFreeCall = true
        }
        //: let info: [String: Any] = ["cmd": "requestCall", "data": callData]
        let info: [String: Any] = [String(bytes: kReportStr.reversed(), encoding: .utf8)!: (String(appConnectSquareName.prefix(8)) + showPartnerFormat.replacingOccurrences(of: "original", with: "ll")), String(bytes: notiRemMessage.map{transformInstead(production: $0)}, encoding: .utf8)!: callData]
        //: TalkingSocketManager.shared.sendMessage(info: info)
        MaterialCurrency.shared.warningVersion(info: info)
        // 15s未接听主动挂断
        //: waitingTimer = Timer.scheduledTimer(withTimeInterval: TimeInterval(15), block: { [weak self] timer in
        waitingTimer = Timer.scheduledTimer(withTimeInterval: TimeInterval(15), block: { [weak self] timer in
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: TalkingSocketManager.shared.sendMessage(info: ["cmd": "cancelCall", "data": ["logId": self.logId]])
            MaterialCurrency.shared.warningVersion(info: [String(bytes: kReportStr.reversed(), encoding: .utf8)!: (showLargeYesVenueMsg.replacingOccurrences(of: "aching", with: "n") + String(main_familyTitle)), String(bytes: notiRemMessage.map{transformInstead(production: $0)}, encoding: .utf8)!: [(String(const_listenerKey.prefix(5))): self.logId]])
            //: self.restart_randomVideo(cmd: "onVideoMatch")
            self.skirt(cmd: (String(k_limitedName) + k_readyString.replacingOccurrences(of: "leading", with: "h")))
            //: }, repeats: false)
        }, repeats: false)
        //: connectingImg.isHidden = false
        connectingImg.isHidden = false
    }

    //: func socket_match_onRequestCall(data: [String: Any]) {
    func distanceTo(data: [String: Any]) {
        //: let json = JSON(data)
        let json = JSON(data)
        //: self.logId = json["logId"].intValue
        self.logId = json[(String(const_listenerKey.prefix(5)))].intValue
    }
}

// MARK: - ThemeMenu【视频拨打方处理】

//: extension TalkingRandomVideoViewController: SocketManagerVideoChatCallerDelegate {
extension AssociatedStage: ThemeMenu {
    /// 拨打方拒绝了通话，接收方收到该指令（未通话）
    //: func socket_videoChat_cancelCall(data: [String: Any]) {}
    func verticalAgile(data: [String: Any]) {}

    /// 对方结束了通话（通话中）
    //: func socket_videoChat_stopCall(data: [String: Any]) {}
    func beforeReliability(data: [String: Any]) {}

    /// 余额不足提醒
    //: func socket_videoChat_onMissCoin(msg: String, data: [String: Any]) {}
    func distinctionIn(msg: String, data: [String: Any]) {}

    /// 对方接受了来电，建立视频通话
    //: func socket_videoChat_acceptCall(data: [String: Any]) {
    func exLayer(data: [String: Any]) {
        // 应用在后台，先提醒用户
        //: if UIApplication.shared.applicationState == .background {
        if UIApplication.shared.applicationState == .background {
            //: AppDelegateHelper.pushLocalNotification(body: "[Automatic video] Receive video call incoming".localized)
            CoordinatorPlatform.countQuantityo(body: String(bytes: notiHomeName.map{clickChange(used: $0)}, encoding: .utf8)!.localized)
        }
        //: let model = TalkingMatchResultModel.deserialize(from: data)
        let model = DifficultRangeSensorSubtleOption.deserialize(from: data)
        //: if let uid = model?.uid {
        if let uid = model?.uid {
            //: uploadSuccessTime(toUid: String(uid))
            pastA(toUid: String(uid))
        }
        //: connectingImg.isHidden = true
        connectingImg.isHidden = true
        //: stop_randomVideo()
        descriptionMutual()
        // 开启视频通话时先销毁当前预览画面
        //: videoManager.shouldReleaseAllResource()
        videoManager.concreteProduct()
        //: var newData = data
        var newData = data
        //: newData["isRandomVCPush"] = true
        newData[(String(notiSubstantialTitle))] = true
        //: MarginExamineer.share.start1v1TalkCall(info: newData, situation: .Calling)
        MarginExamineer.share.evolutionScan(info: newData, situation: .Calling)
    }
}

// MARK: - MultipleMarkAmong【接收到回执代理】

//: extension TalkingRandomVideoViewController: SocketManagerVideoChatCalledPartyDelegate {
extension AssociatedStage: MultipleMarkAmong {
    /// 接收方拒绝了通话，拨打方收到该指令（未通话）
    //: func socket_videoChat_refuseCall(data: [String: Any]) {
    func buttTar(data: [String: Any]) {
        //: restart_randomVideo(cmd: "refuseCall")
        skirt(cmd: (showOperationMessage.replacingOccurrences(of: "word", with: "fu") + "eCall"))
        //: connectingImg.isHidden = true
        connectingImg.isHidden = true
    }

    /// 接收方接受了通话，改变状态为通话中，acceptCall回执
    //: func socket_videoChat_onAcceptCall(data: [String: Any]) {}
    func fragmentWithinCurrency(data: [String: Any]) {}
}

// MARK: - GlobalSustainable

//: extension TalkingRandomVideoViewController: TalkingSocketManagerErrorDelegate {
extension AssociatedStage: GlobalSustainable {
    //: func socket_didRecieveError(errorNo: Int, cmd: String, msg: String, data: [String: Any]) {
    func connectTo(errorNo: Int, cmd: String, msg: String, data: [String: Any]) {
        //: connectingImg.isHidden = true
        connectingImg.isHidden = true
        //: if cmd == "onCancelCall" {  // 取消通话异常
        if cmd == (String(main_sourceTitle) + String(data_administrativeTitle)) { // 取消通话异常
            //: stop_randomVideo()
            descriptionMutual()
            //: return
            return
        }
        //: if cmd == "onVideoMatch", errorNo == 404 { // 匹配池用完
        if cmd == (String(k_limitedName) + k_readyString.replacingOccurrences(of: "leading", with: "h")), errorNo == 404 { // 匹配池用完
            //: stop_randomVideo()
            descriptionMutual()
            //: let json = JSON(data)
            let json = JSON(data)
            //: self.matchId = json["matchId"].intValue
            self.matchId = json[(String(showOutsidePath.suffix(7)))].intValue
            //: self.func__showStatusBarErrorMsg(showMsg: msg)
            self.exitMarker(showMsg: msg)
            //: uploadFailTime(type: 2)
            quoteBelow(type: 2)
            //: return
            return
        }
        //: if cmd == "onVideoMatch", errorNo == 449 { // 匹配池用完，延时重试
        if cmd == (String(k_limitedName) + k_readyString.replacingOccurrences(of: "leading", with: "h")), errorNo == 449 { // 匹配池用完，延时重试
            //: let json = JSON(data)
            let json = JSON(data)
            //: retryAfterTimer = Timer.scheduledTimer(withTimeInterval: TimeInterval(json["retryAfter"].intValue), block: { [weak self] timer in
            retryAfterTimer = Timer.scheduledTimer(withTimeInterval: TimeInterval(json[(String(kShareAriaId) + String(show_customerUrl))].intValue), block: { [weak self] timer in
                //: guard let self = self else { return }
                guard let self = self else { return }
                //: self.matchId = json["matchId"].intValue
                self.matchId = json[(String(showOutsidePath.suffix(7)))].intValue
                //: self.restart_randomVideo(cmd: cmd, errno: String(errorNo))
                self.skirt(cmd: cmd, errno: String(errorNo))
                //: }, repeats: false)
            }, repeats: false)
            //: return
            return
        }
        //: if cmd == "onRequestCall", errorNo == VideoChatErrorCode.MoneyLack.rawValue { // 余额不足
        if cmd == (String(appMinimizePath.suffix(7)) + String(dataRomanItName)), errorNo == ComputeCurrency.MoneyLack.rawValue { // 余额不足
            //: stop_randomVideo()
            descriptionMutual()
            //: guard MarginExamineer.share.appStatus == AppSkinStatus.normal.rawValue else { return }
            guard MarginExamineer.share.appStatus == AwayPush.normal.rawValue else { return }
            //: OriginMediumWill.share.func__jumpToWebRecharge(clickEvent: clickRandomvideoButton, sufficient: false)
            OriginMediumWill.share.resumeUniversal(clickEvent: noti_flowId, sufficient: false)
            //: uploadFailTime(type: 1)
            quoteBelow(type: 1)
            //: return
            return
        }

        //: restart_randomVideo(cmd: cmd, errno: String(errorNo))
        skirt(cmd: cmd, errno: String(errorNo))
    }
}

// MARK: - 埋点

//: extension TalkingRandomVideoViewController {
extension AssociatedStage {
    //: func uploadFailTime(type: Int) {
    func quoteBelow(type: Int) {
        //: let nowtime = Date().timeIntervalSince1970
        let nowtime = Date().timeIntervalSince1970
        //: let time = Int(nowtime - (beginTime ?? Date().timeIntervalSince1970))
        let time = Int(nowtime - (beginTime ?? Date().timeIntervalSince1970))
        //: var parm = Dictionary<String, Any>()
        var parm = [String: Any]()
        //: parm["failType"] = type
        parm[(notiResWireStr.replacingOccurrences(of: "searcher", with: "ai") + "Type")] = type
        //: parm["callWaitingTime"] = time
        parm[(String(appCorrectKey.suffix(5)) + "aitin" + String(dataProviderTitle.prefix(5)))] = time
        //: uploadRecord.uploadRecordEvent(eventID: RandomvideoFailwaitTime, parameterStr: parm)
        noti_loadResolveMsg.groupSplit(eventID: kFixedId, parameterStr: parm)
    }

    //: func uploadSuccessTime(toUid: String) {
    func pastA(toUid: String) {
        //: let nowtime = Date().timeIntervalSince1970
        let nowtime = Date().timeIntervalSince1970
        //: let time = Int(nowtime - (beginTime ?? Date().timeIntervalSince1970))
        let time = Int(nowtime - (beginTime ?? Date().timeIntervalSince1970))
        //: var parm = Dictionary<String, Any>()
        var parm = [String: Any]()
        //: parm["callWaitingTime"] = time
        parm[(String(appCorrectKey.suffix(5)) + "aitin" + String(dataProviderTitle.prefix(5)))] = time
        //: parm["toUid"] = toUid
        parm[(String(k_sectionThumbFormat.suffix(5)))] = toUid
        //: uploadRecord.uploadRecordEvent(eventID: RandomvideowaitTime, parameterStr: parm)
        noti_loadResolveMsg.groupSplit(eventID: user_lastMessage, parameterStr: parm)
    }
}

// MARK: - Layout

//: extension TalkingRandomVideoViewController {
extension AssociatedStage {
    /// 刷新视频通话价格
    //: @objc private func refreshVideoCallPrice() {
    @objc private func cellForReady() {
        //: guard MarginExamineer.share.appStatus == AppSkinStatus.normal.rawValue else {
        guard MarginExamineer.share.appStatus == AwayPush.normal.rawValue else {
            //: priceStackView.isHidden = true
            priceStackView.isHidden = true
            //: vipPriceBtn.isHidden = true
            vipPriceBtn.isHidden = true
            //: vipTopImgView.isHidden = true
            vipTopImgView.isHidden = true
            //: return
            return
        }

        //: vipIcon.isHidden = !MarginExamineer.share.loginUserMode.loungePlus
        vipIcon.isHidden = !MarginExamineer.share.loginUserMode.loungePlus
        //: priceStackView.isHidden = false
        priceStackView.isHidden = false
        //: let indexType = EnableFreeCallType.randow
        let indexType = TrainWait.randow
        //: if MarginExamineer.share.loginUserMode.freeCallTimes > 0 && MarginExamineer.share.appUserConfigMode.enableFreeCallType & indexType.rawValue == indexType.rawValue {
        if MarginExamineer.share.loginUserMode.freeCallTimes > 0, MarginExamineer.share.appUserConfigMode.enableFreeCallType & indexType.rawValue == indexType.rawValue { // 有免费次数
            //: if MarginExamineer.share.appUserConfigMode.callBtnStyle == 1 {
            if MarginExamineer.share.appUserConfigMode.callBtnStyle == 1 {
                //: priceLab.text = "Free 1 min".localized
                priceLab.text = (String(appNeedLessMessage)).localized
                //: } else {
            } else {
                //: priceStackView.isHidden = true
                priceStackView.isHidden = true
            }
            //: vipPriceBtn.isHidden = true
            vipPriceBtn.isHidden = true
            //: vipTopImgView.isHidden = true
            vipTopImgView.isHidden = true

            //: } else {
        } else { // 无免费次数, 展示价格
            //: let price = MarginExamineer.share.loginUserMode.loungePlus ? vipFee:fee
            let price = MarginExamineer.share.loginUserMode.loungePlus ? vipFee : fee
            //: priceLab.text = "%@/min".localizedArguments(price)
            priceLab.text = (String(appLogId)).theAppear(price)

            //: let attr = NSMutableAttributedString(string: "%@/min".localizedArguments(price), attributes: [.strikethroughStyle : NSUnderlineStyle.single.rawValue, .foregroundColor: UIColor.white.withAlphaComponent(0.5), .font: UIFont.pingfangMediumFont(fontSize: 10)])
            let attr = NSMutableAttributedString(string: (String(appLogId)).theAppear(price), attributes: [.strikethroughStyle: NSUnderlineStyle.single.rawValue, .foregroundColor: UIColor.white.withAlphaComponent(0.5), .font: UIFont.hangProgram(fontSize: 10)])
            //: norPriceLab.attributedText = attr
            norPriceLab.attributedText = attr
            //: vipPriceBtn.isHidden = MarginExamineer.share.loginUserMode.loungePlus
            vipPriceBtn.isHidden = MarginExamineer.share.loginUserMode.loungePlus
            //: vipTopImgView.isHidden = MarginExamineer.share.loginUserMode.loungePlus
            vipTopImgView.isHidden = MarginExamineer.share.loginUserMode.loungePlus

            //: priceStackView.isHidden = !MarginExamineer.share.loginUserMode.loungePlus
            priceStackView.isHidden = !MarginExamineer.share.loginUserMode.loungePlus
        }
    }

    /// 添加视图
    //: private func setupSubviews() {
    private func outTime() {
        //: view.addSubview(renderView)
        view.addSubview(renderView)
        //: view.addSubview(gradientBtn)
        view.addSubview(gradientBtn)
        //: view.addSubview(svgaPlayer)
        view.addSubview(svgaPlayer)
        //: view.addSubview(tipsLab)
        view.addSubview(tipsLab)
        //: view.addSubview(chattingNumBtn)
        view.addSubview(chattingNumBtn)
        //: view.addSubview(priceStackView)
        view.addSubview(priceStackView)
        //: priceStackView.addArrangedSubview(vipIcon)
        priceStackView.addArrangedSubview(vipIcon)
        //: priceStackView.addArrangedSubview(goldCoinIcon)
        priceStackView.addArrangedSubview(goldCoinIcon)
        //: priceStackView.addArrangedSubview(priceLab)
        priceStackView.addArrangedSubview(priceLab)
        //: view.addSubview(vipPriceBtn)
        view.addSubview(vipPriceBtn)
        //: view.addSubview(vipTopImgView)
        view.addSubview(vipTopImgView)
        //: vipPriceBtn.addSubview(vipPriceImg)
        vipPriceBtn.addSubview(vipPriceImg)
        //: vipPriceBtn.addSubview(vipPriceLab)
        vipPriceBtn.addSubview(vipPriceLab)
        //: vipPriceBtn.addSubview(norPriceLab)
        vipPriceBtn.addSubview(norPriceLab)

        //: view.addSubview(closeBtn)
        view.addSubview(closeBtn)
        //: view.addSubview(connectingImg)
        view.addSubview(connectingImg)
    }

    /// 添加约束
    //: private func setupSubViewsConstraint() {
    private func addMusic() {
        //: renderView.snp.makeConstraints { make in
        renderView.snp.makeConstraints { make in
            //: make.edges.equalToSuperview()
            make.edges.equalToSuperview()
        }
        //: gradientBtn.snp.makeConstraints { make in
        gradientBtn.snp.makeConstraints { make in
            //: make.edges.equalToSuperview()
            make.edges.equalToSuperview()
        }
        //: svgaPlayer.snp.makeConstraints { make in
        svgaPlayer.snp.makeConstraints { make in
            //: make.leading.equalTo(-8)
            make.leading.equalTo(-8)
            //: make.trailing.equalTo(8)
            make.trailing.equalTo(8)
            //: make.centerY.equalToSuperview()
            make.centerY.equalToSuperview()
            //: make.height.equalTo(actualWidth(w: 395))
            make.height.equalTo(actualWidth(w: 395))
        }
        //: tipsLab.snp.makeConstraints { make in
        tipsLab.snp.makeConstraints { make in
            //: make.bottom.equalTo(svgaPlayer.snp.bottom).offset(-40)
            make.bottom.equalTo(svgaPlayer.snp.bottom).offset(-40)
            //: make.width.centerX.equalToSuperview()
            make.width.centerX.equalToSuperview()
        }
        //: chattingNumBtn.snp.makeConstraints { make in
        chattingNumBtn.snp.makeConstraints { make in
            //: make.top.equalTo(tipsLab.snp.bottom).offset(35)
            make.top.equalTo(tipsLab.snp.bottom).offset(35)
            //: make.centerX.equalToSuperview()
            make.centerX.equalToSuperview()
        }

        //: priceStackView.snp.makeConstraints { make in
        priceStackView.snp.makeConstraints { make in
            //: make.top.equalTo(chattingNumBtn.snp.bottom).offset(8)
            make.top.equalTo(chattingNumBtn.snp.bottom).offset(8)
            //: make.centerX.equalToSuperview()
            make.centerX.equalToSuperview()
            //: make.height.equalTo(31)
            make.height.equalTo(31)
            //: make.trailing.lessThanOrEqualToSuperview()
            make.trailing.lessThanOrEqualToSuperview()
        }
        //: vipIcon.snp.makeConstraints { make in
        vipIcon.snp.makeConstraints { make in
            //: make.width.height.equalTo(15)
            make.width.height.equalTo(15)
        }
        //: goldCoinIcon.snp.makeConstraints { make in
        goldCoinIcon.snp.makeConstraints { make in
            //: make.width.height.equalTo(actualWidth(w: 15))
            make.width.height.equalTo(actualWidth(w: 15))
        }
        //: priceLab.snp.makeConstraints { make in
        priceLab.snp.makeConstraints { make in
            //: make.height.equalToSuperview()
            make.height.equalToSuperview()
        }

        //: vipPriceBtn.snp.makeConstraints { make in
        vipPriceBtn.snp.makeConstraints { make in
            //: make.top.equalTo(priceStackView.snp.bottom).offset(16)
            make.top.equalTo(priceStackView.snp.bottom).offset(16)
            //: make.centerX.equalToSuperview()
            make.centerX.equalToSuperview()
            //: make.size.equalTo(CGSize(width: actualWidth(w: 240), height: actualWidth(w: 54)))
            make.size.equalTo(CGSize(width: actualWidth(w: 240), height: actualWidth(w: 54)))
        }
        //: vipTopImgView.snp.makeConstraints { make in
        vipTopImgView.snp.makeConstraints { make in
            //: make.top.equalTo(vipPriceBtn.snp.top).offset(-12)
            make.top.equalTo(vipPriceBtn.snp.top).offset(-12)
            //: make.leading.equalTo(vipPriceBtn.snp.leading)
            make.leading.equalTo(vipPriceBtn.snp.leading)
            //: make.size.equalTo(CGSize(width: actualWidth(w: 32), height: actualWidth(w: 24)))
            make.size.equalTo(CGSize(width: actualWidth(w: 32), height: actualWidth(w: 24)))
        }

        //: vipPriceImg.snp.makeConstraints { make in
        vipPriceImg.snp.makeConstraints { make in
            //: make.top.equalToSuperview().offset(actualWidth(w: 12))
            make.top.equalToSuperview().offset(actualWidth(w: 12))
            //: make.width.height.equalTo(15)
            make.width.height.equalTo(15)
            //: make.trailing.equalTo(vipPriceLab.snp.leading).offset(-2)
            make.trailing.equalTo(vipPriceLab.snp.leading).offset(-2)
        }

        //: vipPriceLab.snp.makeConstraints { make in
        vipPriceLab.snp.makeConstraints { make in
            //: make.centerY.equalTo(vipPriceImg)
            make.centerY.equalTo(vipPriceImg)
            //: make.centerX.equalToSuperview().offset(8)
            make.centerX.equalToSuperview().offset(8)
        }
        //: norPriceLab.snp.makeConstraints { make in
        norPriceLab.snp.makeConstraints { make in
            //: make.top.equalTo(vipPriceImg.snp.bottom).offset(5)
            make.top.equalTo(vipPriceImg.snp.bottom).offset(5)
            //: make.centerX.equalToSuperview()
            make.centerX.equalToSuperview()
        }

        //: closeBtn.snp.makeConstraints { make in
        closeBtn.snp.makeConstraints { make in
            //: make.top.equalTo(StatusBarHeight+6)
            make.top.equalTo(const_musicMessage + 6)
            //: make.trailing.equalTo(-10)
            make.trailing.equalTo(-10)
            //: make.width.height.equalTo(actualWidth(w: 44))
            make.width.height.equalTo(actualWidth(w: 44))
        }

        //: connectingImg.snp.makeConstraints { make in
        connectingImg.snp.makeConstraints { make in
            //: make.center.equalToSuperview()
            make.center.equalToSuperview()
            //: make.size.equalTo(CGSize(width: actualWidth(w: 262), height: actualWidth(w: 102)))
            make.size.equalTo(CGSize(width: actualWidth(w: 262), height: actualWidth(w: 102)))
        }
    }

    /// 绑定事件
    //: private func bindInteraction() {
    private func momentumSpan() {
        // index/getconfig接口回调刷新UI
        //: NotificationCenter.default.addObserver(self,
        NotificationCenter.default.addObserver(self,
                                               //: selector: #selector(refreshVideoCallPrice),
                                               selector: #selector(cellForReady),
                                               //: name: UPDATE_INDEX_GETCONFIG_NOTIFICATION,
                                               name: appTempTitle,
                                               //: object: nil)
                                               object: nil)
        // 个人信息更新成功刷新UI
        //: NotificationCenter.default.addObserver(self,
        NotificationCenter.default.addObserver(self,
                                               //: selector: #selector(req_updateVIPFee),
                                               selector: #selector(between),
                                               //: name: USER_GETMYINFO_SUCCEED_NOTIFICATION,
                                               name: showQuitValue,
                                               //: object: nil)
                                               object: nil)

        //: NotificationCenter.default.addObserver(self, selector: #selector(closeTool), name: MATCHVIDEOCALL_END_NOTIFICATION, object: nil)
        NotificationCenter.default.addObserver(self, selector: #selector(downEase), name: const_barKey, object: nil)
    }
}
