
//: Declare String Begin

/*: "init(coder:) has not been implemented" :*/
fileprivate let data_distanceAdjustmentName:[UInt8] = [0x37,0x30,0x37,0x2a,0x76,0x3d,0x31,0x3a,0x3b,0x2c,0x64,0x77,0x7e,0x36,0x3f,0x2d,0x7e,0x30,0x31,0x2a,0x7e,0x3c,0x3b,0x3b,0x30,0x7e,0x37,0x33,0x2e,0x32,0x3b,0x33,0x3b,0x30,0x2a,0x3b,0x3a]

private func filterYield(over num: UInt8) -> UInt8 {
    return num ^ 94
}

/*: "Open then %@ will recommend users to you. A new chat will be made after a successful recommendation." :*/
fileprivate let showMissingDrawName:[UInt8] = [0xed,0xd2,0xc7,0xcc,0x82,0xd6,0xca,0xc7,0xcc,0x82,0x87,0xe2,0x82,0xd5,0xcb,0xce,0xce,0x82,0xd0,0xc7,0xc1,0xcd,0xcf,0xcf,0xc7,0xcc,0xc6,0x82,0xd7,0xd1,0xc7,0xd0,0xd1,0x82,0xd6,0xcd,0x82,0xdb,0xcd,0xd7,0x8c,0x82,0xe3,0x82,0xcc,0xc7,0xd5,0x82,0xc1,0xca,0xc3,0xd6,0x82,0xd5,0xcb,0xce,0xce,0x82,0xc0,0xc7,0x82,0xcf,0xc3,0xc6,0xc7,0x82,0xc3,0xc4,0xd6,0xc7,0xd0,0x82,0xc3,0x82,0xd1,0xd7,0xc1,0xc1,0xc7,0xd1,0xd1,0xc4,0xd7,0xce,0x82,0xd0,0xc7,0xc1,0xcd,0xcf,0xcf,0xc7,0xcc,0xc6,0xc3,0xd6,0xcb,0xcd,0xcc,0x8c]

/*: "Your avatar and nickname will not be displayed in the ranking." :*/
fileprivate let main_closedName:[UInt8] = [0x90,0xa6,0xbc,0xbb,0xe9,0xa8,0xbf,0xa8,0xbd,0xa8,0xbb,0xe9,0xa8,0xa7,0xad,0xe9,0xa7,0xa0,0xaa,0xa2,0xa7,0xa8,0xa4,0xac,0xe9,0xbe,0xa0,0xa5,0xa5,0xe9,0xa7,0xa6,0xbd,0xe9,0xab,0xac,0xe9,0xad,0xa0,0xba,0xb9,0xa5,0xa8,0xb0,0xac,0xad,0xe9,0xa0,0xa7,0xe9,0xbd,0xa1,0xac,0xe9,0xbb,0xa8,0xa7,0xa2,0xa0,0xa7,0xae,0xe7]

private func activitySingle(maximum num: UInt8) -> UInt8 {
    return num ^ 201
}

/*: "Open then Applyname will recommend users to you. Use your identity to greet each other." :*/
fileprivate let noti_interruptDadMsg:[UInt8] = [0x31,0x52,0x47,0x50,0x2,0x56,0x4a,0x47,0x50,0x2,0x23,0x52,0x52,0x4e,0x5b,0x50,0x43,0x4f,0x47,0x2,0x59,0x4b,0x4e,0x4e,0x2,0x54,0x47,0x45,0x51,0x4f,0x4f,0x47,0x50,0x46,0x2,0x57,0x55,0x47,0x54,0x55,0x2,0x56,0x51,0x2,0x5b,0x51,0x57,0x10,0x2,0x37,0x55,0x47,0x2,0x5b,0x51,0x57,0x54,0x2,0x4b,0x46,0x47,0x50,0x56,0x4b,0x56,0x5b,0x2,0x56,0x51,0x2,0x49,0x54,0x47,0x47,0x56,0x2,0x47,0x43,0x45,0x4a,0x2,0x51,0x56,0x4a,0x47,0x54,0x10]

fileprivate func numbereractionMiddle(accessible num: UInt8) -> UInt8 {
    let value = Int(num) + 30
    if value > 255 {
        return UInt8(value - 256)
    } else {
        return UInt8(value)
    }
}

/*: "Open then %@ will recommend users to you. Use your identity to greet each other." :*/
fileprivate let constCommunicateFormat:[UInt8] = [0x2e,0x72,0x65,0x68,0x74,0x6f,0x20,0x68,0x63,0x61,0x65,0x20,0x74,0x65,0x65,0x72,0x67,0x20,0x6f,0x74,0x20,0x79,0x74,0x69,0x74,0x6e,0x65,0x64,0x69,0x20,0x72,0x75,0x6f,0x79,0x20,0x65,0x73,0x55,0x20,0x2e,0x75,0x6f,0x79,0x20,0x6f,0x74,0x20,0x73,0x72,0x65,0x73,0x75,0x20,0x64,0x6e,0x65,0x6d,0x6d,0x6f,0x63,0x65,0x72,0x20,0x6c,0x6c,0x69,0x77,0x20,0x40,0x25,0x20,0x6e,0x65,0x68,0x74,0x20,0x6e,0x65,0x70,0x4f]

/*: "#FF2348" :*/
fileprivate let k_instanceString:String = "monster weight honey mention strategy#FF2348"

/*: "You're going to delete your profile,messages, photos and matched friends PERMANENTLY and your account will be UNRECOVERABLE.Continue to delete your account? " :*/
fileprivate let main_exhaustFrameValue:[UInt8] = []

/*: "UNRECOVERABLE" :*/
fileprivate let noti_spellMovePath:[Character] = ["U","N","R","E","C","O","V","E","R","A","B","L","E"]

/*: "Delete Account" :*/
fileprivate let notiMakeUrl:String = "Deletorigin framework"
fileprivate let noti_goString:String = "OUNT"

/*: "Cancel" :*/
fileprivate let main_cropFormat:String = "select filter clean mist provideCancel"

/*: "Delete" :*/
fileprivate let k_saltData:String = "Deletenumb monster reduction admin"

/*: "Logout succeeded!" :*/
fileprivate let data_borderTingString:String = "shake handle po restaurantLogout "
fileprivate let dataElevatorUrl:[Character] = ["s","u","c","c","e"]
fileprivate let noti_correctString:[Character] = ["e","d","e","d","!"]

//: Declare String End

// __DEBUG__
// __CLOSE_PRINT__
//
//  AssociateMountEstimated.swift
//  AbroadTalking
//
//  Created by Charlotte on 2023/4/26.
//

//: import UIKit
import UIKit

//: enum SettingsUniversal: String {
enum MoveFlexible: String {
    //: case Contact_information          = "Contact information"
    case Contact_information = "Contact information"
    //: case Notifications                = "Notifications"
    case Notifications
    //: case Social_Preferences           = "Social Preferences"
    case Social_Preferences = "Social Preferences"
}

//: enum SettingsUniversalType: String {
enum SectionConvert: String {
    //: case Turnon_Notif          = "Turn on Notification"
    case Turnon_Notif = "Turn on Notification"
    //: case Same_gendered         = "Same-gendered messaging"
    case Same_gendered = "Same-gendered messaging"
    //: case Cupid                 = "Cupid messaging"
    case Cupid = "Cupid messaging"
    //: case Ranking               = "Ranking anonymous"
    case Ranking = "Ranking anonymous"
    //: case Automatic             = "Automatic message"
    case Automatic = "Automatic message"
    //: case DeleteA               = "Delete Account"
    case DeleteA = "Delete Account"
}

//: class TalkingSettingsUniversalVC: TalkingBaseViewController {
class AssociateMountEstimated: EasyChainSensor {
    //: fileprivate lazy var bag = DisposeBag()
    fileprivate lazy var bag = DisposeBag()
    //: var settingModel = TalkingSettingModel()
    var settingModel = ChapterModel()

    //: var  type = SettingsUniversal.Contact_information
    var type = MoveFlexible.Contact_information

    //: override init(nibName nibNameOrNil: String?, bundle nibBundleOrNil: Bundle?) {
    override init(nibName nibNameOrNil: String?, bundle nibBundleOrNil: Bundle?) {
        //: super.init(nibName: nil, bundle: nil)
        super.init(nibName: nil, bundle: nil)
    }

    //: required init?(coder: NSCoder) {
    required init?(coder: NSCoder) {
        //: fatalError("init(coder:) has not been implemented")
        fatalError(String(bytes: data_distanceAdjustmentName.map{filterYield(over: $0)}, encoding: .utf8)!)
    }

    //: override func viewWillAppear(_ animated: Bool) {
    override func viewWillAppear(_ animated: Bool) {
        //: super.viewWillAppear(animated)
        super.viewWillAppear(animated)
        //: self.MainTable.reloadData()
        self.MainTable.reloadData()
    }

    //: override func viewDidAppear(_ animated: Bool) {
    override func viewDidAppear(_ animated: Bool) {
        //: super.viewDidAppear(animated)
        super.viewDidAppear(animated)
    }

    //: override func viewDidDisappear(_ animated: Bool) {
    override func viewDidDisappear(_ animated: Bool) {
        //: super.viewDidDisappear(animated)
        super.viewDidDisappear(animated)
    }

    //: override func viewDidLoad() {
    override func viewDidLoad() {
        //: super.viewDidLoad()
        super.viewDidLoad()
        //: self.view.backgroundColor = UIColor.appBgColor()
        self.view.backgroundColor = UIColor.alongSupposed()
        //: designView()
        renderWithNameure()
        //: getSettingData()
        aspectShrimp()
    }

    // MARK: - Lazy load

    //: private lazy var DataSource: [SettingsUniversalType] = {
    private lazy var DataSource: [SectionConvert] = {
        //: var array = [SettingsUniversalType]()
        var array = [SectionConvert]()
        //: return array
        return array
        //: }()
    }()

    //: lazy var MainTable: UITableView = {
    lazy var MainTable: UITableView = {
        //: let table = UITableView(frame: CGRect(x: 0, y: 0, width: ScreenWidth, height: ScreenHeight-TabBarViewHeight), style: UITableView.Style.plain)
        let table = UITableView(frame: CGRect(x: 0, y: 0, width: show_errorChangeFormat, height: appLoadMessage - user_directionAnalyzeId), style: UITableView.Style.plain)
        //: table.backgroundColor = UIColor.clear
        table.backgroundColor = UIColor.clear
        //: return table
        return table
        //: }()
    }()

    //: private lazy var DetailSource: [String] = {
    private lazy var DetailSource: [String] = {
        //: var array = ["", "Open then %@ will recommend users to you. A new chat will be made after a successful recommendation.".localizedArguments(AppName), "Your avatar and nickname will not be displayed in the ranking.".localized, "Open then Applyname will recommend users to you. Use your identity to greet each other.".localized]
        var array = ["", String(bytes: showMissingDrawName.map{$0^162}, encoding: .utf8)!.theAppear(appClosetoTitle), String(bytes: main_closedName.map{activitySingle(maximum: $0)}, encoding: .utf8)!.localized, String(bytes: noti_interruptDadMsg.map{numbereractionMiddle(accessible: $0)}, encoding: .utf8)!.localized]
        //: if MarginExamineer.share.loginUserMode.sex == "2" {
        if MarginExamineer.share.loginUserMode.sex == "2" {
            //: array[1] = "Open then %@ will recommend users to you. Use your identity to greet each other.".localizedArguments(AppName)
            array[1] = String(bytes: constCommunicateFormat.reversed(), encoding: .utf8)!.theAppear(appClosetoTitle)
        }
        //: return array
        return array
        //: }()
    }()
}

// MARK: - Load data

//: extension TalkingSettingsUniversalVC {
extension AssociateMountEstimated {
    //: func getSettingData() {
    func aspectShrimp() {
        //: let dict = Dictionary<String, Any>()
        let dict = [String: Any]()
        //: TalkingMeRequestTool.req_GetSetting(params: dict) { succeed, result, errorModel in
        ConstraintPublisher.butt(params: dict) { succeed, result, errorModel in
            //: if succeed {
            if succeed {
                //: self.settingModel = JSONDeserializer<TalkingSettingModel>.deserializeFrom(dict: result as? Dictionary<String, Any>, designatedPath: nil)!
                self.settingModel = JSONDeserializer<ChapterModel>.deserializeFrom(dict: result as? [String: Any], designatedPath: nil)!
                //: self.MainTable.reloadData()
                self.MainTable.reloadData()
            }
        }
    }

    //: func setUnicersalView(type: SettingsUniversal) {
    func delayBack(type: MoveFlexible) {
        //: self.type = type
        self.type = type
        //: self.title = type.rawValue.localized
        self.title = type.rawValue.localized
        //: switch type {
        switch type {
        //: case .Contact_information:
        case .Contact_information:
            //: DataSource.append(.DeleteA)
            DataSource.append(.DeleteA)
        //: break
        //: case .Notifications:
        case .Notifications:
            //: DataSource.append(.Turnon_Notif)
            DataSource.append(.Turnon_Notif)
        //: break
        //: case .Social_Preferences:
        case .Social_Preferences:
            //: DataSource.append(.Same_gendered)
            DataSource.append(.Same_gendered)
            //: DataSource.append(.Cupid)
            DataSource.append(.Cupid)
            //: DataSource.append(.Ranking)
            DataSource.append(.Ranking)
            //: DataSource.append(.Automatic)
            DataSource.append(.Automatic)
            //: break
        }
        //: self.MainTable.reloadData()
        self.MainTable.reloadData()
    }
}

// MARK: - UITableViewDelegate, UITableViewDataSource

//: extension TalkingSettingsUniversalVC: UITableViewDelegate, UITableViewDataSource {
extension AssociateMountEstimated: UITableViewDelegate, UITableViewDataSource {
    //: func numberOfSections(in tableView: UITableView) -> Int {
    func numberOfSections(in tableView: UITableView) -> Int {
        //: return 1
        return 1
    }

    //: func tableView(_ tableView: UITableView, numberOfRowsInSection section: Int) -> Int {
    func tableView(_ tableView: UITableView, numberOfRowsInSection section: Int) -> Int {
        //: return DataSource.count
        return DataSource.count
    }

    //: func tableView(_ tableView: UITableView, heightForRowAt indexPath: IndexPath) -> CGFloat {
    func tableView(_ tableView: UITableView, heightForRowAt indexPath: IndexPath) -> CGFloat {
        //: switch type {
        switch type {
        //: case .Contact_information, .Notifications:
        case .Contact_information, .Notifications:
            //: return actualHeight(h: 50)
            return actualHeight(h: 50)
        //: case .Social_Preferences:
        case .Social_Preferences:
            //: if DataSource[indexPath.row] == .Same_gendered {
            if DataSource[indexPath.row] == .Same_gendered {
                //: return actualHeight(h: 50)
                return actualHeight(h: 50)
                //: }else if DataSource[indexPath.row] == .Cupid {
            } else if DataSource[indexPath.row] == .Cupid {
                //: if MarginExamineer.share.loginUserMode.sex == "2" {
                if MarginExamineer.share.loginUserMode.sex == "2" {
                    //: return actualHeight(h: 89)
                    return actualHeight(h: 89)
                    //: } else {
                } else {
                    //: return actualHeight(h: 109)
                    return actualHeight(h: 109)
                }
                //: }  else if (DataSource[indexPath.row] == .Ranking ||  DataSource[indexPath.row] == .Automatic) {
            } else if DataSource[indexPath.row] == .Ranking || DataSource[indexPath.row] == .Automatic {
                //: return actualHeight(h: 92)
                return actualHeight(h: 92)
            }
            //: return actualHeight(h: 50)
            return actualHeight(h: 50)
        }
    }

    //: func tableView(_ tableView: UITableView, cellForRowAt indexPath: IndexPath) -> UITableViewCell {
    func tableView(_ tableView: UITableView, cellForRowAt indexPath: IndexPath) -> UITableViewCell {
        //: switch self.type {
        switch self.type {
        //: case .Contact_information:
        case .Contact_information:
            //: let cell: TalkingSettingCell = tableView.dequeueReusableCell(withIdentifier: TalkingSettingCell.className(), for: indexPath) as! TalkingSettingCell
            let cell: DensityLimit = tableView.dequeueReusableCell(withIdentifier: DensityLimit.className(), for: indexPath) as! DensityLimit

            //: cell.setCellMessage(titile: DataSource[indexPath.row].rawValue, row: indexPath.row, isLast: indexPath.row == DataSource.count-1 )
            cell.particle(titile: DataSource[indexPath.row].rawValue, row: indexPath.row, isLast: indexPath.row == DataSource.count - 1)
            //: cell.titleBLB.textColor = UIColor.appTitleColor()
            cell.titleBLB.textColor = UIColor.untilExpected()

            //: if DataSource[indexPath.row] == SettingsUniversalType.DeleteA {
            if DataSource[indexPath.row] == SectionConvert.DeleteA {
                //: cell.editImag.isHidden = true
                cell.editImag.isHidden = true
                //: cell.titleBLB.textColor = UIColor.init(hex: "#FF2348")
                cell.titleBLB.textColor = UIColor(hex: (String(k_instanceString.suffix(7))))
            }

            //: return cell
            return cell

        //: case .Notifications, .Social_Preferences:
        case .Notifications, .Social_Preferences:
            //: let cell: TalkingSettingNotifCell = tableView.dequeueReusableCell(withIdentifier: TalkingSettingNotifCell.className(), for: indexPath) as! TalkingSettingNotifCell
            let cell: HeapUniformEvent = tableView.dequeueReusableCell(withIdentifier: HeapUniformEvent.className(), for: indexPath) as! HeapUniformEvent

            //: cell.setCellMessage(titile: DataSource[indexPath.row].rawValue, row: indexPath.row, isLast: indexPath.row == DataSource.count-1 )
            cell.measure(titile: DataSource[indexPath.row].rawValue, row: indexPath.row, isLast: indexPath.row == DataSource.count - 1)
            //: if self.type == .Social_Preferences {
            if self.type == .Social_Preferences {
                //: cell.setCellDetails(detail: DetailSource[indexPath.row])
                cell.visualSuccessfully(detail: DetailSource[indexPath.row])
            }
            //: if DataSource[indexPath.row] == .Turnon_Notif {
            if DataSource[indexPath.row] == .Turnon_Notif {
                //: UNUserNotificationCenter.current().getNotificationSettings { settings in
                UNUserNotificationCenter.current().getNotificationSettings { settings in
                    //: DispatchQueue.main.async {
                    DispatchQueue.main.async {
                        //: let result = settings.authorizationStatus == .authorized
                        let result = settings.authorizationStatus == .authorized
                        //: cell.setCellNotif(isShow: result ? 1 : -1)
                        cell.packageSocial(isShow: result ? 1 : -1)
                    }
                }
                //: } else if DataSource[indexPath.row] == .Cupid {
            } else if DataSource[indexPath.row] == .Cupid {
                //: cell.setCellNotif(isShow: self.settingModel.isCupid ?? 1)
                cell.packageSocial(isShow: self.settingModel.isCupid ?? 1)
                //: } else if DataSource[indexPath.row] == .Ranking {
            } else if DataSource[indexPath.row] == .Ranking {
                //: cell.setCellNotif(isShow: self.settingModel.isLeaderboardAnonymous ?? -1)
                cell.packageSocial(isShow: self.settingModel.isLeaderboardAnonymous ?? -1)
                //: } else if DataSource[indexPath.row] == .Same_gendered {
            } else if DataSource[indexPath.row] == .Same_gendered {
                //: cell.setCellNotif(isShow: self.settingModel.isSameGendered ?? -1)
                cell.packageSocial(isShow: self.settingModel.isSameGendered ?? -1)
                //: } else if DataSource[indexPath.row] == .Automatic {
            } else if DataSource[indexPath.row] == .Automatic {
                //: cell.setCellNotif(isShow: self.settingModel.autoGreetAuth ?? -1)
                cell.packageSocial(isShow: self.settingModel.autoGreetAuth ?? -1)
            }
            //: return cell
            return cell
        }
    }

    //: func tableView(_ tableView: UITableView, didSelectRowAt indexPath: IndexPath) {
    func tableView(_ tableView: UITableView, didSelectRowAt indexPath: IndexPath) {
        //: if self.type == .Contact_information {
        if self.type == .Contact_information {
            //: if DataSource[indexPath.row].rawValue == SettingsUniversalType.DeleteA.rawValue {
            if DataSource[indexPath.row].rawValue == SectionConvert.DeleteA.rawValue {
                //: writeOff()
                commonFor()
            }
        }
    }
}

//: extension TalkingSettingsUniversalVC {
extension AssociateMountEstimated {
    /// 注销
    //: private func writeOff() {
    private func commonFor() {
        //: guard TalkingSocketManager.shared.pacific == false else {
        guard MaterialCurrency.shared.pacific == false else { // 音视频通话中
            //: self.func__showStatusBarErrorMsg(showMsg: kMessage_videoTalking_limit)
            self.exitMarker(showMsg: constFormalValue)
            //: return
            return
        }

        //: var seconds = 15
        var seconds = 15
        //: let timer: DispatchSourceTimer = DispatchSource.makeTimerSource(flags: [], queue: DispatchQueue.main)
        let timer: DispatchSourceTimer = DispatchSource.makeTimerSource(flags: [], queue: DispatchQueue.main)
        //: timer.schedule(deadline: .now(), repeating: 1.0)
        timer.schedule(deadline: .now(), repeating: 1.0)

        //: let str = "You're going to delete your profile,messages, photos and matched friends PERMANENTLY and your account will be UNRECOVERABLE.Continue to delete your account? ".localized
        let str = String(bytes: main_exhaustFrameValue.reversed(), encoding: .utf8)!.localized

        //: let range =  str.exMatchStrRange("UNRECOVERABLE")
        let range = str.cropUniform((String(noti_spellMovePath)))

        //: let string = NSMutableAttributedString.init(string: str )
        let string = NSMutableAttributedString(string: str)
        //: if range.count > 0 {
        if range.count > 0 {
            //: string.addAttribute(.foregroundColor, value: UIColor.msgTipsColor(), range: range.first!)
            string.addAttribute(.foregroundColor, value: UIColor.atMount(), range: range.first!)
        }
        //: TalkingAlertShow.attribAlert(title: "Delete Account".localized, attributedMessage: string, leftBtnTitle: "Cancel".localized, rightBtnTitle: "Delete".localized) {
        InputMight.beyondExtendSh(title: (String(notiMakeUrl.prefix(5)) + "e Acc" + noti_goString.lowercased()).localized, attributedMessage: string, leftBtnTitle: (String(main_cropFormat.suffix(6))).localized, rightBtnTitle: (String(k_saltData.prefix(6))).localized) {
            //: TalkingAlertShow.hideAlert()
            InputMight.runningProjection()
            //: timer.cancel()
            timer.cancel()
            //: } rightBlock: {
        } rightBlock: {
            //: TalkingAlertShow.hideAlert()
            InputMight.runningProjection()

            //: self.LockAccount()
            self.hangInvisible()
        }

        //: timer.setEventHandler {
        timer.setEventHandler {
            //: seconds -= 1
            seconds -= 1
            //: if seconds <= 0 {
            if seconds <= 0 {
                //: timer.cancel()
                timer.cancel()
                //: TalkingAlertShow.changeRightBtn(title: "Delete".localized, isTouch: true)
                InputMight.bigPop(title: (String(k_saltData.prefix(6))).localized, isTouch: true)
                //: } else {
            } else {
                //: TalkingAlertShow.changeRightBtn(title: "Delete".localized + " (\(seconds)s)", isTouch: false)
                InputMight.bigPop(title: (String(k_saltData.prefix(6))).localized + " (\(seconds)s)", isTouch: false)
            }
        }
        //: timer.resume()
        timer.resume()
    }

    //: private func LockAccount() {
    private func hangInvisible() {
        //: ProgressHUD.show()
        BeforeImagePhase.theGemRoll()
        //: TalkingMeRequestTool.req_LockAccount(params: [:]) { succeed, result, errorModel in
        ConstraintPublisher.service(params: [:]) { succeed, result, errorModel in
            //: ProgressHUD.dismiss()
            BeforeImagePhase.totalGap()
            //: if succeed {
            if succeed {
                //: self.logoutTool()
                self.universalKit()
                //: self.func__showStatusBarSuccessMsg(showMsg: "Logout succeeded!".localized)
                self.nearEasy(showMsg: (String(data_borderTingString.suffix(7)) + String(dataElevatorUrl) + String(noti_correctString)).localized)
            }
        }
    }

    //: private func logoutTool() {
    private func universalKit() {
        //: guard TalkingSocketManager.shared.pacific == false else {
        guard MaterialCurrency.shared.pacific == false else { // 音视频通话中
            //: self.func__showStatusBarErrorMsg(showMsg: kMessage_videoTalking_limit)
            self.exitMarker(showMsg: constFormalValue)
            //: return
            return
        }

        //: ProgressHUD.show()
        BeforeImagePhase.theGemRoll()
        //: TalkingLoginRequestTool.req_loginOut { t in
        ConvertLocal.innerCountensity { t in
            //: ProgressHUD.dismiss()
            BeforeImagePhase.totalGap()
            //: if t {
            if t {
                //: NotificationCenter.default.post(name: DID_LOGIN_OUT_SUCCESS_NOTIFICATION, object: nil, userInfo: nil)
                NotificationCenter.default.post(name: mainOperationPadString, object: nil, userInfo: nil)
            }
        }
    }
}

// MARK: - UI

//: extension TalkingSettingsUniversalVC {
extension AssociateMountEstimated {
    //: private func designView() {
    private func renderWithNameure() {
        //: self.view.addSubview(MainTable)
        self.view.addSubview(MainTable)
        //: MainTable.register(TalkingSettingCell.self, forCellReuseIdentifier: TalkingSettingCell.className())
        MainTable.register(DensityLimit.self, forCellReuseIdentifier: DensityLimit.className())
        //: MainTable.register(TalkingSettingNotifCell.self, forCellReuseIdentifier: TalkingSettingNotifCell.className())
        MainTable.register(HeapUniformEvent.self, forCellReuseIdentifier: HeapUniformEvent.className())
        //: MainTable.separatorStyle = .none
        MainTable.separatorStyle = .none
        //: MainTable.dataSource = self
        MainTable.dataSource = self
        //: MainTable.delegate = self
        MainTable.delegate = self
        //: MainTable.snp.makeConstraints { make in
        MainTable.snp.makeConstraints { make in
            //: make.top.equalTo(20)
            make.top.equalTo(20)
            //: make.leading.trailing.bottom.equalTo(self.view)
            make.leading.trailing.bottom.equalTo(self.view)
        }
        //: MainTable.contentInset = UIEdgeInsets(top: 0, left: 0, bottom: 36, right: 0)
        MainTable.contentInset = UIEdgeInsets(top: 0, left: 0, bottom: 36, right: 0)
    }
}
