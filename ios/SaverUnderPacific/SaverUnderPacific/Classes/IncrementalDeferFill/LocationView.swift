
//: Declare String Begin

/*: "init(coder:) has not been implemented" :*/
fileprivate let constSingleValueData:[UInt8] = [0x82,0x85,0x82,0x9f,0xc3,0x88,0x84,0x8f,0x8e,0x99,0xd1,0xc2,0xcb,0x83,0x8a,0x98,0xcb,0x85,0x84,0x9f,0xcb,0x89,0x8e,0x8e,0x85,0xcb,0x82,0x86,0x9b,0x87,0x8e,0x86,0x8e,0x85,0x9f,0x8e,0x8f]

/*: "#322870" :*/
fileprivate let constSearcherKey:String = "#32287"
fileprivate let app_maintainData:String = "0"

/*: "icon_home_position" :*/
fileprivate let data_detectEarlyPath:[Character] = ["i","c","o","n","_","h","o","m","e","_","p","o","s","i"]
fileprivate let app_sceneTitle:String = "tisuccessn"

/*: "btn_video_minimize" :*/
fileprivate let user_tapBlueName:[Character] = ["b","t","n","_","v","i","d","e","o","_"]
fileprivate let const_typicalId:String = "minimitransitione"

/*: "btn_video_drop_nor" :*/
fileprivate let noti_viewUrl:[Character] = ["b","t","n","_","v","i","d","e","o","_","d","r","o"]
fileprivate let const_spotKey:String = "p_norrespective already"

/*: "btn_video_call_nor" :*/
fileprivate let mainPlaceId:String = "back find combine recentbtn_"
fileprivate let userPromiseId:String = "video_drown cancel"

/*: "transform.scale" :*/
fileprivate let user_botStr:String = "trprevioussf"
fileprivate let dataPastId:[Character] = ["o","r","m",".","s","c","a","l","e"]

/*: "animation" :*/
fileprivate let data_instanceTransactionFormat:[Character] = ["a","n","i","m","a","t","i","o","n"]

/*: "btn_voice_Micr_nor" :*/
fileprivate let userEasyPath:[Character] = ["b","t","n","_","v","o","i","c","e","_","M","i","c","r","_"]
fileprivate let dataWaitButtonMessage:String = "nostat"

/*: "btn_voice_Micr_pre" :*/
fileprivate let data_offAgoMessage:String = "btn_voiapp recording accept program"
fileprivate let notiWeeklyId:[Character] = ["c","e"]
fileprivate let appPutRecordValue:[Character] = ["_","M","i","c","r","_","p","r","e"]

/*: "btn_voice_hands_nor" :*/
fileprivate let dataArcString:String = "text riotbtn_v"
fileprivate let constWhiteUrl:String = "ands_nomultiple extra sir union false"
fileprivate let userDownResData:[Character] = ["r"]

/*: "btn_voice_hands_pre" :*/
fileprivate let appSpecKey:String = "btn_voor week plain"
fileprivate let noti_infoStripPath:[Character] = ["i","c","e","_","h","a","n","d","s","_","p","r","e"]

/*: "icon_coin_nor" :*/
fileprivate let show_orientationHealthyMessage:String = "icon_black title where"
fileprivate let k_absoluteUrl:String = "chart charm reportcoin_nor"

/*: "btn_voice_gift_nor" :*/
fileprivate let app_assertData:[Character] = ["b","t","n","_","v","o","i","c","e","_","g","i","f","t","_","n","o","r"]

/*: "Waiting to answer the phone..." :*/
fileprivate let const_nobodyUrl:[Character] = ["W","a","i","t","i","n","g"," ","t","o"," "]
fileprivate let main_primaryPath:String = "centers"
fileprivate let dataTrainData:String = "surrounding titlewer th"
fileprivate let mainSlowRateFormat:String = "mm reflect premiume..."

/*: "00:00" :*/
fileprivate let noti_enhanceKey:[Character] = ["0","0",":","0","0"]

/*: "Waiting for %@ to answer the phone..." :*/
fileprivate let kAdditionId:[UInt8] = [0xc7,0xf1,0xf9,0xe4,0xf9,0xfe,0xf7,0xb0,0xf6,0xff,0xe2,0xb0,0xb5,0xd0,0xb0,0xe4,0xff,0xb0,0xf1,0xfe,0xe3,0xe7,0xf5,0xe2,0xb0,0xe4,0xf8,0xf5,0xb0,0xe0,0xf8,0xff,0xfe,0xf5,0xbe,0xbe,0xbe]

private func secureThere(ceiling num: UInt8) -> UInt8 {
    return num ^ 144
}

/*: "CIGaussianBlur" :*/
fileprivate let show_duringStr:[Character] = ["C","I","G","a","u","s","s"]
fileprivate let user_aliveUrl:[Character] = ["i","a"]
fileprivate let notiMinuteValue:String = "model from persist editnBlur"

/*: "toUid" :*/
fileprivate let main_mistStr:[Character] = ["t","o","U","i","d"]

/*: "giftId" :*/
fileprivate let k_reportMsg:[Character] = ["g","i","f","t","I","d"]

/*: "giftNum" :*/
fileprivate let userReduceData:[Character] = ["g","i","f","t","N","u"]
fileprivate let data_mageTitle:String = "go"

/*: "from" :*/
fileprivate let mainFitFollowingMsg:String = "fprocessingom"

/*: "audioCall" :*/
fileprivate let const_reflectUrl:String = "auhighlight"
fileprivate let dataEveryoneKey:[Character] = ["i","o","C","a","l","l"]

/*: "pkgItemsetId" :*/
fileprivate let userSpaceScopeSameId:String = "topic min reading possiblepkgIte"

/*: "msgInfo" :*/
fileprivate let main_runningUrl:[Character] = ["m"]
fileprivate let app_timingTitle:String = "read odd invisible shadow manualsgInfo"

/*: "msgType" :*/
fileprivate let user_pageWrittenName:String = "msgTyorientation texture graphic production"
fileprivate let noti_fitPath:[Character] = ["p","e"]

/*: "gift" :*/
fileprivate let userLayerMiddleString:[Character] = ["g","i","f","t"]

/*: "totalMfCoin" :*/
fileprivate let dataDeliveryArableFarTitle:String = "manual purchasetotalM"

/*: "cellData" :*/
fileprivate let user_fixedData:[UInt8] = [0x61,0x74,0x61,0x44,0x6c,0x6c,0x65,0x63]

//: Declare String End

// __DEBUG__
// __CLOSE_PRINT__
//
//  LocationView.swift
//  AbroadTalking
//
//  Created by young on 2022/12/15.
//

//: import UIKit
import UIKit

//: protocol Talking1v1VoiceInteractionViewDelegate: NSObjectProtocol {
protocol PerformPause: NSObjectProtocol {
    /// 发送方结束通话
    //: func interactionView_shouldEndCall()
    func tolerancePut()

    /// 接听音频通话
    //: func interactionView_shouldAcceptCall()
    func refuse()

    /// 是否静音
    /// - Parameter isMute: 是否静音（默认否）
    //: func interactionView_shouldChangeMute(isMute: Bool)
    func eachInvisible(isMute: Bool)

    /// 是否免提
    /// - Parameter isHandsfree: 是否免提（默认否）
    //: func interactionView_shouldChangeSpeaker(isHandsfree: Bool)
    func anPresentation(isHandsfree: Bool)

    /// 展示语音通话小窗口
    //: func interactionView_shouldDisplayMiniWindow()
    func no()

    /// 更新小窗口通话时间
    /// - Parameter time: 通话时间
    //: func interactionView_shouldUpdateTime(time: Double)
    func frameworkStar(time: Double)
}

//: class Talking1v1VoiceInteractionView: UIView {
class LocationView: UIView {
    //: weak var delegate: Talking1v1VoiceInteractionViewDelegate?
    weak var delegate: PerformPause?
    //: private var chatModel = TalkingVideoChatModel()
    private var chatModel = IconModel() // 引用类型，整个音频通话共用同一个model
    //: private var waitTimer: Timer?
    private var waitTimer: Timer? // 等待接听动画定时器
    //: private var startTime: TimeInterval?
    private var startTime: TimeInterval? // 开始的时间戳记 since1970
    //: private var countTimer: Timer?
    private var countTimer: Timer? // 通话时间定时器

    //: init(frame: CGRect, chatModel: TalkingVideoChatModel?) {
    init(frame: CGRect, chatModel: IconModel?) {
        //: super.init(frame: frame)
        super.init(frame: frame)
        //: setupSubviews()
        connectionWho()
        //: setupSubViewsConstraint()
        ond()
        //: if chatModel != nil {
        if chatModel != nil {
            //: self.chatModel = chatModel!
            self.chatModel = chatModel!
            //: self.startTime = chatModel!.startTime
            self.startTime = chatModel!.startTime
        }
        //: showViewsViaStage()
        blockTrigger()
        //: initCachEffectData()
        stopCurrency()
    }

    //: required init?(coder: NSCoder) {
    required init?(coder: NSCoder) {
        //: fatalError("init(coder:) has not been implemented")
        fatalError(String(bytes: constSingleValueData.map{$0^235}, encoding: .utf8)!)
    }

    //: deinit {
    deinit {
        //: if waitTimer != nil {
        if waitTimer != nil {
            //: waitTimer?.invalidate()
            waitTimer?.invalidate()
            //: waitTimer = nil
            waitTimer = nil
        }
        //: if countTimer != nil {
        if countTimer != nil {
            //: countTimer?.invalidate()
            countTimer?.invalidate()
            //: countTimer = nil
            countTimer = nil
        }
//        self.player.stopPlay()
    }

    // MARK: - Lazy Load

    // 渐变背景视图
    //: lazy var bgView: UIImageView = {
    lazy var bgView: UIImageView = {
        //: let v = UIImageView()
        let v = UIImageView()
        //: v.contentMode = .scaleAspectFill
        v.contentMode = .scaleAspectFill
        //: v.layer.masksToBounds = true
        v.layer.masksToBounds = true
        // 渐变色
        //: let layer = CAGradientLayer()
        let layer = CAGradientLayer()
        // 宽高+1是为了适配在某些设备上出现渐变填充不满的情况
        //: layer.frame = CGRect(x: 0, y: 0, width: ScreenWidth+1, height: ScreenHeight+1)
        layer.frame = CGRect(x: 0, y: 0, width: show_errorChangeFormat + 1, height: appLoadMessage + 1)
        //: layer.startPoint = CGPoint(x: 0.5, y: 0)
        layer.startPoint = CGPoint(x: 0.5, y: 0)
        //: layer.endPoint = CGPoint(x: 0.5, y: 1)
        layer.endPoint = CGPoint(x: 0.5, y: 1)
        //: layer.colors = [UIColor(white: 0, alpha: 0.15).cgColor, UIColor(hex: "#322870")!.cgColor]
        layer.colors = [UIColor(white: 0, alpha: 0.15).cgColor, UIColor(hex: (constSearcherKey.capitalized + app_maintainData.capitalized))!.cgColor]
        //: layer.locations = [0, 1]
        layer.locations = [0, 1]
        //: v.layer.addSublayer(layer)
        v.layer.addSublayer(layer)

        //: return v
        return v
        //: }()
    }()

    // 点击展示/隐藏交互按钮
    //: private lazy var touchHidenBtn: UIButton = {
    private lazy var touchHidenBtn: UIButton = {
        //: let btn = UIButton()
        let btn = UIButton()
        //: btn.addTarget(self, action: #selector(changeInteractionShowState), for: .touchUpInside)
        btn.addTarget(self, action: #selector(awakeOn), for: .touchUpInside)
        //: return btn
        return btn
        //: }()
    }()

    // 接收方头像
    //: private lazy var pairPicV: UIImageView = {
    private lazy var pairPicV: UIImageView = {
        //: let v = UIImageView()
        let v = UIImageView()
        //: v.contentMode = .scaleAspectFill
        v.contentMode = .scaleAspectFill
        //: v.isUserInteractionEnabled = true
        v.isUserInteractionEnabled = true
        //: v.layer.cornerRadius = actualWidth(w: 100)/2
        v.layer.cornerRadius = actualWidth(w: 100) / 2
        //: v.layer.masksToBounds = true
        v.layer.masksToBounds = true
        //: return v
        return v
        //: }()
    }()

    // 接收方昵称
    //: private lazy var pairNameLab: UILabel = {
    private lazy var pairNameLab: UILabel = {
        //: let lab = UILabel()
        let lab = UILabel()
        //: lab.textColor = .white
        lab.textColor = .white
        //: lab.font = UIFont.pingfangMediumFont(fontSize: 20)
        lab.font = UIFont.hangProgram(fontSize: 20)
        //: lab.textAlignment = .center
        lab.textAlignment = .center
        //: return lab
        return lab
        //: }()
    }()

    //: private lazy var locationBtn: TalkingButton = {
    private lazy var locationBtn: DistinctionButton = {
        //: let btn = TalkingButton()
        let btn = DistinctionButton()
        //: btn.setImage(UIImage.BundleImageNamed(name: "icon_home_position"), for: .normal)
        btn.setImage(UIImage.managerToSecond(name: (String(data_detectEarlyPath) + app_sceneTitle.replacingOccurrences(of: "success", with: "o"))), for: .normal)
        //: btn.spaceBetweenTitleAndImage = 2
        btn.spaceBetweenTitleAndImage = 2
        //: btn.setTitleColor(.white, for: .normal)
        btn.setTitleColor(.white, for: .normal)
        //: btn.titleLabel?.font = UIFont.pingfangRugularFont(fontSize: 12)
        btn.titleLabel?.font = UIFont.enterCommon(fontSize: 12)
        //: btn.titleLabel?.lineBreakMode = .byTruncatingTail
        btn.titleLabel?.lineBreakMode = .byTruncatingTail
        //: return btn
        return btn
        //: }()
    }()

    // 呼叫中描述
    //: private lazy var desLab: UILabel = {
    private lazy var desLab: UILabel = {
        //: let lab = UILabel()
        let lab = UILabel()
        //: lab.textColor = UIColor(white: 1, alpha: 1)
        lab.textColor = UIColor(white: 1, alpha: 1)
        //: lab.font = UIFont.pingfangRugularFont(fontSize: 17)
        lab.font = UIFont.enterCommon(fontSize: 17)
        //: lab.numberOfLines = 0
        lab.numberOfLines = 0
        //: lab.textAlignment = .center
        lab.textAlignment = .center
        //: return lab
        return lab
        //: }()
    }()

    // 最小化按钮
    //: private lazy var minimizeBtn: UIButton = {
    private lazy var minimizeBtn: UIButton = {
        //: let btn = UIButton()
        let btn = UIButton()
        //: btn.setImage(UIImage.BundleImageNamed(name: "btn_video_minimize"), for: .normal)
        btn.setImage(UIImage.managerToSecond(name: (String(user_tapBlueName) + const_typicalId.replacingOccurrences(of: "transition", with: "z"))), for: .normal)
        //: btn.addTarget(self, action: #selector(minimizeButtonClick), for: .touchUpInside)
        btn.addTarget(self, action: #selector(attributeAnonymous), for: .touchUpInside)
        //: return btn
        return btn
        //: }()
    }()

    // 挂断/取消 按钮
    //: private lazy var hangupBtn: UIButton = {
    private lazy var hangupBtn: UIButton = {
        //: let btn = UIButton()
        let btn = UIButton()
        //: btn.setImage(UIImage.BundleImageNamed(name: "btn_video_drop_nor"), for: .normal)
        btn.setImage(UIImage.managerToSecond(name: (String(noti_viewUrl) + String(const_spotKey.prefix(5)))), for: .normal)
        //: btn.addTarget(self, action: #selector(hangupButtonClick), for: .touchUpInside)
        btn.addTarget(self, action: #selector(tickOf), for: .touchUpInside)
        //: return btn
        return btn
        //: }()
    }()

    // 接听按钮
    //: private lazy var acceptBtn: UIButton = {
    private lazy var acceptBtn: UIButton = {
        //: let btn = UIButton()
        let btn = UIButton()
        //: btn.setImage(UIImage.BundleImageNamed(name: "btn_video_call_nor"), for: .normal)
        btn.setImage(UIImage.managerToSecond(name: (String(mainPlaceId.suffix(4)) + String(userPromiseId.prefix(6)) + "call_nor")), for: .normal)
        //: btn.addTarget(self, action: #selector(acceptButtonClick), for: .touchUpInside)
        btn.addTarget(self, action: #selector(betweenSlow), for: .touchUpInside)

        // 添加关键帧动画
        //: let keyAnimation = CAKeyframeAnimation()
        let keyAnimation = CAKeyframeAnimation()
        //: keyAnimation.keyPath = "transform.scale"
        keyAnimation.keyPath = (user_botStr.replacingOccurrences(of: "previous", with: "an") + String(dataPastId))
        //: keyAnimation.values = [NSValue(cgAffineTransform: CGAffineTransform(scaleX: 1, y: 1)),
        keyAnimation.values = [NSValue(cgAffineTransform: CGAffineTransform(scaleX: 1, y: 1)),
                               //: NSValue(cgAffineTransform: CGAffineTransform(scaleX: actualWidth(w: 80/70), y: actualWidth(w: 80/70))),
                               NSValue(cgAffineTransform: CGAffineTransform(scaleX: actualWidth(w: 80 / 70), y: actualWidth(w: 80 / 70))),
                               //: NSValue(cgAffineTransform: CGAffineTransform(scaleX: 1, y: 1))]
                               NSValue(cgAffineTransform: CGAffineTransform(scaleX: 1, y: 1))]
        //: keyAnimation.duration = 1.2
        keyAnimation.duration = 1.2
        //: keyAnimation.repeatCount = MAXFLOAT
        keyAnimation.repeatCount = MAXFLOAT
        //: keyAnimation.isRemovedOnCompletion = false
        keyAnimation.isRemovedOnCompletion = false
        //: keyAnimation.fillMode = .both
        keyAnimation.fillMode = .both
        //: keyAnimation.calculationMode = .cubicPaced
        keyAnimation.calculationMode = .cubicPaced
        //: btn.layer.add(keyAnimation, forKey: "animation")
        btn.layer.add(keyAnimation, forKey: (String(data_instanceTransactionFormat)))

        //: return btn
        return btn
        //: }()
    }()

    // 开启/关闭麦克风按钮
    //: private lazy var micBtn: UIButton = {
    private lazy var micBtn: UIButton = {
        //: let btn = UIButton()
        let btn = UIButton()
        //: btn.setImage(UIImage.BundleImageNamed(name: "btn_voice_Micr_nor"), for: .normal)
        btn.setImage(UIImage.managerToSecond(name: (String(userEasyPath) + dataWaitButtonMessage.replacingOccurrences(of: "stat", with: "r"))), for: .normal)
        //: btn.setImage(UIImage.BundleImageNamed(name: "btn_voice_Micr_pre"), for: .selected)
        btn.setImage(UIImage.managerToSecond(name: (String(data_offAgoMessage.prefix(7)) + String(notiWeeklyId) + String(appPutRecordValue))), for: .selected)
        //: btn.addTarget(self, action: #selector(microphoneButtonClick), for: .touchUpInside)
        btn.addTarget(self, action: #selector(appFromLaunch), for: .touchUpInside)
        //: return btn
        return btn
        //: }()
    }()

    // 开启/关闭免提按钮
    //: lazy var handsfreeBtn: UIButton = {
    lazy var handsfreeBtn: UIButton = {
        //: let btn = UIButton()
        let btn = UIButton()
        //: btn.setImage(UIImage.BundleImageNamed(name: "btn_voice_hands_nor"), for: .normal)
        btn.setImage(UIImage.managerToSecond(name: (String(dataArcString.suffix(5)) + "oice_h" + String(constWhiteUrl.prefix(7)) + String(userDownResData))), for: .normal)
        //: btn.setImage(UIImage.BundleImageNamed(name: "btn_voice_hands_pre"), for: .selected)
        btn.setImage(UIImage.managerToSecond(name: (String(appSpecKey.prefix(6)) + String(noti_infoStripPath))), for: .selected)
        //: btn.addTarget(self, action: #selector(handsfreeButtonClick), for: .touchUpInside)
        btn.addTarget(self, action: #selector(doMaster), for: .touchUpInside)
        //: return btn
        return btn
        //: }()
    }()

    // 充值按钮
    //: private lazy var rechargeBtn: UIButton = {
    private lazy var rechargeBtn: UIButton = {
        //: let btn = UIButton()
        let btn = UIButton()
        //: btn.setImage(UIImage.BundleImageNamed(name: "icon_coin_nor"), for: .normal)
        btn.setImage(UIImage.managerToSecond(name: (String(show_orientationHealthyMessage.prefix(5)) + String(k_absoluteUrl.suffix(8)))), for: .normal)
        //: btn.setImage(UIImage.BundleImageNamed(name: "icon_coin_nor"), for: .selected)
        btn.setImage(UIImage.managerToSecond(name: (String(show_orientationHealthyMessage.prefix(5)) + String(k_absoluteUrl.suffix(8)))), for: .selected)
        //: btn.addTarget(self, action: #selector(rechargeBtnClick), for: .touchUpInside)
        btn.addTarget(self, action: #selector(mountPlatform), for: .touchUpInside)
        //: return btn
        return btn
        //: }()
    }()

    // 送礼按钮
    //: private lazy var sendGiftBtn: UIButton = {
    private lazy var sendGiftBtn: UIButton = {
        //: let btn = UIButton()
        let btn = UIButton()
        //: btn.setImage(UIImage.BundleImageNamed(name: "btn_voice_gift_nor"), for: .normal)
        btn.setImage(UIImage.managerToSecond(name: (String(app_assertData))), for: .normal)
        //: btn.setImage(UIImage.BundleImageNamed(name: "btn_voice_gift_nor"), for: .selected)
        btn.setImage(UIImage.managerToSecond(name: (String(app_assertData))), for: .selected)
        //: btn.addTarget(self, action: #selector(sendGiftButtonClick), for: .touchUpInside)
        btn.addTarget(self, action: #selector(packageBy), for: .touchUpInside)
        //: return btn
        return btn
        //: }()
    }()

    //: private lazy var timeLab: UILabel = {
    private lazy var timeLab: UILabel = {
        //: let lab = UILabel()
        let lab = UILabel()
        //: lab.textColor = .white
        lab.textColor = .white
        //: lab.font = UIFont.pingfangMediumFont(fontSize: 18)
        lab.font = UIFont.hangProgram(fontSize: 18)
        //: lab.textAlignment = .center
        lab.textAlignment = .center
        //: return lab
        return lab
        //: }()
    }()

    //: lazy var giftView: TalkingChatGiftView = {
    lazy var giftView: EndView = {
        //: let giftV = TalkingChatGiftView.init(style: .call)
        let giftV = EndView(style: .call)
        //: giftV.frame = CGRect(x: 0, y: 0, width: ScreenWidth, height: ScreenHeight)
        giftV.frame = CGRect(x: 0, y: 0, width: show_errorChangeFormat, height: appLoadMessage)
        //: return giftV
        return giftV
        //: }()
    }()

    //: lazy var giftEffectView: TalkingPrivateChatAnimatView = {
    lazy var giftEffectView: ThresholdView = {
        //: let effectView = TalkingPrivateChatAnimatView()
        let effectView = ThresholdView()
        //: effectView.contentMode = .scaleAspectFill
        effectView.contentMode = .scaleAspectFill
        //: return effectView
        return effectView
        //: }()
    }()

    //: lazy var giftTrackView: TalkingGiftTrackView = {
    lazy var giftTrackView: AgileClean = {
        //: let trackView = TalkingGiftTrackView()
        let trackView = AgileClean()
        //: trackView.isUserInteractionEnabled = false
        trackView.isUserInteractionEnabled = false
        //: return trackView
        return trackView
        //: }()
    }()
}

// MARK: - Event

//: extension Talking1v1VoiceInteractionView {
extension LocationView {
    /// 展示视图状态
    //: func showViewsViaStage() {
    func blockTrigger() {
        //: switch chatModel.videoStage {
        switch chatModel.videoStage {
        //: case .Waiting:
        case .Waiting:
            //: desLab.text = "Waiting to answer the phone...".localized
            desLab.text = (String(const_nobodyUrl) + main_primaryPath.replacingOccurrences(of: "center", with: "an") + String(dataTrainData.suffix(6)) + "e phon" + String(mainSlowRateFormat.suffix(4))).localized
            //: desLab.isHidden = false
            desLab.isHidden = false
            //: minimizeBtn.isHidden = true
            minimizeBtn.isHidden = true
            //: hangupBtn.isHidden = false
            hangupBtn.isHidden = false
            //: acceptBtn.isHidden = true
            acceptBtn.isHidden = true
            //: micBtn.isHidden = true
            micBtn.isHidden = true
            //: handsfreeBtn.isHidden = true
            handsfreeBtn.isHidden = true
            //: sendGiftBtn.isHidden = true
            sendGiftBtn.isHidden = true
            //: rechargeBtn.isHidden = true
            rechargeBtn.isHidden = true
            //: timeLab.isHidden = true
            timeLab.isHidden = true
            //: touchHidenBtn.isEnabled = false
            touchHidenBtn.isEnabled = false

        //: case .Recieved:
        case .Recieved:
            //: desLab.text = nil
            desLab.text = nil
            //: desLab.isHidden = true
            desLab.isHidden = true
            //: minimizeBtn.isHidden = true
            minimizeBtn.isHidden = true
            //: hangupBtn.isHidden = false
            hangupBtn.isHidden = false
            //: acceptBtn.isHidden = false
            acceptBtn.isHidden = false
            //: micBtn.isHidden = true
            micBtn.isHidden = true
            //: handsfreeBtn.isHidden = true
            handsfreeBtn.isHidden = true
            //: sendGiftBtn.isHidden = true
            sendGiftBtn.isHidden = true
            //: rechargeBtn.isHidden = true
            rechargeBtn.isHidden = true
            //: timeLab.isHidden = true
            timeLab.isHidden = true
            //: touchHidenBtn.isEnabled = false
            touchHidenBtn.isEnabled = false

        //: case .Calling:
        case .Calling:
            //: desLab.text = nil
            desLab.text = nil
            //: desLab.isHidden = true
            desLab.isHidden = true
            //: minimizeBtn.isHidden = false
            minimizeBtn.isHidden = false
            //: hangupBtn.isHidden = false
            hangupBtn.isHidden = false
            //: acceptBtn.isHidden = true
            acceptBtn.isHidden = true
            //: micBtn.isHidden = false
            micBtn.isHidden = false
            //: handsfreeBtn.isHidden = false
            handsfreeBtn.isHidden = false
            //: sendGiftBtn.isHidden = false
            sendGiftBtn.isHidden = false
            //: rechargeBtn.isHidden = false
            rechargeBtn.isHidden = false
            //: timeLab.isHidden = false
            timeLab.isHidden = false
            //: touchHidenBtn.isEnabled = true
            touchHidenBtn.isEnabled = true
            //: locationBtn.isHidden = true
            locationBtn.isHidden = true
            //: if startTime != nil {
            if startTime != nil {
                //: changeTalkTime()
                behindConstraint()
                //: } else {
            } else {
                //: timeLab.text = "00:00"
                timeLab.text = (String(noti_enhanceKey))
            }
            //: startTalkCount()
            applicationBy()
            //: touchHiddenTimer()
            detailSparkFoundMe()
        }

        //: updateLayout()
        sizeBeauty()
        //: afterGetInfo()
        destroyView()
    }

    /// 刷新通话视图数据
    //: func afterGetInfo() {
    func destroyView() {
        //: if chatModel.videoStage == .Waiting {
        if chatModel.videoStage == .Waiting {
            //: desLab.text = "Waiting for %@ to answer the phone...".localizedArguments(chatModel.pairNickName)
            desLab.text = String(bytes: kAdditionId.map{secureThere(ceiling: $0)}, encoding: .utf8)!.theAppear(chatModel.pairNickName)
        }
        //: pairNameLab.text = chatModel.pairNickName
        pairNameLab.text = chatModel.pairNickName
        //: locationBtn.isHidden = chatModel.location.count <= 0
        locationBtn.isHidden = chatModel.location.count <= 0
        //: locationBtn.setTitle("\(chatModel.location)", for: .normal)
        locationBtn.setTitle("\(chatModel.location)", for: .normal)
        //: let optionsInfo = [KingfisherOptionsInfoItem.processor(RoundCornerImageProcessor(cornerRadius: 20))]
        let optionsInfo = [KingfisherOptionsInfoItem.processor(RoundCornerImageProcessor(cornerRadius: 20))]
        //: pairPicV.setUrlImage(urlStr: chatModel.pairHeadPic, options: optionsInfo)
        pairPicV.chest(urlStr: chatModel.pairHeadPic, options: optionsInfo)
        // 设置背景头像
        //: if !chatModel.pairHeadPic.isEmptyString {
        if !chatModel.pairHeadPic.isEmptyString {
            //: bgView.setUrlImage(urlStr: chatModel.pairHeadPic, options: nil) { [weak self] _, image in
            bgView.chest(urlStr: chatModel.pairHeadPic, options: nil) { [weak self] _, image in
                //: guard let self = self else { return }
                guard let self = self else { return }
                //: if image != nil {
                if image != nil {
                    //: self.bgView.image = self.blurImage(image!)
                    self.bgView.image = self.character(image!)
                }
            }
        }
    }

    /// 最小化按钮事件
    //: @objc private func minimizeButtonClick() {
    @objc private func attributeAnonymous() {
        //: delegate?.interactionView_shouldDisplayMiniWindow()
        delegate?.no()
    }

    /// 挂断按钮事件
    //: @objc private func hangupButtonClick() {
    @objc private func tickOf() {
        //: delegate?.interactionView_shouldEndCall()
        delegate?.tolerancePut()
    }

    /// 接听按钮事件
    //: @objc private func acceptButtonClick() {
    @objc private func betweenSlow() {
        //: delegate?.interactionView_shouldAcceptCall()
        delegate?.refuse()
    }

    /// 麦克风按钮事件
    //: @objc private func microphoneButtonClick() {
    @objc private func appFromLaunch() {
        //: micBtn.isSelected = !micBtn.isSelected
        micBtn.isSelected = !micBtn.isSelected
        //: delegate?.interactionView_shouldChangeMute(isMute: micBtn.isSelected)
        delegate?.eachInvisible(isMute: micBtn.isSelected)
    }

    /// 免提按钮事件
    //: @objc private func handsfreeButtonClick() {
    @objc private func doMaster() {
        //: delegate?.interactionView_shouldChangeSpeaker(isHandsfree: !handsfreeBtn.isSelected)
        delegate?.anPresentation(isHandsfree: !handsfreeBtn.isSelected)
    }

    /// 送礼按钮事件
    //: @objc private func sendGiftButtonClick() {
    @objc private func packageBy() {
        //: func__sendGift()
        constraintProperty()
    }

    /// 充值按钮事件
    //: @objc private func rechargeBtnClick() {
    @objc private func mountPlatform() {
        //: OriginMediumWill.share.func__pushToHalfWebVC(webViewType: .RechargeHalfPage)
        OriginMediumWill.share.untilAmongForce(webViewType: .RechargeHalfPage)
    }

    /// 点击展示/隐藏通话中的按钮
    //: @objc private func changeInteractionShowState() {
    @objc private func awakeOn() {
        //: let isHidden = !hangupBtn.isHidden
        let isHidden = !hangupBtn.isHidden
        //: minimizeBtn.isHidden = isHidden
        minimizeBtn.isHidden = isHidden
        //: hangupBtn.isHidden = isHidden
        hangupBtn.isHidden = isHidden
        //: micBtn.isHidden = isHidden
        micBtn.isHidden = isHidden
        //: handsfreeBtn.isHidden = isHidden
        handsfreeBtn.isHidden = isHidden
        //: sendGiftBtn.isHidden = isHidden
        sendGiftBtn.isHidden = isHidden
        //: rechargeBtn.isHidden = isHidden
        rechargeBtn.isHidden = isHidden
        //: if isHidden {
        if isHidden {
            //: invalidateIdleTimer()
            would()
            //: } else {
        } else {
            //: touchHiddenTimer()
            detailSparkFoundMe()
        }
    }

    //: func blurImage(_ image: UIImage) -> UIImage {
    func character(_ image: UIImage) -> UIImage {
        //: let context = CIContext(options: nil)
        let context = CIContext(options: nil)
        //: let inputImage = CIImage(image: image)
        let inputImage = CIImage(image: image)
        // 使用高斯模糊滤镜
        //: let filter = CIFilter(name: "CIGaussianBlur")!
        let filter = CIFilter(name: (String(show_duringStr) + String(user_aliveUrl) + String(notiMinuteValue.suffix(5))))!
        //: filter.setValue(inputImage, forKey: kCIInputImageKey)
        filter.setValue(inputImage, forKey: kCIInputImageKey)
        // 设置模糊半径值（越大越模糊）
        //: filter.setValue(5, forKey: kCIInputRadiusKey)
        filter.setValue(5, forKey: kCIInputRadiusKey)
        //: let outputCIImage = filter.outputImage!
        let outputCIImage = filter.outputImage!
        //: let rect = CGRect(origin: CGPoint.zero, size: image.size)
        let rect = CGRect(origin: CGPoint.zero, size: image.size)
        //: let cgImage = context.createCGImage(outputCIImage, from: rect)
        let cgImage = context.createCGImage(outputCIImage, from: rect)
        // 显示生成的模糊图片
        //: let newImage = UIImage(cgImage: cgImage!)
        let newImage = UIImage(cgImage: cgImage!)
        //: return newImage
        return newImage
    }
}

// MARK: - 送礼

//: extension Talking1v1VoiceInteractionView {
extension LocationView {
    //: func func__sendGift() {
    func constraintProperty() {
        //: TalkingChatGiftManager.share.func__sendGiftEvent {
        CommonLog.share.beyondDepth {
            //: self.func__showGiftChoiceView()
            self.noCon()
        }
    }

    //: func func__showGiftChoiceView() {
    func noCon() {
        //: giftView.updateGiftInfo(needReload: true, mf_coin: MarginExamineer.share.loginUserMode.mf_coin)
        giftView.color(needReload: true, mf_coin: MarginExamineer.share.loginUserMode.mf_coin)
        //: window?.addSubview(giftView)
        window?.addSubview(giftView)
        //: giftView.showView()
        giftView.readingConnect()
        //: giftView.sendActionBlock = { [weak self] (_ giftModel: TalkingRoomGiftModel, _ num: String) in
        giftView.sendActionBlock = { [weak self] (_ giftModel: EvaluationMenuRate, _ num: String) in
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: self.func__sendGiftMsg(giftModel: giftModel, giftNum: num)
            self.sumerval(giftModel: giftModel, giftNum: num)
        }
    }

    //: func func_giftViewDismiss() {
    func underLike() {
        //: giftView.dismissView()
        giftView.fadePhone()
    }

    //: func func__sendGiftMsg(giftModel: TalkingRoomGiftModel, giftNum: String) {
    func sumerval(giftModel: EvaluationMenuRate, giftNum: String) {
        //: let reachability = try? Reachability()
        let reachability = try? Reachability()
        //: if reachability?.connection == .unavailable {
        if reachability?.connection == .unavailable {
            //: func__showStatusBarErrorMsg(showMsg: kNetErrorMsg)
            exitMarker(showMsg: kRequestPath)
            //: return
            return
        }
        //: var params: [String: Any] = [:]
        var params: [String: Any] = [:]
        //: if chatModel.iUid == MarginExamineer.share.loginUserMode.userID {
        if chatModel.iUid == MarginExamineer.share.loginUserMode.userID {
            //: params["toUid"] = chatModel.pairUid
            params[(String(main_mistStr))] = chatModel.pairUid
            //: } else {
        } else {
            //: params["toUid"] = chatModel.iUid
            params[(String(main_mistStr))] = chatModel.iUid
        }
        //: params["giftId"] = giftModel.gid
        params[(String(k_reportMsg))] = giftModel.gid
        //: params["giftNum"] = giftNum
        params[(String(userReduceData) + data_mageTitle.replacingOccurrences(of: "go", with: "m"))] = giftNum
        //: params["from"] = "audioCall"
        params[(mainFitFollowingMsg.replacingOccurrences(of: "processing", with: "r"))] = (const_reflectUrl.replacingOccurrences(of: "highlight", with: "d") + String(dataEveryoneKey))
        //: if giftModel.pkgItemsetId != nil {
        if giftModel.pkgItemsetId != nil {
            //: params["pkgItemsetId"] = giftModel.pkgItemsetId
            params[(String(userSpaceScopeSameId.suffix(6)) + "msetId")] = giftModel.pkgItemsetId
        }

        //: TalkingChatRequestTool.req_SendGiftPrivete(param: params, completion: { succeed, result, errorModel in
        PriorMedia.along(param: params, completion: { succeed, result, errorModel in
            //: guard succeed else {
            guard succeed else {
                //: self.dealSendMsgError(errorCode: errorModel!.errorCode, errorStr: errorModel!.errorMsg, isGift: true, isResend: false)
                self.groupSpeed(errorCode: errorModel!.errorCode, errorStr: errorModel!.errorMsg, isGift: true, isResend: false)
                //: return
                return
            }

            // 更新本地礼物背包
            //: if giftModel.pkgItemsetId != nil {
            if giftModel.pkgItemsetId != nil {
                //: self.giftView.bags_removeGiftPackage(pkgItemsetId: giftModel.pkgItemsetId!, num: Int(giftNum) ?? 0)
                self.giftView.strategyBy(pkgItemsetId: giftModel.pkgItemsetId!, num: Int(giftNum) ?? 0)
            }
            //: self.func__insertGiftMessageWithExtral(extral: result as! [String: Any?])
            self.thePic(extral: result as! [String: Any?])
            //: })
        })
    }

    //: func func__insertGiftMessageWithExtral(extral: [String: Any?]) {
    func thePic(extral: [String: Any?]) {
        //: var extralInfo = extral
        var extralInfo = extral
        //: var msgInfoDic: [String: Any] = extralInfo["msgInfo"] as! [String: Any]
        var msgInfoDic: [String: Any] = extralInfo[(String(main_runningUrl) + String(app_timingTitle.suffix(6)))] as! [String: Any]
        //: msgInfoDic["msgType"] = "gift"
        msgInfoDic[(String(user_pageWrittenName.prefix(5)) + String(noti_fitPath))] = (String(userLayerMiddleString))
        //: extralInfo["msgInfo"] = msgInfoDic
        extralInfo[(String(main_runningUrl) + String(app_timingTitle.suffix(6)))] = msgInfoDic

        //: sendGiftByMeToEfffecct(giftDic: extral as [String: Any])
        stackPop(giftDic: extral as [String: Any])
        //: if extralInfo.keys.contains("totalMfCoin") {
        if extralInfo.keys.contains((String(dataDeliveryArableFarTitle.suffix(6)) + "fCoin")) {
            //: let totalMfCoin: NSNumber = extralInfo["totalMfCoin"] as! NSNumber
            let totalMfCoin: NSNumber = extralInfo[(String(dataDeliveryArableFarTitle.suffix(6)) + "fCoin")] as! NSNumber
            //: MarginExamineer.share.loginUserMode.mf_coin = totalMfCoin.stringValue
            MarginExamineer.share.loginUserMode.mf_coin = totalMfCoin.stringValue
        }
        //: giftView.updateGiftInfo(needReload: false, mf_coin: MarginExamineer.share.loginUserMode.mf_coin)
        giftView.color(needReload: false, mf_coin: MarginExamineer.share.loginUserMode.mf_coin)

        //: var toUid = ""
        var toUid = ""
        //: if chatModel.iUid == MarginExamineer.share.loginUserMode.userID {
        if chatModel.iUid == MarginExamineer.share.loginUserMode.userID {
            //: toUid = chatModel.pairUid
            toUid = chatModel.pairUid
            //: } else {
        } else {
            //: toUid = chatModel.iUid
            toUid = chatModel.iUid
        }

        //: let cellData = TUISocialChatManager.insertTXMessage(withExtral: extralInfo as [String: Any], toUid: toUid, isSender: true, isTip: false)
        let cellData = IndependentObserve.belowAlongsideLayer(withExtral: extralInfo as [String: Any], toUid: toUid, isSender: true, isTip: false)
        //: NotificationCenter.default.post(name: CHAT_ADD_CALL_RECORD_MSG_NOTIFICATION, object: self, userInfo: ["cellData": cellData])
        NotificationCenter.default.post(name: app_evenStorageString, object: self, userInfo: [String(bytes: user_fixedData.reversed(), encoding: .utf8)!: cellData])
    }

    //: func dealSendMsgError(errorCode: Int, errorStr: String, isGift: Bool, isResend: Bool) {
    func groupSpeed(errorCode: Int, errorStr: String, isGift: Bool, isResend: Bool) {
        //: if errorCode == 50203 {
        if errorCode == 50203 {
            //: guard MarginExamineer.share.loginUserMode.status != 1 else {
            guard MarginExamineer.share.loginUserMode.status != 1 else {
                //: if !errorStr.isEmptyString {
                if !errorStr.isEmptyString {
                    //: func__showStatusBarErrorMsg(showMsg: errorStr)
                    exitMarker(showMsg: errorStr)
                }
                //: return
                return
            }
            //: OriginMediumWill.share.func__jumpToWebRecharge(clickEvent: clickSendingiftsButton, sufficient: false)
            OriginMediumWill.share.resumeUniversal(clickEvent: notiProgressId, sufficient: false)
            //: giftView.dismissView()
            giftView.fadePhone()
            //: } else if errorCode == 10888 {
        } else if errorCode == 10888 {
            //: let view = TalkingLiveVipSubscribePopView(frame: CGRect(x: 0, y: 0, width: ScreenWidth, height: ScreenHeight))
            let view = ProjectionView(frame: CGRect(x: 0, y: 0, width: show_errorChangeFormat, height: appLoadMessage))
            //: view.show()
            view.someModify()
            //: } else {
        } else {
            //: if !errorStr.isEmptyString {
            if !errorStr.isEmptyString {
                //: func__showStatusBarErrorMsg(showMsg: errorStr)
                exitMarker(showMsg: errorStr)
            }
        }
    }

    //: func initCachEffectData() {
    func stopCurrency() {
        //: var targetId = ""
        var targetId = ""
        //: if chatModel.iUid == MarginExamineer.share.loginUserMode.userID {
        if chatModel.iUid == MarginExamineer.share.loginUserMode.userID {
            //: targetId = chatModel.pairUid
            targetId = chatModel.pairUid
            //: } else {
        } else {
            //: targetId = chatModel.iUid
            targetId = chatModel.iUid
        }
        //: giftEffectView.initCachAnimatData(tagetID: targetId)
        giftEffectView.titleure(tagetID: targetId)
    }

    //: func sendGiftByMeToEfffecct(giftDic: [String: Any]) {
    func stackPop(giftDic: [String: Any]) {
        //: let giftModel = giftEffectView.changeGiftToModel(dict: giftDic) ?? TalkingGiftMsgAnimatModel()
        let giftModel = giftEffectView.sequence(dict: giftDic) ?? FutureMendSumerval()
        //: if giftTrackView.giftSendItem.count == 0 {
        if giftTrackView.giftSendItem.count == 0 {
            //: let arrM = NSMutableArray()
            let arrM = NSMutableArray()
            //: for model in TalkingChatGiftManager.share.getSendingItems() {
            for model in CommonLog.share.light() {
                //: arrM.add(model)
                arrM.add(model)
            }
            //: giftTrackView.giftSendItem = arrM as! [TalkingGiftNumArrModel]
            giftTrackView.giftSendItem = arrM as! [SClip]
        }
        //: giftTrackView.func__didReceiveGiftMsgModel(model: giftModel)
        giftTrackView.appearanceFill(model: giftModel)
        //: giftEffectView.dealGiftAnimatData(dict: giftDic)
        giftEffectView.onIndex(dict: giftDic)
    }

    //: func addGiftEffectModelArr(giftArr: [Any]) {
    func offerIn(giftArr: [Any]) {
        //: if giftTrackView.giftSendItem.count == 0 {
        if giftTrackView.giftSendItem.count == 0 {
            //: let arrM = NSMutableArray()
            let arrM = NSMutableArray()
            //: for model in TalkingChatGiftManager.share.getSendingItems() {
            for model in CommonLog.share.light() {
                //: arrM.add(model)
                arrM.add(model)
            }
            //: giftTrackView.giftSendItem = arrM as! [TalkingGiftNumArrModel]
            giftTrackView.giftSendItem = arrM as! [SClip]
        }
        //: for i in 0 ..< giftArr.count {
        for i in 0 ..< giftArr.count {
            //: let giftModel = giftArr[i]
            let giftModel = giftArr[i]
            //: giftTrackView.func__didReceiveGiftMsgModel(model: giftModel as! TalkingGiftMsgAnimatModel)
            giftTrackView.appearanceFill(model: giftModel as! FutureMendSumerval)
        }
        //: giftEffectView.addGiftAnimatModelArr(modelArr: giftArr)
        giftEffectView.who(modelArr: giftArr)
    }
}

// MARK: - 通话计时器

//: extension Talking1v1VoiceInteractionView {
extension LocationView {
    /// 开始通话计时
    //: private func startTalkCount() {
    private func applicationBy() {
        //: guard countTimer == nil else { return }
        guard countTimer == nil else { return }

        //: if startTime == nil {
        if startTime == nil {
            //: let interval = Date().timeIntervalSince1970
            let interval = Date().timeIntervalSince1970
            //: startTime = interval
            startTime = interval
            //: chatModel.startTime = startTime
            chatModel.startTime = startTime
        }
        //: countTimer = Timer.scheduledTimer(withTimeInterval: 1, block: { [weak self] _ in
        countTimer = Timer.scheduledTimer(withTimeInterval: 1, block: { [weak self] _ in
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: self.changeTalkTime()
            self.behindConstraint()
            //: }, repeats: true)
        }, repeats: true)
    }

    // 展示通话时间
    //: private func changeTalkTime() {
    private func behindConstraint() {
        //: let duration = getCurrentTalkingTime()
        let duration = easy()
        //: let min = floor(duration/60)
        let min = floor(duration / 60)
        //: let sec = floor(duration.truncatingRemainder(dividingBy: 60))
        let sec = floor(duration.truncatingRemainder(dividingBy: 60))
        //: timeLab.text = String(format: "%02i:%02i", Int(min), Int(sec))
        timeLab.text = String(format: "%02i:%02i", Int(min), Int(sec))
        // 更新小窗口通话时间
        //: delegate?.interactionView_shouldUpdateTime(time: duration)
        delegate?.frameworkStar(time: duration)
    }

    /// 恢复通话，更新通话时间
    /// - Parameter startTime: 通话时间
    //: func updateStartTime(startTime: TimeInterval) {
    func borderBit(startTime: TimeInterval) {
        //: self.startTime = startTime
        self.startTime = startTime
        //: changeTalkTime()
        behindConstraint()
    }

    /// 获取当前通话时间
    /// - Returns: 秒
    //: func getCurrentTalkingTime() -> Double {
    func easy() -> Double {
        //: guard chatModel.videoStage == .Calling else { return 0 }
        guard chatModel.videoStage == .Calling else { return 0 }
        //: let interval = Date().timeIntervalSince1970
        let interval = Date().timeIntervalSince1970
        //: let duration = floor(interval-(startTime ?? 0))
        let duration = floor(interval - (startTime ?? 0))
        //: return duration
        return duration
    }
}

// MARK: - 【通话中】触屏隐藏交互按钮逻辑

//: extension Talking1v1VoiceInteractionView {
extension LocationView {
    /// 触摸隐藏开启延时
    //: private func touchHiddenTimer() {
    private func detailSparkFoundMe() {
        //: invalidateIdleTimer()
        would()
        //: perform(#selector(hideInteractionView), with: nil, afterDelay: 10)
        perform(#selector(blueQuick), with: nil, afterDelay: 10)
    }

    /// 销毁延迟
    //: private func invalidateIdleTimer() {
    private func would() {
        //: NSObject.cancelPreviousPerformRequests(withTarget: self, selector: #selector(hideInteractionView), object: nil)
        NSObject.cancelPreviousPerformRequests(withTarget: self, selector: #selector(blueQuick), object: nil)
    }

    /// 延时到时隐藏视图
    //: @objc private func hideInteractionView() {
    @objc private func blueQuick() {
        //: DispatchQueue.main.async {
        DispatchQueue.main.async {
            //: self.changeInteractionShowState()
            self.awakeOn()
        }
    }
}

// MARK: - Layout

//: extension Talking1v1VoiceInteractionView {
extension LocationView {
    /// 添加视图
    //: private func setupSubviews() {
    private func connectionWho() {
        //: addSubview(bgView)
        addSubview(bgView)
        //: addSubview(giftEffectView)
        addSubview(giftEffectView)
        //: insertSubview(giftTrackView, belowSubview: giftEffectView)
        insertSubview(giftTrackView, belowSubview: giftEffectView)
        //: addSubview(touchHidenBtn)
        addSubview(touchHidenBtn)
        //: addSubview(pairPicV)
        addSubview(pairPicV)
        //: addSubview(pairNameLab)
        addSubview(pairNameLab)
        //: addSubview(locationBtn)
        addSubview(locationBtn)
        //: addSubview(minimizeBtn)
        addSubview(minimizeBtn)
        //: addSubview(hangupBtn)
        addSubview(hangupBtn)
        //: addSubview(acceptBtn)
        addSubview(acceptBtn)
        //: addSubview(micBtn)
        addSubview(micBtn)
        //: addSubview(handsfreeBtn)
        addSubview(handsfreeBtn)
        //: addSubview(sendGiftBtn)
        addSubview(sendGiftBtn)
        //: addSubview(rechargeBtn)
        addSubview(rechargeBtn)
        //: addSubview(desLab)
        addSubview(desLab)
        //: addSubview(timeLab)
        addSubview(timeLab)
    }

    /// 添加约束
    //: private func setupSubViewsConstraint() {
    private func ond() {
        //: bgView.snp.makeConstraints { make in
        bgView.snp.makeConstraints { make in
            //: make.edges.equalToSuperview()
            make.edges.equalToSuperview()
        }
        //: giftEffectView.snp.makeConstraints { make in
        giftEffectView.snp.makeConstraints { make in
            //: make.leading.trailing.top.bottom.equalTo(self)
            make.leading.trailing.top.bottom.equalTo(self)
        }
        //: touchHidenBtn.snp.makeConstraints { make in
        touchHidenBtn.snp.makeConstraints { make in
            //: make.edges.equalToSuperview()
            make.edges.equalToSuperview()
        }
        //: pairPicV.snp.makeConstraints { make in
        pairPicV.snp.makeConstraints { make in
            //: make.bottom.equalTo(self.snp.centerY).offset(actualHeight(h: -148))
            make.bottom.equalTo(self.snp.centerY).offset(actualHeight(h: -148))
            //: make.centerX.equalToSuperview()
            make.centerX.equalToSuperview()
            //: make.size.equalTo(CGSize(width: actualWidth(w: 100), height: actualWidth(w: 100)))
            make.size.equalTo(CGSize(width: actualWidth(w: 100), height: actualWidth(w: 100)))
        }
        //: pairNameLab.snp.makeConstraints { make in
        pairNameLab.snp.makeConstraints { make in
            //: make.top.equalTo(pairPicV.snp.bottom).offset(14)
            make.top.equalTo(pairPicV.snp.bottom).offset(14)
            //: make.centerX.equalToSuperview()
            make.centerX.equalToSuperview()
            //: make.width.equalTo(actualWidth(w: 133))
            make.width.equalTo(actualWidth(w: 133))
        }
        //: locationBtn.snp.makeConstraints { make in
        locationBtn.snp.makeConstraints { make in
            //: make.centerX.equalToSuperview()
            make.centerX.equalToSuperview()
            //: make.top.equalTo(pairNameLab.snp.bottom).offset(8)
            make.top.equalTo(pairNameLab.snp.bottom).offset(8)
            //: make.height.equalTo(14)
            make.height.equalTo(14)
        }
        //: giftTrackView.snp.makeConstraints { make in
        giftTrackView.snp.makeConstraints { make in
            //: make.leading.equalTo(self)
            make.leading.equalTo(self)
            //: make.height.equalTo(180)
            make.height.equalTo(180)
            //: make.top.equalTo(pairNameLab.snp.bottom).offset(90)
            make.top.equalTo(pairNameLab.snp.bottom).offset(90)
        }
        //: minimizeBtn.snp.makeConstraints { make in
        minimizeBtn.snp.makeConstraints { make in
            //: make.top.equalTo(StatusBarHeight+10)
            make.top.equalTo(const_musicMessage + 10)
            //: make.leading.equalTo(10)
            make.leading.equalTo(10)
            //: make.width.height.equalTo(actualWidth(w: 42))
            make.width.height.equalTo(actualWidth(w: 42))
        }
        //: hangupBtn.snp.makeConstraints { make in
        hangupBtn.snp.makeConstraints { make in
            //: make.centerX.equalToSuperview()
            make.centerX.equalToSuperview()
            //: make.bottom.equalTo(-kDeviceSafeBottomHeight-161)
            make.bottom.equalTo(-const_closedAddSegmentPath - 161)
            //: make.size.equalTo(actualWidth(w: 70))
            make.size.equalTo(actualWidth(w: 70))
        }
        //: acceptBtn.snp.makeConstraints { make in
        acceptBtn.snp.makeConstraints { make in
            //: make.edges.equalTo(hangupBtn)
            make.edges.equalTo(hangupBtn)
        }
        //: let letftPlace = (ScreenWidth-actualWidth(w: 45)*4-actualWidth(w: 25)*3)/2
        let letftPlace = (show_errorChangeFormat - actualWidth(w: 45) * 4 - actualWidth(w: 25) * 3) / 2
        //: micBtn.snp.makeConstraints { make in
        micBtn.snp.makeConstraints { make in
            //: make.leading.equalTo(letftPlace)
            make.leading.equalTo(letftPlace)
            //: make.bottom.equalTo(-kDeviceSafeBottomHeight-16)
            make.bottom.equalTo(-const_closedAddSegmentPath - 16)
            //: make.size.equalTo(actualWidth(w: 45))
            make.size.equalTo(actualWidth(w: 45))
        }
        //: handsfreeBtn.snp.makeConstraints { make in
        handsfreeBtn.snp.makeConstraints { make in
            //: make.leading.equalTo(micBtn.snp.trailing).offset(actualWidth(w: 25))
            make.leading.equalTo(micBtn.snp.trailing).offset(actualWidth(w: 25))
            //: make.bottom.size.equalTo(micBtn)
            make.bottom.size.equalTo(micBtn)
        }
        //: rechargeBtn.snp.makeConstraints { make in
        rechargeBtn.snp.makeConstraints { make in
            //: make.leading.equalTo(handsfreeBtn.snp.trailing).offset(actualWidth(w: 25))
            make.leading.equalTo(handsfreeBtn.snp.trailing).offset(actualWidth(w: 25))
            //: make.bottom.size.equalTo(micBtn)
            make.bottom.size.equalTo(micBtn)
        }
        //: sendGiftBtn.snp.makeConstraints { make in
        sendGiftBtn.snp.makeConstraints { make in
            //: make.leading.equalTo(rechargeBtn.snp.trailing).offset(actualWidth(w: 25))
            make.leading.equalTo(rechargeBtn.snp.trailing).offset(actualWidth(w: 25))
            //: make.bottom.size.equalTo(micBtn)
            make.bottom.size.equalTo(micBtn)
        }
        //: desLab.snp.makeConstraints { make in
        desLab.snp.makeConstraints { make in
            //: make.top.equalTo(locationBtn.snp.bottom).offset(10)
            make.top.equalTo(locationBtn.snp.bottom).offset(10)
            //: make.leading.equalTo(54)
            make.leading.equalTo(54)
            //: make.trailing.equalTo(-54)
            make.trailing.equalTo(-54)
        }
        //: timeLab.snp.makeConstraints { make in
        timeLab.snp.makeConstraints { make in
            //: make.centerX.equalToSuperview()
            make.centerX.equalToSuperview()
            //: make.top.equalTo(StatusBarHeight+16)
            make.top.equalTo(const_musicMessage + 16)
        }
    }

    /// 更新约束
    //: private func updateLayout() {
    private func sizeBeauty() {
        //: if chatModel.videoStage == .Recieved {
        if chatModel.videoStage == .Recieved {
            //: hangupBtn.snp.remakeConstraints { make in
            hangupBtn.snp.remakeConstraints { make in
                //: make.leading.equalTo(ScreenWidth/2-42-70)
                make.leading.equalTo(show_errorChangeFormat / 2 - 42 - 70)
                //: make.bottom.equalTo(-kDeviceSafeBottomHeight-161)
                make.bottom.equalTo(-const_closedAddSegmentPath - 161)
                //: make.size.equalTo(70)
                make.size.equalTo(70)
            }
            //: acceptBtn.snp.remakeConstraints { make in
            acceptBtn.snp.remakeConstraints { make in
                //: make.leading.equalTo(ScreenWidth/2+42)
                make.leading.equalTo(show_errorChangeFormat / 2 + 42)
                //: make.bottom.equalTo(hangupBtn)
                make.bottom.equalTo(hangupBtn)
                //: make.size.equalTo(hangupBtn)
                make.size.equalTo(hangupBtn)
            }
            //: } else {
        } else {
            //: hangupBtn.snp.remakeConstraints { make in
            hangupBtn.snp.remakeConstraints { make in
                //: make.centerX.equalToSuperview()
                make.centerX.equalToSuperview()
                //: make.bottom.equalTo(-kDeviceSafeBottomHeight-161)
                make.bottom.equalTo(-const_closedAddSegmentPath - 161)
                //: make.size.equalTo(actualWidth(w: 70))
                make.size.equalTo(actualWidth(w: 70))
            }
        }
    }
}
