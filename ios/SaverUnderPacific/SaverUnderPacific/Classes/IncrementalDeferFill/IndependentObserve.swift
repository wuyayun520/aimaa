
//: Declare String Begin

/*: "msgType" :*/
fileprivate let constFilterUrl:String = "available formalmsgType"

/*: "tips" :*/
fileprivate let user_distanceStr:String = "tipinmate"

/*: "extra" :*/
fileprivate let showDonKnockStr:[Character] = ["e","x","t","r","a"]

/*: "msgInfo" :*/
fileprivate let const_purchaseHealthyData:[Character] = ["m","s","g","I","n","f","o"]

/*: "user" :*/
fileprivate let k_mixId:String = "usecrush"

/*: "uid" :*/
fileprivate let user_systemWeekFormat:String = "UID"

/*: "nickname" :*/
fileprivate let main_makerConPath:[UInt8] = [0x20,0x1b,0x15,0x1d,0x20,0x13,0x1f,0x17]

fileprivate func effCritical(detect num: UInt8) -> UInt8 {
    let value = Int(num) - 178
    if value < 0 {
        return UInt8(value + 256)
    } else {
        return UInt8(value)
    }
}

/*: "headPic" :*/
fileprivate let main_numberScanMsg:[UInt8] = [0x63,0x69,0x50,0x64,0x61,0x65,0x68]

/*: "age" :*/
fileprivate let user_policyData:[UInt8] = [0x1,0x7,0x5]

private func himFade(progress num: UInt8) -> UInt8 {
    return num ^ 96
}

/*: "gift" :*/
fileprivate let user_paintMsg:String = "farift"

/*: "isGroup" :*/
fileprivate let data_scanMsg:String = "recover"
fileprivate let show_platFailThroughData:[Character] = ["s","G","r","o","u","p"]

/*: "WantGift" :*/
fileprivate let main_mixConnectString:String = "WantGifinterest framework dad"
fileprivate let show_seaNameName:String = "three"

/*: "txt" :*/
fileprivate let userPolicyStr:String = "regionxregion"

/*: "img" :*/
fileprivate let constBeautySnapKey:String = "ilawg"

/*: "video" :*/
fileprivate let constDeviceCountTitle:String = "viconcreteeo"

/*: "audio" :*/
fileprivate let constWeekId:[Character] = ["a","u","d","i","o"]

/*: "messageType" :*/
fileprivate let appTransformKey:[Character] = ["m","e","s","s"]
fileprivate let kDetailData:String = "ageTypethrough judge once construct"

/*: "contentType" :*/
fileprivate let k_streakPath:String = "CONT"
fileprivate let noti_calculateStr:[Character] = ["p","e"]

/*: "ImgMsg" :*/
fileprivate let userSignatureMsg:String = "despite deadline fireImgMsg"

/*: "imageUri" :*/
fileprivate let const_methodUrl:[Character] = ["i","m","a"]
fileprivate let show_snapTitle:[Character] = ["g","e","U","r","i"]

/*: "type" :*/
fileprivate let noti_weeklyFormat:[Character] = ["t","y","p","e"]

/*: "pic" :*/
fileprivate let app_projectionStr:String = "pichoose"

/*: "isBurn" :*/
fileprivate let showAirTitle:String = "difficult statusisBurn"

/*: "giftId" :*/
fileprivate let k_relativeWritingKey:[Character] = ["g","i","f","t","I","d"]

/*: "params" :*/
fileprivate let userMenuValue:String = "papleaseams"

/*: "audioLength" :*/
fileprivate let user_poseLowString:String = "aquakedi"

/*: "len" :*/
fileprivate let constMirrorEvaluationChestMessage:[Character] = ["l","e","n"]

/*: "audioSandbox" :*/
fileprivate let noti_clickZoneId:[Character] = ["a","u","d","i","o","S","a","n"]
fileprivate let appChartFormat:String = "DBOX"

/*: "senderId" :*/
fileprivate let user_purpleNobodyUrl:String = "sstandardd"

/*: "AudioMsg" :*/
fileprivate let showMentionSmallString:String = "Audcurrently ex put infrastructure catch"
fileprivate let const_consumeMessage:String = "ioMsgorientation warning item deem broken"

/*: "content" :*/
fileprivate let showSunString:String = "cogenerateegenerate"

/*: "TxtMsg" :*/
fileprivate let showSufficientValue:[Character] = ["T","x","t","M","s","g"]

/*: "message" :*/
fileprivate let mainFeedPath:[Character] = ["m","e","s","s","a","g","e"]

/*: "quoteMsgId" :*/
fileprivate let constAbsSuccessfullyKey:String = "QUOTE"

/*: "quoteMsgInfo" :*/
fileprivate let user_carrierUrl:[Character] = ["q","u","o","t","e","M","s","g","I","n","f","o"]

/*: "replayId" :*/
fileprivate let main_corePoseId:[Character] = ["r","e","p","l","a","y","I"]
fileprivate let kSceneData:String = "stop"

/*: "rYMsgType" :*/
fileprivate let constSomethingTitle:String = "rYMsgaccessible busy"
fileprivate let userPolicyRevenueData:[Character] = ["T","y","p","e"]

/*: "GJ:Sess:AutoGreet:TxtCustom" :*/
fileprivate let user_manualFrameworkName:[Character] = ["G","J",":","S","e","s","s",":","A","u","t","o","G","r","e","e","t",":","T","x","t","C","u","s","t","o"]
fileprivate let notiRawString:String = "same"

/*: "changeDirection" :*/
fileprivate let main_whipFormat:String = "CHAN"
fileprivate let noti_flowPadUrl:String = "ecsquareon"

/*: "system" :*/
fileprivate let const_removeSubmitPath:String = "aboveyabovetem"

/*: "title" :*/
fileprivate let userHavePath:String = "securitytle"

/*: "icon" :*/
fileprivate let constReadyKey:String = "ICON"

/*: "all" :*/
fileprivate let main_statData:String = "acover"

/*: "icon_chats_pcr" :*/
fileprivate let appOkId:[Character] = ["i","c","o","n","_","c"]
fileprivate let k_roleId:[Character] = ["h","a","t","s","_","p","c","r"]

/*: "status" :*/
fileprivate let k_wireId:String = "stretus"

/*: "msgIncome" :*/
fileprivate let notiWarnMsg:[Character] = ["m","s","g","I","n","c","o","m","e"]

/*: "msgId" :*/
fileprivate let notiNoticeFrontUrl:String = "reveal schedule unction mobilemsgId"

/*: "TrackActiveChannel" :*/
fileprivate let appLeaderShrimpPath:String = "TMsgsequence gravity install make bath"
fileprivate let appWeValue:String = "ateStbound sun crucial threshold"

/*: "TMsgTranslatedContent" :*/
fileprivate let dataYieldPresentationKey:[Character] = ["T","M","s","g","T","r","a","n","s","l","a","t","e","d","C"]
fileprivate let kSampleStr:String = "onnorennor"

/*: "formatType" :*/
fileprivate let app_careFormat:String = "forscreening"
fileprivate let user_curRankTitle:[Character] = ["e"]

/*: "plain" :*/
fileprivate let noti_mendBlindMessage:[Character] = ["p","l","a","i","n"]

/*: "imageData" :*/
fileprivate let k_intensityId:String = "delivery union maintain movement oneimageDa"
fileprivate let mainCrucialName:String = "script"

/*: "VideoMsg" :*/
fileprivate let main_deployKey:String = "Videotension install ex club subject"
fileprivate let show_thePath:String = "Msgcluster safe"

/*: "match" :*/
fileprivate let dataTabSignificantFormat:String = "MATCH"

/*: "userIntro" :*/
fileprivate let const_drawerInstallStr:[Character] = ["u"]
fileprivate let notiImageString:[Character] = ["s","e","r","I","n","t","r","o"]

/*: "targetId" :*/
fileprivate let noti_policyId:String = "tbar"
fileprivate let main_interestedPath:String = "rgetIdunderly appeal search"

/*: "yyyy-MM-dd HH:mm:ss" :*/
fileprivate let userLastStr:String = "eh distributionyyyy-M"
fileprivate let noti_indexName:String = "HH:mm:ssthread pressure"

/*: "sendTime" :*/
fileprivate let dataBetweenId:String = "smust"
fileprivate let mainAlreadyName:String = "automobile ret shallndTime"

/*: "isRead" :*/
fileprivate let notiTranslateFuturePath:String = "isRearesponse you problem"
fileprivate let noti_containerPath:String = "D"

/*: "audioData" :*/
fileprivate let appDeliveryIncomeName:String = "auehi"
fileprivate let noti_sceneUrl:String = "oDataimage tension ache"

/*: "audioUri" :*/
fileprivate let constLackValue:[Character] = ["a","u","d","i","o","U"]
fileprivate let mainMemoryTitle:[Character] = ["r","i"]

//: Declare String End

// __DEBUG__
// __CLOSE_PRINT__
//
//  IndependentObserve.swift

//: import Foundation
import Foundation

// MARK: - 枚举定义

//: @objc public enum TXMessageContentType: UInt {
@objc public enum MaximumRange: UInt {
    //: case TXMessageContentType_text = 1
    case TXMessageContentType_text = 1
    //: case TXMessageContentType_picture
    case TXMessageContentType_picture
    //: case TXMessageContentType_gift
    case TXMessageContentType_gift
    //: case TXMessageContentType_askforGift
    case TXMessageContentType_askforGift
    //: case TXMessageContentType_voice
    case TXMessageContentType_voice
    //: case TXMessageContentType_local
    case TXMessageContentType_local
    //: case TXMessageContentType_tips
    case TXMessageContentType_tips
    //: case TXMessageContentType_userCard
    case TXMessageContentType_userCard
    //: case TXMessageContentType_video
    case TXMessageContentType_video
}

// MARK: - IndependentObserve

//: @objcMembers public class TUISocialChatManager: NSObject {
@objcMembers public class IndependentObserve: NSObject {
    // MARK: - 公共方法

    /// 获取消息插入的时间戳 毫秒
    //: public class func getMessageInsertTime() -> Int64 {
    public class func each() -> Int64 {
        //: let curDate = Date().timeIntervalSince1970
        let curDate = Date().timeIntervalSince1970
        //: let msInterval = Int64(floor(curDate * 1000))
        let msInterval = Int64(floor(curDate * 1000))
        //: var deltaTime = Int64(V2TIMManager.sharedInstance().getServerTime())
        var deltaTime = Int64(V2TIMManager.sharedInstance().getServerTime()) // 秒为单位
        //: if deltaTime < 1 {
        if deltaTime < 1 {
            //: deltaTime = 0
            deltaTime = 0
        }
        //: let timeStamp = msInterval - deltaTime
        let timeStamp = msInterval - deltaTime
        //: return timeStamp
        return timeStamp
    }

    // MARK: - 腾讯IM消息处理

    /// 腾讯IM消息插入
    //: public class func insertTXMessage(withExtral extral: [String: Any], toUid: String, isSender: Bool, isTip: Bool) -> AlongCrucial? {
    public class func belowAlongsideLayer(withExtral extral: [String: Any], toUid: String, isSender: Bool, isTip: Bool) -> AlongCrucial? {
        //: let data = handleTXMessageSend(withExtral: extral, isSender: isSender, isTip: isTip)
        let data = on(withExtral: extral, isSender: isSender, isTip: isTip)
        //: if let data = data {
        if let data = data {
            //: EditChannel.shared.func__handleMsg(targetId: toUid, msgCellData: data, isPrivete: true)
            EditChannel.shared.all(targetId: toUid, msgCellData: data, isPrivete: true)
        }
        //: return data
        return data
    }

    /// 腾讯IM发送消息组装
    //: public class func handleTXMessageSend(withExtral extral: [String: Any], isSender: Bool, isTip: Bool) -> AlongCrucial? {
    public class func on(withExtral extral: [String: Any], isSender: Bool, isTip: Bool) -> AlongCrucial? {
        //: if isTip {
        if isTip {
            //: var extralDict = extral
            var extralDict = extral
            //: extralDict["msgType"] = "tips"
            extralDict[(String(constFilterUrl.suffix(7)))] = (user_distanceStr.replacingOccurrences(of: "inmate", with: "s"))
            //: let extraDict = ["extra": extralDict]
            let extraDict = [(String(showDonKnockStr)): extralDict]
            //: let extra = parseTXMessageJsonStr(data: extraDict)
            let extra = takeOfTiming(data: extraDict)
            //: let data = TalkingChatTipsMsgCellData(direction: isSender ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
            let data = ToAdjust(direction: isSender ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
            //: let cData = extra.data(using: .utf8)
            let cData = extra.data(using: .utf8)
            //: data.innerMessage = V2TIMManager.sharedInstance().createCustomMessage(cData)
            data.innerMessage = V2TIMManager.sharedInstance().createCustomMessage(cData)
            //: data.innerMessage.isExcludedFromLastMessage = true
            data.innerMessage.isExcludedFromLastMessage = true
            //: let model = AbTalkingChatMsgBaseModel()
            let model = EnhanceModel()
            //: if let tipModel = try? AbTalkingChatMsgTipsModel(dictionary: extralDict) {
            if let tipModel = try? SumModel(dictionary: extralDict) {
                //: model.tips = tipModel
                model.tips = tipModel
            }
            //: data.msgModel = model
            data.msgModel = model
            //: return data
            return data
        }

        //: var tempDict = extral
        var tempDict = extral
        //: guard let msgInfo = extral["msgInfo"] as? [String: Any] else {
        guard let msgInfo = extral[(String(const_purchaseHealthyData))] as? [String: Any] else {
            //: return nil
            return nil
        }

        //: tempDict["user"] = [
        tempDict[(k_mixId.replacingOccurrences(of: "crush", with: "r"))] = [
            //: "uid": MarginExamineer.share.loginUserMode.userID,
            (user_systemWeekFormat.lowercased()): MarginExamineer.share.loginUserMode.userID,
            //: "nickname": MarginExamineer.share.loginUserMode.nickname,
            String(bytes: main_makerConPath.map{effCritical(detect: $0)}, encoding: .utf8)!: MarginExamineer.share.loginUserMode.nickname,
            //: "headPic": MarginExamineer.share.loginUserMode.headPic,
            String(bytes: main_numberScanMsg.reversed(), encoding: .utf8)!: MarginExamineer.share.loginUserMode.headPic,
            //: "age": MarginExamineer.share.loginUserMode.age,
            String(bytes: user_policyData.map{himFade(progress: $0)}, encoding: .utf8)!: MarginExamineer.share.loginUserMode.age,
        ]

        //: let extraDict = ["extra": tempDict]
        let extraDict = [(String(showDonKnockStr)): tempDict]
        //: let extraString = parseTXMessageJsonStr(data: extraDict)
        let extraString = takeOfTiming(data: extraDict)

        // 礼物
        //: if msgInfo["msgType"] as? String == "gift" {
        if msgInfo[(String(constFilterUrl.suffix(7)))] as? String == (user_paintMsg.replacingOccurrences(of: "far", with: "g")) {
            //: var data: AlongCrucial
            var data: AlongCrucial
            //: if msgInfo["isGroup"] as? String == "1" {  // 群聊
            if msgInfo[(data_scanMsg.replacingOccurrences(of: "recover", with: "i") + String(show_platFailThroughData))] as? String == "1" { // 群聊
                //: data = TalkingGroupChatGiftMsgCellData(direction: isSender ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
                data = MultiLastSine(direction: isSender ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
                //: } else {
            } else {
                //: data = TalkingChatGiftMsgCellData(direction: isSender ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
                data = ContextLength(direction: isSender ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
            }

            //: let cData = extraString.data(using: .utf8)
            let cData = extraString.data(using: .utf8)
            //: data.innerMessage = V2TIMManager.sharedInstance().createCustomMessage(cData)
            data.innerMessage = V2TIMManager.sharedInstance().createCustomMessage(cData)
            //: if let model = try? AbTalkingChatMsgBaseModel(dictionary: tempDict) {
            if let model = try? EnhanceModel(dictionary: tempDict) {
                //: data.msgModel = model
                data.msgModel = model
            }
            //: return data
            return data
        }

        // 索要礼物
        //: if msgInfo["msgType"] as? String == "WantGift" {
        if msgInfo[(String(constFilterUrl.suffix(7)))] as? String == (String(main_mixConnectString.prefix(7)) + show_seaNameName.replacingOccurrences(of: "three", with: "t")) {
            //: let data = LightSaveLabel(direction: isSender ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
            let data = LightSaveLabel(direction: isSender ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
            //: let cData = extraString.data(using: .utf8)
            let cData = extraString.data(using: .utf8)
            //: data.innerMessage = V2TIMManager.sharedInstance().createCustomMessage(cData)
            data.innerMessage = V2TIMManager.sharedInstance().createCustomMessage(cData)
            //: if let model = try? AbTalkingChatMsgBaseModel(dictionary: tempDict) {
            if let model = try? EnhanceModel(dictionary: tempDict) {
                //: data.msgModel = model
                data.msgModel = model
            }
            //: return data
            return data
        }

        // 文字
        //: if msgInfo["msgType"] as? String == "txt" {
        if msgInfo[(String(constFilterUrl.suffix(7)))] as? String == (userPolicyStr.replacingOccurrences(of: "region", with: "t")) {
            //: var data: AlongCrucial
            var data: AlongCrucial
            //: if msgInfo["isGroup"] as? String == "1" {  // 群聊
            if msgInfo[(data_scanMsg.replacingOccurrences(of: "recover", with: "i") + String(show_platFailThroughData))] as? String == "1" { // 群聊
                //: data = TalkingGroupChatTextMsgCellData(direction: isSender ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
                data = GlobalDuring(direction: isSender ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
                //: } else {
            } else {
                //: data = ExceptionKindLocationCreate(direction: isSender ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
                data = ExceptionKindLocationCreate(direction: isSender ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
            }
            //: let cData = extraString.data(using: .utf8)
            let cData = extraString.data(using: .utf8)
            //: data.innerMessage = V2TIMManager.sharedInstance().createCustomMessage(cData)
            data.innerMessage = V2TIMManager.sharedInstance().createCustomMessage(cData)
            //: if let model = try? AbTalkingChatMsgBaseModel(dictionary: tempDict) {
            if let model = try? EnhanceModel(dictionary: tempDict) {
                //: data.msgModel = model
                data.msgModel = model
            }
            //: return data
            return data
        }

        // 图片
        //: if msgInfo["msgType"] as? String == "img" {
        if msgInfo[(String(constFilterUrl.suffix(7)))] as? String == (constBeautySnapKey.replacingOccurrences(of: "law", with: "m")) {
            //: let data = TalkingChatImageMsgCellData(direction: isSender ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
            let data = DelayComposition(direction: isSender ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
            //: let cData = extraString.data(using: .utf8)
            let cData = extraString.data(using: .utf8)
            //: data.innerMessage = V2TIMManager.sharedInstance().createCustomMessage(cData)
            data.innerMessage = V2TIMManager.sharedInstance().createCustomMessage(cData)
            //: if let model = try? AbTalkingChatMsgBaseModel(dictionary: tempDict) {
            if let model = try? EnhanceModel(dictionary: tempDict) {
                //: data.msgModel = model
                data.msgModel = model
            }
            //: return data
            return data
        }

        // 视频
        //: if msgInfo["msgType"] as? String == "video" {
        if msgInfo[(String(constFilterUrl.suffix(7)))] as? String == (constDeviceCountTitle.replacingOccurrences(of: "concrete", with: "d")) {
            //: let data = TalkingChatVideoMsgCellData(direction: isSender ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
            let data = MeasurePaint(direction: isSender ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
            //: let cData = extraString.data(using: .utf8)
            let cData = extraString.data(using: .utf8)
            //: data.innerMessage = V2TIMManager.sharedInstance().createCustomMessage(cData)
            data.innerMessage = V2TIMManager.sharedInstance().createCustomMessage(cData)
            //: if let model = try? AbTalkingChatMsgBaseModel(dictionary: tempDict) {
            if let model = try? EnhanceModel(dictionary: tempDict) {
                //: data.msgModel = model
                data.msgModel = model
            }
            //: return data
            return data
        }

        // 语音
        //: if msgInfo["msgType"] as? String == "audio" {
        if msgInfo[(String(constFilterUrl.suffix(7)))] as? String == (String(constWeekId)) {
            //: let data = BoxStack(direction: isSender ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
            let data = BoxStack(direction: isSender ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
            //: let cData = extraString.data(using: .utf8)
            let cData = extraString.data(using: .utf8)
            //: data.innerMessage = V2TIMManager.sharedInstance().createCustomMessage(cData)
            data.innerMessage = V2TIMManager.sharedInstance().createCustomMessage(cData)
            //: if let model = try? AbTalkingChatMsgBaseModel(dictionary: tempDict) {
            if let model = try? EnhanceModel(dictionary: tempDict) {
                //: data.msgModel = model
                data.msgModel = model
            }
            //: return data
            return data
        }

        // 打招呼
        //: if extral["gift"] != nil && (msgInfo["messageType"] as? Int) == 4 {
        if extral[(user_paintMsg.replacingOccurrences(of: "far", with: "g"))] != nil && (msgInfo[(String(appTransformKey) + String(kDetailData.prefix(7)))] as? Int) == 4 {
            //: let data = ExceptionKindLocationCreate(direction: isSender ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
            let data = ExceptionKindLocationCreate(direction: isSender ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
            //: let cData = extraString.data(using: .utf8)
            let cData = extraString.data(using: .utf8)
            //: data.innerMessage = V2TIMManager.sharedInstance().createCustomMessage(cData)
            data.innerMessage = V2TIMManager.sharedInstance().createCustomMessage(cData)
            //: if let model = try? AbTalkingChatMsgBaseModel(dictionary: tempDict) {
            if let model = try? EnhanceModel(dictionary: tempDict) {
                //: data.msgModel = model
                data.msgModel = model
            }
            //: return data
            return data
        }

        //: return nil
        return nil
    }

    /// 腾讯IM重发消息组装 （只有私聊在用）
    //: public class func handleTXMessageReSend(withData cellData: AlongCrucial) -> AlongCrucial? {
    public class func despiteCur(withData cellData: AlongCrucial) -> AlongCrucial? {
        // 图片
        //: if let reCellData = cellData as? TalkingChatImageMsgCellData {
        if let reCellData = cellData as? DelayComposition {
            //: let paths = NSSearchPathForDirectoriesInDomains(.cachesDirectory, .userDomainMask, true)
            let paths = NSSearchPathForDirectoriesInDomains(.cachesDirectory, .userDomainMask, true)
            //: var filePath = paths.first!
            var filePath = paths.first!
            //: filePath = (filePath as NSString).appendingPathComponent(MovementStructure.getImageFileName())
            filePath = (filePath as NSString).appendingPathComponent(MovementStructure.underly())
            //: filePath = (filePath as NSString).appendingPathComponent((reCellData.imageUrl! as NSString).lastPathComponent)
            filePath = (filePath as NSString).appendingPathComponent((reCellData.imageUrl! as NSString).lastPathComponent)

            //: if !FileManager.default.fileExists(atPath: filePath) {
            if !FileManager.default.fileExists(atPath: filePath) {
                //: return nil
                return nil
            }

            //: var dic: [String: Any] = [:]
            var dic: [String: Any] = [:]
            //: var params: [String: Any] = [:]
            var params: [String: Any] = [:]
            //: var msgInfo: [String: Any] = [:]
            var msgInfo: [String: Any] = [:]
            //: msgInfo["contentType"] = "ImgMsg"
            msgInfo[(k_streakPath.lowercased() + "entTy" + String(noti_calculateStr))] = (String(userSignatureMsg.suffix(6)))
            //: msgInfo["msgType"] = "img"
            msgInfo[(String(constFilterUrl.suffix(7)))] = (constBeautySnapKey.replacingOccurrences(of: "law", with: "m"))
            //: msgInfo["imageUri"] = filePath
            msgInfo[(String(const_methodUrl) + String(show_snapTitle))] = filePath
            //: dic["msgInfo"] = msgInfo
            dic[(String(const_purchaseHealthyData))] = msgInfo

            //: params["type"] = "ImgMsg"
            params[(String(noti_weeklyFormat))] = (String(userSignatureMsg.suffix(6)))
            //: params["pic"] = filePath
            params[(app_projectionStr.replacingOccurrences(of: "choose", with: "c"))] = filePath

            // 重发私密照片
            //: let isBurn = reCellData.msgModel.msgInfo.customSend.isBurn
            let isBurn = reCellData.msgModel.msgInfo.customSend.isBurn
            //: if !isBurn.isEmpty {
            if !isBurn.isEmpty {
                //: params["isBurn"] = isBurn
                params[(String(showAirTitle.suffix(6)))] = isBurn
                //: } else {
            } else {
                //: params["isBurn"] = "0"
                params[(String(showAirTitle.suffix(6)))] = "0"
            }

            //: let giftId = reCellData.msgModel.msgInfo.customSend.giftId
            let giftId = reCellData.msgModel.msgInfo.customSend.giftId
            //: if !giftId.isEmpty {
            if !giftId.isEmpty {
                //: params["giftId"] = giftId
                params[(String(k_relativeWritingKey))] = giftId
                //: } else {
            } else {
                //: params["giftId"] = "0"
                params[(String(k_relativeWritingKey))] = "0"
            }

            //: dic["params"] = params
            dic[(userMenuValue.replacingOccurrences(of: "please", with: "r"))] = params

            //: let data = handleTXMessageSend(withExtral: dic, isSender: true, isTip: false)
            let data = on(withExtral: dic, isSender: true, isTip: false)
            //: data?.status = .Msg_Status_Fail
            data?.status = .Msg_Status_Fail
            //: return data
            return data
        }

        // 视频
        //: if let reCellData = cellData as? TalkingChatVideoMsgCellData {
        if let reCellData = cellData as? MeasurePaint {
            //: reCellData.status = .Msg_Status_Fail
            reCellData.status = .Msg_Status_Fail
            //: return reCellData
            return reCellData
        }

        // 语音
        //: if let reCellData = cellData as? BoxStack {
        if let reCellData = cellData as? BoxStack {
            //: let voiceModel = DBUserInfoManager.cache_getAudioDB(message: reCellData.innerMessage)
            let voiceModel = ProgressAssociated.triggerReady(message: reCellData.innerMessage)
            //: var dic: [String: Any] = [:]
            var dic: [String: Any] = [:]
            //: var msgInfo: [String: Any] = [:]
            var msgInfo: [String: Any] = [:]
            //: var params: [String: Any] = [:]
            var params: [String: Any] = [:]

            //: if !voiceModel.db_audioLength.isEmpty {
            if !voiceModel.db_audioLength.isEmpty {
                //: msgInfo["audioLength"] = voiceModel.db_audioLength
                msgInfo[(user_poseLowString.replacingOccurrences(of: "quake", with: "u") + "oLength")] = voiceModel.db_audioLength
                //: params["len"] = voiceModel.db_audioLength
                params[(String(constMirrorEvaluationChestMessage))] = voiceModel.db_audioLength
            }
            //: if !voiceModel.db_filePath.isEmpty {
            if !voiceModel.db_filePath.isEmpty {
                //: msgInfo["audioSandbox"] = voiceModel.db_filePath
                msgInfo[(String(noti_clickZoneId) + appChartFormat.lowercased())] = voiceModel.db_filePath
                //: params["audioSandbox"] = voiceModel.db_filePath
                params[(String(noti_clickZoneId) + appChartFormat.lowercased())] = voiceModel.db_filePath
            }
            //: msgInfo["msgType"] = "audio"
            msgInfo[(String(constFilterUrl.suffix(7)))] = (String(constWeekId))
            //: msgInfo["senderId"] = MarginExamineer.share.loginUserMode.userID
            msgInfo[(user_purpleNobodyUrl.replacingOccurrences(of: "standard", with: "en") + "erId")] = MarginExamineer.share.loginUserMode.userID
            //: dic["msgInfo"] = msgInfo
            dic[(String(const_purchaseHealthyData))] = msgInfo

            //: params["type"] = "AudioMsg"
            params[(String(noti_weeklyFormat))] = (String(showMentionSmallString.prefix(3)) + String(const_consumeMessage.prefix(5)))
            //: dic["params"] = params
            dic[(userMenuValue.replacingOccurrences(of: "please", with: "r"))] = params

            //: let data = handleTXMessageSend(withExtral: dic, isSender: true, isTip: false)
            let data = on(withExtral: dic, isSender: true, isTip: false)
            //: data?.status = .Msg_Status_Fail
            data?.status = .Msg_Status_Fail
            //: return data
            return data
        }

        // 文本
        //: if let reCellData = cellData as? ExceptionKindLocationCreate {
        if let reCellData = cellData as? ExceptionKindLocationCreate {
            //: var dic: [String: Any] = [:]
            var dic: [String: Any] = [:]
            //: var params: [String: Any] = [:]
            var params: [String: Any] = [:]
            //: var msgInfo: [String: Any] = [:]
            var msgInfo: [String: Any] = [:]

            //: msgInfo["content"] = reCellData.contentStr ?? ""
            msgInfo[(showSunString.replacingOccurrences(of: "generate", with: "nt"))] = reCellData.contentStr ?? ""
            //: msgInfo["msgType"] = "txt"
            msgInfo[(String(constFilterUrl.suffix(7)))] = (userPolicyStr.replacingOccurrences(of: "region", with: "t"))
            //: dic["msgInfo"] = msgInfo
            dic[(String(const_purchaseHealthyData))] = msgInfo
            //: params["type"] = "TxtMsg"
            params[(String(noti_weeklyFormat))] = (String(showSufficientValue))
            //: params["message"] = reCellData.contentStr ?? ""
            params[(String(mainFeedPath))] = reCellData.contentStr ?? ""

            //: if reCellData.msgModel.quoteMsgInfo.isQuoteMsg() == true {
            if reCellData.msgModel.quoteMsgInfo.blue() == true { // 引用消息重发
                //: params["quoteMsgId"] = reCellData.msgModel.quoteMsgInfo.msgId
                params[(constAbsSuccessfullyKey.lowercased() + "MsgId")] = reCellData.msgModel.quoteMsgInfo.msgId
                //: dic["quoteMsgInfo"] = reCellData.msgModel.quoteMsgInfo
                dic[(String(user_carrierUrl))] = reCellData.msgModel.quoteMsgInfo
                    //: .toDictionary()
                    .toDictionary()
            }

            //: if let replayId = reCellData.msgModel.params["replayId"] { // 问题回答消息重发
            if let replayId = reCellData.msgModel.params[(String(main_corePoseId) + kSceneData.replacingOccurrences(of: "stop", with: "d"))] { // 问题回答消息重发
                //: params["replayId"] = replayId
                params[(String(main_corePoseId) + kSceneData.replacingOccurrences(of: "stop", with: "d"))] = replayId
            }

            //: dic["params"] = params
            dic[(userMenuValue.replacingOccurrences(of: "please", with: "r"))] = params

            //: let data = handleTXMessageSend(withExtral: dic, isSender: true, isTip: false)
            let data = on(withExtral: dic, isSender: true, isTip: false)
            //: data?.status = .Msg_Status_Fail
            data?.status = .Msg_Status_Fail
            //: return data
            return data
        }

        //: return nil
        return nil
    }

    /// 腾讯IM接收消息组装
    //: public class func handleTXReceiveMessage(withExtral dic: [String: Any], cusData: Data, tempModel: V2TIMMessage) -> AlongCrucial? {
    public class func moreBloc(withExtral dic: [String: Any], cusData: Data, tempModel: V2TIMMessage) -> AlongCrucial? {
        //: var cellData: AlongCrucial?
        var cellData: AlongCrucial?
        //: let extraDict = dic["extra"] as? [String: Any]
        let extraDict = dic[(String(showDonKnockStr))] as? [String: Any]
        //: let msgInfoDict = extraDict?["msgInfo"] as? [String: Any]
        let msgInfoDict = extraDict?[(String(const_purchaseHealthyData))] as? [String: Any]
        //: let user = dic["user"] as? [String: Any] ?? [:]
        let user = dic[(k_mixId.replacingOccurrences(of: "crush", with: "r"))] as? [String: Any] ?? [:]

        //: guard let model = try? AbTalkingChatMsgBaseModel(dictionary: extraDict) else {
        guard let model = try? EnhanceModel(dictionary: extraDict) else {
            //: return nil
            return nil
        }
        //: var isSelf = (model.user.uid == MarginExamineer.share.loginUserMode.userID.intValue)
        var isSelf = (model.user.uid == MarginExamineer.share.loginUserMode.userID.intValue)

        // 后台代发送的自动打招呼消息
        //: var isAutoSendMsg = false
        var isAutoSendMsg = false
        //: if model.msgInfo.uid.intValue == MarginExamineer.share.loginUserMode.userID.intValue &&
        if model.msgInfo.uid.intValue == MarginExamineer.share.loginUserMode.userID.intValue &&
            //: model.msgInfo.messageType.rawValue == 3 {
            model.msgInfo.messageType.rawValue == 3
        {
            //: isSelf = true
            isSelf = true
            //: isAutoSendMsg = true
            isAutoSendMsg = true
        }

        //: if dic["rYMsgType"] as? String == "GJ:Sess:AutoGreet:TxtCustom" {
        if dic[(String(constSomethingTitle.prefix(5)) + String(userPolicyRevenueData))] as? String == (String(user_manualFrameworkName) + notiRawString.replacingOccurrences(of: "same", with: "m")) {
            //: isSelf = msgInfoDict?["changeDirection"] as? Bool ?? false
            isSelf = msgInfoDict?[(main_whipFormat.lowercased() + "geDir" + noti_flowPadUrl.replacingOccurrences(of: "square", with: "ti"))] as? Bool ?? false
        }

        //: let contentType = messageCellType(dic)
        let contentType = responseVertical(dic)

        // 系统消息
        //: if extraDict?["msgType"] as? String == "system" ||
        if extraDict?[(String(constFilterUrl.suffix(7)))] as? String == (const_removeSubmitPath.replacingOccurrences(of: "above", with: "s")) ||
            //: contentType == .TXMessageContentType_tips {
            contentType == .TXMessageContentType_tips
        {
            //: let model = AbTalkingChatMsgBaseModel()
            let model = EnhanceModel()
            //: let tipModel = try? AbTalkingChatMsgTipsModel(dictionary: extraDict)
            let tipModel = try? SumModel(dictionary: extraDict)
            //: cellData = TalkingChatTipsMsgCellData(direction: isSelf ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
            cellData = ToAdjust(direction: isSelf ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
            //: if let content = dic["content"] as? String {
            if let content = dic[(showSunString.replacingOccurrences(of: "generate", with: "nt"))] as? String {
                //: tipModel?.content = content
                tipModel?.content = content
            }
            //: if tipModel != nil {
            if tipModel != nil {
                //: model.tips = tipModel!
                model.tips = tipModel!
            }
            //: cellData?.msgModel = model
            cellData?.msgModel = model
            //: return cellData
            return cellData
        }

        // 其他消息类型的创建...
        //: switch contentType {
        switch contentType {
        //: case .TXMessageContentType_picture:
        case .TXMessageContentType_picture:
            //: cellData = TalkingChatImageMsgCellData(direction: isSelf ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
            cellData = DelayComposition(direction: isSelf ? .MsgDirectionOutgoing : .MsgDirectionIncoming)

        //: case .TXMessageContentType_video:
        case .TXMessageContentType_video:
            //: cellData = TalkingChatVideoMsgCellData(direction: isSelf ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
            cellData = MeasurePaint(direction: isSelf ? .MsgDirectionOutgoing : .MsgDirectionIncoming)

        //: case .TXMessageContentType_gift:
        case .TXMessageContentType_gift:
            //: if let groupID = tempModel.groupID, !groupID.isEmpty {
            if let groupID = tempModel.groupID, !groupID.isEmpty { // 群聊gift
                //: cellData = TalkingGroupChatGiftMsgCellData(direction: isSelf ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
                cellData = MultiLastSine(direction: isSelf ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
                //: } else {
            } else { // 私聊gift
                //: cellData = TalkingChatGiftMsgCellData(direction: isSelf ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
                cellData = ContextLength(direction: isSelf ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
            }

        //: case .TXMessageContentType_askforGift:
        case .TXMessageContentType_askforGift: // 索要礼物
            //: let tempDic = parseTXMessageData(data: cusData)
            let tempDic = tagDoing(data: cusData)
            //: if let extraData = tempDic["extra"] as? [String: Any],
            if let extraData = tempDic[(String(showDonKnockStr))] as? [String: Any],
               //: let msgInfoData = extraData["msgInfo"] as? [String: Any],
               let msgInfoData = extraData[(String(const_purchaseHealthyData))] as? [String: Any],
               //: !msgInfoData.isEmpty {
               !msgInfoData.isEmpty
            {
                //: if let info = try? AbTalkingChatMsgInfoModel(dictionary: msgInfoData) {
                if let info = try? ColumnModel(dictionary: msgInfoData) {
                    //: model.msgInfo = info
                    model.msgInfo = info
                }
            }
            //: cellData = LightSaveLabel(direction: isSelf ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
            cellData = LightSaveLabel(direction: isSelf ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
            //: cellData?.msgModel = model
            cellData?.msgModel = model

        //: case .TXMessageContentType_voice:
        case .TXMessageContentType_voice:
            //: cellData = BoxStack(direction: isSelf ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
            cellData = BoxStack(direction: isSelf ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
            //: if let audioCellData = cellData as? BoxStack {
            if let audioCellData = cellData as? BoxStack {
                //: if audioCellData.voiceModel.db_voiceCacheWrap.msgId.isEmpty {
                if audioCellData.voiceModel.db_voiceCacheWrap.msgId.isEmpty {
                    //: let mainKey = checkPrimaryKey(withMsgModel: tempModel)
                    let mainKey = run(withMsgModel: tempModel)
                    //: var cacheWrap = WCDBVoiceMsgTable.db_getVoiceMsg(with: mainKey)
                    var cacheWrap = ComponentMultiple.goldRecommend(with: mainKey)
                    //: if cacheWrap == nil {
                    if cacheWrap == nil {
                        //: cacheWrap = WCDBVoiceMsgTable.db_inserVoiceMsgToDB(packageDict(withTXMessage: tempModel))
                        cacheWrap = ComponentMultiple.liteAction(distanceContext(withTXMessage: tempModel))
                        //: audioCellData.voiceModel.db_voiceCacheWrap = cacheWrap!
                        audioCellData.voiceModel.db_voiceCacheWrap = cacheWrap!
                        //: } else {
                    } else {
                        //: audioCellData.voiceModel.db_voiceCacheWrap = cacheWrap!
                        audioCellData.voiceModel.db_voiceCacheWrap = cacheWrap!
                    }
                }
            }

        //: case .TXMessageContentType_local:
        case .TXMessageContentType_local:
            //: cellData = TalkingChatMatchMsgCellData(direction: isSelf ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
            cellData = EfficiencyDraw(direction: isSelf ? .MsgDirectionOutgoing : .MsgDirectionIncoming)

        //: default:
        default:
            //: let title = extraDict?["title"] as? String ?? ""
            let title = extraDict?[(userHavePath.replacingOccurrences(of: "security", with: "ti"))] as? String ?? ""
            //: let content = extraDict?["content"] as? String ?? ""
            let content = extraDict?[(showSunString.replacingOccurrences(of: "generate", with: "nt"))] as? String ?? ""
            //: let img = extraDict?["img"] as? String ?? ""
            let img = extraDict?[(constBeautySnapKey.replacingOccurrences(of: "law", with: "m"))] as? String ?? ""
            //: if !title.isEmpty || !content.isEmpty || !img.isEmpty {
            if !title.isEmpty || !content.isEmpty || !img.isEmpty { // 系统新消息
                //: cellData = TalkingChatSystemNotifJumpMsgCellData(direction: isSelf ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
                cellData = SearchPublish(direction: isSelf ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
                //: cellData?.extraDict = extraDict
                cellData?.extraDict = extraDict
                //: } else if let groupID = tempModel.groupID, !groupID.isEmpty {
            } else if let groupID = tempModel.groupID, !groupID.isEmpty { // 群聊text
                //: cellData = TalkingGroupChatTextMsgCellData(direction: isSelf ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
                cellData = GlobalDuring(direction: isSelf ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
                //: } else {
            } else { // 私聊text
                //: cellData = ExceptionKindLocationCreate(direction: isSelf ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
                cellData = ExceptionKindLocationCreate(direction: isSelf ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
            }
        }

        // 设置用户信息
        //: if model.user.nickname.isEmpty {
        if model.user.nickname.isEmpty {
            //: var userDict = user
            var userDict = user
            //: userDict.merge(msgInfoDict ?? [:]) { (_, new) in new }
            userDict.merge(msgInfoDict ?? [:]) { _, new in new }
            //: if let userModel = try? AbTalkingChatMsgUserModel(dictionary: userDict) {
            if let userModel = try? NextModel(dictionary: userDict) {
                //: if userModel.uid == 0 {
                if userModel.uid == 0 {
                    //: userModel.uid = userDict["id"] as? Int ?? 0
                    userModel.uid = userDict["id"] as? Int ?? 0
                }
                //: if userModel.headPic.isEmpty == true {
                if userModel.headPic.isEmpty == true {
                    //: userModel.headPic = userDict["icon"] as? String ?? ""
                    userModel.headPic = userDict[(constReadyKey.lowercased())] as? String ?? ""
                }

                //: if isAutoSendMsg {
                if isAutoSendMsg {
                    //: userModel.uid = model.msgInfo.toUid.intValue
                    userModel.uid = model.msgInfo.toUid.intValue
                    //: userModel.headPic =
                    userModel.headPic =
                        //: MarginExamineer.share.loginUserMode.headPic ?? ""
                        MarginExamineer.share.loginUserMode.headPic ?? ""
                }
                //: model.user = userModel
                model.user = userModel
            }
        }

        //: if model.msgInfo == nil {
        if model.msgInfo == nil {
            //: let msgInfoModel = AbTalkingChatMsgInfoModel()
            let msgInfoModel = ColumnModel()
            //: msgInfoModel.content = dic["content"] as? String ?? ""
            msgInfoModel.content = dic[(showSunString.replacingOccurrences(of: "generate", with: "nt"))] as? String ?? ""
            //: msgInfoModel.jumps = extraDict?["extra"] as? [Any] ?? []
            msgInfoModel.jumps = extraDict?[(String(showDonKnockStr))] as? [Any] ?? []
            //: model.msgInfo = msgInfoModel
            model.msgInfo = msgInfoModel
        }

        // 聊天室送礼消息
        //: if contentType == .TXMessageContentType_gift && tempModel.groupID != nil && !tempModel.groupID!.isEmpty {
        if contentType == .TXMessageContentType_gift && tempModel.groupID != nil && !tempModel.groupID!.isEmpty {
            //: if model.toUser.uid.isEmpty {
            if model.toUser.uid.isEmpty { // 送礼所有人
                //: let toUserInfo = AbTalkingChatMsgToUserModel()
                let toUserInfo = PropertyCharacteristicModel()
                //: toUserInfo.uid = "all"
                toUserInfo.uid = (main_statData.replacingOccurrences(of: "cover", with: "ll"))
                //: toUserInfo.nickname = model.gift.sendNum
                toUserInfo.nickname = model.gift.sendNum
                //: toUserInfo.headPic = "icon_chats_pcr"
                toUserInfo.headPic = (String(appOkId) + String(k_roleId))
                //: model.toUser = toUserInfo
                model.toUser = toUserInfo
            }
        }
        //: cellData?.msgModel = model
        cellData?.msgModel = model

        // 本地自定义字段处理
        //: let customStr = String(data: cusData, encoding: .utf8) ?? ""
        let customStr = String(data: cusData, encoding: .utf8) ?? ""
        //: let customDic = NSDictionary.dictionary(withJsonString: customStr) ?? [:]
        let customDic = NSDictionary.surroundingDown(withJsonString: customStr) ?? [:]
        //: let statusStr = customDic["status"] as? String ?? "0"
        let statusStr = customDic[(k_wireId.replacingOccurrences(of: "ret", with: "at"))] as? String ?? "0"
        //: let status = TMsgStatus(rawValue: UInt(statusStr) ?? 0) ?? .Msg_Status_Init
        let status = TMsgStatus(rawValue: UInt(statusStr) ?? 0) ?? .Msg_Status_Init
        //: cellData?.status = status
        cellData?.status = status
        //: cellData?.msgIncome = CGFloat(customDic["msgIncome"] as? Float ?? 0)
        cellData?.msgIncome = CGFloat(customDic[(String(notiWarnMsg))] as? Float ?? 0)
        //: cellData?.messageType = msgInfoDict?["messageType"] as? Int ?? 0
        cellData?.messageType = msgInfoDict?[(String(appTransformKey) + String(kDetailData.prefix(7)))] as? Int ?? 0

        //: if let msgId = customDic["msgId"] as? String {
        if let msgId = customDic[(String(notiNoticeFrontUrl.suffix(5)))] as? String {
            //: cellData?.msgModel.msgInfo.msgId = msgId
            cellData?.msgModel.msgInfo.msgId = msgId
        }

        // 文本翻译
        //: if let translateStatus = customDic["TMsgTranslateStatus"] as? Int {
        if let translateStatus = customDic[(String(appLeaderShrimpPath.prefix(4)) + "Transl" + String(appWeValue.prefix(5)) + "atus")] as? Int {
            //: cellData?.translateStatus = TMsgTranslateStatus(rawValue: UInt(translateStatus)) ?? .MsgTranslate_init
            cellData?.translateStatus = TrackActiveChannel(rawValue: UInt(translateStatus)) ?? .MsgTranslate_init
        }
        //: if let translatedContent = customDic["TMsgTranslatedContent"] as? String {
        if let translatedContent = customDic[(String(dataYieldPresentationKey) + kSampleStr.replacingOccurrences(of: "nor", with: "t"))] as? String {
            //: cellData?.translatedContent = translatedContent
            cellData?.translatedContent = translatedContent
        }

        // 私密照片Cache
        //: if contentType == .TXMessageContentType_picture {
        if contentType == .TXMessageContentType_picture {
            //: let dict = AbTalkingChatMsgIntimatePhotoModel.loadLocalIntimatePhotoDict()
            let dict = EaseModel.force()
            //: if let photoData = dict[tempModel.msgID as Any] as? String {
            if let photoData = dict[tempModel.msgID as Any] as? String {
                //: cellData?.msgModel.msgInfo.intimatePhoto = AbTalkingChatMsgIntimatePhotoModel(string: photoData, error: nil)
                cellData?.msgModel.msgInfo.intimatePhoto = EaseModel(string: photoData, error: nil)
            }
        }

        // 私密视频Cache
        //: if contentType == .TXMessageContentType_video {
        if contentType == .TXMessageContentType_video {
            //: let dict = AbTalkingChatMsgVideoModel.loadLocalIntimateVideoDict()
            let dict = PermanentModel.composition()
            //: if let videoData = dict[tempModel.msgID as Any] as? String {
            if let videoData = dict[tempModel.msgID as Any] as? String {
                //: cellData?.msgModel.msgInfo.video = AbTalkingChatMsgVideoModel(string: videoData, error: nil)
                cellData?.msgModel.msgInfo.video = PermanentModel(string: videoData, error: nil)
            }
        }

        // 索要礼物Cache
        //: if contentType == .TXMessageContentType_askforGift {
        if contentType == .TXMessageContentType_askforGift {
            //: let arr = WantGiftModel.loadAskForGiftArr()
            let arr = LocalModel.found()
            //: if arr.contains(tempModel.msgID as Any) {
            if arr.contains(tempModel.msgID as Any) { // 已发送
                //: cellData?.msgModel.msgInfo.wantGift.sendStatus = 1
                cellData?.msgModel.msgInfo.wantGift.sendStatus = 1
            }
        }

        //: return cellData
        return cellData
    }

    // MARK: - 辅助方法

    /// 根据字典获取消息类型
    //: public class func messageCellType(_ extraDic: [String: Any]) -> TXMessageContentType {
    public class func responseVertical(_ extraDic: [String: Any]) -> MaximumRange {
        //: let dic = extraDic["extra"] as? [String: Any]
        let dic = extraDic[(String(showDonKnockStr))] as? [String: Any]
        //: let msgInfo = dic?["msgInfo"] as? [String: Any]
        let msgInfo = dic?[(String(const_purchaseHealthyData))] as? [String: Any]
        //: guard let model = try? AbTalkingChatMsgBaseModel(dictionary: dic) else {
        guard let model = try? EnhanceModel(dictionary: dic) else {
            //: return .TXMessageContentType_text
            return .TXMessageContentType_text
        }

        // 系统消息 官方小密
        //: if dic?["formatType"] as? String == "plain" {
        if dic?[(app_careFormat.replacingOccurrences(of: "screening", with: "m") + "atTyp" + String(user_curRankTitle))] as? String == (String(noti_mendBlindMessage)) {
            //: let imageUri = dic?["imageUri"] as? String ?? ""
            let imageUri = dic?[(String(const_methodUrl) + String(show_snapTitle))] as? String ?? ""
            //: let imageData = dic?["imageData"] as? String ?? ""
            let imageData = dic?[(String(k_intensityId.suffix(7)) + mainCrucialName.replacingOccurrences(of: "script", with: "ta"))] as? String ?? ""
            //: let isImageStyle = !imageData.isEmpty || !imageUri.isEmpty
            let isImageStyle = !imageData.isEmpty || !imageUri.isEmpty
            //: if isImageStyle {
            if isImageStyle {
                //: return .TXMessageContentType_picture
                return .TXMessageContentType_picture
            }
        }

        // 图片||私密照片
        //: if !model.msgInfo.imageUri.isEmpty || !model.msgInfo.imageData.isEmpty || !model.msgInfo.intimatePhoto.originUrl.isEmpty {
        if !model.msgInfo.imageUri.isEmpty || !model.msgInfo.imageData.isEmpty || !model.msgInfo.intimatePhoto.originUrl.isEmpty {
            //: return .TXMessageContentType_picture
            return .TXMessageContentType_picture
        }

        // 接收的视频消息 || 本人发送的视频消息
        //: if !model.msgInfo.video.videoUrl.isEmpty || model.msgInfo.contentType == "VideoMsg" {
        if !model.msgInfo.video.videoUrl.isEmpty || model.msgInfo.contentType == (String(main_deployKey.prefix(5)) + String(show_thePath.prefix(3))) {
            //: return .TXMessageContentType_video
            return .TXMessageContentType_video
        }

        // 礼物，非打招呼消息
        //: if !model.gift.name.isEmpty && model.msgInfo.messageType.rawValue != 4 {
        if !model.gift.name.isEmpty && model.msgInfo.messageType.rawValue != 4 {
            //: return .TXMessageContentType_gift
            return .TXMessageContentType_gift
        }

        //: let isVoiceStyle = checkVoiceMsg(msginfo: msgInfo ?? [:])
        let isVoiceStyle = averageCamera(msginfo: msgInfo ?? [:])
        //: if isVoiceStyle {
        if isVoiceStyle {
            //: return .TXMessageContentType_voice
            return .TXMessageContentType_voice
        }

        //: if (dic?["match"] as? Int) == 1 || (msgInfo?["match"] as? Int) == 1 {
        if (dic?[(dataTabSignificantFormat.lowercased())] as? Int) == 1 || (msgInfo?[(dataTabSignificantFormat.lowercased())] as? Int) == 1 {
            // 匹配消息
            //: return .TXMessageContentType_local
            return .TXMessageContentType_local
        }

        //: if dic?["msgType"] as? String == "tips" || (dic?["messageType"] as? Int) == 17 {
        if dic?[(String(constFilterUrl.suffix(7)))] as? String == (user_distanceStr.replacingOccurrences(of: "inmate", with: "s")) || (dic?[(String(appTransformKey) + String(kDetailData.prefix(7)))] as? Int) == 17 {
            // 提示消息,包含超时未回复
            //: return .TXMessageContentType_tips
            return .TXMessageContentType_tips
        }

        //: if (dic?["formatType"] as? String == "mf") && ((dic?["messageType"] as? Int) == 15 || (dic?["messageType"] as? Int) == 16) {
        if (dic?[(app_careFormat.replacingOccurrences(of: "screening", with: "m") + "atTyp" + String(user_curRankTitle))] as? String == "mf") && ((dic?[(String(appTransformKey) + String(kDetailData.prefix(7)))] as? Int) == 15 || (dic?[(String(appTransformKey) + String(kDetailData.prefix(7)))] as? Int) == 16) {
            // 提示消息，打招呼提示送出神秘礼物,对方设置价格消息提示
            //: return .TXMessageContentType_tips
            return .TXMessageContentType_tips
        }

        //: if dic?["msgType"] as? String == "userIntro" {
        if dic?[(String(constFilterUrl.suffix(7)))] as? String == (String(const_drawerInstallStr) + String(notiImageString)) {
            //: return .TXMessageContentType_userCard
            return .TXMessageContentType_userCard
        }

        //: if msgInfo?["msgType"] as? String == "WantGift" || (msgInfo?["messageType"] as? Int) == 25 {
        if msgInfo?[(String(constFilterUrl.suffix(7)))] as? String == (String(main_mixConnectString.prefix(7)) + show_seaNameName.replacingOccurrences(of: "three", with: "t")) || (msgInfo?[(String(appTransformKey) + String(kDetailData.prefix(7)))] as? Int) == 25 {
            //: return .TXMessageContentType_askforGift
            return .TXMessageContentType_askforGift
        }

        //: return .TXMessageContentType_text
        return .TXMessageContentType_text
    }

    // MARK: - 语音消息处理

    /// 从TX的model里提取参数
    //: public class func packageDict(withTXMessage message: V2TIMMessage) -> [String: Any] {
    public class func distanceContext(withTXMessage message: V2TIMMessage) -> [String: Any] {
        //: let extra = String(data: message.customElem.data, encoding: .utf8) ?? ""
        let extra = String(data: message.customElem.data, encoding: .utf8) ?? ""
        //: let dic = NSDictionary.dictionary(withJsonString: extra) ?? [:]
        let dic = NSDictionary.surroundingDown(withJsonString: extra) ?? [:]
        //: let extraData = dic["extra"] as? [String: Any]
        let extraData = dic[(String(showDonKnockStr))] as? [String: Any]
        //: let msgInfo = extraData?["msgInfo"] as? [String: Any]
        let msgInfo = extraData?[(String(const_purchaseHealthyData))] as? [String: Any]
        //: let user = extraData?["user"] as? [String: Any]
        let user = extraData?[(k_mixId.replacingOccurrences(of: "crush", with: "r"))] as? [String: Any]

        //: let senderId = msgInfo?["senderId"] as? String ?? ""
        let senderId = msgInfo?[(user_purpleNobodyUrl.replacingOccurrences(of: "standard", with: "en") + "erId")] as? String ?? ""
        //: var info = msgInfo
        var info = msgInfo

        // 自己发送的和接收的数据结构不一样
        //: if senderId == MarginExamineer.share.loginUserMode.userID {
        if senderId == MarginExamineer.share.loginUserMode.userID {
            //: info?["senderId"] = senderId
            info?[(user_purpleNobodyUrl.replacingOccurrences(of: "standard", with: "en") + "erId")] = senderId
            //: info?["targetId"] = message.userID ?? (user?["targetId"] as? String ?? "")
            info?[(noti_policyId.replacingOccurrences(of: "bar", with: "a") + String(main_interestedPath.prefix(6)))] = message.userID ?? (user?[(noti_policyId.replacingOccurrences(of: "bar", with: "a") + String(main_interestedPath.prefix(6)))] as? String ?? "")
            //: } else {
        } else {
            //: info?["senderId"] = "\(msgInfo?["uid"] as? Int ?? 0)"
            info?[(user_purpleNobodyUrl.replacingOccurrences(of: "standard", with: "en") + "erId")] = "\(msgInfo?[(user_systemWeekFormat.lowercased())] as? Int ?? 0)"
            //: info?["targetId"] = "\(msgInfo?["uid"] as? Int ?? 0)"
            info?[(noti_policyId.replacingOccurrences(of: "bar", with: "a") + String(main_interestedPath.prefix(6)))] = "\(msgInfo?[(user_systemWeekFormat.lowercased())] as? Int ?? 0)"
        }

        //: if msgInfo?.keys.contains("msgId") ?? false {
        if msgInfo?.keys.contains((String(notiNoticeFrontUrl.suffix(5)))) ?? false {
            //: info?["msgId"] = "\(msgInfo?["msgId"] ?? "")"
            info?[(String(notiNoticeFrontUrl.suffix(5)))] = "\(msgInfo?[(String(notiNoticeFrontUrl.suffix(5)))] ?? "")"
            //: } else {
        } else {
            //: let customStr = String(data: message.localCustomData, encoding: .utf8) ?? ""
            let customStr = String(data: message.localCustomData, encoding: .utf8) ?? ""
            //: let customDic = NSDictionary.dictionary(withJsonString: customStr) ?? [:]
            let customDic = NSDictionary.surroundingDown(withJsonString: customStr) ?? [:]
            //: if customDic["msgId"] != nil {
            if customDic[(String(notiNoticeFrontUrl.suffix(5)))] != nil {
                //: info?["msgId"] = "\(customDic["msgId"] ?? "")"
                info?[(String(notiNoticeFrontUrl.suffix(5)))] = "\(customDic[(String(notiNoticeFrontUrl.suffix(5)))] ?? "")"
                //: } else {
            } else {
                //: let txMsgId = !message.msgID.isEmpty ? message.msgID : "\(Int64(Date().timeIntervalSince1970))"
                let txMsgId = !message.msgID.isEmpty ? message.msgID : "\(Int64(Date().timeIntervalSince1970))"
                //: info?["msgId"] = txMsgId
                info?[(String(notiNoticeFrontUrl.suffix(5)))] = txMsgId
            }
        }

        //: if msgInfo?.keys.contains("audioSandbox") ?? false {
        if msgInfo?.keys.contains((String(noti_clickZoneId) + appChartFormat.lowercased())) ?? false {
            //: info?["audioSandbox"] = "\(msgInfo?["audioSandbox"] ?? "")"
            info?[(String(noti_clickZoneId) + appChartFormat.lowercased())] = "\(msgInfo?[(String(noti_clickZoneId) + appChartFormat.lowercased())] ?? "")"
        }
        //: if msgInfo?.keys.contains("audioLength") ?? false {
        if msgInfo?.keys.contains((user_poseLowString.replacingOccurrences(of: "quake", with: "u") + "oLength")) ?? false {
            //: info?["audioLength"] = "\(msgInfo?["audioLength"] ?? "")"
            info?[(user_poseLowString.replacingOccurrences(of: "quake", with: "u") + "oLength")] = "\(msgInfo?[(user_poseLowString.replacingOccurrences(of: "quake", with: "u") + "oLength")] ?? "")"
        }

        //: let formatter = DateFormatter()
        let formatter = DateFormatter()
        //: formatter.dateFormat = "yyyy-MM-dd HH:mm:ss"
        formatter.dateFormat = (String(userLastStr.suffix(6)) + "M-dd " + String(noti_indexName.prefix(8)))
        //: info?["sendTime"] = formatter.string(from: message.timestamp ?? Date())
        info?[(dataBetweenId.replacingOccurrences(of: "must", with: "e") + String(mainAlreadyName.suffix(6)))] = formatter.string(from: message.timestamp ?? Date())
        //: info?["isRead"] = senderId == MarginExamineer.share.loginUserMode.userID ? "1" : "0"
        info?[(String(notiTranslateFuturePath.prefix(5)) + noti_containerPath.lowercased())] = senderId == MarginExamineer.share.loginUserMode.userID ? "1" : "0"

        //: return info ?? [:]
        return info ?? [:]
    }

    //: public class func checkPrimaryKey(withMsgModel tempModel: V2TIMMessage) -> String {
    public class func run(withMsgModel tempModel: V2TIMMessage) -> String {
        //: let v2Info = packageDict(withTXMessage: tempModel)
        let v2Info = distanceContext(withTXMessage: tempModel)
        //: let key = "\(v2Info["msgId"] ?? "")"
        let key = "\(v2Info[(String(notiNoticeFrontUrl.suffix(5)))] ?? "")"
        //: print("key = \(key)")
        //: return key
        return key
    }

    // MARK: - JSON处理方法

    //: @objc public class func parseTXMessageData(data: Data) -> [String: Any] {
    @objc public class func tagDoing(data: Data) -> [String: Any] {
        //: do {
        do {
            //: let dict = try JSONSerialization.jsonObject(
            let dict = try JSONSerialization.jsonObject(
                //: with: data, options: .mutableContainers)
                with: data, options: .mutableContainers
            )
            //: return dict as? [String: Any] ?? [:]
            return dict as? [String: Any] ?? [:]
            //: } catch {
        } catch {
            //: return [:]
            return [:]
        }
    }

    //: @objc public class func parseTXMessageJsonStr(data: [String: Any]) -> String {
    @objc public class func takeOfTiming(data: [String: Any]) -> String {
        //: do {
        do {
            //: let jsonData = try JSONSerialization.data(
            let jsonData = try JSONSerialization.data(
                //: withJSONObject: data, options: [])
                withJSONObject: data, options: []
            )
            //: return String(data: jsonData, encoding: .utf8) ?? ""
            return String(data: jsonData, encoding: .utf8) ?? ""
            //: } catch {
        } catch {
            //: return ""
            return ""
        }
    }

    // MARK: - 语音消息检测

    //: @objc public class func checkVoiceMsg(msginfo: [String: Any]) -> Bool {
    @objc public class func averageCamera(msginfo: [String: Any]) -> Bool {
        //: if let msgType = msginfo["msgType"] as? String, msgType == "audio" {
        if let msgType = msginfo[(String(constFilterUrl.suffix(7)))] as? String, msgType == (String(constWeekId)) {
            //: return true
            return true
        }
        //: if let contentType = msginfo["contentType"] as? String,
        if let contentType = msginfo[(k_streakPath.lowercased() + "entTy" + String(noti_calculateStr))] as? String,
           //: contentType == "AudioMsg"
           contentType == (String(showMentionSmallString.prefix(3)) + String(const_consumeMessage.prefix(5)))
        {
            //: return true
            return true
        }
        //: if let audioData = msginfo["audioData"] as? String, !audioData.isEmpty {
        if let audioData = msginfo[(appDeliveryIncomeName.replacingOccurrences(of: "eh", with: "d") + String(noti_sceneUrl.prefix(5)))] as? String, !audioData.isEmpty {
            //: return true
            return true
        }
        //: if let audioUri = msginfo["audioUri"] as? String, !audioUri.isEmpty {
        if let audioUri = msginfo[(String(constLackValue) + String(mainMemoryTitle))] as? String, !audioUri.isEmpty {
            //: return true
            return true
        }
        //: return false
        return false
    }
}
